var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp.call(b2, prop))
      __defNormalProp(a2, prop, b2[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b2)) {
      if (__propIsEnum.call(b2, prop))
        __defNormalProp(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps = (a2, b2) => __defProps(a2, __getOwnPropDescs(b2));
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e2) {
        reject(e2);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e2) {
        reject(e2);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
/*!
 * ApexCharts v6.9.0
 * (c) 2018-2026 ApexCharts
 */
class Environment {
  /**
   * Check if running in server-side rendering environment (Node.js)
   * @returns {boolean} True if in SSR/Node.js, false if in browser
   */
  static isSSR() {
    return typeof window === "undefined" || typeof document === "undefined";
  }
  /**
   * Check if running in browser environment
   * @returns {boolean} True if in browser, false if in SSR/Node.js
   */
  static isBrowser() {
    return !this.isSSR();
  }
  /**
   * Check if a specific browser API is available
   * @param {string} api - Name of the API to check (e.g., 'ResizeObserver')
   * @returns {boolean} True if API is available
   */
  static hasAPI(api) {
    if (this.isSSR()) return false;
    return typeof /** @type {any} */
    window[api] !== "undefined";
  }
  /**
   * Returns the global Apex config object regardless of environment.
   * In browser: window.Apex; in SSR/Node.js: global.Apex; fallback: {}.
   * @returns {any}
   */
  static getApex() {
    if (typeof window !== "undefined" && window.Apex) return window.Apex;
    if (typeof global !== "undefined" && global.Apex) return global.Apex;
    return {};
  }
}
class SSRElement {
  /**
   * @param {string} nodeName
   * @param {any} namespaceURI
   */
  constructor(nodeName, namespaceURI = null) {
    this.nodeName = nodeName;
    this.namespaceURI = namespaceURI;
    this.attributes = /* @__PURE__ */ new Map();
    this.children = [];
    this.textContent = "";
    this.style = {};
    this.classList = new SSRClassList();
    this.parentNode = /** @type {SSRElement | null} */
    null;
    this._ssrWidth = void 0;
    this._ssrHeight = void 0;
    this._ssrMode = void 0;
  }
  /**
   * @param {string} name
   * @param {any} value
   */
  setAttribute(name2, value) {
    this.attributes.set(name2, value);
  }
  /**
   * @param {string} name
   */
  getAttribute(name2) {
    return this.attributes.get(name2);
  }
  /**
   * @param {string} name
   */
  removeAttribute(name2) {
    this.attributes.delete(name2);
  }
  /**
   * @param {string} name
   */
  hasAttribute(name2) {
    return this.attributes.has(name2);
  }
  /**
   * @param {any} child
   */
  appendChild(child) {
    if (child && child !== this) {
      if (child.parentNode && child.parentNode !== this) {
        child.parentNode.removeChild(child);
      } else if (child.parentNode === this) {
        const index = this.children.indexOf(child);
        if (index !== -1) this.children.splice(index, 1);
      }
      child.parentNode = this;
      this.children.push(child);
    }
    return child;
  }
  /**
   * @param {any} child
   */
  removeChild(child) {
    const index = this.children.indexOf(child);
    if (index !== -1) {
      this.children.splice(index, 1);
      child.parentNode = null;
    }
    return child;
  }
  /**
   * @param {any} newNode
   * @param {any} referenceNode
   */
  insertBefore(newNode, referenceNode) {
    if (!referenceNode) {
      return this.appendChild(newNode);
    }
    if (newNode.parentNode && newNode.parentNode !== this) {
      newNode.parentNode.removeChild(newNode);
    } else if (newNode.parentNode === this) {
      const existingIndex = this.children.indexOf(newNode);
      if (existingIndex !== -1) this.children.splice(existingIndex, 1);
    }
    const index = this.children.indexOf(referenceNode);
    if (index !== -1) {
      newNode.parentNode = this;
      this.children.splice(index, 0, newNode);
    }
    return newNode;
  }
  cloneNode(deep = false) {
    const clone = new SSRElement(this.nodeName, this.namespaceURI);
    clone.textContent = this.textContent;
    this.attributes.forEach((value, key) => {
      clone.attributes.set(key, value);
    });
    Object.assign(clone.style, this.style);
    if (deep) {
      this.children.forEach((child) => {
        if (child.cloneNode) {
          clone.appendChild(child.cloneNode(true));
        }
      });
    }
    return clone;
  }
  getBoundingClientRect() {
    return {
      width: this._ssrWidth || 0,
      height: this._ssrHeight || 0,
      top: 0,
      left: 0,
      right: this._ssrWidth || 0,
      bottom: this._ssrHeight || 0,
      x: 0,
      y: 0
    };
  }
  getRootNode() {
    let root = this;
    while (root.parentNode) {
      root = root.parentNode;
    }
    return root;
  }
  querySelector() {
    return null;
  }
  querySelectorAll() {
    return [];
  }
  getElementsByClassName() {
    return [];
  }
  addEventListener() {
  }
  removeEventListener() {
  }
  get childNodes() {
    return this.children;
  }
  toString() {
    let attrs = "";
    this.attributes.forEach((value, key) => {
      attrs += ` ${key}="${value}"`;
    });
    if (this.children.length === 0 && !this.textContent) {
      return `<${this.nodeName}${attrs}/>`;
    }
    const childrenStr = this.children.map((c2) => c2.toString()).join("");
    return `<${this.nodeName}${attrs}>${this.textContent}${childrenStr}</${this.nodeName}>`;
  }
  // Property getters/setters
  get innerHTML() {
    return this.children.map((c2) => c2.toString()).join("");
  }
  set innerHTML(value) {
    this.children = [];
    this.textContent = value;
  }
  get outerHTML() {
    return this.toString();
  }
  get isConnected() {
    return true;
  }
}
class SSRClassList {
  constructor() {
    this.classes = /* @__PURE__ */ new Set();
  }
  add(...classNames) {
    classNames.forEach((name2) => this.classes.add(name2));
  }
  remove(...classNames) {
    classNames.forEach((name2) => this.classes.delete(name2));
  }
  /**
   * @param {string} className
   */
  contains(className) {
    return this.classes.has(className);
  }
  /**
   * @param {string} className
   * @param {any} force
   */
  toggle(className, force) {
    if (force === true) {
      this.classes.add(className);
      return true;
    } else if (force === false) {
      this.classes.delete(className);
      return false;
    } else {
      if (this.classes.has(className)) {
        this.classes.delete(className);
        return false;
      } else {
        this.classes.add(className);
        return true;
      }
    }
  }
  toString() {
    return Array.from(this.classes).join(" ");
  }
}
class SSRDOMShim {
  constructor() {
    this.SVGNS = "http://www.w3.org/2000/svg";
    this.XLINKNS = "http://www.w3.org/1999/xlink";
  }
  /**
   * Create SVG element with namespace
   * @param {string} namespaceURI - Namespace URI
   * @param {string} qualifiedName - Element tag name
   * @returns {SSRElement} Mock SVG element
   */
  createElementNS(namespaceURI, qualifiedName) {
    return new SSRElement(qualifiedName, namespaceURI);
  }
  /**
   * Create text node
   * @param {string} data - Text content
   * @returns {object} Text node mock
   */
  createTextNode(data) {
    const node = {
      nodeName: "#text",
      nodeType: 3,
      textContent: data,
      toString() {
        return node.textContent;
      }
    };
    return node;
  }
  /**
   * Query selector (returns null in SSR)
   * @returns {null}
   */
  querySelector() {
    return null;
  }
  /**
   * Query selector all (returns empty array in SSR)
   * @returns {any[]}
   */
  querySelectorAll() {
    return [];
  }
  /**
   * Get computed style (returns empty object in SSR)
   * @returns {object}
   */
  getComputedStyle() {
    return {};
  }
  /**
   * Get bounding client rect for element
   * @param {SSRElement} element - Element to measure
   * @returns {object} Mock dimensions
   */
  getBoundingClientRect(element) {
    if (element && element.getBoundingClientRect) {
      return element.getBoundingClientRect();
    }
    return {
      width: 0,
      height: 0,
      top: 0,
      left: 0,
      right: 0,
      bottom: 0,
      x: 0,
      y: 0
    };
  }
  /**
   * Create mock XMLSerializer for SSR
   * @returns {object} XMLSerializer mock
   */
  createXMLSerializer() {
    return {
      /**
       * @param {Element} element
       */
      serializeToString(element) {
        return element.toString ? element.toString() : "";
      }
    };
  }
  /**
   * Create mock DOMParser for SSR
   * @returns {object} DOMParser mock
   */
  createDOMParser() {
    return {
      /**
       * @param {string} str
       * @param {string} _type
       */
      parseFromString(str, _type) {
        const root = new SSRElement("root");
        root.innerHTML = str;
        return {
          documentElement: root
        };
      }
    };
  }
}
let shim = null;
let xmlSerializerInstance = null;
let domParserInstance = null;
class BrowserAPIs {
  /**
   * Initialize the SSR shim if in SSR environment
   * Must be called before using other methods
   */
  static init() {
    if (Environment.isSSR() && !shim) {
      shim = new SSRDOMShim();
    }
  }
  /**
   * Create an HTML element
   * @param {string} tagName - Element tag name
   * @returns {HTMLElement} HTML element
   */
  static createElement(tagName) {
    if (Environment.isSSR()) {
      if (!shim) this.init();
      return shim.createElementNS(null, tagName);
    }
    return document.createElement(tagName);
  }
  /**
   * Create an SVG element with namespace
   * @param {string} namespaceURI - Namespace URI
   * @param {string} qualifiedName - Element tag name
   * @returns {HTMLElement} created element
   */
  static createElementNS(namespaceURI, qualifiedName) {
    if (Environment.isSSR()) {
      if (!shim) this.init();
      return shim.createElementNS(namespaceURI, qualifiedName);
    }
    return (
      /** @type {HTMLElement} */
      document.createElementNS(namespaceURI, qualifiedName)
    );
  }
  /**
   * Create a text node
   * @param {string} data - Text content
   * @returns {Text|object} Text node
   */
  static createTextNode(data) {
    if (Environment.isSSR()) {
      if (!shim) this.init();
      return shim.createTextNode(data);
    }
    return document.createTextNode(data);
  }
  /**
   * Query selector
   * @param {string} selector - CSS selector
   * @returns {Element|null}
   */
  static querySelector(selector) {
    if (Environment.isSSR()) {
      return null;
    }
    return document.querySelector(selector);
  }
  /**
   * Query selector all
   * @param {string} selector - CSS selector
   * @returns {NodeList|any[]}
   */
  static querySelectorAll(selector) {
    if (Environment.isSSR()) {
      return [];
    }
    return document.querySelectorAll(selector);
  }
  /**
   * Get computed style for an element
   * @param {Element} element - Element to get styles for
   * @returns {CSSStyleDeclaration|object}
   */
  static getComputedStyle(element) {
    if (Environment.isSSR()) {
      return {};
    }
    return window.getComputedStyle(element);
  }
  /**
   * Evaluate a media query. Returns the live MediaQueryList in a browser (so
   * callers can attach a `change` listener) or null under SSR / when
   * unsupported. Facet (#13) uses this for `theme.follow: 'os'`.
   * @param {string} query
   * @returns {MediaQueryList|null}
   */
  static matchMedia(query) {
    if (Environment.isSSR() || typeof window.matchMedia !== "function") {
      return null;
    }
    try {
      return window.matchMedia(query);
    } catch (e2) {
      return null;
    }
  }
  /**
   * Get bounding client rect for an element
   * @param {Element} element - Element to measure
   * @returns {DOMRect|object}
   */
  static getBoundingClientRect(element) {
    if (Environment.isSSR()) {
      if (!shim) this.init();
      return shim.getBoundingClientRect(element);
    }
    return element ? element.getBoundingClientRect() : {
      width: 0,
      height: 0,
      top: 0,
      left: 0,
      right: 0,
      bottom: 0,
      x: 0,
      y: 0
    };
  }
  /**
   * Get XMLSerializer instance
   * @returns {XMLSerializer|object}
   */
  static getXMLSerializer() {
    if (Environment.isSSR()) {
      if (!shim) this.init();
      if (!xmlSerializerInstance) {
        xmlSerializerInstance = shim.createXMLSerializer();
      }
      return xmlSerializerInstance;
    }
    if (!xmlSerializerInstance) {
      xmlSerializerInstance = new XMLSerializer();
    }
    return xmlSerializerInstance;
  }
  /**
   * Get DOMParser instance
   * @returns {DOMParser|object}
   */
  static getDOMParser() {
    if (Environment.isSSR()) {
      if (!shim) this.init();
      if (!domParserInstance) {
        domParserInstance = shim.createDOMParser();
      }
      return domParserInstance;
    }
    if (!domParserInstance) {
      domParserInstance = new DOMParser();
    }
    return domParserInstance;
  }
  /**
   * Add event listener to window
   * @param {string} event - Event name
   * @param {EventListenerOrEventListenerObject} handler - Event handler
   * @param {object} options - Event options
   */
  static addWindowEventListener(event, handler, options2) {
    if (Environment.isBrowser()) {
      window.addEventListener(event, handler, options2);
    }
  }
  /**
   * Remove event listener from window
   * @param {string} event - Event name
   * @param {EventListenerOrEventListenerObject} handler - Event handler
   * @param {object} options - Event options
   */
  static removeWindowEventListener(event, handler, options2) {
    if (Environment.isBrowser()) {
      window.removeEventListener(event, handler, options2);
    }
  }
  /**
   * Request animation frame
   * @param {FrameRequestCallback} callback - Callback function
   * @returns {number|null}
   */
  static requestAnimationFrame(callback) {
    if (Environment.isBrowser()) {
      return window.requestAnimationFrame(callback);
    }
    callback(0);
    return null;
  }
  /**
   * Cancel animation frame
   * @param {number} id - Animation frame ID
   */
  static cancelAnimationFrame(id) {
    if (Environment.isBrowser() && id) {
      window.cancelAnimationFrame(id);
    }
  }
  /**
   * Check if element exists
   * @param {Element} element - Element to check
   * @returns {boolean}
   */
  static elementExists(element) {
    if (!element) return false;
    if (Environment.isSSR()) {
      return element._ssrMode === true || element.nodeName !== void 0;
    }
    return element.getRootNode ? element.getRootNode({ composed: true }) === document || element.isConnected : false;
  }
  /**
   * Get window object (or null in SSR)
   * @returns {Window|null}
   */
  static getWindow() {
    return Environment.isBrowser() ? window : null;
  }
  /**
   * Get document object (or null in SSR)
   * @returns {Document|null}
   */
  static getDocument() {
    return Environment.isBrowser() ? document : null;
  }
  /**
   * Get the shim instance (for testing purposes)
   * @returns {SSRDOMShim|null}
   */
  static _getShim() {
    return shim;
  }
  /**
   * Reset the shim instance (for testing purposes)
   */
  static _resetShim() {
    shim = null;
    xmlSerializerInstance = null;
    domParserInstance = null;
  }
}
let Utils$1 = class Utils {
  /**
   * @param {*} item
   */
  static isObject(item) {
    return item && typeof item === "object" && !Array.isArray(item);
  }
  // Type checking that works across different window objects
  /**
   * @param {string} type
   * @param {string} val
   */
  static is(type, val) {
    return Object.prototype.toString.call(val) === "[object " + type + "]";
  }
  static isSafari() {
    return Environment.isBrowser() && /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
  }
  // to extend defaults with user options
  // credit: http://stackoverflow.com/questions/27936772/deep-object-merging-in-es6-es7#answer-34749873
  /**
   * @param {any} target
   * @param {any} source
   */
  static extend(target, source) {
    if (!this.isObject(target) && this.isObject(source)) {
      return this.clone(source);
    }
    const output = Object.assign({}, target);
    if (this.isObject(target) && this.isObject(source)) {
      Object.keys(source).forEach((key) => {
        if (this.isObject(source[key])) {
          if (!(key in target)) {
            Object.assign(output, {
              [key]: source[key]
            });
          } else {
            output[key] = this.extend(target[key], source[key]);
          }
        } else {
          Object.assign(output, {
            [key]: source[key]
          });
        }
      });
    }
    return output;
  }
  /**
   * @param {any[]} arrToExtend
   * @param {any} resultArr
   */
  static extendArray(arrToExtend, resultArr) {
    const extendedArr = [];
    arrToExtend.map((item) => {
      extendedArr.push(Utils.extend(resultArr, item));
    });
    arrToExtend = extendedArr;
    return arrToExtend;
  }
  // If month counter exceeds 12, it starts again from 1
  /**
   * @param {number} month
   */
  static monthMod(month) {
    return month % 12;
  }
  /**
   * clone object with optional shallow copy for performance
   * @param {*} source - Source object to clone
   * @param {WeakMap<any, any>} visited - Circular reference tracker
   * @param {boolean} shallow - If true, performs shallow copy (default: false)
   * @returns {*} Cloned object
   */
  static clone(source, visited = /* @__PURE__ */ new WeakMap(), shallow = false) {
    if (source === null || typeof source !== "object") {
      return source;
    }
    if (visited.has(source)) {
      return visited.get(source);
    }
    let cloneResult;
    if (Array.isArray(source)) {
      if (shallow) {
        cloneResult = source.slice();
      } else {
        cloneResult = [];
        visited.set(source, cloneResult);
        for (let i2 = 0; i2 < source.length; i2++) {
          cloneResult[i2] = this.clone(source[i2], visited, false);
        }
      }
    } else if (source instanceof Date) {
      cloneResult = new Date(source.getTime());
    } else {
      if (shallow) {
        cloneResult = Object.assign({}, source);
      } else {
        cloneResult = {};
        visited.set(source, cloneResult);
        for (const prop in source) {
          if (Object.prototype.hasOwnProperty.call(source, prop)) {
            cloneResult[prop] = this.clone(
              /** @type {Record<string,any>} */
              source[prop],
              visited,
              false
            );
          }
        }
      }
    }
    return cloneResult;
  }
  /**
   * Shallow clone for performance when deep clone isn't needed
   * @param {*} source - Source to clone
   * @returns {*} Shallow cloned object
   */
  static shallowClone(source) {
    if (source === null || typeof source !== "object") {
      return source;
    }
    if (Array.isArray(source)) {
      return source.slice();
    }
    return Object.assign({}, source);
  }
  /**
   * Fast shallow equality check for objects
   * @param {Object} obj1 - First object
   * @param {Object} obj2 - Second object
   * @returns {boolean} True if shallowly equal
   */
  static shallowEqual(obj1, obj2) {
    if (obj1 === obj2) return true;
    if (!obj1 || !obj2) return false;
    if (typeof obj1 !== "object" || typeof obj2 !== "object") {
      return obj1 === obj2;
    }
    const keys1 = Object.keys(obj1);
    const keys2 = Object.keys(obj2);
    if (keys1.length !== keys2.length) return false;
    for (const key of keys1) {
      if (
        /** @type {Record<string,any>} */
        obj1[key] !== /** @type {Record<string,any>} */
        obj2[key]
      )
        return false;
    }
    return true;
  }
  /**
   * @param {number} x
   */
  static log10(x) {
    return Math.log(x) / Math.LN10;
  }
  /**
   * @param {number} x
   */
  static roundToBase10(x) {
    return Math.pow(10, Math.floor(Math.log10(x)));
  }
  /**
   * @param {number} x
   * @param {number} base
   */
  static roundToBase(x, base) {
    return Math.pow(base, Math.floor(Math.log(x) / Math.log(base)));
  }
  /**
   * @param {any} val
   */
  static parseNumber(val) {
    if (typeof val === "number" || val === null) return val;
    return parseFloat(val);
  }
  /**
   * @param {number} num
   */
  static stripNumber(num, precision = 2) {
    return Number.isInteger(num) ? num : parseFloat(num.toPrecision(precision));
  }
  static randomId() {
    return (Math.random() + 1).toString(36).substring(4);
  }
  /**
   * @param {number} num
   */
  static noExponents(num) {
    if (num.toString().includes("e")) {
      return Math.round(num);
    }
    return num;
  }
  /**
   * @param {any} element
   */
  static elementExists(element) {
    if (!element || !element.isConnected) {
      return false;
    }
    return true;
  }
  /**
   * @param {any} el
   */
  static getDimensions(el) {
    if (!el) return [0, 0];
    if (Environment.isSSR()) {
      return [el._ssrWidth || 400, el._ssrHeight || 300];
    }
    let computedStyle;
    try {
      computedStyle = getComputedStyle(el, null);
    } catch (e2) {
      return [el.clientWidth || 0, el.clientHeight || 0];
    }
    let elementWidth = el.clientWidth;
    let elementHeight = el.clientHeight;
    if (!elementWidth || !elementHeight) {
      const rect = el.getBoundingClientRect();
      elementWidth = elementWidth || rect.width;
      elementHeight = elementHeight || rect.height;
    }
    elementHeight -= parseFloat(computedStyle.paddingTop) + parseFloat(computedStyle.paddingBottom);
    elementWidth -= parseFloat(computedStyle.paddingLeft) + parseFloat(computedStyle.paddingRight);
    return [elementWidth, elementHeight];
  }
  /**
   * @returns {any}
   * @param {any} element
   */
  static getBoundingClientRect(element) {
    if (!element) {
      return {
        top: 0,
        right: 0,
        bottom: 0,
        left: 0,
        width: 0,
        height: 0,
        x: 0,
        y: 0
      };
    }
    if (Environment.isSSR()) {
      return BrowserAPIs.getBoundingClientRect(element);
    }
    const rect = element.getBoundingClientRect();
    return {
      top: rect.top,
      right: rect.right,
      bottom: rect.bottom,
      left: rect.left,
      width: element.clientWidth,
      height: element.clientHeight,
      x: rect.left,
      y: rect.top
    };
  }
  /**
   * @param {any[]} arr
   */
  static getLargestStringFromArr(arr) {
    return arr.reduce((a2, b2) => {
      if (Array.isArray(b2)) {
        b2 = b2.reduce((aa, bb) => aa.length > bb.length ? aa : bb);
      }
      return a2.length > b2.length ? a2 : b2;
    }, 0);
  }
  // http://stackoverflow.com/questions/5623838/rgb-to-hex-and-hex-to-rgb#answer-12342275
  static hexToRgba(hex = "#999999", opacity = 0.6) {
    if (hex.substring(0, 1) !== "#") {
      hex = "#999999";
    }
    const hexStr = hex.replace("#", "");
    const h2 = hexStr.match(new RegExp("(.{" + hexStr.length / 3 + "})", "g")) || [];
    for (let i2 = 0; i2 < h2.length; i2++) {
      h2[i2] = parseInt(h2[i2].length === 1 ? h2[i2] + h2[i2] : h2[i2], 16);
    }
    if (typeof opacity !== "undefined") h2.push(opacity);
    return "rgba(" + h2.join(",") + ")";
  }
  /**
   * @param {string} rgba
   */
  static getOpacityFromRGBA(rgba) {
    return parseFloat(rgba.replace(/^.*,(.+)\)/, "$1"));
  }
  /**
   * Parse a #RGB or #RRGGBB hex colour string into [r,g,b] in 0–255.
   * Returns null for invalid input. Used by getContrastRatio.
   * @param {string} hex
   * @returns {[number, number, number] | null}
   */
  static parseHex(hex) {
    if (typeof hex !== "string") return null;
    let h2 = hex.trim().replace("#", "");
    if (h2.length === 3) {
      h2 = h2.split("").map((c2) => c2 + c2).join("");
    }
    if (!/^[0-9a-fA-F]{6}$/.test(h2)) return null;
    return [
      parseInt(h2.slice(0, 2), 16),
      parseInt(h2.slice(2, 4), 16),
      parseInt(h2.slice(4, 6), 16)
    ];
  }
  /**
   * Relative luminance per WCAG 2.x (https://www.w3.org/TR/WCAG22/#dfn-relative-luminance).
   * @param {[number, number, number]} rgb 0–255 sRGB triplet
   * @returns {number} 0.0–1.0
   */
  static relativeLuminance([r2, g2, b2]) {
    const channel = (c2) => {
      const v2 = c2 / 255;
      return v2 <= 0.03928 ? v2 / 12.92 : Math.pow((v2 + 0.055) / 1.055, 2.4);
    };
    return 0.2126 * channel(r2) + 0.7152 * channel(g2) + 0.0722 * channel(b2);
  }
  /**
   * WCAG contrast ratio between two hex colours. Returns 0 for invalid input.
   * Range: 1 (identical) … 21 (#000 vs #fff).
   * WCAG AA requires ≥ 4.5 for normal text and ≥ 3.0 for large text / UI components.
   * @param {string} hex1
   * @param {string} hex2
   * @returns {number}
   */
  static getContrastRatio(hex1, hex2) {
    const rgb1 = Utils.parseHex(hex1);
    const rgb2 = Utils.parseHex(hex2);
    if (!rgb1 || !rgb2) return 0;
    const l1 = Utils.relativeLuminance(rgb1);
    const l2 = Utils.relativeLuminance(rgb2);
    const lighter = Math.max(l1, l2);
    const darker = Math.min(l1, l2);
    return (lighter + 0.05) / (darker + 0.05);
  }
  /**
   * @param {any} rgb
   */
  static rgb2hex(rgb) {
    rgb = rgb.match(
      /^rgba?[\s+]?\([\s+]?(\d+)[\s+]?,[\s+]?(\d+)[\s+]?,[\s+]?(\d+)[\s+]?/i
    );
    return rgb && rgb.length === 4 ? "#" + ("0" + parseInt(rgb[1], 10).toString(16)).slice(-2) + ("0" + parseInt(rgb[2], 10).toString(16)).slice(-2) + ("0" + parseInt(rgb[3], 10).toString(16)).slice(-2) : "";
  }
  /**
   * @param {number} percent
   * @param {string} color
   */
  shadeRGBColor(percent, color) {
    const f = color.split(","), t2 = percent < 0 ? 0 : 255, p2 = percent < 0 ? percent * -1 : percent, R = parseInt(f[0].slice(4), 10), G2 = parseInt(f[1], 10), B = parseInt(f[2], 10);
    return "rgb(" + (Math.round((t2 - R) * p2) + R) + "," + (Math.round((t2 - G2) * p2) + G2) + "," + (Math.round((t2 - B) * p2) + B) + ")";
  }
  /**
   * @param {number} percent
   * @param {string} color
   */
  shadeHexColor(percent, color) {
    const f = parseInt(color.slice(1), 16), t2 = percent < 0 ? 0 : 255, p2 = percent < 0 ? percent * -1 : percent, R = f >> 16, G2 = f >> 8 & 255, B = f & 255;
    return "#" + (16777216 + (Math.round((t2 - R) * p2) + R) * 65536 + (Math.round((t2 - G2) * p2) + G2) * 256 + (Math.round((t2 - B) * p2) + B)).toString(16).slice(1);
  }
  // beautiful color shading blending code
  // http://stackoverflow.com/questions/5560248/programmatically-lighten-or-darken-a-hex-color-or-rgb-and-blend-colors
  /**
   * @param {number} p
   * @param {string} color
   */
  shadeColor(p2, color) {
    if (Utils.isColorHex(color)) {
      return this.shadeHexColor(p2, color);
    } else {
      return this.shadeRGBColor(p2, color);
    }
  }
  /**
   * @param {string} color
   */
  static isColorHex(color) {
    return /(^#[0-9A-F]{6}$)|(^#[0-9A-F]{3}$)|(^#[0-9A-F]{8}$)/i.test(color);
  }
  /**
   * @param {string} color
   */
  static isCSSVariable(color) {
    if (typeof color !== "string") return false;
    const value = color.trim();
    return value.startsWith("var(") && value.endsWith(")");
  }
  /**
   * @param {string} color
   */
  static getThemeColor(color) {
    if (!Utils.isCSSVariable(color)) return color;
    if (Environment.isSSR()) return color;
    const tempElem = document.createElement("div");
    tempElem.style.cssText = "position:fixed; left: -9999px; visibility:hidden;";
    tempElem.style.color = color;
    document.body.appendChild(tempElem);
    let computedColor;
    try {
      computedColor = window.getComputedStyle(tempElem).color;
    } finally {
      if (tempElem.parentNode) {
        tempElem.parentNode.removeChild(tempElem);
      }
    }
    return computedColor;
  }
  /**
   * @param {string} color
   * @param {number} opacity
   */
  static applyOpacityToColor(color, opacity) {
    const value = Number(opacity);
    if (!Number.isFinite(value)) return color;
    if (value <= 0) return "transparent";
    if (value >= 1) return color;
    const percent = Math.round(value * 100);
    return `color-mix(in srgb, ${color} ${percent}%, transparent)`;
  }
  /**
   * @param {number} size
   * @param {number} dataPointsLen
   */
  static getPolygonPos(size, dataPointsLen) {
    const dotsArray = [];
    const angle = Math.PI * 2 / dataPointsLen;
    for (let i2 = 0; i2 < dataPointsLen; i2++) {
      const curPos = {};
      curPos.x = size * Math.sin(i2 * angle);
      curPos.y = -size * Math.cos(i2 * angle);
      dotsArray.push(curPos);
    }
    return dotsArray;
  }
  /**
   * @param {number} centerX
   * @param {number} centerY
   * @param {number} radius
   * @param {number} angleInDegrees
   */
  static polarToCartesian(centerX, centerY, radius, angleInDegrees) {
    const angleInRadians = (angleInDegrees - 90) * Math.PI / 180;
    return {
      x: centerX + radius * Math.cos(angleInRadians),
      y: centerY + radius * Math.sin(angleInRadians)
    };
  }
  /**
   * @param {string} str
   */
  static escapeString(str, escapeWith = "x") {
    let newStr = str.toString().slice();
    newStr = newStr.replace(/[` ~!@#$%^&*()|+=?;:'",.<>{}[\]\\/]/gi, escapeWith);
    return newStr;
  }
  /**
   * @param {number} val
   */
  static negToZero(val) {
    return val < 0 ? 0 : val;
  }
  /**
   * @param {any[]} arr
   * @param {number} old_index
   * @param {number} new_index
   */
  static moveIndexInArray(arr, old_index, new_index) {
    if (new_index >= arr.length) {
      let k = new_index - arr.length + 1;
      while (k--) {
        arr.push(void 0);
      }
    }
    arr.splice(new_index, 0, arr.splice(old_index, 1)[0]);
    return arr;
  }
  /**
   * @param {string} s
   */
  static extractNumber(s2) {
    return parseFloat(s2.replace(/[^\d.]*/g, ""));
  }
  /**
   * @param {any} el
   * @param {string} cls
   */
  static findAncestor(el, cls) {
    while ((el = el.parentElement) && !el.classList.contains(cls)) ;
    return el;
  }
  /**
   * @param {any} el
   * @param {Record<string, any>} styles
   */
  static setELstyles(el, styles) {
    for (const key in styles) {
      if (Object.prototype.hasOwnProperty.call(styles, key)) {
        el.style[key] = styles[key];
      }
    }
  }
  // prevents JS prevision errors when adding
  /**
   * @param {number} a
   * @param {number} b
   */
  static preciseAddition(a2, b2) {
    const aDecimals = (String(a2).split(".")[1] || "").length;
    const bDecimals = (String(b2).split(".")[1] || "").length;
    const factor = Math.pow(10, Math.max(aDecimals, bDecimals));
    return (Math.round(a2 * factor) + Math.round(b2 * factor)) / factor;
  }
  /**
   * @param {any} value
   */
  static isNumber(value) {
    return !isNaN(value) && parseFloat(String(Number(value))) === value && !isNaN(parseInt(value, 10));
  }
  /**
   * @param {number} n
   */
  static isFloat(n2) {
    return Number(n2) === n2 && n2 % 1 !== 0;
  }
  static isMsEdge() {
    if (Environment.isSSR()) return false;
    const ua = window.navigator.userAgent;
    const edge = ua.indexOf("Edge/");
    if (edge > 0) {
      return parseInt(ua.substring(edge + 5, ua.indexOf(".", edge)), 10);
    }
    return false;
  }
  //
  // Find the Greatest Common Divisor of two numbers
  //
  /**
   * @param {number} a
   * @param {number} b
   */
  static getGCD(a2, b2, p2 = 7) {
    let factor = Math.pow(10, p2 - Math.floor(Math.log10(Math.max(a2, b2))));
    if (factor > 1) {
      a2 = Math.round(Math.abs(a2) * factor);
      b2 = Math.round(Math.abs(b2) * factor);
    } else {
      factor = 1;
    }
    while (b2) {
      const t2 = b2;
      b2 = a2 % b2;
      a2 = t2;
    }
    return a2 / factor;
  }
  /**
   * @param {number} n
   */
  static getPrimeFactors(n2) {
    const factors = [];
    let divisor = 2;
    while (n2 >= 2) {
      if (n2 % divisor == 0) {
        factors.push(divisor);
        n2 = n2 / divisor;
      } else {
        divisor++;
      }
    }
    return factors;
  }
  /**
   * @param {number} a
   * @param {number} b
   */
  static mod(a2, b2, p2 = 7) {
    const big = Math.pow(10, p2 - Math.floor(Math.log10(Math.max(a2, b2))));
    a2 = Math.round(Math.abs(a2) * big);
    b2 = Math.round(Math.abs(b2) * big);
    return a2 % b2 / big;
  }
};
class DateTime {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.months31 = [1, 3, 5, 7, 8, 10, 12];
    this.months30 = [2, 4, 6, 9, 11];
    this.daysCntOfYear = [0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334];
  }
  /**
   * @param {any} date
   */
  isValidDate(date) {
    if (typeof date === "number") {
      return false;
    }
    return !isNaN(this.parseDate(date));
  }
  /**
   * @param {any} dateStr
   */
  getTimeStamp(dateStr) {
    if (isNaN(Date.parse(dateStr))) {
      return dateStr;
    }
    const utc = this.w.config.xaxis.labels.datetimeUTC;
    return !utc ? new Date(dateStr).getTime() : new Date(new Date(dateStr).toISOString().substr(0, 25)).getTime();
  }
  /**
   * @param {any} timestamp
   */
  getDate(timestamp) {
    const utc = this.w.config.xaxis.labels.datetimeUTC;
    return utc ? new Date(new Date(timestamp).toUTCString()) : new Date(timestamp);
  }
  /**
   * @param {string} dateStr
   */
  parseDate(dateStr) {
    const parsed = Date.parse(dateStr);
    if (!isNaN(parsed)) {
      return this.getTimeStamp(dateStr);
    }
    let output = Date.parse(dateStr.replace(/-/g, "/").replace(/[a-z]+/gi, " "));
    output = this.getTimeStamp(output);
    return output;
  }
  // This fixes the difference of x-axis labels between chrome/safari
  // Fixes #1726, #1544, #1485, #1255
  /**
   * @param {string} dateStr
   */
  parseDateWithTimezone(dateStr) {
    return Date.parse(dateStr.replace(/-/g, "/").replace(/[a-z]+/gi, " "));
  }
  // http://stackoverflow.com/questions/14638018/current-time-formatting-with-javascript#answer-14638191
  /**
   * @param {Date} date
   * @param {string} format
   */
  formatDate(date, format) {
    const locale = this.w.globals.locale;
    const utc = this.w.config.xaxis.labels.datetimeUTC;
    const MMMM = ["\0", ...locale.months];
    const MMM = ["", ...locale.shortMonths];
    const dddd = ["", ...locale.days];
    const ddd = ["", ...locale.shortDays];
    function ii(i2, len = 2) {
      let s3 = i2 + "";
      while (s3.length < len) s3 = "0" + s3;
      return s3;
    }
    const y = utc ? date.getUTCFullYear() : date.getFullYear();
    format = format.replace(/(^|[^\\])yyyy+/g, "$1" + y);
    format = format.replace(/(^|[^\\])yy/g, "$1" + y.toString().substr(2, 2));
    format = format.replace(/(^|[^\\])y/g, "$1" + y);
    const M2 = (utc ? date.getUTCMonth() : date.getMonth()) + 1;
    format = format.replace(/(^|[^\\])MMMM+/g, "$1" + MMMM[0]);
    format = format.replace(/(^|[^\\])MMM/g, "$1" + MMM[0]);
    format = format.replace(/(^|[^\\])MM/g, "$1" + ii(M2));
    format = format.replace(/(^|[^\\])M/g, "$1" + M2);
    const d = utc ? date.getUTCDate() : date.getDate();
    format = format.replace(/(^|[^\\])dddd+/g, "$1" + dddd[0]);
    format = format.replace(/(^|[^\\])ddd/g, "$1" + ddd[0]);
    format = format.replace(/(^|[^\\])dd/g, "$1" + ii(d));
    format = format.replace(/(^|[^\\])d/g, "$1" + d);
    const H = utc ? date.getUTCHours() : date.getHours();
    format = format.replace(/(^|[^\\])HH+/g, "$1" + ii(H));
    format = format.replace(/(^|[^\\])H/g, "$1" + H);
    const h2 = H > 12 ? H - 12 : H === 0 ? 12 : H;
    format = format.replace(/(^|[^\\])hh+/g, "$1" + ii(h2));
    format = format.replace(/(^|[^\\])h/g, "$1" + h2);
    const m2 = utc ? date.getUTCMinutes() : date.getMinutes();
    format = format.replace(/(^|[^\\])mm+/g, "$1" + ii(m2));
    format = format.replace(/(^|[^\\])m/g, "$1" + m2);
    const s2 = utc ? date.getUTCSeconds() : date.getSeconds();
    format = format.replace(/(^|[^\\])ss+/g, "$1" + ii(s2));
    format = format.replace(/(^|[^\\])s/g, "$1" + s2);
    let f = utc ? date.getUTCMilliseconds() : date.getMilliseconds();
    format = format.replace(/(^|[^\\])fff+/g, "$1" + ii(f, 3));
    f = Math.round(f / 10);
    format = format.replace(/(^|[^\\])ff/g, "$1" + ii(f));
    f = Math.round(f / 10);
    format = format.replace(/(^|[^\\])f/g, "$1" + f);
    const T = H < 12 ? "AM" : "PM";
    format = format.replace(/(^|[^\\])TT+/g, "$1" + T);
    format = format.replace(/(^|[^\\])T/g, "$1" + T.charAt(0));
    const t2 = T.toLowerCase();
    format = format.replace(/(^|[^\\])tt+/g, "$1" + t2);
    format = format.replace(/(^|[^\\])t/g, "$1" + t2.charAt(0));
    let tz = -date.getTimezoneOffset();
    let K = utc || !tz ? "Z" : tz > 0 ? "+" : "-";
    if (!utc) {
      tz = Math.abs(tz);
      const tzHrs = Math.floor(tz / 60);
      const tzMin = tz % 60;
      K += ii(tzHrs) + ":" + ii(tzMin);
    }
    format = format.replace(/(^|[^\\])K/g, "$1" + K);
    const day = (utc ? date.getUTCDay() : date.getDay()) + 1;
    format = format.replace(new RegExp(dddd[0], "g"), dddd[day]);
    format = format.replace(new RegExp(ddd[0], "g"), ddd[day]);
    format = format.replace(new RegExp(MMMM[0], "g"), MMMM[M2]);
    format = format.replace(new RegExp(MMM[0], "g"), MMM[M2]);
    format = format.replace(/\\(.)/g, "$1");
    return format;
  }
  /**
   * @param {number} minX
   * @param {number} maxX
   */
  getTimeUnitsfromTimestamp(minX, maxX) {
    const w2 = this.w;
    if (w2.config.xaxis.min !== void 0) {
      minX = w2.config.xaxis.min;
    }
    if (w2.config.xaxis.max !== void 0) {
      maxX = w2.config.xaxis.max;
    }
    const tsMin = this.getDate(minX);
    const tsMax = this.getDate(maxX);
    const minD = this.formatDate(tsMin, "yyyy MM dd HH mm ss fff").split(" ");
    const maxD = this.formatDate(tsMax, "yyyy MM dd HH mm ss fff").split(" ");
    return {
      minMillisecond: parseInt(minD[6], 10),
      maxMillisecond: parseInt(maxD[6], 10),
      minSecond: parseInt(minD[5], 10),
      maxSecond: parseInt(maxD[5], 10),
      minMinute: parseInt(minD[4], 10),
      maxMinute: parseInt(maxD[4], 10),
      minHour: parseInt(minD[3], 10),
      maxHour: parseInt(maxD[3], 10),
      minDate: parseInt(minD[2], 10),
      maxDate: parseInt(maxD[2], 10),
      minMonth: parseInt(minD[1], 10) - 1,
      maxMonth: parseInt(maxD[1], 10) - 1,
      minYear: parseInt(minD[0], 10),
      maxYear: parseInt(maxD[0], 10)
    };
  }
  /**
   * @param {number} year
   */
  isLeapYear(year) {
    return year % 4 === 0 && year % 100 !== 0 || year % 400 === 0;
  }
  /**
   * @param {number} month
   * @param {number} year
   * @param {number} subtract
   */
  calculcateLastDaysOfMonth(month, year, subtract) {
    const days = this.determineDaysOfMonths(month, year);
    return days - subtract;
  }
  /**
   * @param {number} year
   */
  determineDaysOfYear(year) {
    let days = 365;
    if (this.isLeapYear(year)) {
      days = 366;
    }
    return days;
  }
  /**
   * @param {number} year
   * @param {number} month
   * @param {number} date
   */
  determineRemainingDaysOfYear(year, month, date) {
    let dayOfYear = this.daysCntOfYear[month] + date;
    if (month > 1 && this.isLeapYear(year)) dayOfYear++;
    return dayOfYear;
  }
  /**
   * @param {number} month
   * @param {number} year
   */
  determineDaysOfMonths(month, year) {
    let days = 30;
    month = Utils$1.monthMod(month);
    switch (true) {
      case this.months30.indexOf(month) > -1:
        if (month === 2) {
          if (this.isLeapYear(year)) {
            days = 29;
          } else {
            days = 28;
          }
        }
        break;
      case this.months31.indexOf(month) > -1:
        days = 31;
        break;
      default:
        days = 31;
        break;
    }
    return days;
  }
  // ───────────────────────────────────────────────────────────────────────────
  // Pure date helpers used by the v6 single-interval TimeScale.
  // Native Date setters/getters do all rollover work (leap years, month
  // length, year crossings), eliminating the manual rollover code that was
  // the source of the historical TimeScale boundary bugs.
  // ───────────────────────────────────────────────────────────────────────────
  /**
   * Extract calendar fields from a timestamp. Month is 0-indexed (matches
   * Date.getMonth / getUTCMonth).
   *
   * @param {number} timestamp
   * @param {boolean} isUTC
   * @returns {{ year: number, month: number, date: number, hour: number, minute: number, second: number, ms: number, weekday: number }}
   */
  getDateFields(timestamp, isUTC) {
    const d = new Date(timestamp);
    return isUTC ? {
      year: d.getUTCFullYear(),
      month: d.getUTCMonth(),
      date: d.getUTCDate(),
      hour: d.getUTCHours(),
      minute: d.getUTCMinutes(),
      second: d.getUTCSeconds(),
      ms: d.getUTCMilliseconds(),
      weekday: d.getUTCDay()
    } : {
      year: d.getFullYear(),
      month: d.getMonth(),
      date: d.getDate(),
      hour: d.getHours(),
      minute: d.getMinutes(),
      second: d.getSeconds(),
      ms: d.getMilliseconds(),
      weekday: d.getDay()
    };
  }
  /**
   * Advance a timestamp by `count` units. Native setters handle cross-month,
   * cross-year, leap-year, and DST rollover correctly.
   *
   * @param {number} timestamp
   * @param {'year'|'month'|'week'|'day'|'hour'|'minute'|'second'} unit
   * @param {number} count  may be negative
   * @param {boolean} isUTC
   * @returns {number}
   */
  addInterval(timestamp, unit, count, isUTC) {
    const d = new Date(timestamp);
    if (isUTC) {
      switch (unit) {
        case "year":
          d.setUTCFullYear(d.getUTCFullYear() + count);
          break;
        case "month":
          d.setUTCMonth(d.getUTCMonth() + count);
          break;
        case "week":
          d.setUTCDate(d.getUTCDate() + count * 7);
          break;
        case "day":
          d.setUTCDate(d.getUTCDate() + count);
          break;
        case "hour":
          d.setUTCHours(d.getUTCHours() + count);
          break;
        case "minute":
          d.setUTCMinutes(d.getUTCMinutes() + count);
          break;
        case "second":
          d.setUTCSeconds(d.getUTCSeconds() + count);
          break;
      }
    } else {
      switch (unit) {
        case "year":
          d.setFullYear(d.getFullYear() + count);
          break;
        case "month":
          d.setMonth(d.getMonth() + count);
          break;
        case "week":
          d.setDate(d.getDate() + count * 7);
          break;
        case "day":
          d.setDate(d.getDate() + count);
          break;
        case "hour":
          d.setHours(d.getHours() + count);
          break;
        case "minute":
          d.setMinutes(d.getMinutes() + count);
          break;
        case "second":
          d.setSeconds(d.getSeconds() + count);
          break;
      }
    }
    return d.getTime();
  }
  /**
   * Snap a timestamp UP to the next boundary aligned with `step` units of
   * `unit`. Returns the input unchanged if already on a boundary. Alignment
   * rules per unit:
   *   - second: 0/step/2*step/... seconds within a minute (step must divide 60)
   *   - minute: 0/step/2*step/... minutes within an hour (step must divide 60)
   *   - hour:   0/step/... hours within a day (step must divide 24)
   *   - day:    every day (step always 1)
   *   - week:   next Monday on a `step`-week stride from a fixed epoch Monday
   *   - month:  Jan/Apr/Jul/Oct for step=3, Jan/Jul for step=6 (step must divide 12)
   *   - year:   year % step === 0
   *
   * @param {number} timestamp
   * @param {'year'|'month'|'week'|'day'|'hour'|'minute'|'second'} unit
   * @param {number} step
   * @param {boolean} isUTC
   * @returns {number}
   */
  ceilToBoundary(timestamp, unit, step, isUTC) {
    const d = new Date(timestamp);
    if (isUTC) {
      switch (unit) {
        case "second": {
          const s2 = d.getUTCSeconds();
          const aligned = Math.ceil(s2 / step) * step;
          if (aligned === s2 && d.getUTCMilliseconds() === 0)
            return timestamp;
          d.setUTCMilliseconds(0);
          d.setUTCSeconds(aligned);
          return d.getTime();
        }
        case "minute": {
          const m2 = d.getUTCMinutes();
          const aligned = Math.ceil(m2 / step) * step;
          if (aligned === m2 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0)
            return timestamp;
          d.setUTCMilliseconds(0);
          d.setUTCSeconds(0);
          d.setUTCMinutes(aligned);
          return d.getTime();
        }
        case "hour": {
          const h2 = d.getUTCHours();
          const aligned = Math.ceil(h2 / step) * step;
          if (aligned === h2 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0)
            return timestamp;
          d.setUTCMilliseconds(0);
          d.setUTCSeconds(0);
          d.setUTCMinutes(0);
          d.setUTCHours(aligned);
          return d.getTime();
        }
        case "day": {
          if (d.getUTCHours() === 0 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0)
            return timestamp;
          d.setUTCMilliseconds(0);
          d.setUTCSeconds(0);
          d.setUTCMinutes(0);
          d.setUTCHours(0);
          d.setUTCDate(d.getUTCDate() + 1);
          return d.getTime();
        }
        case "week": {
          const MS_PER_WEEK = 7 * 24 * 60 * 60 * 1e3;
          const REF_MONDAY_UTC = Date.UTC(1970, 0, 5);
          d.setUTCMilliseconds(0);
          d.setUTCSeconds(0);
          d.setUTCMinutes(0);
          d.setUTCHours(0);
          const startOfDay = d.getTime();
          const weeksSinceRef = Math.ceil(
            (startOfDay - REF_MONDAY_UTC) / MS_PER_WEEK
          );
          const alignedWeeks = Math.ceil(weeksSinceRef / step) * step;
          const aligned = REF_MONDAY_UTC + alignedWeeks * MS_PER_WEEK;
          if (aligned >= timestamp) return aligned;
          return aligned + step * MS_PER_WEEK;
        }
        case "month": {
          const m2 = d.getUTCMonth();
          const aligned = Math.ceil(m2 / step) * step;
          if (aligned === m2 && d.getUTCDate() === 1 && d.getUTCHours() === 0 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0)
            return timestamp;
          d.setUTCMilliseconds(0);
          d.setUTCSeconds(0);
          d.setUTCMinutes(0);
          d.setUTCHours(0);
          d.setUTCDate(1);
          d.setUTCMonth(aligned);
          return d.getTime();
        }
        case "year": {
          const y = d.getUTCFullYear();
          const aligned = Math.ceil(y / step) * step;
          if (aligned === y && d.getUTCMonth() === 0 && d.getUTCDate() === 1 && d.getUTCHours() === 0 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0)
            return timestamp;
          return Date.UTC(aligned, 0, 1);
        }
      }
    } else {
      switch (unit) {
        case "second": {
          const s2 = d.getSeconds();
          const aligned = Math.ceil(s2 / step) * step;
          if (aligned === s2 && d.getMilliseconds() === 0) return timestamp;
          d.setMilliseconds(0);
          d.setSeconds(aligned);
          return d.getTime();
        }
        case "minute": {
          const m2 = d.getMinutes();
          const aligned = Math.ceil(m2 / step) * step;
          if (aligned === m2 && d.getSeconds() === 0 && d.getMilliseconds() === 0)
            return timestamp;
          d.setMilliseconds(0);
          d.setSeconds(0);
          d.setMinutes(aligned);
          return d.getTime();
        }
        case "hour": {
          const h2 = d.getHours();
          const aligned = Math.ceil(h2 / step) * step;
          if (aligned === h2 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0)
            return timestamp;
          d.setMilliseconds(0);
          d.setSeconds(0);
          d.setMinutes(0);
          d.setHours(aligned);
          return d.getTime();
        }
        case "day": {
          if (d.getHours() === 0 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0)
            return timestamp;
          d.setMilliseconds(0);
          d.setSeconds(0);
          d.setMinutes(0);
          d.setHours(0);
          d.setDate(d.getDate() + 1);
          return d.getTime();
        }
        case "week": {
          const MS_PER_WEEK = 7 * 24 * 60 * 60 * 1e3;
          const REF_MONDAY_LOCAL = new Date(1970, 0, 5).getTime();
          d.setMilliseconds(0);
          d.setSeconds(0);
          d.setMinutes(0);
          d.setHours(0);
          const startOfDay = d.getTime();
          const weeksSinceRef = Math.ceil(
            (startOfDay - REF_MONDAY_LOCAL) / MS_PER_WEEK
          );
          const alignedWeeks = Math.ceil(weeksSinceRef / step) * step;
          const aligned = REF_MONDAY_LOCAL + alignedWeeks * MS_PER_WEEK;
          if (aligned >= timestamp) return aligned;
          return aligned + step * MS_PER_WEEK;
        }
        case "month": {
          const m2 = d.getMonth();
          const aligned = Math.ceil(m2 / step) * step;
          if (aligned === m2 && d.getDate() === 1 && d.getHours() === 0 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0)
            return timestamp;
          d.setMilliseconds(0);
          d.setSeconds(0);
          d.setMinutes(0);
          d.setHours(0);
          d.setDate(1);
          d.setMonth(aligned);
          return d.getTime();
        }
        case "year": {
          const y = d.getFullYear();
          const aligned = Math.ceil(y / step) * step;
          if (aligned === y && d.getMonth() === 0 && d.getDate() === 1 && d.getHours() === 0 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0)
            return timestamp;
          return new Date(aligned, 0, 1).getTime();
        }
      }
    }
    return timestamp;
  }
  /**
   * Test whether a timestamp falls exactly on a unit boundary (start of year,
   * start of month, start of day, etc.). Used by the multi-resolution
   * formatter to upgrade a tick's display unit to a coarser scale.
   *
   * @param {number} timestamp
   * @param {'year'|'month'|'day'|'hour'|'minute'|'second'} unit
   * @param {boolean} isUTC
   * @returns {boolean}
   */
  isAtBoundary(timestamp, unit, isUTC) {
    const f = this.getDateFields(timestamp, isUTC);
    switch (unit) {
      case "year":
        return f.month === 0 && f.date === 1 && f.hour === 0 && f.minute === 0 && f.second === 0 && f.ms === 0;
      case "month":
        return f.date === 1 && f.hour === 0 && f.minute === 0 && f.second === 0 && f.ms === 0;
      case "day":
        return f.hour === 0 && f.minute === 0 && f.second === 0 && f.ms === 0;
      case "hour":
        return f.minute === 0 && f.second === 0 && f.ms === 0;
      case "minute":
        return f.second === 0 && f.ms === 0;
      case "second":
        return f.ms === 0;
    }
    return false;
  }
}
class Formatters {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.tooltipKeyFormat = "dd MMM";
  }
  /**
   * @param {Function} fn
   * @param {any} val
   * @param {any} timestamp
   * @param {any} _opts
   */
  xLabelFormat(fn, val, timestamp, _opts) {
    const w2 = this.w;
    if (w2.config.xaxis.type === "datetime") {
      if (w2.config.xaxis.labels.formatter === void 0) {
        if (w2.config.tooltip.x.formatter === void 0) {
          const datetimeObj = new DateTime(this.w);
          return datetimeObj.formatDate(
            datetimeObj.getDate(val),
            w2.config.tooltip.x.format
          );
        }
      }
    }
    return fn(val, timestamp, _opts);
  }
  /**
   * @param {any} val
   */
  defaultGeneralFormatter(val) {
    if (Array.isArray(val)) {
      return val.map((v2) => {
        return v2;
      });
    } else {
      return val;
    }
  }
  /**
   * @param {any} v
   * @param {ApexYAxis} yaxe
   */
  defaultYFormatter(v2, yaxe) {
    const w2 = this.w;
    if (Utils$1.isNumber(v2)) {
      if (w2.globals.yValueDecimal !== 0) {
        v2 = v2.toFixed(
          yaxe.decimalsInFloat !== void 0 ? yaxe.decimalsInFloat : w2.globals.yValueDecimal
        );
      } else {
        const f = v2.toFixed(0);
        v2 = Number(f) === v2 ? f : v2.toFixed(1);
      }
    }
    return v2;
  }
  setLabelFormatters() {
    const w2 = this.w;
    const fmt = w2.formatters;
    fmt.xaxisTooltipFormatter = (val) => {
      return this.defaultGeneralFormatter(val);
    };
    fmt.ttKeyFormatter = (val) => {
      return this.defaultGeneralFormatter(val);
    };
    fmt.ttZFormatter = (val) => {
      return val;
    };
    fmt.legendFormatter = (val) => {
      return this.defaultGeneralFormatter(val);
    };
    if (w2.config.xaxis.labels.formatter !== void 0) {
      fmt.xLabelFormatter = w2.config.xaxis.labels.formatter;
    } else {
      fmt.xLabelFormatter = (val) => {
        if (Utils$1.isNumber(val)) {
          if (!w2.config.xaxis.convertedCatToNumeric && w2.config.xaxis.type === "numeric") {
            if (Utils$1.isNumber(w2.config.xaxis.decimalsInFloat)) {
              return val.toFixed(w2.config.xaxis.decimalsInFloat);
            } else {
              const diff = w2.globals.maxX - w2.globals.minX;
              if (diff > 0) {
                const step = diff / 10;
                const decimals = Math.max(0, -Math.floor(Math.log10(step)));
                return val.toFixed(decimals);
              }
              return val.toFixed(0);
            }
          }
          if (w2.globals.isBarHorizontal) {
            const range = w2.globals.maxY - w2.globals.minY;
            if (range < 4) {
              return val.toFixed(1);
            }
          }
          return val.toFixed(0);
        }
        return val;
      };
    }
    if (typeof w2.config.tooltip.x.formatter === "function") {
      fmt.ttKeyFormatter = w2.config.tooltip.x.formatter;
    } else {
      fmt.ttKeyFormatter = fmt.xLabelFormatter;
    }
    if (typeof w2.config.xaxis.tooltip.formatter === "function") {
      fmt.xaxisTooltipFormatter = w2.config.xaxis.tooltip.formatter;
    }
    if (Array.isArray(w2.config.tooltip.y)) {
      fmt.ttVal = w2.config.tooltip.y;
    } else {
      if (w2.config.tooltip.y.formatter !== void 0) {
        fmt.ttVal = w2.config.tooltip.y;
      }
    }
    if (w2.config.tooltip.z.formatter !== void 0) {
      fmt.ttZFormatter = w2.config.tooltip.z.formatter;
    }
    if (w2.config.legend.formatter !== void 0) {
      fmt.legendFormatter = w2.config.legend.formatter;
    }
    fmt.yLabelFormatters = [];
    w2.config.yaxis.forEach((yaxe, i2) => {
      if (yaxe.labels.formatter !== void 0) {
        fmt.yLabelFormatters[i2] = yaxe.labels.formatter;
      } else if (w2.config.chart.type === "violin") {
        const round = (v2) => typeof v2 === "number" && isFinite(v2) ? `${Math.round(v2 * 100) / 100}` : v2;
        fmt.yLabelFormatters[i2] = (val) => {
          if (!w2.globals.xyCharts) return val;
          return Array.isArray(val) ? val.map(round) : round(val);
        };
      } else {
        fmt.yLabelFormatters[i2] = (val) => {
          if (!w2.globals.xyCharts) return val;
          if (Array.isArray(val)) {
            return val.map((v2) => {
              return this.defaultYFormatter(v2, yaxe);
            });
          } else {
            return this.defaultYFormatter(val, yaxe);
          }
        };
      }
    });
    return w2.globals;
  }
  heatmapLabelFormatters() {
    const w2 = this.w;
    if (w2.config.chart.type === "heatmap") {
      w2.globals.yAxisScale[0].result = /** @type {any} */
      w2.seriesData.seriesNames.slice();
      const longest = (
        /** @type {any} */
        w2.seriesData.seriesNames.reduce(
          (a2, b2) => a2.length > b2.length ? a2 : b2,
          0
        )
      );
      w2.globals.yAxisScale[0].niceMax = longest;
      w2.globals.yAxisScale[0].niceMin = longest;
    }
  }
}
const name = "en";
const options = { "months": ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"], "shortMonths": ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"], "days": ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"], "shortDays": ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"], "toolbar": { "exportToSVG": "Download SVG", "exportToPNG": "Download PNG", "exportToCSV": "Download CSV", "menu": "Menu", "selection": "Selection", "selectionZoom": "Selection Zoom", "zoomIn": "Zoom In", "zoomOut": "Zoom Out", "pan": "Panning", "reset": "Reset Zoom", "measure": "Measure" } };
const en = {
  name,
  options
};
class Options {
  constructor() {
    this.yAxis = {
      show: true,
      showAlways: false,
      showForNullSeries: true,
      seriesName: void 0,
      opposite: false,
      reversed: false,
      logarithmic: false,
      logBase: 10,
      tickAmount: void 0,
      stepSize: void 0,
      forceNiceScale: false,
      alignZero: false,
      max: void 0,
      min: void 0,
      floating: false,
      decimalsInFloat: void 0,
      labels: {
        show: true,
        showDuplicates: false,
        minWidth: 0,
        maxWidth: 160,
        offsetX: 0,
        offsetY: 0,
        align: void 0,
        rotate: 0,
        padding: 20,
        style: {
          colors: [],
          fontSize: "11px",
          fontWeight: 400,
          fontFamily: void 0,
          cssClass: ""
        },
        formatter: void 0
      },
      axisBorder: {
        show: false,
        color: "#e0e0e0",
        width: 1,
        offsetX: 0,
        offsetY: 0
      },
      axisTicks: {
        show: false,
        color: "#e0e0e0",
        width: 6,
        offsetX: 0,
        offsetY: 0
      },
      title: {
        text: void 0,
        rotate: -90,
        offsetY: 0,
        offsetX: 0,
        style: {
          color: void 0,
          fontSize: "11px",
          fontWeight: 900,
          fontFamily: void 0,
          cssClass: ""
        }
      },
      tooltip: {
        enabled: false,
        offsetX: 0
      },
      crosshairs: {
        show: true,
        position: "front",
        stroke: {
          color: "#b6b6b6",
          width: 1,
          dashArray: 0
        }
      }
    };
    this.pointAnnotation = {
      id: void 0,
      x: 0,
      y: null,
      yAxisIndex: 0,
      seriesIndex: void 0,
      mouseEnter: void 0,
      mouseLeave: void 0,
      click: void 0,
      marker: {
        size: 4,
        fillColor: "#fff",
        strokeWidth: 2,
        strokeColor: "#333",
        shape: "circle",
        offsetX: 0,
        offsetY: 0,
        // radius: 2, // DEPRECATED
        cssClass: ""
      },
      label: {
        borderColor: "#c2c2c2",
        borderWidth: 1,
        borderRadius: 2,
        text: void 0,
        textAnchor: "middle",
        offsetX: 0,
        offsetY: 0,
        mouseEnter: void 0,
        mouseLeave: void 0,
        click: void 0,
        style: {
          background: "#fff",
          color: void 0,
          fontSize: "11px",
          fontFamily: void 0,
          fontWeight: 400,
          cssClass: "",
          padding: {
            left: 5,
            right: 5,
            top: 2,
            bottom: 2
          }
        }
      },
      customSVG: {
        // this will be deprecated in the next major version as it is going to be replaced with a better alternative below (image)
        SVG: void 0,
        cssClass: void 0,
        offsetX: 0,
        offsetY: 0
      },
      image: {
        path: void 0,
        width: 20,
        height: 20,
        offsetX: 0,
        offsetY: 0
      },
      tooltip: {
        // Show a hover tooltip over the annotation marker, like a regular
        // data point. Lets you surface richer detail than fits in the label.
        enabled: false,
        // Static tooltip content (HTML allowed; array joined with <br/>).
        // Falls back to label.text when omitted.
        text: void 0,
        // formatter({ annotation, seriesIndex, id, w }) => string (HTML).
        // Takes precedence over `text` when provided.
        formatter: void 0,
        // 'light' | 'dark'. Falls back to the global tooltip.theme.
        theme: void 0,
        offsetX: 0,
        offsetY: 0
      }
    };
    this.yAxisAnnotation = {
      id: void 0,
      y: 0,
      y2: null,
      strokeDashArray: 1,
      fillColor: "#c2c2c2",
      borderColor: "#c2c2c2",
      borderWidth: 1,
      opacity: 0.3,
      offsetX: 0,
      offsetY: 0,
      width: "100%",
      yAxisIndex: 0,
      label: {
        borderColor: "#c2c2c2",
        borderWidth: 1,
        borderRadius: 2,
        text: void 0,
        textAnchor: "end",
        position: "right",
        offsetX: 0,
        offsetY: -3,
        mouseEnter: void 0,
        mouseLeave: void 0,
        click: void 0,
        style: {
          background: "#fff",
          color: void 0,
          fontSize: "11px",
          fontFamily: void 0,
          fontWeight: 400,
          cssClass: "",
          padding: {
            left: 5,
            right: 5,
            top: 2,
            bottom: 2
          }
        }
      }
    };
    this.xAxisAnnotation = {
      id: void 0,
      x: 0,
      x2: null,
      strokeDashArray: 1,
      fillColor: "#c2c2c2",
      borderColor: "#c2c2c2",
      borderWidth: 1,
      opacity: 0.3,
      offsetX: 0,
      offsetY: 0,
      label: {
        borderColor: "#c2c2c2",
        borderWidth: 1,
        borderRadius: 2,
        text: void 0,
        textAnchor: "middle",
        orientation: "vertical",
        position: "top",
        offsetX: 0,
        offsetY: 0,
        mouseEnter: void 0,
        mouseLeave: void 0,
        click: void 0,
        style: {
          background: "#fff",
          color: void 0,
          fontSize: "11px",
          fontFamily: void 0,
          fontWeight: 400,
          cssClass: "",
          padding: {
            left: 5,
            right: 5,
            top: 2,
            bottom: 2
          }
        }
      }
    };
    this.text = {
      x: 0,
      y: 0,
      text: "",
      textAnchor: "start",
      foreColor: void 0,
      fontSize: "13px",
      fontFamily: void 0,
      fontWeight: 400,
      appendTo: ".apexcharts-annotations",
      backgroundColor: "transparent",
      borderColor: "#c2c2c2",
      borderRadius: 0,
      borderWidth: 0,
      paddingLeft: 4,
      paddingRight: 4,
      paddingTop: 2,
      paddingBottom: 2
    };
  }
  init() {
    return {
      annotations: {
        yaxis: [this.yAxisAnnotation],
        xaxis: [this.xAxisAnnotation],
        points: [this.pointAnnotation],
        texts: [],
        images: [],
        shapes: []
      },
      // Weave (#1): public plugin platform. Per-chart activation list:
      // { name, options?, order? }. Requires the Weave host to be bundled
      // (`import 'apexcharts/features/weave'`, included in the full bundle) and
      // the plugin registered via ApexCharts.registerPlugin().
      plugins: [],
      chart: {
        animations: {
          // Master switch — set false to render charts without any animation.
          // Each chart type gets a tailored initial-mount animation by default:
          //   line/area/rangeArea/radar: pen-stroke draw + radial fill bloom
          //   bar/stacked/range/funnel:  grow from baseline (+ stagger)
          //   scatter/bubble:            scale-up pop with overshoot
          //   heatmap:                   diagonal-wave cell reveal
          //   treemap:                   largest-tile-first cascade
          //   pie/donut/polar/gauge:     arc sweep + needle settle
          // Speed is controlled by `speed`; per-element stagger by
          // `animateGradually.enabled` / `animateGradually.delay`.
          enabled: true,
          speed: 800,
          // Cadence (#6): easing for the generic tweens (data-update value
          // transitions, path morphs, marker animate). A registered name, a
          // cubic-bezier [x1,y1,x2,y2] array, or a function (t in [0,1]).
          // 'easeInOutSine' is the historical curve, so the default is
          // behavior-neutral. The tuned initial-draw pen/pop easings are fixed.
          easing: "easeInOutSine",
          animateGradually: {
            // Drives per-element stagger across all chart types. When enabled,
            // bars/heatmap-cells/scatter-points/treemap-tiles reveal in
            // sequence; line/area markers fade in progressively along the
            // draw. `delay` is the requested step in ms (auto-capped per
            // chart so total stagger ≤ ~half the animation duration).
            delay: 150,
            enabled: true
          },
          dynamicAnimation: {
            // Data-change (updateSeries) animation. Independent from the
            // initial-mount animations above.
            enabled: true,
            speed: 350,
            // Easing for data-change morphs only (same accepted forms as
            // `animations.easing`). Unset -> inherit the chart-wide easing,
            // except detected streaming scrolls (rolling window / append
            // under xaxis.range) which default to 'linear' so the window
            // slides at constant velocity instead of pulsing each tick.
            easing: void 0
          },
          chartTypeMorph: {
            // Cross-type morph (updateOptions changing chart.type). Bridges
            // the destroy+recreate flicker by sampling source + target paths
            // into N evenly-spaced perimeter points and tweening point-by-point
            // with rotation-search alignment, so the transition is always smooth
            // and non-self-intersecting even between very different shapes (bar
            // rect ↔ pie wedge / radial arc). Supported pairs include bar ↔
            // pie / donut / radialBar / polarArea / funnel / pyramid (plus the
            // trivial pie ↔ donut ↔ polarArea cases). Falls back to instant
            // snap when types or data shape are incompatible.
            enabled: true,
            speed: 600
          },
          // Honor the OS-level prefers-reduced-motion setting. When true (default)
          // and the user has the accessibility preference enabled, all initial-mount
          // animations are skipped and the chart renders instantly.
          respectReducedMotion: true,
          // Above this many data points, per-element morph + stagger (which
          // spins up one JS-driven animation timeline per path — three chained
          // tweens each in morphSVG) is replaced by a single GPU-composited
          // opacity fade of the whole series. Thousands of candlesticks/bars
          // otherwise jank the main thread on initial render and on every zoom.
          // The fade reuses the existing delayedElements reveal so the result
          // still animates in, just at O(1) cost instead of O(n). Set to 0 to
          // always animate per-element regardless of dataset size.
          largeDatasetThreshold: 1e3
        },
        background: "",
        locales: [en],
        defaultLocale: "en",
        // Perspectives (#10): serializable/shareable view state. Passive:
        // requires `import 'apexcharts/features/perspectives'`. serializeOptions
        // is the whitelist of function-free option paths stored in a token.
        perspectives: {
          serializeOptions: ["theme", "xaxis", "yaxis", "title", "subtitle"]
        },
        // Rewind (#3): undo/redo history. Opt-in (bundle + behavior): requires
        // `import 'apexcharts/features/history'` AND chart.history.enabled.
        history: {
          enabled: false,
          maxDepth: 100,
          coalesceMs: 250,
          keyboard: true
        },
        // Strata (#2): hybrid SVG+canvas series renderer. 'svg' (default) |
        // 'canvas' | 'auto'. 'auto'/'canvas' need the canvas renderer feature
        // (`import 'apexcharts/features/renderer-canvas'`); without it, or with
        // a canvas-unsupported feature (pattern/image fill, color-matrix state
        // filters), selection falls back to 'svg'. Only the series layer is
        // canvas-capable in v1; chrome stays SVG.
        renderer: "svg",
        rendererThreshold: 8e3,
        layers: {
          series: "auto",
          grid: "svg",
          annotations: "svg",
          dataLabels: "svg"
        },
        dropShadow: {
          enabled: false,
          enabledOnSeries: void 0,
          top: 2,
          left: 2,
          blur: 4,
          color: "#000",
          opacity: 0.7
        },
        events: {
          animationEnd: void 0,
          beforeMount: void 0,
          mounted: void 0,
          updated: void 0,
          click: void 0,
          mouseMove: void 0,
          mouseLeave: void 0,
          xAxisLabelClick: void 0,
          legendClick: void 0,
          markerClick: void 0,
          selection: void 0,
          dataPointSelection: void 0,
          dataPointMouseEnter: void 0,
          dataPointMouseLeave: void 0,
          beforeZoom: void 0,
          beforeResetZoom: void 0,
          zoomed: void 0,
          scrolled: void 0,
          brushScrolled: void 0,
          crossFilter: void 0,
          filterChange: void 0,
          annotationDragged: void 0,
          annotationEdited: void 0,
          annotationCreated: void 0,
          annotationStyled: void 0,
          annotationDeleted: void 0,
          measured: void 0,
          keyDown: void 0,
          keyUp: void 0
        },
        foreColor: "#373d3f",
        fontFamily: "Helvetica, Arial, sans-serif",
        height: "auto",
        parentHeightOffset: 15,
        redrawOnParentResize: true,
        redrawOnWindowResize: true,
        id: void 0,
        group: void 0,
        nonce: void 0,
        // Per-chart license key override for the gated premium features
        // (storyboard, link/crossfilter, ink, measure, contextMenu,
        // perspectives, history). Most specific wins: chart.license ->
        // ApexCharts.setLicense() -> window.Apex.license -> unlicensed (trial
        // watermark). Shared across the ApexCharts family. See setLicense().
        license: void 0,
        offsetX: 0,
        offsetY: 0,
        injectStyleSheet: true,
        selection: {
          enabled: false,
          type: "x",
          // selectedPoints: undefined, // default datapoints that should be selected automatically
          fill: {
            color: "#24292e",
            opacity: 0.1
          },
          stroke: {
            width: 1,
            color: "#24292e",
            opacity: 0.4,
            dashArray: 3
          },
          xaxis: {
            min: void 0,
            max: void 0
          },
          yaxis: {
            min: void 0,
            max: void 0
          }
        },
        sparkline: {
          enabled: false
        },
        brush: {
          enabled: false,
          autoScaleYaxis: true,
          target: void 0,
          targets: void 0
        },
        // Linked Views (#4): crossfilter / linked highlighting. Requires the
        // `link` feature. Two modes:
        //   HIGHLIGHT (P1): `enabled` with no `dimension`. Charts sharing a
        //   `chart.group` form a set; brushing a range (needs
        //   `chart.selection.enabled`) dims out-of-range marks in place.
        //   FILTER (P2): set `dimension` (its presence selects this path). Each
        //   chart declares a dimension + reduction over a shared record set
        //   registered with ApexCharts.crossfilter({ id, records }); clicking a
        //   bucket re-aggregates the other charts. See docs/spec.
        link: {
          enabled: false,
          mode: "highlight",
          dimOpacity: 0.2,
          // FILTER-mode config (all optional except dimension):
          id: void 0,
          // crossfilter coordinator id (defaults to chart.group)
          dimension: void 0,
          // (row) => key; presence selects filter mode
          reduce: void 0,
          // 'count' | { sum|avg|min|max: field } | (rows)=>n
          type: void 0,
          // 'category' | 'range' (else inferred from bins)
          bins: void 0,
          // range dims: { width } | { count } | { thresholds }
          order: void 0,
          // category order: 'first-seen' | 'asc' | 'desc' | fn
          seriesName: void 0
          // axis-chart series name (default 'Count')
        },
        // Ink Layer (#7): direct-manipulation annotations. When enabled, every
        // point annotation is draggable (unless it sets draggable:false); or opt
        // in per annotation with annotations.points[].draggable. Clicking an
        // ink-managed annotation opens a floating editor card: rename inline,
        // recolor, toggle bold, step the font size, size/reshape the marker, or
        // delete the note. Axis-line annotations get separate Label and Line
        // color rows, so restyling the label chip never touches the stroke.
        // Requires the `ink` feature. Fires annotationDragged,
        // annotationEdited, annotationStyled and annotationDeleted.
        ink: {
          enabled: false,
          // Show a minimal "add note" tool palette; clicking it arms create
          // mode (the next plot click drops an editable, draggable annotation).
          palette: false,
          // Snap a dragged point / axis-line annotation to the nearest gridline
          // (numeric x + linear y). Undo/redo of ink edits is automatic when the
          // history (Rewind) feature is enabled.
          snap: false,
          // Accent swatches offered by the floating note editor; defaults to a
          // built-in 6-color palette when undefined.
          noteColors: void 0
        },
        // Measure ruler (#18): a measure/delta ruler. Requires the `measure`
        // feature. Hold `key` (default 'm') and drag A->B on the plot, or call
        // chart.startMeasure()/chart.stopMeasure() to arm it. The live ruler
        // reads dx, dy, %change and slope; on release it pins as a data-anchored
        // overlay that re-projects on zoom/resize. Fires `measured`.
        measure: {
          enabled: false,
          // 'span' (default): finance-style vertical band between two x-points
          // with a "change (%) + range" readout, endpoints snapped to the first
          // series. 'free': a diagonal ruler between two arbitrary points.
          mode: "span",
          key: "m",
          pinOnRelease: true,
          // Styling tokens. Every element also carries a stable CSS class
          // (apexcharts-measure-band / -vline / -line / -label-bg / -label,
          // group gets apexcharts-measure-up|down|flat). Colors are left
          // undefined so they resolve config -> `--apx-measure-*` CSS custom
          // property -> built-in default; set them here to force a color from JS.
          colors: {
            up: void 0,
            down: void 0,
            neutral: void 0,
            guide: void 0
          },
          band: true,
          // span mode: shaded band between the two x-positions
          guides: true,
          // span mode: vertical dashed reference lines
          markers: true,
          // endpoint dots on the series line
          // Content: value formatters and a full readout override. `label`
          // receives { from, to, dx, dy, percentChange, slope, mode } and
          // returns a string or string[] (lines).
          format: { x: void 0, y: void 0, percent: void 0 },
          label: void 0
        },
        // Radial Actions (#chrome): right-click / long-press context menu.
        // Requires the `contextMenu` feature. Each action receives the clicked
        // data coordinates, so verbs act at the point (not chart-wide like a
        // toolbar button). `items` is an ordered list of built-in ids
        // ('annotate' | 'xline' | 'yline' | 'measure') and/or custom
        // { id, label, icon, onClick(ctx, { x, y, seriesIndex, dataPointIndex,
        // clientX, clientY }) }. 'measure' is shown only when the measure tool
        // is enabled. `labels` overrides built-in text; `noteText` is the label
        // dropped by 'annotate'.
        contextMenu: {
          enabled: false,
          items: ["annotate", "xline", "yline", "measure"],
          labels: {
            annotate: void 0,
            xline: void 0,
            yline: void 0,
            measure: void 0
          },
          noteText: "Note",
          // 'xline' ("Annotate here") drops a dashed vertical LINE at the
          // clicked x; 'yline' ("Mark this level") a dashed horizontal line at
          // the clicked y. Lines only, never a range rectangle. Like the note,
          // a line is ink-managed when the ink feature is bundled: it opens
          // the floating editor (rename; separate Label and Line color rows,
          // so restyling the chip cannot blank the stroke; delete), drags
          // along its axis, and undoes via Rewind. `line` styles both items.
          line: {
            text: "",
            // label drawn on the line; empty for no label
            strokeDashArray: 4,
            color: void 0
            // undefined keeps the annotation default color
          }
        },
        stacked: false,
        stackOnlyBar: true,
        // mixed chart with stacked bars and line series - incorrect line draw #907
        stackType: "normal",
        // Real-time streaming mode. When enabled, appendData() bounds memory
        // automatically: each series is trimmed to `maxPoints` (when set) or
        // to the visible `xaxis.range` window plus a two-point off-screen
        // runway (so exiting segments slide off the left edge instead of
        // popping). The scroll animation itself needs no opt-in; updates
        // that continue the previous window (appendData, or updateSeries with
        // a shifted fixed-length window) always translate at constant
        // velocity; see modules/animations/StreamScroll.
        streaming: {
          enabled: false,
          maxPoints: void 0
        },
        toolbar: {
          show: true,
          offsetX: 0,
          offsetY: 0,
          tools: {
            download: true,
            selection: true,
            zoom: true,
            zoomin: true,
            zoomout: true,
            pan: true,
            reset: true,
            // Shown only when the measure ruler is active (chart.measure.enabled
            // and the `measure` feature bundled). Toggles the measure tool; set
            // false to keep the ruler key-driven only. See chart.measure.
            measure: true,
            customIcons: []
          },
          export: {
            csv: {
              filename: void 0,
              columnDelimiter: ",",
              headerCategory: "category",
              headerValue: "value",
              categoryFormatter: void 0,
              valueFormatter: void 0
            },
            png: {
              filename: void 0
            },
            svg: {
              filename: void 0
            },
            scale: void 0,
            width: void 0,
            // An exported SVG is a standalone document: it cannot reach the
            // page's @font-face rules, so a custom font falls back to a
            // generic one in the PNG/SVG. When true, matching @font-face
            // rules are inlined as base64 data URIs. See #3617.
            embedFonts: true
          },
          autoSelected: "zoom"
          // accepts -> zoom, pan, selection, measure
        },
        type: "line",
        width: "100%",
        zoom: {
          enabled: true,
          type: "x",
          autoScaleYaxis: false,
          // Wheel and pinch zoom can both be triggered without meaning to (a
          // page scroll or a two-finger swipe over the chart), so 'auto' offers
          // them only when the viewer has a way back: the toolbar's reset
          // button. With the toolbar hidden they stay off, since a zoom nobody
          // asked for and nobody can undo is a trap. Set either to true to
          // force the gesture on regardless (for a page that supplies its own
          // reset control), or false to turn it off outright.
          allowMouseWheelZoom: "auto",
          // Momentum: two-finger pinch-zoom on touch devices. Zooms the x-axis
          // around the pinch centroid (matching the x-only wheel/toolbar zoom),
          // frame-by-frame rather than the 400ms wheel throttle.
          pinch: "auto",
          zoomedArea: {
            fill: {
              color: "#90CAF9",
              opacity: 0.4
            },
            stroke: {
              color: "#0D47A1",
              opacity: 0.4,
              width: 1
            }
          }
        },
        // Momentum: kinetic panning on touch. When a one-finger pan is released
        // with velocity, the chart keeps gliding and decelerates by `friction`
        // each frame, clamping (no elastic overshoot) at the data edges.
        pan: {
          inertia: true,
          friction: 0.92
        },
        accessibility: {
          enabled: true,
          description: void 0,
          announcements: {
            enabled: true
          },
          keyboard: {
            enabled: true,
            navigation: {
              enabled: true,
              wrapAround: false
            }
          }
        },
        dataReducer: {
          enabled: false,
          algorithm: "lttb",
          targetPoints: 250,
          threshold: 500
        }
      },
      parsing: {
        x: void 0,
        y: void 0
      },
      plotOptions: {
        line: {
          isSlopeChart: false,
          colors: {
            threshold: 0,
            colorAboveThreshold: void 0,
            colorBelowThreshold: void 0
          }
        },
        area: {
          fillTo: "origin"
        },
        bar: {
          horizontal: false,
          columnWidth: "70%",
          // should be in percent 0 - 100
          barHeight: "70%",
          // should be in percent 0 - 100
          distributed: false,
          borderRadius: 0,
          borderRadiusApplication: "around",
          // [around, end]
          borderRadiusWhenStacked: "last",
          // [all, last]
          rangeBarOverlap: true,
          rangeBarGroupRows: false,
          hideZeroBarsWhenGrouped: false,
          isDumbbell: false,
          dumbbellColors: void 0,
          isFunnel: false,
          isFunnel3d: true,
          colors: {
            ranges: [],
            backgroundBarColors: [],
            backgroundBarOpacity: 1,
            backgroundBarRadius: 0
          },
          dataLabels: {
            position: "top",
            // top, center, bottom
            maxItems: 100,
            hideOverflowingLabels: true,
            orientation: "horizontal",
            total: {
              enabled: false,
              formatter: void 0,
              offsetX: 0,
              offsetY: 0,
              style: {
                color: "#373d3f",
                fontSize: "12px",
                fontFamily: void 0,
                fontWeight: 600
              }
            }
          }
        },
        bubble: {
          zScaling: true,
          minBubbleRadius: void 0,
          maxBubbleRadius: void 0
        },
        scatter: {
          // Spread overlapping points apart ("jitter"). Two uses, one engine:
          //  - Strip plot: supply data as { x: 'Category', y: [v1, v2, ...] }.
          //    Each category becomes a band and the values are scattered
          //    horizontally within it. Marker styling comes from the standard
          //    `markers` / `colors` config.
          //  - Overplotting: ordinary { x, y } points get a small random offset
          //    so dense clusters fan out. The underlying data (and tooltip
          //    values) stay exact — only the drawn position moves.
          // Offsets are in axis units (x: 1 = one category step) and are
          // deterministic (stable across re-renders, SSR-safe).
          jitter: {
            enabled: false,
            x: 0,
            // max ± horizontal offset, in x-axis units
            y: 0,
            // max ± vertical offset, in y-axis units
            distributed: false,
            // single series: colour each band differently
            maxPoints: 5e3
            // per band; excess values are stride-thinned
          }
        },
        candlestick: {
          colors: {
            upward: "#00B746",
            downward: "#EF403C"
          },
          wick: {
            useFillColor: true
          }
        },
        boxPlot: {
          colors: {
            upper: "#00E396",
            lower: "#008FFB"
          },
          // Where the whiskers reach when the summary is DERIVED from raw
          // observations (a datum supplying `points` instead of a 5-number y,
          // which needs `apexcharts/features/stats`). Ignored for precomputed
          // summaries, which are drawn exactly as given.
          //   'minmax' → the extremes, so nothing is hidden
          //   'tukey'  → the last observation inside 1.5 * IQR of each
          //              quartile. Points beyond the fence fall outside the
          //              whisker, so pair it with `points.show` or they become
          //              invisible.
          whiskers: "minmax",
          // Optional individual observations ("jitter") overlaid on each box.
          // Inert unless a data point supplies a `points: number[]` array; off
          // by default so existing boxPlot charts are unchanged.
          points: {
            show: false,
            shape: "circle",
            // 'circle' | 'square'
            size: 2.5,
            // radius (px)
            jitter: 0.5,
            // 0..1 fraction of the box half-width to scatter within
            maxPoints: 3e3,
            // cap per box; excess is stride-thinned
            opacity: 0.9,
            // 'series-dark' (default) → a darker shade of the series colour,
            // 'series' → the series colour, or any literal colour string.
            fillColor: "series-dark",
            strokeColor: "#fff",
            strokeWidth: 1
            // Optional `colorScale` (undeclared so a user object merges cleanly)
            // colours each dot by its value: { colors, min, max, steps }
          }
        },
        violin: {
          // Multiply the density-derived half-width. 1 = density's own maxWeight
          // maps to half the category slot.
          bandwidthScale: 1,
          // Kernel density estimation, used only when the density is DERIVED
          // from raw observations (a datum supplying `points`, or a flat number
          // array as `y`, which needs `apexcharts/features/stats`). A
          // precomputed density profile is drawn exactly as given.
          //   bandwidth  → kernel width in value units. Unset uses Silverman's
          //                rule of thumb, which takes the smaller of the
          //                standard deviation and a scaled IQR so one distant
          //                outlier cannot smear the curve flat. Note this is a
          //                statistical parameter, unlike `bandwidthScale`
          //                above, which only scales the drawn width.
          //   resolution → density samples per violin (default 64).
          kde: {
            bandwidth: void 0,
            resolution: 64
          },
          // 'individual' → every violin uses the full slot width (scaled to its
          // own peak). 'group' → all violins share one scale (the densest in the
          // series), so widths stay proportional to density across categories.
          normalize: "individual",
          // Individual observations ("jitter") overlaid on the violin shape.
          points: {
            show: true,
            shape: "circle",
            // 'circle' | 'square'
            size: 2.5,
            // radius (px)
            jitter: 0.5,
            // 0..1 fraction of the half-width to scatter within
            constrainToViolin: true,
            // clamp jitter to the density width at each value
            maxPoints: 3e3,
            // cap per violin; excess is stride-thinned
            opacity: 0.9,
            // Default: a darker shade of each violin's own colour, with a white
            // outline. fillColor accepts 'series-dark' (default), 'series' (the
            // violin's colour as-is), or any literal colour string.
            fillColor: "series-dark",
            strokeColor: "#fff",
            strokeWidth: 1
            // Optional `colorScale` (left undeclared so a user object merges
            // cleanly) colours each dot by its value along a ramp:
            //   { colors: ['#0d0887', … '#f0f921'], min, max, steps }
          }
        },
        histogram: {
          // How the bin width is chosen from the raw observations. A rule name
          // ('auto' | 'fd' | 'sturges' | 'scott' | 'rice' | 'sqrt') or a fixed
          // bin count. 'auto' takes the narrower of Freedman-Diaconis and
          // Sturges, falling back to Sturges when the IQR is 0 (which happens
          // as soon as most values are identical).
          bins: "auto",
          // Explicit bin width in value units. Wins over `bins` when set: use
          // it when the bin boundaries carry meaning (decades, 5-minute
          // buckets) rather than being a statistical choice.
          binWidth: void 0,
          // [min, max] to bin over, instead of the data's own extent. Lets
          // several histograms share one scale.
          range: void 0,
          // y units: 'count' (observations per bin), 'relative' (percent of
          // the series total), or 'density' (count / (n * binWidth), so the
          // total area is 1 and bins of different widths stay comparable).
          normalize: "count",
          // Running total across bins, i.e. a cumulative distribution.
          cumulative: false,
          // With more than one series, draw every distribution across the FULL
          // bin so they overlay, instead of dividing the bin between them. All
          // series already share one set of edges, and comparing two shapes is
          // the reason to put them on one axis; splitting the bin makes the
          // columns stop touching, which reads as a clustered bar chart rather
          // than a distribution. Set false for side-by-side bars.
          //
          // An overlay is unreadable opaque, so it also softens the fill and
          // drops the bin separator stroke. Both remain overridable.
          overlap: true
        },
        heatmap: {
          radius: 2,
          enableShades: true,
          shadeIntensity: 0.5,
          reverseNegativeShade: false,
          distributed: false,
          useFillColorAsStroke: false,
          colorScale: {
            inverse: false,
            ranges: [],
            min: void 0,
            max: void 0,
            // Replaces the default categorical legend with a continuous
            // gradient stripe + a hover indicator arrow. Honors
            // `chart.legend.position` (top / right / bottom / left).
            gradientLegend: {
              enabled: false,
              // Strip length along the legend's long axis. Accepts a number
              // (pixels) or a percentage string. For top/bottom placement the
              // percentage is resolved against the chart's SVG width; for
              // left/right placement, against the SVG height.
              width: "70%",
              height: "70%",
              thickness: 12,
              // Alignment of the strip within the legend area:
              //  - top/bottom: 'start' = left, 'center', 'end' = right
              //  - left/right: 'start' = top,  'center', 'end' = bottom
              align: "center",
              // Number of gradient stops sampled from the shade function when
              // no explicit `ranges` are provided.
              stops: 16,
              // Show min/max labels at the ends of the strip.
              showLabels: true,
              // Show a value tooltip next to the arrow when hovering a cell.
              showHoverValue: true,
              labelStyle: {
                fontSize: "11px",
                fontFamily: void 0,
                colors: void 0
              },
              arrow: {
                size: 8,
                color: void 0
                // falls back to chart.foreColor
              },
              formatter: void 0
              // (val) => string, for min/max + hover value
            }
          }
        },
        funnel: {
          // 'rectangle' preserves the existing centered-rectangle funnel
          // geometry. 'trapezoid' produces continuous sloped sides between
          // consecutive stages (each stage's bottom width matches the next
          // stage's top width).
          shape: "rectangle",
          // For shape: 'trapezoid' only — what to do with the last stage's
          // bottom edge: 'flat' (parallel sides) or 'taper' (taper to a point).
          lastShape: "flat"
        },
        treemap: {
          enableShades: true,
          shadeIntensity: 0.5,
          distributed: false,
          reverseNegativeShade: false,
          useFillColorAsStroke: false,
          borderRadius: 4,
          dataLabels: {
            format: "scale",
            // scale | truncate
            // Skip a tile's label when it would render below this size.
            //
            // With `format: 'scale'` the font size follows the tile's area, so
            // on a dense treemap most tiles ask for text a few pixels tall,
            // which nobody can read. Drawing it is not free: each label has to
            // be built and measured against the DOM, and on a 10k-tile chart
            // that was the entire render cost (~7.5s, versus ~0.1s once the
            // unreadable ones are skipped).
            //
            // The default sits below the smallest label any bundled sample
            // draws, so it only ever removes text that was already illegible.
            // Set 0 to draw a label on every tile regardless of size.
            minFontSize: 4
          },
          colorScale: {
            inverse: false,
            ranges: [],
            min: void 0,
            max: void 0,
            // Colour a tile by a SECOND metric, independent of the value that
            // sizes it (area = how big, colour = how it did). Reads
            // `datum.colorValue` unless this names another key or supplies an
            // accessor `(datum, {seriesIndex, dataPointIndex, w}) => number`.
            colorValue: void 0,
            // Continuous interpolation between colour stops. Active as soon as
            // any datum carries a colour metric; `enabled: false` opts out and
            // `true` forces it on. `ranges` is unaffected and still applies
            // wherever it is set.
            gradient: {
              enabled: void 0,
              // Domain. Defaults to the extent of the colour metric.
              min: void 0,
              max: void 0,
              // The value the middle colour is pinned to. Defaults to 0 when
              // the domain straddles zero (a diverging metric), else none.
              midpoint: void 0,
              // With a midpoint, balance the domain around it so equal moves
              // in either direction read as equally saturated.
              symmetric: true,
              // Low -> mid -> high. Two colours make a sequential ramp.
              colors: void 0,
              // Explicit `[{ value, color }]` stops; overrides colors/midpoint.
              stops: void 0
            },
            // Continuous colour legend: a gradient strip with end labels and a
            // hover indicator, in place of the categorical legend. Same options
            // as the heatmap's.
            gradientLegend: {
              enabled: false,
              width: "70%",
              height: "70%",
              thickness: 12,
              align: "center",
              stops: 16,
              showLabels: true,
              showHoverValue: true,
              labelStyle: {
                fontSize: "11px",
                fontFamily: void 0,
                colors: void 0
              },
              arrow: {
                size: 8,
                color: void 0
              },
              formatter: void 0
            }
          },
          // Arbitrary-depth treemap: a datum may carry `children`.
          nested: {
            // Parent containers appear on their own as soon as the data is
            // nested; `false` forces the flat two-level layout.
            enabled: void 0,
            // Read `drilldown: '<id>'` ids as extra levels instead of as a
            // click target for the drilldown feature. Off by default, because
            // on a treemap that id has always meant "descend on click".
            drilldownAsLevels: false
          },
          // How a parent is drawn once the data is nested. Per-level overrides
          // go in `levels` below.
          parents: {
            // 'auto' (default): on when the data carries `children`.
            show: "auto",
            // Inset between a parent's edge and the children inside it.
            padding: 4,
            fill: void 0,
            fillOpacity: 1,
            borderColor: void 0,
            borderWidth: 1,
            borderRadius: void 0,
            hover: {
              show: true,
              color: void 0,
              width: 2
            },
            header: {
              show: true,
              height: 22,
              // Skip the strip on tiles too narrow to show a name.
              minWidth: 40,
              align: "left",
              offsetX: 0,
              offsetY: 0,
              showValue: false,
              // (name, { value, depth, seriesIndex, node, w }) => string
              formatter: void 0,
              style: {
                fontSize: "12px",
                fontFamily: void 0,
                fontWeight: 600,
                color: void 0,
                background: void 0,
                cssClass: ""
              }
            },
            tooltip: {
              // ({ name, value, depth, leafCount, percentOfParent,
              //    percentOfTotal, node, w }) => html
              formatter: void 0
            }
          },
          // Per-depth overrides of `parents`, indexed from the outermost group
          // actually drawn (0 = the series, or the first authored level when a
          // single series is unwrapped).
          levels: [],
          // Click a group to fill the canvas with it; a breadcrumb goes back.
          // Ignored when the drilldown feature is active on the same chart:
          // both navigate the hierarchy, and drilldown owns the click there.
          zoom: {
            enabled: false,
            // Overrides `drilldown.breadcrumb` for this chart only. Same shape
            // (show / position / separator / rootLabel / offsetX / offsetY /
            // formatter), so a zoomed treemap and a drilled-in chart present
            // the same affordance without importing the drilldown feature.
            breadcrumb: void 0
          },
          seriesTitle: {
            show: true,
            offsetY: 1,
            offsetX: 1,
            borderColor: "#000",
            borderWidth: 1,
            borderRadius: 2,
            style: {
              background: "rgba(0, 0, 0, 0.6)",
              color: "#fff",
              fontSize: "12px",
              fontFamily: void 0,
              fontWeight: 400,
              cssClass: "",
              padding: {
                left: 6,
                right: 6,
                top: 2,
                bottom: 2
              }
            }
          }
        },
        unit: {
          // 'grouped' (each category is its own cluster, laid out in a row) |
          // 'packed' (one blob; categories coloured + sorted, minority centred) |
          // 'columns' (each category is a vertical bar built from stacked dots) |
          // 'grid' (one lattice of cells filled in category order - a waffle /
          // part-to-whole square "pie"; `chart.type:'waffle'` presets this) |
          // 'scatter' (units on real value axes: a beeswarm, or a 2D
          // value-value scatter - see the `scatter` block below) |
          // 'arc' (a semicircular fan) |
          // 'custom' (positions come from `positions` below).
          layout: "grouped",
          // `layout: 'custom'` only. The layout provider: either a function
          // `(objects, rect) => [{id, x, y, r?}]` returning plot pixels, or the
          // name of one registered with `ApexCharts.registerUnitLayout`.
          //
          // This is the whole extension point. A layout is objects in,
          // positions out; it knows nothing about animation, because the engine
          // already tweens position, radius and colour and already keeps each
          // mark's identity across a relayout. So an arrangement this file
          // cannot know about - a country silhouette, a hex grid, a timeline, a
          // projection handed over by ApexMaps - is a plugin, not a core edit.
          //
          // `objects` carries one entry per mark: {id, index, seriesIndex,
          // dataPointIndex, label, value, datum, r}. `id` is the datum's own
          // id/name when the per-unit object form supplies one, so a provider
          // can address a specific unit rather than a positional slot.
          //
          // A mark whose id the provider omits animates out; ids matching no
          // mark are ignored.
          positions: void 0,
          // Update transition, controlling which previous dot each new dot
          // tweens from. 'group' (default): keyed per category, so dots stay in
          // their group and category-level enters/exits fade. 'flow': keyed by
          // global order, so the anonymous crowd migrates across a regroup (the
          // circles-to-bars effect). 'identity': keyed by each datum's id/name,
          // so a SPECIFIC unit migrates across any regroup/relayout keeping its
          // colour and size (needs the object form with unique ids/names).
          transition: "group",
          // 'circle' | 'square' | 'image' (isotype pictogram).
          shape: "circle",
          // Icon used when shape:'image'. Each unit renders this icon at the
          // given size. Set `tint:true` to recolour a monochrome icon to the
          // category colour (or a per-unit fillColor) so the pictogram matches
          // the legend; leave it off for multi-colour icons that should keep
          // their own colours.
          image: {
            src: void 0,
            width: 20,
            height: 20,
            tint: false
          },
          // dot radius in px, or 'auto' to size dots so the largest cluster
          // fits its allotted box.
          size: "auto",
          // The 'columns' layout can size its dots independently of `size`
          // (which the circle layouts / storyboard beats often pin to a
          // constant so dots do not resize while migrating). 'inherit' uses
          // `size`; 'auto' sizes the dots to fill the plot height; a number
          // pins a columns-only size. Circle / square only (image icons keep
          // their intrinsic size).
          columns: {
            size: "inherit"
          },
          // The 'grid' (waffle) layout: one lattice of cells filled in category
          // order. `columns` = cells per row. `total` (optional) fixes the cell
          // budget - e.g. 100 for a percentage waffle - and largest-remainder
          // allocates the cells to categories; leave it undefined for one cell
          // per unit (respects unitValue / maxUnits). `fillFrom` picks the first
          // row: 'bottom' (default) or 'top'.
          //
          // `split:true` switches to SMALL MULTIPLES: one mini-waffle per
          // category, arranged in a near-square trellis (or `tileColumns` per
          // row). Each tile then has `total` cells (default 100) and fills a
          // fraction equal to the category's value over `max` (default = the
          // largest count, so the leader fills its tile; set `max:100` with
          // percentage data for true "of 100" tiles). The unfilled cells show as
          // a faint `trackColor` backdrop, and each tile gets its own label.
          grid: {
            columns: 10,
            total: void 0,
            fillFrom: "bottom",
            // small-multiple (one waffle per category) mode + its knobs:
            split: false,
            // tiles per row; undefined = auto (near-square).
            tileColumns: void 0,
            // value -> filled-cell denominator; undefined = the largest count.
            max: void 0,
            // colour of the empty "track" cells; undefined = a neutral grey.
            trackColor: void 0
          },
          // The 'scatter' layout places each unit on real value axes (needs the
          // object-form data). Two modes via `y`:
          //  - `y:'lanes'` (default): a BEESWARM. X is the per-unit value axis,
          //    Y is a category lane. `spread:'swarm'` packs dots off the centre
          //    line so equal values do not overlap; 'jitter' scatters randomly.
          //  - `y:'value'`: a 2D value-value scatter. X = each datum's `x`, Y =
          //    each datum's `y`, on two numeric axes; category = colour.
          // `sizeRange:[min,max]` turns dots into BUBBLES scaled (by area) from
          // each datum's `sizeField` (default 'z') - a bubble scatter / bubble
          // beeswarm. `tickAmount`/`xMin`/`xMax`/`xTitle`/`xFormatter` control the
          // X axis; the `y*` twins the Y axis (2D only); `laneLabelWidth` the
          // lane-label gutter (lanes mode); `gridlines` the grid.
          scatter: {
            y: "lanes",
            spread: "swarm",
            // Beeswarm orientation (1D lanes mode only): 'horizontal' lays the
            // value on the X axis with category lanes stacked on Y (default);
            // 'vertical' lays the value on the Y axis with category lanes as
            // columns across X. Ignored for the 2D value-value scatter (y:'value').
            orientation: "horizontal",
            tickAmount: 5,
            xMin: void 0,
            xMax: void 0,
            xTitle: void 0,
            // (value) => string
            xFormatter: void 0,
            yTickAmount: 5,
            yMin: void 0,
            yMax: void 0,
            yTitle: void 0,
            // (value) => string
            yFormatter: void 0,
            // bubble sizing: datum key for the size value + [minR, maxR] in px.
            sizeField: "z",
            sizeRange: void 0,
            laneLabelWidth: void 0,
            gridlines: true
          },
          // Opt-in bubble sizing: scale each dot's radius by its per-unit value
          // (needs the object-form data). Circle shape only; the lattice is
          // spaced for the largest bubble so dots never overlap.
          sizeByValue: {
            enabled: false,
            // radius (px) for the largest value, or 'auto' to fit the largest
            // bubble to the plot like uniform auto-sizing.
            maxRadius: "auto",
            // radius (px) for the smallest value; defaults to ~35% of maxRadius.
            minRadius: void 0,
            // 'area' (bubble AREA proportional to value) | 'linear'.
            scale: "area"
          },
          // packing gap factor between spiral shells (1 = dots touch).
          spacing: 1.05,
          // corner radius for shape:'square'.
          borderRadius: 0,
          // How marks move between layouts on an update, and where entering
          // marks come from.
          //
          // `motion`: 'spring' settles each mark on a damped spring, so a
          // gather interrupted by the next update carries the marks' velocity
          // into it instead of restarting them from a standstill - which is
          // what a dragged slider or a scrubbed storyboard does on almost every
          // frame. 'tween' runs the fixed-duration ease below instead. 'auto'
          // (the default) is spring, unless `easing` was set to something other
          // than the default, so an explicit curve keeps working without having
          // to set `motion` as well.
          //
          // `spring`: 'crisp' (default) | 'gentle' (softer, for large reflows)
          // | 'snappy' (faster, a hint of settle). Scaled by
          // `chart.animations.speed`, which stretches the spring in time
          // without making it bouncier.
          //
          // `easing` (tween only): 'outCubic' (default: decelerate to a stop) |
          // 'inOutCubic' (accelerate gently out of rest, weighted travel) |
          // 'outBack' (overshoot each mark past its slot and spring back - a
          // per-mark settle), with `overshoot` tuning the spring strength.
          //
          // `enter`: where a fresh / appearing mark animates FROM - 'burst'
          // (default: fly out from the cluster centre) | 'fade' (materialise in
          // place) | 'rise' (fade in while drifting gently up into the slot).
          //
          // Colour, radius and opacity always stay on the out-cubic, under
          // either motion (a back ease overshoots past 1, which would push RGB
          // channels / radii out of range; and the spring's rest thresholds are
          // absolute, so they are far too coarse for a 0..1 quantity).
          gather: {
            motion: "auto",
            spring: "crisp",
            easing: "outCubic",
            overshoot: 1.70158,
            enter: "burst"
          },
          // The 'arc' layout arranges marks as a PARLIAMENT / hemicycle: seats in
          // concentric arced rows across an annulus, filled in category order so
          // each category is a contiguous wedge (the classic seating chart). Angles
          // use the radialBar convention (0 = top, clockwise); the default sweep is
          // a top semicircle (a full circle = startAngle 0, endAngle 360).
          // `innerRadiusRatio` is the donut hole (inner radius / outer); `rows`
          // fixes the number of seat rows, or 'auto' to size the dots as large as
          // fit. Like 'packed' the legend carries the category names (no per-wedge
          // labels).
          arc: {
            startAngle: -90,
            endAngle: 90,
            innerRadiusRatio: 0.4,
            rows: "auto"
          },
          // 1 dot represents this many units of value (waffle scaling).
          unitValue: 1,
          // safety cap on total dots; counts scale down proportionally above it.
          maxUnits: 5e3,
          // packed layout: order categories smallest-first so the minority
          // group nests in the centre of the blob.
          sortByGroup: true,
          clusterLabels: {
            show: true,
            // Label placement relative to the cluster/bar: 'top' (default) or
            // 'bottom'. A 'bottom' label is always straight; the curved arc
            // (below) rides the top crown only.
            position: "top",
            curved: true,
            fontSize: "13px",
            fontFamily: void 0,
            fontWeight: 600,
            // defaults to the cluster's own colour when undefined.
            color: void 0,
            offsetY: 0,
            // (name, { seriesIndex, value, percent, w }) => string
            formatter: void 0
          },
          tooltip: {
            // Per-unit tooltip body. Each dot carries its category (seriesIndex)
            // and its index within that category (dataPointIndex), so the
            // formatter can look up per-unit data and return a string/HTML.
            // ({ seriesName, seriesIndex, dataPointIndex, count, unitValue,
            //    color, w }) => string
            // Default: "#<dataPointIndex+1> of <count>".
            formatter: void 0
          }
        },
        radialBar: {
          inverseOrder: false,
          startAngle: 0,
          endAngle: 360,
          offsetX: 0,
          offsetY: 0,
          // Gauge sub-shape. 'arc' (default) renders the existing filled
          // value-arc gauge; 'needle' replaces the value-arc with a rotating
          // pointer/needle. Bands and ticks are independent and work for both
          // shapes.
          shape: "arc",
          // Value-to-angle mapping. Defaults to the existing 0..100 range
          // used by radialBar. Override for gauges that need a custom domain
          // (e.g. min: 0, max: 240 for a speedometer).
          min: 0,
          max: 100,
          // Threshold bands rendered as colored arc segments along the gauge
          // arc (e.g. [{from:0,to:30,color:'#FF4560'}, ...]). Bands draw
          // behind the value-arc and tick marks. Set to [] (default) to
          // disable.
          bands: [],
          bandsStyle: {
            // % of arc radius. Slightly less than the value-arc stroke so
            // the value-arc reads on top by default.
            strokeWidth: "40%",
            // px gap between consecutive bands.
            gap: 0,
            // Hide the track when bands cover the full range; the bands
            // themselves act as the visual backdrop.
            hideTrackWhenPresent: true
          },
          // Tick marks rendered along (outside) the gauge arc.
          ticks: {
            show: false,
            major: {
              count: 11,
              length: 10,
              width: 2,
              color: "#666",
              // 'inside' draws ticks from `radius - length` to `radius`;
              // 'outside' draws from `radius` to `radius + length`.
              placement: "outside"
            },
            minor: {
              count: 4,
              // minor ticks BETWEEN each pair of major ticks
              length: 5,
              width: 1,
              color: "#999",
              placement: "outside"
            },
            labels: {
              show: false,
              offset: 6,
              fontSize: "11px",
              fontFamily: void 0,
              fontWeight: 400,
              color: "#666",
              /** @param {number} v */
              formatter(v2) {
                return String(v2);
              }
            }
          },
          // Needle/dial configuration. Only applies when `shape: 'needle'`.
          needle: {
            color: "#333",
            // Needle length as a % of the gauge radius (string like '85%')
            // or as an absolute px number.
            length: "85%",
            // px width of the needle line at the base.
            baseWidth: 4,
            // px width of the needle tip (tapered if smaller than baseWidth).
            tipWidth: 1,
            // px offset from the geometric arc center on Y. Positive values
            // push the needle base down (toward the chord midpoint of a
            // ∩-shape gauge); negative pushes up. The needle rotates around
            // this shifted point.
            offsetY: 0,
            // When true, also render the filled value-arc alongside the
            // needle. Default false preserves the previous needle-only
            // behavior. Useful for gauges that want a progress ring plus a
            // pointer indicator.
            showValueArc: false,
            animation: {
              enabled: true,
              duration: 800,
              easing: "ease-out"
            }
          },
          hollow: {
            margin: 5,
            size: "50%",
            background: "transparent",
            image: void 0,
            imageWidth: 150,
            imageHeight: 150,
            imageOffsetX: 0,
            imageOffsetY: 0,
            imageClipped: true,
            position: "front",
            // Optional stroke around the hollow ring. Combined with
            // `strokeDasharray` this produces a dashed indicator circle
            // around the value text — useful for gauge designs where the
            // value sits inside its own boundary.
            stroke: void 0,
            strokeWidth: 1,
            strokeDasharray: void 0,
            dropShadow: {
              enabled: false,
              top: 0,
              left: 0,
              blur: 3,
              color: "#000",
              opacity: 0.5
            }
          },
          track: {
            show: true,
            startAngle: void 0,
            endAngle: void 0,
            background: "#f2f2f2",
            strokeWidth: "97%",
            opacity: 1,
            margin: 5,
            // margin is in pixels
            dropShadow: {
              enabled: false,
              top: 0,
              left: 0,
              blur: 3,
              color: "#000",
              opacity: 0.5
            }
          },
          dataLabels: {
            show: true,
            name: {
              show: true,
              fontSize: "16px",
              fontFamily: void 0,
              fontWeight: 600,
              color: void 0,
              offsetY: 0,
              /**
               * @param {any} val
               */
              formatter(val) {
                return val;
              }
            },
            value: {
              show: true,
              fontSize: "14px",
              fontFamily: void 0,
              fontWeight: 400,
              color: void 0,
              offsetY: 16,
              /**
               * @param {any} val
               */
              formatter(val) {
                return val + "%";
              }
            },
            total: {
              show: false,
              label: "Total",
              fontSize: "16px",
              fontWeight: 600,
              fontFamily: void 0,
              color: void 0,
              /**
               * @param {import('../../types/internal').ChartStateW} w
               */
              formatter(w2) {
                return (
                  /**
                   * @param {number} a
                   * @param {number} b
                   */
                  w2.globals.seriesTotals.reduce((a2, b2) => a2 + b2, 0) / w2.seriesData.series.length + "%"
                );
              }
            }
          },
          barLabels: {
            enabled: false,
            offsetX: 0,
            offsetY: 0,
            useSeriesColors: true,
            fontFamily: void 0,
            fontWeight: 600,
            fontSize: "16px",
            /**
             * @param {any} val
             */
            formatter(val) {
              return val;
            },
            onClick: void 0
          }
        },
        pie: {
          customScale: 1,
          offsetX: 0,
          offsetY: 0,
          startAngle: 0,
          endAngle: 360,
          expandOnClick: true,
          // How far a clicked slice slides out of the pie (px), measured along
          // its own mid-angle. The slice is translated, not redrawn at a bigger
          // radius, so its shape and the quantity it encodes are unchanged and
          // a gap opens between it and the rest of the pie. The inner
          // percentage label (and the outer name label, when enabled) ride
          // along with it. Ignored for polarArea, where the radius is the
          // value, and in a drilldown pie/donut, where a click navigates (the
          // slice would slide out only to be discarded by the drill, and
          // states.active takes the click feedback back over). Set 0 to keep
          // the slice in place.
          expandOffset: 10,
          // Hover outline: a translucent band traced just outside the rim of
          // the hovered slice, so the slice keeps its own colour instead of
          // being lightened. Takes the place of the states.hover filter for
          // pie / donut / polarArea, and is skipped entirely when
          // states.hover.filter.type is 'none' (that stays the way to turn all
          // hover feedback off). Applies to pie, donut and polarArea.
          hoverOutline: {
            show: true,
            size: 8,
            // band thickness (px)
            // Extra clearance between the slice rim and the band (px), ON TOP
            // of the slice stroke: the band always starts at the outer edge of
            // the stroke, never under it. Default 0, because a stroke is
            // normally present (stroke.width defaults to 2) and already reads
            // as the separation. Any gap beyond that and the band stops
            // belonging to its slice and starts reading as a ring of its own.
            gap: 0,
            opacity: 0.3,
            // band opacity, over the slice colour
            color: void 0
            // defaults to the hovered slice's colour
          },
          // Rounds the corners of each slice (in px). Applies to pie, donut and
          // polarArea (all rendered by the Pie module). 0 = sharp corners
          // (default, unchanged behavior). The value is clamped per slice so
          // opposing corner fillets never cross on thin/narrow slices.
          borderRadius: 0,
          // Gap between adjacent slices (in px). Applies to pie, donut and
          // polarArea. 0 = slices touch (default). Each slice is inset
          // symmetrically, so its mid-angle (data label + hit region) is kept.
          spacing: 0,
          dataLabels: {
            // These are the percentage values which are displayed on slice
            offset: 0,
            // offset by which labels will move outside
            minAngleToShowLabel: 10,
            // External (outer) labels: render the category/series name outside
            // the slice, joined to it by a leader (connector) line, so users
            // don't have to map legend colors back to slices. The percentage
            // keeps rendering inside the slice (governed by dataLabels.enabled).
            // Pie + donut only; ignored for polarArea (radial length already
            // encodes the value there).
            external: {
              show: false,
              // master switch for the external (outer) labels
              offsetX: 0,
              offsetY: 0,
              fontSize: void 0,
              // falls back to dataLabels.style.fontSize
              fontFamily: void 0,
              // falls back to dataLabels.style.fontFamily
              fontWeight: void 0,
              // falls back to dataLabels.style.fontWeight
              color: void 0,
              // defaults to chart.foreColor (readable text)
              /**
               * Return a string (single line) or an array of strings (stacked
               * lines, e.g. [name, percent + '%']).
               * @param {string} name
               * @param {{ seriesIndex: number, percent: number, value: number, w: any }} opts
               * @returns {string | string[]}
               */
              formatter: void 0,
              connector: {
                show: true,
                width: 1,
                color: void 0,
                // defaults to the slice color
                length: 16,
                // horizontal run after the radial elbow (px)
                gap: 6
                // radial gap from slice edge to the elbow point (px)
              }
            }
          },
          donut: {
            size: "65%",
            background: "transparent",
            labels: {
              // These are the inner labels appearing inside donut
              show: false,
              name: {
                show: true,
                fontSize: "16px",
                fontFamily: void 0,
                fontWeight: 600,
                color: void 0,
                offsetY: -10,
                /**
                 * @param {any} val
                 */
                formatter(val) {
                  return val;
                }
              },
              value: {
                show: true,
                fontSize: "20px",
                fontFamily: void 0,
                fontWeight: 400,
                color: void 0,
                offsetY: 10,
                /**
                 * @param {any} val
                 */
                formatter(val) {
                  return val;
                }
              },
              total: {
                show: false,
                showAlways: false,
                label: "Total",
                fontSize: "16px",
                fontWeight: 400,
                fontFamily: void 0,
                color: void 0,
                /**
                 * @param {import('../../types/internal').ChartStateW} w
                 */
                formatter(w2) {
                  return w2.globals.seriesTotals.reduce((a2, b2) => a2 + b2, 0);
                }
              }
            }
          }
        },
        polarArea: {
          rings: {
            strokeWidth: 1,
            strokeColor: "#e8e8e8"
          },
          spokes: {
            strokeWidth: 1,
            connectorColors: "#e8e8e8"
          }
        },
        sunburst: {
          // Sunburst / nested pie-donut (hierarchical radial). Rings go from the
          // centre hole outward, one per hierarchy level; each child arc is
          // nested inside its parent's angular wedge.
          offsetX: 0,
          offsetY: 0,
          startAngle: 0,
          endAngle: 360,
          // Centre hole radius, as a % of the max radius (like donut size).
          innerSize: "15%",
          // Corner rounding + inter-arc gap (px), same semantics as the pie
          // family (see plotOptions.pie.borderRadius / .spacing).
          borderRadius: 0,
          spacing: 1,
          // How a branch that bottoms out before the deepest level is drawn:
          // 'extend' stretches the leaf arc out to the rim (default, matches
          // d3); 'stop' leaves the outer rings empty behind it.
          leaf: "extend",
          // Angular partition of a parent's wedge among its children:
          // 'normalize' splits by each child's share of its siblings (safe for
          // any data); 'strict' expects children to sum to the parent's value.
          partition: "normalize",
          // Each depth level is tinted this much lighter than its parent colour
          // (0 = keep parent colour, 1 = white). A per-node `color` overrides.
          tint: 0.14,
          // Click a wedge to zoom into that branch (its subtree fills the
          // chart; a breadcrumb walks back). Click the focused inner ring to
          // zoom out. Set false to disable.
          zoomOnClick: true,
          dataLabels: {
            show: true,
            // Hide the label on any arc narrower than this (degrees), so tiny
            // wedges do not overflow with text.
            minAngleToShow: 8,
            style: {
              fontSize: "12px",
              fontFamily: void 0,
              fontWeight: 400,
              colors: void 0
            }
          }
        },
        radar: {
          size: void 0,
          offsetX: 0,
          offsetY: 0,
          polygons: {
            // strokeColor: '#e8e8e8', // should be deprecated in the minor version i.e 3.2
            strokeWidth: 1,
            strokeColors: "#e8e8e8",
            connectorColors: "#e8e8e8",
            fill: {
              colors: void 0
            }
          }
        }
      },
      colors: void 0,
      dataLabels: {
        enabled: true,
        enabledOnSeries: void 0,
        /**
         * @param {any} val
         */
        formatter(val) {
          return val !== null ? val : "";
        },
        textAnchor: "middle",
        distributed: false,
        offsetX: 0,
        offsetY: 0,
        style: {
          fontSize: "12px",
          fontFamily: void 0,
          fontWeight: 600,
          colors: void 0
        },
        background: {
          enabled: true,
          foreColor: "#fff",
          backgroundColor: void 0,
          borderRadius: 2,
          padding: 4,
          opacity: 0.9,
          borderWidth: 1,
          borderColor: "#fff",
          dropShadow: {
            enabled: false,
            top: 1,
            left: 1,
            blur: 1,
            color: "#000",
            opacity: 0.8
          }
        },
        dropShadow: {
          enabled: false,
          top: 1,
          left: 1,
          blur: 1,
          color: "#000",
          opacity: 0.8
        },
        // Ride data labels to their new position on a data-change update
        // (e.g. a bar chart race), instead of snapping. Off by default so
        // existing charts are unchanged. Speed/easing follow
        // chart.animations.dynamicAnimation. Bar/column only.
        animate: {
          enabled: false
        },
        // Count the numeric value up/down from its previous value on update,
        // like countUp.js. Off by default. The dataLabels.formatter runs each
        // frame, so number formatting (decimals, separators, prefixes) is
        // preserved. Bar/column only.
        countUp: {
          enabled: false
        }
      },
      fill: {
        type: "solid",
        colors: void 0,
        // array of colors
        opacity: 0.85,
        gradient: {
          shade: "dark",
          type: "horizontal",
          shadeIntensity: 0.5,
          gradientToColors: void 0,
          inverseColors: true,
          opacityFrom: 1,
          opacityTo: 1,
          stops: [0, 50, 100],
          colorStops: []
        },
        image: {
          src: [],
          width: void 0,
          // optional
          height: void 0
          // optional
        },
        pattern: {
          style: "squares",
          // String | Array of Strings
          width: 6,
          height: 6,
          strokeWidth: 2
        }
      },
      forecastDataPoints: {
        count: 0,
        fillOpacity: 0.5,
        strokeWidth: void 0,
        dashArray: 4
      },
      grid: {
        show: true,
        borderColor: "#e0e0e0",
        strokeDashArray: 0,
        position: "back",
        xaxis: {
          lines: {
            show: false
          }
        },
        yaxis: {
          lines: {
            show: true
          }
        },
        row: {
          colors: void 0,
          // takes as array which will be repeated on rows
          opacity: 0.5
        },
        column: {
          colors: void 0,
          // takes an array which will be repeated on columns
          opacity: 0.5
        },
        padding: {
          top: 0,
          right: 10,
          bottom: 0,
          left: 12
        }
      },
      labels: [],
      drilldown: {
        // Opt-in. When false, the Drilldown feature module stays inert even if
        // it was imported. Requires `import 'apexcharts/features/drilldown'`.
        enabled: false,
        // Child levels referenced by a data point's `drilldown: '<id>'` field.
        // Each: { id, name?, data, chart?, xaxis?, yaxis?, colors?, plotOptions? }
        series: [],
        breadcrumb: {
          show: true,
          position: "top-left",
          // 'top-left' | 'top-right'
          separator: " / ",
          rootLabel: "All",
          offsetX: 0,
          offsetY: 0
          // formatter: (label, { index, depth }) => label,
        },
        // Animation is delegated to the chart's update pipeline; `enabled`
        // gates whether the drill transition animates at all.
        animation: {
          enabled: true,
          // Anchor the drill transition at the clicked point: the child unfolds
          // outward from it (and settles back on drill-up) instead of the chart
          // simply re-rendering. A gentle scale layered on the SVG; opt-in.
          zoomFromPoint: false,
          // Base duration (ms) of the transition, used only when zoomFromPoint
          // is true. The fade-out phase runs a little shorter than this.
          speed: 260
        },
        // Optional async resolver, called when a drillable point has no inline
        // match in `series`. Receives ({ id, point, seriesIndex, dataPointIndex })
        // and returns a level (or a promise of one). A throw, a rejection, or a
        // resolved value without a `data` array leaves the chart exactly where
        // it was and fires `drillDownError` - a failed fetch is ordinary, not
        // a reason to strand the view.
        // onDrillDown: undefined,
        // Overlay shown while an async level resolves. `text` is optional: with
        // none, the spinner shows alone (and carries 'Loading' as its
        // accessible name), which keeps the default free of any language.
        loading: {
          show: true
          // text: 'Loading…',
        },
        // Cache levels resolved by `onDrillDown`, keyed by id, so drilling back
        // down a branch does not re-fetch it. Call `chart.drillDown` module's
        // clearCache() when the data behind an already-drilled chart changes.
        cache: true,
        // The dot that marks a drillable point on a line/area chart drawn
        // without markers. A bar, slice, tile or cell is already a visible,
        // clickable mark; a line point is not, so without this the chart would
        // give no sign that anything can be opened. Only drillable points get
        // one, which is what makes them read as openable. Set `show: false` to
        // supply your own affordance (turning `markers.size` on, for instance).
        // Omitted colours inherit the series marker defaults.
        marker: {
          show: true,
          size: 6,
          // shape: undefined,      // 'circle' | 'square' | 'rect'
          // fillColor: undefined,  // defaults to the series colour
          strokeColor: "#fff"
        }
      },
      legend: {
        show: true,
        showForSingleSeries: false,
        showForNullSeries: true,
        showForZeroSeries: true,
        floating: false,
        position: "bottom",
        // whether to position legends in 1 of 4
        // direction - top, bottom, left, right
        horizontalAlign: "center",
        // when position top/bottom, you can specify whether to align legends left, right or center
        inverseOrder: false,
        fontSize: "12px",
        fontFamily: void 0,
        fontWeight: 400,
        width: void 0,
        height: void 0,
        formatter: void 0,
        tooltipHoverFormatter: void 0,
        offsetX: -20,
        offsetY: 4,
        customLegendItems: [],
        clusterGroupedSeries: true,
        clusterGroupedSeriesOrientation: "vertical",
        labels: {
          colors: void 0,
          useSeriesColors: false
        },
        markers: {
          size: 7,
          fillColors: void 0,
          strokeWidth: 1,
          shape: void 0,
          offsetX: 0,
          offsetY: 0,
          customHTML: void 0,
          onClick: void 0
        },
        itemMargin: {
          horizontal: 5,
          vertical: 4
        },
        onItemClick: {
          toggleDataSeries: true
        },
        onItemHover: {
          highlightDataSeries: true
        }
      },
      markers: {
        discrete: [],
        size: 0,
        colors: void 0,
        strokeColors: "#fff",
        strokeWidth: 2,
        strokeOpacity: 0.9,
        strokeDashArray: 0,
        fillOpacity: 1,
        shape: "circle",
        offsetX: 0,
        offsetY: 0,
        showNullDataPoints: true,
        onClick: void 0,
        onDblClick: void 0,
        hover: {
          size: void 0,
          sizeOffset: 3
        }
      },
      noData: {
        text: void 0,
        align: "center",
        offsetX: 0,
        offsetY: 0,
        style: {
          color: void 0,
          fontSize: "14px",
          fontFamily: void 0
        }
      },
      responsive: [],
      // breakpoints should follow ascending order 400, then 700, then 1000
      series: void 0,
      states: {
        hover: {
          filter: {
            type: "lighten",
            value: 0.15
          }
        },
        active: {
          allowMultipleDataPointsSelection: false,
          filter: {
            type: "darken",
            value: 0.35
          }
        }
      },
      title: {
        text: void 0,
        align: "left",
        margin: 5,
        offsetX: 0,
        offsetY: 0,
        floating: false,
        style: {
          fontSize: "14px",
          fontWeight: 900,
          fontFamily: void 0,
          color: void 0
        }
      },
      subtitle: {
        text: void 0,
        align: "left",
        margin: 5,
        offsetX: 0,
        offsetY: 30,
        floating: false,
        style: {
          fontSize: "12px",
          fontWeight: 400,
          fontFamily: void 0,
          color: void 0
        }
      },
      stroke: {
        show: true,
        curve: "smooth",
        // "smooth" / "straight" / "monotoneCubic" / "stepline" / "linestep"
        lineCap: "butt",
        // round, butt , square
        width: 2,
        colors: void 0,
        // array of colors
        dashArray: 0,
        // single value or array of values
        fill: {
          type: "solid",
          colors: void 0,
          // array of colors
          opacity: 0.85,
          gradient: {
            shade: "dark",
            type: "horizontal",
            shadeIntensity: 0.5,
            gradientToColors: void 0,
            inverseColors: true,
            opacityFrom: 1,
            opacityTo: 1,
            stops: [0, 50, 100],
            colorStops: []
          }
        }
      },
      tooltip: {
        enabled: true,
        enabledOnSeries: void 0,
        shared: true,
        hideEmptySeries: false,
        followCursor: false,
        // when disabled, the tooltip will show on top of the series instead of mouse position
        intersect: false,
        // when enabled, tooltip will only show when user directly hovers over point
        inverseOrder: false,
        arrow: true,
        custom: void 0,
        fillSeriesColor: false,
        theme: "light",
        cssClass: "",
        style: {
          fontSize: "12px",
          fontFamily: void 0,
          background: void 0
        },
        onDatasetHover: {
          highlightDataSeries: false
        },
        x: {
          // x value
          show: true,
          format: "dd MMM",
          // dd/MM, dd MMM yy, dd MMM yyyy
          formatter: void 0
          // a custom user supplied formatter function
        },
        y: {
          formatter: void 0,
          title: {
            /**
             * @param {string} seriesName
             */
            formatter(seriesName) {
              return seriesName ? seriesName + ": " : "";
            }
          }
        },
        z: {
          formatter: void 0,
          title: "Size: "
        },
        marker: {
          show: true,
          fillColors: void 0
        },
        items: {
          display: "flex"
        },
        fixed: {
          enabled: false,
          position: "topRight",
          // topRight, topLeft, bottomRight, bottomLeft
          offsetX: 0,
          offsetY: 0
        }
      },
      xaxis: {
        type: "category",
        categories: [],
        convertedCatToNumeric: false,
        // internal property which should not be altered outside
        offsetX: 0,
        offsetY: 0,
        overwriteCategories: void 0,
        labels: {
          show: true,
          rotate: -45,
          rotateAlways: false,
          hideOverlappingLabels: true,
          trim: false,
          minHeight: void 0,
          maxHeight: 120,
          showDuplicates: true,
          style: {
            colors: [],
            fontSize: "12px",
            fontWeight: 400,
            fontFamily: void 0,
            cssClass: ""
          },
          offsetX: 0,
          offsetY: 0,
          format: void 0,
          formatter: void 0,
          // custom formatter function which will override format
          datetimeUTC: true,
          datetimeFormatter: {
            // Base format per interval unit. TimeScale.formatDates folds in
            // coarser context automatically when the data range spans it
            // (e.g. month-scale across years → 'MMM yyyy', hour-scale across
            // days → 'dd MMM HH:mm'). Customizing a base format that already
            // includes the higher-unit token disables the auto-expansion for
            // that level.
            year: "yyyy",
            month: "MMM",
            day: "dd MMM",
            hour: "HH:mm",
            minute: "HH:mm",
            second: "HH:mm:ss"
          }
        },
        group: {
          groups: [],
          style: {
            colors: [],
            fontSize: "12px",
            fontWeight: 400,
            fontFamily: void 0,
            cssClass: ""
          }
        },
        axisBorder: {
          show: true,
          color: "#e0e0e0",
          width: "100%",
          height: 1,
          offsetX: 0,
          offsetY: 0
        },
        axisTicks: {
          show: true,
          color: "#e0e0e0",
          height: 6,
          offsetX: 0,
          offsetY: 0
        },
        stepSize: void 0,
        tickAmount: void 0,
        tickPlacement: "on",
        min: void 0,
        max: void 0,
        range: void 0,
        floating: false,
        decimalsInFloat: void 0,
        position: "bottom",
        title: {
          text: void 0,
          offsetX: 0,
          offsetY: 0,
          style: {
            color: void 0,
            fontSize: "12px",
            fontWeight: 900,
            fontFamily: void 0,
            cssClass: ""
          }
        },
        crosshairs: {
          show: true,
          width: 1,
          // tickWidth/barWidth or an integer
          position: "back",
          opacity: 0.9,
          stroke: {
            color: "#b6b6b6",
            width: 1,
            dashArray: 3
          },
          fill: {
            type: "solid",
            // solid, gradient
            color: "#B1B9C4",
            gradient: {
              colorFrom: "#D8E3F0",
              colorTo: "#BED1E6",
              stops: [0, 100],
              opacityFrom: 0.4,
              opacityTo: 0.5
            }
          },
          dropShadow: {
            enabled: false,
            left: 0,
            top: 0,
            blur: 1,
            opacity: 0.8
          }
        },
        tooltip: {
          enabled: false,
          offsetY: 0,
          formatter: void 0,
          style: {
            fontSize: "12px",
            fontFamily: void 0
          }
        }
      },
      yaxis: this.yAxis,
      theme: {
        mode: "",
        palette: "palette1",
        // If defined, it will overwrite globals.colors variable
        // Facet (#13): read `--apx-*` CSS design tokens from the cascade
        // (accent/fore/grid/surface + series-1..N). true (default) reads any
        // present (absence is a no-op); false disables. Tokens top the
        // resolution chain below explicit config. Tokens are re-read on every
        // render; call chart.refreshTokens() to pick up a runtime CSS change
        // that does not itself trigger a render.
        tokens: true,
        // Facet (#13): 'os' follows prefers-color-scheme + prefers-contrast
        // reactively (SSR-safe, cleaned up on destroy). false disables.
        follow: false,
        // 'os' | false
        // Facet (#13): a theme registered via ApexCharts.registerTheme(name, def)
        name: "",
        // '' | registered theme name
        monochrome: {
          // monochrome allows you to select just 1 color and fill out the rest with light/dark shade (intensity can be selected)
          enabled: false,
          color: "#008FFB",
          shadeTo: "light",
          shadeIntensity: 0.65
        },
        accessibility: {
          colorBlindMode: ""
          // '' | 'deuteranopia' | 'protanopia' | 'tritanopia' | 'highContrast'
        }
      }
    };
  }
}
const TYPE_OWNED = "_apexOwnedByType";
const ownedBy = (types, fn) => {
  const marked = (
    /** @type {any} */
    fn
  );
  marked[TYPE_OWNED] = types;
  return fn;
};
const TYPE_OWNED_PATHS = [
  // What the tooltip reads and how it resolves a hover. `custom` is the sharp
  // one: each built-in reads globals only its own type fills.
  "tooltip.custom",
  "tooltip.shared",
  "tooltip.intersect",
  "tooltip.followCursor",
  // Whether values are written on the marks, and how they are phrased. A pie's
  // percentage formatter handed a treemap a category name.
  "dataLabels.enabled",
  "dataLabels.formatter",
  "plotOptions.bar.dataLabels.position",
  // A box plot's outlier markers are hit targets; a violin draws none.
  "markers.size",
  // Hover and select feedback, off by design on the types that draw their own.
  "states.hover.filter.type",
  "states.active.filter.type",
  // Axis chrome that exists to be pointed at.
  "xaxis.crosshairs.width",
  "xaxis.tickPlacement",
  "xaxis.tooltip.enabled",
  // Interaction the type either supports or does not: a violin's categories
  // cannot be range-zoomed, and an index-keyed summary animates as churn.
  "chart.zoom.enabled",
  "chart.animations.dynamicAnimation.enabled"
];
const readPath = (obj, path) => {
  let cur = (
    /** @type {any} */
    obj
  );
  for (const key of path.split(".")) {
    if (cur == null || typeof cur !== "object") return void 0;
    cur = cur[key];
  }
  return cur;
};
const writePath = (obj, path, value) => {
  const keys = path.split(".");
  const last = keys.pop();
  let cur = (
    /** @type {any} */
    obj
  );
  for (const key of keys) {
    if (cur[key] == null || typeof cur[key] !== "object") cur[key] = {};
    cur = cur[key];
  }
  if (value === void 0) delete cur[
    /** @type {string} */
    last
  ];
  else cur[
    /** @type {string} */
    last
  ] = value;
};
const TYPE_ALIASES = {
  funnel: "bar",
  pyramid: "bar",
  gauge: "radialBar",
  waffle: "unit",
  histogram: "bar"
};
const typeOpts = (type, config) => {
  var _a, _b;
  const base = (
    /** @type {Record<string, string>} */
    TYPE_ALIASES[type]
  );
  return {
    chart: {
      type: base || type,
      requestedType: base ? type : void 0,
      // Stacking belongs to the chart, not to its type, so both sides see it.
      stacked: (_a = config.chart) == null ? void 0 : _a.stacked
    },
    plotOptions: {
      bar: { isFunnel: type === "funnel" || type === "pyramid" },
      histogram: (_b = config.plotOptions) == null ? void 0 : _b.histogram
    },
    // The histogram's defaults read the series to decide whether overlaid bins
    // share a tooltip. Both sides are asked with the series the chart has now.
    series: config.series,
    yaxis: [{ title: {}, labels: {}, axisBorder: {}, axisTicks: {} }]
  };
};
const isUntouched = (current, fromDefault) => {
  if (typeof current === "function") {
    if (Array.isArray(
      /** @type {any} */
      current[TYPE_OWNED]
    )) return true;
    return typeof fromDefault === "function" && String(current) === String(fromDefault);
  }
  if (typeof fromDefault === "function") return current === void 0;
  if (current === fromDefault) return true;
  try {
    return JSON.stringify(current) === JSON.stringify(fromDefault);
  } catch (e2) {
    return false;
  }
};
const getRangeValues = ({
  isTimeline,
  seriesIndex,
  dataPointIndex,
  y1,
  y2,
  w: w2
}) => {
  var _a;
  let start = w2.rangeData.seriesRangeStart[seriesIndex][dataPointIndex];
  let end = w2.rangeData.seriesRangeEnd[seriesIndex][dataPointIndex];
  let ylabel = w2.labelData.labels[dataPointIndex];
  let seriesName = w2.config.series[seriesIndex].name ? w2.config.series[seriesIndex].name : "";
  const yLbFormatter = w2.formatters.ttKeyFormatter;
  const yLbTitleFormatter = w2.config.tooltip.y.title.formatter;
  const opts = {
    w: w2,
    seriesIndex,
    dataPointIndex,
    start,
    end
  };
  if (typeof yLbTitleFormatter === "function") {
    seriesName = yLbTitleFormatter(seriesName, opts);
  }
  if ((_a = w2.config.series[seriesIndex].data[dataPointIndex]) == null ? void 0 : _a.x) {
    ylabel = w2.config.series[seriesIndex].data[dataPointIndex].x;
  }
  if (!isTimeline) {
    if (w2.config.xaxis.type === "datetime") {
      const xFormat = new Formatters(w2);
      ylabel = xFormat.xLabelFormat(
        w2.formatters.ttKeyFormatter,
        ylabel,
        ylabel,
        {
          i: void 0,
          dateFormatter: new DateTime(w2).formatDate,
          w: w2
        }
      );
    }
  }
  if (typeof yLbFormatter === "function") {
    ylabel = yLbFormatter(ylabel, opts);
  }
  if (Number.isFinite(y1) && Number.isFinite(y2)) {
    start = y1;
    end = y2;
  }
  let startVal = "";
  let endVal = "";
  const color = w2.globals.colors[seriesIndex];
  if (w2.config.tooltip.x.formatter === void 0) {
    if (w2.config.xaxis.type === "datetime") {
      const datetimeObj = new DateTime(w2);
      startVal = datetimeObj.formatDate(
        datetimeObj.getDate(start),
        w2.config.tooltip.x.format
      );
      endVal = datetimeObj.formatDate(
        datetimeObj.getDate(end),
        w2.config.tooltip.x.format
      );
    } else {
      startVal = start;
      endVal = end;
    }
  } else {
    startVal = w2.config.tooltip.x.formatter(start);
    endVal = w2.config.tooltip.x.formatter(end);
  }
  return { start, end, startVal, endVal, ylabel, color, seriesName };
};
const buildRangeTooltipHTML = (opts) => {
  let { color, seriesName, ylabel, start, end, seriesIndex, dataPointIndex } = opts;
  const formatter = opts.w.globals.tooltip.tooltipLabels.getFormatters(seriesIndex);
  start = formatter.yLbFormatter(start);
  end = formatter.yLbFormatter(end);
  const val = formatter.yLbFormatter(
    opts.w.seriesData.series[seriesIndex][dataPointIndex]
  );
  let valueHTML = "";
  const rangeValues = `<span class="value start-value">
  ${start}
  </span> <span class="separator">-</span> <span class="value end-value">
  ${end}
  </span>`;
  if (opts.w.globals.comboCharts) {
    if (opts.w.config.series[seriesIndex].type === "rangeArea" || opts.w.config.series[seriesIndex].type === "rangeBar") {
      valueHTML = rangeValues;
    } else {
      valueHTML = `<span>${val}</span>`;
    }
  } else {
    valueHTML = rangeValues;
  }
  return '<div class="apexcharts-tooltip-rangebar"><div> <span class="series-name" style="color: ' + color + '">' + (seriesName ? seriesName : "") + '</span></div><div> <span class="category">' + ylabel + ": </span> " + valueHTML + " </div></div>";
};
class Defaults {
  /**
   * @param {Record<string, any>} opts
   */
  constructor(opts) {
    this.opts = opts;
  }
  /**
   * The defaults a chart type chooses for itself, which is the same pick
   * Config.init makes on the initial render. Shared with the update path so
   * that "what does type X want" has one answer and cannot drift between the
   * two. Modes layered on top of a type (brush, slope, sparkline) are not
   * included: they belong to the chart, not to its type, and a type change does
   * not disturb them.
   *
   * @param {Record<string, any>} opts a config, read for the type and for the
   *   flags that pick a variant of it
   * @returns {Record<string, any>}
   */
  static forType(opts) {
    var _a, _b;
    const defaults = new Defaults(opts);
    const chartTypes = [
      "line",
      "area",
      "bar",
      "candlestick",
      "boxPlot",
      "violin",
      "rangeBar",
      "rangeArea",
      "bubble",
      "scatter",
      "heatmap",
      "treemap",
      "unit",
      "sunburst",
      "pie",
      "polarArea",
      "donut",
      "radar",
      "radialBar"
    ];
    const requestedType = opts.chart.requestedType;
    let chartDefaults;
    if (requestedType === "funnel" || requestedType === "pyramid") {
      chartDefaults = /** @type {any} */
      defaults[requestedType]();
    } else if (requestedType === "gauge") {
      chartDefaults = defaults.gauge();
    } else if (requestedType === "histogram") {
      chartDefaults = defaults.histogram();
    } else if (chartTypes.indexOf(opts.chart.type) !== -1) {
      chartDefaults = /** @type {any} */
      defaults[opts.chart.type]();
    } else {
      chartDefaults = defaults.line();
    }
    if ((_b = (_a = opts.plotOptions) == null ? void 0 : _a.bar) == null ? void 0 : _b.isFunnel) {
      chartDefaults = defaults.funnel();
    }
    if (opts.chart.stacked && opts.chart.type === "bar") {
      chartDefaults = defaults.stackedBars();
    }
    return chartDefaults;
  }
  /**
   * Re-choose the type-owned leaves (see TYPE_OWNED_PATHS) after
   * `updateOptions({ chart: { type } })` moved the chart from one type to
   * another, so the chart behaves as the type it now is.
   *
   * A leaf is only handed over when it still holds exactly what `fromType`
   * chose for it. That single test covers every way a value can be the user's
   * instead of ours: set at construction, set by an earlier update, or set by
   * this very update. `options` is consulted too, so an explicit ask in the
   * same call wins even when it happens to equal the outgoing default.
   *
   * @param {Record<string, any>} config the merged w.config, mutated in place
   * @param {string} fromType the type (or requested alias) before this update
   * @param {Record<string, any>} [options] this update's payload
   */
  static handOverTypeDefaults(config, fromType, options2) {
    const toType = config.chart.requestedType || config.chart.type;
    if (!fromType || fromType === toType) return;
    const base = new Options().init();
    const from = Utils$1.extend(
      base,
      Defaults.forType(typeOpts(fromType, config))
    );
    const to = Utils$1.extend(base, Defaults.forType(typeOpts(toType, config)));
    for (const path of TYPE_OWNED_PATHS) {
      if (options2 && readPath(options2, path) !== void 0) continue;
      const fromDefault = readPath(from, path);
      const toDefault = readPath(to, path);
      if (fromDefault === void 0 && toDefault === void 0) continue;
      if (!isUntouched(readPath(config, path), fromDefault)) continue;
      writePath(config, path, toDefault);
    }
  }
  hideYAxis() {
    this.opts.yaxis[0].show = false;
    this.opts.yaxis[0].title.text = "";
    this.opts.yaxis[0].axisBorder.show = false;
    this.opts.yaxis[0].axisTicks.show = false;
    this.opts.yaxis[0].floating = true;
  }
  line() {
    return {
      dataLabels: {
        enabled: false
      },
      stroke: {
        width: 5,
        curve: "straight"
      },
      markers: {
        size: 0,
        hover: {
          sizeOffset: 6
        }
      },
      xaxis: {
        crosshairs: {
          width: 1
        }
      }
    };
  }
  /**
   * @param {Record<string, any>} defaults
   */
  sparkline(defaults) {
    this.hideYAxis();
    const ret = {
      grid: {
        show: false,
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      },
      legend: {
        show: false
      },
      xaxis: {
        labels: {
          show: false
        },
        tooltip: {
          enabled: false
        },
        axisBorder: {
          show: false
        },
        axisTicks: {
          show: false
        }
      },
      chart: {
        toolbar: {
          show: false
        },
        zoom: {
          enabled: false
        }
      },
      dataLabels: {
        enabled: false
      }
    };
    return Utils$1.extend(defaults, ret);
  }
  slope() {
    this.hideYAxis();
    return {
      chart: {
        toolbar: {
          show: false
        },
        zoom: {
          enabled: false
        }
      },
      dataLabels: {
        enabled: true,
        /**
         * @param {any} val
         * @param {Record<string, any>} opts
         */
        formatter(val, opts) {
          const seriesName = opts.w.config.series[opts.seriesIndex].name;
          return val !== null ? seriesName + ": " + val : "";
        },
        background: {
          enabled: false
        },
        offsetX: -5
      },
      grid: {
        xaxis: {
          lines: {
            show: true
          }
        },
        yaxis: {
          lines: {
            show: false
          }
        }
      },
      xaxis: {
        position: "top",
        labels: {
          style: {
            fontSize: 14,
            fontWeight: 900
          }
        },
        tooltip: {
          enabled: false
        },
        crosshairs: {
          show: false
        }
      },
      markers: {
        size: 8,
        hover: {
          sizeOffset: 1
        }
      },
      legend: {
        show: false
      },
      tooltip: {
        shared: false,
        intersect: true,
        followCursor: true
      },
      stroke: {
        width: 5,
        curve: "straight"
      }
    };
  }
  bar() {
    return {
      chart: {
        stacked: false
      },
      plotOptions: {
        bar: {
          dataLabels: {
            position: "center"
          }
        }
      },
      dataLabels: {
        style: {
          colors: ["#fff"]
        },
        background: {
          enabled: false
        }
      },
      stroke: {
        width: 0,
        lineCap: "square"
      },
      fill: {
        opacity: 0.85
      },
      legend: {
        markers: {
          shape: "square"
        }
      },
      tooltip: {
        shared: false,
        intersect: true
      },
      xaxis: {
        tooltip: {
          enabled: false
        },
        tickPlacement: "between",
        crosshairs: {
          width: "barWidth",
          position: "back",
          fill: {
            type: "gradient"
          },
          dropShadow: {
            enabled: false
          },
          stroke: {
            width: 0
          }
        }
      }
    };
  }
  funnel() {
    this.hideYAxis();
    return __spreadProps(__spreadValues({}, this.bar()), {
      chart: {
        animations: {
          speed: 800,
          animateGradually: {
            enabled: false
          }
        }
      },
      plotOptions: {
        bar: {
          horizontal: true,
          borderRadiusApplication: "around",
          borderRadius: 0,
          dataLabels: {
            position: "center"
          }
        }
      },
      grid: {
        show: false,
        padding: {
          left: 0,
          right: 0
        }
      },
      xaxis: {
        labels: {
          show: false
        },
        tooltip: {
          enabled: false
        },
        axisBorder: {
          show: false
        },
        axisTicks: {
          show: false
        }
      }
    });
  }
  pyramid() {
    return this.funnel();
  }
  gauge() {
    const base = this.radialBar();
    return __spreadProps(__spreadValues({}, base), {
      plotOptions: {
        radialBar: {
          startAngle: -135,
          endAngle: 135,
          hollow: {
            margin: 0,
            size: "60%"
          },
          track: {
            background: "#e7e7e7",
            strokeWidth: "100%",
            margin: 5
          },
          dataLabels: {
            name: {
              show: false
            },
            value: {
              show: true,
              fontSize: "32px",
              fontWeight: 600,
              offsetY: 8
            }
          }
        }
      }
    });
  }
  histogram() {
    var _a, _b, _c, _d;
    const overlaid = Array.isArray((_a = this.opts) == null ? void 0 : _a.series) && this.opts.series.length > 1 && ((_d = (_c = (_b = this.opts) == null ? void 0 : _b.plotOptions) == null ? void 0 : _c.histogram) == null ? void 0 : _d.overlap) !== false;
    return __spreadProps(__spreadValues({}, this.bar()), {
      chart: {
        stacked: false,
        // The bins are the summary: they are chosen once from the whole sample
        // and do NOT re-derive from the visible window, so zooming only
        // magnifies bars while hiding the rest of the distribution the shape is
        // read against. Off by default, mirroring heatmap and violin; users can
        // opt back in with chart.zoom.enabled: true.
        zoom: {
          enabled: false
        },
        animations: {
          // The bars of a histogram are one shape, not N independent
          // categories, so revealing them one by one reads as a sequence that
          // is not in the data. The distribution rises as a whole instead.
          animateGradually: {
            enabled: false
          }
        }
      },
      plotOptions: {
        bar: {
          // Bins are adjacent by definition, so the columns touch: a gap
          // between them would read as a gap in the data. Rounded corners are
          // dropped for the same reason (they shave area off each bar, and a
          // histogram's whole claim is that area is proportional to count).
          columnWidth: "100%",
          borderRadius: 0,
          dataLabels: {
            position: "top"
          }
        }
      },
      dataLabels: {
        enabled: false
      },
      fill: overlaid ? { opacity: 0.65 } : {},
      // A hairline separator keeps the bin boundaries readable once the columns
      // touch, the same treatment heatmap cells get.
      stroke: overlaid ? { show: false } : {
        show: true,
        width: 1,
        colors: ["#fff"]
      },
      xaxis: {
        type: "numeric",
        // The axis carries bin midpoints; the range is what people read, and
        // the tooltip already states it.
        tooltip: {
          enabled: false
        }
      },
      tooltip: {
        // Overlaid bars share a bin, so hovering one has to report both
        // distributions: the comparison is the whole point of stacking them on
        // one axis.
        shared: overlaid,
        intersect: false,
        x: {
          formatter: (val, opts) => {
            var _a2, _b2;
            const edges = (_b2 = (_a2 = opts == null ? void 0 : opts.w) == null ? void 0 : _a2.histogramData) == null ? void 0 : _b2.edges;
            const k = opts == null ? void 0 : opts.dataPointIndex;
            if (!Array.isArray(edges) || typeof k !== "number" || k < 0) {
              return String(val);
            }
            const lo = edges[k];
            const hi = edges[k + 1];
            if (lo === void 0 || hi === void 0) return String(val);
            const fmt = (v2) => Number.isInteger(v2) ? String(v2) : v2.toFixed(2);
            return `${fmt(lo)} to ${fmt(hi)}`;
          }
        }
      }
    });
  }
  candlestick() {
    return {
      stroke: {
        width: 1
      },
      fill: {
        opacity: 1
      },
      dataLabels: {
        enabled: false
      },
      tooltip: {
        shared: true,
        custom: ownedBy(
          ["candlestick"],
          ({ seriesIndex, dataPointIndex, w: w2 }) => {
            return this._getBoxTooltip(
              w2,
              seriesIndex,
              dataPointIndex,
              ["Open", "High", "", "Low", "Close"],
              "candlestick"
            );
          }
        )
      },
      states: {
        active: {
          filter: {
            type: "none"
          }
        }
      },
      xaxis: {
        crosshairs: {
          width: 1
        }
      }
    };
  }
  boxPlot() {
    return {
      chart: {
        animations: {
          dynamicAnimation: {
            enabled: false
          }
        }
      },
      stroke: {
        width: 1,
        colors: ["#24292e"]
      },
      dataLabels: {
        enabled: false
      },
      tooltip: {
        shared: true,
        custom: ownedBy(
          ["boxPlot"],
          ({ seriesIndex, dataPointIndex, w: w2 }) => {
            return this._getBoxTooltip(
              w2,
              seriesIndex,
              dataPointIndex,
              ["Minimum", "Q1", "Median", "Q3", "Maximum"],
              "boxPlot"
            );
          }
        )
      },
      markers: {
        size: 7,
        strokeWidth: 1,
        strokeColors: "#111"
      },
      xaxis: {
        crosshairs: {
          width: 1
        }
      }
    };
  }
  violin() {
    return {
      chart: {
        // Violins are a per-category distribution plot (discrete category
        // x-axis), so range zooming/panning is meaningless — off by default.
        zoom: {
          enabled: false
        },
        animations: {
          dynamicAnimation: {
            enabled: false
          }
        }
      },
      stroke: {
        width: 1,
        colors: ["#24292e"]
      },
      fill: {
        opacity: 0.7
      },
      dataLabels: {
        enabled: false
      },
      tooltip: {
        shared: true,
        custom: ownedBy(
          ["violin"],
          ({ seriesIndex, dataPointIndex, w: w2 }) => {
            return this._getViolinTooltip(w2, seriesIndex, dataPointIndex);
          }
        )
      },
      states: {
        active: {
          filter: {
            type: "none"
          }
        }
      },
      xaxis: {
        crosshairs: {
          width: 1
        }
      }
    };
  }
  rangeBar() {
    const handleTimelineTooltip = (opts) => {
      const { color, seriesName, ylabel, startVal, endVal } = getRangeValues(__spreadProps(__spreadValues({}, opts), {
        isTimeline: true
      }));
      return buildRangeTooltipHTML(__spreadProps(__spreadValues({}, opts), {
        color,
        seriesName,
        ylabel,
        start: startVal,
        end: endVal
      }));
    };
    const handleRangeColumnTooltip = (opts) => {
      const { color, seriesName, ylabel, start, end } = getRangeValues(opts);
      return buildRangeTooltipHTML(__spreadProps(__spreadValues({}, opts), {
        color,
        seriesName,
        ylabel,
        start,
        end
      }));
    };
    return {
      chart: {
        animations: {
          animateGradually: false
        }
      },
      stroke: {
        width: 0,
        lineCap: "square"
      },
      plotOptions: {
        bar: {
          borderRadius: 0,
          dataLabels: {
            position: "center"
          }
        }
      },
      dataLabels: {
        enabled: false,
        /**
         * @param {any} val
         */
        formatter(val, { seriesIndex, dataPointIndex, w: w2 }) {
          const getVal = () => {
            const start = w2.rangeData.seriesRangeStart[seriesIndex][dataPointIndex];
            const end = w2.rangeData.seriesRangeEnd[seriesIndex][dataPointIndex];
            return end - start;
          };
          if (w2.globals.comboCharts) {
            if (w2.config.series[seriesIndex].type === "rangeBar" || w2.config.series[seriesIndex].type === "rangeArea") {
              return getVal();
            } else {
              return val;
            }
          } else {
            return getVal();
          }
        },
        background: {
          enabled: false
        },
        style: {
          colors: ["#fff"]
        }
      },
      markers: {
        size: 10
      },
      tooltip: {
        shared: false,
        followCursor: true,
        custom: ownedBy(
          ["rangeBar"],
          /** @param {Record<string, any>} opts */
          (opts) => {
            if (opts.w.config.plotOptions && opts.w.config.plotOptions.bar && opts.w.config.plotOptions.bar.horizontal) {
              return handleTimelineTooltip(opts);
            } else {
              return handleRangeColumnTooltip(opts);
            }
          }
        )
      },
      xaxis: {
        tickPlacement: "between",
        tooltip: {
          enabled: false
        },
        crosshairs: {
          stroke: {
            width: 0
          }
        }
      }
    };
  }
  /**
   * @param {Record<string, any>} opts
   */
  dumbbell(opts) {
    var _a, _b;
    if (!((_a = opts.plotOptions.bar) == null ? void 0 : _a.barHeight)) {
      opts.plotOptions.bar.barHeight = 2;
    }
    if (!((_b = opts.plotOptions.bar) == null ? void 0 : _b.columnWidth)) {
      opts.plotOptions.bar.columnWidth = 2;
    }
    return opts;
  }
  area() {
    return {
      stroke: {
        width: 4,
        fill: {
          type: "solid",
          gradient: {
            inverseColors: false,
            shade: "light",
            type: "vertical",
            opacityFrom: 0.65,
            opacityTo: 0.5,
            stops: [0, 100, 100]
          }
        }
      },
      fill: {
        type: "gradient",
        gradient: {
          inverseColors: false,
          shade: "light",
          type: "vertical",
          opacityFrom: 0.65,
          opacityTo: 0.5,
          stops: [0, 100, 100]
        }
      },
      markers: {
        size: 0,
        hover: {
          sizeOffset: 6
        }
      },
      tooltip: {
        followCursor: false
      }
    };
  }
  rangeArea() {
    const handleRangeAreaTooltip = (opts) => {
      const { color, seriesName, ylabel, start, end } = getRangeValues(opts);
      return buildRangeTooltipHTML(__spreadProps(__spreadValues({}, opts), {
        color,
        seriesName,
        ylabel,
        start,
        end
      }));
    };
    return {
      stroke: {
        curve: "straight",
        width: 0
      },
      fill: {
        type: "solid",
        opacity: 0.6
      },
      markers: {
        size: 0
      },
      states: {
        hover: {
          filter: {
            type: "none"
          }
        },
        active: {
          filter: {
            type: "none"
          }
        }
      },
      tooltip: {
        intersect: false,
        shared: true,
        followCursor: true,
        custom: ownedBy(
          ["rangeArea"],
          /** @param {Record<string, any>} opts */
          (opts) => handleRangeAreaTooltip(opts)
        )
      }
    };
  }
  /**
   * @param {Record<string, any>} defaults
   */
  brush(defaults) {
    const ret = {
      chart: {
        toolbar: {
          autoSelected: "selection",
          show: false
        },
        zoom: {
          enabled: false
        }
      },
      dataLabels: {
        enabled: false
      },
      stroke: {
        width: 1
      },
      tooltip: {
        enabled: false
      },
      xaxis: {
        tooltip: {
          enabled: false
        }
      }
    };
    return Utils$1.extend(defaults, ret);
  }
  /**
   * @param {Record<string, any>} opts
   */
  stacked100(opts) {
    opts.dataLabels = opts.dataLabels || {};
    opts.dataLabels.formatter = opts.dataLabels.formatter || void 0;
    const existingDataLabelFormatter = opts.dataLabels.formatter;
    opts.yaxis.forEach((yaxe, index) => {
      opts.yaxis[index].min = 0;
      opts.yaxis[index].max = 100;
    });
    const isBar = opts.chart.type === "bar";
    if (isBar) {
      opts.dataLabels.formatter = existingDataLabelFormatter || /**
       * @param {any} val
       */
      function(val) {
        if (typeof val === "number") {
          return val ? val.toFixed(0) + "%" : val;
        }
        return val;
      };
    }
    return opts;
  }
  stackedBars() {
    const barDefaults = this.bar();
    return __spreadProps(__spreadValues({}, barDefaults), {
      plotOptions: __spreadProps(__spreadValues({}, barDefaults.plotOptions), {
        bar: __spreadProps(__spreadValues({}, barDefaults.plotOptions.bar), {
          borderRadiusApplication: "end",
          borderRadiusWhenStacked: "last"
        })
      })
    });
  }
  // This function removes the left and right spacing in chart for line/area/scatter if xaxis type = category for those charts by converting xaxis = numeric. Numeric/Datetime xaxis prevents the unnecessary spacing in the left/right of the chart area
  /**
   * @param {Record<string, any>} opts
   */
  convertCatToNumeric(opts) {
    opts.xaxis.convertedCatToNumeric = true;
    return opts;
  }
  /**
   * @param {Record<string, any>} opts
   * @param {any} cats
   */
  convertCatToNumericXaxis(opts, cats) {
    opts.xaxis.type = "numeric";
    opts.xaxis.labels = opts.xaxis.labels || {};
    opts.xaxis.labels.formatter = opts.xaxis.labels.formatter || /**
     * @param {any} val
     */
    function(val) {
      return Utils$1.isNumber(val) ? Math.floor(val) : val;
    };
    const defaultFormatter = opts.xaxis.labels.formatter;
    let labels = opts.xaxis.categories && opts.xaxis.categories.length ? opts.xaxis.categories : opts.labels;
    if (cats && cats.length) {
      labels = cats.map((c2) => {
        return Array.isArray(c2) ? c2 : String(c2);
      });
    }
    if (labels && labels.length) {
      opts.xaxis.labels.formatter = function(val) {
        return Utils$1.isNumber(val) ? defaultFormatter(labels[Math.floor(val) - 1]) : defaultFormatter(val);
      };
    }
    opts.xaxis.categories = [];
    opts.labels = [];
    opts.xaxis.tickAmount = opts.xaxis.tickAmount || "dataPoints";
    return opts;
  }
  bubble() {
    return {
      dataLabels: {
        style: {
          colors: ["#fff"]
        }
      },
      tooltip: {
        shared: false,
        intersect: true
      },
      xaxis: {
        crosshairs: {
          width: 0
        }
      },
      fill: {
        type: "solid",
        gradient: {
          shade: "light",
          inverse: true,
          shadeIntensity: 0.55,
          opacityFrom: 0.4,
          opacityTo: 0.8
        }
      }
    };
  }
  scatter() {
    return {
      dataLabels: {
        enabled: false
      },
      tooltip: {
        shared: false,
        intersect: true
      },
      markers: {
        size: 6,
        strokeWidth: 1,
        hover: {
          sizeOffset: 2
        }
      }
    };
  }
  heatmap() {
    return {
      chart: {
        stacked: false,
        // A heatmap is a fixed grid: zooming/panning only distorts the cells
        // and (on a datetime axis) collapses the month labels to repeats, so
        // it is off by default, mirroring treemap. Users can opt back in with
        // chart.zoom.enabled: true.
        zoom: {
          enabled: false
        }
      },
      fill: {
        opacity: 1
      },
      dataLabels: {
        style: {
          colors: ["#fff"]
        }
      },
      stroke: {
        colors: ["#fff"]
      },
      tooltip: {
        // Anchor the tooltip above the hovered cell with a downward arrow
        // (flipping below near the top edge), the same treatment horizontal
        // bars get, rather than trailing the cursor. Opt back into the old
        // behavior with tooltip.followCursor: true.
        followCursor: false,
        marker: {
          show: false
        },
        x: {
          show: false
        }
      },
      legend: {
        position: "top",
        markers: {
          shape: "square"
        }
      },
      grid: {
        padding: {
          right: 20
        }
      }
    };
  }
  treemap() {
    return {
      chart: {
        zoom: {
          enabled: false
        }
      },
      dataLabels: {
        style: {
          fontSize: 14,
          fontWeight: 600,
          colors: ["#fff"]
        }
      },
      stroke: {
        show: true,
        width: 2,
        colors: ["#fff"]
      },
      legend: {
        show: false
      },
      fill: {
        opacity: 1,
        gradient: {
          stops: [0, 100]
        }
      },
      tooltip: {
        followCursor: true,
        x: {
          show: false
        }
      },
      grid: {
        padding: {
          left: 0,
          right: 0
        }
      },
      xaxis: {
        crosshairs: {
          show: false
        },
        tooltip: {
          enabled: false
        },
        // A treemap has no x axis to read: the tiles are the whole plot, and
        // the ticks mark data-point positions that mean nothing here. One tick
        // per row is invisible at a dozen rows and a solid comb under the plot
        // at several hundred, so they are off by default. Set
        // `xaxis.axisTicks.show: true` to bring them back.
        axisTicks: {
          show: false
        }
      }
    };
  }
  unit() {
    return {
      chart: {
        toolbar: {
          show: false
        }
      },
      dataLabels: {
        enabled: false
      },
      stroke: {
        show: false,
        width: 0
      },
      fill: {
        opacity: 1
      },
      tooltip: {
        followCursor: true,
        x: {
          show: false
        }
      },
      legend: {
        show: true,
        position: "bottom"
      },
      grid: {
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      }
    };
  }
  sunburst() {
    return {
      chart: {
        toolbar: {
          show: false
        }
      },
      dataLabels: {
        style: {
          colors: ["#fff"]
        },
        dropShadow: {
          enabled: true
        }
      },
      stroke: {
        colors: ["#fff"]
      },
      fill: {
        opacity: 1
      },
      // Unlike pie, sunburst keeps the STANDARD themed tooltip (light/dark).
      // Slice-coloured tooltips (fillSeriesColor) wash out here because child
      // arcs are tinted toward white per depth; users can still opt in.
      legend: {
        position: "right"
      },
      grid: {
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      }
    };
  }
  pie() {
    return {
      chart: {
        toolbar: {
          show: false
        }
      },
      plotOptions: {
        pie: {
          donut: {
            labels: {
              show: false
            }
          }
        }
      },
      dataLabels: {
        /**
         * The share of the whole, as a percentage. Guarded because a config
         * survives a chart-type change: a pie that becomes a treemap hands
         * this same default a category NAME, and a default of ours must not
         * throw on a value it was never designed for.
         * @param {any} val
         */
        formatter(val) {
          return typeof val === "number" ? val.toFixed(1) + "%" : val;
        },
        style: {
          colors: ["#fff"]
        },
        background: {
          enabled: false
        },
        dropShadow: {
          enabled: true
        }
      },
      stroke: {
        colors: ["#fff"]
      },
      fill: {
        opacity: 1,
        gradient: {
          shade: "light",
          stops: [0, 100]
        }
      },
      tooltip: {
        theme: "dark",
        fillSeriesColor: true
      },
      legend: {
        position: "right"
      },
      grid: {
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      }
    };
  }
  donut() {
    return {
      chart: {
        toolbar: {
          show: false
        }
      },
      dataLabels: {
        /**
         * The share of the whole, as a percentage. Guarded because a config
         * survives a chart-type change: a pie that becomes a treemap hands
         * this same default a category NAME, and a default of ours must not
         * throw on a value it was never designed for.
         * @param {any} val
         */
        formatter(val) {
          return typeof val === "number" ? val.toFixed(1) + "%" : val;
        },
        style: {
          colors: ["#fff"]
        },
        background: {
          enabled: false
        },
        dropShadow: {
          enabled: true
        }
      },
      stroke: {
        colors: ["#fff"]
      },
      fill: {
        opacity: 1,
        gradient: {
          shade: "light",
          shadeIntensity: 0.35,
          stops: [80, 100],
          opacityFrom: 1,
          opacityTo: 1
        }
      },
      tooltip: {
        theme: "dark",
        fillSeriesColor: true
      },
      legend: {
        position: "right"
      },
      grid: {
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      }
    };
  }
  polarArea() {
    return {
      chart: {
        toolbar: {
          show: false
        }
      },
      dataLabels: {
        /**
         * The share of the whole, as a percentage. Guarded because a config
         * survives a chart-type change: a pie that becomes a treemap hands
         * this same default a category NAME, and a default of ours must not
         * throw on a value it was never designed for.
         * @param {any} val
         */
        formatter(val) {
          return typeof val === "number" ? val.toFixed(1) + "%" : val;
        },
        enabled: false
      },
      stroke: {
        show: true,
        width: 2
      },
      fill: {
        opacity: 0.7
      },
      tooltip: {
        theme: "dark",
        fillSeriesColor: true
      },
      legend: {
        position: "right"
      },
      grid: {
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      }
    };
  }
  radar() {
    this.opts.yaxis[0].labels.offsetY = this.opts.yaxis[0].labels.offsetY ? this.opts.yaxis[0].labels.offsetY : 6;
    return {
      dataLabels: {
        enabled: false,
        style: {
          fontSize: "11px"
        }
      },
      stroke: {
        width: 2
      },
      markers: {
        size: 5,
        strokeWidth: 1,
        strokeOpacity: 1
      },
      fill: {
        opacity: 0.2
      },
      tooltip: {
        shared: false,
        intersect: true,
        followCursor: true
      },
      grid: {
        show: false,
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      },
      xaxis: {
        labels: {
          formatter: (val) => val,
          style: {
            colors: ["#a8a8a8"],
            fontSize: "11px"
          }
        },
        tooltip: {
          enabled: false
        },
        crosshairs: {
          show: false
        }
      }
    };
  }
  radialBar() {
    return {
      chart: {
        animations: {
          dynamicAnimation: {
            enabled: true,
            speed: 800
          }
        },
        toolbar: {
          show: false
        }
      },
      stroke: {
        // Radial value arcs are stroked open arcs; square/round caps would
        // extend the stroke half a stroke-width past each endpoint, making
        // the "starting edge" visibly stick out past the geometric arc.
        // Butt cap is the only one that aligns with the arc's true angular
        // span. Without this, a chart that previously was a bar (whose
        // defaults set lineCap='square') would carry that cap across into
        // the radial render after a type morph.
        lineCap: "butt"
      },
      fill: {
        gradient: {
          shade: "dark",
          shadeIntensity: 0.4,
          inverseColors: false,
          type: "diagonal2",
          opacityFrom: 1,
          opacityTo: 1,
          stops: [70, 98, 100]
        }
      },
      legend: {
        show: false,
        position: "right"
      },
      tooltip: {
        enabled: false,
        fillSeriesColor: true
      },
      grid: {
        padding: {
          left: 0,
          right: 0,
          top: 0,
          bottom: 0
        }
      }
    };
  }
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   * @param {any[]} labels
   * @param {string} chartType
   */
  _getBoxTooltip(w2, seriesIndex, dataPointIndex, labels, chartType) {
    const o2 = w2.candleData.seriesCandleO[seriesIndex][dataPointIndex];
    const h2 = w2.candleData.seriesCandleH[seriesIndex][dataPointIndex];
    const m2 = w2.candleData.seriesCandleM[seriesIndex][dataPointIndex];
    const l2 = w2.candleData.seriesCandleL[seriesIndex][dataPointIndex];
    const c2 = w2.candleData.seriesCandleC[seriesIndex][dataPointIndex];
    const _si = (
      /** @type {Record<string,any>} */
      w2.config.series[seriesIndex]
    );
    if (_si.type && _si.type !== chartType) {
      return `<div class="apexcharts-custom-tooltip">
          ${_si.name ? _si.name : "series-" + (seriesIndex + 1)}: <strong>${w2.seriesData.series[seriesIndex][dataPointIndex]}</strong>
        </div>`;
    } else {
      return `<div class="apexcharts-tooltip-box apexcharts-tooltip-${w2.config.chart.type}"><div>${labels[0]}: <span class="value">` + o2 + `</span></div><div>${labels[1]}: <span class="value">` + h2 + "</span></div>" + (m2 ? `<div>${labels[2]}: <span class="value">` + m2 + "</span></div>" : "") + `<div>${labels[3]}: <span class="value">` + l2 + `</span></div><div>${labels[4]}: <span class="value">` + c2 + "</span></div></div>";
    }
  }
  /**
   * Shared tooltip for a violin: distribution value range and observation
   * count. Per-point hover is intentionally unsupported (jitter renders as a
   * single path), so the tooltip summarizes the violin as a whole.
   *
   * @param {import('../../types/internal').ChartStateW} w
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   */
  _getViolinTooltip(w2, seriesIndex, dataPointIndex) {
    var _a, _b, _c;
    const minV = (_a = w2.violinData.seriesViolinMin[seriesIndex]) == null ? void 0 : _a[dataPointIndex];
    const maxV = (_b = w2.violinData.seriesViolinMax[seriesIndex]) == null ? void 0 : _b[dataPointIndex];
    const pts = ((_c = w2.violinData.seriesViolinPoints[seriesIndex]) == null ? void 0 : _c[dataPointIndex]) || [];
    const name2 = (
      /** @type {Record<string,any>} */
      w2.config.series[seriesIndex].name || "series-" + (seriesIndex + 1)
    );
    return `<div class="apexcharts-tooltip-box apexcharts-tooltip-${w2.config.chart.type}"><div class="apexcharts-tooltip-violin-name">${name2}</div><div>Min: <span class="value">${minV}</span></div><div>Max: <span class="value">${maxV}</span></div><div>Observations: <span class="value">${pts.length}</span></div></div>`;
  }
}
class Config {
  /**
   * @param {Record<string, any>} opts
   */
  constructor(opts) {
    this.opts = opts;
  }
  /** @param {{responsiveOverride: any}} opts */
  init({ responsiveOverride }) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    let opts = this.opts;
    const options2 = new Options();
    const defaults = new Defaults(opts);
    opts = this.normalizeAliasedChartType(opts);
    this.chartType = opts.chart.type;
    opts = this.extendYAxis(opts);
    opts = this.extendAnnotations(opts);
    let config = options2.init();
    let newDefaults = {};
    if (opts && typeof opts === "object") {
      let chartDefaults = Defaults.forType(opts);
      if ((_a = opts.chart.brush) == null ? void 0 : _a.enabled) {
        chartDefaults = defaults.brush(chartDefaults);
      }
      if ((_c = (_b = opts.plotOptions) == null ? void 0 : _b.line) == null ? void 0 : _c.isSlopeChart) {
        chartDefaults = defaults.slope();
      }
      if (opts.chart.stacked && opts.chart.stackType === "100%") {
        opts = defaults.stacked100(opts);
      }
      if ((_e = (_d = opts.plotOptions) == null ? void 0 : _d.bar) == null ? void 0 : _e.isDumbbell) {
        opts = defaults.dumbbell(opts);
      }
      this.checkForDarkTheme(Environment.getApex());
      this.checkForDarkTheme(opts);
      opts.xaxis = opts.xaxis || Environment.getApex().xaxis || {};
      if (!responsiveOverride) {
        opts.xaxis.convertedCatToNumeric = false;
      }
      opts = this.checkForCatToNumericXAxis(this.chartType, chartDefaults, opts);
      if (((_f = opts.chart.sparkline) == null ? void 0 : _f.enabled) || ((_h = (_g = Environment.getApex().chart) == null ? void 0 : _g.sparkline) == null ? void 0 : _h.enabled)) {
        chartDefaults = defaults.sparkline(chartDefaults);
      }
      newDefaults = Utils$1.extend(config, chartDefaults);
    }
    const mergedWithDefaultConfig = Utils$1.extend(
      newDefaults,
      Environment.getApex()
    );
    config = Utils$1.extend(mergedWithDefaultConfig, opts);
    config = this.handleUserInputErrors(config);
    return config;
  }
  /**
   * Promoted chart-type aliases — `funnel`, `pyramid`, `gauge` — render via
   * the existing `bar` (with `isFunnel`) and `radialBar` pathways. To keep
   * the ~20 internal `chart.type === 'bar' | 'radialBar'` checks working
   * unchanged, we normalize `chart.type` to the base renderer name here and
   * preserve the user-facing name on `chart.requestedType` for the public
   * API and for default selection.
   *
   * @param {Record<string, any>} opts
   * @returns {Record<string, any>}
   */
  normalizeAliasedChartType(opts) {
    if (!opts || !opts.chart) return opts;
    const requested = opts.chart.type;
    if (requested !== "funnel" && requested !== "pyramid" && requested !== "gauge" && requested !== "waffle" && requested !== "histogram") {
      return opts;
    }
    opts.chart.requestedType = requested;
    if (requested === "waffle") {
      opts.plotOptions = opts.plotOptions || {};
      opts.plotOptions.unit = opts.plotOptions.unit || {};
      if (opts.plotOptions.unit.layout == null) {
        opts.plotOptions.unit.layout = "grid";
      }
      if (opts.plotOptions.unit.shape == null) {
        opts.plotOptions.unit.shape = "square";
      }
      opts.chart.type = "unit";
    } else if (requested === "funnel" || requested === "pyramid") {
      opts.plotOptions = opts.plotOptions || {};
      opts.plotOptions.bar = opts.plotOptions.bar || {};
      opts.plotOptions.bar.isFunnel = true;
      opts.plotOptions.bar.horizontal = true;
      opts.chart.type = "bar";
      if (requested === "pyramid") {
        opts.plotOptions.bar.isPyramid = true;
      } else {
        opts.plotOptions.bar.isPyramid = false;
      }
    } else if (requested === "gauge") {
      opts.chart.type = "radialBar";
    } else if (requested === "histogram") {
      opts.xaxis = opts.xaxis || {};
      if (opts.xaxis.type == null) {
        opts.xaxis.type = "numeric";
      }
      opts.chart.type = "bar";
    }
    return opts;
  }
  /**
   * @param {string} chartType
   * @param {Record<string, any>} chartDefaults
   * @param {Record<string, any>} opts
   */
  checkForCatToNumericXAxis(chartType, chartDefaults, opts) {
    var _a, _b, _c, _d, _e;
    const defaults = new Defaults(opts);
    const isBarHorizontal = (chartType === "bar" || chartType === "boxPlot" || chartType === "violin") && ((_b = (_a = opts.plotOptions) == null ? void 0 : _a.bar) == null ? void 0 : _b.horizontal);
    const unsupportedZoom = chartType === "pie" || chartType === "polarArea" || chartType === "donut" || chartType === "radar" || chartType === "radialBar" || chartType === "heatmap" || chartType === "unit" || chartType === "sunburst";
    const notNumericXAxis = opts.xaxis.type !== "datetime" && opts.xaxis.type !== "numeric";
    const isScatterJitter = (chartType === "scatter" || chartType === "bubble") && ((_e = (_d = (_c = opts.plotOptions) == null ? void 0 : _c.scatter) == null ? void 0 : _d.jitter) == null ? void 0 : _e.enabled);
    const tickPlacement = opts.xaxis.tickPlacement ? opts.xaxis.tickPlacement : chartDefaults.xaxis && chartDefaults.xaxis.tickPlacement;
    if (!isBarHorizontal && !unsupportedZoom && !isScatterJitter && notNumericXAxis && tickPlacement !== "between") {
      opts = defaults.convertCatToNumeric(opts);
    }
    return opts;
  }
  /**
   * @param {Record<string, any>} opts
   * @param {import('../../types/internal').ChartStateW} [w]
   */
  extendYAxis(opts, w2) {
    const options2 = new Options();
    if (typeof opts.yaxis === "undefined" || !opts.yaxis || Array.isArray(opts.yaxis) && opts.yaxis.length === 0) {
      opts.yaxis = {};
    }
    const globalApex = Environment.getApex();
    if (opts.yaxis.constructor !== Array && globalApex.yaxis && globalApex.yaxis.constructor !== Array) {
      opts.yaxis = Utils$1.extend(opts.yaxis, globalApex.yaxis);
    }
    if (opts.yaxis.constructor !== Array) {
      opts.yaxis = [Utils$1.extend(options2.yAxis, opts.yaxis)];
    } else {
      opts.yaxis = Utils$1.extendArray(opts.yaxis, options2.yAxis);
    }
    let isLogY = false;
    opts.yaxis.forEach((y) => {
      if (y.logarithmic) {
        isLogY = true;
      }
    });
    let series = opts.series;
    if (w2 && !series) {
      series = w2.config.series;
    }
    if (isLogY && series.length !== opts.yaxis.length && series.length) {
      opts.yaxis = series.map((s2, i2) => {
        if (!s2.name) {
          series[i2].name = `series-${i2 + 1}`;
        }
        if (opts.yaxis[i2]) {
          opts.yaxis[i2].seriesName = series[i2].name;
          return opts.yaxis[i2];
        } else {
          const newYaxis = Utils$1.extend(options2.yAxis, opts.yaxis[0]);
          newYaxis.show = false;
          return newYaxis;
        }
      });
    }
    if (isLogY && series.length > 1 && series.length !== opts.yaxis.length) {
      console.warn(
        "A multi-series logarithmic chart should have equal number of series and y-axes"
      );
    }
    return opts;
  }
  // annotations also accepts array, so we need to extend them manually
  /**
   * @param {Record<string, any>} opts
   */
  extendAnnotations(opts) {
    if (typeof opts.annotations === "undefined") {
      opts.annotations = {};
      opts.annotations.yaxis = [];
      opts.annotations.xaxis = [];
      opts.annotations.points = [];
    }
    opts = this.extendYAxisAnnotations(opts);
    opts = this.extendXAxisAnnotations(opts);
    opts = this.extendPointAnnotations(opts);
    return opts;
  }
  /**
   * @param {Record<string, any>} opts
   */
  extendYAxisAnnotations(opts) {
    const options2 = new Options();
    opts.annotations.yaxis = Utils$1.extendArray(
      typeof opts.annotations.yaxis !== "undefined" ? opts.annotations.yaxis : [],
      options2.yAxisAnnotation
    );
    return opts;
  }
  /**
   * @param {Record<string, any>} opts
   */
  extendXAxisAnnotations(opts) {
    const options2 = new Options();
    opts.annotations.xaxis = Utils$1.extendArray(
      typeof opts.annotations.xaxis !== "undefined" ? opts.annotations.xaxis : [],
      options2.xAxisAnnotation
    );
    return opts;
  }
  /**
   * @param {Record<string, any>} opts
   */
  extendPointAnnotations(opts) {
    const options2 = new Options();
    opts.annotations.points = Utils$1.extendArray(
      typeof opts.annotations.points !== "undefined" ? opts.annotations.points : [],
      options2.pointAnnotation
    );
    return opts;
  }
  /**
   * @param {Record<string, any>} opts
   */
  checkForDarkTheme(opts) {
    if (opts.theme && opts.theme.mode === "dark") {
      if (!opts.tooltip) {
        opts.tooltip = {};
      }
      if (opts.tooltip.theme !== "light") {
        opts.tooltip.theme = "dark";
      }
      if (!opts.chart.foreColor) {
        opts.chart.foreColor = "#f6f7f8";
      }
      if (!opts.theme.palette) {
        opts.theme.palette = "palette4";
      }
    }
  }
  /**
   * @param {any} opts
   */
  handleUserInputErrors(opts) {
    const config = opts;
    if (config.tooltip.shared && config.tooltip.intersect) {
      throw new Error(
        "tooltip.shared cannot be enabled when tooltip.intersect is true. Turn off any other option by setting it to false."
      );
    }
    if (config.chart.type === "bar" && config.plotOptions.bar.horizontal) {
      if (config.yaxis.length > 1) {
        throw new Error(
          "Multiple Y Axis for bars are not supported. Switch to column chart by setting plotOptions.bar.horizontal=false"
        );
      }
      if (config.yaxis[0].reversed) {
        config.yaxis[0].opposite = true;
      }
      config.xaxis.tooltip.enabled = false;
      config.yaxis[0].tooltip.enabled = false;
      config.chart.zoom.enabled = false;
    }
    if (config.chart.type === "bar" || config.chart.type === "rangeBar") {
      if (config.tooltip.shared) {
        if (config.xaxis.crosshairs.width === "barWidth" && config.series.length > 1) {
          config.xaxis.crosshairs.width = "tickWidth";
        }
      }
    }
    if (config.chart.type === "candlestick" || config.chart.type === "boxPlot") {
      if (config.yaxis[0].reversed) {
        console.warn(
          `Reversed y-axis in ${config.chart.type} chart is not supported.`
        );
        config.yaxis[0].reversed = false;
      }
    }
    return config;
  }
}
const LINE_HEIGHT_RATIO = 1.618;
const NICE_SCALE_ALLOWED_MAG_MSD = [
  [1, 1, 2, 5, 5, 5, 10, 10, 10, 10, 10],
  [1, 1, 2, 5, 5, 5, 10, 10, 10, 10, 10]
];
const NICE_SCALE_DEFAULT_TICKS = [
  1,
  2,
  4,
  4,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  6,
  12,
  12,
  12,
  12,
  12,
  12,
  12,
  12,
  12,
  24
];
class Globals {
  /**
   * @param {any} gl
   */
  initGlobalVars(gl) {
    gl.series = [];
    gl.seriesCandleO = [];
    gl.seriesCandleH = [];
    gl.seriesCandleM = [];
    gl.seriesCandleL = [];
    gl.seriesCandleC = [];
    gl.seriesRangeStart = [];
    gl.seriesRangeEnd = [];
    gl.seriesRange = [];
    gl.seriesPercent = [];
    gl.seriesGoals = [];
    gl.seriesX = [];
    gl.seriesZ = [];
    gl.seriesNames = [];
    gl.seriesTotals = [];
    gl.seriesLog = [];
    gl.seriesColors = [];
    gl.stackedSeriesTotals = [];
    gl.seriesXvalues = [];
    gl.seriesYvalues = [];
    gl.dataWasParsed = false;
    gl.originalSeries = null;
    gl.maxValsInArrayIndex = 0;
    gl.yValueDecimal = 0;
    gl.allSeriesHasEqualX = true;
    gl.hasNullValues = false;
    gl.invalidLogScale = false;
    gl.seriesRangeName = {};
    gl.labels = [];
    gl.hasXaxisGroups = false;
    gl.groups = [];
    gl.barGroups = [];
    gl.lineGroups = [];
    gl.areaGroups = [];
    gl.hasSeriesGroups = false;
    gl.seriesGroups = [];
    gl.categoryLabels = [];
    gl.timescaleLabels = [];
    gl.noLabelsProvided = false;
    gl.isXNumeric = false;
    gl.skipLastTimelinelabel = false;
    gl.skipFirstTimelinelabel = false;
    gl.isDataXYZ = false;
    gl.isMultiLineX = false;
    gl.isMultipleYAxis = false;
    gl.maxY = -Number.MAX_VALUE;
    gl.minY = Number.MIN_VALUE;
    gl.minYArr = [];
    gl.maxYArr = [];
    gl.maxX = -Number.MAX_VALUE;
    gl.minX = Number.MAX_VALUE;
    gl.initialMaxX = -Number.MAX_VALUE;
    gl.initialMinX = Number.MAX_VALUE;
    gl.maxDate = 0;
    gl.minDate = Number.MAX_VALUE;
    gl.minZ = Number.MAX_VALUE;
    gl.maxZ = -Number.MAX_VALUE;
    gl.minXDiff = Number.MAX_VALUE;
    gl.yAxisScale = [];
    gl.xAxisScale = null;
    gl.xAxisTicksPositions = [];
    gl.xRange = 0;
    gl.yRange = [];
    gl.zRange = 0;
    gl.dataPoints = 0;
    gl.xTickAmount = 0;
    gl.multiAxisTickAmount = 0;
    gl.disableZoomIn = false;
    gl.disableZoomOut = false;
    gl.yLabelsCoords = [];
    gl.yTitleCoords = [];
    gl.barPadForNumericAxis = 0;
    gl.padHorizontal = 0;
    gl.rotateXLabels = false;
    gl.overlappingXLabels = false;
    gl.radialSize = 0;
    gl.barHeight = 0;
    gl.barWidth = 0;
    gl.animationEnded = false;
    gl.isDestroyed = false;
    gl.bulkRevealScheduled = false;
    gl.resizeTimer = null;
    gl.selectionResizeTimer = null;
    gl.delayedElements = [];
    gl.pointsArray = [];
    gl.barCanvasCoords = null;
    gl.activeRenderer = null;
    gl.dataLabelsRects = [];
    gl.lastDrawnDataLabelsIndexes = [];
    gl.textRectsCache = /* @__PURE__ */ new Map();
    gl.domCache = /* @__PURE__ */ new Map();
    gl.dimensionCache = {};
    gl.cachedSelectors = {};
    if (!gl.seriesNS) {
      this._attachNamespaces(gl);
    }
  }
  /**
   * Attach domain-grouped namespace sub-objects onto gl.
   * Each sub-object is a plain object whose properties are defined as
   * getters/setters that read/write the canonical flat properties on gl.
   * This means there is exactly ONE storage location per value — no copies,
   * no sync needed.
   *
   * Namespaces:
   *   gl.series  — parsed series data and chart-type-specific arrays
   *   gl.axes    — axis bounds, scales, ranges, tick state
   *   gl.layout  — SVG/grid dimensions, translations, label sizes
   *   gl.cache   — DOM caches, timers, observers, drawing scratch space
   *
   * Note: interact state lives on w.interact (not gl) — see Base.js.
   * @param {any} gl
   */
  _attachNamespaces(gl) {
    const proxy = (ns, key, nsKey = key) => {
      Object.defineProperty(ns, nsKey, {
        get() {
          return gl[key];
        },
        set(v2) {
          gl[key] = v2;
        },
        enumerable: true,
        configurable: true
      });
    };
    const seriesNS = {};
    proxy(seriesNS, "series", "data");
    for (const key of [
      "seriesNames",
      "seriesX",
      "seriesZ",
      "seriesXvalues",
      "seriesYvalues",
      "seriesGoals",
      "seriesLog",
      "seriesColors",
      "seriesPercent",
      "seriesTotals",
      "stackedSeriesTotals",
      "seriesCandleO",
      "seriesCandleH",
      "seriesCandleM",
      "seriesCandleL",
      "seriesCandleC",
      "seriesRangeStart",
      "seriesRangeEnd",
      "seriesRange",
      "seriesYAxisMap",
      "seriesYAxisReverseMap",
      "seriesGroups",
      "barGroups",
      "lineGroups",
      "areaGroups",
      "originalSeries",
      "collapsedSeries",
      "collapsedSeriesIndices",
      "ancillaryCollapsedSeries",
      "ancillaryCollapsedSeriesIndices",
      "allSeriesCollapsed",
      "risingSeries",
      "previousPaths",
      "ignoreYAxisIndexes",
      "labels",
      "categoryLabels",
      "timescaleLabels",
      "groups"
    ]) {
      proxy(seriesNS, key);
    }
    Object.defineProperty(gl, "seriesNS", {
      value: seriesNS,
      writable: false,
      enumerable: false,
      configurable: true
    });
    const axesNS = {};
    for (const key of [
      "minX",
      "maxX",
      "initialMinX",
      "initialMaxX",
      "minY",
      "maxY",
      "minYArr",
      "maxYArr",
      "minZ",
      "maxZ",
      "minDate",
      "maxDate",
      "minXDiff",
      "xRange",
      "yRange",
      "zRange",
      "xAxisScale",
      "yAxisScale",
      "xAxisTicksPositions",
      "xTickAmount",
      "multiAxisTickAmount",
      "dataPoints",
      "maxValsInArrayIndex",
      "isXNumeric",
      "isMultipleYAxis",
      "isMultiLineX",
      "isDataXYZ",
      "dataFormatXNumeric",
      "allSeriesHasEqualX",
      "hasNullValues",
      "dataWasParsed",
      "hasXaxisGroups",
      "hasSeriesGroups",
      "skipFirstTimelinelabel",
      "skipLastTimelinelabel",
      "yValueDecimal",
      "invalidLogScale",
      "noLabelsProvided"
    ]) {
      proxy(axesNS, key);
    }
    Object.defineProperty(gl, "axes", {
      value: axesNS,
      writable: false,
      enumerable: false,
      configurable: true
    });
    const layoutNS = {};
    for (const key of [
      "svgWidth",
      "svgHeight",
      "gridWidth",
      "gridHeight",
      "translateX",
      "translateY",
      "translateXAxisX",
      "translateXAxisY",
      "translateYAxisX",
      "xAxisLabelsHeight",
      "xAxisGroupLabelsHeight",
      "xAxisLabelsWidth",
      "yAxisLabelsWidth",
      "yAxisWidths",
      "yLabelsCoords",
      "yTitleCoords",
      "padHorizontal",
      "barPadForNumericAxis",
      "rotateXLabels",
      "scaleX",
      "scaleY",
      "radialSize",
      "defaultLabels",
      "overlappingXLabels"
    ]) {
      proxy(layoutNS, key);
    }
    Object.defineProperty(gl, "layout", {
      value: layoutNS,
      writable: false,
      enumerable: false,
      configurable: true
    });
    const cacheNS = {};
    for (const key of [
      "domCache",
      "dimensionCache",
      "cachedSelectors",
      "textRectsCache",
      "pointsArray",
      "dataLabelsRects",
      "lastDrawnDataLabelsIndexes",
      "delayedElements",
      "resizeTimer",
      "selectionResizeTimer",
      "resizeObserver"
    ]) {
      proxy(cacheNS, key);
    }
    Object.defineProperty(gl, "cache", {
      value: cacheNS,
      writable: false,
      enumerable: false,
      configurable: true
    });
  }
  /**
   * Persistent chart state — set ONCE at chart construction and intentionally NOT
   * reset by initGlobalVars.  These values must survive updateSeries / re-render.
   *
   * Rule: if a value is recalculated fresh on every render it belongs in
   * initGlobalVars instead, not here.
   * @returns {import('../../types/internal').ChartGlobals}
   * @param {Record<string, any>} config
   */
  globalVars(config) {
    const globals = {
      // ── Identity (set once, never changes) ───────────────────────────────────
      chartID: null,
      // full chart ID: "apexcharts-<cuid>"
      cuid: null,
      // random suffix only
      // ── Event registry (accumulates listeners, never reset) ───────────────────
      events: {
        beforeMount: [],
        mounted: [],
        updated: [],
        clicked: [],
        selection: [],
        dataPointSelection: [],
        zoomed: [],
        scrolled: []
      },
      // ── Theme colors (set by Theme module after config merge) ─────────────────
      colors: [],
      fill: { colors: [] },
      // ── Animation-frame handles cancellable across re-renders ─────────────────
      // The chart-type instances that own these loops (Radial needle, Unit
      // gather/exit) are recreated every render, so the handle lives here: a new
      // render cancels the previous render's loop before starting its own,
      // instead of leaving it to animate detached nodes. NOT reset per render.
      radialNeedleRAF: null,
      unitGatherRAF: null,
      unitExitRAF: null,
      stroke: { colors: [] },
      dataLabels: { style: { colors: [] } },
      radarPolygons: { fill: { colors: [] } },
      markers: {
        colors: [],
        size: config.markers.size,
        largestSize: 0
      },
      // ── Device / environment detected once at startup ─────────────────────────
      // Note: isTouchDevice lives on w.interact — see Base.js. Shim installed there.
      LINE_HEIGHT_RATIO,
      // ── Chart-type flags (derived from config, set during Core.mount) ─────────
      axisCharts: true,
      // false for pie/radial/treemap etc.
      isSlopeChart: config.plotOptions.line.isSlopeChart,
      comboCharts: false,
      // true when mixing line + column series
      // ── Config snapshots (backups for zoom-reset / updateOptions) ────────────
      initialConfig: null,
      // deep clone of the original user config
      initialSeries: [],
      // The `--apx-surface` value Theme wrote into chart.background, so a later
      // token read can tell its own value from an explicit user background and
      // update it (see Theme.applyTokenChrome). Persistent: a re-render must
      // not forget it.
      tokenSurface: void 0,
      lastXAxis: [],
      lastYAxis: [],
      // ── User interaction state (must survive re-renders) ──────────────────────
      // Note: zoomEnabled, panEnabled, selectionEnabled, zoomed, selection,
      //       visibleXRange, selectedDataPoints, mousedown, clientX, clientY,
      //       lastClientPosition, capturedSeriesIndex, capturedDataPointIndex,
      //       disableZoomIn, disableZoomOut, isTouchDevice
      //       live on w.interact — see Base.js. Backward-compat shims installed there.
      // Series collapse state (user-driven, must persist across re-renders)
      allSeriesCollapsed: false,
      collapsedSeries: [],
      collapsedSeriesIndices: [],
      ancillaryCollapsedSeries: [],
      ancillaryCollapsedSeriesIndices: [],
      risingSeries: [],
      // series being re-shown after collapse
      ignoreYAxisIndexes: [],
      // y-axis indices excluded during series collapse
      // ── Lifecycle / update flags ──────────────────────────────────────────────
      isDirty: false,
      // true when user called an update method manually
      isExecCalled: false,
      // true when update came via exec()
      dataChanged: false,
      // true when series data was changed dynamically
      resized: false,
      // true after a container resize
      // ── Data format flags (derived from config/series, stable between renders) ─
      // Note: dataFormatXNumeric lives on w.axisFlags — see Base.js. Shim installed there.
      // hasNullValues / invalidLogScale are recomputed every render and reset in
      // initGlobalVars (they are NOT stable between renders).
      // Persistent data tracking
      columnSeries: null,
      // tracks which series are rendered as bars/columns
      yaxis: null,
      // resolved yaxis config array
      total: 0,
      // running total (used by pie/radial)
      // ── Animation control ─────────────────────────────────────────────────────
      shouldAnimate: true,
      previousPaths: [],
      // paths from previous render — source for enter animation
      // polarArea's last-drawn sector angles. Its angles are count-based, not
      // value-based, so a data-change animation cannot reconstruct them from
      // previousPaths (the previous VALUES) the way pie does; Pie.draw stashes
      // the real ones here each render.
      prevPolarAngles: null,
      // Streaming scroll: previous frame's parsed rows + pixel positions,
      // captured by Series.getPreviousPaths(). Consulted (like previousPaths)
      // only while a data-change morph renders. See StreamScroll.
      prevStreamFrame: null,
      // Axis-chrome snapshot (tick label texts/positions + gridline positions)
      // captured alongside prevStreamFrame; consumed once by AxisTransition
      // after a variable-length re-render mounts.
      prevChromeFrame: null,
      // ── SVG viewport (set by Dimensions, but persistent as layout anchor) ─────
      svgWidth: 0,
      svgHeight: 0,
      // Fingerprint of the container inputs that fed the last rendered size, so
      // the window-resize handler can skip a redraw (and the animation teardown
      // it causes) when a resize does not change the chart's drawing box.
      lastResizeSignature: null,
      // Note: gridWidth, gridHeight, translateX, translateY, translateXAxisX,
      // translateXAxisY, xAxisLabelsHeight, xAxisGroupLabelsHeight, xAxisLabelsWidth,
      // rotateXLabels, xAxisHeight, yLabelsCoords, yTitleCoords live on w.layout —
      // see Base.js. Backward-compat shims installed there.
      defaultLabels: false,
      // Note: formatter properties (xLabelFormatter, yLabelFormatters, etc.) live on
      // w.formatters — see Base.js. Backward-compat shims installed there.
      yAxisLabelsWidth: 0,
      scaleX: 1,
      scaleY: 1,
      translateYAxisX: [],
      yAxisWidths: [],
      // ── Instances (created once, replaced only on full re-init) ──────────────
      tooltip: null,
      resizeObserver: null,
      // ── Locale (loaded once; changes only via setLocale()) ───────────────────
      locale: {},
      // ── Method queue (deferred calls during async operations) ────────────────
      memory: {
        methodsToExec: []
      },
      // ── Scale configuration constants — imported from utils/Constants.js ──────
      niceScaleAllowedMagMsd: NICE_SCALE_ALLOWED_MAG_MSD,
      niceScaleDefaultTicks: NICE_SCALE_DEFAULT_TICKS,
      // ── Multi-axis series mapping ─────────────────────────────────────────────
      seriesYAxisMap: [],
      // yAxis index → series indices[]
      seriesYAxisReverseMap: [],
      // series index → yAxis index
      noData: false
      // true when there is nothing to render
    };
    return (
      /** @type {import('../../types/internal').ChartGlobals} */
      /** @type {unknown} */
      globals
    );
  }
  /**
   * Lazy initial-series snapshot. Capturing `initialSeries` used to deep-clone
   * the entire series on every parse and every update: the single largest CPU
   * bucket at 50k+ points (profiled at ~23ms per 50k update). The setter now
   * stores a cheap per-series shallow copy (series OBJECTS copied, data arrays
   * shared) and the deep snapshot materializes only when something actually
   * reads it (resetSeries, toolbar reset-home, tooltip same-length check).
   *
   * Why sharing the data arrays is safe: internal "mutations" of a series'
   * data are property REPLACEMENTS (`series[i].data = []` on legend collapse),
   * which cannot reach the captured copies because each series object was
   * copied at capture time. The one in-place mutator (appendData's push loop)
   * re-captures immediately after mutating, so the pending snapshot never
   * spans the mutation.
   *
   * @param {Record<string, any>} globals
   */
  defineLazyInitialSeries(globals) {
    let src = [];
    let snap = null;
    Object.defineProperty(globals, "initialSeries", {
      configurable: true,
      enumerable: true,
      get() {
        if (snap === null) {
          snap = Utils$1.clone(src);
        }
        return snap;
      },
      set(value) {
        src = Array.isArray(value) ? value.map(
          (s2) => s2 && typeof s2 === "object" && !Array.isArray(s2) ? __spreadValues({}, s2) : s2
        ) : value;
        snap = null;
        globals._initialSeriesPeek = src;
      }
    });
    globals._initialSeriesPeek = src;
  }
  /**
   * @param {Record<string, any>} config
   */
  init(config) {
    const globals = this.globalVars(config);
    this.initGlobalVars(globals);
    this.defineLazyInitialSeries(globals);
    globals.initialConfig = Utils$1.extend({}, config);
    globals.initialSeries = config.series;
    globals.lastXAxis = Utils$1.clone(
      /** @type {NonNullable<typeof globals.initialConfig>} */
      globals.initialConfig.xaxis
    );
    globals.lastYAxis = Utils$1.clone(
      /** @type {NonNullable<typeof globals.initialConfig>} */
      globals.initialConfig.yaxis
    );
    return globals;
  }
}
class Base {
  /**
   * @param {object} opts
   */
  constructor(opts) {
    this.opts = opts;
  }
  /**
   * Build and return the full chart state object `w`.
   * @returns {import('../types/internal').ChartStateW}
   */
  init() {
    const config = new Config(this.opts).init({ responsiveOverride: false });
    const globals = new Globals().init(config);
    const w2 = {
      config,
      globals,
      dom: {},
      // DOM node cache — lives here, not inside globals
      interact: {
        // Tool mode (derived from toolbar config at construction, updated by Toolbar)
        zoomEnabled: config.chart.toolbar.autoSelected === "zoom" && config.chart.toolbar.tools.zoom && config.chart.zoom.enabled,
        panEnabled: config.chart.toolbar.autoSelected === "pan" && config.chart.toolbar.tools.pan,
        selectionEnabled: config.chart.toolbar.autoSelected === "selection" && config.chart.toolbar.tools.selection,
        // Measure tool pre-selected via toolbar.autoSelected: 'measure'. Armed by
        // the Measure module (self-arms on mount) and the toolbar button.
        measureEnabled: config.chart.toolbar.autoSelected === "measure" && !!config.chart.toolbar.tools.measure && !!(config.chart.measure && config.chart.measure.enabled),
        // Zoom / pan state (user-driven, must persist across re-renders)
        zoomed: false,
        selection: void 0,
        visibleXRange: void 0,
        selectedDataPoints: [],
        // Mouse / pointer state
        mousedown: false,
        clientX: null,
        clientY: null,
        lastClientPosition: {},
        // Tooltip capture state
        capturedSeriesIndex: -1,
        capturedDataPointIndex: -1,
        // Timescale zoom bounds (reset per render by TimeScale)
        disableZoomIn: false,
        disableZoomOut: false,
        // Device detection (set once at construction)
        isTouchDevice: Environment.isBrowser() ? "ontouchstart" in window || navigator.maxTouchPoints > 0 : false
      },
      formatters: {
        // Populated by Formatters.setLabelFormatters() each render
        xLabelFormatter: void 0,
        yLabelFormatters: [],
        xaxisTooltipFormatter: void 0,
        ttKeyFormatter: void 0,
        ttVal: void 0,
        ttZFormatter: void 0,
        legendFormatter: void 0
      },
      // Candlestick / boxplot OHLC arrays — written by Data.handleCandleStickBoxData()
      // each render; empty for all other chart types.
      candleData: {
        seriesCandleO: [],
        seriesCandleH: [],
        seriesCandleM: [],
        seriesCandleL: [],
        seriesCandleC: [],
        // Optional raw observations per boxPlot data point ([i][j] = number[]),
        // rendered as jitter dots. Empty unless boxPlot points are supplied.
        seriesBoxPoints: []
      },
      // Range chart arrays — written by Data.handleRangeData() each render;
      // empty for all other chart types.
      rangeData: {
        seriesRangeStart: [],
        seriesRangeEnd: [],
        seriesRange: []
      },
      // Violin distribution arrays — written by Data.handleViolinData() each
      // render; empty for all other chart types.
      //   seriesViolinDensity[i][j] = { values:number[], weights:number[], maxWeight:number }
      //   seriesViolinPoints[i][j]  = number[] (raw observations, value-axis units)
      //   seriesViolinMin/Max[i][j] = number (extent of density + points — drives Range.js)
      violinData: {
        seriesViolinDensity: [],
        seriesViolinPoints: [],
        seriesViolinMin: [],
        seriesViolinMax: []
      },
      // Histogram binning — written by Data.binHistogramData() each render;
      // empty for all other chart types.
      //   edges[k]     = bin boundaries (length = binCount + 1)
      //   counts[i][k] = raw observation count per series, before normalize
      //   rule         = the rule that chose the width ('fd', 'sturges', ...)
      histogramData: {
        edges: [],
        binWidth: 0,
        counts: [],
        rule: "",
        capped: false
      },
      // Label / category data — written by Data.parseData() and TimeScale each render.
      labelData: {
        labels: [],
        categoryLabels: [],
        timescaleLabels: [],
        // written by TimeScale.calculateTimeScaleMinMax()
        hasXaxisGroups: false,
        groups: [],
        seriesGroups: []
      },
      // Axis / parsing behaviour flags — written by Data.parseData() each render.
      axisFlags: {
        isXNumeric: false,
        dataFormatXNumeric: false,
        isDataXYZ: false,
        isRangeData: false,
        isRangeBar: false,
        isMultiLineX: false,
        noLabelsProvided: false,
        dataWasParsed: false
      },
      // Parsed series data — written by Data.parseData() each render.
      // Note: initialSeries and originalSeries are intentionally excluded —
      // they are persistent (survive re-renders) and remain on w.globals.
      seriesData: {
        series: [],
        // main y-values array
        seriesNames: [],
        seriesX: [],
        seriesZ: [],
        seriesColors: [],
        seriesGoals: [],
        stackedSeriesTotals: [],
        stackedSeriesTotalsByGroups: [],
        unitData: []
        // per-unit data for the `unit` chart (see SeriesData)
      },
      // Grid / axis layout computed by Dimensions.plotCoords() each render.
      // gridWidth/gridHeight/translateX/translateY are also used as starting
      // points by Dimensions on the next render (accumulated values), so the
      // shim must be bidirectional — reads and writes both route correctly.
      layout: {
        gridHeight: 0,
        gridWidth: 0,
        translateX: 0,
        translateY: 0,
        translateXAxisX: 0,
        translateXAxisY: 0,
        rotateXLabels: false,
        xAxisHeight: 0,
        xAxisLabelsHeight: 0,
        xAxisGroupLabelsHeight: 0,
        xAxisLabelsWidth: 0,
        yLabelsCoords: [],
        yTitleCoords: [],
        gridPad: { top: 0, right: 0, bottom: 0, left: 0 }
      }
    };
    Object.defineProperty(globals, "dom", {
      get() {
        return w2.dom;
      },
      set(v2) {
        w2.dom = v2;
      },
      enumerable: false,
      configurable: true
    });
    for (const key of [
      "xLabelFormatter",
      "yLabelFormatters",
      "xaxisTooltipFormatter",
      "ttKeyFormatter",
      "ttVal",
      "ttZFormatter",
      "legendFormatter"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.formatters[key]
          );
        },
        set(v2) {
          w2.formatters[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "zoomEnabled",
      "panEnabled",
      "selectionEnabled",
      "zoomed",
      "selection",
      "visibleXRange",
      "selectedDataPoints",
      "mousedown",
      "clientX",
      "clientY",
      "lastClientPosition",
      "capturedSeriesIndex",
      "capturedDataPointIndex",
      "disableZoomIn",
      "disableZoomOut",
      "isTouchDevice"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.interact[key]
          );
        },
        set(v2) {
          w2.interact[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "gridHeight",
      "gridWidth",
      "translateX",
      "translateY",
      "translateXAxisX",
      "translateXAxisY",
      "rotateXLabels",
      "xAxisHeight",
      "xAxisLabelsHeight",
      "xAxisGroupLabelsHeight",
      "xAxisLabelsWidth",
      "yLabelsCoords",
      "yTitleCoords",
      "gridPad"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.layout[key]
          );
        },
        set(v2) {
          w2.layout[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "series",
      "seriesNames",
      "seriesX",
      "seriesZ",
      "seriesColors",
      "seriesGoals",
      "stackedSeriesTotals",
      "stackedSeriesTotalsByGroups"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.seriesData[key]
          );
        },
        set(v2) {
          w2.seriesData[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "isXNumeric",
      "dataFormatXNumeric",
      "isDataXYZ",
      "isRangeData",
      "isRangeBar",
      "isMultiLineX",
      "noLabelsProvided",
      "dataWasParsed"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.axisFlags[key]
          );
        },
        set(v2) {
          w2.axisFlags[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "labels",
      "categoryLabels",
      "timescaleLabels",
      "hasXaxisGroups",
      "groups",
      "seriesGroups"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.labelData[key]
          );
        },
        set(v2) {
          w2.labelData[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of ["seriesRangeStart", "seriesRangeEnd", "seriesRange"]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.rangeData[key]
          );
        },
        set(v2) {
          w2.rangeData[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "seriesCandleO",
      "seriesCandleH",
      "seriesCandleM",
      "seriesCandleL",
      "seriesCandleC"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.candleData[key]
          );
        },
        set(v2) {
          w2.candleData[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    for (const key of [
      "seriesViolinDensity",
      "seriesViolinPoints",
      "seriesViolinMin",
      "seriesViolinMax"
    ]) {
      Object.defineProperty(globals, key, {
        get() {
          return (
            /** @type {Record<string,any>} */
            w2.violinData[key]
          );
        },
        set(v2) {
          w2.violinData[key] = v2;
        },
        enumerable: false,
        configurable: true
      });
    }
    return (
      /** @type {import('../types/internal').ChartStateW} */
      /** @type {unknown} */
      w2
    );
  }
}
class CoreUtils {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
  }
  /**
   * @param {any[]} series
   * @param {string} chartType
   */
  static checkComboSeries(series, chartType) {
    let comboCharts = false;
    let comboBarCount = 0;
    let comboCount = 0;
    if (chartType === void 0) {
      chartType = "line";
    }
    if (series.length && typeof series[0].type !== "undefined") {
      series.forEach((s2) => {
        if (s2.type === "bar" || s2.type === "column" || s2.type === "candlestick" || s2.type === "boxPlot" || s2.type === "violin") {
          comboBarCount++;
        }
        if (typeof s2.type !== "undefined" && s2.type !== chartType) {
          comboCount++;
        }
      });
    }
    if (comboCount > 0) {
      comboCharts = true;
    }
    return {
      comboBarCount,
      comboCharts
    };
  }
  /**
   * @memberof CoreUtils
   * returns the sum of all individual values in a multiple stacked series
   * Eg. w.seriesData.series = [[32,33,43,12], [2,3,5,1]]
   *  @return [34,36,48,13]
   * @param {number[]} excludedSeriesIndices
   **/
  getStackedSeriesTotals(excludedSeriesIndices = []) {
    const w2 = this.w;
    const total = [];
    if (w2.seriesData.series.length === 0) return total;
    for (let i2 = 0; i2 < w2.seriesData.series[w2.globals.maxValsInArrayIndex].length; i2++) {
      let t2 = 0;
      for (let j = 0; j < w2.seriesData.series.length; j++) {
        if (typeof w2.seriesData.series[j][i2] !== "undefined" && excludedSeriesIndices.indexOf(j) === -1) {
          t2 += w2.seriesData.series[j][i2];
        }
      }
      total.push(t2);
    }
    return total;
  }
  // get total of the all values inside all series
  /**
   * @param {number | null} [index]
   */
  getSeriesTotalByIndex(index = null) {
    if (index === null) {
      return (
        /** @type {any[]} */
        this.w.config.series.reduce(
          (acc, cur) => acc + cur,
          0
        )
      );
    } else {
      const seriesAtIndex = this.w.seriesData.series[index];
      if (!Array.isArray(seriesAtIndex)) {
        return seriesAtIndex != null ? seriesAtIndex : 0;
      }
      return seriesAtIndex.reduce(
        (acc, cur) => acc + cur,
        0
      );
    }
  }
  /**
   * @memberof CoreUtils
   * returns the sum of values in a multiple stacked grouped charts
   * Eg. w.seriesData.series = [[32,33,43,12], [2,3,5,1], [43, 23, 34, 22]]
   * series 1 and 2 are in a group, while series 3 is in another group
   *  @return [[34, 36, 48, 12], [43, 23, 34, 22]]
   **/
  getStackedSeriesTotalsByGroups() {
    const w2 = this.w;
    const total = [];
    w2.labelData.seriesGroups.forEach((sg) => {
      const includedIndexes = [];
      w2.config.series.forEach((s2, si) => {
        if (sg.indexOf(w2.seriesData.seriesNames[si]) > -1) {
          includedIndexes.push(si);
        }
      });
      const excludedIndices = w2.seriesData.series.map((_, fi) => includedIndexes.indexOf(fi) === -1 ? fi : -1).filter((f) => f !== -1);
      total.push(this.getStackedSeriesTotals(excludedIndices));
    });
    return total;
  }
  setSeriesYAxisMappings() {
    const gl = this.w.globals;
    const cnf = this.w.config;
    let axisSeriesMap = [];
    const seriesYAxisReverseMap = [];
    const unassignedSeriesIndices = [];
    const seriesNameArrayStyle = this.w.seriesData.series.length > cnf.yaxis.length || /**
     * @param {ApexYAxis} a
     */
    cnf.yaxis.some((a2) => Array.isArray(a2.seriesName));
    cnf.series.forEach((_s, i2) => {
      unassignedSeriesIndices.push(i2);
      seriesYAxisReverseMap.push(null);
    });
    cnf.yaxis.forEach(
      (_yaxe, yi) => {
        axisSeriesMap[yi] = [];
      }
    );
    const unassignedYAxisIndices = [];
    cnf.yaxis.forEach((yaxe, yi) => {
      let assigned = false;
      if (yaxe.seriesName) {
        let seriesNames = [];
        if (Array.isArray(yaxe.seriesName)) {
          seriesNames = yaxe.seriesName;
        } else {
          seriesNames.push(yaxe.seriesName);
        }
        seriesNames.forEach((name2) => {
          cnf.series.forEach((s2, si) => {
            if (
              /** @type {any} */
              s2.name === name2
            ) {
              let remove = si;
              if (yi === si || seriesNameArrayStyle) {
                if (!seriesNameArrayStyle || unassignedSeriesIndices.indexOf(si) > -1) {
                  axisSeriesMap[yi].push([yi, si]);
                } else {
                  console.warn(
                    "Series '" + /** @type {any} */
                    s2.name + "' referenced more than once in what looks like the new style. That is, when using either seriesName: [], or when there are more series than yaxes."
                  );
                }
              } else {
                axisSeriesMap[si].push([si, yi]);
                remove = yi;
              }
              assigned = true;
              remove = unassignedSeriesIndices.indexOf(remove);
              if (remove !== -1) {
                unassignedSeriesIndices.splice(remove, 1);
              }
            }
          });
        });
      }
      if (!assigned) {
        unassignedYAxisIndices.push(yi);
      }
    });
    axisSeriesMap = axisSeriesMap.map((yaxe) => {
      const ra = [];
      yaxe.forEach((sa) => {
        seriesYAxisReverseMap[sa[1]] = sa[0];
        ra.push(sa[1]);
      });
      return ra;
    });
    let lastUnassignedYAxis = cnf.yaxis.length - 1;
    for (let i2 = 0; i2 < unassignedYAxisIndices.length; i2++) {
      lastUnassignedYAxis = unassignedYAxisIndices[i2];
      axisSeriesMap[lastUnassignedYAxis] = [];
      if (unassignedSeriesIndices) {
        const si = unassignedSeriesIndices[0];
        unassignedSeriesIndices.shift();
        axisSeriesMap[lastUnassignedYAxis].push(si);
        seriesYAxisReverseMap[si] = lastUnassignedYAxis;
      } else {
        break;
      }
    }
    unassignedSeriesIndices.forEach((i2) => {
      axisSeriesMap[lastUnassignedYAxis].push(i2);
      seriesYAxisReverseMap[i2] = lastUnassignedYAxis;
    });
    gl.seriesYAxisMap = axisSeriesMap.map((x) => x);
    gl.seriesYAxisReverseMap = seriesYAxisReverseMap.map((x) => x);
    gl.seriesYAxisMap.forEach((axisSeries, ai) => {
      axisSeries.forEach((si) => {
        if (
          /** @type {any} */
          cnf.series[si] && /** @type {any} */
          cnf.series[si].group === void 0
        ) {
          const _series = (
            /** @type {any} */
            cnf.series[si]
          );
          _series.group = "apexcharts-axis-".concat(ai.toString());
        }
      });
    });
  }
  /**
   * @param {number | null} [index]
   */
  isSeriesNull(index = null) {
    let r2 = [];
    const series = (
      /** @type {any[]} */
      this.w.config.series
    );
    if (index === null) {
      r2 = series.filter((d) => d !== null);
    } else if (series[index] && Array.isArray(series[index].data)) {
      r2 = series[index].data.filter((d) => d !== null);
    } else {
      r2 = series[index] !== null && series[index] !== void 0 ? [series[index]] : [];
    }
    return r2.length === 0;
  }
  /**
   * @param {number} index
   */
  seriesHaveSameValues(index) {
    const seriesAtIndex = this.w.seriesData.series[index];
    if (!Array.isArray(seriesAtIndex)) {
      return true;
    }
    return seriesAtIndex.every(
      (val, i2, arr) => val === arr[0]
    );
  }
  /**
   * @param {any[]} labels
   */
  getCategoryLabels(labels) {
    const w2 = this.w;
    let catLabels = labels.slice();
    if (w2.config.xaxis.convertedCatToNumeric) {
      catLabels = labels.map((i2) => {
        return w2.config.xaxis.labels.formatter(i2 - w2.globals.minX + 1);
      });
    }
    return catLabels;
  }
  // maxValsInArrayIndex is the index of series[] which has the largest number of items
  getLargestSeries() {
    const w2 = this.w;
    w2.globals.maxValsInArrayIndex = w2.seriesData.series.map((a2) => a2.length).indexOf(
      Math.max.apply(
        Math,
        /**
         * @param {number[]} a
         */
        w2.seriesData.series.map((a2) => a2.length)
      )
    );
  }
  getLargestMarkerSize() {
    const w2 = this.w;
    let size = 0;
    w2.globals.markers.size.forEach((m2) => {
      size = Math.max(size, m2);
    });
    if (w2.config.markers.discrete && w2.config.markers.discrete.length) {
      w2.config.markers.discrete.forEach((m2) => {
        size = Math.max(size, m2.size);
      });
    }
    if (size > 0) {
      if (w2.config.markers.hover.size > 0) {
        size = w2.config.markers.hover.size;
      } else {
        size += w2.config.markers.hover.sizeOffset;
      }
    }
    w2.globals.markers.largestSize = size;
    return size;
  }
  /**
   * @memberof Core
   * returns the sum of all values in a series
   * Eg. w.seriesData.series = [[32,33,43,12], [2,3,5,1]]
   *  @return [120, 11]
   **/
  getSeriesTotals() {
    const w2 = this.w;
    w2.globals.seriesTotals = w2.seriesData.series.map((ser) => {
      let total = 0;
      if (Array.isArray(ser)) {
        for (let j = 0; j < ser.length; j++) {
          total += ser[j];
        }
      } else {
        total += ser;
      }
      return total;
    });
  }
  /**
   * @param {number} minX
   * @param {number} maxX
   */
  getSeriesTotalsXRange(minX, maxX) {
    const w2 = this.w;
    const seriesTotalsXRange = w2.seriesData.series.map((ser, index) => {
      let total = 0;
      for (let j = 0; j < ser.length; j++) {
        if (w2.seriesData.seriesX[index][j] > minX && w2.seriesData.seriesX[index][j] < maxX) {
          total += ser[j];
        }
      }
      return total;
    });
    return seriesTotalsXRange;
  }
  /**
   * @memberof CoreUtils
   * returns the percentage value of all individual values which can be used in a 100% stacked series
   * Eg. w.seriesData.series = [[32, 33, 43, 12], [2, 3, 5, 1]]
   *  @return [[94.11, 91.66, 89.58, 92.30], [5.88, 8.33, 10.41, 7.7]]
   **/
  getPercentSeries() {
    const w2 = this.w;
    w2.globals.seriesPercent = w2.seriesData.series.map((ser) => {
      const seriesPercent = [];
      if (Array.isArray(ser)) {
        for (let j = 0; j < ser.length; j++) {
          const total = w2.seriesData.stackedSeriesTotals[j];
          let percent = 0;
          if (total) {
            percent = 100 * ser[j] / total;
          }
          seriesPercent.push(percent);
        }
      } else {
        const total = w2.globals.seriesTotals.reduce((acc, val) => acc + val, 0);
        const percent = 100 * ser / total;
        seriesPercent.push(percent);
      }
      return seriesPercent;
    });
  }
  getCalculatedRatios() {
    const w2 = this.w;
    const gl = w2.globals;
    const yRatio = [];
    let invertedYRatio = 0;
    let xRatio = 0;
    let invertedXRatio = 0;
    let zRatio = 0;
    let baseLineY = [];
    let baseLineInvertedY = 0.1;
    let baseLineX = 0;
    gl.yRange = [];
    if (gl.isMultipleYAxis) {
      for (let i2 = 0; i2 < gl.minYArr.length; i2++) {
        gl.yRange.push(Math.abs(gl.minYArr[i2] - gl.maxYArr[i2]));
        baseLineY.push(0);
      }
    } else {
      gl.yRange.push(Math.abs(gl.minY - gl.maxY));
    }
    gl.xRange = Math.abs(gl.maxX - gl.minX);
    gl.zRange = Math.abs(gl.maxZ - gl.minZ);
    for (let i2 = 0; i2 < gl.yRange.length; i2++) {
      yRatio.push(gl.yRange[i2] / this.w.layout.gridHeight);
    }
    xRatio = gl.xRange / this.w.layout.gridWidth;
    invertedYRatio = /** @type {any} */
    gl.yRange / this.w.layout.gridWidth;
    invertedXRatio = gl.xRange / this.w.layout.gridHeight;
    zRatio = gl.zRange / this.w.layout.gridHeight * 16;
    if (!zRatio) {
      zRatio = 1;
    }
    if (gl.minY !== Number.MIN_VALUE && Math.abs(gl.minY) !== 0) {
      const _hasNegsGl = (
        /** @type {any} */
        gl
      );
      _hasNegsGl.hasNegs = true;
    }
    if (w2.globals.seriesYAxisReverseMap.length > 0) {
      const scaleBaseLineYScale = (y, i2) => {
        const yAxis = w2.config.yaxis[w2.globals.seriesYAxisReverseMap[i2]];
        if (!yAxis) return 0;
        const sign = y < 0 ? -1 : 1;
        y = Math.abs(y);
        if (yAxis.logarithmic) {
          y = this.getBaseLog(yAxis.logBase, y);
        }
        return -sign * y / yRatio[i2];
      };
      if (gl.isMultipleYAxis) {
        baseLineY = [];
        for (let i2 = 0; i2 < yRatio.length; i2++) {
          baseLineY.push(scaleBaseLineYScale(gl.minYArr[i2], i2));
        }
      } else {
        baseLineY = [];
        baseLineY.push(scaleBaseLineYScale(gl.minY, 0));
        if (gl.minY !== Number.MIN_VALUE && Math.abs(gl.minY) !== 0) {
          baseLineInvertedY = -gl.minY / invertedYRatio;
          baseLineX = gl.minX / xRatio;
        }
      }
    } else {
      baseLineY = [];
      baseLineY.push(0);
      baseLineInvertedY = 0;
      baseLineX = 0;
    }
    return {
      yRatio,
      invertedYRatio,
      zRatio,
      xRatio,
      invertedXRatio,
      baseLineInvertedY,
      baseLineY,
      baseLineX
    };
  }
  /**
   * @param {any[]} series
   */
  getLogSeries(series) {
    const w2 = this.w;
    w2.globals.seriesLog = series.map((s2, i2) => {
      const yAxisIndex = w2.globals.seriesYAxisReverseMap[i2];
      if (w2.config.yaxis[yAxisIndex] && w2.config.yaxis[yAxisIndex].logarithmic) {
        return s2.map((d) => {
          if (d === null) return null;
          return this.getLogVal(w2.config.yaxis[yAxisIndex].logBase, d, i2);
        });
      } else {
        return s2;
      }
    });
    return w2.globals.invalidLogScale ? series : w2.globals.seriesLog;
  }
  /**
   * @param {number} val
   * @param {number} seriesIndex
   * @returns {number}
   */
  getLogValAtSeriesIndex(val, seriesIndex) {
    if (val === null) return (
      /** @type {any} */
      null
    );
    const w2 = this.w;
    const yAxisIndex = w2.globals.seriesYAxisReverseMap[seriesIndex];
    if (w2.config.yaxis[yAxisIndex] && w2.config.yaxis[yAxisIndex].logarithmic) {
      return this.getLogVal(
        w2.config.yaxis[yAxisIndex].logBase,
        val,
        seriesIndex
      );
    }
    return val;
  }
  /**
   * @param {number} base
   * @param {number} value
   */
  getBaseLog(base, value) {
    return Math.log(value) / Math.log(base);
  }
  /**
   * @param {number} b
   * @param {number} d
   * @param {number} seriesIndex
   */
  getLogVal(b2, d, seriesIndex) {
    if (d <= 0) {
      return 0;
    }
    const w2 = this.w;
    const min_log_val = w2.globals.minYArr[seriesIndex] === 0 ? -1 : this.getBaseLog(b2, w2.globals.minYArr[seriesIndex]);
    const max_log_val = w2.globals.maxYArr[seriesIndex] === 0 ? 0 : this.getBaseLog(b2, w2.globals.maxYArr[seriesIndex]);
    const number_of_height_levels = max_log_val - min_log_val;
    if (d < 1) return d / number_of_height_levels;
    const log_height_value = this.getBaseLog(b2, d) - min_log_val;
    return log_height_value / number_of_height_levels;
  }
  /**
   * @param {number[]} yRatio
   */
  getLogYRatios(yRatio) {
    const w2 = this.w;
    const gl = this.w.globals;
    const _gl = (
      /** @type {any} */
      gl
    );
    _gl.yLogRatio = yRatio.slice();
    _gl.logYRange = /** @type {any[]} */
    gl.yRange.map(
      (_, i2) => {
        const yAxisIndex = w2.globals.seriesYAxisReverseMap[i2];
        if (w2.config.yaxis[yAxisIndex] && this.w.config.yaxis[yAxisIndex].logarithmic) {
          let maxY = -Number.MAX_VALUE;
          let minY = Number.MIN_VALUE;
          let range = 1;
          gl.seriesLog.forEach(
            (s2, si) => {
              s2.forEach((v2) => {
                if (w2.config.yaxis[si] && w2.config.yaxis[si].logarithmic) {
                  maxY = Math.max(v2, maxY);
                  minY = Math.min(v2, minY);
                }
              });
            }
          );
          range = Math.pow(gl.yRange[i2], Math.abs(minY - maxY) / gl.yRange[i2]);
          _gl.yLogRatio[i2] = range / this.w.layout.gridHeight;
          return range;
        }
      }
    );
    return _gl.invalidLogScale ? yRatio.slice() : _gl.yLogRatio;
  }
  // Some config objects can be array - and we need to extend them correctly
  /**
   * @param {any} configInstance
   * @param {Record<string, any>} options
   * @param {import('../types/internal').ChartStateW} w
   */
  static extendArrayProps(configInstance, options2, w2) {
    var _a, _b;
    if (options2 == null ? void 0 : options2.yaxis) {
      options2 = configInstance.extendYAxis(options2, w2);
    }
    if (options2 == null ? void 0 : options2.annotations) {
      if (options2.annotations.yaxis) {
        options2 = configInstance.extendYAxisAnnotations(options2);
      }
      if ((_a = options2 == null ? void 0 : options2.annotations) == null ? void 0 : _a.xaxis) {
        options2 = configInstance.extendXAxisAnnotations(options2);
      }
      if ((_b = options2 == null ? void 0 : options2.annotations) == null ? void 0 : _b.points) {
        options2 = configInstance.extendPointAnnotations(options2);
      }
    }
    return options2;
  }
  // Series of the same group and type can be stacked together distinct from
  // other series of the same type on the same axis.
  /**
   * @param {Record<string, any>} typeSeries
   * @param {string[]} typeGroups
   * @param {string} type
   * @param {string} chartClass
   */
  drawSeriesByGroup(typeSeries, typeGroups, type, chartClass) {
    const w2 = this.w;
    const graph = [];
    if (typeSeries.series.length > 0) {
      typeGroups.forEach((gn) => {
        const gs = [];
        const gi = [];
        typeSeries.i.forEach((i2, ii) => {
          if (
            /** @type {Record<string,any>} */
            w2.config.series[i2].group === gn
          ) {
            gs.push(typeSeries.series[ii]);
            gi.push(i2);
          }
        });
        gs.length > 0 && graph.push(
          /** @type {any} */
          chartClass.draw(gs, type, gi)
        );
      });
    }
    return graph;
  }
}
const SVGNS$2 = "http://www.w3.org/2000/svg";
class Point {
  /**
   * @param {number|{x:number,y:number}} x
   * @param {number} [y]
   */
  constructor(x, y) {
    if (typeof x === "object") {
      this.x = x.x;
      this.y = x.y;
    } else {
      this.x = x || 0;
      this.y = y || 0;
    }
  }
  /**
   * @param {Matrix} matrix
   */
  transform(matrix) {
    return matrix.apply(this);
  }
  clone() {
    return new Point(this.x, this.y);
  }
}
class Matrix {
  /**
   * Defaults to the identity matrix when called with no args.
   * @param {number} [a]
   * @param {number} [b]
   * @param {number} [c]
   * @param {number} [d]
   * @param {number} [e]
   * @param {number} [f]
   */
  constructor(a2, b2, c2, d, e2, f) {
    this.a = a2 != null ? a2 : 1;
    this.b = b2 != null ? b2 : 0;
    this.c = c2 != null ? c2 : 0;
    this.d = d != null ? d : 1;
    this.e = e2 != null ? e2 : 0;
    this.f = f != null ? f : 0;
  }
  /**
   * @param {number} deg
   */
  rotate(deg) {
    const rad = deg * Math.PI / 180;
    const cos = Math.cos(rad);
    const sin = Math.sin(rad);
    return this.multiply(new Matrix(cos, sin, -sin, cos, 0, 0));
  }
  /**
   * @param {number} sx
   * @param {number} sy
   */
  scale(sx, sy) {
    return this.multiply(new Matrix(sx, 0, 0, sy != null ? sy : sx, 0, 0));
  }
  /**
   * @param {Matrix} m
   */
  multiply(m2) {
    return new Matrix(
      this.a * m2.a + this.c * m2.b,
      this.b * m2.a + this.d * m2.b,
      this.a * m2.c + this.c * m2.d,
      this.b * m2.c + this.d * m2.d,
      this.a * m2.e + this.c * m2.f + this.e,
      this.b * m2.e + this.d * m2.f + this.f
    );
  }
  /**
   * @param {Point} point
   */
  apply(point) {
    return new Point(
      this.a * point.x + this.c * point.y + this.e,
      this.b * point.x + this.d * point.y + this.f
    );
  }
}
class Box {
  /**
   * @param {number} x
   * @param {number} y
   * @param {number} w
   * @param {number} h
   */
  constructor(x, y, w2, h2) {
    this.x = x;
    this.y = y;
    this.w = w2;
    this.h = h2;
    this.width = w2;
    this.height = h2;
    this.x2 = x + w2;
    this.y2 = y + h2;
  }
}
/*!
 * Path morphing for SVG path animations
 * Based on svg.pathmorphing.js by Ulrich-Matthias Schäfer (MIT License)
 * Refactored to be standalone (no SVG.js dependency)
 *
 * Two algorithms are exported:
 *   - morphPaths()    — command-level interpolation; preserves curves but can
 *                       produce "wings/flips" when two shapes have very
 *                       different topology (e.g. bar rect → pie arc).
 *   - morphPolygons() — resamples both shapes into N evenly-spaced perimeter
 *                       points and tweens point-by-point with rotation-search
 *                       alignment; always smooth and non-self-intersecting,
 *                       at the cost of throwing away curve smoothness.
 */
function parsePath(d) {
  if (!d || typeof d !== "string") return [["M", 0, 0]];
  const commands = [];
  const re = /([MmLlHhVvCcSsQqTtAaZz])\s*/g;
  const numRe = /[+-]?(?:\d+\.?\d*|\.\d+)(?:e[+-]?\d+)?/gi;
  let match;
  const letters = [];
  const positions = [];
  while ((match = re.exec(d)) !== null) {
    letters.push(match[1]);
    positions.push(match.index);
  }
  for (let i2 = 0; i2 < letters.length; i2++) {
    const start = positions[i2] + letters[i2].length;
    const end = i2 + 1 < positions.length ? positions[i2 + 1] : d.length;
    const paramStr = d.substring(start, end);
    const nums = [];
    let numMatch;
    numRe.lastIndex = 0;
    while ((numMatch = numRe.exec(paramStr)) !== null) {
      nums.push(parseFloat(numMatch[0]));
    }
    const cmd = letters[i2].toUpperCase();
    if (cmd === "Z") {
      commands.push(["Z"]);
    } else if (cmd === "M" || cmd === "L" || cmd === "T") {
      for (let j = 0; j < nums.length; j += 2) {
        commands.push([cmd, nums[j], nums[j + 1]]);
      }
    } else if (cmd === "H") {
      for (let j = 0; j < nums.length; j++) {
        commands.push([cmd, nums[j]]);
      }
    } else if (cmd === "V") {
      for (let j = 0; j < nums.length; j++) {
        commands.push([cmd, nums[j]]);
      }
    } else if (cmd === "C") {
      for (let j = 0; j < nums.length; j += 6) {
        commands.push([
          cmd,
          nums[j],
          nums[j + 1],
          nums[j + 2],
          nums[j + 3],
          nums[j + 4],
          nums[j + 5]
        ]);
      }
    } else if (cmd === "S" || cmd === "Q") {
      for (let j = 0; j < nums.length; j += 4) {
        commands.push([cmd, nums[j], nums[j + 1], nums[j + 2], nums[j + 3]]);
      }
    } else if (cmd === "A") {
      for (let j = 0; j < nums.length; j += 7) {
        commands.push([
          cmd,
          nums[j],
          nums[j + 1],
          nums[j + 2],
          nums[j + 3],
          nums[j + 4],
          nums[j + 5],
          nums[j + 6]
        ]);
      }
    }
  }
  if (commands.length === 0) {
    commands.push(["M", 0, 0]);
  }
  return commands;
}
function pathBbox(arr) {
  let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
  arr.forEach((cmd) => {
    for (let i2 = 1; i2 < cmd.length; i2 += 2) {
      if (i2 + 1 <= cmd.length) {
        const x = cmd[i2];
        const y = cmd[i2 + 1];
        if (typeof x === "number" && typeof y === "number") {
          if (x < minX) minX = x;
          if (x > maxX) maxX = x;
          if (y < minY) minY = y;
          if (y > maxY) maxY = y;
        }
      }
    }
  });
  if (minX === Infinity) {
    return { x: 0, y: 0, width: 0, height: 0 };
  }
  return { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
}
function arrayToPath(arr) {
  return arr.map((cmd) => cmd.join(" ")).join(" ");
}
function simplify(val) {
  switch (val[0]) {
    case "z":
    case "Z":
      val[0] = "L";
      val[1] = this.start[0];
      val[2] = this.start[1];
      break;
    case "H":
      val[0] = "L";
      val[2] = this.pos[1];
      break;
    case "V":
      val[0] = "L";
      val[2] = val[1];
      val[1] = this.pos[0];
      break;
    case "T":
      val[0] = "Q";
      val[3] = val[1];
      val[4] = val[2];
      val[1] = this.reflection[1];
      val[2] = this.reflection[0];
      break;
    case "S":
      val[0] = "C";
      val[6] = val[4];
      val[5] = val[3];
      val[4] = val[2];
      val[3] = val[1];
      val[2] = this.reflection[1];
      val[1] = this.reflection[0];
      break;
  }
  return val;
}
function setPosAndReflection(val) {
  var len = val.length;
  this.pos = [val[len - 2], val[len - 1]];
  if ("SCQT".indexOf(val[0]) != -1) {
    this.reflection = [
      2 * this.pos[0] - val[len - 4],
      2 * this.pos[1] - val[len - 3]
    ];
  }
  return val;
}
function toBezier(val) {
  var _a;
  var retVal = [val];
  switch (val[0]) {
    case "M":
      this.pos = this.start = [val[1], val[2]];
      return retVal;
    case "L":
      val[5] = val[3] = val[1];
      val[6] = val[4] = val[2];
      val[1] = this.pos[0];
      val[2] = this.pos[1];
      break;
    case "Q":
      val[6] = val[4];
      val[5] = val[3];
      val[4] = val[4] * 1 / 3 + val[2] * 2 / 3;
      val[3] = val[3] * 1 / 3 + val[1] * 2 / 3;
      val[2] = this.pos[1] * 1 / 3 + val[2] * 2 / 3;
      val[1] = this.pos[0] * 1 / 3 + val[1] * 2 / 3;
      break;
    case "A":
      retVal = arcToBezier((_a = this.pos) != null ? _a : [], val);
      val = retVal[0];
      break;
  }
  val[0] = "C";
  this.pos = [val[5], val[6]];
  this.reflection = [2 * val[5] - val[3], 2 * val[6] - val[4]];
  return retVal;
}
function findNextM(arr, offset) {
  if (offset === false) return false;
  for (var i2 = offset, len = arr.length; i2 < len; ++i2) {
    if (arr[i2][0] == "M") return i2;
  }
  return false;
}
function arcToBezier(pos, val) {
  var rx = Math.abs(val[1]), ry = Math.abs(val[2]), xAxisRotation = val[3] % 360, largeArcFlag = val[4], sweepFlag = val[5], x = val[6], y = val[7], A = new Point(pos[0], pos[1]), B = new Point(x, y), primedCoord, lambda, mat, k, c2, cSquare, t2, O, OA, OB, tetaStart, tetaEnd, deltaTeta, nbSectors, f, arcSegPoints, angle, sinAngle, cosAngle, pt, i2, il, retVal = [], x1, y1, x2, y2;
  if (rx === 0 || ry === 0 || A.x === B.x && A.y === B.y) {
    return [["C", A.x, A.y, B.x, B.y, B.x, B.y]];
  }
  primedCoord = new Point((A.x - B.x) / 2, (A.y - B.y) / 2).transform(
    // Start with the identity matrix (no args → Matrix defaults a=d=1, others 0).
    // Passing all-zero args here would produce a degenerate zero matrix, since
    // `0 ?? 1` is `0`, not `1` — every subsequent transform then yields (0,0)
    // and the arc-to-bezier conversion crashes on a NaN cascade.
    /** @type {any} */
    new Matrix().rotate(xAxisRotation)
  );
  lambda = primedCoord.x * primedCoord.x / (rx * rx) + primedCoord.y * primedCoord.y / (ry * ry);
  if (lambda > 1) {
    lambda = Math.sqrt(lambda);
    rx = lambda * rx;
    ry = lambda * ry;
  }
  mat = /** @type {any} */
  new Matrix().rotate(xAxisRotation).scale(1 / rx, 1 / ry).rotate(-xAxisRotation);
  A = A.transform(mat);
  B = B.transform(mat);
  k = [B.x - A.x, B.y - A.y];
  cSquare = k[0] * k[0] + k[1] * k[1];
  c2 = Math.sqrt(cSquare);
  k[0] /= c2;
  k[1] /= c2;
  t2 = cSquare < 4 ? Math.sqrt(1 - cSquare / 4) : 0;
  if (largeArcFlag === sweepFlag) {
    t2 *= -1;
  }
  O = new Point((B.x + A.x) / 2 + t2 * -k[1], (B.y + A.y) / 2 + t2 * k[0]);
  OA = new Point(A.x - O.x, A.y - O.y);
  OB = new Point(B.x - O.x, B.y - O.y);
  tetaStart = Math.acos(OA.x / Math.sqrt(OA.x * OA.x + OA.y * OA.y));
  if (OA.y < 0) tetaStart *= -1;
  tetaEnd = Math.acos(OB.x / Math.sqrt(OB.x * OB.x + OB.y * OB.y));
  if (OB.y < 0) tetaEnd *= -1;
  if (sweepFlag && tetaStart > tetaEnd) {
    tetaEnd += 2 * Math.PI;
  }
  if (!sweepFlag && tetaStart < tetaEnd) {
    tetaEnd -= 2 * Math.PI;
  }
  nbSectors = Math.ceil(Math.abs(tetaStart - tetaEnd) * 2 / Math.PI);
  arcSegPoints = [];
  angle = tetaStart;
  deltaTeta = (tetaEnd - tetaStart) / nbSectors;
  f = 4 * Math.tan(deltaTeta / 4) / 3;
  for (i2 = 0; i2 <= nbSectors; i2++) {
    cosAngle = Math.cos(angle);
    sinAngle = Math.sin(angle);
    pt = new Point(O.x + cosAngle, O.y + sinAngle);
    arcSegPoints[i2] = [
      new Point(pt.x + f * sinAngle, pt.y - f * cosAngle),
      pt,
      new Point(pt.x - f * sinAngle, pt.y + f * cosAngle)
    ];
    angle += deltaTeta;
  }
  arcSegPoints[0][0] = arcSegPoints[0][1].clone();
  arcSegPoints[arcSegPoints.length - 1][2] = arcSegPoints[arcSegPoints.length - 1][1].clone();
  mat = /** @type {any} */
  new Matrix().rotate(xAxisRotation).scale(rx, ry).rotate(-xAxisRotation);
  for (i2 = 0, il = arcSegPoints.length; i2 < il; i2++) {
    arcSegPoints[i2][0] = arcSegPoints[i2][0].transform(mat);
    arcSegPoints[i2][1] = arcSegPoints[i2][1].transform(mat);
    arcSegPoints[i2][2] = arcSegPoints[i2][2].transform(mat);
  }
  for (i2 = 1, il = arcSegPoints.length; i2 < il; i2++) {
    pt = arcSegPoints[i2 - 1][2];
    x1 = pt.x;
    y1 = pt.y;
    pt = arcSegPoints[i2][0];
    x2 = pt.x;
    y2 = pt.y;
    pt = arcSegPoints[i2][1];
    x = pt.x;
    y = pt.y;
    retVal.push(["C", x1, y1, x2, y2, x, y]);
  }
  return retVal;
}
function handleBlock(startArr, startOffsetM, startOffsetNextM, destArr, destOffsetM, destOffsetNextM) {
  var startArrTemp = startArr.slice(startOffsetM, startOffsetNextM || void 0);
  var destArrTemp = destArr.slice(destOffsetM, destOffsetNextM || void 0);
  var i2 = 0, posStart = { pos: [0, 0], start: [0, 0] }, posDest = { pos: [0, 0], start: [0, 0] };
  while (true) {
    startArrTemp[i2] = simplify.call(posStart, startArrTemp[i2]);
    destArrTemp[i2] = simplify.call(posDest, destArrTemp[i2]);
    if (startArrTemp[i2][0] != destArrTemp[i2][0] || startArrTemp[i2][0] == "M" || startArrTemp[i2][0] == "A" && (startArrTemp[i2][4] != destArrTemp[i2][4] || startArrTemp[i2][5] != destArrTemp[i2][5])) {
      Array.prototype.splice.apply(
        startArrTemp,
        /** @type {[number, number, ...any[]]} */
        [i2, 1].concat(
          /** @type {any} */
          toBezier.call(posStart, startArrTemp[i2])
        )
      );
      Array.prototype.splice.apply(
        destArrTemp,
        /** @type {[number, number, ...any[]]} */
        [i2, 1].concat(
          /** @type {any} */
          toBezier.call(posDest, destArrTemp[i2])
        )
      );
    } else {
      startArrTemp[i2] = /** @type {any} */
      setPosAndReflection.call(
        posStart,
        startArrTemp[i2]
      );
      destArrTemp[i2] = /** @type {any} */
      setPosAndReflection.call(
        posDest,
        destArrTemp[i2]
      );
    }
    if (++i2 == startArrTemp.length && i2 == destArrTemp.length) break;
    if (i2 == startArrTemp.length) {
      startArrTemp.push([
        "C",
        posStart.pos[0],
        posStart.pos[1],
        posStart.pos[0],
        posStart.pos[1],
        posStart.pos[0],
        posStart.pos[1]
      ]);
    }
    if (i2 == destArrTemp.length) {
      destArrTemp.push([
        "C",
        posDest.pos[0],
        posDest.pos[1],
        posDest.pos[0],
        posDest.pos[1],
        posDest.pos[0],
        posDest.pos[1]
      ]);
    }
  }
  return { start: startArrTemp, dest: destArrTemp };
}
function synchronizePaths(fromD, toD) {
  var startArr = parsePath(fromD);
  var destArr = parsePath(toD);
  var startOffsetM = 0;
  var destOffsetM = 0;
  var startOffsetNextM = false;
  var destOffsetNextM = false;
  var result;
  while (true) {
    if (startOffsetM === false && destOffsetM === false) break;
    startOffsetNextM = findNextM(
      startArr,
      startOffsetM === false ? false : startOffsetM + 1
    );
    destOffsetNextM = findNextM(
      destArr,
      destOffsetM === false ? false : destOffsetM + 1
    );
    if (startOffsetM === false) {
      const bbox = pathBbox(
        /** @type {any} */
        result.start
      );
      if (bbox.height == 0 || bbox.width == 0) {
        startOffsetM = startArr.push(startArr[0]) - 1;
      } else {
        startOffsetM = startArr.push([
          "M",
          bbox.x + bbox.width / 2,
          bbox.y + bbox.height / 2
        ]) - 1;
      }
    }
    if (destOffsetM === false) {
      const bbox = pathBbox(
        /** @type {any} */
        result.dest
      );
      if (bbox.height == 0 || bbox.width == 0) {
        destOffsetM = destArr.push(destArr[0]) - 1;
      } else {
        destOffsetM = destArr.push([
          "M",
          bbox.x + bbox.width / 2,
          bbox.y + bbox.height / 2
        ]) - 1;
      }
    }
    result = handleBlock(
      startArr,
      startOffsetM,
      startOffsetNextM,
      destArr,
      destOffsetM,
      destOffsetNextM
    );
    startArr = startArr.slice(0, startOffsetM).concat(
      result.start,
      startOffsetNextM === false ? [] : startArr.slice(startOffsetNextM)
    );
    destArr = destArr.slice(0, destOffsetM).concat(
      result.dest,
      destOffsetNextM === false ? [] : destArr.slice(destOffsetNextM)
    );
    startOffsetM = startOffsetNextM === false ? false : startOffsetM + result.start.length;
    destOffsetM = destOffsetNextM === false ? false : destOffsetM + result.dest.length;
  }
  return { start: startArr, dest: destArr };
}
function morphPaths(fromD, toD) {
  var synced = synchronizePaths(fromD, toD);
  var startArr = synced.start;
  var destArr = synced.dest;
  return function(pos) {
    var result = startArr.map(function(from, idx) {
      return destArr[idx].map(function(to, toIdx) {
        if (toIdx === 0) return to;
        return from[toIdx] + (destArr[idx][toIdx] - from[toIdx]) * pos;
      });
    });
    return arrayToPath(result);
  };
}
let _measureSvg = null;
let _measurePath = null;
function samplePathPoints(d, n2) {
  const pts = new Array(n2);
  if (!Environment.isBrowser()) {
    const arr = parsePath(d);
    const bbox = pathBbox(arr);
    const cx = bbox.x + bbox.width / 2;
    const cy = bbox.y + bbox.height / 2;
    for (let i2 = 0; i2 < n2; i2++) pts[i2] = { x: cx, y: cy };
    return pts;
  }
  if (!_measureSvg) {
    _measureSvg = /** @type {SVGSVGElement} */
    document.createElementNS("http://www.w3.org/2000/svg", "svg");
    _measureSvg.setAttribute("width", "0");
    _measureSvg.setAttribute("height", "0");
    _measureSvg.setAttribute(
      "style",
      "position:absolute;width:0;height:0;visibility:hidden;pointer-events:none;"
    );
    _measurePath = /** @type {SVGPathElement} */
    document.createElementNS("http://www.w3.org/2000/svg", "path");
    _measureSvg.appendChild(_measurePath);
    document.body.appendChild(_measureSvg);
  }
  _measurePath.setAttribute("d", d || "M0 0");
  let len = 0;
  try {
    len = _measurePath.getTotalLength();
  } catch (e2) {
    len = 0;
  }
  if (!len || !isFinite(len)) {
    const arr = parsePath(d);
    const bbox = pathBbox(arr);
    const cx = bbox.x + bbox.width / 2;
    const cy = bbox.y + bbox.height / 2;
    for (let i2 = 0; i2 < n2; i2++) pts[i2] = { x: cx, y: cy };
    return pts;
  }
  for (let i2 = 0; i2 < n2; i2++) {
    try {
      const p2 = _measurePath.getPointAtLength(i2 / n2 * len);
      pts[i2] = { x: p2.x, y: p2.y };
    } catch (e2) {
      pts[i2] = { x: 0, y: 0 };
    }
  }
  return pts;
}
function morphPolygons(fromD, toD, n2 = 96) {
  const fromPts = samplePathPoints(fromD, n2);
  const toPts = samplePathPoints(toD, n2);
  let bestOffset = 0;
  let bestDist = Infinity;
  for (let off = 0; off < n2; off++) {
    let dist = 0;
    for (let i2 = 0; i2 < n2; i2++) {
      const a2 = fromPts[(i2 + off) % n2];
      const b2 = toPts[i2];
      const dx = a2.x - b2.x;
      const dy = a2.y - b2.y;
      dist += dx * dx + dy * dy;
      if (dist >= bestDist) break;
    }
    if (dist < bestDist) {
      bestDist = dist;
      bestOffset = off;
    }
  }
  const aligned = new Array(n2);
  for (let i2 = 0; i2 < n2; i2++) {
    aligned[i2] = fromPts[(i2 + bestOffset) % n2];
  }
  return function(pos) {
    let out = "";
    for (let i2 = 0; i2 < n2; i2++) {
      const a2 = aligned[i2];
      const b2 = toPts[i2];
      const x = a2.x + (b2.x - a2.x) * pos;
      const y = a2.y + (b2.y - a2.y) * pos;
      out += (i2 === 0 ? "M" : "L") + x.toFixed(3) + " " + y.toFixed(3) + " ";
    }
    return out + "Z";
  };
}
function easeInOut(t2) {
  return -Math.cos(t2 * Math.PI) / 2 + 0.5;
}
let _defaultEasing = easeInOut;
function setDefaultEasing(fn) {
  _defaultEasing = typeof fn === "function" ? fn : easeInOut;
}
function parseColor$1(str) {
  if (!str || typeof str !== "string") return null;
  if (str[0] === "#") {
    let hex = str.slice(1);
    if (hex.length === 3)
      hex = hex[0] + hex[0] + hex[1] + hex[1] + hex[2] + hex[2];
    const n2 = parseInt(hex, 16);
    return [n2 >> 16 & 255, n2 >> 8 & 255, n2 & 255, 1];
  }
  const m2 = str.match(
    /rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*(?:,\s*([\d.]+))?\s*\)/
  );
  if (m2) return [+m2[1], +m2[2], +m2[3], m2[4] !== void 0 ? +m2[4] : 1];
  return null;
}
function interpolateColor(from, to, pos) {
  return `rgba(${Math.round(from[0] + (to[0] - from[0]) * pos)},${Math.round(from[1] + (to[1] - from[1]) * pos)},${Math.round(from[2] + (to[2] - from[2]) * pos)},${from[3] + (to[3] - from[3]) * pos})`;
}
class SVGAnimationRunner {
  /**
   * @param {any} element
   * @param {number} duration
   * @param {number} delay
   */
  constructor(element, duration, delay) {
    this.el = element;
    this.duration = duration != null ? duration : 300;
    this.delay = delay || 0;
    this._attrTarget = null;
    this._plotTarget = null;
    this._plotSnap = null;
    this._plotAlgorithm = "commands";
    this._afterCb = null;
    this._duringCb = null;
    this._easing = null;
    this._next = null;
    this._root = null;
    this._scheduled = false;
  }
  /**
   * Override the easing for this runner (else the module default is used).
   * @param {(t:number)=>number} fn
   */
  ease(fn) {
    if (typeof fn === "function") this._easing = fn;
    this._schedule();
    return this;
  }
  /**
   * @param {Record<string, any>} to
   */
  attr(to) {
    this._attrTarget = to;
    this._schedule();
    return this;
  }
  /**
   * @param {string} d
   * @param {'commands' | 'polygons'} [algorithm] - morph engine to use for
   *   the d→d interpolation. 'commands' (default) is the legacy
   *   per-command lerp; 'polygons' resamples both paths into N evenly
   *   spaced points and tweens point-by-point (smoother for shapes with
   *   very different anchor-point counts).
   * @param {string} [snapTo] - final d to land on when it differs from the
   *   interpolation target. Used by reconciled length-change morphs: the
   *   tween runs against a padded path (extra anchors lying exactly on the
   *   final geometry) but the element must end with the renderer's clean,
   *   un-padded d so later captures and morphs stay stable.
   */
  plot(d, algorithm, snapTo) {
    this._plotTarget = d;
    if (algorithm) this._plotAlgorithm = algorithm;
    this._plotSnap = snapTo || null;
    this._schedule();
    return this;
  }
  /**
   * @param {Function} fn
   */
  after(fn) {
    this._afterCb = fn;
    this._schedule();
    return this;
  }
  /**
   * @param {Function} fn
   */
  during(fn) {
    this._duringCb = fn;
    this._schedule();
    return this;
  }
  /**
   * @param {number} duration
   * @param {number} delay
   */
  animate(duration, delay) {
    const next = new SVGAnimationRunner(this.el, duration, delay);
    this._next = next;
    next._root = this._root || this;
    return next;
  }
  _schedule() {
    const root = this._root || this;
    if (!root._scheduled) {
      root._scheduled = true;
      queueMicrotask(() => root._executeChain());
    }
  }
  _executeChain() {
    const chain = [];
    let r2 = this;
    while (r2) {
      chain.push(r2);
      r2 = r2._next;
    }
    let cumulativeDelay = 0;
    chain.forEach((runner) => {
      cumulativeDelay += runner.delay;
      runner._execute(cumulativeDelay);
      cumulativeDelay += runner.duration;
    });
  }
  /**
   * @param {number} startDelay
   */
  _execute(startDelay) {
    const el = this.el;
    const duration = this.duration;
    if (duration <= 1) {
      const apply = () => {
        if (this._attrTarget) el.attr(this._attrTarget);
        if (this._plotTarget) el.plot(this._plotSnap || this._plotTarget);
        if (this._afterCb) this._afterCb.call(el);
      };
      if (startDelay > 0) {
        setTimeout(apply, startDelay);
      } else {
        apply();
      }
      return;
    }
    const run = () => {
      const fromAttrs = (
        /** @type {Record<string, any>} */
        {}
      );
      const fromColors = (
        /** @type {Record<string, any>} */
        {}
      );
      const toColors = (
        /** @type {Record<string, any>} */
        {}
      );
      if (this._attrTarget) {
        for (const key of Object.keys(this._attrTarget)) {
          const fromVal = el.attr(key);
          fromAttrs[key] = fromVal;
          const fc = parseColor$1(fromVal);
          const tc = parseColor$1(String(this._attrTarget[key]));
          if (fc && tc) {
            fromColors[key] = fc;
            toColors[key] = tc;
          }
        }
      }
      let morphFn = null;
      if (this._plotTarget) {
        const fromPath = el.attr("d") || "";
        try {
          morphFn = this._plotAlgorithm === "polygons" ? morphPolygons(fromPath, this._plotTarget) : morphPaths(fromPath, this._plotTarget);
        } catch (e2) {
          morphFn = null;
        }
      }
      const start = performance.now();
      const easing = this._easing || _defaultEasing;
      const tick = (now) => {
        const elapsed = now - start;
        const rawPos = Math.min(elapsed / duration, 1);
        const pos = easing(rawPos);
        if (this._attrTarget) {
          if (rawPos >= 1) {
            el.attr(this._attrTarget);
          } else {
            const current = (
              /** @type {Record<string, any>} */
              {}
            );
            for (const key of Object.keys(this._attrTarget)) {
              if (fromColors[key] && toColors[key]) {
                current[key] = interpolateColor(
                  fromColors[key],
                  toColors[key],
                  pos
                );
              } else {
                const from = parseFloat(fromAttrs[key]);
                const to = parseFloat(this._attrTarget[key]);
                if (!isNaN(from) && !isNaN(to)) {
                  current[key] = from + (to - from) * pos;
                }
              }
            }
            el.attr(current);
          }
        }
        if (morphFn && rawPos < 1) {
          el.attr(
            "d",
            /** @type {any} */
            morphFn(pos)
          );
        }
        if (this._duringCb) this._duringCb(pos);
        if (rawPos < 1) {
          BrowserAPIs.requestAnimationFrame(tick);
        } else {
          if (this._plotTarget) {
            el.attr("d", this._plotSnap || this._plotTarget);
          }
          if (this._afterCb) this._afterCb.call(el);
        }
      };
      BrowserAPIs.requestAnimationFrame(tick);
    };
    if (startDelay > 0) {
      setTimeout(run, startDelay);
    } else {
      run();
    }
  }
}
function installAnimationMethods(ElementClass) {
  ElementClass.prototype.animate = function(duration, delay) {
    return new SVGAnimationRunner(this, duration, delay);
  };
}
function easeInOutSine(t2) {
  return -Math.cos(t2 * Math.PI) / 2 + 0.5;
}
function cubicBezier(x1, y1, x2, y2) {
  x1 = Math.min(Math.max(x1, 0), 1);
  x2 = Math.min(Math.max(x2, 0), 1);
  const cx = 3 * x1;
  const bx = 3 * (x2 - x1) - cx;
  const ax = 1 - cx - bx;
  const cy = 3 * y1;
  const by = 3 * (y2 - y1) - cy;
  const ay = 1 - cy - by;
  const sampleX = (t2) => ((ax * t2 + bx) * t2 + cx) * t2;
  const sampleY = (t2) => ((ay * t2 + by) * t2 + cy) * t2;
  const solveT = (x) => {
    let lo = 0;
    let hi = 1;
    let t2 = x;
    if (t2 < lo) return lo;
    if (t2 > hi) return hi;
    while (lo < hi) {
      const xt = sampleX(t2);
      if (Math.abs(xt - x) < 1e-4) return t2;
      if (x > xt) lo = t2;
      else hi = t2;
      t2 = (lo + hi) / 2;
    }
    return t2;
  };
  return (t2) => t2 <= 0 ? 0 : t2 >= 1 ? 1 : sampleY(solveT(t2));
}
const REGISTRY = /* @__PURE__ */ new Map();
const linear = (t2) => t2;
REGISTRY.set("linear", linear);
REGISTRY.set("easeInOutSine", easeInOutSine);
REGISTRY.set("easeInSine", (t2) => 1 - Math.cos(t2 * Math.PI / 2));
REGISTRY.set("easeOutSine", (t2) => Math.sin(t2 * Math.PI / 2));
REGISTRY.set("easeInQuad", (t2) => t2 * t2);
REGISTRY.set("easeOutQuad", (t2) => 1 - (1 - t2) * (1 - t2));
REGISTRY.set(
  "easeInOutQuad",
  (t2) => t2 < 0.5 ? 2 * t2 * t2 : 1 - Math.pow(-2 * t2 + 2, 2) / 2
);
REGISTRY.set("easeInCubic", (t2) => t2 * t2 * t2);
REGISTRY.set("easeOutCubic", (t2) => 1 - Math.pow(1 - t2, 3));
REGISTRY.set(
  "easeInOutCubic",
  (t2) => t2 < 0.5 ? 4 * t2 * t2 * t2 : 1 - Math.pow(-2 * t2 + 2, 3) / 2
);
REGISTRY.set("easeOutBack", (t2) => {
  const c1 = 1.70158;
  const c3 = c1 + 1;
  return 1 + c3 * Math.pow(t2 - 1, 3) + c1 * Math.pow(t2 - 1, 2);
});
REGISTRY.set("easeInOutBack", (t2) => {
  const c1 = 1.70158;
  const c2 = c1 * 1.525;
  return t2 < 0.5 ? Math.pow(2 * t2, 2) * ((c2 + 1) * 2 * t2 - c2) / 2 : (Math.pow(2 * t2 - 2, 2) * ((c2 + 1) * (t2 * 2 - 2) + c2) + 2) / 2;
});
function registerEasing(name2, fn) {
  if (typeof name2 === "string" && name2 && typeof fn === "function") {
    REGISTRY.set(name2, fn);
  }
}
function isBezierArray(v2) {
  return Array.isArray(v2) && v2.length === 4 && v2.every((n2) => typeof n2 === "number");
}
function resolveEasing(value) {
  if (typeof value === "function") return guardEasing(value);
  if (isBezierArray(value))
    return cubicBezier(value[0], value[1], value[2], value[3]);
  if (typeof value === "string" && REGISTRY.has(value)) {
    return guardEasing(
      /** @type {(t:number)=>number} */
      REGISTRY.get(value)
    );
  }
  return easeInOutSine;
}
function guardEasing(fn) {
  return (t2) => {
    const y = fn(t2);
    return typeof y === "number" && isFinite(y) ? y : t2;
  };
}
const SVGNS$1 = "http://www.w3.org/2000/svg";
function easeOutCubic$1(t2) {
  return 1 - Math.pow(1 - t2, 3);
}
function easeOutBack$1(t2) {
  const c1 = 1.70158;
  const c3 = c1 + 1;
  return 1 + c3 * Math.pow(t2 - 1, 3) + c1 * Math.pow(t2 - 1, 2);
}
let _reducedMotionMql = null;
function prefersReducedMotion() {
  if (!Environment.isBrowser()) return false;
  try {
    if (!_reducedMotionMql) {
      _reducedMotionMql = window.matchMedia("(prefers-reduced-motion: reduce)");
    }
    return !!_reducedMotionMql.matches;
  } catch (_) {
    return false;
  }
}
function applyProgressiveReveal(el, x, w2) {
  if (!Environment.isBrowser()) return false;
  if (w2.globals.dataChanged || w2.globals.resized) return false;
  const animCfg = w2.config.chart.animations;
  if (!animCfg || animCfg.enabled === false) return false;
  const chartType = w2.config.chart.type;
  if (chartType !== "line" && chartType !== "area" && chartType !== "rangeArea") {
    return false;
  }
  if (!(w2.layout.gridWidth > 0)) return false;
  const drawSpeed = (animCfg.speed || 800) * 2;
  const xRatio = Math.max(0, Math.min(1, x / w2.layout.gridWidth));
  const easedT = 1 - Math.cbrt(1 - xRatio);
  const revealDelay = easedT * drawSpeed;
  const node = el.node;
  const style = node.style;
  style.opacity = "0";
  let startAnchor = null;
  const tick = (now) => {
    if (startAnchor === null) startAnchor = now;
    if (now - startAnchor >= revealDelay) {
      style.opacity = "";
    } else {
      BrowserAPIs.requestAnimationFrame(tick);
    }
  };
  BrowserAPIs.requestAnimationFrame(tick);
  return true;
}
function applyAnimationPolicy(w2) {
  const anim = w2.config.chart.animations;
  if (!anim) return;
  if (anim.respectReducedMotion !== false && prefersReducedMotion()) {
    anim.enabled = false;
    if (anim.dynamicAnimation) anim.dynamicAnimation.enabled = false;
  }
  setDefaultEasing(resolveEasing(anim.easing));
}
function computeStagger(opts) {
  const style = opts.style;
  const index = opts.index || 0;
  const baseDelay = typeof opts.baseDelay === "number" ? opts.baseDelay : 40;
  const row = opts.row || 0;
  const col = opts.col || 0;
  const groupIndex = opts.groupIndex || 0;
  const perGroup = opts.perGroup || 1;
  const centerDistance = opts.centerDistance || 0;
  switch (style) {
    case "none":
      return 0;
    case "diagonal":
      return (row + col) * baseDelay;
    case "group":
      return groupIndex * baseDelay + index % perGroup * (baseDelay / 4);
    case "centroid":
      return centerDistance * baseDelay * (index + 1);
    case "sequential":
    default:
      return index * baseDelay;
  }
}
class Animations {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} [ctx]
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
  }
  /**
   * @param {any} el
   * @param {Record<string, any>} from
   * @param {Record<string, any>} to
   * @param {object} speed
   */
  animateLine(el, from, to, speed) {
    el.attr(from).animate(speed).attr(to);
  }
  /*
   ** Animate radius of a circle element
   * @param {any} el
   * @param {number} speed
   * @param {string} easing
   * @param {Function} cb
   */
  /** @param {any} el @param {any} speed @param {any} easing @param {any} cb */
  animateMarker(el, speed, easing, cb) {
    el.attr({
      opacity: 0
    }).animate(speed).attr({
      opacity: 1
    }).after(() => {
      cb();
    });
  }
  /**
   * Scale-up "pop" effect for scatter/bubble markers. Animates
   * `transform: scale(0 → 1)` (with `easeOutBack` overshoot) plus opacity,
   * around the marker's own center via `transform-box: fill-box`.
   *
   * Falls back to instant render in SSR / when shouldAnimate is false.
   *
   * @param {any} el
   * @param {{ speed: number, delay?: number, onComplete?: () => void }} params
   */
  animatePop(el, { speed, delay = 0, onComplete }) {
    const w2 = this.w;
    if (!Environment.isBrowser() || !w2.globals.shouldAnimate || speed < 1) {
      if (onComplete) onComplete();
      return;
    }
    const node = el.node;
    const style = node.style;
    style.transformBox = "fill-box";
    style.transformOrigin = "center";
    style.transform = "scale(0)";
    style.opacity = "0";
    const startAt = performance.now() + delay;
    const step = (now) => {
      if (w2.globals.isDestroyed) return;
      const t2 = Math.max(0, Math.min(1, (now - startAt) / speed));
      style.transform = `scale(${easeOutBack$1(t2)})`;
      style.opacity = String(Math.min(1, t2 * 2));
      if (t2 < 1) {
        BrowserAPIs.requestAnimationFrame(step);
      } else {
        style.transform = "";
        style.transformOrigin = "";
        style.transformBox = "";
        style.opacity = "";
        if (onComplete) onComplete();
      }
    };
    BrowserAPIs.requestAnimationFrame(step);
  }
  /*
   ** Animate rect properties
   * @param {any} el
   * @param {any} from
   * @param {any} to
   * @param {number} speed
   * @param {Function} fn
   */
  /** @param {any} el @param {any} from @param {any} to @param {any} speed @param {any} fn @param {number} [delay] */
  animateRect(el, from, to, speed, fn, delay = 0) {
    el.attr(from).animate(speed, delay).attr(to).after(() => fn());
  }
  /**
   * @param {Record<string, any>} params
   */
  animatePathsGradually(params) {
    const {
      el,
      realIndex,
      j,
      fill,
      pathFrom,
      pathTo,
      pathToInterp,
      speed,
      delay,
      scrollMorph
    } = params;
    const me = this;
    const w2 = this.w;
    let delayFactor = 0;
    if (w2.config.chart.animations.animateGradually.enabled) {
      delayFactor = w2.config.chart.animations.animateGradually.delay;
    }
    if (w2.config.chart.animations.dynamicAnimation.enabled && w2.globals.dataChanged && w2.config.chart.type !== "bar") {
      delayFactor = 0;
    }
    me.morphSVG(
      el,
      realIndex,
      j,
      w2.config.chart.type === "line" && !w2.globals.comboCharts ? "stroke" : fill,
      pathFrom,
      pathTo,
      speed,
      delay * delayFactor,
      scrollMorph,
      pathToInterp
    );
  }
  /**
   * Opacity-fade reveal (see Graphics.renderPaths `revealViaFade`): hide this
   * path immediately, then reveal the whole series in a single CSS opacity fade
   * once the synchronous draw pass has finished. Used for two cases that both
   * want to avoid per-path morphs: the large-dataset bulk render (>
   * largeDatasetThreshold) and candlestick/boxPlot data-change updates (where an
   * index-based morph would slide candles around on zoom). Every faded path is
   * pushed to delayedElements, but only the first one schedules the reveal — a
   * lone requestAnimationFrame fires after all paths are in the DOM, so N paths
   * cost one frame callback instead of N morph timelines. The guard flag is
   * reset inside the callback (and in initGlobalVars) so subsequent renders
   * re-arm it.
   * @param {any} el
   */
  revealBulk(el) {
    const w2 = this.w;
    el.node.classList.add("apexcharts-element-hidden");
    w2.globals.delayedElements.push({ el: el.node });
    if (!Environment.isBrowser() || !w2.globals.shouldAnimate) {
      this.animationCompleted(el);
      return;
    }
    if (!w2.globals.bulkRevealScheduled) {
      w2.globals.bulkRevealScheduled = true;
      BrowserAPIs.requestAnimationFrame(() => {
        if (w2.globals.isDestroyed) return;
        w2.globals.bulkRevealScheduled = false;
        this.animationCompleted(el);
      });
    }
  }
  showDelayedElements() {
    this.w.globals.delayedElements.forEach((d) => {
      if (d.holdUntilComplete && !this.w.globals.animationEnded) return;
      const ele = d.el;
      ele.classList.remove("apexcharts-element-hidden");
      ele.classList.add("apexcharts-hidden-element-shown");
    });
  }
  /**
   * @param {any} el
   */
  animationCompleted(el) {
    const w2 = this.w;
    if (w2.globals.animationEnded) return;
    w2.globals.animationEnded = true;
    this.showDelayedElements();
    if (typeof w2.config.chart.events.animationEnd === "function") {
      w2.config.chart.events.animationEnd(this.ctx, { el, w: w2 });
    }
  }
  /**
   * Initial-mount "pen-stroke" draw effect for line / area / rangeArea paths.
   *
   * Stroke paths animate `stroke-dashoffset` from total length → 0.
   * Fill paths animate the width of a per-series SVG `<mask>` rect from 0 → gridWidth,
   * which coexists with the existing `clip-path: gridRectMask` (mask + clip-path
   * are independent SVG attributes).
   *
   * For radar / radial shapes, pass `mask: { type: 'radial', cx, cy, r }` to
   * use a circular mask that blooms from center outward instead of the default
   * left-to-right rect wipe.
   *
   * @param {any} el            - SVG.js path element
   * @param {{realIndex: number, j?: number, isFill: boolean, isLast: boolean, speed: number, delay: number, mask?: {type: 'rect'|'radial', cx?: number, cy?: number, r?: number}}} params
   */
  animateDraw(el, { realIndex, j, isFill, isLast, speed, delay, mask: maskShape }) {
    const w2 = this.w;
    const me = this;
    const finalize = () => {
      if (isLast && w2.globals.shouldAnimate) {
        me.animationCompleted(el);
      }
      me.showDelayedElements();
    };
    if (!Environment.isBrowser() || !w2.globals.shouldAnimate || speed < 1) {
      finalize();
      return;
    }
    const node = el.node;
    const runMaskReveal = () => {
      const pad = 4;
      const isRadial = maskShape && maskShape.type === "radial";
      const targetWidth = w2.layout.gridWidth + pad * 2;
      const radialCx = maskShape && maskShape.cx || 0;
      const radialCy = maskShape && maskShape.cy || 0;
      const targetRadius = (maskShape && maskShape.r || w2.layout.gridWidth / 2) + pad;
      const maskId = `apexDrawMask${w2.globals.cuid}-${realIndex}-${j != null ? j : 0}-${isFill ? "f" : "s"}`;
      const mask = BrowserAPIs.createElementNS(SVGNS$1, "mask");
      mask.setAttribute("id", maskId);
      mask.setAttribute("maskUnits", "userSpaceOnUse");
      let revealEl;
      if (isRadial) {
        const region = targetRadius;
        mask.setAttribute("x", String(radialCx - region));
        mask.setAttribute("y", String(radialCy - region));
        mask.setAttribute("width", String(region * 2));
        mask.setAttribute("height", String(region * 2));
        revealEl = BrowserAPIs.createElementNS(SVGNS$1, "circle");
        revealEl.setAttribute("cx", String(radialCx));
        revealEl.setAttribute("cy", String(radialCy));
        revealEl.setAttribute("r", "0");
        revealEl.setAttribute("fill", "#fff");
      } else {
        mask.setAttribute("x", String(-pad));
        mask.setAttribute("y", String(-pad));
        mask.setAttribute("width", String(targetWidth));
        mask.setAttribute("height", String(w2.layout.gridHeight + pad * 2));
        revealEl = BrowserAPIs.createElementNS(SVGNS$1, "rect");
        revealEl.setAttribute("x", String(-pad));
        revealEl.setAttribute("y", String(-pad));
        revealEl.setAttribute("width", "0");
        revealEl.setAttribute("height", String(w2.layout.gridHeight + pad * 2));
        revealEl.setAttribute("fill", "#fff");
      }
      mask.appendChild(revealEl);
      w2.dom.elDefs.node.appendChild(mask);
      node.setAttribute("mask", `url(#${maskId})`);
      const startAt = performance.now() + (delay || 0);
      const step = (now) => {
        if (w2.globals.isDestroyed) return;
        const t2 = Math.max(0, Math.min(1, (now - startAt) / speed));
        const eased = easeOutCubic$1(t2);
        if (isRadial) {
          revealEl.setAttribute("r", String(eased * targetRadius));
        } else {
          revealEl.setAttribute("width", String(eased * targetWidth));
        }
        if (t2 < 1) {
          BrowserAPIs.requestAnimationFrame(step);
        } else {
          node.removeAttribute("mask");
          if (mask.parentNode) mask.parentNode.removeChild(mask);
          finalize();
        }
      };
      BrowserAPIs.requestAnimationFrame(step);
    };
    const runStrokeDraw = (len) => {
      node.setAttribute("stroke-dasharray", String(len));
      node.setAttribute("stroke-dashoffset", String(len));
      const startAt = performance.now() + (delay || 0);
      const step = (now) => {
        if (w2.globals.isDestroyed) return;
        const t2 = Math.max(0, Math.min(1, (now - startAt) / speed));
        node.setAttribute("stroke-dashoffset", String(len * (1 - easeOutCubic$1(t2))));
        if (t2 < 1) {
          BrowserAPIs.requestAnimationFrame(step);
        } else {
          node.removeAttribute("stroke-dasharray");
          node.removeAttribute("stroke-dashoffset");
          finalize();
        }
      };
      BrowserAPIs.requestAnimationFrame(step);
    };
    BrowserAPIs.requestAnimationFrame(() => {
      if (w2.globals.isDestroyed) return;
      if (isFill) {
        runMaskReveal();
        return;
      }
      const existingDash = node.getAttribute("stroke-dasharray");
      const hasCustomDash = !!existingDash && existingDash !== "0" && existingDash !== "";
      if (hasCustomDash) {
        runMaskReveal();
        return;
      }
      let len = 0;
      try {
        if (typeof node.getTotalLength === "function") {
          len = node.getTotalLength();
        }
      } catch (_) {
        len = 0;
      }
      if (!len) {
        finalize();
        return;
      }
      runStrokeDraw(len);
    });
  }
  // SVG.js animation for morphing one path to another
  /**
   * @param {any} el
   * @param {number} realIndex
   * @param {number} j
   * @param {string} fill
   * @param {string} pathFrom
   * @param {string} pathTo
   * @param {number} speed
   * @param {number} delay
   * @param {boolean} [scrollMorph] - this morph is a streaming scroll (StreamScroll)
   * @param {string} [pathToInterp] - interpolation target that differs from the
   *   final path. Reconciled length-change morphs tween toward a padded copy of
   *   pathTo (extra anchors sitting exactly on the final geometry) and snap to
   *   the clean pathTo at the end.
   */
  morphSVG(el, realIndex, j, fill, pathFrom, pathTo, speed, delay, scrollMorph, pathToInterp) {
    var _a, _b;
    const w2 = this.w;
    if (!pathFrom) {
      pathFrom = el.attr("pathFrom");
    }
    if (!pathTo) {
      pathTo = el.attr("pathTo");
    }
    const disableAnimationForCorrupPath = () => {
      if (w2.config.chart.type === "radar") {
        speed = 1;
      }
      return `M 0 ${w2.layout.gridHeight}`;
    };
    if (!pathFrom || pathFrom.indexOf("undefined") > -1 || pathFrom.indexOf("NaN") > -1) {
      pathFrom = disableAnimationForCorrupPath();
    }
    if (!pathTo.trim() || pathTo.indexOf("undefined") > -1 || pathTo.indexOf("NaN") > -1) {
      pathTo = disableAnimationForCorrupPath();
      pathToInterp = void 0;
    }
    if (pathToInterp && (!pathToInterp.trim() || pathToInterp.indexOf("undefined") > -1 || pathToInterp.indexOf("NaN") > -1)) {
      pathToInterp = void 0;
    }
    if (!w2.globals.shouldAnimate) {
      speed = 1;
    }
    const crossTypeMorph = ((_b = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.isActive()) === true;
    const morphAlgo = crossTypeMorph ? "polygons" : "commands";
    let morphEase = null;
    if (w2.globals.dataChanged) {
      const dynEasing = w2.config.chart.animations.dynamicAnimation.easing;
      if (dynEasing != null) {
        morphEase = resolveEasing(dynEasing);
      } else if (scrollMorph) {
        morphEase = resolveEasing("linear");
      }
    }
    const runner = el.plot(pathFrom).animate(1, delay).plot(pathFrom).animate(speed, delay);
    if (morphEase) {
      runner.ease(morphEase);
    }
    runner.plot(
      pathToInterp || pathTo,
      morphAlgo,
      pathToInterp ? pathTo : void 0
    ).after(() => {
      if (Utils$1.isNumber(j)) {
        const maxSeries = w2.seriesData.series[w2.globals.maxValsInArrayIndex];
        if (maxSeries && j === maxSeries.length - 2 && w2.globals.shouldAnimate) {
          this.animationCompleted(el);
        }
      } else if (fill !== "none" && w2.globals.shouldAnimate) {
        if (!w2.globals.comboCharts && realIndex === w2.seriesData.series.length - 1 || w2.globals.comboCharts) {
          this.animationCompleted(el);
        }
      }
      this.showDelayedElements();
    });
  }
}
const DEFAULT_INTENSITY = { lighten: 0.15, darken: 0.35 };
class Filters {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
  }
  /**
   * The three chart types drawn by the Pie module.
   * @param {any} w
   */
  static isSliceChart(w2) {
    const type = w2.config.chart.type;
    return type === "pie" || type === "donut" || type === "polarArea";
  }
  /**
   * True when a pie / donut slice carries the hover state itself, as the
   * outline band traced outside its rim. The band IS the hover feedback, so
   * the states.hover lighten filter must not also recolour the slice: the
   * point of the band is that a hovered slice keeps its own colour.
   * `states.hover.filter.type: 'none'` still turns every hover visual off, so
   * this only claims the state when a hover visual was wanted at all.
   * @param {any} w
   */
  static hoverOutlineOwnsHoverState(w2) {
    var _a;
    return Filters.isSliceChart(w2) && w2.config.states.hover.filter.type !== "none" && ((_a = w2.config.plotOptions.pie.hoverOutline) == null ? void 0 : _a.show) === true;
  }
  /**
   * True when a pie / donut slice carries the selected state itself, by
   * sliding out of the pie (see Pie.offsetSlice). Moving out of the pie is a
   * strong enough signal on its own, so the states.active darken filter would
   * only muddy the slice colour on top of it. polarArea is excluded: it never
   * slides (its radius is the value), so it keeps the filter as its only
   * click feedback.
   * @param {any} w
   */
  static sliceOffsetOwnsActiveState(w2) {
    return Filters.isSliceChart(w2) && w2.config.chart.type !== "polarArea" && w2.config.plotOptions.pie.expandOnClick === true && w2.config.plotOptions.pie.expandOffset > 0 && !Filters.drilldownBlocksSliceOffset(w2);
  }
  /**
   * A drilldown pie / donut does not slide its slices out at all: a click there
   * is navigation, so the slice would pull out and then be thrown away by the
   * drill it just triggered, which reads as a glitch rather than as motion.
   * The states.active filter comes back as the click feedback (it is instant,
   * so the re-render lands on top of it rather than fighting it).
   * @param {any} w
   */
  static drilldownBlocksSliceOffset(w2) {
    var _a;
    return Filters.isSliceChart(w2) && ((_a = w2.config.drilldown) == null ? void 0 : _a.enabled) === true;
  }
  // create a re-usable filter which can be appended other filter effects and applied to multiple elements
  /**
   * @param {any} el
   * @param {number} i
   */
  getDefaultFilter(el, i2) {
    const w2 = this.w;
    if (el.unfilter) {
      el.unfilter(true);
    }
    if (w2.config.chart.dropShadow.enabled) {
      this.dropShadow(el, w2.config.chart.dropShadow, i2);
    }
  }
  /**
   * @param {any} el
   * @param {number} i
   * @param {string} filterType
   * @param {number} [intensity] Blend strength (0 to 1). Defaults per type.
   */
  applyFilter(el, i2, filterType, intensity) {
    var _a, _b, _c;
    const w2 = this.w;
    if (el.unfilter) {
      el.unfilter(true);
    }
    if (filterType === "none") {
      this.getDefaultFilter(el, i2);
      return;
    }
    const shadowAttr = w2.config.chart.dropShadow;
    const fallback = filterType === "lighten" ? DEFAULT_INTENSITY.lighten : DEFAULT_INTENSITY.darken;
    const t2 = Math.max(
      0,
      Math.min(1, typeof intensity === "number" ? intensity : fallback)
    );
    const diag = 1 - t2;
    const offset = filterType === "lighten" ? t2 : 0;
    if (el.filterWith) {
      el.filterWith((add) => {
        add.colorMatrix({
          type: "matrix",
          values: `
            ${diag} 0 0 0 ${offset}
            0 ${diag} 0 0 ${offset}
            0 0 ${diag} 0 ${offset}
            0 0 0 1 0
          `,
          in: "SourceGraphic",
          result: "brightness"
        });
        if (shadowAttr.enabled) {
          this.addShadow(add, i2, shadowAttr, "brightness");
        }
      });
      if (!shadowAttr.noUserSpaceOnUse) {
        (_b = (_a = el.filterer()) == null ? void 0 : _a.node) == null ? void 0 : _b.setAttribute("filterUnits", "userSpaceOnUse");
      }
      this._scaleFilterSize((_c = el.filterer()) == null ? void 0 : _c.node);
    }
  }
  // appends dropShadow to the filter object which can be chained with other filter effects
  /**
   * @param {any} add
   * @param {number} i
   * @param {Record<string, any>} attrs
   * @param {string} source
   */
  addShadow(add, i2, attrs, source) {
    var _a;
    const w2 = this.w;
    let { blur, top, left, color, opacity } = attrs;
    color = Array.isArray(color) ? color[i2] : color;
    if (((_a = w2.config.chart.dropShadow.enabledOnSeries) == null ? void 0 : _a.length) > 0) {
      if (w2.config.chart.dropShadow.enabledOnSeries.indexOf(i2) === -1) {
        return add;
      }
    }
    add.offset({
      in: source,
      dx: left,
      dy: top,
      result: "offset"
    });
    add.gaussianBlur({
      in: "offset",
      stdDeviation: blur,
      result: "blur"
    });
    add.flood({
      "flood-color": color,
      "flood-opacity": opacity,
      result: "flood"
    });
    add.composite({
      in: "flood",
      in2: "blur",
      operator: "in",
      result: "shadow"
    });
    add.merge(["shadow", source]);
  }
  // directly adds dropShadow to the element and returns the same element.
  /**
   * @param {any} el
   * @param {Record<string, any>} attrs
   */
  dropShadow(el, attrs, i2 = 0) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    if (el.unfilter) {
      el.unfilter(true);
    }
    if (Utils$1.isMsEdge() && w2.config.chart.type === "radialBar") {
      return el;
    }
    if (((_a = w2.config.chart.dropShadow.enabledOnSeries) == null ? void 0 : _a.length) > 0) {
      if (((_b = w2.config.chart.dropShadow.enabledOnSeries) == null ? void 0 : _b.indexOf(i2)) === -1) {
        return el;
      }
    }
    if (el.filterWith) {
      el.filterWith((add) => {
        this.addShadow(add, i2, attrs, "SourceGraphic");
      });
      if (!attrs.noUserSpaceOnUse) {
        (_d = (_c = el.filterer()) == null ? void 0 : _c.node) == null ? void 0 : _d.setAttribute("filterUnits", "userSpaceOnUse");
      }
      this._scaleFilterSize((_e = el.filterer()) == null ? void 0 : _e.node);
    }
    return el;
  }
  /**
   * @param {any} el
   * @param {number} realIndex
   * @param {number} dataPointIndex
   */
  setSelectionFilter(el, realIndex, dataPointIndex) {
    const w2 = this.w;
    if (typeof w2.interact.selectedDataPoints[realIndex] !== "undefined") {
      if (w2.interact.selectedDataPoints[realIndex].indexOf(dataPointIndex) > -1) {
        el.node.setAttribute("selected", true);
        if (Filters.sliceOffsetOwnsActiveState(w2)) return;
        const activeFilter = w2.config.states.active.filter;
        if (activeFilter.type !== "none") {
          this.applyFilter(el, realIndex, activeFilter.type, activeFilter.value);
        }
      }
    }
  }
  /**
   * @param {any} el
   */
  _scaleFilterSize(el) {
    if (!el) return;
    const setAttributes = (attrs) => {
      for (const key in attrs) {
        if (Object.prototype.hasOwnProperty.call(attrs, key)) {
          el.setAttribute(key, attrs[key]);
        }
      }
    };
    setAttributes({
      width: "200%",
      height: "200%",
      x: "-50%",
      y: "-50%"
    });
  }
}
class Graphics {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext | null} ctx
   */
  constructor(w2, ctx = null) {
    this.w = w2;
    this.ctx = ctx;
  }
  /*****************************************************************************
   *                                                                            *
   *  SVG Path Rounding Function                                                *
   *  Copyright (C) 2014 Yona Appletree                                         *
   *                                                                            *
   *  Licensed under the Apache License, Version 2.0 (the "License");           *
   *  you may not use this file except in compliance with the License.          *
   *  You may obtain a copy of the License at                                   *
   *                                                                            *
   *      http://www.apache.org/licenses/LICENSE-2.0                            *
   *                                                                            *
   *  Unless required by applicable law or agreed to in writing, software       *
   *  distributed under the License is distributed on an "AS IS" BASIS,         *
   *  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  *
   *  See the License for the specific language governing permissions and       *
   *  limitations under the License.                                            *
   *                                                                            *
   *****************************************************************************/
  /**
   * SVG Path rounding function. Takes an input path string and outputs a path
   * string where all line-line corners have been rounded. Only supports absolute
   * commands at the moment.
   *
   * @param pathString The SVG input path
   * @param radius The amount to round the corners, either a value in the SVG
   *               coordinate space, or, if useFractionalRadius is true, a value
   *               from 0 to 1.
   * @returns A new SVG path string with the rounding
   * @param {string} pathString
   * @param {number} radius
   */
  roundPathCorners(pathString, radius) {
    if (pathString.indexOf("NaN") > -1) pathString = "";
    function moveTowardsLength(movingPoint, targetPoint, amount) {
      var width = targetPoint.x - movingPoint.x;
      var height = targetPoint.y - movingPoint.y;
      var distance = Math.sqrt(width * width + height * height);
      return moveTowardsFractional(
        movingPoint,
        targetPoint,
        Math.min(1, amount / distance)
      );
    }
    function moveTowardsFractional(movingPoint, targetPoint, fraction) {
      return {
        x: movingPoint.x + (targetPoint.x - movingPoint.x) * fraction,
        y: movingPoint.y + (targetPoint.y - movingPoint.y) * fraction
      };
    }
    function adjustCommand(cmd, newPoint) {
      if (cmd.length > 2) {
        cmd[cmd.length - 2] = newPoint.x;
        cmd[cmd.length - 1] = newPoint.y;
      }
    }
    function pointForCommand(cmd) {
      return {
        x: parseFloat(cmd[cmd.length - 2]),
        y: parseFloat(cmd[cmd.length - 1])
      };
    }
    var pathParts = pathString.split(/[,\s]/).reduce(function(parts, part) {
      var match = part.match(/^([a-zA-Z])(.+)/);
      if (match) {
        parts.push(match[1]);
        parts.push(match[2]);
      } else {
        parts.push(part);
      }
      return parts;
    }, []);
    var commands = pathParts.reduce(function(commands2, part) {
      if (parseFloat(part) == part && commands2.length) {
        commands2[commands2.length - 1].push(part);
      } else {
        commands2.push([part]);
      }
      return commands2;
    }, []);
    var resultCommands = [];
    if (commands.length > 1) {
      var startPoint = pointForCommand(commands[0]);
      var virtualCloseLine = null;
      if (commands[commands.length - 1][0] == "Z" && commands[0].length > 2) {
        virtualCloseLine = ["L", startPoint.x, startPoint.y];
        commands[commands.length - 1] = virtualCloseLine;
      }
      resultCommands.push(commands[0]);
      for (var cmdIndex = 1; cmdIndex < commands.length; cmdIndex++) {
        var prevCmd = resultCommands[resultCommands.length - 1];
        var curCmd = commands[cmdIndex];
        var nextCmd = curCmd == virtualCloseLine ? commands[1] : commands[cmdIndex + 1];
        if (nextCmd && prevCmd && prevCmd.length > 2 && curCmd[0] == "L" && nextCmd.length > 2 && nextCmd[0] == "L") {
          var prevPoint = pointForCommand(prevCmd);
          var curPoint = pointForCommand(curCmd);
          var nextPoint = pointForCommand(nextCmd);
          var curveStart, curveEnd;
          curveStart = moveTowardsLength(curPoint, prevPoint, radius);
          curveEnd = moveTowardsLength(curPoint, nextPoint, radius);
          adjustCommand(curCmd, curveStart);
          curCmd.origPoint = curPoint;
          resultCommands.push(curCmd);
          var startControl = moveTowardsFractional(curveStart, curPoint, 0.5);
          var endControl = moveTowardsFractional(curPoint, curveEnd, 0.5);
          var curveCmd = [
            "C",
            startControl.x,
            startControl.y,
            endControl.x,
            endControl.y,
            curveEnd.x,
            curveEnd.y
          ];
          curveCmd.origPoint = curPoint;
          resultCommands.push(curveCmd);
        } else {
          resultCommands.push(curCmd);
        }
      }
      if (virtualCloseLine) {
        var newStartPoint = pointForCommand(
          resultCommands[resultCommands.length - 1]
        );
        resultCommands.push(["Z"]);
        adjustCommand(resultCommands[0], newStartPoint);
      }
    } else {
      resultCommands = commands;
    }
    return resultCommands.reduce(function(str, c2) {
      return str + c2.join(" ") + " ";
    }, "");
  }
  /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   * @param {number | null} [strokeWidth]
   */
  drawLine(x1, y1, x2, y2, lineColor = "#a8a8a8", dashArray = 0, strokeWidth = null, strokeLineCap = "butt") {
    const w2 = this.w;
    const line = w2.dom.Paper.line().attr({
      x1,
      y1,
      x2,
      y2,
      stroke: lineColor,
      "stroke-dasharray": dashArray,
      "stroke-width": strokeWidth,
      "stroke-linecap": strokeLineCap
    });
    return line;
  }
  /**
   * @param {number | null} [strokeWidth]
   * @param {string | null} [strokeColor]
   */
  drawRect(x1 = 0, y1 = 0, x2 = 0, y2 = 0, radius = 0, color = "#fefefe", opacity = 1, strokeWidth = null, strokeColor = null, strokeDashArray = 0) {
    const w2 = this.w;
    const rect = w2.dom.Paper.rect();
    rect.attr({
      x: x1,
      y: y1,
      width: x2 > 0 ? x2 : 0,
      height: y2 > 0 ? y2 : 0,
      rx: radius,
      ry: radius,
      opacity,
      "stroke-width": strokeWidth !== null ? strokeWidth : 0,
      stroke: strokeColor !== null ? strokeColor : "none",
      "stroke-dasharray": strokeDashArray
    });
    rect.node.setAttribute("fill", color);
    return rect;
  }
  /**
   * @param {string} polygonString
   */
  drawPolygon(polygonString, stroke = "#e1e1e1", strokeWidth = 1, fill = "none") {
    const w2 = this.w;
    const polygon = w2.dom.Paper.polygon(polygonString).attr({
      fill,
      stroke,
      "stroke-width": strokeWidth
    });
    return polygon;
  }
  /**
   * @param {number} radius
   * @param {Record<string, any> | null} attrs
   */
  drawCircle(radius, attrs = null) {
    const w2 = this.w;
    if (radius < 0) radius = 0;
    const c2 = w2.dom.Paper.circle(radius * 2);
    if (attrs !== null) {
      c2.attr(attrs);
    }
    return c2;
  }
  /** @param {{ d?: string, stroke?: string, strokeWidth?: number, fill: any, fillOpacity?: number, strokeOpacity?: number, classes?: any, strokeLinecap?: any, strokeDashArray?: number }} opts */
  drawPath({
    d = "",
    stroke = "#a8a8a8",
    strokeWidth = 1,
    fill,
    fillOpacity = 1,
    strokeOpacity = 1,
    classes,
    strokeLinecap = null,
    strokeDashArray = 0
  }) {
    const w2 = this.w;
    if (strokeLinecap === null) {
      strokeLinecap = w2.config.stroke.lineCap;
    }
    if (d.indexOf("undefined") > -1 || d.indexOf("NaN") > -1) {
      d = `M 0 ${w2.layout.gridHeight}`;
    }
    const p2 = w2.dom.Paper.path(d).attr({
      fill,
      "fill-opacity": fillOpacity,
      stroke,
      "stroke-opacity": strokeOpacity,
      "stroke-linecap": strokeLinecap,
      "stroke-width": strokeWidth,
      "stroke-dasharray": strokeDashArray,
      class: classes
    });
    return p2;
  }
  /**
   * @param {Record<string, any> | null} attrs
   */
  group(attrs = null) {
    const w2 = this.w;
    const g2 = w2.dom.Paper.group();
    if (attrs !== null) {
      g2.attr(attrs);
    }
    return g2;
  }
  /**
   * @param {number} x
   * @param {number} y
   */
  move(x, y) {
    const move = ["M", x, y].join(" ");
    return move;
  }
  /**
   * @param {number | null} x
   * @param {number | null} y
   * @param {string | null} hORv
   * @returns {string}
   */
  line(x, y, hORv = null) {
    if (hORv === "H") return [" H", x].join(" ");
    if (hORv === "V") return [" V", y].join(" ");
    return [" L", x, y].join(" ");
  }
  /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   * @param {number} x
   * @param {number} y
   */
  curve(x1, y1, x2, y2, x, y) {
    const curve = ["C", x1, y1, x2, y2, x, y].join(" ");
    return curve;
  }
  /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x
   * @param {number} y
   */
  quadraticCurve(x1, y1, x, y) {
    const curve = ["Q", x1, y1, x, y].join(" ");
    return curve;
  }
  /**
   * @param {number} rx
   * @param {number} ry
   * @param {number} axisRotation
   * @param {number} largeArcFlag
   * @param {number} sweepFlag
   * @param {number} x
   * @param {number} y
   */
  arc(rx, ry, axisRotation, largeArcFlag, sweepFlag, x, y, relative = false) {
    let coord = "A";
    if (relative) coord = "a";
    const arc = [
      coord,
      rx,
      ry,
      axisRotation,
      largeArcFlag,
      sweepFlag,
      x,
      y
    ].join(" ");
    return arc;
  }
  /**
   * @memberof Graphics
   * @param {Record<string, any>} opts
   *  i = series's index
   *  realIndex = realIndex is series's actual index when it was drawn time. After several redraws, the iterating "i" may change in loops, but realIndex doesn't
   *  pathFrom = existing pathFrom to animateTo
   *  pathTo = new Path to which d attr will be animated from pathFrom to pathTo
   *  stroke = line Color
   *  strokeWidth = width of path Line
   *  fill = it can be gradient, single color, pattern or image
   *  animationDelay = how much to delay when starting animation (in milliseconds)
   *  dataChangeSpeed = for dynamic animations, when data changes
   *  className = class attribute to add
   *  scrollMorph = this data-change morph is a streaming scroll (see StreamScroll);
   *                defaults the morph easing to linear so the slide is constant-velocity
   * @return {any} svg.js path object
   **/
  renderPaths({
    j,
    realIndex,
    pathFrom,
    pathTo,
    pathToInterp,
    stroke,
    strokeWidth,
    strokeLinecap,
    fill,
    animationDelay,
    initialSpeed,
    dataChangeSpeed,
    className,
    chartType,
    shouldClipToGrid = true,
    bindEventsOnPaths = true,
    drawShadow = true,
    drawMask = null,
    scrollMorph = false
  }) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const filters = new Filters(this.w);
    const anim = new Animations(this.w, (_a = this.ctx) != null ? _a : void 0);
    const initialAnim = this.w.config.chart.animations.enabled;
    const dynamicAnim = initialAnim && this.w.config.chart.animations.dynamicAnimation.enabled;
    if (pathFrom && pathFrom.startsWith("M 0 0 ") && pathTo) {
      const moveCommand = pathTo.match(/^M\s+[\d.-]+\s+[\d.-]+/);
      if (moveCommand) {
        pathFrom = pathFrom.replace(/^M\s+0\s+0/, moveCommand[0]);
      }
    }
    let d;
    const crossTypeMorph = ((_c = (_b = this.ctx) == null ? void 0 : _b.morphTypeChange) == null ? void 0 : _c.isActive()) === true;
    const shouldAnimate = !!(initialAnim && !w2.globals.resized || dynamicAnim && w2.globals.dataChanged && w2.globals.shouldAnimate || crossTypeMorph && initialAnim && w2.globals.shouldAnimate);
    const isDrawableSeries = typeof className === "string" && (className.indexOf("apexcharts-line") > -1 || className.indexOf("apexcharts-area") > -1 || className.indexOf("apexcharts-rangeArea") > -1 || className.indexOf("apexcharts-radar") > -1);
    const useDrawMode = !!(initialAnim && !w2.globals.resized && !w2.globals.dataChanged && isDrawableSeries);
    const largeThreshold = (_d = w2.config.chart.animations.largeDatasetThreshold) != null ? _d : 0;
    const bulkRender = !!(shouldAnimate && !useDrawMode && largeThreshold > 0 && w2.globals.dataPoints > largeThreshold);
    const isCandleOrBox = chartType === "candlestick" || chartType === "boxPlot";
    const fadeOnDataChange = !!(isCandleOrBox && shouldAnimate && !useDrawMode && w2.globals.dataChanged && // ...but a cross-type morph is a deliberate one-off with marks already
    // paired, so it keeps its tween instead of fading.
    !crossTypeMorph);
    const revealViaFade = bulkRender || fadeOnDataChange;
    if (shouldAnimate && !useDrawMode && !revealViaFade) {
      d = pathFrom;
    } else {
      d = pathTo;
      if (!shouldAnimate) {
        w2.globals.animationEnded = true;
      }
    }
    const strokeDashArrayOpt = w2.config.stroke.dashArray;
    let strokeDashArray = 0;
    if (Array.isArray(strokeDashArrayOpt)) {
      strokeDashArray = strokeDashArrayOpt[realIndex];
    } else {
      strokeDashArray = w2.config.stroke.dashArray;
    }
    const el = this.drawPath({
      d,
      stroke,
      strokeWidth,
      fill,
      fillOpacity: 1,
      classes: className,
      strokeLinecap,
      strokeDashArray
    });
    el.attr("index", realIndex);
    if (shouldClipToGrid) {
      if (chartType === "bar" && !w2.globals.isBarHorizontal || w2.globals.comboCharts) {
        el.attr({
          "clip-path": `url(#gridRectBarMask${w2.globals.cuid})`
        });
      } else {
        el.attr({
          "clip-path": `url(#gridRectMask${w2.globals.cuid})`
        });
      }
    }
    if (w2.config.chart.dropShadow.enabled && drawShadow) {
      filters.dropShadow(el, w2.config.chart.dropShadow, realIndex);
    }
    if (bindEventsOnPaths) {
      el.node.addEventListener("mouseenter", this.pathMouseEnter.bind(this, el));
      el.node.addEventListener("mouseleave", this.pathMouseLeave.bind(this, el));
      el.node.addEventListener("mousedown", this.pathMouseDown.bind(this, el));
    }
    el.attr({
      pathTo,
      pathFrom
    });
    const defaultAnimateOpts = {
      el,
      j,
      realIndex,
      pathFrom,
      pathTo,
      pathToInterp,
      fill,
      strokeWidth,
      delay: animationDelay,
      scrollMorph
    };
    if (initialAnim && !w2.globals.resized && !w2.globals.dataChanged) {
      if (useDrawMode) {
        const drawSpeed = initialSpeed * 2;
        const isFill = stroke === "none" || strokeWidth === 0;
        const seriesCount = w2.seriesData.series.length;
        const isLast = w2.globals.comboCharts ? true : realIndex === seriesCount - 1;
        anim.animateDraw(el, {
          realIndex,
          j,
          isFill,
          isLast,
          speed: drawSpeed,
          delay: 0,
          mask: drawMask
        });
      } else if (!revealViaFade) {
        anim.animatePathsGradually(__spreadProps(__spreadValues({}, defaultAnimateOpts), {
          speed: initialSpeed
        }));
      }
    } else {
      if ((w2.globals.resized || !w2.globals.dataChanged) && !revealViaFade) {
        anim.showDelayedElements();
      }
    }
    const animateOnUpdate = shouldAnimate && !revealViaFade && (w2.globals.dataChanged && dynamicAnim || crossTypeMorph);
    if (animateOnUpdate) {
      anim.animatePathsGradually(__spreadProps(__spreadValues({}, defaultAnimateOpts), {
        speed: dataChangeSpeed
      }));
    }
    if (revealViaFade) {
      anim.revealBulk(el);
    }
    return el;
  }
  /**
   * @param {string} style
   * @param {number} width
   * @param {number} height
   */
  drawPattern(style, width, height, stroke = "#a8a8a8", strokeWidth = 0) {
    const w2 = this.w;
    const p2 = w2.dom.Paper.pattern(width, height, (add) => {
      if (style === "horizontalLines") {
        add.line(0, 0, height, 0).stroke({ color: stroke, width: strokeWidth + 1 });
      } else if (style === "verticalLines") {
        add.line(0, 0, 0, width).stroke({ color: stroke, width: strokeWidth + 1 });
      } else if (style === "slantedLines") {
        add.line(0, 0, width, height).stroke({ color: stroke, width: strokeWidth });
      } else if (style === "squares") {
        add.rect(width, height).fill("none").stroke({ color: stroke, width: strokeWidth });
      } else if (style === "circles") {
        add.circle(width).fill("none").stroke({ color: stroke, width: strokeWidth });
      }
    });
    return p2;
  }
  /**
   * @param {string} style
   * @param {string} gfrom
   * @param {string} gto
   * @param {number} opacityFrom
   * @param {number} opacityTo
   * @param {number | null} [size]
   * @param {number[] | null} stops
   * @param {any[]} colorStops
   * @param {number} [i]
   * @param {boolean} [verticalUserSpace] anchor a vertical gradient to the plot
   *   area rather than to each path's own bounding box. Needed when one series
   *   is drawn as several path elements (nulls split it into segments): with
   *   the default objectBoundingBox each segment resolves the same offset
   *   against a different bbox, so the color transition lands on a different
   *   value in every segment.
   */
  drawGradient(style, gfrom, gto, opacityFrom, opacityTo, size = null, stops = null, colorStops = [], i2 = 0, verticalUserSpace = false) {
    const w2 = this.w;
    let g2;
    if (gfrom.length < 9 && gfrom.indexOf("#") === 0) {
      gfrom = Utils$1.hexToRgba(gfrom, opacityFrom);
    }
    if (gto.length < 9 && gto.indexOf("#") === 0) {
      gto = Utils$1.hexToRgba(gto, opacityTo);
    }
    let stop1 = 0;
    let stop2 = 1;
    let stop3 = 1;
    let stop4 = null;
    if (stops !== null) {
      stop1 = typeof stops[0] !== "undefined" ? stops[0] / 100 : 0;
      stop2 = typeof stops[1] !== "undefined" ? stops[1] / 100 : 1;
      stop3 = typeof stops[2] !== "undefined" ? stops[2] / 100 : 1;
      stop4 = typeof stops[3] !== "undefined" ? stops[3] / 100 : null;
    }
    const radial = !!(w2.config.chart.type === "donut" || w2.config.chart.type === "pie" || w2.config.chart.type === "polarArea" || w2.config.chart.type === "bubble");
    if (!colorStops || colorStops.length === 0) {
      g2 = w2.dom.Paper.gradient(
        radial ? "radial" : "linear",
        (add) => {
          add.stop(stop1, gfrom, opacityFrom);
          add.stop(stop2, gto, opacityTo);
          add.stop(stop3, gto, opacityTo);
          if (stop4 !== null) {
            add.stop(stop4, gfrom, opacityFrom);
          }
        }
      );
    } else {
      g2 = w2.dom.Paper.gradient(
        radial ? "radial" : "linear",
        (add) => {
          const gradientStops = Array.isArray(colorStops[i2]) ? colorStops[i2] : Array.isArray(colorStops[0]) ? colorStops[0] || [] : colorStops;
          gradientStops.forEach((s2) => {
            add.stop(s2.offset / 100, s2.color, s2.opacity);
          });
        }
      );
    }
    if (!radial) {
      if (style === "vertical") {
        if (verticalUserSpace) {
          g2.attr({ gradientUnits: "userSpaceOnUse" });
          g2.from(0, 0).to(0, w2.layout.gridHeight);
        } else {
          g2.from(0, 0).to(0, 1);
        }
      } else if (style === "diagonal") {
        g2.from(0, 0).to(1, 1);
      } else if (style === "horizontal") {
        g2.from(0, 1).to(1, 1);
      } else if (style === "diagonal2") {
        g2.from(1, 0).to(0, 1);
      }
    } else {
      const offx = w2.layout.gridWidth / 2;
      const offy = w2.layout.gridHeight / 2;
      if (w2.config.chart.type !== "bubble") {
        g2.attr({
          gradientUnits: "userSpaceOnUse",
          cx: offx,
          cy: offy,
          r: size
        });
      } else {
        g2.attr({
          cx: 0.5,
          cy: 0.5,
          r: 0.8,
          fx: 0.2,
          fy: 0.2
        });
      }
    }
    return g2;
  }
  /** @param {{ text: any, maxWidth: any, fontSize: any, fontFamily?: any }} opts */
  getTextBasedOnMaxWidth({ text, maxWidth, fontSize, fontFamily }) {
    const tRects = this.getTextRects(text, fontSize, fontFamily, "");
    const wordWidth = tRects.width / text.length;
    const wordsBasedOnWidth = Math.floor(maxWidth / wordWidth);
    if (maxWidth < tRects.width) {
      return text.slice(0, wordsBasedOnWidth - 3) + "...";
    }
    return text;
  }
  /**
   * @param {{ x: any, y: any, text: any, textAnchor?: any, fontSize?: any, fontFamily?: any, fontWeight?: any, foreColor?: any, opacity?: any, maxWidth?: any, cssClass?: string, isPlainText?: boolean, dominantBaseline?: string }} opts
   */
  drawText({
    x,
    y,
    text,
    textAnchor,
    fontSize,
    fontFamily,
    fontWeight,
    foreColor,
    opacity,
    maxWidth,
    cssClass = "",
    isPlainText = true,
    dominantBaseline = "auto"
  }) {
    const w2 = this.w;
    if (typeof text === "undefined") text = "";
    let truncatedText = text;
    if (!textAnchor) {
      textAnchor = "start";
    }
    if (!foreColor || !foreColor.length) {
      foreColor = w2.config.chart.foreColor;
    }
    fontFamily = fontFamily || w2.config.chart.fontFamily;
    fontSize = fontSize || "11px";
    fontWeight = fontWeight || "regular";
    const commonProps = {
      maxWidth,
      fontSize,
      fontFamily
    };
    let elText;
    if (Array.isArray(text)) {
      elText = w2.dom.Paper.text((add) => {
        for (let i2 = 0; i2 < text.length; i2++) {
          truncatedText = text[i2];
          if (maxWidth) {
            truncatedText = this.getTextBasedOnMaxWidth(__spreadValues({
              text: text[i2]
            }, commonProps));
          }
          i2 === 0 ? add.tspan(truncatedText) : add.tspan(truncatedText).newLine();
        }
      });
    } else {
      if (maxWidth) {
        truncatedText = this.getTextBasedOnMaxWidth(__spreadValues({
          text
        }, commonProps));
      }
      elText = isPlainText ? w2.dom.Paper.plain(text) : (
        /**
         * @param {any} add
         */
        w2.dom.Paper.text((add) => add.tspan(truncatedText))
      );
    }
    elText.attr({
      x,
      y,
      "text-anchor": textAnchor,
      "dominant-baseline": dominantBaseline,
      "font-size": fontSize,
      "font-family": fontFamily,
      "font-weight": fontWeight,
      fill: foreColor,
      class: "apexcharts-text " + cssClass
    });
    elText.node.style.fontFamily = fontFamily;
    elText.node.style.opacity = opacity;
    return elText;
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {string} type
   * @param {number} size
   */
  getMarkerPath(x, y, type, size) {
    const CROSS_SHRINK = 1.4;
    const PLUS_SHRINK = 1.12;
    const STAR_GROW = 1.15;
    const SPARKLE_SHRINK = 1.1;
    const SQUARE_SHRINK = 1.125;
    const DIAMOND_GROW = 1.05;
    const LINE_SHRINK = 1.1;
    const CIRCLE_DIAMETER = 2;
    let d = "";
    switch (type) {
      case "cross":
        size = size / CROSS_SHRINK;
        d = `M ${x - size} ${y - size} L ${x + size} ${y + size}  M ${x - size} ${y + size} L ${x + size} ${y - size}`;
        break;
      case "plus":
        size = size / PLUS_SHRINK;
        d = `M ${x - size} ${y} L ${x + size} ${y}  M ${x} ${y - size} L ${x} ${y + size}`;
        break;
      case "star":
      case "sparkle": {
        let points = 5;
        size = size * STAR_GROW;
        if (type === "sparkle") {
          size = size / SPARKLE_SHRINK;
          points = 4;
        }
        const step = Math.PI / points;
        for (let i2 = 0; i2 <= 2 * points; i2++) {
          const angle = i2 * step;
          const radius = i2 % 2 === 0 ? size : size / 2;
          const xPos = x + radius * Math.sin(angle);
          const yPos = y - radius * Math.cos(angle);
          d += (i2 === 0 ? "M" : "L") + xPos + "," + yPos;
        }
        d += "Z";
        break;
      }
      case "triangle":
        d = `M ${x} ${y - size} 
             L ${x + size} ${y + size} 
             L ${x - size} ${y + size} 
             Z`;
        break;
      case "square":
      case "rect":
        size = size / SQUARE_SHRINK;
        d = `M ${x - size} ${y - size} 
           L ${x + size} ${y - size} 
           L ${x + size} ${y + size} 
           L ${x - size} ${y + size} 
           Z`;
        break;
      case "diamond":
        size = size * DIAMOND_GROW;
        d = `M ${x} ${y - size} 
             L ${x + size} ${y} 
             L ${x} ${y + size} 
             L ${x - size} ${y} 
            Z`;
        break;
      case "line":
        size = size / LINE_SHRINK;
        d = `M ${x - size} ${y} 
           L ${x + size} ${y}`;
        break;
      case "circle":
      default:
        size = size * CIRCLE_DIAMETER;
        d = `M ${x}, ${y} 
           m -${size / 2}, 0 
           a ${size / 2},${size / 2} 0 1,0 ${size},0 
           a ${size / 2},${size / 2} 0 1,0 -${size},0`;
        break;
    }
    return d;
  }
  /**
   * @param {number} x - The x-coordinate of the marker
   * @param {number} y - The y-coordinate of the marker
   * @param {string} type - Marker shape type
   * @param {number} size - The size of the marker
   * @param {Record<string, any>} opts - The options for the marker
   * @returns {any} The created marker.
   */
  drawMarkerShape(x, y, type, size, opts) {
    const path = this.drawPath({
      d: this.getMarkerPath(x, y, type, size),
      stroke: opts.pointStrokeColor,
      strokeDashArray: opts.pointStrokeDashArray,
      strokeWidth: opts.pointStrokeWidth,
      fill: opts.pointFillColor,
      fillOpacity: opts.pointFillOpacity,
      strokeOpacity: opts.pointStrokeOpacity
    });
    path.attr({
      cx: x,
      cy: y,
      shape: opts.shape,
      class: opts.class ? opts.class : ""
    });
    return path;
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {Record<string, any>} opts
   */
  drawMarker(x, y, opts) {
    x = x || 0;
    let size = opts.pSize || 0;
    if (!Utils$1.isNumber(y)) {
      size = 0;
      y = 0;
    }
    return this.drawMarkerShape(x, y, opts == null ? void 0 : opts.shape, size, __spreadValues(__spreadValues({}, opts), opts.shape === "line" || opts.shape === "plus" || opts.shape === "cross" ? {
      pointStrokeColor: opts.pointFillColor,
      pointStrokeOpacity: opts.pointFillOpacity
    } : {}));
  }
  /**
   * @param {any} path
   * @param {Event | null} [e]
   */
  pathMouseEnter(path, e2) {
    var _a, _b;
    const w2 = this.w;
    const filters = new Filters(this.w);
    const i2 = parseInt((_a = path.node.getAttribute("index")) != null ? _a : "", 10);
    const j = parseInt((_b = path.node.getAttribute("j")) != null ? _b : "", 10);
    if (isNaN(i2) || isNaN(j)) return;
    if (typeof w2.config.chart.events.dataPointMouseEnter === "function") {
      w2.config.chart.events.dataPointMouseEnter(e2, this.ctx, {
        seriesIndex: i2,
        dataPointIndex: j,
        w: w2
      });
    }
    Graphics._fireEvent(w2, "dataPointMouseEnter", [
      e2,
      this.ctx,
      { seriesIndex: i2, dataPointIndex: j, w: w2 }
    ]);
    if (w2.config.states.active.filter.type !== "none") {
      if (path.node.getAttribute("selected") === "true") {
        return;
      }
    }
    if (Filters.hoverOutlineOwnsHoverState(w2)) return;
    if (w2.config.states.hover.filter.type !== "none") {
      if (!w2.interact.isTouchDevice) {
        const hoverFilter = w2.config.states.hover.filter;
        filters.applyFilter(path, i2, hoverFilter.type, hoverFilter.value);
      }
    }
  }
  /**
   * @param {any} path
   * @param {Event | null} [e]
   */
  pathMouseLeave(path, e2) {
    var _a, _b;
    const w2 = this.w;
    const filters = new Filters(this.w);
    const i2 = parseInt((_a = path.node.getAttribute("index")) != null ? _a : "", 10);
    const j = parseInt((_b = path.node.getAttribute("j")) != null ? _b : "", 10);
    if (isNaN(i2) || isNaN(j)) return;
    if (typeof w2.config.chart.events.dataPointMouseLeave === "function") {
      w2.config.chart.events.dataPointMouseLeave(e2, this.ctx, {
        seriesIndex: i2,
        dataPointIndex: j,
        w: w2
      });
    }
    Graphics._fireEvent(w2, "dataPointMouseLeave", [
      e2,
      this.ctx,
      { seriesIndex: i2, dataPointIndex: j, w: w2 }
    ]);
    if (w2.config.states.active.filter.type !== "none") {
      if (path.node.getAttribute("selected") === "true") {
        return;
      }
    }
    if (Filters.hoverOutlineOwnsHoverState(w2)) return;
    if (w2.config.states.hover.filter.type !== "none") {
      filters.getDefaultFilter(path, i2);
    }
  }
  /**
   * Clear every selected data point (single-select mode): reset the selection
   * array and restore the default filter on all series path/circle/rect nodes.
   * NOTE: preserves the original behavior of passing the clicked series index
   * `i` to getDefaultFilter for every element (not each element's own index).
   * @param {any} filters @param {number} i
   */
  _clearAllDataPointSelections(filters, i2) {
    const w2 = this.w;
    w2.interact.selectedDataPoints = [];
    const elPaths = w2.dom.Paper.find(
      ".apexcharts-series path:not(.apexcharts-decoration-element)"
    );
    const elCircles = w2.dom.Paper.find(
      ".apexcharts-series circle:not(.apexcharts-decoration-element), .apexcharts-series rect:not(.apexcharts-decoration-element)"
    );
    const deSelect = (els) => {
      Array.prototype.forEach.call(els, (el) => {
        el.node.setAttribute("selected", "false");
        filters.getDefaultFilter(el, i2);
      });
    };
    deSelect(elPaths);
    deSelect(elCircles);
  }
  /**
   * Toggle the selected state of a single data point. Mutates
   * `w.interact.selectedDataPoints` and the path's `selected` attribute,
   * honoring single- vs multi-select mode. Returns the new selected flag.
   * @param {any} path @param {any} filters @param {number} i @param {number} j
   * @returns {'true' | 'false'}
   */
  _togglePointSelection(path, filters, i2, j) {
    const w2 = this.w;
    if (path.node.getAttribute("selected") === "true") {
      path.node.setAttribute("selected", "false");
      const index = w2.interact.selectedDataPoints[i2].indexOf(j);
      if (index > -1) {
        w2.interact.selectedDataPoints[i2].splice(index, 1);
      }
      return "false";
    }
    if (!w2.config.states.active.allowMultipleDataPointsSelection && w2.interact.selectedDataPoints.length > 0) {
      this._clearAllDataPointSelections(filters, i2);
    }
    path.node.setAttribute("selected", "true");
    if (typeof w2.interact.selectedDataPoints[i2] === "undefined") {
      w2.interact.selectedDataPoints[i2] = [];
    }
    w2.interact.selectedDataPoints[i2].push(j);
    return "true";
  }
  /**
   * Apply the active / hover / default state filter after a selection toggle,
   * matching the original inline branching.
   * @param {any} path @param {any} filters @param {number} i
   * @param {'true' | 'false'} selected
   */
  _applyPointSelectionFilter(path, filters, i2, selected) {
    const w2 = this.w;
    if (Filters.sliceOffsetOwnsActiveState(w2)) return;
    if (selected === "true") {
      const activeFilter = w2.config.states.active.filter;
      if (activeFilter !== "none") {
        filters.applyFilter(path, i2, activeFilter.type, activeFilter.value);
      } else {
        if (w2.config.states.hover.filter !== "none") {
          if (!w2.interact.isTouchDevice) {
            const hoverFilter = w2.config.states.hover.filter;
            filters.applyFilter(path, i2, hoverFilter.type, hoverFilter.value);
          }
        }
      }
    } else {
      if (w2.config.states.active.filter.type !== "none") {
        if (w2.config.states.hover.filter.type !== "none" && !w2.interact.isTouchDevice) {
          const hoverFilter = w2.config.states.hover.filter;
          filters.applyFilter(path, i2, hoverFilter.type, hoverFilter.value);
        } else {
          filters.getDefaultFilter(path, i2);
        }
      }
    }
  }
  /**
   * @param {any} path
   * @param {Event | null} e
   */
  pathMouseDown(path, e2) {
    var _a, _b;
    const w2 = this.w;
    const filters = new Filters(this.w);
    const i2 = parseInt((_a = path.node.getAttribute("index")) != null ? _a : "", 10);
    const j = parseInt((_b = path.node.getAttribute("j")) != null ? _b : "", 10);
    if (isNaN(i2) || isNaN(j)) return;
    const link = w2.config.chart.link;
    const crossfilterClick = !!(link && (typeof link.dimension === "function" || link.enabled));
    if (!crossfilterClick) {
      const selected = this._togglePointSelection(path, filters, i2, j);
      this._applyPointSelectionFilter(path, filters, i2, selected);
    }
    if (typeof w2.config.chart.events.dataPointSelection === "function") {
      w2.config.chart.events.dataPointSelection(e2, this.ctx, {
        selectedDataPoints: w2.interact.selectedDataPoints,
        seriesIndex: i2,
        dataPointIndex: j,
        w: w2
      });
    }
    if (e2) {
      Graphics._fireEvent(w2, "dataPointSelection", [
        e2,
        this.ctx,
        {
          selectedDataPoints: w2.interact.selectedDataPoints,
          seriesIndex: i2,
          dataPointIndex: j,
          w: w2
        }
      ]);
    }
  }
  /**
   * @param {any} el
   * @returns {{ x: number, y: number }}
   */
  rotateAroundCenter(el) {
    let coord = (
      /** @type {any} */
      {}
    );
    if (el && typeof el.getBBox === "function") {
      coord = el.getBBox();
    }
    const x = coord.x + coord.width / 2;
    const y = coord.y + coord.height / 2;
    return {
      x,
      y
    };
  }
  /**
   * Sets up event delegation on a parent group element.
   * Uses mouseover/mouseout (which bubble) to simulate mouseenter/mouseleave
   * on matching child elements, reducing per-element listener overhead.
   * @param {any} parentGroup
   * @param {string} targetSelector
   */
  setupEventDelegation(parentGroup, targetSelector) {
    let currentHovered = null;
    parentGroup.node.addEventListener("mouseover", (e2) => {
      const targetNode = Graphics._findDelegateTarget(
        e2.target,
        parentGroup.node,
        targetSelector
      );
      if (!targetNode || targetNode === currentHovered) return;
      if (currentHovered && /** @type {any} */
      currentHovered.instance) {
        this.pathMouseLeave(
          /** @type {any} */
          currentHovered.instance,
          e2
        );
      }
      currentHovered = targetNode;
      if (targetNode.instance) {
        this.pathMouseEnter(targetNode.instance, e2);
      }
    });
    parentGroup.node.addEventListener("mouseout", (e2) => {
      if (!currentHovered) return;
      const relatedNode = e2.relatedTarget ? Graphics._findDelegateTarget(
        e2.relatedTarget,
        parentGroup.node,
        targetSelector
      ) : null;
      if (relatedNode !== currentHovered) {
        if (currentHovered && /** @type {any} */
        currentHovered.instance) {
          this.pathMouseLeave(
            /** @type {any} */
            currentHovered.instance,
            e2
          );
        }
        currentHovered = null;
      }
    });
    parentGroup.node.addEventListener("mousedown", (e2) => {
      const targetNode = Graphics._findDelegateTarget(
        e2.target,
        parentGroup.node,
        targetSelector
      );
      if (targetNode && targetNode.instance) {
        this.pathMouseDown(targetNode.instance, e2);
      }
    });
  }
  // Fire a named event from w.globals.events without requiring a ctx reference.
  // Mirrors Events.fireEvent() but reads the registry directly from w so that
  // pathMouseEnter/Leave/Down work even when this.ctx is null (Graphics instances
  // created without a ctx arg for drawing-only use cases).
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {string} name
   * @param {any[]} args
   */
  static _fireEvent(w2, name2, args) {
    const evs = w2.globals.events;
    if (!evs || !Object.prototype.hasOwnProperty.call(evs, name2)) return;
    const handlers = (
      /** @type {Record<string,any>} */
      evs[name2]
    );
    for (let i2 = 0; i2 < handlers.length; i2++) {
      handlers[i2].apply(null, args);
    }
  }
  /**
   * @param {any} node
   * @param {Record<string, any>} boundary
   * @param {string} selector
   */
  static _findDelegateTarget(node, boundary, selector) {
    while (node && node !== boundary && node !== document) {
      if (node.matches && node.matches(selector)) return node;
      node = node.parentNode;
    }
    return null;
  }
  /**
   * @param {any} el
   * @param {Record<string, any>} attrs
   */
  static setAttrs(el, attrs) {
    for (const key in attrs) {
      if (Object.prototype.hasOwnProperty.call(attrs, key)) {
        el.setAttribute(key, attrs[key]);
      }
    }
  }
  /**
   * @param {string} text
   * @param {string} fontSize
   * @param {string | null | undefined} [fontFamily]
   * @param {string} [transform]
   * @returns {{ width: number, height: number }}
   */
  getTextRects(text, fontSize, fontFamily, transform, useBBox = true) {
    const w2 = this.w;
    const cacheKey = [text, fontSize, fontFamily, transform, useBBox].join("\0");
    const cache = w2.globals.textRectsCache;
    if (cache && cache.has(cacheKey)) {
      return (
        /** @type {{ width: number, height: number }} */
        cache.get(cacheKey)
      );
    }
    const virtualText = this.drawText({
      x: -200,
      y: -200,
      text,
      textAnchor: "start",
      fontSize,
      fontFamily,
      foreColor: "#fff",
      opacity: 0
    });
    if (transform) {
      virtualText.attr("transform", transform);
    }
    w2.dom.Paper.add(virtualText);
    let rect = virtualText.bbox();
    const bboxY = rect.y;
    if (!useBBox) {
      rect = virtualText.node.getBoundingClientRect();
    }
    virtualText.remove();
    const result = {
      width: rect.width,
      height: rect.height,
      // Offset from the text element's `y` (alphabetic baseline) to the
      // bbox vertical center. For most fonts this is NEGATIVE (bbox center
      // sits above the baseline because the ascender is taller than the
      // descender). Use as: `text_y = desired_visual_center_y - centerOffset`.
      // Only populated when useBBox=true (the default).
      centerOffset: useBBox ? bboxY + rect.height / 2 - -200 : 0
    };
    if (cache) {
      cache.set(cacheKey, result);
    }
    return result;
  }
  /**
   * append ... to long text
   * http://stackoverflow.com/questions/9241315/trimming-text-to-a-given-pixel-width-in-svg
   * @memberof Graphics
   * @param {Record<string, any>} textObj
   * @param {string} textString
   * @param {number} width
   **/
  placeTextWithEllipsis(textObj, textString, width) {
    if (typeof textObj.getComputedTextLength !== "function") return;
    textObj.textContent = textString;
    if (textString.length > 0) {
      if (textObj.getComputedTextLength() >= width / 1.1) {
        for (let x = textString.length - 3; x > 0; x -= 3) {
          if (textObj.getSubStringLength(0, x) <= width / 1.1) {
            textObj.textContent = textString.substring(0, x) + "...";
            return;
          }
        }
        textObj.textContent = ".";
      }
    }
  }
}
class Fill {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.opts = null;
    this.seriesIndex = 0;
    this.patternIDs = [];
  }
  /**
   * @param {Record<string, any>} params
   */
  clippedImgArea(params) {
    const w2 = this.w;
    const cnf = w2.config;
    const svgW = parseInt(String(w2.layout.gridWidth), 10);
    const svgH = parseInt(String(w2.layout.gridHeight), 10);
    const size = svgW > svgH ? svgW : svgH;
    const fillImg = params.image;
    let imgWidth = 0;
    let imgHeight = 0;
    if (typeof params.width === "undefined" && typeof params.height === "undefined") {
      if (cnf.fill.image.width !== void 0 && cnf.fill.image.height !== void 0) {
        imgWidth = cnf.fill.image.width + 1;
        imgHeight = cnf.fill.image.height;
      } else {
        imgWidth = size + 1;
        imgHeight = size;
      }
    } else {
      imgWidth = params.width;
      imgHeight = params.height;
    }
    const elPattern = BrowserAPIs.createElementNS(SVGNS$2, "pattern");
    Graphics.setAttrs(elPattern, {
      id: params.patternID,
      patternUnits: params.patternUnits ? params.patternUnits : "userSpaceOnUse",
      width: imgWidth + "px",
      height: imgHeight + "px"
    });
    const elImage = BrowserAPIs.createElementNS(SVGNS$2, "image");
    elPattern.appendChild(elImage);
    const SVGLib = Environment.isBrowser() ? (
      /** @type {any} */
      window.SVG
    ) : (
      /** @type {any} */
      global.SVG
    );
    elImage.setAttributeNS(SVGLib.xlink, "href", fillImg);
    Graphics.setAttrs(elImage, {
      x: 0,
      y: 0,
      preserveAspectRatio: "none",
      width: imgWidth + "px",
      height: imgHeight + "px"
    });
    elImage.style.opacity = params.opacity;
    w2.dom.elDefs.node.appendChild(elPattern);
  }
  /**
   * @param {Record<string, any>} opts
   */
  getSeriesIndex(opts) {
    const w2 = this.w;
    const cType = w2.config.chart.type;
    if ((cType === "bar" || cType === "rangeBar") && w2.config.plotOptions.bar.distributed || cType === "heatmap" || cType === "treemap") {
      this.seriesIndex = opts.seriesNumber;
    } else {
      this.seriesIndex = opts.seriesNumber % w2.seriesData.series.length;
    }
    return this.seriesIndex;
  }
  /**
   * The y-axis window a series is plotted against. This is the space the
   * threshold gradient is measured in, since that gradient spans the plot area.
   *
   * @param {number} realIndex
   */
  getSeriesAxisRange(realIndex) {
    var _a, _b, _c;
    const w2 = this.w;
    const yaxisIndex = (_b = (_a = w2.globals.seriesYAxisReverseMap) == null ? void 0 : _a[realIndex]) != null ? _b : 0;
    return {
      minY: Utils$1.isNumber(w2.globals.minYArr[realIndex]) ? w2.globals.minYArr[realIndex] : w2.globals.minY,
      maxY: Utils$1.isNumber(w2.globals.maxYArr[realIndex]) ? w2.globals.maxYArr[realIndex] : w2.globals.maxY,
      reversed: !!((_c = w2.config.yaxis[yaxisIndex]) == null ? void 0 : _c.reversed)
    };
  }
  /**
   * Builds the two stops that split a series into an above- and a
   * below-threshold color.
   *
   * The stops are painted into a vertical gradient anchored to the plot area
   * (see Graphics.drawGradient's verticalUserSpace branch), so the boundary is
   * positioned over the *axis* range. Deriving it from the data range instead
   * puts the transition on the wrong value whenever the axis extends past the
   * data, which is the common case: an explicit yaxis.min/max, a nice scale, or
   * an axis shared with another series.
   *
   * @param {{ minY: number, maxY: number, reversed?: boolean }} axisRange
   * @param {Record<string, any>} multiColorConfig
   */
  computeColorStops(axisRange, multiColorConfig) {
    const w2 = this.w;
    const { threshold, colorAboveThreshold, colorBelowThreshold } = multiColorConfig;
    const { minY, maxY } = axisRange;
    const span = maxY - minY;
    let offset = span === 0 ? threshold > maxY ? 0 : 100 : (maxY - threshold) / span * 100;
    if (axisRange.reversed) {
      offset = 100 - offset;
    }
    offset = Math.max(0, Math.min(offset, 100));
    const fillOpacity = Array.isArray(w2.config.fill.opacity) ? w2.config.fill.opacity[this.seriesIndex] : w2.config.fill.opacity;
    const above = {
      offset,
      color: colorAboveThreshold,
      opacity: fillOpacity
    };
    const below = {
      offset,
      color: colorBelowThreshold,
      opacity: fillOpacity
    };
    return axisRange.reversed ? [below, above] : [above, below];
  }
  /**
   * @param {Record<string, any>} opts
   */
  fillPath(opts) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    this.opts = opts;
    const cnf = this.w.config;
    let pathFill;
    let patternFill, gradientFill;
    this.seriesIndex = this.getSeriesIndex(opts);
    const drawMultiColorLine = cnf.plotOptions.line.colors.colorAboveThreshold && cnf.plotOptions.line.colors.colorBelowThreshold;
    const fillColors = this.getFillColors();
    let fillColor = fillColors[this.seriesIndex];
    if (w2.seriesData.seriesColors[this.seriesIndex] !== void 0) {
      fillColor = w2.seriesData.seriesColors[this.seriesIndex];
    }
    if (typeof fillColor === "function") {
      fillColor = fillColor({
        seriesIndex: this.seriesIndex,
        dataPointIndex: opts.dataPointIndex,
        value: opts.value,
        w: w2
      });
    }
    const fillType = opts.fillType ? opts.fillType : this.getFillType(this.seriesIndex);
    let fillOpacity = Array.isArray(cnf.fill.opacity) ? cnf.fill.opacity[this.seriesIndex] : cnf.fill.opacity;
    const useGradient = fillType === "gradient" || drawMultiColorLine;
    if (opts.color) {
      fillColor = opts.color;
    }
    const seriesItem = (
      /** @type {Record<string,any>} */
      w2.config.series[this.seriesIndex]
    );
    if ((_b = (_a = seriesItem == null ? void 0 : seriesItem.data) == null ? void 0 : _a[opts.dataPointIndex]) == null ? void 0 : _b.fillColor) {
      fillColor = (_d = (_c = seriesItem == null ? void 0 : seriesItem.data) == null ? void 0 : _c[opts.dataPointIndex]) == null ? void 0 : _d.fillColor;
    }
    if (!fillColor) {
      fillColor = "#fff";
      console.warn("undefined color - ApexCharts");
    }
    if (opts.opacity !== void 0 && opts.opacity !== null) fillOpacity = opts.opacity;
    let defaultColor = fillColor;
    if (Utils$1.isCSSVariable(fillColor)) {
      defaultColor = Utils$1.applyOpacityToColor(fillColor, fillOpacity);
    } else if (fillColor.indexOf("rgb") === -1) {
      if (fillColor.indexOf("#") === -1) {
        defaultColor = fillColor;
      } else if (fillColor.length < 9) {
        defaultColor = Utils$1.hexToRgba(fillColor, fillOpacity);
      }
    } else {
      if (fillColor.indexOf("rgba") > -1) {
        fillOpacity = Utils$1.getOpacityFromRGBA(fillColor);
      } else {
        defaultColor = Utils$1.hexToRgba(Utils$1.rgb2hex(fillColor), fillOpacity);
      }
    }
    const resolvedFillColor = Utils$1.isCSSVariable(fillColor) ? Utils$1.getThemeColor(fillColor) : fillColor;
    if (fillType === "pattern") {
      patternFill = this.handlePatternFill({
        fillConfig: opts.fillConfig,
        patternFill,
        fillColor: resolvedFillColor,
        defaultColor
      });
    }
    if (useGradient) {
      const colorStops = cnf.fill.gradient.colorStops ? [...cnf.fill.gradient.colorStops] : [];
      let type = cnf.fill.gradient.type;
      if (drawMultiColorLine) {
        colorStops[this.seriesIndex] = this.computeColorStops(
          this.getSeriesAxisRange(this.seriesIndex),
          cnf.plotOptions.line.colors
        );
        type = "vertical";
      }
      gradientFill = this.handleGradientFill({
        type,
        fillConfig: opts.fillConfig,
        fillColor: resolvedFillColor,
        fillOpacity,
        colorStops,
        i: this.seriesIndex,
        // Threshold stops are positioned over the axis range, so the gradient
        // has to be anchored to the plot area to match. This also keeps every
        // segment of a null-split series on one shared coordinate space.
        verticalUserSpace: drawMultiColorLine
      });
    }
    if (fillType === "image") {
      const imgSrc = cnf.fill.image.src;
      const patternID = opts.patternID ? opts.patternID : "";
      const patternKey = `pattern${w2.globals.cuid}${opts.seriesNumber + 1}${patternID}`;
      if (this.patternIDs.indexOf(patternKey) === -1) {
        this.clippedImgArea({
          opacity: fillOpacity,
          image: Array.isArray(imgSrc) ? opts.seriesNumber < imgSrc.length ? imgSrc[opts.seriesNumber] : imgSrc[0] : imgSrc,
          width: opts.width ? opts.width : void 0,
          height: opts.height ? opts.height : void 0,
          patternUnits: opts.patternUnits,
          patternID: patternKey
        });
        this.patternIDs.push(patternKey);
      }
      pathFill = `url(#${patternKey})`;
    } else if (useGradient) {
      pathFill = gradientFill;
    } else if (fillType === "pattern") {
      pathFill = patternFill;
    } else {
      pathFill = defaultColor;
    }
    if (opts.solid) {
      pathFill = defaultColor;
    }
    return pathFill;
  }
  /**
   * @param {number} seriesIndex
   */
  getFillType(seriesIndex) {
    const w2 = this.w;
    if (Array.isArray(w2.config.fill.type)) {
      return w2.config.fill.type[seriesIndex];
    } else {
      return w2.config.fill.type;
    }
  }
  getFillColors() {
    const w2 = this.w;
    const cnf = w2.config;
    const opts = this.opts;
    let fillColors = [];
    if (w2.globals.comboCharts) {
      if (
        /** @type {Record<string,any>} */
        w2.config.series[this.seriesIndex].type === "line"
      ) {
        if (Array.isArray(w2.globals.stroke.colors)) {
          fillColors = w2.globals.stroke.colors;
        } else {
          fillColors.push(w2.globals.stroke.colors);
        }
      } else {
        if (Array.isArray(w2.globals.fill.colors)) {
          fillColors = w2.globals.fill.colors;
        } else {
          fillColors.push(w2.globals.fill.colors);
        }
      }
    } else {
      if (cnf.chart.type === "line") {
        if (Array.isArray(w2.globals.stroke.colors)) {
          fillColors = w2.globals.stroke.colors;
        } else {
          fillColors.push(w2.globals.stroke.colors);
        }
      } else {
        if (Array.isArray(w2.globals.fill.colors)) {
          fillColors = w2.globals.fill.colors;
        } else {
          fillColors.push(w2.globals.fill.colors);
        }
      }
    }
    if (typeof opts.fillColors !== "undefined") {
      fillColors = [];
      if (Array.isArray(opts.fillColors)) {
        fillColors = opts.fillColors.slice();
      } else {
        fillColors.push(opts.fillColors);
      }
    }
    return fillColors;
  }
  /** @param {{fillConfig: any, patternFill: any, fillColor: any, defaultColor: any}} opts */
  handlePatternFill({ fillConfig, patternFill, fillColor, defaultColor }) {
    let fillCnf = this.w.config.fill;
    if (fillConfig) {
      fillCnf = fillConfig;
    }
    const opts = this.opts;
    const graphics = new Graphics(this.w);
    const patternStrokeWidth = Array.isArray(fillCnf.pattern.strokeWidth) ? fillCnf.pattern.strokeWidth[this.seriesIndex] : fillCnf.pattern.strokeWidth;
    const patternLineColor = fillColor;
    if (Array.isArray(fillCnf.pattern.style)) {
      if (typeof fillCnf.pattern.style[opts.seriesNumber] !== "undefined") {
        const pf = graphics.drawPattern(
          fillCnf.pattern.style[opts.seriesNumber],
          fillCnf.pattern.width,
          fillCnf.pattern.height,
          patternLineColor,
          patternStrokeWidth
        );
        patternFill = pf;
      } else {
        patternFill = defaultColor;
      }
    } else {
      patternFill = graphics.drawPattern(
        fillCnf.pattern.style,
        fillCnf.pattern.width,
        fillCnf.pattern.height,
        patternLineColor,
        patternStrokeWidth
      );
    }
    return patternFill;
  }
  handleGradientFill({
    type,
    fillColor,
    fillOpacity,
    fillConfig,
    colorStops,
    i: i2,
    verticalUserSpace = false
  }) {
    let fillCnf = this.w.config.fill;
    if (fillConfig) {
      fillCnf = __spreadValues(__spreadValues({}, fillCnf), fillConfig);
    }
    const opts = this.opts;
    const graphics = new Graphics(this.w);
    const utils = new Utils$1();
    type = type || fillCnf.gradient.type;
    let gradientFrom = fillColor;
    let gradientTo;
    let opacityFrom = fillCnf.gradient.opacityFrom === void 0 ? fillOpacity : Array.isArray(fillCnf.gradient.opacityFrom) ? fillCnf.gradient.opacityFrom[i2] : fillCnf.gradient.opacityFrom;
    if (gradientFrom.indexOf("rgba") > -1) {
      opacityFrom = Utils$1.getOpacityFromRGBA(gradientFrom);
    }
    let opacityTo = fillCnf.gradient.opacityTo === void 0 ? fillOpacity : Array.isArray(fillCnf.gradient.opacityTo) ? fillCnf.gradient.opacityTo[i2] : fillCnf.gradient.opacityTo;
    if (fillCnf.gradient.gradientToColors === void 0 || fillCnf.gradient.gradientToColors.length === 0) {
      if (fillCnf.gradient.shade === "dark") {
        gradientTo = utils.shadeColor(
          parseFloat(fillCnf.gradient.shadeIntensity) * -1,
          fillColor.indexOf("rgb") > -1 ? Utils$1.rgb2hex(fillColor) : fillColor
        );
      } else {
        gradientTo = utils.shadeColor(
          parseFloat(fillCnf.gradient.shadeIntensity),
          fillColor.indexOf("rgb") > -1 ? Utils$1.rgb2hex(fillColor) : fillColor
        );
      }
    } else {
      if (fillCnf.gradient.gradientToColors[opts.seriesNumber]) {
        const gToColor = fillCnf.gradient.gradientToColors[opts.seriesNumber];
        gradientTo = gToColor;
        if (gToColor.indexOf("rgba") > -1) {
          opacityTo = Utils$1.getOpacityFromRGBA(gToColor);
        }
      } else {
        gradientTo = fillColor;
      }
    }
    if (fillCnf.gradient.gradientFrom) {
      gradientFrom = fillCnf.gradient.gradientFrom;
    }
    if (fillCnf.gradient.gradientTo) {
      gradientTo = fillCnf.gradient.gradientTo;
    }
    if (fillCnf.gradient.inverseColors) {
      const t2 = gradientFrom;
      gradientFrom = gradientTo;
      gradientTo = t2;
    }
    if (gradientFrom.indexOf("rgb") > -1) {
      gradientFrom = Utils$1.rgb2hex(gradientFrom);
    }
    if (gradientTo.indexOf("rgb") > -1) {
      gradientTo = Utils$1.rgb2hex(gradientTo);
    }
    return graphics.drawGradient(
      type,
      gradientFrom,
      gradientTo,
      opacityFrom,
      opacityTo,
      opts.size,
      fillCnf.gradient.stops,
      colorStops,
      i2,
      verticalUserSpace
    );
  }
}
const OK_FILTER_TYPES = ["none", "lighten", "darken"];
function seriesEmitter(ctx, graphics) {
  const r2 = ctx && ctx.renderer;
  return r2 && r2.kind && r2.kind !== "svg" ? r2 : graphics;
}
function computeMarkCount(w2) {
  const series = w2.config.series || [];
  const type = w2.config.chart.type;
  const scatterish = type === "scatter" || type === "bubble";
  const markerSize = w2.config.markers && w2.config.markers.size;
  const markersOn = Array.isArray(markerSize) ? markerSize.some((s2) => s2 > 0) : (markerSize || 0) > 0;
  const labelsOn = !!(w2.config.dataLabels && w2.config.dataLabels.enabled);
  const isHeatmap = type === "heatmap";
  let total = 0;
  let maxLen = 0;
  series.forEach((s2) => {
    const n2 = Array.isArray(s2.data) ? s2.data.length : 0;
    if (n2 > maxLen) maxLen = n2;
    if (scatterish || markersOn || isHeatmap) total += n2;
    if (labelsOn) total += n2;
  });
  const LARGE_D = 5e4;
  if (maxLen >= LARGE_D) total = Math.max(total, maxLen);
  return total;
}
function hasCanvasUnsupportedFeature(w2) {
  var _a, _b;
  const fillType = w2.config.fill && w2.config.fill.type;
  const isUnsupportedFill = (t2) => t2 === "pattern" || t2 === "image" || t2 === "gradient";
  if (Array.isArray(fillType) ? fillType.some(isUnsupportedFill) : isUnsupportedFill(fillType)) {
    return true;
  }
  const lineColors = (_b = (_a = w2.config.plotOptions) == null ? void 0 : _a.line) == null ? void 0 : _b.colors;
  if (lineColors && lineColors.colorAboveThreshold && lineColors.colorBelowThreshold) {
    return true;
  }
  const states = w2.config.states || {};
  const hoverFilter = states.hover && states.hover.filter && states.hover.filter.type;
  const activeFilter = states.active && states.active.filter && states.active.filter.type;
  if (hoverFilter && !OK_FILTER_TYPES.includes(hoverFilter)) return true;
  if (activeFilter && !OK_FILTER_TYPES.includes(activeFilter)) return true;
  return false;
}
class Markers {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this._filters = new Filters(this.w);
    this._graphics = new Graphics(this.w, this.ctx);
    this._seriesWrap = null;
    this._seriesWrapIndex = -1;
  }
  /**
   * Invalidate the cached per-series wrap group. Callers that drive
   * plotChartMarkers point-by-point (Line) must call this when a series'
   * element tree is (re)created, so a later render never appends markers to a
   * detached group from the previous pass.
   */
  resetSeriesWrapCache() {
    this._seriesWrap = null;
    this._seriesWrapIndex = -1;
  }
  setGlobalMarkerSize() {
    const w2 = this.w;
    w2.globals.markers.size = Array.isArray(w2.config.markers.size) ? w2.config.markers.size : [w2.config.markers.size];
    if (w2.globals.markers.size.length > 0) {
      if (w2.globals.markers.size.length < w2.seriesData.series.length + 1) {
        for (let i2 = 0; i2 <= w2.seriesData.series.length; i2++) {
          if (typeof w2.globals.markers.size[i2] === "undefined") {
            w2.globals.markers.size.push(w2.globals.markers.size[0]);
          }
        }
      }
    } else {
      w2.globals.markers.size = w2.config.series.map(
        () => (
          /** @type {number} */
          w2.config.markers.size
        )
      );
    }
  }
  /** @param {{ pointsPos?: any, seriesIndex?: any, j?: any, pSize?: any, alwaysDrawMarker?: boolean, isVirtualPoint?: boolean }} opts */
  plotChartMarkers({
    pointsPos,
    seriesIndex,
    j,
    pSize,
    alwaysDrawMarker = false,
    isVirtualPoint = false
  }) {
    const w2 = this.w;
    const i2 = seriesIndex;
    const p2 = pointsPos;
    let elMarkersWrap = null;
    const graphics = new Graphics(this.w);
    const emit = seriesEmitter(this.ctx, graphics);
    const hasDiscreteMarkers = w2.config.markers.discrete && w2.config.markers.discrete.length;
    if (Array.isArray(p2.x)) {
      for (let q = 0; q < p2.x.length; q++) {
        let markerElement;
        let dataPointIndex = j;
        let invalidMarker = !Utils$1.isNumber(p2.y[q]);
        if (w2.globals.markers.largestSize === 0 && w2.globals.hasNullValues && w2.seriesData.series[i2][j + 1] !== null && !isVirtualPoint) {
          invalidMarker = true;
        }
        if (j === 1 && q === 0) dataPointIndex = 0;
        if (j === 1 && q === 1) dataPointIndex = 1;
        let markerClasses = "apexcharts-marker";
        if ((w2.config.chart.type === "line" || w2.config.chart.type === "area") && !w2.globals.comboCharts && !w2.config.tooltip.intersect) {
          markerClasses += " no-pointer-events";
        }
        const shouldMarkerDraw = Array.isArray(w2.config.markers.size) ? w2.globals.markers.size[seriesIndex] > 0 : w2.config.markers.size > 0;
        if (shouldMarkerDraw || alwaysDrawMarker || hasDiscreteMarkers) {
          if (emit.kind === "canvas") {
            if (typeof w2.globals.pointsArray[seriesIndex] === "undefined") {
              w2.globals.pointsArray[seriesIndex] = [];
            }
            w2.globals.pointsArray[seriesIndex][dataPointIndex] = [p2.x[q], p2.y[q]];
          }
          if (!invalidMarker) {
            markerClasses += ` w${Utils$1.randomId()}`;
          }
          const opts = this.getMarkerConfig({
            cssClass: markerClasses,
            seriesIndex,
            dataPointIndex
          });
          const _si = (
            /** @type {Record<string,any>} */
            w2.config.series[i2]
          );
          if (_si.data[dataPointIndex]) {
            if (_si.data[dataPointIndex].fillColor) {
              opts.pointFillColor = _si.data[dataPointIndex].fillColor;
            }
            if (_si.data[dataPointIndex].strokeColor) {
              opts.pointStrokeColor = _si.data[dataPointIndex].strokeColor;
            }
          }
          if (typeof pSize !== "undefined") {
            opts.pSize = pSize;
          }
          if (p2.x[q] < -w2.globals.markers.largestSize || p2.x[q] > w2.layout.gridWidth + w2.globals.markers.largestSize || p2.y[q] < -w2.globals.markers.largestSize || p2.y[q] > w2.layout.gridHeight + w2.globals.markers.largestSize) {
            opts.pSize = 0;
          }
          if (!invalidMarker) {
            const shouldCreateMarkerWrap = w2.globals.markers.size[seriesIndex] > 0 || alwaysDrawMarker || hasDiscreteMarkers;
            if (shouldCreateMarkerWrap && !elMarkersWrap) {
              const standardWrap = !alwaysDrawMarker && !hasDiscreteMarkers;
              if (standardWrap && this._seriesWrap && this._seriesWrapIndex === seriesIndex) {
                elMarkersWrap = this._seriesWrap;
              } else {
                elMarkersWrap = emit.group({
                  class: standardWrap ? "apexcharts-series-markers" : ""
                });
                elMarkersWrap.attr(
                  "clip-path",
                  `url(#gridRectMarkerMask${w2.globals.cuid})`
                );
                this.setupMarkerDelegation(elMarkersWrap);
                if (standardWrap) {
                  this._seriesWrap = elMarkersWrap;
                  this._seriesWrapIndex = seriesIndex;
                }
              }
            }
            markerElement = emit.drawMarker(p2.x[q], p2.y[q], opts);
            markerElement.attr("rel", dataPointIndex);
            markerElement.attr("j", dataPointIndex);
            markerElement.attr("index", seriesIndex);
            markerElement.node.setAttribute("default-marker-size", opts.pSize);
            applyProgressiveReveal(markerElement, p2.x[q], w2);
            this._filters.setSelectionFilter(
              markerElement,
              seriesIndex,
              dataPointIndex
            );
            if (elMarkersWrap) {
              elMarkersWrap.add(markerElement);
            }
          }
        } else {
          if (typeof w2.globals.pointsArray[seriesIndex] === "undefined")
            w2.globals.pointsArray[seriesIndex] = [];
          w2.globals.pointsArray[seriesIndex].push([p2.x[q], p2.y[q]]);
        }
      }
    }
    return elMarkersWrap;
  }
  /** @param {{cssClass: any, seriesIndex: any, dataPointIndex?: any, radius?: any, size?: any, strokeWidth?: any}} opts */
  getMarkerConfig({
    cssClass,
    seriesIndex,
    dataPointIndex = null,
    radius = null,
    size = null,
    strokeWidth = null
  }) {
    const w2 = this.w;
    const pStyle = this.getMarkerStyle(seriesIndex);
    let pSize = size === null ? w2.globals.markers.size[seriesIndex] : size;
    const m2 = w2.config.markers;
    if (dataPointIndex !== null && m2.discrete.length) {
      m2.discrete.map((marker) => {
        if (marker.seriesIndex === seriesIndex && marker.dataPointIndex === dataPointIndex) {
          if (marker.strokeColor !== void 0) {
            pStyle.pointStrokeColor = marker.strokeColor;
          }
          if (marker.fillColor !== void 0) {
            pStyle.pointFillColor = marker.fillColor;
          }
          if (marker.size !== void 0) pSize = marker.size;
          if (marker.shape !== void 0) pStyle.pointShape = marker.shape;
        }
      });
    }
    return {
      pSize: radius === null ? pSize : radius,
      pRadius: radius !== null ? radius : m2.radius,
      pointStrokeWidth: strokeWidth !== null ? strokeWidth : Array.isArray(m2.strokeWidth) ? m2.strokeWidth[seriesIndex] : m2.strokeWidth,
      pointStrokeColor: pStyle.pointStrokeColor,
      pointFillColor: pStyle.pointFillColor,
      shape: pStyle.pointShape || (Array.isArray(m2.shape) ? m2.shape[seriesIndex] : m2.shape),
      class: cssClass,
      pointStrokeOpacity: Array.isArray(m2.strokeOpacity) ? m2.strokeOpacity[seriesIndex] : m2.strokeOpacity,
      pointStrokeDashArray: Array.isArray(m2.strokeDashArray) ? m2.strokeDashArray[seriesIndex] : m2.strokeDashArray,
      pointFillOpacity: Array.isArray(m2.fillOpacity) ? m2.fillOpacity[seriesIndex] : m2.fillOpacity,
      seriesIndex
    };
  }
  /**
   * @param {any} parentGroup
   */
  setupMarkerDelegation(parentGroup) {
    const w2 = this.w;
    const selector = ".apexcharts-marker";
    this._graphics.setupEventDelegation(parentGroup, selector);
    parentGroup.node.addEventListener("click", (e2) => {
      if (w2.config.markers.onClick) {
        const targetNode = Graphics._findDelegateTarget(
          e2.target,
          parentGroup.node,
          selector
        );
        if (targetNode) w2.config.markers.onClick(e2);
      }
    });
    parentGroup.node.addEventListener("dblclick", (e2) => {
      if (w2.config.markers.onDblClick) {
        const targetNode = Graphics._findDelegateTarget(
          e2.target,
          parentGroup.node,
          selector
        );
        if (targetNode) w2.config.markers.onDblClick(e2);
      }
    });
    parentGroup.node.addEventListener(
      "touchstart",
      (e2) => {
        const targetNode = Graphics._findDelegateTarget(
          e2.target,
          parentGroup.node,
          selector
        );
        if (targetNode && targetNode.instance) {
          this._graphics.pathMouseDown(targetNode.instance, e2);
        }
      },
      { passive: true }
    );
  }
  /**
   * @returns {any}
   * @param {number} seriesIndex
   */
  getMarkerStyle(seriesIndex) {
    const w2 = this.w;
    const colors = w2.globals.markers.colors;
    const strokeColors = w2.config.markers.strokeColor || w2.config.markers.strokeColors;
    const pointStrokeColor = Array.isArray(strokeColors) ? strokeColors[seriesIndex] : strokeColors;
    const pointFillColor = Array.isArray(colors) ? colors[seriesIndex] : colors;
    return {
      pointStrokeColor,
      pointFillColor
    };
  }
}
class Scatter {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
    this.initialAnim = this.w.config.chart.animations.enabled;
    this.anim = new Animations(this.w);
    this.filters = new Filters(this.w);
    this.fill = new Fill(this.w);
    this.markers = new Markers(this.w, this.ctx);
    this.graphics = new Graphics(this.w);
    this._elPointsWrap = null;
    this._elPointsWrapParent = null;
    this._perSeries = null;
  }
  /**
   * @param {Element} elSeries
   * @param {number} j
   * @param {Record<string, any>} opts
   */
  draw(elSeries, j, opts) {
    const w2 = this.w;
    const graphics = this.graphics;
    const emit = seriesEmitter(this.ctx, graphics);
    const realIndex = opts.realIndex;
    const pointsPos = opts.pointsPos;
    const zRatio = opts.zRatio;
    const elPointsMain = opts.elParent;
    let elPointsWrap = this._elPointsWrap;
    if (!elPointsWrap || this._elPointsWrapParent !== elPointsMain) {
      elPointsWrap = emit.group({
        class: `apexcharts-series-markers apexcharts-series-${w2.config.chart.type}`
      });
      elPointsWrap.attr(
        "clip-path",
        `url(#gridRectMarkerMask${w2.globals.cuid})`
      );
      this.markers.setupMarkerDelegation(elPointsWrap);
      elPointsMain.add(elPointsWrap);
      this._elPointsWrap = elPointsWrap;
      this._elPointsWrapParent = elPointsMain;
      this._perSeries = this._buildPerSeriesCache(realIndex, emit);
    }
    if (Array.isArray(pointsPos.x)) {
      for (let q = 0; q < pointsPos.x.length; q++) {
        let dataPointIndex = j + 1;
        let shouldDraw = true;
        if (j === 0 && q === 0) dataPointIndex = 0;
        if (j === 0 && q === 1) dataPointIndex = 1;
        let radius = w2.globals.markers.size[realIndex];
        if (zRatio !== Infinity) {
          const bubble = w2.config.plotOptions.bubble;
          radius = w2.seriesData.seriesZ[realIndex][dataPointIndex];
          if (bubble.zScaling) {
            radius /= zRatio;
          }
          if (bubble.minBubbleRadius && radius < bubble.minBubbleRadius) {
            radius = bubble.minBubbleRadius;
          }
          if (bubble.maxBubbleRadius && radius > bubble.maxBubbleRadius) {
            radius = bubble.maxBubbleRadius;
          }
        }
        const x = pointsPos.x[q];
        const y = pointsPos.y[q];
        radius = radius || 0;
        if (y === null || typeof w2.seriesData.series[realIndex][dataPointIndex] === "undefined") {
          shouldDraw = false;
        }
        if (shouldDraw) {
          const point = this.drawPoint(
            x,
            y,
            radius,
            realIndex,
            dataPointIndex,
            j
          );
          elPointsWrap.add(point);
          if (emit.kind === "canvas") {
            if (typeof w2.globals.pointsArray[realIndex] === "undefined") {
              w2.globals.pointsArray[realIndex] = [];
            }
            w2.globals.pointsArray[realIndex][dataPointIndex] = [x, y];
          }
        }
      }
    }
  }
  /**
   * Per-series constants for drawPoint's hot path. Everything here is uniform
   * across the points of one series; computing or allocating it per point is
   * measurable overhead at 20k-50k points.
   * @param {number} realIndex
   * @param {any} emit
   */
  _buildPerSeriesCache(realIndex, emit) {
    var _a;
    const w2 = this.w;
    return {
      realIndex,
      emit,
      isBubble: w2.config.chart.type === "bubble" || w2.globals.comboCharts && w2.config.series[realIndex] && /** @type {Record<string,any>} */
      w2.config.series[realIndex].type === "bubble",
      // discrete markers vary per point; they disable both caches below
      canCacheConfig: !w2.config.markers.discrete.length,
      /** @type {any} lazily built shared marker config (first point) */
      markerConfig: null,
      /** @type {boolean|undefined} lazily resolved on the first fillPath call */
      fillCacheable: void 0,
      /** @type {any} cached fillPath result (undefined = not cached yet) */
      fillCircle: void 0,
      dropShadowEnabled: w2.config.chart.dropShadow.enabled,
      doInitialAnim: this.initialAnim && !w2.globals.dataChanged && !w2.globals.resized,
      jitter: (_a = w2.config.plotOptions.scatter) == null ? void 0 : _a.jitter,
      /** @type {any} lazily built pop-animation constants */
      anim: null
    };
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {number} radius
   * @param {number} realIndex
   * @param {number} dataPointIndex
   * @param {number} j
   */
  drawPoint(x, y, radius, realIndex, dataPointIndex, j) {
    var _a;
    const w2 = this.w;
    const i2 = realIndex;
    const anim = this.anim;
    const filters = this.filters;
    const fill = this.fill;
    const markers = this.markers;
    let ps = this._perSeries;
    if (!ps || ps.realIndex !== realIndex) {
      ps = this._perSeries = this._buildPerSeriesCache(
        realIndex,
        seriesEmitter(this.ctx, this.graphics)
      );
    }
    const emit = ps.emit;
    let markerConfig;
    if (ps.canCacheConfig) {
      if (!ps.markerConfig) {
        ps.markerConfig = markers.getMarkerConfig({
          cssClass: "apexcharts-marker",
          seriesIndex: i2,
          dataPointIndex,
          radius: ps.isBubble ? radius : null
        });
      }
      markerConfig = ps.markerConfig;
      if (ps.isBubble) {
        markerConfig.pSize = radius;
        markerConfig.pRadius = radius;
      }
    } else {
      markerConfig = markers.getMarkerConfig({
        cssClass: "apexcharts-marker",
        seriesIndex: i2,
        dataPointIndex,
        radius: ps.isBubble ? radius : null
      });
    }
    const _si = (
      /** @type {Record<string,any>} */
      w2.config.series[i2]
    );
    const dataItem = _si.data[dataPointIndex];
    let pathFillCircle;
    if (ps.fillCircle !== void 0) {
      pathFillCircle = ps.fillCircle;
    } else {
      pathFillCircle = fill.fillPath({
        seriesNumber: realIndex,
        dataPointIndex,
        color: markerConfig.pointFillColor,
        patternUnits: "objectBoundingBox",
        value: w2.seriesData.series[realIndex][j]
      });
      if (ps.fillCacheable === void 0) {
        ps.fillCacheable = ps.canCacheConfig && fill.getFillType(realIndex) === "solid" && typeof markerConfig.pointFillColor === "string" && !!markerConfig.pointFillColor;
      }
      if (ps.fillCacheable && !(dataItem == null ? void 0 : dataItem.fillColor)) {
        ps.fillCircle = pathFillCircle;
      }
    }
    const el = emit.drawMarker(x, y, markerConfig);
    if (dataItem == null ? void 0 : dataItem.fillColor) {
      pathFillCircle = dataItem.fillColor;
    }
    const jt = ps.jitter;
    if ((jt == null ? void 0 : jt.enabled) && jt.distributed && w2.globals.colors.length) {
      const bandIdx = Math.round(
        (_a = w2.seriesData.seriesX[realIndex]) == null ? void 0 : _a[dataPointIndex]
      );
      if (!isNaN(bandIdx)) {
        pathFillCircle = w2.globals.colors[bandIdx % w2.globals.colors.length];
      }
    }
    el.attr({
      fill: pathFillCircle
    });
    if (ps.dropShadowEnabled) {
      const dropShadow = w2.config.chart.dropShadow;
      filters.dropShadow(el, dropShadow, realIndex);
    }
    if (ps.doInitialAnim) {
      if (!ps.anim) {
        const animCfg = w2.config.chart.animations;
        const totalPoints = w2.globals.dataPoints || 1;
        const gradCfg = animCfg.animateGradually;
        const gradEnabled = gradCfg && gradCfg.enabled !== false;
        ps.anim = {
          popSpeed: animCfg.speed,
          baseDelay: gradEnabled ? Math.min(20, animCfg.speed * 0.5 / Math.max(1, totalPoints)) : 0
        };
      }
      const delay = computeStagger({
        style: ps.anim.baseDelay > 0 ? "sequential" : "none",
        index: dataPointIndex,
        baseDelay: ps.anim.baseDelay
      });
      anim.animatePop(el, {
        speed: ps.anim.popSpeed,
        delay,
        onComplete: () => anim.animationCompleted(el)
      });
    } else {
      w2.globals.animationEnded = true;
    }
    el.attr({
      rel: dataPointIndex,
      j: dataPointIndex,
      index: realIndex,
      "default-marker-size": markerConfig.pSize
    });
    filters.setSelectionFilter(el, realIndex, dataPointIndex);
    return el;
  }
  /**
   * @param {number} y
   */
  centerTextInBubble(y) {
    const w2 = this.w;
    y = y + parseInt(w2.config.dataLabels.style.fontSize, 10) / 4;
    return {
      y
    };
  }
}
const resolveDataLabelOffset = (value, w2, seriesIndex, dataPointIndex) => {
  if (typeof value !== "function") return value;
  const resolved = value({
    series: w2.seriesData.series,
    seriesIndex,
    dataPointIndex,
    w: w2
  });
  return Number.isFinite(resolved) ? resolved : 0;
};
class DataLabels {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext | null} ctx
   */
  constructor(w2, ctx = null) {
    this.w = w2;
    this.ctx = ctx;
  }
  // When there are many datalabels to be printed, and some of them overlaps each other in the same series, this method will take care of that
  // Also, when datalabels exceeds the drawable area and get clipped off, we need to adjust and move some pixels to make them visible again
  /**
   * @param {number} x
   * @param {number} y
   * @param {any} val
   * @param {number} i
   * @param {number} dataPointIndex
   * @param {boolean} alwaysDrawDataLabel
   * @param {string} fontSize
   */
  dataLabelsCorrection(x, y, val, i2, dataPointIndex, alwaysDrawDataLabel, fontSize) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    let drawnextLabel = false;
    const textRects = (
      /** @type {any} */
      graphics.getTextRects(val, fontSize)
    );
    const width = textRects.width;
    const height = textRects.height;
    if (y < 0) y = 0;
    if (y > w2.layout.gridHeight + height) y = w2.layout.gridHeight + height / 2;
    if (typeof /** @type {any} */
    w2.globals.dataLabelsRects[i2] === "undefined") {
      w2.globals.dataLabelsRects[i2] = [];
    }
    w2.globals.dataLabelsRects[i2].push({
      x,
      y,
      width,
      height
    });
    const len = (
      /** @type {any} */
      w2.globals.dataLabelsRects[i2].length - 2
    );
    const lastDrawnIndex = typeof w2.globals.lastDrawnDataLabelsIndexes[i2] !== "undefined" ? w2.globals.lastDrawnDataLabelsIndexes[i2][w2.globals.lastDrawnDataLabelsIndexes[i2].length - 1] : 0;
    if (typeof /** @type {any} */
    w2.globals.dataLabelsRects[i2][len] !== "undefined") {
      const lastDataLabelRect = (
        /** @type {any} */
        w2.globals.dataLabelsRects[i2][lastDrawnIndex]
      );
      if (
        // next label forward and x not intersecting
        x > lastDataLabelRect.x + lastDataLabelRect.width || y > lastDataLabelRect.y + lastDataLabelRect.height || y + height < lastDataLabelRect.y || x + width < lastDataLabelRect.x
      ) {
        drawnextLabel = true;
      }
    }
    if (dataPointIndex === 0 || alwaysDrawDataLabel) {
      drawnextLabel = true;
    }
    return {
      x,
      y,
      textRects,
      drawnextLabel
    };
  }
  /** @param {{type: any, pos: any, i: any, j: any, isRangeStart: any, strokeWidth?: any}} opts */
  drawDataLabel({ type, pos, i: i2, j, isRangeStart, strokeWidth = 2 }) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const dataLabelsConfig = w2.config.dataLabels;
    let x = 0;
    let y = 0;
    let dataPointIndex = j;
    let elDataLabelsWrap = null;
    const seriesCollapsed = w2.globals.collapsedSeriesIndices.indexOf(i2) !== -1;
    if (seriesCollapsed || !dataLabelsConfig.enabled || !Array.isArray(pos.x)) {
      return elDataLabelsWrap;
    }
    elDataLabelsWrap = graphics.group({
      class: "apexcharts-data-labels"
    });
    for (let q = 0; q < pos.x.length; q++) {
      if (j === 1 && q === 0) dataPointIndex = 0;
      if (j === 1 && q === 1) dataPointIndex = 1;
      x = pos.x[q] + resolveDataLabelOffset(dataLabelsConfig.offsetX, w2, i2, dataPointIndex);
      y = pos.y[q] + resolveDataLabelOffset(dataLabelsConfig.offsetY, w2, i2, dataPointIndex) + strokeWidth;
      if (!isNaN(x)) {
        let val = w2.seriesData.series[i2][dataPointIndex];
        if (type === "rangeArea") {
          if (isRangeStart) {
            val = w2.rangeData.seriesRangeStart[i2][dataPointIndex];
          } else {
            val = w2.rangeData.seriesRangeEnd[i2][dataPointIndex];
          }
        }
        let text = "";
        const getText = (v2) => {
          return w2.config.dataLabels.formatter(v2, {
            seriesIndex: i2,
            dataPointIndex,
            w: w2
          });
        };
        if (w2.config.chart.type === "bubble") {
          val = w2.seriesData.seriesZ[i2][dataPointIndex];
          text = getText(val);
          y = pos.y[q];
          const scatter = new Scatter(
            this.w,
            /** @type {import('../types/internal').ChartContext} */
            this.ctx
          );
          const centerTextInBubbleCoords = scatter.centerTextInBubble(y);
          y = centerTextInBubbleCoords.y;
        } else {
          if (typeof val !== "undefined") {
            text = getText(val);
          }
        }
        let textAnchor = w2.config.dataLabels.textAnchor;
        if (w2.globals.isSlopeChart) {
          if (dataPointIndex === 0) {
            textAnchor = "end";
          } else if (dataPointIndex === /** @type {Record<string,any>} */
          w2.config.series[i2].data.length - 1) {
            textAnchor = "start";
          } else {
            textAnchor = "middle";
          }
        }
        this.plotDataLabelsText({
          x,
          y,
          text,
          i: i2,
          j: dataPointIndex,
          parent: elDataLabelsWrap,
          offsetCorrection: true,
          dataLabelsConfig: w2.config.dataLabels,
          textAnchor
        });
      }
    }
    return elDataLabelsWrap;
  }
  /**
   * @param {Record<string, any>} opts
   */
  plotDataLabelsText(opts) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    let {
      x,
      y,
      i: i2,
      j,
      text,
      textAnchor,
      fontSize,
      parent,
      dataLabelsConfig,
      color,
      alwaysDrawDataLabel,
      offsetCorrection,
      className,
      // some callers (radar) reuse `j` for something other than the data point
      // index, so per-point offsets take these explicit indices when supplied
      seriesIndex = i2,
      dataPointIndex = j
    } = opts;
    let dataLabelText = null;
    if (Array.isArray(w2.config.dataLabels.enabledOnSeries)) {
      if (w2.config.dataLabels.enabledOnSeries.indexOf(i2) < 0) {
        return dataLabelText;
      }
    }
    let correctedLabels = {
      x,
      y,
      drawnextLabel: true,
      textRects: null
    };
    if (offsetCorrection) {
      correctedLabels = this.dataLabelsCorrection(
        x,
        y,
        text,
        i2,
        j,
        alwaysDrawDataLabel,
        parseInt(
          /** @type {any} */
          dataLabelsConfig.style.fontSize,
          10
        ).toString()
      );
    }
    if (!w2.interact.zoomed) {
      x = correctedLabels.x;
      y = correctedLabels.y;
    }
    if (correctedLabels.textRects) {
      const barPad = w2.globals.barPadForNumericAxis || 0;
      if (x < -(barPad + 20) - /** @type {any} */
      correctedLabels.textRects.width || x > w2.layout.gridWidth + /** @type {any} */
      correctedLabels.textRects.width + barPad + 30) {
        text = "";
      }
    }
    let dataLabelColor = w2.globals.dataLabels.style.colors[i2];
    if ((w2.config.chart.type === "bar" || w2.config.chart.type === "rangeBar") && w2.config.plotOptions.bar.distributed || w2.config.dataLabels.distributed) {
      dataLabelColor = w2.globals.dataLabels.style.colors[j];
    }
    if (typeof dataLabelColor === "function") {
      dataLabelColor = /** @type {any} */
      dataLabelColor({
        series: w2.seriesData.series,
        seriesIndex: i2,
        dataPointIndex: j,
        w: w2
      });
    }
    if (color) {
      dataLabelColor = color;
    }
    const offsetsHandledElsewhere = w2.config.chart.type === "bar" || w2.config.chart.type === "rangeBar";
    const resolvedOffX = offsetsHandledElsewhere ? 0 : resolveDataLabelOffset(
      dataLabelsConfig.offsetX,
      w2,
      seriesIndex,
      dataPointIndex
    );
    const resolvedOffY = offsetsHandledElsewhere ? 0 : resolveDataLabelOffset(
      dataLabelsConfig.offsetY,
      w2,
      seriesIndex,
      dataPointIndex
    );
    let offX = resolvedOffX;
    const offY = resolvedOffY;
    if (w2.globals.isSlopeChart) {
      if (j !== 0) {
        offX = resolvedOffX * -2 + 5;
      }
      if (j !== 0 && j !== /** @type {Record<string,any>} */
      w2.config.series[i2].data.length - 1) {
        offX = 0;
      }
    }
    if (correctedLabels.drawnextLabel) {
      if (textAnchor === "middle") {
        if (x === w2.layout.gridWidth) {
          textAnchor = "end";
        }
      }
      dataLabelText = graphics.drawText({
        x: x + offX,
        y: y + offY,
        foreColor: dataLabelColor,
        textAnchor: textAnchor || dataLabelsConfig.textAnchor,
        text,
        fontSize: fontSize || dataLabelsConfig.style.fontSize,
        fontFamily: dataLabelsConfig.style.fontFamily,
        fontWeight: dataLabelsConfig.style.fontWeight || "normal"
      });
      dataLabelText.attr({
        class: className || "apexcharts-datalabel",
        cx: x,
        cy: y
      });
      if (dataLabelsConfig.dropShadow.enabled) {
        const textShadow = dataLabelsConfig.dropShadow;
        const filters = new Filters(this.w);
        filters.dropShadow(dataLabelText, textShadow);
      }
      parent.add(dataLabelText);
      applyProgressiveReveal(dataLabelText, x, w2);
      if (typeof w2.globals.lastDrawnDataLabelsIndexes[i2] === "undefined") {
        w2.globals.lastDrawnDataLabelsIndexes[i2] = [];
      }
      w2.globals.lastDrawnDataLabelsIndexes[i2].push(j);
    }
    return dataLabelText;
  }
  /**
   * @param {Element} el
   * @param {{x: number, y: number, width: number, height: number}} coords
   */
  addBackgroundToDataLabel(el, coords) {
    const w2 = this.w;
    const bCnf = w2.config.dataLabels.background;
    const paddingH = bCnf.padding;
    const paddingV = bCnf.padding / 2;
    const width = coords.width;
    const height = coords.height;
    const graphics = new Graphics(this.w);
    const elRect = graphics.drawRect(
      coords.x - paddingH,
      coords.y - paddingV / 2,
      width + paddingH * 2,
      height + paddingV,
      bCnf.borderRadius,
      w2.config.chart.background === "transparent" || !w2.config.chart.background ? "#fff" : w2.config.chart.background,
      bCnf.opacity,
      bCnf.borderWidth,
      bCnf.borderColor
    );
    if (bCnf.dropShadow.enabled) {
      const filters = new Filters(this.w);
      filters.dropShadow(elRect, bCnf.dropShadow);
    }
    return elRect;
  }
  dataLabelsBackground() {
    var _a;
    const w2 = this.w;
    if (w2.config.chart.type === "bubble") return;
    const elDataLabels = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-datalabels text"
    );
    for (let i2 = 0; i2 < elDataLabels.length; i2++) {
      const el = elDataLabels[i2];
      const coords = (
        /** @type {SVGGraphicsElement} */
        el.getBBox()
      );
      let elRect = null;
      if (coords.width && coords.height) {
        elRect = this.addBackgroundToDataLabel(el, coords);
      }
      if (elRect) {
        (_a = el.parentNode) == null ? void 0 : _a.insertBefore(elRect.node, el);
        const background = w2.config.dataLabels.background.backgroundColor || el.getAttribute("fill");
        const shouldAnim = w2.config.chart.animations.enabled && !w2.globals.resized && !w2.globals.dataChanged;
        if (shouldAnim) {
          elRect.animate().attr({ fill: background });
        } else {
          elRect.attr({ fill: background });
        }
        el.setAttribute("fill", w2.config.dataLabels.background.foreColor);
        const cxAttr = el.getAttribute("cx");
        if (cxAttr !== null) {
          applyProgressiveReveal(elRect, parseFloat(cxAttr), w2);
        }
      }
    }
  }
  bringForward() {
    const w2 = this.w;
    const elDataLabelsNodes = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-datalabels"
    );
    const elSeries = w2.dom.baseEl.querySelector(
      ".apexcharts-plot-series:last-child"
    );
    for (let i2 = 0; i2 < elDataLabelsNodes.length; i2++) {
      if (elSeries) {
        elSeries.insertBefore(elDataLabelsNodes[i2], elSeries.nextSibling);
      }
    }
  }
}
class PerformanceCache {
  /**
   * Invalidate all caches
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   */
  static invalidateAll(w2) {
    if (!w2 || !w2.globals) return;
    if (w2.globals.cachedSelectors) {
      w2.globals.cachedSelectors = {};
    }
    if (w2.globals.domCache) {
      w2.globals.domCache.clear();
    }
    w2.globals.dimensionCache = {};
  }
  /**
   * Invalidate dimension cache only
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   */
  static invalidateDimensions(w2) {
    if (!w2 || !w2.globals) return;
    w2.globals.dimensionCache = {};
  }
  /**
   * Invalidate selector cache only
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   */
  static invalidateSelectors(w2) {
    if (!w2 || !w2.globals) return;
    if (w2.globals.cachedSelectors) {
      w2.globals.cachedSelectors = {};
    }
  }
  /**
   * Get cached selector result or compute and cache it
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @param {Function} queryFn - Function to execute if not cached
   * @returns {*} Cached or newly computed result
   */
  static getCachedSelector(w2, key, queryFn) {
    if (!w2 || !w2.globals) return queryFn();
    if (!w2.globals.cachedSelectors) {
      w2.globals.cachedSelectors = {};
    }
    if (!w2.globals.cachedSelectors[key]) {
      w2.globals.cachedSelectors[key] = queryFn();
    }
    return w2.globals.cachedSelectors[key];
  }
  /**
   * Get cached dimension or compute and cache it
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @param {Function} computeFn - Function to compute dimensions
   * @param {number} maxAge - Maximum cache age in milliseconds (default: 1000ms)
   * @returns {*} Cached or newly computed dimensions
   */
  static getCachedDimension(w2, key, computeFn, maxAge = 1e3) {
    if (!w2 || !w2.globals) return computeFn();
    if (!w2.globals.dimensionCache) {
      w2.globals.dimensionCache = {};
    }
    const cache = w2.globals.dimensionCache[key];
    const now = Date.now();
    if (cache && cache.lastUpdate && now - cache.lastUpdate < maxAge) {
      return cache.value;
    }
    const value = computeFn();
    w2.globals.dimensionCache[key] = {
      value,
      lastUpdate: now
    };
    return value;
  }
  /**
   * Cache a DOM element reference
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @param {Element} element - DOM element to cache
   */
  static cacheDOMElement(w2, key, element) {
    if (!w2 || !w2.globals) return;
    if (!w2.globals.domCache) {
      w2.globals.domCache = /* @__PURE__ */ new Map();
    }
    w2.globals.domCache.set(key, element);
  }
  /**
   * Get cached DOM element
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @returns {Element|null} Cached element or null
   */
  static getCachedDOMElement(w2, key) {
    if (!w2 || !w2.globals || !w2.globals.domCache) return null;
    return w2.globals.domCache.get(key) || null;
  }
}
class AxesUtils {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   */
  constructor(w2, { theme = null, timeScale = null } = {}) {
    this.w = w2;
    this.theme = theme;
    this.timeScale = timeScale;
  }
  // Based on the formatter function, get the label text and position
  /**
   * @param {any[]} labels
   * @param {Array<Record<string, any>>} timescaleLabels
   * @param {number} x
   * @param {number} i
   * @param {any[]} drawnLabels
   */
  getLabel(labels, timescaleLabels, x, i2, drawnLabels = [], fontSize = "12px", isLeafGroup = true) {
    const w2 = this.w;
    const rawLabel = typeof labels[i2] === "undefined" ? "" : labels[i2];
    let label = rawLabel;
    const xlbFormatter = w2.formatters.xLabelFormatter;
    const customFormatter = w2.config.xaxis.labels.formatter;
    const xFormat = new Formatters(this.w);
    const timestamp = rawLabel;
    if (isLeafGroup) {
      label = /** @type {any} */
      xFormat.xLabelFormat(
        xlbFormatter,
        rawLabel,
        timestamp,
        {
          i: i2,
          dateFormatter: new DateTime(this.w).formatDate,
          w: w2
        }
      );
      if (customFormatter !== void 0) {
        label = customFormatter(rawLabel, labels[i2], {
          i: i2,
          dateFormatter: new DateTime(this.w).formatDate,
          w: w2
        });
      }
    }
    if (timescaleLabels.length > 0) {
      x = timescaleLabels[i2].position;
      label = timescaleLabels[i2].value;
    } else {
      if (w2.config.xaxis.type === "datetime" && customFormatter === void 0) {
        label = "";
      }
    }
    if (typeof label === "undefined") label = "";
    label = Array.isArray(label) ? label : label.toString();
    const graphics = new Graphics(this.w);
    let textRect = {};
    if (w2.layout.rotateXLabels && isLeafGroup) {
      textRect = graphics.getTextRects(
        label,
        parseInt(fontSize, 10).toString(),
        null,
        `rotate(${w2.config.xaxis.labels.rotate} 0 0)`,
        false
      );
    } else {
      textRect = graphics.getTextRects(label, parseInt(fontSize, 10).toString());
    }
    const allowDuplicatesInTimeScale = !w2.config.xaxis.labels.showDuplicates && this.timeScale;
    if (!Array.isArray(label) && (String(label) === "NaN" || drawnLabels.indexOf(label) >= 0 && allowDuplicatesInTimeScale)) {
      label = "";
    }
    return {
      x,
      text: label,
      textRect
    };
  }
  /**
   * @param {number} i
   * @param {any} label
   * @param {number} labelsLen
   */
  checkLabelBasedOnTickamount(i2, label, labelsLen) {
    const w2 = this.w;
    let ticks = w2.config.xaxis.tickAmount;
    if (ticks === "dataPoints") ticks = Math.round(w2.layout.gridWidth / 120);
    if (ticks > labelsLen) return label;
    const tickMultiple = Math.round(labelsLen / (ticks + 1));
    if (i2 % tickMultiple === 0) {
      return label;
    } else {
      label.text = "";
    }
    return label;
  }
  /**
   * @param {number} i
   * @param {any} label
   * @param {number} labelsLen
   * @param {any[]} drawnLabels
   * @param {Array<Record<string, any>>} drawnLabelsRects
   */
  checkForOverflowingLabels(i2, label, labelsLen, drawnLabels, drawnLabelsRects) {
    const w2 = this.w;
    if (i2 === 0) {
      if (w2.globals.skipFirstTimelinelabel) {
        label.text = "";
      }
    }
    if (i2 === labelsLen - 1) {
      if (w2.globals.skipLastTimelinelabel) {
        label.text = "";
      }
    }
    if (w2.config.xaxis.labels.hideOverlappingLabels && drawnLabels.length > 0) {
      const prev = drawnLabelsRects[drawnLabelsRects.length - 1];
      if (w2.config.xaxis.labels.trim && w2.config.xaxis.type !== "datetime") {
        return label;
      }
      if (
        /** @type {any} */
        label.x < prev.textRect.width / (w2.layout.rotateXLabels ? (
          // Floor the rotation at 1deg: rotateAlways:true forces
          // rotateXLabels even with rotate:0, and a 0 divisor here makes the
          // spread Infinity, blanking every label after the first.
          Math.max(Math.abs(w2.config.xaxis.labels.rotate), 1) / 12
        ) : 1.01) + prev.x
      ) {
        label.text = "";
      }
    }
    return label;
  }
  /**
   * @param {number} i
   * @param {any[]} labels
   */
  checkForReversedLabels(i2, labels) {
    const w2 = this.w;
    if (w2.config.yaxis[i2] && w2.config.yaxis[i2].reversed) {
      labels.reverse();
    }
    return labels;
  }
  /**
   * @param {number} index
   */
  yAxisAllSeriesCollapsed(index) {
    const gl = this.w.globals;
    return !gl.seriesYAxisMap[index].some((si) => {
      return gl.collapsedSeriesIndices.indexOf(si) === -1;
    });
  }
  // Method to translate annotation.yAxisIndex values from
  // seriesName-as-a-string values to seriesName-as-an-array values (old style
  // series mapping to new style).
  /**
   * @param {number} index
   */
  translateYAxisIndex(index) {
    const w2 = this.w;
    const gl = w2.globals;
    const yaxis = w2.config.yaxis;
    const newStyle = w2.seriesData.series.length > yaxis.length || /**
     * @param {Record<string, any>} a
     */
    yaxis.some((a2) => Array.isArray(a2.seriesName));
    if (newStyle) {
      return index;
    } else {
      return gl.seriesYAxisReverseMap[index];
    }
  }
  /**
   * @param {number} index
   */
  isYAxisHidden(index) {
    const w2 = this.w;
    const yaxis = w2.config.yaxis[index];
    if (!yaxis.show || this.yAxisAllSeriesCollapsed(index)) {
      return true;
    }
    if (!yaxis.showForNullSeries) {
      const seriesIndices = w2.globals.seriesYAxisMap[index];
      const coreUtils = new CoreUtils(this.w);
      return seriesIndices.every((si) => coreUtils.isSeriesNull(si));
    }
    return false;
  }
  // get the label color for y-axis
  // realIndex is the actual series index, while i is the tick Index
  /**
   * @param {string[]} yColors
   * @param {number} realIndex
   */
  getYAxisForeColor(yColors, realIndex) {
    var _a;
    const w2 = this.w;
    if (Array.isArray(yColors) && w2.globals.yAxisScale[realIndex]) {
      (_a = this.theme) == null ? void 0 : _a.pushExtraColors(
        yColors,
        w2.globals.yAxisScale[realIndex].result.length,
        false
      );
    }
    return yColors;
  }
  /**
   * @param {number} x
   * @param {number} tickAmount
   * @param {Record<string, any>} axisBorder
   * @param {Record<string, any>} axisTicks
   * @param {number} realIndex
   * @param {any} labelsDivider
   * @param {any} elYaxis
   */
  drawYAxisTicks(x, tickAmount, axisBorder, axisTicks, realIndex, labelsDivider, elYaxis) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    let tY = w2.layout.translateY + w2.config.yaxis[realIndex].labels.offsetY;
    if (w2.globals.isBarHorizontal) {
      tY = 0;
    } else if (w2.config.chart.type === "heatmap") {
      tY += labelsDivider / 2;
    }
    if (axisTicks.show && tickAmount > 0) {
      if (w2.config.yaxis[realIndex].opposite === true) x = x + axisTicks.width;
      for (let i2 = tickAmount; i2 >= 0; i2--) {
        const elTick = graphics.drawLine(
          x + axisBorder.offsetX - axisTicks.width + axisTicks.offsetX,
          tY + axisTicks.offsetY,
          x + axisBorder.offsetX + axisTicks.offsetX,
          tY + axisTicks.offsetY,
          axisTicks.color
        );
        elYaxis.add(elTick);
        tY += labelsDivider;
      }
    }
  }
}
class XAxis {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   * @param {any} [elgrid]
   */
  constructor(w2, ctx, elgrid) {
    this.w = w2;
    this.ctx = ctx;
    this.elgrid = elgrid;
    this.axesUtils = new AxesUtils(w2, {
      theme: ctx.theme,
      timeScale: ctx.timeScale
    });
    this.xaxisLabels = w2.labelData.labels.slice();
    if (w2.labelData.timescaleLabels.length > 0 && !w2.globals.isBarHorizontal) {
      this.xaxisLabels = w2.labelData.timescaleLabels.slice();
    }
    if (w2.config.xaxis.overwriteCategories) {
      this.xaxisLabels = w2.config.xaxis.overwriteCategories;
    }
    this.drawnLabels = [];
    this.drawnLabelsRects = [];
    if (w2.config.xaxis.position === "top") {
      this.offY = 0;
    } else {
      this.offY = w2.layout.gridHeight;
    }
    this.offY = this.offY + w2.config.xaxis.axisBorder.offsetY;
    this.isCategoryBarHorizontal = w2.config.chart.type === "bar" && w2.config.plotOptions.bar.horizontal;
    this.xaxisFontSize = w2.config.xaxis.labels.style.fontSize;
    this.xaxisFontFamily = w2.config.xaxis.labels.style.fontFamily;
    this.xaxisForeColors = w2.config.xaxis.labels.style.colors;
    this.xaxisBorderWidth = w2.config.xaxis.axisBorder.width;
    if (this.isCategoryBarHorizontal) {
      this.xaxisBorderWidth = w2.config.yaxis[0].axisBorder.width.toString();
    }
    if (String(this.xaxisBorderWidth).indexOf("%") > -1) {
      this.xaxisBorderWidth = w2.layout.gridWidth * parseInt(this.xaxisBorderWidth, 10) / 100;
    } else {
      this.xaxisBorderWidth = parseInt(this.xaxisBorderWidth, 10);
    }
    this.xaxisBorderHeight = w2.config.xaxis.axisBorder.height;
    this.yaxis = w2.config.yaxis[0];
  }
  drawXaxis() {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const elXaxis = graphics.group({
      class: "apexcharts-xaxis",
      transform: `translate(${w2.config.xaxis.offsetX}, ${w2.config.xaxis.offsetY})`
    });
    const elXaxisTexts = graphics.group({
      class: "apexcharts-xaxis-texts-g",
      transform: `translate(${w2.layout.translateXAxisX}, ${w2.layout.translateXAxisY})`
    });
    elXaxis.add(elXaxisTexts);
    let labels = [];
    for (let i2 = 0; i2 < this.xaxisLabels.length; i2++) {
      labels.push(this.xaxisLabels[i2]);
    }
    this.drawXAxisLabelAndGroup(
      true,
      graphics,
      elXaxisTexts,
      labels,
      w2.axisFlags.isXNumeric,
      (i2, colWidth) => colWidth
    );
    if (w2.labelData.hasXaxisGroups) {
      const labelsGroup = w2.labelData.groups;
      labels = [];
      for (let i2 = 0; i2 < labelsGroup.length; i2++) {
        labels.push(labelsGroup[i2].title);
      }
      const overwriteStyles = (
        /** @type {any} */
        {}
      );
      if (w2.config.xaxis.group.style) {
        overwriteStyles.xaxisFontSize = w2.config.xaxis.group.style.fontSize;
        overwriteStyles.xaxisFontFamily = w2.config.xaxis.group.style.fontFamily;
        overwriteStyles.xaxisForeColors = w2.config.xaxis.group.style.colors;
        overwriteStyles.fontWeight = w2.config.xaxis.group.style.fontWeight;
        overwriteStyles.cssClass = w2.config.xaxis.group.style.cssClass;
      }
      this.drawXAxisLabelAndGroup(
        false,
        graphics,
        elXaxisTexts,
        labels,
        false,
        (i2, colWidth) => labelsGroup[i2].cols * colWidth,
        overwriteStyles
      );
    }
    if (w2.config.xaxis.title.text !== void 0) {
      const elXaxisTitle = graphics.group({
        class: "apexcharts-xaxis-title"
      });
      const elXAxisTitleText = graphics.drawText({
        x: w2.layout.gridWidth / 2 + w2.config.xaxis.title.offsetX,
        y: this.offY + parseFloat(this.xaxisFontSize) + (w2.config.xaxis.position === "bottom" ? w2.layout.xAxisLabelsHeight : -w2.layout.xAxisLabelsHeight - 10) + w2.config.xaxis.title.offsetY,
        text: w2.config.xaxis.title.text,
        textAnchor: "middle",
        fontSize: w2.config.xaxis.title.style.fontSize,
        fontFamily: w2.config.xaxis.title.style.fontFamily,
        fontWeight: w2.config.xaxis.title.style.fontWeight,
        foreColor: w2.config.xaxis.title.style.color,
        cssClass: "apexcharts-xaxis-title-text " + w2.config.xaxis.title.style.cssClass
      });
      elXaxisTitle.add(elXAxisTitleText);
      elXaxis.add(elXaxisTitle);
    }
    if (w2.config.xaxis.axisBorder.show) {
      const offX = w2.globals.barPadForNumericAxis;
      const elHorzLine = graphics.drawLine(
        w2.globals.padHorizontal + w2.config.xaxis.axisBorder.offsetX - offX,
        this.offY,
        this.xaxisBorderWidth + offX,
        this.offY,
        w2.config.xaxis.axisBorder.color,
        0,
        this.xaxisBorderHeight
      );
      if (this.elgrid && this.elgrid.elGridBorders && w2.config.grid.show) {
        this.elgrid.elGridBorders.add(elHorzLine);
      } else {
        elXaxis.add(elHorzLine);
      }
    }
    return elXaxis;
  }
  /**
   * @param {Record<string, any>} [overwriteStyles]
   * @param {boolean} isLeafGroup
   * @param {import('../Graphics').default} graphics
   * @param {any} elXaxisTexts
   * @param {any[]} labels
   * @param {boolean} isXNumeric
   * @param {any} colWidthCb
   */
  drawXAxisLabelAndGroup(isLeafGroup, graphics, elXaxisTexts, labels, isXNumeric, colWidthCb, overwriteStyles = {}) {
    var _a, _b;
    const drawnLabels = [];
    const drawnLabelsRects = [];
    const w2 = this.w;
    const xaxisFontSize = overwriteStyles.xaxisFontSize || this.xaxisFontSize;
    const xaxisFontFamily = overwriteStyles.xaxisFontFamily || this.xaxisFontFamily;
    const xaxisForeColors = overwriteStyles.xaxisForeColors || this.xaxisForeColors;
    const fontWeight = overwriteStyles.fontWeight || w2.config.xaxis.labels.style.fontWeight;
    const cssClass = overwriteStyles.cssClass || w2.config.xaxis.labels.style.cssClass;
    let colWidth;
    let xPos = w2.globals.padHorizontal;
    const labelsLen = labels.length;
    let dataPoints = w2.config.xaxis.type === "category" ? w2.globals.dataPoints : labelsLen;
    if (dataPoints === 0 && labelsLen > dataPoints) dataPoints = labelsLen;
    if (isXNumeric) {
      const len = Math.max(
        Number(w2.config.xaxis.tickAmount) || 1,
        dataPoints > 1 ? dataPoints - 1 : dataPoints
      );
      colWidth = w2.layout.gridWidth / Math.min(len, labelsLen - 1);
      xPos = xPos + colWidthCb(0, colWidth) / 2 + w2.config.xaxis.labels.offsetX;
    } else {
      colWidth = w2.layout.gridWidth / dataPoints;
      xPos = xPos + colWidthCb(0, colWidth) + w2.config.xaxis.labels.offsetX;
    }
    for (let i2 = 0; i2 <= labelsLen - 1; i2++) {
      let x = xPos - colWidthCb(i2, colWidth) / 2 + w2.config.xaxis.labels.offsetX;
      if (i2 === 0 && labelsLen === 1 && colWidth / 2 === xPos && dataPoints === 1) {
        x = w2.layout.gridWidth / 2;
      }
      let label = this.axesUtils.getLabel(
        labels,
        w2.labelData.timescaleLabels,
        x,
        i2,
        drawnLabels,
        xaxisFontSize,
        isLeafGroup
      );
      let offsetYCorrection = 28;
      if (w2.layout.rotateXLabels && isLeafGroup) {
        offsetYCorrection = 22;
      }
      if (w2.config.xaxis.title.text && w2.config.xaxis.position === "top") {
        offsetYCorrection += parseFloat(w2.config.xaxis.title.style.fontSize) + 2;
      }
      if (!isLeafGroup) {
        offsetYCorrection = offsetYCorrection + parseFloat(xaxisFontSize) + (w2.layout.xAxisLabelsHeight - w2.layout.xAxisGroupLabelsHeight) + (w2.layout.rotateXLabels ? 10 : 0);
      }
      const isCategoryTickAmounts = typeof w2.config.xaxis.tickAmount !== "undefined" && w2.config.xaxis.tickAmount !== "dataPoints" && w2.config.xaxis.type !== "datetime";
      if (isCategoryTickAmounts) {
        label = this.axesUtils.checkLabelBasedOnTickamount(i2, label, labelsLen);
      } else {
        label = this.axesUtils.checkForOverflowingLabels(
          i2,
          label,
          labelsLen,
          drawnLabels,
          drawnLabelsRects
        );
      }
      const getCatForeColor = () => {
        return isLeafGroup && w2.config.xaxis.convertedCatToNumeric ? xaxisForeColors[w2.globals.minX + i2 - 1] : xaxisForeColors[i2];
      };
      const labelRectWidth = (
        /** @type {any} */
        (_b = (_a = label.textRect) == null ? void 0 : _a.width) != null ? _b : 0
      );
      const halfWidth = labelRectWidth / 2;
      const fullyOutsideGrid = label.x + halfWidth < 0;
      if (w2.config.xaxis.labels.show && !fullyOutsideGrid) {
        const elText = graphics.drawText({
          x: label.x,
          y: this.offY + w2.config.xaxis.labels.offsetY + offsetYCorrection - (w2.config.xaxis.position === "top" ? w2.layout.xAxisHeight + w2.config.xaxis.axisTicks.height - 2 : 0),
          text: label.text,
          textAnchor: "middle",
          fontWeight,
          fontSize: xaxisFontSize,
          fontFamily: xaxisFontFamily,
          foreColor: Array.isArray(xaxisForeColors) ? getCatForeColor() : xaxisForeColors,
          isPlainText: false,
          cssClass: (isLeafGroup ? "apexcharts-xaxis-label " : "apexcharts-xaxis-group-label ") + cssClass
        });
        elXaxisTexts.add(elText);
        elText.on("click", (e2) => {
          if (typeof w2.config.chart.events.xAxisLabelClick === "function") {
            const opts = Object.assign({}, w2, {
              labelIndex: i2
            });
            w2.config.chart.events.xAxisLabelClick(e2, this.ctx, opts);
          }
        });
        if (isLeafGroup) {
          const elTooltipTitle = BrowserAPIs.createElementNS(SVGNS$2, "title");
          elTooltipTitle.textContent = Array.isArray(label.text) ? label.text.join(" ") : label.text;
          elText.node.appendChild(elTooltipTitle);
          if (label.text !== "") {
            drawnLabels.push(label.text);
            drawnLabelsRects.push(label);
          }
        }
      }
      if (i2 < labelsLen - 1) {
        xPos = xPos + colWidthCb(i2 + 1, colWidth);
      }
    }
  }
  // this actually becomes the vertical axis (for bar charts)
  /**
   * @param {number} realIndex
   */
  drawXaxisInversed(realIndex) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const translateYAxisX = w2.config.yaxis[0].opposite ? w2.globals.translateYAxisX[realIndex] : 0;
    const elYaxis = graphics.group({
      class: "apexcharts-yaxis apexcharts-xaxis-inversed",
      rel: realIndex
    });
    const elYaxisTexts = graphics.group({
      class: "apexcharts-yaxis-texts-g apexcharts-xaxis-inversed-texts-g",
      transform: "translate(" + translateYAxisX + ", 0)"
    });
    elYaxis.add(elYaxisTexts);
    const labels = [];
    if (w2.config.yaxis[realIndex].show) {
      for (let i2 = 0; i2 < this.xaxisLabels.length; i2++) {
        labels.push(this.xaxisLabels[i2]);
      }
    }
    const colHeight = w2.layout.gridHeight / labels.length;
    let yPos = -(colHeight / 2.2);
    const lbFormatter = w2.formatters.yLabelFormatters[0];
    const ylabels = w2.config.yaxis[0].labels;
    if (ylabels.show) {
      for (let i2 = 0; i2 <= labels.length - 1; i2++) {
        let label = typeof labels[i2] === "undefined" ? "" : labels[i2];
        label = lbFormatter(label, {
          seriesIndex: realIndex,
          dataPointIndex: i2,
          w: w2
        });
        const yColors = this.axesUtils.getYAxisForeColor(
          ylabels.style.colors,
          realIndex
        );
        const getForeColor = () => {
          return Array.isArray(yColors) ? yColors[i2] : yColors;
        };
        let multiY = 0;
        if (Array.isArray(label)) {
          multiY = label.length / 2 * parseInt(ylabels.style.fontSize, 10);
        }
        let offsetX = ylabels.offsetX - 15;
        let textAnchor = "end";
        if (this.yaxis.opposite) {
          textAnchor = "start";
        }
        if (w2.config.yaxis[0].labels.align === "left") {
          offsetX = ylabels.offsetX;
          textAnchor = "start";
        } else if (w2.config.yaxis[0].labels.align === "center") {
          offsetX = ylabels.offsetX;
          textAnchor = "middle";
        } else if (w2.config.yaxis[0].labels.align === "right") {
          textAnchor = "end";
        }
        const elLabel = graphics.drawText({
          x: offsetX,
          y: yPos + colHeight + ylabels.offsetY - multiY,
          text: label,
          textAnchor,
          foreColor: getForeColor(),
          fontSize: ylabels.style.fontSize,
          fontFamily: ylabels.style.fontFamily,
          fontWeight: ylabels.style.fontWeight,
          isPlainText: false,
          cssClass: "apexcharts-yaxis-label " + ylabels.style.cssClass,
          maxWidth: ylabels.maxWidth
        });
        elYaxisTexts.add(elLabel);
        elLabel.on("click", (e2) => {
          if (typeof w2.config.chart.events.xAxisLabelClick === "function") {
            const opts = Object.assign({}, w2, {
              labelIndex: i2
            });
            w2.config.chart.events.xAxisLabelClick(e2, this.ctx, opts);
          }
        });
        const elTooltipTitle = BrowserAPIs.createElementNS(SVGNS$2, "title");
        elTooltipTitle.textContent = Array.isArray(label) ? label.join(" ") : label;
        elLabel.node.appendChild(elTooltipTitle);
        if (w2.config.yaxis[realIndex].labels.rotate !== 0) {
          const labelRotatingCenter = graphics.rotateAroundCenter(elLabel.node);
          elLabel.node.setAttribute(
            "transform",
            `rotate(${w2.config.yaxis[realIndex].labels.rotate} 0 ${labelRotatingCenter.y})`
          );
        }
        yPos = yPos + colHeight;
      }
    }
    if (w2.config.yaxis[0].title.text !== void 0) {
      const elXaxisTitle = graphics.group({
        class: "apexcharts-yaxis-title apexcharts-xaxis-title-inversed",
        transform: "translate(" + translateYAxisX + ", 0)"
      });
      const elXAxisTitleText = graphics.drawText({
        x: w2.config.yaxis[0].title.offsetX,
        y: w2.layout.gridHeight / 2 + w2.config.yaxis[0].title.offsetY,
        text: w2.config.yaxis[0].title.text,
        textAnchor: "middle",
        foreColor: w2.config.yaxis[0].title.style.color,
        fontSize: w2.config.yaxis[0].title.style.fontSize,
        fontWeight: w2.config.yaxis[0].title.style.fontWeight,
        fontFamily: w2.config.yaxis[0].title.style.fontFamily,
        cssClass: "apexcharts-yaxis-title-text " + w2.config.yaxis[0].title.style.cssClass
      });
      elXaxisTitle.add(elXAxisTitleText);
      elYaxis.add(elXaxisTitle);
    }
    let offX = 0;
    if (this.isCategoryBarHorizontal && w2.config.yaxis[0].opposite) {
      offX = w2.layout.gridWidth;
    }
    const axisBorder = w2.config.xaxis.axisBorder;
    if (axisBorder.show) {
      const elVerticalLine = graphics.drawLine(
        w2.globals.padHorizontal + axisBorder.offsetX + offX,
        1 + axisBorder.offsetY,
        w2.globals.padHorizontal + axisBorder.offsetX + offX,
        w2.layout.gridHeight + axisBorder.offsetY,
        axisBorder.color,
        0
      );
      if (this.elgrid && this.elgrid.elGridBorders && w2.config.grid.show) {
        this.elgrid.elGridBorders.add(elVerticalLine);
      } else {
        elYaxis.add(elVerticalLine);
      }
    }
    if (w2.config.yaxis[0].axisTicks.show) {
      this.axesUtils.drawYAxisTicks(
        offX,
        labels.length,
        w2.config.yaxis[0].axisBorder,
        w2.config.yaxis[0].axisTicks,
        0,
        colHeight,
        elYaxis
      );
    }
    return elYaxis;
  }
  /**
   * @param {number} x1
   * @param {number} y2
   * @param {any} appendToElement
   */
  drawXaxisTicks(x1, y2, appendToElement) {
    const w2 = this.w;
    const x2 = x1;
    if (x1 < 0 || x1 - 2 > w2.layout.gridWidth) return;
    const y1 = this.offY + w2.config.xaxis.axisTicks.offsetY;
    y2 = y2 + y1 + w2.config.xaxis.axisTicks.height;
    if (w2.config.xaxis.position === "top") {
      y2 = y1 - w2.config.xaxis.axisTicks.height;
    }
    if (w2.config.xaxis.axisTicks.show) {
      const graphics = new Graphics(this.w);
      const line = graphics.drawLine(
        x1 + w2.config.xaxis.axisTicks.offsetX,
        y1 + w2.config.xaxis.offsetY,
        x2 + w2.config.xaxis.axisTicks.offsetX,
        y2 + w2.config.xaxis.offsetY,
        w2.config.xaxis.axisTicks.color
      );
      appendToElement.add(line);
      line.node.classList.add("apexcharts-xaxis-tick");
    }
  }
  getXAxisTicksPositions() {
    const w2 = this.w;
    const xAxisTicksPositions = [];
    const xCount = this.xaxisLabels.length;
    let x1 = w2.globals.padHorizontal;
    if (w2.labelData.timescaleLabels.length > 0) {
      for (let i2 = 0; i2 < xCount; i2++) {
        x1 = this.xaxisLabels[i2].position;
        xAxisTicksPositions.push(x1);
      }
    } else {
      const xCountForCategoryCharts = xCount;
      for (let i2 = 0; i2 < xCountForCategoryCharts; i2++) {
        let x1Count = xCountForCategoryCharts;
        if (w2.axisFlags.isXNumeric && w2.config.chart.type !== "bar") {
          x1Count -= 1;
        }
        x1 = x1 + w2.layout.gridWidth / x1Count;
        xAxisTicksPositions.push(x1);
      }
    }
    return xAxisTicksPositions;
  }
  // to rotate x-axis labels or to put ... for longer text in xaxis
  xAxisLabelCorrections() {
    var _a, _b, _c;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const xAxis = w2.dom.baseEl.querySelector(".apexcharts-xaxis-texts-g");
    const xAxisTexts = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-xaxis-texts-g text:not(.apexcharts-xaxis-group-label)"
    );
    const yAxisTextsInversed = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-yaxis-inversed text"
    );
    const xAxisTextsInversed = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-xaxis-inversed-texts-g text tspan"
    );
    if (w2.layout.rotateXLabels || w2.config.xaxis.labels.rotateAlways) {
      for (let xat = 0; xat < xAxisTexts.length; xat++) {
        const textRotatingCenter = graphics.rotateAroundCenter(xAxisTexts[xat]);
        textRotatingCenter.y = textRotatingCenter.y - 1;
        textRotatingCenter.x = textRotatingCenter.x + 1;
        xAxisTexts[xat].setAttribute(
          "transform",
          `rotate(${w2.config.xaxis.labels.rotate} ${textRotatingCenter.x} ${textRotatingCenter.y})`
        );
        xAxisTexts[xat].setAttribute("text-anchor", `end`);
        xAxis == null ? void 0 : xAxis.setAttribute("transform", `translate(0, ${-10})`);
        const tSpan = xAxisTexts[xat].childNodes;
        if (w2.config.xaxis.labels.trim) {
          Array.prototype.forEach.call(tSpan, (ts) => {
            graphics.placeTextWithEllipsis(
              ts,
              ts.textContent,
              w2.layout.xAxisLabelsHeight - (w2.config.legend.position === "bottom" ? 20 : 10)
            );
          });
        }
      }
    } else {
      const width = w2.layout.gridWidth / (w2.labelData.labels.length + 1);
      for (let xat = 0; xat < xAxisTexts.length; xat++) {
        const tSpan = xAxisTexts[xat].childNodes;
        if (w2.config.xaxis.labels.trim && w2.config.xaxis.type !== "datetime") {
          Array.prototype.forEach.call(tSpan, (ts) => {
            graphics.placeTextWithEllipsis(ts, ts.textContent, width);
          });
        }
      }
    }
    if (yAxisTextsInversed.length > 0) {
      const firstLabelPosX = (
        /** @type {SVGGraphicsElement} */
        yAxisTextsInversed[yAxisTextsInversed.length - 1].getBBox()
      );
      const lastLabelPosX = (
        /** @type {SVGGraphicsElement} */
        yAxisTextsInversed[0].getBBox()
      );
      if (firstLabelPosX.x < -20) {
        (_a = yAxisTextsInversed[yAxisTextsInversed.length - 1].parentNode) == null ? void 0 : _a.removeChild(
          yAxisTextsInversed[yAxisTextsInversed.length - 1]
        );
      }
      if (lastLabelPosX.x + lastLabelPosX.width > w2.layout.gridWidth && !w2.globals.isBarHorizontal) {
        (_b = yAxisTextsInversed[0].parentNode) == null ? void 0 : _b.removeChild(yAxisTextsInversed[0]);
      }
      for (let xat = 0; xat < xAxisTextsInversed.length; xat++) {
        graphics.placeTextWithEllipsis(
          xAxisTextsInversed[xat],
          (_c = xAxisTextsInversed[xat].textContent) != null ? _c : "",
          w2.config.yaxis[0].labels.maxWidth - (w2.config.yaxis[0].title.text ? parseFloat(w2.config.yaxis[0].title.style.fontSize) * 2 : 0) - 15
        );
      }
    }
  }
  // renderXAxisBands() {
  //   let w = this.w;
  //   let plotBand = document.createElementNS(SVGNS, 'rect')
  //   w.dom.elGraphical.add(plotBand)
  // }
}
class Grid {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.xaxisLabels = w2.labelData.labels.slice();
    this.axesUtils = new AxesUtils(ctx.w, {
      theme: ctx.theme,
      timeScale: ctx.timeScale
    });
    this.isRangeBar = w2.rangeData.seriesRange.length && w2.globals.isBarHorizontal;
    if (w2.labelData.timescaleLabels.length > 0) {
      this.xaxisLabels = w2.labelData.timescaleLabels.slice();
    }
  }
  /**
   * @param {any} elGrid
   */
  drawGridArea(elGrid = null) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    if (!elGrid) {
      elGrid = graphics.group({ class: "apexcharts-grid" });
    }
    const elVerticalLine = graphics.drawLine(
      w2.globals.padHorizontal,
      1,
      w2.globals.padHorizontal,
      w2.layout.gridHeight,
      "transparent"
    );
    const elHorzLine = graphics.drawLine(
      w2.globals.padHorizontal,
      w2.layout.gridHeight,
      w2.layout.gridWidth,
      w2.layout.gridHeight,
      "transparent"
    );
    elGrid.add(elHorzLine);
    elGrid.add(elVerticalLine);
    return elGrid;
  }
  drawGrid() {
    const gl = this.w.globals;
    if (gl.axisCharts) {
      const elgrid = this.renderGrid();
      this.drawGridArea(elgrid.el);
      return elgrid;
    }
    return null;
  }
  createGridMask() {
    const w2 = this.w;
    const gl = w2.globals;
    const graphics = new Graphics(this.w);
    const strokeSize = Array.isArray(w2.config.stroke.width) ? Math.max(...w2.config.stroke.width) : w2.config.stroke.width;
    const createClipPath = (id) => {
      const clipPath = BrowserAPIs.createElementNS(SVGNS$2, "clipPath");
      clipPath.setAttribute("id", id);
      return clipPath;
    };
    w2.dom.elGridRectMask = createClipPath(`gridRectMask${gl.cuid}`);
    w2.dom.elGridRectBarMask = createClipPath(`gridRectBarMask${gl.cuid}`);
    w2.dom.elGridRectMarkerMask = createClipPath(`gridRectMarkerMask${gl.cuid}`);
    w2.dom.elForecastMask = createClipPath(`forecastMask${gl.cuid}`);
    w2.dom.elNonForecastMask = createClipPath(`nonForecastMask${gl.cuid}`);
    const hasBar = ["bar", "rangeBar", "candlestick", "boxPlot", "violin"].includes(
      w2.config.chart.type
    ) || w2.globals.comboBarCount > 0;
    let barWidthLeft = 0;
    let barWidthRight = 0;
    if (hasBar && w2.axisFlags.isXNumeric && !w2.globals.isBarHorizontal) {
      barWidthLeft = Math.max(w2.layout.gridPad.left, gl.barPadForNumericAxis);
      barWidthRight = Math.max(w2.layout.gridPad.right, gl.barPadForNumericAxis);
    }
    w2.dom.elGridRect = graphics.drawRect(
      -strokeSize / 2 - 2,
      -strokeSize / 2 - 2,
      w2.layout.gridWidth + strokeSize + 4,
      w2.layout.gridHeight + strokeSize + 4,
      0,
      "#fff"
    );
    w2.dom.elGridRectBar = graphics.drawRect(
      -strokeSize / 2 - barWidthLeft - 2,
      -strokeSize / 2 - 2,
      w2.layout.gridWidth + strokeSize + barWidthRight + barWidthLeft + 4,
      w2.layout.gridHeight + strokeSize + 4,
      0,
      "#fff"
    );
    const markerSize = w2.globals.markers.largestSize;
    w2.dom.elGridRectMarker = graphics.drawRect(
      Math.min(-strokeSize / 2 - barWidthLeft - 2, -markerSize),
      -markerSize,
      w2.layout.gridWidth + Math.max(strokeSize + barWidthRight + barWidthLeft + 4, markerSize * 2),
      w2.layout.gridHeight + markerSize * 2,
      0,
      "#fff"
    );
    w2.dom.elGridRectMask.appendChild(w2.dom.elGridRect.node);
    w2.dom.elGridRectBarMask.appendChild(w2.dom.elGridRectBar.node);
    w2.dom.elGridRectMarkerMask.appendChild(w2.dom.elGridRectMarker.node);
    const defs = w2.dom.elDefs.node;
    defs.appendChild(w2.dom.elGridRectMask);
    defs.appendChild(w2.dom.elGridRectBarMask);
    defs.appendChild(w2.dom.elGridRectMarkerMask);
    defs.appendChild(w2.dom.elForecastMask);
    defs.appendChild(w2.dom.elNonForecastMask);
  }
  /** @param {{i: any, x1: any, y1: any, x2: any, y2: any, xCount: any, parent: any}} opts */
  _drawGridLines({ i: i2, x1, y1, x2, y2, xCount, parent }) {
    const w2 = this.w;
    const shouldDraw = () => {
      if (i2 === 0 && w2.globals.skipFirstTimelinelabel) return false;
      if (i2 === xCount - 1 && w2.globals.skipLastTimelinelabel && !w2.config.xaxis.labels.formatter)
        return false;
      if (w2.config.chart.type === "radar") return false;
      return true;
    };
    if (shouldDraw()) {
      if (w2.config.grid.xaxis.lines.show) {
        this._drawGridLine({ i: i2, x1, y1, x2, y2, xCount, parent });
      }
      let y_2 = 0;
      if (w2.labelData.hasXaxisGroups && w2.config.xaxis.tickPlacement === "between") {
        const groups = w2.labelData.groups;
        if (groups) {
          let gacc = 0;
          for (let gi = 0; gacc < i2 && gi < groups.length; gi++) {
            gacc += groups[gi].cols;
          }
          if (gacc === i2) {
            y_2 = w2.layout.xAxisLabelsHeight * 0.6;
          }
        }
      }
      const xAxis = new XAxis(this.w, this.ctx);
      xAxis.drawXaxisTicks(x1, y_2, w2.dom.elGraphical);
    }
  }
  /** @param {{i: any, x1: any, y1: any, x2: any, y2: any, xCount: any, parent: any}} opts */
  _drawGridLine({ i: i2, x1, y1, x2, y2, xCount, parent }) {
    const w2 = this.w;
    const isHorzLine = parent.node.classList.contains(
      "apexcharts-gridlines-horizontal"
    );
    const offX = w2.globals.barPadForNumericAxis;
    const excludeBorders = y1 === 0 && y2 === 0 || x1 === 0 && x2 === 0 || y1 === w2.layout.gridHeight && y2 === w2.layout.gridHeight || w2.globals.isBarHorizontal && (i2 === 0 || i2 === xCount - 1);
    const graphics = new Graphics(this.w);
    const line = graphics.drawLine(
      x1 - (isHorzLine ? offX : 0),
      y1,
      x2 + (isHorzLine ? offX : 0),
      y2,
      w2.config.grid.borderColor,
      w2.config.grid.strokeDashArray
    );
    line.node.classList.add("apexcharts-gridline");
    if (excludeBorders && w2.config.grid.show) {
      this.elGridBorders.add(line);
    } else {
      parent.add(line);
    }
  }
  /** @param {{c: any, x1: any, y1: any, x2: any, y2: any, type: any}} opts */
  _drawGridBandRect({ c: c2, x1, y1, x2, y2, type }) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const offX = w2.globals.barPadForNumericAxis;
    const color = w2.config.grid[type].colors[c2];
    const rect = graphics.drawRect(
      x1 - (type === "row" ? offX : 0),
      y1,
      x2 + (type === "row" ? offX * 2 : 0),
      y2,
      0,
      color,
      w2.config.grid[type].opacity
    );
    this.elg.add(rect);
    rect.attr("clip-path", `url(#gridRectMask${w2.globals.cuid})`);
    rect.node.classList.add(`apexcharts-grid-${type}`);
  }
  /** @param {{xCount: any, tickAmount: any}} opts */
  _drawXYLines({ xCount, tickAmount }) {
    var _a;
    const w2 = this.w;
    const datetimeLines = ({ xC, x1, y1, x2, y2 }) => {
      for (let i2 = 0; i2 < xC; i2++) {
        x1 = /** @type {any} */
        this.xaxisLabels[i2].position;
        x2 = /** @type {any} */
        this.xaxisLabels[i2].position;
        if (x1 < 0 || x1 - 2 > w2.layout.gridWidth) continue;
        this._drawGridLines({
          i: i2,
          x1,
          y1,
          x2,
          y2,
          xCount,
          parent: this.elgridLinesV
        });
      }
    };
    const categoryLines = ({ xC, x1, y1, x2, y2 }) => {
      for (let i2 = 0; i2 < xC + (w2.axisFlags.isXNumeric ? 0 : 1); i2++) {
        if (i2 === 0 && xC === 1 && w2.globals.dataPoints === 1) {
          x1 = w2.layout.gridWidth / 2;
          x2 = x1;
        }
        this._drawGridLines({
          i: i2,
          x1,
          y1,
          x2,
          y2,
          xCount,
          parent: this.elgridLinesV
        });
        x1 += w2.layout.gridWidth / (w2.axisFlags.isXNumeric ? xC - 1 : xC);
        x2 = x1;
      }
    };
    if (w2.config.grid.xaxis.lines.show || w2.config.xaxis.axisTicks.show) {
      const x1 = w2.globals.padHorizontal;
      const y1 = 0;
      let x2;
      const y2 = w2.layout.gridHeight;
      if (w2.labelData.timescaleLabels.length) {
        datetimeLines({ xC: xCount, x1, y1, x2, y2 });
      } else {
        if (w2.axisFlags.isXNumeric) {
          xCount = (_a = w2.globals.xAxisScale) == null ? void 0 : _a.result.length;
        }
        categoryLines({ xC: xCount, x1, y1, x2, y2 });
      }
    }
    if (w2.config.grid.yaxis.lines.show) {
      const x1 = 0;
      let y1 = 0;
      let y2 = 0;
      const x2 = w2.layout.gridWidth;
      let tA = tickAmount + 1;
      if (this.isRangeBar) {
        tA = w2.labelData.labels.length;
      }
      for (let i2 = 0; i2 < tA + (this.isRangeBar ? 1 : 0); i2++) {
        this._drawGridLine({
          i: i2,
          xCount: tA + (this.isRangeBar ? 1 : 0),
          x1,
          y1,
          x2,
          y2,
          parent: this.elgridLinesH
        });
        y1 += w2.layout.gridHeight / (this.isRangeBar ? tA : tickAmount);
        y2 = y1;
      }
    }
  }
  /** @param {{ xCount?: any, tickAmount?: any }} opts */
  _drawInvertedXYLines({ xCount }) {
    const w2 = this.w;
    if (w2.config.grid.xaxis.lines.show || w2.config.xaxis.axisTicks.show) {
      let x1 = w2.globals.padHorizontal;
      const y1 = 0;
      let x2;
      const y2 = w2.layout.gridHeight;
      for (let i2 = 0; i2 < xCount + 1; i2++) {
        if (w2.config.grid.xaxis.lines.show) {
          this._drawGridLine({
            i: i2,
            xCount: xCount + 1,
            x1,
            y1,
            x2,
            y2,
            parent: this.elgridLinesV
          });
        }
        const xAxis = new XAxis(this.w, this.ctx);
        xAxis.drawXaxisTicks(x1, 0, w2.dom.elGraphical);
        x1 += w2.layout.gridWidth / xCount;
        x2 = x1;
      }
    }
    if (w2.config.grid.yaxis.lines.show) {
      const x1 = 0;
      let y1 = 0;
      let y2 = 0;
      const x2 = w2.layout.gridWidth;
      for (let i2 = 0; i2 < w2.globals.dataPoints + 1; i2++) {
        this._drawGridLine({
          i: i2,
          xCount: w2.globals.dataPoints + 1,
          x1,
          y1,
          x2,
          y2,
          parent: this.elgridLinesH
        });
        y1 += w2.layout.gridHeight / w2.globals.dataPoints;
        y2 = y1;
      }
    }
  }
  renderGrid() {
    var _a, _b, _c;
    const w2 = this.w;
    const gl = w2.globals;
    const graphics = new Graphics(this.w);
    this.elg = graphics.group({ class: "apexcharts-grid" });
    this.elgridLinesH = graphics.group({
      class: "apexcharts-gridlines-horizontal"
    });
    this.elgridLinesV = graphics.group({
      class: "apexcharts-gridlines-vertical"
    });
    this.elGridBorders = graphics.group({ class: "apexcharts-grid-borders" });
    this.elg.add(this.elgridLinesH);
    this.elg.add(this.elgridLinesV);
    if (!w2.config.grid.show) {
      this.elgridLinesV.hide();
      this.elgridLinesH.hide();
      this.elGridBorders.hide();
    }
    let gridAxisIndex = 0;
    while (gridAxisIndex < gl.seriesYAxisMap.length && gl.ignoreYAxisIndexes.includes(gridAxisIndex)) {
      gridAxisIndex++;
    }
    if (gridAxisIndex === gl.seriesYAxisMap.length) {
      gridAxisIndex = 0;
    }
    let yTickAmount = gl.yAxisScale[gridAxisIndex].result.length - 1;
    let xCount;
    if (!gl.isBarHorizontal || this.isRangeBar) {
      xCount = this.xaxisLabels.length;
      if (this.isRangeBar) {
        yTickAmount = w2.labelData.labels.length;
        if (w2.config.xaxis.tickAmount && w2.config.xaxis.labels.formatter) {
          xCount = w2.config.xaxis.tickAmount;
        }
        if (((_c = (_b = (_a = gl.yAxisScale) == null ? void 0 : _a[gridAxisIndex]) == null ? void 0 : _b.result) == null ? void 0 : _c.length) > 0 && w2.config.xaxis.type !== "datetime") {
          xCount = gl.yAxisScale[gridAxisIndex].result.length - 1;
        }
      }
      this._drawXYLines({ xCount, tickAmount: yTickAmount });
    } else {
      xCount = yTickAmount;
      yTickAmount = gl.xTickAmount;
      this._drawInvertedXYLines({ xCount, tickAmount: yTickAmount });
    }
    this.drawGridBands(xCount, yTickAmount);
    return {
      el: this.elg,
      elGridBorders: this.elGridBorders,
      xAxisTickWidth: w2.layout.gridWidth / xCount
    };
  }
  /**
   * @param {number} xCount
   * @param {number} tickAmount
   */
  drawGridBands(xCount, tickAmount) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const drawBands = (type, count, x1, y1, x2, y2) => {
      for (let i2 = 0, c2 = 0; i2 < count; i2++, c2++) {
        if (c2 >= w2.config.grid[type].colors.length) {
          c2 = 0;
        }
        this._drawGridBandRect({ c: c2, x1, y1, x2, y2, type });
        y1 += w2.layout.gridHeight / tickAmount;
      }
    };
    if (((_a = w2.config.grid.row.colors) == null ? void 0 : _a.length) > 0) {
      drawBands(
        "row",
        tickAmount,
        0,
        0,
        w2.layout.gridWidth,
        w2.layout.gridHeight / tickAmount
      );
    }
    if (((_b = w2.config.grid.column.colors) == null ? void 0 : _b.length) > 0) {
      let xc = !w2.globals.isBarHorizontal && w2.config.xaxis.tickPlacement === "on" && (w2.config.xaxis.type === "category" || w2.config.xaxis.convertedCatToNumeric) ? xCount - 1 : xCount;
      if (w2.axisFlags.isXNumeric) {
        xc = ((_d = (_c = w2.globals.xAxisScale) == null ? void 0 : _c.result.length) != null ? _d : 1) - 1;
      }
      let x1 = w2.globals.padHorizontal;
      const y1 = 0;
      let x2 = w2.globals.padHorizontal + w2.layout.gridWidth / xc;
      const y2 = w2.layout.gridHeight;
      for (let i2 = 0, c2 = 0; i2 < xCount; i2++, c2++) {
        if (c2 >= w2.config.grid.column.colors.length) {
          c2 = 0;
        }
        if (w2.config.xaxis.type === "datetime") {
          x1 = /** @type {any} */
          this.xaxisLabels[i2].position;
          x2 = /** @type {any} */
          (((_e = this.xaxisLabels[i2 + 1]) == null ? void 0 : _e.position) || w2.layout.gridWidth) - /** @type {any} */
          this.xaxisLabels[i2].position;
        }
        this._drawGridBandRect({ c: c2, x1, y1, x2, y2, type: "column" });
        x1 += w2.layout.gridWidth / xc;
      }
    }
  }
}
class Scales {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.coreUtils = new CoreUtils(this.w);
  }
  // http://stackoverflow.com/questions/326679/choosing-an-attractive-linear-scale-for-a-graphs-y-axis
  // This routine creates the Y axis values for a graph.
  /**
   * @param {number} yMin
   * @param {number} yMax
   */
  niceScale(yMin, yMax, index = 0) {
    const jsPrecision = 1e-11;
    const w2 = this.w;
    const gl = w2.globals;
    let axisCnf;
    let maxTicks;
    let gotMin;
    let gotMax;
    if (gl.isBarHorizontal) {
      axisCnf = w2.config.xaxis;
      maxTicks = Math.max((gl.svgWidth - 100) / 25, 2);
    } else {
      axisCnf = w2.config.yaxis[index];
      maxTicks = Math.max((gl.svgHeight - 100) / 15, 2);
    }
    if (!Utils$1.isNumber(maxTicks)) {
      maxTicks = 10;
    }
    gotMin = axisCnf.min !== void 0 && axisCnf.min !== null;
    gotMax = axisCnf.max !== void 0 && axisCnf.max !== null;
    let gotStepSize = axisCnf.stepSize !== void 0 && axisCnf.stepSize !== null;
    let gotTickAmount = axisCnf.tickAmount !== void 0 && axisCnf.tickAmount !== null;
    let ticks = gotTickAmount ? axisCnf.tickAmount : NICE_SCALE_DEFAULT_TICKS[Math.min(
      Math.round(maxTicks / 2),
      NICE_SCALE_DEFAULT_TICKS.length - 1
    )];
    if (gl.isMultipleYAxis && !gotTickAmount && gl.multiAxisTickAmount > 0) {
      ticks = gl.multiAxisTickAmount;
      gotTickAmount = true;
    }
    if (ticks === "dataPoints") {
      ticks = gl.dataPoints - 1;
    } else {
      ticks = Math.abs(Math.round(ticks));
    }
    if (yMin === Number.MIN_VALUE && yMax === 0 || !Utils$1.isNumber(yMin) && !Utils$1.isNumber(yMax) || yMin === Number.MIN_VALUE && yMax === -Number.MAX_VALUE) {
      yMin = Utils$1.isNumber(axisCnf.min) ? axisCnf.min : 0;
      yMax = Utils$1.isNumber(axisCnf.max) ? axisCnf.max : yMin + ticks;
      gl.allSeriesCollapsed = false;
    }
    if (yMin > yMax) {
      console.warn(
        "axis.min cannot be greater than axis.max: swapping min and max"
      );
      const temp = yMax;
      yMax = yMin;
      yMin = temp;
    } else if (yMin === yMax) {
      yMin = yMin === 0 ? 0 : yMin - 1;
      yMax = yMax === 0 ? 2 : yMax + 1;
    }
    const result = [];
    if (ticks < 1) {
      ticks = 1;
    }
    let tiks = ticks;
    let range = Math.abs(yMax - yMin);
    const proximityRatio = 0.15;
    if (!gotMin && yMin > 0 && yMin / range < proximityRatio) {
      yMin = 0;
      gotMin = true;
    }
    if (!gotMax && yMax < 0 && -yMax / range < proximityRatio) {
      yMax = 0;
      gotMax = true;
    }
    range = Math.abs(yMax - yMin);
    let stepSize = range / tiks;
    let niceStep = stepSize;
    const mag = Math.floor(Math.log10(niceStep));
    const magPow = Math.pow(10, mag);
    let magMsd = Math.ceil(niceStep / magPow);
    magMsd = NICE_SCALE_ALLOWED_MAG_MSD[gl.yValueDecimal === 0 ? 0 : 1][magMsd];
    niceStep = magMsd * magPow;
    stepSize = niceStep;
    if (gl.isBarHorizontal && axisCnf.stepSize && axisCnf.type !== "datetime") {
      stepSize = axisCnf.stepSize;
      gotStepSize = true;
    } else if (gotStepSize) {
      stepSize = axisCnf.stepSize;
    }
    if (gotStepSize) {
      if (axisCnf.forceNiceScale) {
        const stepMag = Math.floor(Math.log10(stepSize));
        stepSize *= Math.pow(10, mag - stepMag);
      }
    }
    if (gotMin && gotMax) {
      let crudeStep = range / tiks;
      if (gotTickAmount) {
        if (gotStepSize) {
          if (Utils$1.mod(range, stepSize) != 0) {
            const gcdStep = Utils$1.getGCD(stepSize, crudeStep);
            if (crudeStep / gcdStep < 10) {
              stepSize = gcdStep;
            } else {
              stepSize = crudeStep;
            }
          } else {
            if (Utils$1.mod(stepSize, crudeStep) == 0) {
              stepSize = crudeStep;
            } else {
              crudeStep = stepSize;
              gotTickAmount = false;
            }
          }
        } else {
          stepSize = crudeStep;
        }
      } else {
        if (gotStepSize) {
          if (Utils$1.mod(range, stepSize) == 0) {
            crudeStep = stepSize;
          } else {
            stepSize = crudeStep;
          }
        } else {
          if (Utils$1.mod(range, stepSize) == 0) {
            crudeStep = stepSize;
          } else {
            tiks = Math.ceil(range / stepSize);
            crudeStep = range / tiks;
            const gcdStep = Utils$1.getGCD(range, stepSize);
            if (range / gcdStep < maxTicks) {
              crudeStep = gcdStep;
            }
            stepSize = crudeStep;
          }
        }
      }
      tiks = Math.round(range / stepSize);
    } else {
      if (!gotMin && !gotMax) {
        if (gl.isMultipleYAxis && gotTickAmount) {
          const tMin = stepSize * Math.floor(yMin / stepSize);
          let tMax = tMin + stepSize * tiks;
          if (tMax < yMax) {
            stepSize *= 2;
          }
          yMin = tMin;
          tMax = yMax;
          yMax = yMin + stepSize * tiks;
          range = Math.abs(yMax - yMin);
          if (yMin > 0 && yMin < Math.abs(tMax - yMax)) {
            yMin = 0;
            yMax = stepSize * tiks;
          }
          if (yMax < 0 && -yMax < Math.abs(tMin - yMin)) {
            yMax = 0;
            yMin = -stepSize * tiks;
          }
        } else {
          yMin = stepSize * Math.floor(yMin / stepSize);
          yMax = stepSize * Math.ceil(yMax / stepSize);
        }
      } else if (gotMax) {
        if (gotTickAmount) {
          yMin = yMax - stepSize * tiks;
        } else {
          const yMinPrev = yMin;
          yMin = stepSize * Math.floor(yMin / stepSize);
          if (Math.abs(yMax - yMin) / Utils$1.getGCD(range, stepSize) > maxTicks) {
            yMin = yMax - stepSize * ticks;
            yMin += stepSize * Math.floor((yMinPrev - yMin) / stepSize);
          }
        }
      } else if (gotMin) {
        if (gotTickAmount) {
          yMax = yMin + stepSize * tiks;
        } else {
          const yMaxPrev = yMax;
          yMax = stepSize * Math.ceil(yMax / stepSize);
          if (Math.abs(yMax - yMin) / Utils$1.getGCD(range, stepSize) > maxTicks) {
            yMax = yMin + stepSize * ticks;
            yMax += stepSize * Math.ceil((yMaxPrev - yMax) / stepSize);
          }
        }
      }
      range = Math.abs(yMax - yMin);
      stepSize = Utils$1.getGCD(range, stepSize);
      tiks = Math.round(range / stepSize);
    }
    if (!gotTickAmount && !(gotMin || gotMax)) {
      tiks = Math.ceil((range - jsPrecision) / (stepSize + jsPrecision));
      if (tiks > 16 && Utils$1.getPrimeFactors(tiks).length < 2) {
        tiks++;
      }
    }
    if (!gotTickAmount && axisCnf.forceNiceScale && gl.yValueDecimal === 0 && tiks > range) {
      tiks = range;
      stepSize = Math.round(range / tiks);
    }
    if (tiks > maxTicks && (!(gotTickAmount || gotStepSize) || axisCnf.forceNiceScale)) {
      const pf = Utils$1.getPrimeFactors(tiks);
      const last = pf.length - 1;
      let tt = tiks;
      reduceLoop: for (var xFactors = 0; xFactors < last; xFactors++) {
        for (var lowest = 0; lowest <= last - xFactors; lowest++) {
          const stop = Math.min(lowest + xFactors, last);
          let t2 = tt;
          let div = 1;
          for (var next = lowest; next <= stop; next++) {
            div *= pf[next];
          }
          t2 /= div;
          if (t2 < maxTicks) {
            tt = t2;
            break reduceLoop;
          }
        }
      }
      if (tt === tiks) {
        stepSize = range;
      } else {
        stepSize = range / tt;
      }
      tiks = Math.round(range / stepSize);
    }
    if (gl.isMultipleYAxis && gl.multiAxisTickAmount == 0 && gl.ignoreYAxisIndexes.indexOf(index) < 0) {
      gl.multiAxisTickAmount = tiks;
    }
    let val = yMin - stepSize;
    const err = stepSize * jsPrecision;
    do {
      val += stepSize;
      result.push(Utils$1.stripNumber(val, 7));
    } while (yMax - val > err);
    return {
      result,
      niceMin: result[0],
      niceMax: result[result.length - 1]
    };
  }
  /** @param {number} yMin @param {number} yMax @param {number|string} ticks @param {number} index @param {number|undefined} step */
  linearScale(yMin, yMax, ticks = 10, index = 0, step = void 0) {
    const range = Math.abs(yMax - yMin);
    let result = [];
    if (yMin === yMax) {
      result = [yMin];
      return {
        result,
        niceMin: result[0],
        niceMax: result[result.length - 1]
      };
    }
    ticks = this._adjustTicksForSmallRange(ticks, index, range);
    if (
      /** @type {any} */
      ticks === "dataPoints"
    ) {
      ticks = this.w.globals.dataPoints - 1;
    }
    const ticksNum = (
      /** @type {number} */
      ticks
    );
    if (!step) {
      step = range / ticksNum;
    }
    const MIN_PRECISION = 2;
    if (step !== 0 && isFinite(step)) {
      const magnitude = Math.floor(Math.log10(Math.abs(step)));
      const precision = Math.max(MIN_PRECISION, -magnitude + MIN_PRECISION);
      const multiplier = Math.pow(10, precision);
      step = Math.round((step + Number.EPSILON) * multiplier) / multiplier;
    }
    let tickCount = ticks === Number.MAX_VALUE ? 5 : ticksNum;
    if (ticks === Number.MAX_VALUE) {
      step = 1;
    }
    let v2 = yMin;
    while (tickCount >= 0) {
      result.push(v2);
      v2 = Utils$1.preciseAddition(v2, step);
      tickCount -= 1;
    }
    return {
      result,
      niceMin: result[0],
      niceMax: result[result.length - 1]
    };
  }
  /**
   * @param {number} yMin
   * @param {number} yMax
   * @param {number} base
   */
  logarithmicScaleNice(yMin, yMax, base) {
    if (yMax <= 0) yMax = Math.max(yMin, base);
    if (yMin <= 0) yMin = Math.min(yMax, base);
    const logs = [];
    const logMax = Math.ceil(Math.log(yMax) / Math.log(base) + 1);
    const logMin = Math.floor(Math.log(yMin) / Math.log(base));
    for (let i2 = logMin; i2 < logMax; i2++) {
      logs.push(Math.pow(base, i2));
    }
    return {
      result: logs,
      niceMin: logs[0],
      niceMax: logs[logs.length - 1]
    };
  }
  /**
   * @param {number} yMin
   * @param {number} yMax
   * @param {number} base
   */
  logarithmicScale(yMin, yMax, base) {
    if (yMax <= 0) yMax = Math.max(yMin, base);
    if (yMin <= 0) yMin = Math.min(yMax, base);
    const logs = [];
    const logMax = Math.log(yMax) / Math.log(base);
    const logMin = Math.log(yMin) / Math.log(base);
    const logRange = logMax - logMin;
    const ticks = Math.round(logRange);
    const logTickSpacing = logRange / ticks;
    for (let i2 = 0, logTick = logMin; i2 < ticks; i2++, logTick += logTickSpacing) {
      logs.push(Math.pow(base, logTick));
    }
    logs.push(Math.pow(base, logMax));
    return {
      result: logs,
      niceMin: yMin,
      niceMax: yMax
    };
  }
  /**
   * @param {number | string} ticks
   * @param {number} index
   * @param {number} range
   */
  _adjustTicksForSmallRange(ticks, index, range) {
    let newTicks = ticks;
    if (typeof index !== "undefined" && this.w.config.yaxis[index].labels.formatter && this.w.config.yaxis[index].tickAmount === void 0) {
      const formattedVal = Number(
        this.w.config.yaxis[index].labels.formatter(1)
      );
      if (Utils$1.isNumber(formattedVal) && this.w.globals.yValueDecimal === 0) {
        newTicks = Math.ceil(range);
      }
    }
    return newTicks < ticks ? newTicks : ticks;
  }
  /**
   * @param {number} index
   * @param {number} minY
   * @param {number} maxY
   */
  setYScaleForIndex(index, minY, maxY) {
    const gl = this.w.globals;
    const cnf = this.w.config;
    const y = gl.isBarHorizontal ? cnf.xaxis : cnf.yaxis[index];
    if (typeof gl.yAxisScale[index] === "undefined") {
      gl.yAxisScale[index] = [];
    }
    const range = Math.abs(maxY - minY);
    if (y.logarithmic && range <= 5) {
      gl.invalidLogScale = true;
    }
    if (y.logarithmic && range > 5) {
      gl.allSeriesCollapsed = false;
      gl.yAxisScale[index] = y.forceNiceScale ? this.logarithmicScaleNice(minY, maxY, y.logBase) : this.logarithmicScale(minY, maxY, y.logBase);
    } else {
      if (maxY === -Number.MAX_VALUE || !Utils$1.isNumber(maxY) || minY === Number.MAX_VALUE || !Utils$1.isNumber(minY)) {
        gl.yAxisScale[index] = this.niceScale(
          Number.MIN_VALUE,
          0,
          index
        );
      } else {
        gl.allSeriesCollapsed = false;
        gl.yAxisScale[index] = this.niceScale(
          minY,
          maxY,
          index
        );
      }
    }
  }
  /**
   * @param {number} minX
   * @param {number} maxX
   */
  setXScale(minX, maxX) {
    const w2 = this.w;
    const gl = w2.globals;
    if (maxX === -Number.MAX_VALUE || !Utils$1.isNumber(maxX)) {
      gl.xAxisScale = this.linearScale(0, 10, 10);
    } else {
      const ticks = gl.xTickAmount;
      gl.xAxisScale = this.linearScale(
        minX,
        maxX,
        ticks,
        0,
        w2.config.xaxis.max === void 0 ? w2.config.xaxis.stepSize : void 0
      );
    }
    return gl.xAxisScale;
  }
  scaleMultipleYAxes() {
    const cnf = this.w.config;
    const gl = this.w.globals;
    this.coreUtils.setSeriesYAxisMappings();
    const axisSeriesMap = gl.seriesYAxisMap;
    const minYArr = gl.minYArr;
    const maxYArr = gl.maxYArr;
    const alignZeroParticipants = [];
    const canAlignZero = !gl.isBarHorizontal;
    gl.allSeriesCollapsed = true;
    gl.barGroups = [];
    axisSeriesMap.forEach((axisSeries, ai) => {
      const groupNames = [];
      axisSeries.forEach((as) => {
        var _a;
        const group = (
          /** @type {Record<string,any>} */
          (_a = cnf.series[as]) == null ? void 0 : _a.group
        );
        if (groupNames.indexOf(group) < 0) {
          groupNames.push(group);
        }
      });
      if (axisSeries.length > 0) {
        let minY = Number.MAX_VALUE;
        let maxY = -Number.MAX_VALUE;
        let lowestY = minY;
        let highestY = maxY;
        let seriesType;
        let seriesGroupName;
        if (cnf.chart.stacked) {
          const mapSeries = new Array(gl.dataPoints).fill(0);
          const sumSeries = [];
          const posSeries = [];
          const negSeries = [];
          groupNames.forEach(() => {
            sumSeries.push(mapSeries.map(() => Number.MIN_VALUE));
            posSeries.push(mapSeries.map(() => Number.MIN_VALUE));
            negSeries.push(mapSeries.map(() => Number.MIN_VALUE));
          });
          for (let i2 = 0; i2 < axisSeries.length; i2++) {
            if (!seriesType && /** @type {Record<string,any>} */
            cnf.series[axisSeries[i2]].type) {
              seriesType = /** @type {Record<string,any>} */
              cnf.series[axisSeries[i2]].type;
            }
            const si = axisSeries[i2];
            if (
              /** @type {Record<string,any>} */
              cnf.series[si].group
            ) {
              seriesGroupName = /** @type {Record<string,any>} */
              cnf.series[si].group;
            } else {
              seriesGroupName = "axis-".concat(ai.toString());
            }
            const collapsed = !(gl.collapsedSeriesIndices.indexOf(si) < 0 && gl.ancillaryCollapsedSeriesIndices.indexOf(si) < 0);
            if (!collapsed) {
              gl.allSeriesCollapsed = false;
              groupNames.forEach((gn, gni) => {
                if (
                  /** @type {Record<string,any>} */
                  cnf.series[si].group === gn
                ) {
                  for (let j = 0; j < this.w.seriesData.series[si].length; j++) {
                    const val = this.w.seriesData.series[si][j];
                    if (val >= 0) {
                      posSeries[gni][j] += val;
                    } else {
                      negSeries[gni][j] += val;
                    }
                    sumSeries[gni][j] += val;
                    lowestY = Math.min(lowestY, val);
                    highestY = Math.max(highestY, val);
                  }
                }
              });
            }
            if (seriesType === "bar" || seriesType === "column") {
              gl.barGroups.push(seriesGroupName);
            }
          }
          if (!seriesType) {
            seriesType = cnf.chart.type;
          }
          if (seriesType === "bar" || seriesType === "column") {
            groupNames.forEach((gn, gni) => {
              minY = Math.min(minY, Math.min.apply(null, negSeries[gni]));
              maxY = Math.max(maxY, Math.max.apply(null, posSeries[gni]));
            });
          } else {
            groupNames.forEach((gn, gni) => {
              lowestY = Math.min(lowestY, Math.min.apply(null, sumSeries[gni]));
              highestY = Math.max(
                highestY,
                Math.max.apply(null, sumSeries[gni])
              );
            });
            minY = lowestY;
            maxY = highestY;
          }
          if (minY === Number.MIN_VALUE && maxY === Number.MIN_VALUE) {
            maxY = -Number.MAX_VALUE;
          }
        } else {
          for (let i2 = 0; i2 < axisSeries.length; i2++) {
            const si = axisSeries[i2];
            minY = Math.min(minY, minYArr[si]);
            maxY = Math.max(maxY, maxYArr[si]);
            const collapsed = !(gl.collapsedSeriesIndices.indexOf(si) < 0 && gl.ancillaryCollapsedSeriesIndices.indexOf(si) < 0);
            if (!collapsed) {
              gl.allSeriesCollapsed = false;
            }
          }
        }
        if (cnf.yaxis[ai].min !== void 0) {
          if (typeof cnf.yaxis[ai].min === "function") {
            minY = cnf.yaxis[ai].min(minY);
          } else {
            minY = cnf.yaxis[ai].min;
          }
        }
        if (cnf.yaxis[ai].max !== void 0) {
          if (typeof cnf.yaxis[ai].max === "function") {
            maxY = cnf.yaxis[ai].max(maxY);
          } else {
            maxY = cnf.yaxis[ai].max;
          }
        }
        gl.barGroups = gl.barGroups.filter((v2, i2, a2) => a2.indexOf(v2) === i2);
        const yaxe = cnf.yaxis[ai];
        const participates = canAlignZero && yaxe.alignZero === true && !yaxe.logarithmic && yaxe.min === void 0 && yaxe.max === void 0 && gl.ignoreYAxisIndexes.indexOf(ai) < 0 && Utils$1.isNumber(minY) && Utils$1.isNumber(maxY);
        if (participates) {
          alignZeroParticipants.push({ ai, minY, maxY });
        } else {
          this.setYScaleForIndex(ai, minY, maxY);
          axisSeries.forEach((si) => {
            minYArr[si] = gl.yAxisScale[ai].niceMin;
            maxYArr[si] = gl.yAxisScale[ai].niceMax;
          });
        }
      } else {
        this.setYScaleForIndex(ai, 0, -Number.MAX_VALUE);
      }
    });
    if (alignZeroParticipants.length >= 2) {
      alignZeroParticipants.forEach((p2) => {
        this.setYScaleForIndex(p2.ai, p2.minY, p2.maxY);
        axisSeriesMap[p2.ai].forEach((si) => {
          minYArr[si] = gl.yAxisScale[p2.ai].niceMin;
          maxYArr[si] = gl.yAxisScale[p2.ai].niceMax;
        });
      });
      let targetRatio = 0;
      alignZeroParticipants.forEach((p2) => {
        const scale = gl.yAxisScale[p2.ai];
        const range = scale.niceMax - scale.niceMin;
        if (range > 0) {
          const r2 = -scale.niceMin / range;
          if (r2 > targetRatio) targetRatio = r2;
        }
      });
      if (targetRatio > 1) targetRatio = 1;
      if (targetRatio < 0) targetRatio = 0;
      const niceRoundUp = (raw) => {
        if (raw <= 0) return 1;
        const mag = Math.floor(Math.log10(raw));
        const magPow = Math.pow(10, mag);
        const msd = raw / magPow;
        let mult;
        if (msd <= 1 + 1e-9) mult = 1;
        else if (msd <= 2 + 1e-9) mult = 2;
        else if (msd <= 2.5 + 1e-9) mult = 2.5;
        else if (msd <= 5 + 1e-9) mult = 5;
        else mult = 10;
        return mult * magPow;
      };
      alignZeroParticipants.forEach((p2) => {
        const scale = gl.yAxisScale[p2.ai];
        if (!scale.result || scale.result.length < 2) return;
        const range = scale.niceMax - scale.niceMin;
        if (range <= 0) return;
        const r2 = -scale.niceMin / range;
        if (Math.abs(r2 - targetRatio) <= 1e-9) return;
        const extendMin = r2 < targetRatio && targetRatio < 1 - 1e-9;
        const extendMaxOnly = !extendMin && r2 > targetRatio && targetRatio > 1e-9;
        if (!extendMin && !extendMaxOnly) return;
        const targetNiceMin = extendMin ? -targetRatio * scale.niceMax / (1 - targetRatio) : scale.niceMin;
        const targetNiceMax = extendMaxOnly ? -scale.niceMin * (1 - targetRatio) / targetRatio : scale.niceMax;
        const newRange = targetNiceMax - targetNiceMin;
        if (newRange <= 0) return;
        const desiredTicks = Math.max(scale.result.length, 5);
        const newStep = niceRoundUp(newRange / Math.max(desiredTicks - 1, 1));
        if (newStep <= 0) return;
        let newNiceMin;
        let newNiceMax;
        if (extendMin) {
          newNiceMin = Math.floor(targetNiceMin / newStep + 1e-9) * newStep;
          const requiredMax = targetRatio > 1e-9 ? newNiceMin * (targetRatio - 1) / targetRatio : scale.niceMax;
          const maxNeeded = Math.max(requiredMax, scale.niceMax);
          newNiceMax = Math.ceil(maxNeeded / newStep - 1e-9) * newStep;
        } else {
          newNiceMax = Math.ceil(targetNiceMax / newStep - 1e-9) * newStep;
          const requiredMin = targetRatio < 1 - 1e-9 ? -targetRatio * newNiceMax / (1 - targetRatio) : scale.niceMin;
          const minNeeded = Math.min(requiredMin, scale.niceMin);
          newNiceMin = Math.floor(minNeeded / newStep + 1e-9) * newStep;
        }
        scale.result = [];
        for (let v2 = newNiceMin; v2 <= newNiceMax + newStep * 1e-9; v2 = Utils$1.preciseAddition(v2, newStep)) {
          scale.result.push(Utils$1.stripNumber(v2, 7));
        }
        scale.niceMin = newNiceMin;
        scale.niceMax = newNiceMax;
        axisSeriesMap[p2.ai].forEach((si) => {
          minYArr[si] = scale.niceMin;
          maxYArr[si] = scale.niceMax;
        });
      });
    } else if (alignZeroParticipants.length === 1) {
      const p2 = alignZeroParticipants[0];
      this.setYScaleForIndex(p2.ai, p2.minY, p2.maxY);
      axisSeriesMap[p2.ai].forEach((si) => {
        minYArr[si] = gl.yAxisScale[p2.ai].niceMin;
        maxYArr[si] = gl.yAxisScale[p2.ai].niceMax;
      });
    }
  }
}
class Range {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.scales = new Scales(this.w);
  }
  init() {
    this.setYRange();
    this.setXRange();
    this.setZRange();
  }
  /**
   * One rendered x-pixel expressed in data units, used to widen the
   * autoScaleYaxis window so a boundary point that is still drawn inside the
   * plot survives a sub-pixel rounding error in `xaxis.min`/`xaxis.max`.
   *
   * The window is taken from the configured bounds (that is what maps onto the
   * grid) and falls back to the series' own x-extent for whichever bound is
   * unset. Returns 0 when there is nothing to scale against: `gridWidth` is
   * still 0 before the first `plotCoords()` run, and a first render has no
   * pixel round-trip to compensate for anyway.
   *
   * @param {number[] | undefined} seriesX
   * @returns {number}
   */
  _xPixelTolerance(seriesX) {
    const gridWidth = this.w.layout.gridWidth;
    if (!gridWidth || !seriesX || !seriesX.length) return 0;
    const cnfX = this.w.config.xaxis;
    const lo = typeof cnfX.min === "number" ? cnfX.min : seriesX[0];
    const hi = typeof cnfX.max === "number" ? cnfX.max : seriesX[seriesX.length - 1];
    if (!(hi > lo)) return 0;
    return (hi - lo) / gridWidth;
  }
  /**
   * @param {number} startingSeriesIndex
   * @param {number | null} [endingSeriesIndex]
   */
  getMinYMaxY(startingSeriesIndex, lowestY = Number.MAX_VALUE, highestY = -Number.MAX_VALUE, endingSeriesIndex = null) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const cnf = this.w.config;
    const gl = this.w.globals;
    let maxY = -Number.MAX_VALUE;
    let minY = Number.MIN_VALUE;
    if (endingSeriesIndex === null) {
      endingSeriesIndex = startingSeriesIndex + 1;
    }
    const series = this.w.seriesData.series;
    let seriesMin = series;
    let seriesMax = series;
    if (cnf.chart.type === "candlestick") {
      seriesMin = this.w.candleData.seriesCandleL;
      seriesMax = this.w.candleData.seriesCandleH;
    } else if (cnf.chart.type === "boxPlot") {
      seriesMin = this.w.candleData.seriesCandleO;
      seriesMax = this.w.candleData.seriesCandleC;
    } else if (cnf.chart.type === "violin") {
      seriesMin = this.w.violinData.seriesViolinMin;
      seriesMax = this.w.violinData.seriesViolinMax;
    } else if (this.w.axisFlags.isRangeData) {
      seriesMin = this.w.rangeData.seriesRangeStart;
      seriesMax = this.w.rangeData.seriesRangeEnd;
    }
    let autoScaleYaxis = false;
    if (this.w.seriesData.seriesX.length >= endingSeriesIndex) {
      const brush = (
        /** @type {any} */
        (_a = gl.brushSource) == null ? void 0 : _a.w.config.chart.brush
      );
      if (cnf.chart.zoom.enabled && cnf.chart.zoom.autoScaleYaxis || (brush == null ? void 0 : brush.enabled) && (brush == null ? void 0 : brush.autoScaleYaxis)) {
        autoScaleYaxis = true;
      }
    }
    for (let i2 = startingSeriesIndex; i2 < endingSeriesIndex; i2++) {
      gl.dataPoints = Math.max(gl.dataPoints, series[i2].length);
      const seriesType = (
        /** @type {Record<string,any>} */
        cnf.series[i2].type
      );
      if (this.w.labelData.categoryLabels.length) {
        gl.dataPoints = this.w.labelData.categoryLabels.filter(
          (label) => typeof label !== "undefined"
        ).length;
      }
      if (this.w.labelData.labels.length && cnf.xaxis.type !== "datetime" && /**
       * @param {number} a
       * @param {number[]} c
       */
      this.w.seriesData.series.reduce((a2, c2) => a2 + c2.length, 0) !== 0) {
        gl.dataPoints = Math.max(gl.dataPoints, this.w.labelData.labels.length);
      }
      let firstXIndex = 0;
      let lastXIndex = series[i2].length - 1;
      if (autoScaleYaxis) {
        const xTolerance = this._xPixelTolerance(this.w.seriesData.seriesX[i2]);
        if (cnf.xaxis.min) {
          const lowerBound = cnf.xaxis.min - xTolerance;
          for (; firstXIndex < lastXIndex && this.w.seriesData.seriesX[i2][firstXIndex] < lowerBound; firstXIndex++) {
          }
        }
        if (cnf.xaxis.max) {
          const upperBound = cnf.xaxis.max + xTolerance;
          for (; lastXIndex > firstXIndex && this.w.seriesData.seriesX[i2][lastXIndex] > upperBound; lastXIndex--) {
          }
        }
      }
      const plainNumeric = seriesMin === series && seriesMax === series && cnf.chart.type !== "boxPlot" && seriesType !== "candlestick" && seriesType !== "boxPlot" && seriesType !== "violin" && seriesType !== "rangeArea" && seriesType !== "rangeBar" && !(this.w.seriesData.seriesGoals[i2] && this.w.seriesData.seriesGoals[i2].length);
      if (plainNumeric) {
        const arr = series[i2];
        const jEnd = Math.min(lastXIndex, arr.length - 1);
        const pe = (_b = this.w.seriesData._parsedExtrema) == null ? void 0 : _b[i2];
        if (pe && pe.ref === arr && pe.len === arr.length && firstXIndex === 0 && jEnd === arr.length - 1) {
          if (pe.maxY > maxY) maxY = pe.maxY;
          if (pe.lowestY < lowestY) lowestY = pe.lowestY;
          if (pe.negMinY < 0 && pe.negMinY < minY) minY = pe.negMinY;
          if (pe.yDec > gl.yValueDecimal) gl.yValueDecimal = pe.yDec;
          if (pe.hasNulls) gl.hasNullValues = true;
        } else {
          let yDec = gl.yValueDecimal;
          let hasNulls = false;
          for (let j = firstXIndex; j <= jEnd; j++) {
            const val = arr[j];
            if (val !== null && typeof val === "number" && val === val && val !== Infinity && val !== -Infinity) {
              if (val > maxY) maxY = val;
              if (val < lowestY) lowestY = val;
              if (minY > val && val < 0) minY = val;
              if (!Number.isInteger(val)) {
                const av = val < 0 ? -val : val;
                if (av >= 1e-6 && av < 1e21) {
                  const str = "" + val;
                  const dot = str.indexOf(".");
                  const dec = dot === -1 ? 0 : str.length - dot - 1;
                  if (dec > yDec) yDec = dec;
                } else {
                  const nv = Utils$1.noExponents(val);
                  if (Utils$1.isFloat(nv)) {
                    yDec = Math.max(yDec, nv.toString().split(".")[1].length);
                  }
                }
              }
            } else {
              hasNulls = true;
            }
          }
          gl.yValueDecimal = yDec;
          if (hasNulls) gl.hasNullValues = true;
        }
        highestY = maxY;
        if (seriesType === "bar" || seriesType === "column") {
          if (minY < 0 && maxY < 0) {
            maxY = 0;
            highestY = Math.max(highestY, 0);
          }
          if (minY === Number.MIN_VALUE) {
            minY = 0;
            lowestY = Math.min(lowestY, 0);
          }
        }
        continue;
      }
      for (let j = firstXIndex; j <= lastXIndex && j < this.w.seriesData.series[i2].length; j++) {
        let val = series[i2][j];
        if (val !== null && Utils$1.isNumber(val)) {
          if (typeof ((_c = seriesMax[i2]) == null ? void 0 : _c[j]) !== "undefined") {
            maxY = Math.max(maxY, seriesMax[i2][j]);
            lowestY = Math.min(lowestY, seriesMax[i2][j]);
          }
          if (typeof ((_d = seriesMin[i2]) == null ? void 0 : _d[j]) !== "undefined") {
            lowestY = Math.min(lowestY, seriesMin[i2][j]);
            highestY = Math.max(highestY, seriesMin[i2][j]);
          }
          switch (seriesType) {
            case "candlestick":
              {
                if (typeof this.w.candleData.seriesCandleC[i2][j] !== "undefined") {
                  maxY = Math.max(maxY, this.w.candleData.seriesCandleH[i2][j]);
                  lowestY = Math.min(
                    lowestY,
                    this.w.candleData.seriesCandleL[i2][j]
                  );
                }
              }
              break;
            case "boxPlot":
              {
                if (typeof this.w.candleData.seriesCandleC[i2][j] !== "undefined") {
                  maxY = Math.max(maxY, this.w.candleData.seriesCandleC[i2][j]);
                  lowestY = Math.min(
                    lowestY,
                    this.w.candleData.seriesCandleO[i2][j]
                  );
                }
              }
              break;
            case "violin":
              {
                if (typeof ((_e = this.w.violinData.seriesViolinMax[i2]) == null ? void 0 : _e[j]) !== "undefined") {
                  maxY = Math.max(maxY, this.w.violinData.seriesViolinMax[i2][j]);
                  lowestY = Math.min(
                    lowestY,
                    this.w.violinData.seriesViolinMin[i2][j]
                  );
                }
              }
              break;
          }
          if (seriesType && seriesType !== "candlestick" && seriesType !== "boxPlot" && seriesType !== "violin" && seriesType !== "rangeArea" && seriesType !== "rangeBar") {
            maxY = Math.max(maxY, this.w.seriesData.series[i2][j]);
            lowestY = Math.min(lowestY, this.w.seriesData.series[i2][j]);
          }
          if (this.w.seriesData.seriesGoals[i2] && this.w.seriesData.seriesGoals[i2][j] && Array.isArray(this.w.seriesData.seriesGoals[i2][j])) {
            this.w.seriesData.seriesGoals[i2][j].forEach(
              (g2) => {
                maxY = Math.max(maxY, g2.value);
                lowestY = Math.min(lowestY, g2.value);
              }
            );
          }
          if (this.w.config.chart.type === "boxPlot" || seriesType === "boxPlot") {
            const boxPts = (_g = (_f = this.w.candleData.seriesBoxPoints) == null ? void 0 : _f[i2]) == null ? void 0 : _g[j];
            if (boxPts) {
              for (let p2 = 0; p2 < boxPts.length; p2++) {
                const pv = boxPts[p2];
                if (typeof pv === "number") {
                  maxY = Math.max(maxY, pv);
                  lowestY = Math.min(lowestY, pv);
                }
              }
            }
          }
          highestY = maxY;
          val = Utils$1.noExponents(val);
          if (Utils$1.isFloat(val)) {
            gl.yValueDecimal = Math.max(
              gl.yValueDecimal,
              val.toString().split(".")[1].length
            );
          }
          if (minY > ((_h = seriesMin[i2]) == null ? void 0 : _h[j]) && ((_i = seriesMin[i2]) == null ? void 0 : _i[j]) < 0) {
            minY = seriesMin[i2][j];
          }
        } else {
          gl.hasNullValues = true;
        }
      }
      if (seriesType === "bar" || seriesType === "column") {
        if (minY < 0 && maxY < 0) {
          maxY = 0;
          highestY = Math.max(highestY, 0);
        }
        if (minY === Number.MIN_VALUE) {
          minY = 0;
          lowestY = Math.min(lowestY, 0);
        }
      }
    }
    if (cnf.chart.type === "rangeBar" && this.w.rangeData.seriesRangeStart.length && gl.isBarHorizontal) {
      minY = lowestY;
    }
    if (cnf.chart.type === "bar") {
      if (minY < 0 && maxY < 0) {
        maxY = 0;
      }
      if (minY === Number.MIN_VALUE) {
        minY = 0;
      }
    }
    return {
      minY,
      maxY,
      lowestY,
      highestY
    };
  }
  setYRange() {
    const gl = this.w.globals;
    const cnf = this.w.config;
    gl.maxY = -Number.MAX_VALUE;
    gl.minY = Number.MIN_VALUE;
    let lowestYInAllSeries = Number.MAX_VALUE;
    let minYMaxY;
    if (gl.isMultipleYAxis) {
      lowestYInAllSeries = Number.MAX_VALUE;
      for (let i2 = 0; i2 < this.w.seriesData.series.length; i2++) {
        minYMaxY = this.getMinYMaxY(i2);
        gl.minYArr[i2] = minYMaxY.lowestY;
        gl.maxYArr[i2] = minYMaxY.highestY;
        lowestYInAllSeries = Math.min(lowestYInAllSeries, minYMaxY.lowestY);
      }
    }
    minYMaxY = this.getMinYMaxY(
      0,
      lowestYInAllSeries,
      void 0,
      this.w.seriesData.series.length
    );
    if (cnf.chart.type === "bar") {
      gl.minY = minYMaxY.minY;
      gl.maxY = minYMaxY.maxY;
    } else {
      gl.minY = minYMaxY.lowestY;
      gl.maxY = minYMaxY.highestY;
    }
    lowestYInAllSeries = minYMaxY.lowestY;
    if (cnf.chart.stacked) {
      this._setStackedMinMax();
    }
    if (cnf.chart.type === "line" || cnf.chart.type === "area" || cnf.chart.type === "scatter" || cnf.chart.type === "candlestick" || cnf.chart.type === "boxPlot" || cnf.chart.type === "violin" || cnf.chart.type === "rangeBar" && !gl.isBarHorizontal) {
      if (gl.minY === Number.MIN_VALUE && lowestYInAllSeries !== -Number.MAX_VALUE && lowestYInAllSeries !== gl.maxY) {
        gl.minY = lowestYInAllSeries;
      }
    } else {
      gl.minY = gl.minY !== Number.MIN_VALUE ? Math.min(minYMaxY.minY, gl.minY) : minYMaxY.minY;
    }
    cnf.yaxis.forEach((yaxe, index) => {
      if (yaxe.max !== void 0) {
        if (typeof yaxe.max === "number") {
          gl.maxYArr[index] = yaxe.max;
        } else if (typeof yaxe.max === "function") {
          gl.maxYArr[index] = yaxe.max(
            gl.isMultipleYAxis ? gl.maxYArr[index] : gl.maxY
          );
        }
        gl.maxY = gl.maxYArr[index];
      }
      if (yaxe.min !== void 0) {
        if (typeof yaxe.min === "number") {
          gl.minYArr[index] = yaxe.min;
        } else if (typeof yaxe.min === "function") {
          gl.minYArr[index] = yaxe.min(
            gl.isMultipleYAxis ? gl.minYArr[index] === Number.MIN_VALUE ? 0 : gl.minYArr[index] : gl.minY
          );
        }
        gl.minY = gl.minYArr[index];
      }
    });
    if (gl.isBarHorizontal) {
      const minmax = ["min", "max"];
      minmax.forEach((m2) => {
        if (cnf.xaxis[m2] !== void 0 && typeof cnf.xaxis[m2] === "number") {
          m2 === "min" ? gl.minY = cnf.xaxis[m2] : gl.maxY = cnf.xaxis[m2];
        }
      });
    }
    if (gl.isMultipleYAxis) {
      this.scales.scaleMultipleYAxes();
      gl.minY = lowestYInAllSeries;
    } else {
      this.scales.setYScaleForIndex(0, gl.minY, gl.maxY);
      gl.minY = gl.yAxisScale[0].niceMin;
      gl.maxY = gl.yAxisScale[0].niceMax;
      gl.minYArr[0] = gl.minY;
      gl.maxYArr[0] = gl.maxY;
    }
    gl.barGroups = [];
    gl.lineGroups = [];
    gl.areaGroups = [];
    cnf.series.forEach((s2) => {
      const _s = (
        /** @type {any} */
        s2
      );
      const type = _s.type || cnf.chart.type;
      switch (type) {
        case "bar":
        case "column":
          gl.barGroups.push(_s.group);
          break;
        case "line":
          gl.lineGroups.push(_s.group);
          break;
        case "area":
          gl.areaGroups.push(_s.group);
          break;
      }
    });
    gl.barGroups = gl.barGroups.filter((v2, i2, a2) => a2.indexOf(v2) === i2);
    gl.lineGroups = gl.lineGroups.filter((v2, i2, a2) => a2.indexOf(v2) === i2);
    gl.areaGroups = gl.areaGroups.filter((v2, i2, a2) => a2.indexOf(v2) === i2);
    return {
      minY: gl.minY,
      maxY: gl.maxY,
      minYArr: gl.minYArr,
      maxYArr: gl.maxYArr,
      yAxisScale: gl.yAxisScale
    };
  }
  setXRange() {
    const gl = this.w.globals;
    const cnf = this.w.config;
    const isXNumeric = cnf.xaxis.type === "numeric" || cnf.xaxis.type === "datetime" || cnf.xaxis.type === "category" && !this.w.axisFlags.noLabelsProvided || this.w.axisFlags.noLabelsProvided || this.w.axisFlags.isXNumeric;
    const getInitialMinXMaxX = () => {
      var _a;
      let minX = gl.minX;
      let maxX = gl.maxX;
      for (let i2 = 0; i2 < this.w.seriesData.series.length; i2++) {
        const lbls = (
          /** @type {any} */
          this.w.labelData.labels[i2]
        );
        if (!lbls) continue;
        const pe = (_a = this.w.seriesData._parsedExtrema) == null ? void 0 : _a[i2];
        if (pe && pe.xNumeric && pe.xref === lbls && pe.len === lbls.length) {
          if (pe.maxX > maxX) maxX = pe.maxX;
          if (pe.minX < minX) minX = pe.minX;
          continue;
        }
        for (let j = 0; j < lbls.length; j++) {
          const v2 = lbls[j];
          if (v2 !== null && typeof v2 === "number" && v2 === v2) {
            if (v2 > maxX) maxX = v2;
            if (v2 < minX) minX = v2;
          }
        }
      }
      gl.maxX = maxX;
      gl.initialMaxX = maxX;
      gl.minX = minX;
      gl.initialMinX = minX;
    };
    if (this.w.axisFlags.isXNumeric) {
      getInitialMinXMaxX();
    }
    if (this.w.axisFlags.noLabelsProvided) {
      if (cnf.xaxis.categories.length === 0) {
        gl.maxX = /** @type {any} */
        this.w.labelData.labels[this.w.labelData.labels.length - 1];
        gl.initialMaxX = /** @type {any} */
        this.w.labelData.labels[this.w.labelData.labels.length - 1];
        gl.minX = 1;
        gl.initialMinX = 1;
      }
    }
    if (this.w.axisFlags.isXNumeric || this.w.axisFlags.noLabelsProvided || this.w.axisFlags.dataFormatXNumeric) {
      let ticks = 10;
      if (cnf.xaxis.tickAmount === void 0) {
        ticks = Math.round(gl.svgWidth / 150);
        if (cnf.xaxis.type === "numeric" && gl.dataPoints < 30) {
          ticks = gl.dataPoints - 1;
        }
        if (ticks > gl.dataPoints && gl.dataPoints !== 0) {
          ticks = gl.dataPoints - 1;
        }
      } else if (cnf.xaxis.tickAmount === "dataPoints") {
        if (this.w.seriesData.series.length > 1) {
          ticks = this.w.seriesData.series[gl.maxValsInArrayIndex].length - 1;
        }
        if (this.w.axisFlags.isXNumeric) {
          const diff = Math.round(gl.maxX - gl.minX);
          if (diff < 30) {
            ticks = diff;
          }
        }
      } else {
        ticks = cnf.xaxis.tickAmount;
      }
      gl.xTickAmount = ticks;
      if (cnf.xaxis.max !== void 0 && typeof cnf.xaxis.max === "number") {
        gl.maxX = cnf.xaxis.max;
      }
      if (cnf.xaxis.min !== void 0 && typeof cnf.xaxis.min === "number") {
        gl.minX = cnf.xaxis.min;
      }
      if (cnf.xaxis.range !== void 0) {
        gl.minX = gl.maxX - cnf.xaxis.range;
      }
      if (gl.minX !== Number.MAX_VALUE && gl.maxX !== -Number.MAX_VALUE) {
        if (cnf.xaxis.convertedCatToNumeric && !this.w.axisFlags.dataFormatXNumeric) {
          const catScale = [];
          for (let i2 = gl.minX - 1; i2 < gl.maxX; i2++) {
            catScale.push(i2 + 1);
          }
          gl.xAxisScale = {
            result: catScale,
            niceMin: catScale[0],
            niceMax: catScale[catScale.length - 1]
          };
        } else {
          gl.xAxisScale = this.scales.setXScale(gl.minX, gl.maxX);
        }
      } else {
        gl.xAxisScale = this.scales.linearScale(
          0,
          ticks,
          ticks,
          0,
          cnf.xaxis.stepSize
        );
        if (this.w.axisFlags.noLabelsProvided && this.w.labelData.labels.length > 0) {
          gl.xAxisScale = this.scales.linearScale(
            1,
            this.w.labelData.labels.length,
            ticks - 1,
            0,
            cnf.xaxis.stepSize
          );
          this.w.seriesData.seriesX = /** @type {any} */
          this.w.labelData.labels.slice();
        }
      }
      if (isXNumeric) {
        this.w.labelData.labels = /** @type {any} */
        gl.xAxisScale.result.slice();
      }
    }
    if (gl.isBarHorizontal && this.w.labelData.labels.length) {
      gl.xTickAmount = this.w.labelData.labels.length;
    }
    this._handleSingleDataPoint();
    this._getMinXDiff();
    return {
      minX: gl.minX,
      maxX: gl.maxX
    };
  }
  setZRange() {
    const gl = this.w.globals;
    if (!this.w.axisFlags.isDataXYZ) return;
    for (let i2 = 0; i2 < this.w.seriesData.series.length; i2++) {
      if (typeof this.w.seriesData.seriesZ[i2] !== "undefined") {
        for (let j = 0; j < this.w.seriesData.seriesZ[i2].length; j++) {
          if (this.w.seriesData.seriesZ[i2][j] !== null && Utils$1.isNumber(this.w.seriesData.seriesZ[i2][j])) {
            gl.maxZ = Math.max(gl.maxZ, this.w.seriesData.seriesZ[i2][j]);
            gl.minZ = Math.min(gl.minZ, this.w.seriesData.seriesZ[i2][j]);
          }
        }
      }
    }
  }
  _handleSingleDataPoint() {
    const gl = this.w.globals;
    const cnf = this.w.config;
    if (gl.minX === gl.maxX) {
      const datetimeObj = new DateTime(this.w);
      if (cnf.xaxis.type === "datetime") {
        const newMinX = datetimeObj.getDate(gl.minX);
        if (cnf.xaxis.labels.datetimeUTC) {
          newMinX.setUTCDate(newMinX.getUTCDate() - 2);
        } else {
          newMinX.setDate(newMinX.getDate() - 2);
        }
        gl.minX = new Date(newMinX).getTime();
        const newMaxX = datetimeObj.getDate(gl.maxX);
        if (cnf.xaxis.labels.datetimeUTC) {
          newMaxX.setUTCDate(newMaxX.getUTCDate() + 2);
        } else {
          newMaxX.setDate(newMaxX.getDate() + 2);
        }
        gl.maxX = new Date(newMaxX).getTime();
      } else if (cnf.xaxis.type === "numeric" || cnf.xaxis.type === "category" && !this.w.axisFlags.noLabelsProvided) {
        gl.minX = gl.minX - 2;
        gl.initialMinX = gl.minX;
        gl.maxX = gl.maxX + 2;
        gl.initialMaxX = gl.maxX;
      }
    }
  }
  _getMinXDiff() {
    const gl = this.w.globals;
    if (this.w.axisFlags.isXNumeric) {
      this.w.seriesData.seriesX.forEach((sX, si) => {
        var _a;
        if (sX.length) {
          if (sX.length === 1) {
            sX.push(
              this.w.seriesData.seriesX[gl.maxValsInArrayIndex][this.w.seriesData.seriesX[gl.maxValsInArrayIndex].length - 1]
            );
          }
          const pe = (_a = this.w.seriesData._parsedExtrema) == null ? void 0 : _a[si];
          if (pe && pe.xNumeric && pe.xSorted && pe.xref === sX && pe.len === sX.length) {
            if (pe.minXDiff < gl.minXDiff) gl.minXDiff = pe.minXDiff;
            if (gl.dataPoints === 1 || gl.minXDiff === Number.MAX_VALUE) {
              gl.minXDiff = 0.5;
            }
            return;
          }
          let presorted = true;
          let minDiff = gl.minXDiff;
          for (let j = 1; j < sX.length; j++) {
            const d = sX[j] - sX[j - 1];
            if (d > 0) {
              if (d < minDiff) minDiff = d;
            } else if (d < 0) {
              presorted = false;
              break;
            }
          }
          if (presorted) {
            gl.minXDiff = minDiff;
            if (gl.dataPoints === 1 || gl.minXDiff === Number.MAX_VALUE) {
              gl.minXDiff = 0.5;
            }
            return;
          }
          const seriesX = sX.slice();
          seriesX.sort((a2, b2) => a2 - b2);
          seriesX.forEach((s2, j) => {
            if (j > 0) {
              const xDiff = s2 - seriesX[j - 1];
              if (xDiff > 0) {
                gl.minXDiff = Math.min(xDiff, gl.minXDiff);
              }
            }
          });
          if (gl.dataPoints === 1 || gl.minXDiff === Number.MAX_VALUE) {
            gl.minXDiff = 0.5;
          }
        }
      });
    }
  }
  _setStackedMinMax() {
    const gl = this.w.globals;
    if (!this.w.seriesData.series.length) return;
    let seriesGroups = this.w.labelData.seriesGroups;
    if (!seriesGroups.length) {
      seriesGroups = [this.w.seriesData.seriesNames.map((name2) => name2)];
    }
    const stackedPoss = {};
    const stackedNegs = {};
    seriesGroups.forEach((group) => {
      stackedPoss[group] = [];
      stackedNegs[group] = [];
      const indicesOfSeriesInGroup = this.w.config.series.map(
        (serie, si) => group.indexOf(this.w.seriesData.seriesNames[si]) > -1 ? si : null
      ).filter((f) => f !== null);
      indicesOfSeriesInGroup.forEach((i2) => {
        var _a, _b, _c, _d;
        for (let j = 0; j < this.w.seriesData.series[gl.maxValsInArrayIndex].length; j++) {
          if (typeof stackedPoss[group][j] === "undefined") {
            stackedPoss[group][j] = 0;
            stackedNegs[group][j] = 0;
          }
          const stackSeries = this.w.config.chart.stacked && !gl.comboCharts || this.w.config.chart.stacked && gl.comboCharts && (!this.w.config.chart.stackOnlyBar || /** @type {Record<string,any>} */
          ((_b = (_a = this.w.config.series) == null ? void 0 : _a[i2]) == null ? void 0 : _b.type) === "bar" || /** @type {Record<string,any>} */
          ((_d = (_c = this.w.config.series) == null ? void 0 : _c[i2]) == null ? void 0 : _d.type) === "column");
          if (stackSeries) {
            if (this.w.seriesData.series[i2][j] !== null && Utils$1.isNumber(this.w.seriesData.series[i2][j])) {
              this.w.seriesData.series[i2][j] > 0 ? stackedPoss[group][j] += parseFloat(String(this.w.seriesData.series[i2][j])) + 1e-4 : stackedNegs[group][j] += parseFloat(
                String(this.w.seriesData.series[i2][j])
              );
            }
          }
        }
      });
    });
    Object.entries(stackedPoss).forEach(([key]) => {
      stackedPoss[key].forEach(
        (_, stgi) => {
          gl.maxY = Math.max(gl.maxY, stackedPoss[key][stgi]);
          gl.minY = Math.min(gl.minY, stackedNegs[key][stgi]);
        }
      );
    });
  }
}
function getThemePalettes() {
  return {
    // All colours pass WCAG 1.4.11 non-text contrast (≥ 3:1) against the
    // default light (#fff) and dark (#293450) theme backgrounds.
    palette1: ["#008FFB", "#00A86F", "#CA8501", "#FF4560", "#846DD5"],
    palette2: ["#6978CB", "#039DE2", "#49A84D", "#B39105", "#D68000"],
    palette3: ["#209FCC", "#648291", "#D4526E", "#0FA783", "#A19285"],
    palette4: ["#2FA59D", "#73A20B", "#099DE1", "#FD5D5D", "#648291"],
    palette5: ["#2B908F", "#F56566", "#2EAB16", "#FA4443", "#1EA2BD"],
    palette6: ["#449DD1", "#F86624", "#EA3A4A", "#9C63D1", "#899E2A"],
    palette7: ["#DF475B", "#1B998B", "#7E75B7", "#F46036", "#B1911B"],
    palette8: ["#9C63D1", "#F86624", "#B38F04", "#EA3A4A", "#2FA2B3"],
    palette9: ["#98776F", "#A19285", "#A8705E", "#BA6560", "#A0927F"],
    palette10: ["#C91EFF", "#A94BFD", "#6C6AFE", "#2983FF", "#009ED8"],
    // CVD-safe palettes (Wong 2011 / IBM design)
    cvdDeuteranopia: [
      "#0072B2",
      "#E69F00",
      "#56B4E9",
      "#009E73",
      "#F0E442",
      "#D55E00",
      "#CC79A7"
    ],
    cvdProtanopia: [
      "#0077BB",
      "#EE7733",
      "#009988",
      "#EE3377",
      "#BBBBBB",
      "#33BBEE",
      "#CC3311"
    ],
    cvdTritanopia: [
      "#CC3311",
      "#009988",
      "#EE7733",
      "#0077BB",
      "#EE3377",
      "#BBBBBB",
      "#33BBEE"
    ],
    highContrast: [
      "#005A9C",
      "#C00000",
      "#007A33",
      "#6C3483",
      "#7B3F00",
      "#0097A7",
      "#4A235A"
    ]
  };
}
class YAxis {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {any} [elgrid]
   */
  constructor(w2, { theme = null, timeScale = null } = {}, elgrid) {
    this.w = w2;
    this.elgrid = elgrid;
    this.xaxisFontSize = w2.config.xaxis.labels.style.fontSize;
    this.axisFontFamily = w2.config.xaxis.labels.style.fontFamily;
    this.xaxisForeColors = w2.config.xaxis.labels.style.colors;
    this.isCategoryBarHorizontal = w2.config.chart.type === "bar" && w2.config.plotOptions.bar.horizontal;
    this.xAxisoffX = w2.config.xaxis.position === "bottom" ? w2.layout.gridHeight : 0;
    this.drawnLabels = [];
    this.axesUtils = new AxesUtils(w2, { theme, timeScale });
  }
  /**
   * @param {number} realIndex
   */
  drawYaxis(realIndex) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const yaxisStyle = w2.config.yaxis[realIndex].labels.style;
    const {
      fontSize: yaxisFontSize,
      fontFamily: yaxisFontFamily,
      fontWeight: yaxisFontWeight
    } = yaxisStyle;
    const elYaxis = graphics.group({
      class: "apexcharts-yaxis",
      rel: realIndex,
      transform: `translate(${w2.globals.translateYAxisX[realIndex]}, 0)`
    });
    if (this.axesUtils.isYAxisHidden(realIndex)) return elYaxis;
    const elYaxisTexts = graphics.group({ class: "apexcharts-yaxis-texts-g" });
    elYaxis.add(elYaxisTexts);
    const tickAmount = w2.globals.yAxisScale[realIndex].result.length - 1;
    const labelsDivider = w2.layout.gridHeight / tickAmount;
    const lbFormatter = w2.formatters.yLabelFormatters[realIndex];
    const labels = this.axesUtils.checkForReversedLabels(
      realIndex,
      w2.globals.yAxisScale[realIndex].result.slice()
    );
    let labelStep = 1;
    if (w2.config.chart.type === "heatmap" && !w2.config.yaxis[realIndex].labels.formatter) {
      const fs = parseInt(yaxisFontSize, 10) || 11;
      const maxLabels = Math.max(1, Math.floor(w2.layout.gridHeight / (fs * 1.4)));
      const count = tickAmount + 1;
      if (count > maxLabels) labelStep = Math.ceil(count / maxLabels);
    }
    if (w2.config.yaxis[realIndex].labels.show) {
      let lY = w2.layout.translateY + w2.config.yaxis[realIndex].labels.offsetY;
      if (w2.globals.isBarHorizontal) lY = 0;
      else if (w2.config.chart.type === "heatmap") lY -= labelsDivider / 2;
      lY += parseInt(yaxisFontSize, 10) / 3;
      let firstLabel = null;
      for (let i2 = tickAmount; i2 >= 0; i2--) {
        const thinned = labelStep > 1 && i2 % labelStep !== 0;
        const val = thinned ? "" : lbFormatter(labels[i2], i2, w2);
        let xPad = w2.config.yaxis[realIndex].labels.padding;
        if (w2.config.yaxis[realIndex].opposite && w2.config.yaxis.length !== 0)
          xPad *= -1;
        const textAnchor = this.getTextAnchor(
          w2.config.yaxis[realIndex].labels.align,
          w2.config.yaxis[realIndex].opposite
        );
        const yColors = this.axesUtils.getYAxisForeColor(
          yaxisStyle.colors,
          realIndex
        );
        const foreColor = Array.isArray(yColors) ? yColors[i2] : yColors;
        const existingYLabels = Array.from(
          w2.dom.baseEl.querySelectorAll(
            `.apexcharts-yaxis[rel='${realIndex}'] .apexcharts-yaxis-label tspan`
          )
        ).map((label2) => label2.textContent);
        const label = graphics.drawText({
          x: xPad,
          y: lY,
          text: existingYLabels.includes(val) && !w2.config.yaxis[realIndex].labels.showDuplicates ? "" : val,
          textAnchor,
          fontSize: yaxisFontSize,
          fontFamily: yaxisFontFamily,
          fontWeight: yaxisFontWeight,
          maxWidth: w2.config.yaxis[realIndex].labels.maxWidth,
          foreColor,
          isPlainText: false,
          cssClass: `apexcharts-yaxis-label ${yaxisStyle.cssClass}`
        });
        elYaxisTexts.add(label);
        this.addTooltip(label, val);
        if (firstLabel === null) {
          firstLabel = label;
        }
        if (w2.config.yaxis[realIndex].labels.rotate !== 0) {
          this.rotateLabel(
            graphics,
            label,
            firstLabel,
            w2.config.yaxis[realIndex].labels.rotate
          );
        }
        lY += labelsDivider;
      }
    }
    this.addYAxisTitle(graphics, elYaxis, realIndex);
    this.addAxisBorder(graphics, elYaxis, realIndex, tickAmount, labelsDivider);
    return elYaxis;
  }
  /**
   * @param {string} align
   * @param {boolean} opposite
   */
  getTextAnchor(align, opposite) {
    if (align === "left") return "start";
    if (align === "center") return "middle";
    if (align === "right") return "end";
    return opposite ? "start" : "end";
  }
  /**
   * @param {any} label
   * @param {any} val
   */
  addTooltip(label, val) {
    const elTooltipTitle = BrowserAPIs.createElementNS(SVGNS$2, "title");
    elTooltipTitle.textContent = Array.isArray(val) ? val.join(" ") : val;
    label.node.appendChild(elTooltipTitle);
  }
  /**
   * @param {import('../Graphics').default} graphics
   * @param {any} label
   * @param {any} firstLabel
   * @param {number} rotate
   */
  rotateLabel(graphics, label, firstLabel, rotate) {
    const firstLabelCenter = graphics.rotateAroundCenter(firstLabel.node);
    const labelCenter = graphics.rotateAroundCenter(label.node);
    label.node.setAttribute(
      "transform",
      `rotate(${rotate} ${firstLabelCenter.x} ${labelCenter.y})`
    );
  }
  /**
   * @param {import('../Graphics').default} graphics
   * @param {any} elYaxis
   * @param {number} realIndex
   */
  addYAxisTitle(graphics, elYaxis, realIndex) {
    const w2 = this.w;
    if (w2.config.yaxis[realIndex].title.text !== void 0) {
      const elYaxisTitle = graphics.group({ class: "apexcharts-yaxis-title" });
      const x = w2.config.yaxis[realIndex].opposite ? w2.globals.translateYAxisX[realIndex] : 0;
      const elYAxisTitleText = graphics.drawText({
        x,
        y: w2.layout.gridHeight / 2 + w2.layout.translateY + w2.config.yaxis[realIndex].title.offsetY,
        text: w2.config.yaxis[realIndex].title.text,
        textAnchor: "end",
        foreColor: w2.config.yaxis[realIndex].title.style.color,
        fontSize: w2.config.yaxis[realIndex].title.style.fontSize,
        fontWeight: w2.config.yaxis[realIndex].title.style.fontWeight,
        fontFamily: w2.config.yaxis[realIndex].title.style.fontFamily,
        cssClass: `apexcharts-yaxis-title-text ${w2.config.yaxis[realIndex].title.style.cssClass}`
      });
      elYaxisTitle.add(elYAxisTitleText);
      elYaxis.add(elYaxisTitle);
    }
  }
  /**
   * @param {import('../Graphics').default} graphics
   * @param {any} elYaxis
   * @param {number} realIndex
   * @param {number} tickAmount
   * @param {number} labelsDivider
   */
  addAxisBorder(graphics, elYaxis, realIndex, tickAmount, labelsDivider) {
    const w2 = this.w;
    const axisBorder = w2.config.yaxis[realIndex].axisBorder;
    let x = 31 + axisBorder.offsetX;
    if (w2.config.yaxis[realIndex].opposite) x = -31 - axisBorder.offsetX;
    if (axisBorder.show) {
      const elVerticalLine = graphics.drawLine(
        x,
        w2.layout.translateY + axisBorder.offsetY - 2,
        x,
        w2.layout.gridHeight + w2.layout.translateY + axisBorder.offsetY + 2,
        axisBorder.color,
        0,
        axisBorder.width
      );
      elYaxis.add(elVerticalLine);
    }
    if (w2.config.yaxis[realIndex].axisTicks.show) {
      this.axesUtils.drawYAxisTicks(
        x,
        tickAmount,
        axisBorder,
        w2.config.yaxis[realIndex].axisTicks,
        realIndex,
        labelsDivider,
        elYaxis
      );
    }
  }
  /**
   * @param {number} realIndex
   */
  drawYaxisInversed(realIndex) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const elXaxis = graphics.group({
      class: "apexcharts-xaxis apexcharts-yaxis-inversed"
    });
    const elXaxisTexts = graphics.group({
      class: "apexcharts-xaxis-texts-g",
      transform: `translate(${w2.layout.translateXAxisX}, ${w2.layout.translateXAxisY})`
    });
    elXaxis.add(elXaxisTexts);
    let tickAmount = w2.globals.yAxisScale[realIndex].result.length - 1;
    const labelsDivider = w2.layout.gridWidth / tickAmount + 0.1;
    let l2 = labelsDivider + w2.config.xaxis.labels.offsetX;
    const lbFormatter = w2.formatters.xLabelFormatter;
    let labels = this.axesUtils.checkForReversedLabels(
      realIndex,
      w2.globals.yAxisScale[realIndex].result.slice()
    );
    const timescaleLabels = w2.labelData.timescaleLabels;
    if (timescaleLabels.length > 0) {
      this.xaxisLabels = timescaleLabels.slice();
      labels = timescaleLabels.slice();
      tickAmount = labels.length;
    }
    if (w2.config.xaxis.labels.show) {
      for (let i2 = timescaleLabels.length ? 0 : tickAmount; timescaleLabels.length ? i2 < timescaleLabels.length : i2 >= 0; timescaleLabels.length ? i2++ : i2--) {
        let val = lbFormatter == null ? void 0 : lbFormatter(labels[i2], i2, w2);
        let x = w2.layout.gridWidth + w2.globals.padHorizontal - (l2 - labelsDivider + w2.config.xaxis.labels.offsetX);
        if (timescaleLabels.length) {
          const label = this.axesUtils.getLabel(
            labels,
            timescaleLabels,
            x,
            i2,
            this.drawnLabels,
            this.xaxisFontSize
          );
          x = label.x;
          val = label.text;
          this.drawnLabels.push(label.text);
          if (i2 === 0 && w2.globals.skipFirstTimelinelabel) val = "";
          if (i2 === labels.length - 1 && w2.globals.skipLastTimelinelabel)
            val = "";
        }
        const elTick = graphics.drawText({
          x,
          y: this.xAxisoffX + w2.config.xaxis.labels.offsetY + 30 - (w2.config.xaxis.position === "top" ? w2.layout.xAxisHeight + w2.config.xaxis.axisTicks.height - 2 : 0),
          text: val,
          textAnchor: "middle",
          foreColor: Array.isArray(this.xaxisForeColors) ? this.xaxisForeColors[realIndex] : this.xaxisForeColors,
          fontSize: this.xaxisFontSize,
          fontFamily: this.axisFontFamily,
          fontWeight: w2.config.xaxis.labels.style.fontWeight,
          isPlainText: false,
          cssClass: `apexcharts-xaxis-label ${w2.config.xaxis.labels.style.cssClass}`
        });
        elXaxisTexts.add(elTick);
        this.addTooltip(elTick, val);
        l2 += labelsDivider;
      }
    }
    this.inversedYAxisTitleText(elXaxis);
    this.inversedYAxisBorder(elXaxis);
    return elXaxis;
  }
  /**
   * @param {any} parent
   */
  inversedYAxisBorder(parent) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const axisBorder = w2.config.xaxis.axisBorder;
    if (axisBorder.show) {
      let lineCorrection = 0;
      if (w2.config.chart.type === "bar" && w2.axisFlags.isXNumeric)
        lineCorrection -= 15;
      const elHorzLine = graphics.drawLine(
        w2.globals.padHorizontal + lineCorrection + axisBorder.offsetX,
        this.xAxisoffX,
        w2.layout.gridWidth,
        this.xAxisoffX,
        axisBorder.color,
        0,
        axisBorder.height
      );
      if (this.elgrid && this.elgrid.elGridBorders && w2.config.grid.show) {
        this.elgrid.elGridBorders.add(elHorzLine);
      } else {
        parent.add(elHorzLine);
      }
    }
  }
  /**
   * @param {any} parent
   */
  inversedYAxisTitleText(parent) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    if (w2.config.xaxis.title.text !== void 0) {
      const elYaxisTitle = graphics.group({
        class: "apexcharts-xaxis-title apexcharts-yaxis-title-inversed"
      });
      const elYAxisTitleText = graphics.drawText({
        x: w2.layout.gridWidth / 2 + w2.config.xaxis.title.offsetX,
        y: this.xAxisoffX + parseFloat(this.xaxisFontSize) + parseFloat(w2.config.xaxis.title.style.fontSize) + w2.config.xaxis.title.offsetY + 20,
        text: w2.config.xaxis.title.text,
        textAnchor: "middle",
        fontSize: w2.config.xaxis.title.style.fontSize,
        fontFamily: w2.config.xaxis.title.style.fontFamily,
        fontWeight: w2.config.xaxis.title.style.fontWeight,
        foreColor: w2.config.xaxis.title.style.color,
        cssClass: `apexcharts-xaxis-title-text ${w2.config.xaxis.title.style.cssClass}`
      });
      elYaxisTitle.add(elYAxisTitleText);
      parent.add(elYaxisTitle);
    }
  }
  /**
   * @param {number} realIndex
   * @param {boolean} yAxisOpposite
   */
  yAxisTitleRotate(realIndex, yAxisOpposite) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const elYAxisLabelsWrap = w2.dom.baseEl.querySelector(
      `.apexcharts-yaxis[rel='${realIndex}'] .apexcharts-yaxis-texts-g`
    );
    const yAxisLabelsCoord = elYAxisLabelsWrap ? elYAxisLabelsWrap.getBoundingClientRect() : { width: 0, height: 0 };
    const yAxisTitle = w2.dom.baseEl.querySelector(
      `.apexcharts-yaxis[rel='${realIndex}'] .apexcharts-yaxis-title text`
    );
    const yAxisTitleCoord = yAxisTitle ? yAxisTitle.getBoundingClientRect() : { width: 0, height: 0 };
    if (yAxisTitle) {
      const x = this.xPaddingForYAxisTitle(
        realIndex,
        yAxisLabelsCoord,
        yAxisTitleCoord,
        yAxisOpposite
      );
      yAxisTitle.setAttribute("x", String(x.xPos - (yAxisOpposite ? 10 : 0)));
      const titleRotatingCenter = graphics.rotateAroundCenter(yAxisTitle);
      yAxisTitle.setAttribute(
        "transform",
        `rotate(${yAxisOpposite ? w2.config.yaxis[realIndex].title.rotate * -1 : w2.config.yaxis[realIndex].title.rotate} ${titleRotatingCenter.x} ${titleRotatingCenter.y})`
      );
    }
  }
  /**
   * @param {number} realIndex
   * @param {{width: number, height: number}} yAxisLabelsCoord
   * @param {{width: number, height: number}} yAxisTitleCoord
   * @param {boolean} yAxisOpposite
   */
  xPaddingForYAxisTitle(realIndex, yAxisLabelsCoord, yAxisTitleCoord, yAxisOpposite) {
    const w2 = this.w;
    let x = 0;
    let padd = 10;
    if (w2.config.yaxis[realIndex].title.text === void 0 || realIndex < 0) {
      return { xPos: x, padd: 0 };
    }
    if (yAxisOpposite) {
      x = yAxisLabelsCoord.width + w2.config.yaxis[realIndex].title.offsetX + yAxisTitleCoord.width / 2 + padd / 2;
    } else {
      x = yAxisLabelsCoord.width * -1 + w2.config.yaxis[realIndex].title.offsetX + padd / 2 + yAxisTitleCoord.width / 2;
      if (w2.globals.isBarHorizontal) {
        padd = 25;
        x = yAxisLabelsCoord.width * -1 - w2.config.yaxis[realIndex].title.offsetX - padd;
      }
    }
    return { xPos: x, padd };
  }
  /**
   * @param {Array<{width: number, height: number}>} yaxisLabelCoords
   * @param {Array<{width: number, height: number}>} yTitleCoords
   */
  setYAxisXPosition(yaxisLabelCoords, yTitleCoords) {
    const w2 = this.w;
    let xLeft = 0;
    let xRight = 0;
    let leftOffsetX = 18;
    let rightOffsetX = 1;
    if (w2.config.yaxis.length > 1) this.multipleYs = true;
    w2.config.yaxis.forEach((yaxe, index) => {
      const shouldNotDrawAxis = w2.globals.ignoreYAxisIndexes.includes(index) || !yaxe.show || yaxe.floating || yaxisLabelCoords[index].width === 0;
      const axisWidth = yaxisLabelCoords[index].width + yTitleCoords[index].width;
      if (!yaxe.opposite) {
        xLeft = w2.layout.translateX - leftOffsetX;
        if (!shouldNotDrawAxis) leftOffsetX += axisWidth + 20;
        w2.globals.translateYAxisX[index] = xLeft + yaxe.labels.offsetX;
      } else {
        if (w2.globals.isBarHorizontal) {
          xRight = w2.layout.gridWidth + w2.layout.translateX - 1;
          w2.globals.translateYAxisX[index] = xRight - yaxe.labels.offsetX;
        } else {
          xRight = w2.layout.gridWidth + w2.layout.translateX + rightOffsetX;
          if (!shouldNotDrawAxis) rightOffsetX += axisWidth + 20;
          w2.globals.translateYAxisX[index] = xRight - yaxe.labels.offsetX + 20;
        }
      }
    });
  }
  setYAxisTextAlignments() {
    const w2 = this.w;
    const yaxis = Array.from(
      w2.dom.baseEl.getElementsByClassName("apexcharts-yaxis")
    );
    yaxis.forEach((y, index) => {
      const yaxe = w2.config.yaxis[index];
      if (yaxe && !yaxe.floating && yaxe.labels.align !== void 0) {
        const yAxisInner = w2.dom.baseEl.querySelector(
          `.apexcharts-yaxis[rel='${index}'] .apexcharts-yaxis-texts-g`
        );
        const yAxisTexts = Array.from(
          w2.dom.baseEl.querySelectorAll(
            `.apexcharts-yaxis[rel='${index}'] .apexcharts-yaxis-label`
          )
        );
        const rect = (
          /** @type {Element} */
          yAxisInner.getBoundingClientRect()
        );
        yAxisTexts.forEach((label) => {
          label.setAttribute("text-anchor", yaxe.labels.align);
        });
        if (yaxe.labels.align === "left" && !yaxe.opposite) {
          yAxisInner.setAttribute(
            "transform",
            `translate(-${rect.width}, 0)`
          );
        } else if (yaxe.labels.align === "center") {
          yAxisInner.setAttribute(
            "transform",
            `translate(${rect.width / 2 * (!yaxe.opposite ? -1 : 1)}, 0)`
          );
        } else if (yaxe.labels.align === "right" && yaxe.opposite) {
          yAxisInner.setAttribute(
            "transform",
            `translate(${rect.width}, 0)`
          );
        }
      }
    });
  }
}
class Events {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.documentEvent = this.documentEvent.bind(this);
  }
  /**
   * @param {string} name
   * @param {Function} handler
   */
  addEventListener(name2, handler) {
    const w2 = this.w;
    if (Object.prototype.hasOwnProperty.call(w2.globals.events, name2)) {
      w2.globals.events[name2].push(handler);
    } else {
      w2.globals.events[name2] = [handler];
    }
  }
  /**
   * @param {string} name
   * @param {Function} handler
   */
  removeEventListener(name2, handler) {
    const w2 = this.w;
    if (!Object.prototype.hasOwnProperty.call(w2.globals.events, name2)) {
      return;
    }
    const index = (
      /** @type {Record<string,any>} */
      w2.globals.events[name2].indexOf(handler)
    );
    if (index !== -1) {
      w2.globals.events[name2].splice(
        index,
        1
      );
    }
  }
  /**
   * @param {string} name
   * @param {any[]} args
   */
  fireEvent(name2, args) {
    const w2 = this.w;
    if (!Object.prototype.hasOwnProperty.call(w2.globals.events, name2)) {
      return;
    }
    if (!args || !args.length) {
      args = [];
    }
    const evs = (
      /** @type {Record<string,any>} */
      w2.globals.events[name2]
    );
    const l2 = evs.length;
    for (let i2 = 0; i2 < l2; i2++) {
      evs[i2].apply(null, args);
    }
  }
  setupEventHandlers() {
    const w2 = this.w;
    const me = this.ctx;
    const clickableArea = w2.dom.baseEl.querySelector(w2.globals.chartClass);
    this.ctx.eventList.forEach((event) => {
      clickableArea == null ? void 0 : clickableArea.addEventListener(
        event,
        (e2) => {
          const capturedSeriesIndex = e2.target.getAttribute("i") === null && w2.interact.capturedSeriesIndex !== -1 ? w2.interact.capturedSeriesIndex : e2.target.getAttribute("i");
          const capturedDataPointIndex = e2.target.getAttribute("j") === null && w2.interact.capturedDataPointIndex !== -1 ? w2.interact.capturedDataPointIndex : e2.target.getAttribute("j");
          const opts = Object.assign({}, w2, {
            seriesIndex: w2.globals.axisCharts ? capturedSeriesIndex : 0,
            dataPointIndex: capturedDataPointIndex
          });
          if (e2.type === "keydown") {
            if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.keyboard.enabled) {
              if (me.ctx.keyboardNavigation) {
                me.ctx.keyboardNavigation.handleKey(e2);
              }
              if (typeof w2.config.chart.events.keyDown === "function") {
                w2.config.chart.events.keyDown(e2, me, opts);
              }
              me.ctx.events.fireEvent("keydown", [e2, me, opts]);
            }
          } else if (e2.type === "keyup") {
            if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.keyboard.enabled) {
              if (typeof w2.config.chart.events.keyUp === "function") {
                w2.config.chart.events.keyUp(e2, me, opts);
              }
              me.ctx.events.fireEvent("keyup", [e2, me, opts]);
            }
          } else if (e2.type === "mousemove" || e2.type === "touchmove") {
            if (typeof w2.config.chart.events.mouseMove === "function") {
              w2.config.chart.events.mouseMove(e2, me, opts);
            }
          } else if (e2.type === "mouseleave" || e2.type === "touchleave") {
            if (typeof w2.config.chart.events.mouseLeave === "function") {
              w2.config.chart.events.mouseLeave(e2, me, opts);
            }
          } else if (e2.type === "mouseup" && e2.which === 1 || e2.type === "touchend") {
            if (typeof w2.config.chart.events.click === "function") {
              w2.config.chart.events.click(e2, me, opts);
            }
            me.ctx.events.fireEvent("click", [e2, me, opts]);
          }
        },
        { capture: false, passive: true }
      );
    });
    this.ctx.eventList.forEach((event) => {
      w2.dom.baseEl.addEventListener(event, this.documentEvent, {
        passive: true
      });
    });
    this.ctx.core.setupBrushHandler();
  }
  /**
   * @param {any} e
   */
  documentEvent(e2) {
    const w2 = this.w;
    const target = e2.target.className;
    if (e2.type === "click") {
      const elMenu = w2.dom.baseEl.querySelector(".apexcharts-menu");
      if (elMenu && elMenu.classList.contains("apexcharts-menu-open") && target !== "apexcharts-menu-icon") {
        elMenu.classList.remove("apexcharts-menu-open");
      }
    }
    w2.interact.clientX = e2.type === "touchmove" ? e2.touches[0].clientX : e2.clientX;
    w2.interact.clientY = e2.type === "touchmove" ? e2.touches[0].clientY : e2.clientY;
  }
}
class Localization {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
  }
  /**
   * @param {string} localeName
   */
  setCurrentLocaleValues(localeName) {
    let locales = this.w.config.chart.locales;
    const globalApex = Environment.getApex();
    if (globalApex.chart && globalApex.chart.locales && globalApex.chart.locales.length > 0) {
      locales = this.w.config.chart.locales.concat(globalApex.chart.locales);
    }
    const selectedLocale = locales.filter(
      (c2) => c2.name === localeName
    )[0];
    if (selectedLocale) {
      const ret = Utils$1.extend(en, selectedLocale);
      this.w.globals.locale = ret.options;
    } else {
      throw new Error(
        "Wrong locale name provided. Please make sure you set the correct locale name in options"
      );
    }
  }
}
class Axes {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
  }
  /**
   * @param {string} type
   * @param {any} elgrid
   */
  drawAxis(type, elgrid) {
    const gl = this.w.globals;
    const cnf = this.w.config;
    const xAxis = new XAxis(this.w, this.ctx, elgrid);
    const yAxis = new YAxis(this.w, { theme: this.ctx.theme, timeScale: this.ctx.timeScale }, elgrid);
    if (gl.axisCharts && type !== "radar") {
      let elXaxis, elYaxis;
      if (gl.isBarHorizontal) {
        elYaxis = yAxis.drawYaxisInversed(0);
        elXaxis = xAxis.drawXaxisInversed(0);
        this.w.dom.elGraphical.add(elXaxis);
        this.w.dom.elGraphical.add(elYaxis);
      } else {
        elXaxis = xAxis.drawXaxis();
        this.w.dom.elGraphical.add(elXaxis);
        cnf.yaxis.map((yaxe, index) => {
          if (gl.ignoreYAxisIndexes.indexOf(index) === -1) {
            elYaxis = yAxis.drawYaxis(index);
            this.w.dom.Paper.add(elYaxis);
            if (this.w.config.grid.position === "back") {
              const inner = this.w.dom.Paper.children()[1];
              if (inner) {
                inner.remove();
                this.w.dom.Paper.add(inner);
              }
            }
          }
        });
      }
    }
  }
}
class Crosshairs {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
  }
  drawXCrosshairs() {
    const w2 = this.w;
    w2.dom.elGraphical.node.querySelectorAll(":scope > .apexcharts-xcrosshairs").forEach((el) => el.remove());
    const graphics = new Graphics(this.w);
    const filters = new Filters(this.w);
    const crosshairGradient = w2.config.xaxis.crosshairs.fill.gradient;
    const crosshairShadow = w2.config.xaxis.crosshairs.dropShadow;
    const fillType = w2.config.xaxis.crosshairs.fill.type;
    const gradientFrom = crosshairGradient.colorFrom;
    const gradientTo = crosshairGradient.colorTo;
    const opacityFrom = crosshairGradient.opacityFrom;
    const opacityTo = crosshairGradient.opacityTo;
    const stops = crosshairGradient.stops;
    const shadow = "none";
    const dropShadow = crosshairShadow.enabled;
    const shadowLeft = crosshairShadow.left;
    const shadowTop = crosshairShadow.top;
    const shadowBlur = crosshairShadow.blur;
    const shadowColor = crosshairShadow.color;
    const shadowOpacity = crosshairShadow.opacity;
    let xcrosshairsFill = w2.config.xaxis.crosshairs.fill.color;
    if (w2.config.xaxis.crosshairs.show) {
      if (fillType === "gradient") {
        xcrosshairsFill = graphics.drawGradient(
          "vertical",
          gradientFrom,
          gradientTo,
          opacityFrom,
          opacityTo,
          null,
          stops,
          []
        );
      }
      let xcrosshairs = w2.config.xaxis.crosshairs.width === 1 ? graphics.drawLine(0, 0, 0, 0) : graphics.drawRect();
      let gridHeight = w2.layout.gridHeight;
      if (!Utils$1.isNumber(gridHeight) || gridHeight < 0) {
        gridHeight = 0;
      }
      let crosshairsWidth = w2.config.xaxis.crosshairs.width;
      if (!Utils$1.isNumber(crosshairsWidth) || Number(crosshairsWidth) < 0) {
        crosshairsWidth = 0;
      }
      xcrosshairs.attr({
        class: "apexcharts-xcrosshairs",
        x: 0,
        y: 0,
        y2: gridHeight,
        width: crosshairsWidth,
        height: gridHeight,
        fill: xcrosshairsFill,
        filter: shadow,
        "fill-opacity": w2.config.xaxis.crosshairs.opacity,
        stroke: w2.config.xaxis.crosshairs.stroke.color,
        "stroke-width": w2.config.xaxis.crosshairs.stroke.width,
        "stroke-dasharray": w2.config.xaxis.crosshairs.stroke.dashArray
      });
      if (dropShadow) {
        xcrosshairs = filters.dropShadow(xcrosshairs, {
          left: shadowLeft,
          top: shadowTop,
          blur: shadowBlur,
          color: shadowColor,
          opacity: shadowOpacity
        });
      }
      w2.dom.elGraphical.add(xcrosshairs);
    }
  }
  drawYCrosshairs() {
    const w2 = this.w;
    w2.dom.elGraphical.node.querySelectorAll(
      ":scope > .apexcharts-ycrosshairs, :scope > .apexcharts-ycrosshairs-hidden"
    ).forEach((el) => el.remove());
    const graphics = new Graphics(this.w);
    const crosshair = (
      /** @type {any[]} */
      w2.config.yaxis[0].crosshairs
    );
    const offX = w2.globals.barPadForNumericAxis;
    if (
      /** @type {any[]} */
      w2.config.yaxis[0].crosshairs.show
    ) {
      const ycrosshairs = graphics.drawLine(
        -offX,
        0,
        w2.layout.gridWidth + offX,
        0,
        crosshair.stroke.color,
        crosshair.stroke.dashArray,
        crosshair.stroke.width
      );
      ycrosshairs.attr({
        class: "apexcharts-ycrosshairs"
      });
      w2.dom.elGraphical.add(ycrosshairs);
    }
    const ycrosshairsHidden = graphics.drawLine(
      -offX,
      0,
      w2.layout.gridWidth + offX,
      0,
      crosshair.stroke.color,
      0,
      0
    );
    ycrosshairsHidden.attr({
      class: "apexcharts-ycrosshairs-hidden"
    });
    w2.dom.elGraphical.add(ycrosshairsHidden);
  }
}
function mergeYaxisOverride(base, override) {
  if (!Utils$1.isObject(base) || !Utils$1.isObject(override)) {
    return override !== void 0 ? override : base;
  }
  const out = __spreadValues({}, base);
  for (const key of Object.keys(override)) {
    const v2 = override[key];
    if (v2 === void 0) continue;
    if (Utils$1.isObject(v2) && Utils$1.isObject(base[key])) {
      out[key] = mergeYaxisOverride(base[key], v2);
    } else {
      out[key] = v2;
    }
  }
  return out;
}
class Responsive {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this._activeBreakpoint = null;
  }
  // the opts parameter if not null has to be set overriding everything
  // as the opts is set by user externally
  /**
   * @param {object} opts
   */
  checkResponsiveConfig(opts) {
    const w2 = this.w;
    const cnf = w2.config;
    if (cnf.responsive.length === 0) return;
    const res = cnf.responsive.slice();
    res.sort(
      (a2, b2) => a2.breakpoint > b2.breakpoint ? 1 : b2.breakpoint > a2.breakpoint ? -1 : 0
    ).reverse();
    const config = new Config({});
    const iterateResponsiveOptions = (newOptions = {}) => {
      var _a;
      const largestBreakpoint = res[0].breakpoint;
      const width = Environment.isBrowser() ? window.innerWidth > 0 ? window.innerWidth : screen.width : 0;
      if (width > largestBreakpoint) {
        if (this._activeBreakpoint !== null) {
          if (!w2.globals.initialConfig) return;
          const initialConfig = Utils$1.clone(w2.globals.initialConfig);
          initialConfig.series = Utils$1.clone(w2.config.series);
          const options2 = CoreUtils.extendArrayProps(config, initialConfig, w2);
          newOptions = Utils$1.extend(options2, newOptions);
          this.overrideResponsiveOptions(newOptions);
          this._activeBreakpoint = null;
        }
      } else {
        for (let i2 = 0; i2 < res.length; i2++) {
          if (width < res[i2].breakpoint) {
            const originalUserYaxis = ((_a = res[i2].options) == null ? void 0 : _a.yaxis) ? Utils$1.clone(res[i2].options.yaxis) : null;
            newOptions = CoreUtils.extendArrayProps(config, res[i2].options, w2);
            newOptions = Utils$1.extend(w2.config, newOptions);
            if (Array.isArray(w2.config.yaxis) && originalUserYaxis) {
              const userYaxis = Array.isArray(originalUserYaxis) ? originalUserYaxis : [originalUserYaxis];
              newOptions = __spreadProps(__spreadValues({}, newOptions), {
                yaxis: w2.config.yaxis.map(
                  (baseAxis, idx) => mergeYaxisOverride(baseAxis, userYaxis[idx])
                )
              });
            }
            this.overrideResponsiveOptions(newOptions);
            this._activeBreakpoint = res[i2].breakpoint;
          }
        }
      }
    };
    if (opts) {
      let options2 = CoreUtils.extendArrayProps(config, opts, w2);
      options2 = Utils$1.extend(w2.config, options2);
      options2 = Utils$1.extend(options2, opts);
      iterateResponsiveOptions(options2);
    } else {
      iterateResponsiveOptions({});
    }
  }
  /**
   * @param {Record<string, any>} newOptions
   */
  overrideResponsiveOptions(newOptions) {
    const newConfig = new Config(newOptions).init({ responsiveOverride: true });
    this.w.config = /** @type {any} */
    newConfig;
  }
}
function captureStreamFrame(w2) {
  var _a, _b, _c;
  const gl = w2.globals;
  if (!gl.axisCharts || !w2.seriesData || !Array.isArray(w2.seriesData.series) || w2.seriesData.series.length === 0) {
    gl.prevStreamFrame = null;
    return;
  }
  const rPixels = [];
  if ((_b = (_a = w2.dom) == null ? void 0 : _a.baseEl) == null ? void 0 : _b.querySelectorAll) {
    w2.dom.baseEl.querySelectorAll(".apexcharts-marker").forEach((node) => {
      var _a2, _b2, _c2, _d, _e;
      const ri = parseInt((_a2 = node.getAttribute("index")) != null ? _a2 : "", 10);
      const j = parseInt(
        (_c2 = (_b2 = node.getAttribute("j")) != null ? _b2 : node.getAttribute("rel")) != null ? _c2 : "",
        10
      );
      const r2 = parseFloat(
        (_e = (_d = node.getAttribute("r")) != null ? _d : node.getAttribute("default-marker-size")) != null ? _e : ""
      );
      if (isFinite(ri) && isFinite(j) && isFinite(r2)) {
        (rPixels[ri] = rPixels[ri] || [])[j] = r2;
      }
    });
  }
  gl.prevStreamFrame = {
    seriesX: (w2.seriesData.seriesX || []).slice(),
    seriesY: w2.seriesData.series.slice(),
    xPixels: (gl.seriesXvalues || []).slice(),
    yPixels: (gl.seriesYvalues || []).slice(),
    rPixels,
    labels: (gl.labels || []).slice(),
    isXNumeric: !!((_c = w2.axisFlags) == null ? void 0 : _c.isXNumeric)
  };
}
function detectStreamScroll(w2, realIndex, newXPixels, newYPixels) {
  var _a;
  const gl = w2.globals;
  const frame = gl.prevStreamFrame;
  if (!frame || !gl.dataChanged || !((_a = w2.axisFlags) == null ? void 0 : _a.isXNumeric)) return null;
  const oldX = frame.seriesX[realIndex];
  const oldY = frame.seriesY[realIndex];
  const newX = w2.seriesData.seriesX[realIndex];
  const newY = w2.seriesData.series[realIndex];
  if (!oldX || !oldY || !newX || !newY) return null;
  if (oldX.length < 3 || newX.length < 3) return null;
  let k = -1;
  for (let i2 = 0; i2 < oldX.length; i2++) {
    if (oldX[i2] === newX[0]) {
      k = i2;
      break;
    }
  }
  if (k === -1) return null;
  const overlap = Math.min(oldX.length - k, newX.length);
  if (overlap < 2) return null;
  const appended = newX.length - overlap;
  if (k === 0 && appended === 0) return null;
  for (let i2 = 0; i2 < overlap; i2++) {
    if (oldX[k + i2] !== newX[i2]) return null;
    const oy = oldY[k + i2];
    const ny = newY[i2];
    if (oy !== ny && !(oy == null && ny == null)) return null;
  }
  const oldXP = frame.xPixels[realIndex];
  const oldYP = frame.yPixels[realIndex];
  if (!oldXP || !oldYP) return null;
  let a2 = -1;
  let b2 = -1;
  for (let i2 = 0; i2 < overlap; i2++) {
    if (oldXP[k + i2] == null || oldYP[k + i2] == null || newXPixels[i2] == null || newYPixels[i2] == null) {
      continue;
    }
    if (a2 === -1) a2 = i2;
    b2 = i2;
  }
  if (a2 === -1 || b2 <= a2) return null;
  const nxA = (
    /** @type {number} */
    newXPixels[a2]
  );
  const nxB = (
    /** @type {number} */
    newXPixels[b2]
  );
  const oxA = (
    /** @type {number} */
    oldXP[k + a2]
  );
  const oxB = (
    /** @type {number} */
    oldXP[k + b2]
  );
  if (Math.abs(nxB - nxA) < 1e-6) return null;
  const ax = (oxB - oxA) / (nxB - nxA);
  const bx = oxA - ax * nxA;
  if (!isFinite(ax) || !isFinite(bx)) return null;
  if (Math.abs(ax - 1) > 0.02) return null;
  if (Math.abs(bx) < 0.5) return null;
  let yLo = a2;
  let yHi = a2;
  for (let i2 = a2; i2 <= b2; i2++) {
    const ny = newYPixels[i2];
    if (ny == null || oldYP[k + i2] == null) continue;
    if (ny < /** @type {number} */
    newYPixels[yLo]) yLo = i2;
    if (ny > /** @type {number} */
    newYPixels[yHi]) yHi = i2;
  }
  let ay = 1;
  let by = 0;
  const nyLo = (
    /** @type {number} */
    newYPixels[yLo]
  );
  const nyHi = (
    /** @type {number} */
    newYPixels[yHi]
  );
  if (Math.abs(nyHi - nyLo) > 1e-6) {
    ay = /** @type {number} */
    (oldYP[k + yHi] - /** @type {number} */
    oldYP[k + yLo]) / (nyHi - nyLo);
    by = /** @type {number} */
    oldYP[k + yLo] - ay * nyLo;
  } else {
    by = /** @type {number} */
    oldYP[k + yLo] - nyLo;
  }
  if (!isFinite(ay) || !isFinite(by) || ay < 0.2 || ay > 5) return null;
  const m2 = Math.floor((a2 + b2) / 2);
  if (m2 !== a2 && m2 !== b2 && newXPixels[m2] != null && oldXP[k + m2] != null) {
    const predX = ax * /** @type {number} */
    newXPixels[m2] + bx;
    if (Math.abs(predX - /** @type {number} */
    oldXP[k + m2]) > 1.5) {
      return null;
    }
    if (newYPixels[m2] != null && oldYP[k + m2] != null) {
      const predY = ay * /** @type {number} */
      newYPixels[m2] + by;
      if (Math.abs(predY - /** @type {number} */
      oldYP[k + m2]) > 1.5) {
        return null;
      }
    }
  }
  return { ax, bx, ay, by };
}
const NUM_RE = /[+-]?(?:\d+\.?\d*|\.\d+)(?:e[+-]?\d+)?/gi;
function projectPathToPrevFrame(d, t2) {
  const { ax, bx, ay, by } = t2;
  const out = [];
  const re = /([MmLlHhVvCcSsQqTtAaZz])([^MmLlHhVvCcSsQqTtAaZz]*)/g;
  let match;
  while ((match = re.exec(d)) !== null) {
    const cmd = match[1].toUpperCase();
    const nums = (match[2].match(NUM_RE) || []).map(parseFloat);
    if (cmd === "Z") {
      out.push("z");
      continue;
    }
    if (cmd === "H") {
      for (const x of nums) out.push(`H ${ax * x + bx}`);
      continue;
    }
    if (cmd === "V") {
      for (const y of nums) out.push(`V ${ay * y + by}`);
      continue;
    }
    if (cmd === "A") {
      for (let i2 = 0; i2 + 6 < nums.length; i2 += 7) {
        out.push(
          `A ${nums[i2]} ${nums[i2 + 1]} ${nums[i2 + 2]} ${nums[i2 + 3]} ${nums[i2 + 4]} ${ax * nums[i2 + 5] + bx} ${ay * nums[i2 + 6] + by}`
        );
      }
      continue;
    }
    const coords = [];
    for (let i2 = 0; i2 + 1 < nums.length; i2 += 2) {
      coords.push(`${ax * nums[i2] + bx} ${ay * nums[i2 + 1] + by}`);
    }
    if (coords.length) out.push(`${cmd} ${coords.join(" ")}`);
  }
  return out.join(" ");
}
function trimStreamingSeries(newSeries, w2) {
  const cfg = w2.config.chart.streaming;
  if (!cfg || !cfg.enabled) return;
  const maxPoints = cfg.maxPoints;
  const range = w2.config.xaxis.range;
  const xOf = (p2) => {
    if (p2 == null) return null;
    if (Array.isArray(p2)) return typeof p2[0] === "number" ? p2[0] : null;
    if (typeof p2 === "object") return typeof p2.x === "number" ? p2.x : null;
    return null;
  };
  newSeries.forEach((s2) => {
    var _a;
    const data = s2 == null ? void 0 : s2.data;
    if (!Array.isArray(data) || data.length < 2) return;
    if (typeof maxPoints === "number" && maxPoints > 0) {
      if (data.length > maxPoints) s2.data = data.slice(data.length - maxPoints);
      return;
    }
    if (!range) return;
    const lastX = xOf(data[data.length - 1]);
    const firstX = xOf(data[0]);
    if (lastX == null || firstX == null || lastX <= firstX) return;
    const avgSpacing = (lastX - firstX) / (data.length - 1);
    const cutoff = lastX - range - 2 * avgSpacing;
    let idx = 0;
    while (idx < data.length && ((_a = xOf(data[idx])) != null ? _a : cutoff) < cutoff) idx++;
    if (idx > 0) s2.data = data.slice(idx);
  });
}
function buildUnionEntries(join, oldN) {
  const exitSet = new Set(join.exits);
  const entries = [];
  let oi = 0;
  for (let nj = 0; nj < join.toOld.length; nj++) {
    const oj = join.toOld[nj];
    if (oj !== -1) {
      while (oi < oj) {
        if (exitSet.has(oi)) entries.push({ oldJ: oi, newJ: -1 });
        oi++;
      }
      entries.push({ oldJ: oj, newJ: nj });
      oi = oj + 1;
    } else {
      entries.push({ oldJ: -1, newJ: nj });
    }
  }
  while (oi < oldN) {
    if (exitSet.has(oi)) entries.push({ oldJ: oi, newJ: -1 });
    oi++;
  }
  return entries;
}
function analyzeSeriesPath(d, expectedAnchors, isArea) {
  if (!d || typeof d !== "string") return null;
  const cmds = parsePath(d);
  if (!cmds.length || cmds[0][0] !== "M") return null;
  for (let i2 = 1; i2 < cmds.length; i2++) {
    if (cmds[i2][0] === "M") return null;
  }
  let body = cmds;
  let closing = null;
  if (isArea) {
    if (cmds.length < 5) return null;
    closing = cmds.slice(-3);
    body = cmds.slice(0, -3);
    if (closing[2][0] !== "Z") return null;
    if (closing[1][0] !== "L") return null;
    if (closing[0][0] !== "L" && closing[0][0] !== "C") return null;
  } else if (cmds[cmds.length - 1][0] === "Z") {
    return null;
  }
  if (body.length !== expectedAnchors || body.length < 2) return null;
  const segType = body[1][0];
  if (segType !== "L" && segType !== "C") return null;
  for (let i2 = 2; i2 < body.length; i2++) {
    if (body[i2][0] !== segType) return null;
  }
  const anchors = body.map(
    (c2) => c2[0] === "C" ? [Number(c2[5]), Number(c2[6])] : [Number(c2[1]), Number(c2[2])]
  );
  for (const a2 of anchors) {
    if (!isFinite(a2[0]) || !isFinite(a2[1])) return null;
  }
  return { body, closing, segType, anchors };
}
function lerpPt(a2, b2, t2) {
  return [a2[0] + (b2[0] - a2[0]) * t2, a2[1] + (b2[1] - a2[1]) * t2];
}
function splitCubic(s2, cmd, t2) {
  const p0 = s2;
  const p1 = [cmd[1], cmd[2]];
  const p2 = [cmd[3], cmd[4]];
  const p3 = [cmd[5], cmd[6]];
  const p01 = lerpPt(p0, p1, t2);
  const p12 = lerpPt(p1, p2, t2);
  const p23 = lerpPt(p2, p3, t2);
  const p012 = lerpPt(p01, p12, t2);
  const p123 = lerpPt(p12, p23, t2);
  const mid = lerpPt(p012, p123, t2);
  return {
    first: ["C", p01[0], p01[1], p012[0], p012[1], mid[0], mid[1]],
    second: ["C", p123[0], p123[1], p23[0], p23[1], p3[0], p3[1]],
    mid
  };
}
function splitLine(s2, cmd, t2) {
  const e2 = [cmd[1], cmd[2]];
  const mid = lerpPt(s2, e2, t2);
  return {
    first: ["L", mid[0], mid[1]],
    second: ["L", e2[0], e2[1]],
    mid
  };
}
function expandPath(analysis, ownIdx) {
  const { body, closing, segType, anchors } = analysis;
  const n2 = anchors.length;
  const m2 = ownIdx.length;
  const degen = (p2) => segType === "C" ? ["C", p2[0], p2[1], p2[0], p2[1], p2[0], p2[1]] : ["L", p2[0], p2[1]];
  const out = [["M", anchors[0][0], anchors[0][1]]];
  let firstOwn = 0;
  while (firstOwn < m2 && ownIdx[firstOwn] !== 0) firstOwn++;
  for (let q = 1; q <= firstOwn; q++) out.push(degen(anchors[0]));
  let entry = firstOwn + 1;
  for (let s2 = 0; s2 < n2 - 1; s2++) {
    let interior = 0;
    while (entry + interior < m2 && ownIdx[entry + interior] === -1) interior++;
    const cmd = body[s2 + 1];
    if (!interior) {
      out.push(cmd.slice());
    } else {
      const totalParts = interior + 1;
      let start = anchors[s2];
      let rest = cmd;
      for (let q = 0; q < interior; q++) {
        const t2 = 1 / (totalParts - q);
        const sp = segType === "C" ? splitCubic(start, rest, t2) : splitLine(start, rest, t2);
        out.push(sp.first);
        start = sp.mid;
        rest = sp.second;
      }
      out.push(rest);
    }
    entry += interior + 1;
  }
  while (entry < m2) {
    out.push(degen(anchors[n2 - 1]));
    entry++;
  }
  if (closing) closing.forEach((c2) => out.push(c2.slice()));
  return out;
}
function reconcilePathPair(fromD, toD, entries, oldN, newN, isArea) {
  const fromAnalysis = analyzeSeriesPath(fromD, oldN, isArea);
  if (!fromAnalysis) return null;
  const toAnalysis = analyzeSeriesPath(toD, newN, isArea);
  if (!toAnalysis) return null;
  if (fromAnalysis.segType !== toAnalysis.segType) return null;
  if ((fromAnalysis.closing || toAnalysis.closing) && (!fromAnalysis.closing || !toAnalysis.closing || fromAnalysis.closing[0][0] !== toAnalysis.closing[0][0])) {
    return null;
  }
  const fromOwn = entries.map((e2) => e2.oldJ);
  const toOwn = entries.map((e2) => e2.newJ);
  return {
    from: arrayToPath(expandPath(fromAnalysis, fromOwn)),
    toInterp: arrayToPath(expandPath(toAnalysis, toOwn))
  };
}
function lengthTransitionEnabled(w2) {
  var _a;
  const anim = w2.config.chart.animations;
  if (!anim || anim.enabled === false) return false;
  if (!anim.dynamicAnimation || anim.dynamicAnimation.enabled === false) {
    return false;
  }
  const largeThreshold = (_a = anim.largeDatasetThreshold) != null ? _a : 0;
  if (largeThreshold > 0 && w2.globals.dataPoints > largeThreshold) return false;
  return !!(Environment.isBrowser() && w2.globals.dataChanged && w2.globals.shouldAnimate);
}
function datumKey(w2, realIndex, j) {
  var _a, _b, _c, _d;
  if ((_a = w2.axisFlags) == null ? void 0 : _a.isXNumeric) {
    const sx = (_c = (_b = w2.seriesData) == null ? void 0 : _b.seriesX) == null ? void 0 : _c[realIndex];
    if (sx && sx.length && sx[j] != null) return "x:" + sx[j];
  }
  const lbl = (_d = w2.globals.labels) == null ? void 0 : _d[j];
  if (lbl != null && String(lbl) !== "") {
    return "c:" + (Array.isArray(lbl) ? lbl.join(" ") : String(lbl));
  }
  return "j:" + j;
}
function frameDatumKey(frame, realIndex, j) {
  var _a, _b;
  if (frame.isXNumeric) {
    const sx = (_a = frame.seriesX) == null ? void 0 : _a[realIndex];
    if (sx && sx.length && sx[j] != null) return "x:" + sx[j];
  }
  const lbl = (_b = frame.labels) == null ? void 0 : _b[j];
  if (lbl != null && String(lbl) !== "") {
    return "c:" + (Array.isArray(lbl) ? lbl.join(" ") : String(lbl));
  }
  return "j:" + j;
}
function joinKeys(oldKeys, newKeys) {
  const oldIndex = /* @__PURE__ */ new Map();
  oldKeys.forEach((k, i2) => {
    if (!oldIndex.has(k)) oldIndex.set(k, i2);
  });
  const toOld = new Array(newKeys.length);
  const usedOld = /* @__PURE__ */ new Set();
  let prev = -1;
  let ordered = true;
  let identity = oldKeys.length === newKeys.length;
  newKeys.forEach((k, i2) => {
    const oi = oldIndex.has(k) && !usedOld.has(oldIndex.get(k)) ? oldIndex.get(k) : -1;
    toOld[i2] = oi;
    if (oi !== -1) {
      usedOld.add(oi);
      if (oi < prev) ordered = false;
      prev = oi;
    }
    if (oi !== i2) identity = false;
  });
  const exits = [];
  for (let i2 = 0; i2 < oldKeys.length; i2++) {
    if (!usedOld.has(i2)) exits.push(i2);
  }
  return { toOld, exits, ordered, changed: !identity };
}
function uniquifyKeys(keys) {
  const seen = /* @__PURE__ */ new Map();
  return keys.map((k) => {
    const count = seen.get(k) || 0;
    seen.set(k, count + 1);
    return count === 0 ? k : `${k}#${count}`;
  });
}
function seriesJoin(w2, realIndex, includeIdentity = false, allowReorder = false) {
  var _a, _b;
  if (!lengthTransitionEnabled(w2)) return null;
  const frame = w2.globals.prevStreamFrame;
  if (!frame) return null;
  const oldY = (_a = frame.seriesY) == null ? void 0 : _a[realIndex];
  const newY = (_b = w2.seriesData.series) == null ? void 0 : _b[realIndex];
  if (!Array.isArray(oldY) || !Array.isArray(newY)) return null;
  if (!oldY.length || !newY.length) return null;
  const oldKeys = uniquifyKeys(
    oldY.map((_, j) => frameDatumKey(frame, realIndex, j))
  );
  const newKeys = uniquifyKeys(newY.map((_, j) => datumKey(w2, realIndex, j)));
  const join = joinKeys(oldKeys, newKeys);
  if (!join.ordered && !allowReorder) return null;
  if (!join.changed && !includeIdentity) return null;
  return { join, oldKeys, newKeys };
}
function morphEasing(w2) {
  var _a, _b;
  const anim = w2.config.chart.animations;
  return resolveEasing((_b = (_a = anim.dynamicAnimation) == null ? void 0 : _a.easing) != null ? _b : anim.easing);
}
function rafTween(w2, duration, ease, onFrame, onDone) {
  const startAt = performance.now();
  const step = (now) => {
    if (w2.globals.isDestroyed) return;
    const raw = Math.max(0, Math.min(1, (now - startAt) / duration));
    onFrame(ease(raw), raw);
    if (raw < 1) {
      BrowserAPIs.requestAnimationFrame(step);
    } else if (onDone) {
      onDone();
    }
  };
  BrowserAPIs.requestAnimationFrame(step);
}
function tweenSeriesMarkers(w2, { elPointsMain, realIndex, speed }) {
  var _a, _b, _c, _d;
  if (!(elPointsMain == null ? void 0 : elPointsMain.node)) return false;
  const sj = seriesJoin(w2, realIndex, true);
  if (!sj) return false;
  const frame = w2.globals.prevStreamFrame;
  if (!frame) return false;
  const oldXP = (_a = frame.xPixels) == null ? void 0 : _a[realIndex];
  const oldYP = (_b = frame.yPixels) == null ? void 0 : _b[realIndex];
  if (!oldXP || !oldYP) return false;
  const markers = elPointsMain.node.querySelectorAll(".apexcharts-marker");
  if (!markers.length) return false;
  const newXP = ((_c = w2.globals.seriesXvalues) == null ? void 0 : _c[realIndex]) || [];
  const newYP = ((_d = w2.globals.seriesYvalues) == null ? void 0 : _d[realIndex]) || [];
  const ease = morphEasing(w2);
  const duration = Math.max(1, speed || 1);
  elPointsMain.node.classList.remove("apexcharts-element-hidden");
  markers.forEach((node) => {
    var _a2, _b2, _c2, _d2, _e, _f, _g, _h, _i;
    const j = parseInt(
      (_b2 = (_a2 = node.getAttribute("j")) != null ? _a2 : node.getAttribute("rel")) != null ? _b2 : "",
      10
    );
    if (!isFinite(j) || j < 0 || j >= sj.join.toOld.length) return;
    const oldJ = sj.join.toOld[j];
    const to = newXP[j] != null && newYP[j] != null ? [newXP[j], newYP[j]] : null;
    if (oldJ === -1 || !to) {
      const style = (
        /** @type {any} */
        node.style
      );
      style.opacity = "0";
      rafTween(
        w2,
        duration,
        ease,
        (eased) => {
          style.opacity = String(eased);
        },
        () => {
          style.opacity = "";
        }
      );
      return;
    }
    const dx = ((_c2 = oldXP[oldJ]) != null ? _c2 : NaN) - to[0];
    const dy = ((_d2 = oldYP[oldJ]) != null ? _d2 : NaN) - to[1];
    if (!isFinite(dx) || !isFinite(dy)) return;
    const rFrom = (_g = (_f = (_e = frame.rPixels) == null ? void 0 : _e[realIndex]) == null ? void 0 : _f[oldJ]) != null ? _g : NaN;
    const rTo = parseFloat(
      (_i = (_h = node.getAttribute("r")) != null ? _h : node.getAttribute("default-marker-size")) != null ? _i : ""
    );
    const scales = isFinite(rFrom) && isFinite(rTo) && rTo > 0 && Math.abs(rFrom - rTo) > 0.25;
    const moves = Math.abs(dx) >= 0.5 || Math.abs(dy) >= 0.5;
    if (!moves && !scales) return;
    const apply = (eased) => {
      const offX = dx * (1 - eased);
      const offY = dy * (1 - eased);
      if (scales) {
        const s2 = (rFrom + (rTo - rFrom) * eased) / rTo;
        node.setAttribute(
          "transform",
          `translate(${offX + to[0] * (1 - s2)}, ${offY + to[1] * (1 - s2)}) scale(${s2})`
        );
      } else {
        node.setAttribute("transform", `translate(${offX}, ${offY})`);
      }
    };
    apply(0);
    rafTween(w2, duration, ease, apply, () => {
      node.removeAttribute("transform");
    });
  });
  return true;
}
function reconcileSeriesPaths(w2, { type, realIndex, pathFromLine, pathFromArea, linePaths, areaPaths }) {
  var _a, _b;
  const sj = seriesJoin(w2, realIndex);
  if (!sj) return null;
  const { join, oldKeys, newKeys } = sj;
  const frame = w2.globals.prevStreamFrame;
  if (!frame) return null;
  const oldY = (_a = frame.seriesY) == null ? void 0 : _a[realIndex];
  const newY = (_b = w2.seriesData.series) == null ? void 0 : _b[realIndex];
  if (oldY.length < 2 || newY.length < 2) return null;
  if (oldY.some((v2) => v2 === null) || newY.some((v2) => v2 === null)) return null;
  const entries = buildUnionEntries(join, oldKeys.length);
  const out = {};
  if (Array.isArray(linePaths) && linePaths.length === 1 && pathFromLine) {
    out.line = reconcilePathPair(
      pathFromLine,
      linePaths[0],
      entries,
      oldKeys.length,
      newKeys.length,
      false
    );
  }
  if (type === "area" && Array.isArray(areaPaths) && areaPaths.length === 1 && pathFromArea) {
    out.area = reconcilePathPair(
      pathFromArea,
      areaPaths[0],
      entries,
      oldKeys.length,
      newKeys.length,
      true
    );
  }
  if (!out.line && !out.area) return null;
  return out;
}
function firstMove(d) {
  const m2 = /^M\s*([+-]?[\d.eE]+)[\s,]+([+-]?[\d.eE]+)/.exec(d || "");
  if (!m2) return null;
  const x = parseFloat(m2[1]);
  const y = parseFloat(m2[2]);
  return isFinite(x) && isFinite(y) ? { x, y } : null;
}
function renderBarExitGhosts({
  w: w2,
  elSeries,
  record,
  newKeys,
  isHorizontal,
  speed
}) {
  var _a;
  if (!lengthTransitionEnabled(w2)) return;
  if (!record || !Array.isArray(record.paths) || !(elSeries == null ? void 0 : elSeries.node)) return;
  const newKeySet = new Set(newKeys);
  const exits = record.paths.filter(
    (p2) => p2 && p2.d && p2.key != null && !newKeySet.has(p2.key)
  );
  if (!exits.length) return;
  const graphics = new Graphics(w2);
  const fallbackFill = (_a = w2.globals.colors) == null ? void 0 : _a[parseInt(String(record.realIndex), 10)];
  exits.forEach((p2) => {
    let fill = p2.fill || fallbackFill || "#c8c8c8";
    if (String(fill).indexOf("url(") === 0) fill = fallbackFill || "#c8c8c8";
    const ghost = graphics.drawPath({
      d: p2.d,
      stroke: "none",
      strokeWidth: 0,
      fill,
      fillOpacity: 1,
      classes: "apexcharts-bar-ghost"
    });
    const node = ghost.node;
    node.setAttribute("pointer-events", "none");
    ghost.attr(
      "clip-path",
      `url(#gridRectBarMask${w2.globals.cuid})`
    );
    elSeries.node.insertBefore(node, elSeries.node.firstChild);
    const start = firstMove(p2.d);
    let origin = isHorizontal ? "left center" : "center bottom";
    try {
      const bb = node.getBBox();
      if (start && bb) {
        if (isHorizontal) {
          origin = Math.abs(start.x - bb.x) <= Math.abs(start.x - (bb.x + bb.width)) ? "left center" : "right center";
        } else {
          origin = Math.abs(start.y - (bb.y + bb.height)) <= Math.abs(start.y - bb.y) ? "center bottom" : "center top";
        }
      }
    } catch (_) {
    }
    const style = node.style;
    style.transformBox = "fill-box";
    style.transformOrigin = origin;
    const duration = Math.max(1, speed || 1);
    const startAt = performance.now();
    const step = (now) => {
      if (w2.globals.isDestroyed || !node.parentNode) return;
      const t2 = Math.max(0, Math.min(1, (now - startAt) / duration));
      const eased = 1 - Math.pow(1 - t2, 3);
      const scale = 1 - eased;
      style.transform = isHorizontal ? `scaleX(${scale})` : `scaleY(${scale})`;
      style.opacity = String(1 - eased);
      if (t2 < 1) {
        BrowserAPIs.requestAnimationFrame(step);
      } else {
        node.parentNode.removeChild(node);
      }
    };
    BrowserAPIs.requestAnimationFrame(step);
  });
}
function grabLabels(root, sel, posAttr) {
  return [...root.querySelectorAll(sel)].map((el) => {
    var _a, _b, _c;
    return {
      // `text` is the matching KEY (textContent, which doubles tspan + title
      // but does so consistently on both sides); `display` is the visible
      // string, used when rendering an exit ghost.
      text: el.textContent || "",
      display: (_c = (_b = (_a = el.querySelector("tspan")) == null ? void 0 : _a.textContent) != null ? _b : el.textContent) != null ? _c : "",
      pos: parseFloat(el.getAttribute(posAttr) || ""),
      transform: el.getAttribute("transform")
    };
  });
}
function grabLines(root, sel, posAttr) {
  return [...root.querySelectorAll(sel)].map(
    (el) => parseFloat(el.getAttribute(posAttr) || "")
  );
}
const NO_GHOST = ":not(.apexcharts-tick-ghost)";
const X_LABELS_SEL = `.apexcharts-xaxis-texts-g text:not(.apexcharts-xaxis-group-label)${NO_GHOST}`;
const Y_LABELS_SEL = `.apexcharts-yaxis-texts-g text${NO_GHOST}`;
const V_GRID_SEL = `.apexcharts-gridlines-vertical line${NO_GHOST}`;
const H_GRID_SEL = `.apexcharts-gridlines-horizontal line${NO_GHOST}`;
function currentXScale(w2) {
  var _a, _b;
  const gl = w2.globals;
  if (!((_a = w2.axisFlags) == null ? void 0 : _a.isXNumeric) || gl.isBarHorizontal) return null;
  if ((_b = w2.config.xaxis) == null ? void 0 : _b.reversed) return null;
  const min = gl.minX;
  const max = gl.maxX;
  const width = w2.layout.gridWidth;
  if (!isFinite(min) || !isFinite(max) || !(max > min) || !(width > 0)) {
    return null;
  }
  return { min, max, width };
}
function currentYAnchors(w2, labels) {
  var _a;
  const gl = w2.globals;
  if (gl.isBarHorizontal) return null;
  if (!Array.isArray(w2.config.yaxis) || w2.config.yaxis.length !== 1) return null;
  if ((_a = w2.config.yaxis[0]) == null ? void 0 : _a.logarithmic) return null;
  const min = gl.minY;
  const max = gl.maxY;
  if (!isFinite(min) || !isFinite(max) || !(max > min)) return null;
  const ps = labels.map((l2) => l2.pos).filter((p2) => isFinite(p2));
  if (ps.length < 2) return null;
  return { min, max, pLo: Math.max(...ps), pHi: Math.min(...ps) };
}
function composeXMap(o2, n2) {
  if (!o2 || !n2) return null;
  const os = o2.max - o2.min;
  const ns = n2.max - n2.min;
  if (!(os > 0) || !(ns > 0) || !(o2.width > 0) || !(n2.width > 0)) return null;
  return {
    toNew: (p2) => (o2.min + p2 / o2.width * os - n2.min) / ns * n2.width,
    toOld: (p2) => (n2.min + p2 / n2.width * ns - o2.min) / os * o2.width
  };
}
function composeYMap(o2, n2) {
  if (!o2 || !n2) return null;
  const oSpanP = o2.pHi - o2.pLo;
  const nSpanP = n2.pHi - n2.pLo;
  const oSpanV = o2.max - o2.min;
  const nSpanV = n2.max - n2.min;
  if (!oSpanP || !nSpanP || !(oSpanV > 0) || !(nSpanV > 0)) return null;
  const oldVal = (p2) => o2.min + (p2 - o2.pLo) / oSpanP * oSpanV;
  const newVal = (p2) => n2.min + (p2 - n2.pLo) / nSpanP * nSpanV;
  return {
    toNew: (p2) => n2.pLo + (oldVal(p2) - n2.min) / nSpanV * nSpanP,
    toOld: (p2) => o2.pLo + (newVal(p2) - o2.min) / oSpanV * oSpanP
  };
}
function captureAxisChrome(w2) {
  const gl = w2.globals;
  gl.prevChromeFrame = null;
  if (!gl.axisCharts || !Environment.isBrowser()) return;
  const root = w2.dom.baseEl;
  if (!Utils$1.elementExists(root)) return;
  try {
    const yLabels = grabLabels(root, Y_LABELS_SEL, "y");
    gl.prevChromeFrame = {
      xLabels: grabLabels(root, X_LABELS_SEL, "x"),
      yLabels,
      vGrid: grabLines(root, V_GRID_SEL, "x1"),
      hGrid: grabLines(root, H_GRID_SEL, "y1"),
      // Value scales of the outgoing render, so ticks whose TEXT has no
      // counterpart (e.g. a zoom across datetime granularities) can still be
      // re-projected: new ticks slide in from where their value sat, old
      // ticks ghost out to where their value lands.
      xScale: currentXScale(w2),
      yAnchors: currentYAnchors(w2, yLabels)
    };
  } catch (_) {
    gl.prevChromeFrame = null;
  }
}
function fadeIn$1(w2, node, duration, ease) {
  const style = (
    /** @type {any} */
    node.style
  );
  style.opacity = "0";
  rafTween(
    w2,
    duration,
    ease,
    (eased) => {
      style.opacity = String(eased);
    },
    () => {
      style.opacity = "";
    }
  );
}
function tweenPos(w2, node, attrs, from, to, duration, ease) {
  attrs.forEach((a2) => node.setAttribute(a2, String(from)));
  rafTween(
    w2,
    duration,
    ease,
    (eased) => {
      const v2 = String(from + (to - from) * eased);
      attrs.forEach((a2) => node.setAttribute(a2, v2));
    },
    () => {
      attrs.forEach((a2) => node.setAttribute(a2, String(to)));
    }
  );
}
function spawnGhost(w2, { template, display, attrs, from, to, duration, ease }) {
  const parent = template.parentNode;
  if (!parent) return;
  const ghost = (
    /** @type {Element} */
    template.cloneNode(true)
  );
  ghost.classList.add("apexcharts-tick-ghost");
  ghost.setAttribute("pointer-events", "none");
  ghost.removeAttribute("id");
  if (display !== void 0) {
    const tspan = ghost.querySelector("tspan");
    if (tspan) tspan.textContent = display;
    else ghost.textContent = display;
    const title = ghost.querySelector("title");
    if (title) title.textContent = display;
  }
  attrs.forEach((a2) => ghost.setAttribute(a2, String(from)));
  const style = (
    /** @type {any} */
    ghost.style
  );
  style.opacity = "1";
  parent.appendChild(ghost);
  rafTween(
    w2,
    duration,
    ease,
    (eased) => {
      const v2 = String(from + (to - from) * eased);
      attrs.forEach((a2) => ghost.setAttribute(a2, v2));
      style.opacity = String(1 - eased);
    },
    () => {
      if (ghost.parentNode) ghost.parentNode.removeChild(ghost);
    }
  );
}
const MAX_GHOSTS = 20;
function transitionAxis(w2, {
  newLabels,
  oldLabels,
  posAttr,
  newLines,
  oldLines,
  lineAttrs,
  duration,
  ease,
  project
}) {
  const oldByText = /* @__PURE__ */ new Map();
  oldLabels.forEach((l2, i2) => {
    if (!oldByText.has(l2.text)) oldByText.set(l2.text, __spreadProps(__spreadValues({}, l2), { i: i2 }));
  });
  const matchedOld = /* @__PURE__ */ new Set();
  const newLinesAligned = newLines.length === newLabels.length;
  const oldLinesAligned = oldLines.length === oldLabels.length;
  const spanPs = oldLabels.map((l2) => l2.pos).concat(
    newLabels.map((l2) => parseFloat(l2.getAttribute(posAttr) || ""))
  ).filter((p2) => isFinite(p2));
  const spanLo = Math.min(...spanPs);
  const spanHi = Math.max(...spanPs);
  const margin = Math.max(40, (spanHi - spanLo) * 0.25);
  const clamp = (p2) => Math.max(spanLo - margin, Math.min(spanHi + margin, p2));
  const tweenPairedLine = (line, old) => {
    if (!line) return;
    const lineTo = parseFloat(line.getAttribute(lineAttrs[0]) || "");
    const lineFrom = oldLines[old.i];
    if (isFinite(lineTo) && isFinite(lineFrom)) {
      tweenPos(w2, line, lineAttrs, lineFrom, lineTo, duration, ease);
    }
  };
  newLabels.forEach((label, i2) => {
    const to = parseFloat(label.getAttribute(posAttr) || "");
    const old = oldByText.get(label.textContent || "");
    const line = newLinesAligned ? newLines[i2] : null;
    if (old) matchedOld.add(old.i);
    const labelTransform = label.getAttribute("transform");
    if (!old || !isFinite(old.pos)) {
      if (!old) {
        if (project && isFinite(to) && !labelTransform) {
          const from = isFinite(project.toOld(to)) ? clamp(project.toOld(to)) : NaN;
          if (isFinite(from) && Math.abs(from - to) > 0.5) {
            tweenPos(w2, label, [posAttr], from, to, duration, ease);
            if (line) tweenPos(w2, line, lineAttrs, from, to, duration, ease);
          }
        }
        fadeIn$1(w2, label, duration, ease);
        if (line) fadeIn$1(w2, line, duration, ease);
      }
      return;
    }
    if (!isFinite(to) || Math.abs(old.pos - to) < 0.5) return;
    if (labelTransform || old.transform) {
      const delta = old.pos - to;
      if (isFinite(delta)) {
        const base = labelTransform || "";
        rafTween(
          w2,
          duration,
          ease,
          (eased) => {
            const v2 = delta * (1 - eased);
            const t2 = posAttr === "x" ? `translate(${v2} 0)` : `translate(0 ${v2})`;
            label.setAttribute("transform", `${t2} ${base}`.trim());
          },
          () => {
            if (base) label.setAttribute("transform", base);
            else label.removeAttribute("transform");
          }
        );
      }
      tweenPairedLine(line, old);
      return;
    }
    tweenPos(w2, label, [posAttr], old.pos, to, duration, ease);
    tweenPairedLine(line, old);
  });
  if (!project || !newLabels.length) return;
  let ghosts = 0;
  oldLabels.forEach((old, i2) => {
    if (matchedOld.has(i2)) return;
    if (!isFinite(old.pos) || old.transform) return;
    if (ghosts >= MAX_GHOSTS) return;
    const rawTo = project.toNew(old.pos);
    if (!isFinite(rawTo) || Math.abs(rawTo - old.pos) < 0.5) return;
    ghosts++;
    spawnGhost(w2, {
      template: newLabels[0],
      display: old.display,
      attrs: [posAttr],
      from: old.pos,
      to: clamp(rawTo),
      duration,
      ease
    });
    if (oldLinesAligned && newLines.length && isFinite(oldLines[i2])) {
      spawnGhost(w2, {
        template: newLines[0],
        attrs: lineAttrs,
        from: oldLines[i2],
        to: clamp(project.toNew(oldLines[i2])),
        duration,
        ease
      });
    }
  });
}
function applyAxisTransition(w2) {
  const gl = w2.globals;
  const chrome = gl.prevChromeFrame;
  gl.prevChromeFrame = null;
  if (!chrome || !gl.axisCharts || !Environment.isBrowser()) return;
  if (!lengthTransitionEnabled(w2)) return;
  const anyMotion = (w2.seriesData.series || []).some(
    (_, i2) => seriesJoin(w2, i2, true, true) !== null
  );
  if (!anyMotion) return;
  const root = w2.dom.baseEl;
  if (!Utils$1.elementExists(root)) return;
  const duration = Math.max(1, w2.config.chart.animations.dynamicAnimation.speed || 1);
  const ease = morphEasing(w2);
  try {
    const newYLabels = [...root.querySelectorAll(Y_LABELS_SEL)];
    const projX = composeXMap(chrome.xScale, currentXScale(w2));
    const projY = composeYMap(
      chrome.yAnchors,
      currentYAnchors(
        w2,
        newYLabels.map((el) => ({
          pos: parseFloat(el.getAttribute("y") || "")
        }))
      )
    );
    transitionAxis(w2, {
      newLabels: [...root.querySelectorAll(X_LABELS_SEL)],
      oldLabels: chrome.xLabels,
      posAttr: "x",
      newLines: [...root.querySelectorAll(V_GRID_SEL)],
      oldLines: chrome.vGrid,
      lineAttrs: ["x1", "x2"],
      duration,
      ease,
      project: projX
    });
    transitionAxis(w2, {
      newLabels: newYLabels,
      oldLabels: chrome.yLabels,
      posAttr: "y",
      newLines: [...root.querySelectorAll(H_GRID_SEL)],
      oldLines: chrome.hGrid,
      lineAttrs: ["y1", "y2"],
      duration,
      ease,
      project: projY
    });
  } catch (_) {
  }
}
const DL_GROUP_SEL = ".apexcharts-data-labels[data\\:dlKey]";
const DL_TEXT_SEL = ".apexcharts-datalabel";
function dataLabelMotionEnabled(w2) {
  var _a, _b;
  const dl = w2.config.dataLabels;
  return !!(((_a = dl == null ? void 0 : dl.animate) == null ? void 0 : _a.enabled) || ((_b = dl == null ? void 0 : dl.countUp) == null ? void 0 : _b.enabled));
}
function decimalsOf(n2) {
  if (!isFinite(n2)) return 0;
  const s2 = String(Math.abs(n2));
  const e2 = s2.indexOf("e");
  if (e2 !== -1) {
    const mantissa = s2.slice(0, e2);
    const exp = parseInt(s2.slice(e2 + 1), 10);
    const dot2 = mantissa.indexOf(".");
    const mantissaDec = dot2 === -1 ? 0 : mantissa.length - dot2 - 1;
    return Math.min(6, Math.max(0, mantissaDec - exp));
  }
  const dot = s2.indexOf(".");
  return dot === -1 ? 0 : Math.min(6, s2.length - dot - 1);
}
function writeLabel(textEl, s2) {
  const tspan = textEl.querySelector("tspan");
  if (tspan) tspan.textContent = s2;
  else textEl.textContent = s2;
}
function captureDataLabels(w2) {
  const gl = w2.globals;
  gl.prevDataLabels = null;
  if (!gl.axisCharts || !Environment.isBrowser()) return;
  if (!dataLabelMotionEnabled(w2)) return;
  const root = w2.dom.baseEl;
  if (!Utils$1.elementExists(root)) return;
  try {
    const map = /* @__PURE__ */ new Map();
    root.querySelectorAll(DL_GROUP_SEL).forEach((group) => {
      const key = group.getAttribute("data:dlKey");
      if (!key) return;
      const textEl = group.querySelector(DL_TEXT_SEL);
      if (!textEl) return;
      map.set(key, {
        cx: parseFloat(textEl.getAttribute("cx") || ""),
        cy: parseFloat(textEl.getAttribute("cy") || ""),
        val: parseFloat(group.getAttribute("data:dlVal") || "")
      });
    });
    gl.prevDataLabels = map.size ? map : null;
  } catch (_) {
    gl.prevDataLabels = null;
  }
}
function fadeIn(w2, node, duration, ease) {
  const style = (
    /** @type {any} */
    node.style
  );
  style.opacity = "0";
  rafTween(
    w2,
    duration,
    ease,
    (eased) => {
      style.opacity = String(eased);
    },
    () => {
      style.opacity = "";
    }
  );
}
function applyDataLabelTransition(w2) {
  var _a, _b;
  const gl = w2.globals;
  const prev = gl.prevDataLabels;
  gl.prevDataLabels = null;
  if (!prev || !gl.axisCharts || !Environment.isBrowser()) return;
  if (!dataLabelMotionEnabled(w2)) return;
  if (!lengthTransitionEnabled(w2)) return;
  const root = w2.dom.baseEl;
  if (!Utils$1.elementExists(root)) return;
  const dl = w2.config.dataLabels;
  const ride = !!((_a = dl.animate) == null ? void 0 : _a.enabled);
  const countUp = !!((_b = dl.countUp) == null ? void 0 : _b.enabled);
  const formatter = dl.formatter;
  const duration = Math.max(1, w2.config.chart.animations.dynamicAnimation.speed || 1);
  const ease = morphEasing(w2);
  try {
    root.querySelectorAll(DL_GROUP_SEL).forEach((group) => {
      const key = group.getAttribute("data:dlKey");
      if (!key) return;
      const textEl = group.querySelector(DL_TEXT_SEL);
      if (!textEl) return;
      const old = prev.get(key);
      if (ride) {
        if (old && isFinite(old.cx) && isFinite(old.cy)) {
          const newCx = parseFloat(textEl.getAttribute("cx") || "");
          const newCy = parseFloat(textEl.getAttribute("cy") || "");
          const dx = old.cx - newCx;
          const dy = old.cy - newCy;
          if (isFinite(dx) && isFinite(dy) && Math.abs(dx) + Math.abs(dy) > 0.5) {
            const base = group.getAttribute("transform") || "";
            rafTween(
              w2,
              duration,
              ease,
              (eased) => {
                const t2 = 1 - eased;
                group.setAttribute(
                  "transform",
                  `translate(${dx * t2} ${dy * t2}) ${base}`.trim()
                );
              },
              () => {
                group.setAttribute("transform", base);
              }
            );
          }
        } else if (!old) {
          fadeIn(w2, group, duration, ease);
        }
      }
      if (countUp && old && isFinite(old.val)) {
        const newVal = parseFloat(group.getAttribute("data:dlVal") || "");
        if (isFinite(newVal) && Math.abs(newVal - old.val) > 1e-9) {
          const from = old.val;
          const dec = Math.max(decimalsOf(from), decimalsOf(newVal));
          const realIndex = parseInt(key, 10);
          const j = parseInt(group.getAttribute("data:dlJ") || "", 10);
          const fmtOpts = __spreadProps(__spreadValues({}, w2), {
            seriesIndex: realIndex,
            dataPointIndex: isFinite(j) ? j : 0,
            w: w2
          });
          const format = (v2) => {
            const rounded = Number(v2.toFixed(dec));
            let out = rounded;
            if (typeof formatter === "function") {
              try {
                out = formatter(rounded, fmtOpts);
              } catch (_) {
                out = rounded;
              }
            }
            return String(out);
          };
          rafTween(
            w2,
            duration,
            ease,
            (eased) => {
              writeLabel(textEl, format(from + (newVal - from) * eased));
            },
            () => {
              writeLabel(textEl, format(newVal));
            }
          );
        }
      }
    });
  } catch (_) {
  }
}
class Series {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {{ toggleDataSeries?: Function, revertDefaultAxisMinMax?: Function, updateSeries?: Function }} [callbacks]
   */
  constructor(w2, {
    toggleDataSeries = void 0,
    revertDefaultAxisMinMax = void 0,
    updateSeries = void 0
  } = {}) {
    this.w = w2;
    this._toggleDataSeries = toggleDataSeries || null;
    this._revertDefaultAxisMinMax = revertDefaultAxisMinMax || null;
    this._updateSeries = updateSeries || null;
    this.legendInactiveClass = "legend-mouseover-inactive";
  }
  clearSeriesCache() {
    const w2 = this.w;
    if (w2.globals.cachedSelectors) {
      delete w2.globals.cachedSelectors.allSeriesEls;
      delete w2.globals.cachedSelectors.highlightSeriesEls;
    }
  }
  getAllSeriesEls() {
    const w2 = this.w;
    const cacheKey = "allSeriesEls";
    if (!w2.globals.cachedSelectors[cacheKey]) {
      w2.globals.cachedSelectors[cacheKey] = /** @type {any} */
      w2.dom.baseEl.getElementsByClassName(`apexcharts-series`);
    }
    return w2.globals.cachedSelectors[cacheKey];
  }
  /**
   * @param {string} seriesName
   */
  getSeriesByName(seriesName) {
    return this.w.dom.baseEl.querySelector(
      `.apexcharts-inner .apexcharts-series[seriesName='${Utils$1.escapeString(
        seriesName
      )}']`
    );
  }
  /**
   * @param {string} seriesName
   */
  isSeriesHidden(seriesName) {
    var _a;
    const targetElement = this.getSeriesByName(seriesName);
    const el = (
      /** @type {Element} */
      targetElement
    );
    const realIndex = parseInt((_a = el.getAttribute("data:realIndex")) != null ? _a : "0", 10);
    const isHidden = el.classList.contains("apexcharts-series-collapsed");
    return { isHidden, realIndex };
  }
  /**
   * @param {any} elSeries
   * @param {number} index
   */
  addCollapsedClassToSeries(elSeries, index) {
    Series.addCollapsedClassToSeries(this.w, elSeries, index);
  }
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {any} elSeries
   * @param {number} index
   */
  static addCollapsedClassToSeries(w2, elSeries, index) {
    function iterateOnAllCollapsedSeries(series) {
      for (let cs = 0; cs < series.length; cs++) {
        if (series[cs].index === index) {
          elSeries.node.classList.add("apexcharts-series-collapsed");
        }
      }
    }
    iterateOnAllCollapsedSeries(w2.globals.collapsedSeries);
    iterateOnAllCollapsedSeries(w2.globals.ancillaryCollapsedSeries);
  }
  /**
   * @param {string} seriesName
   */
  toggleSeries(seriesName) {
    var _a;
    const isSeriesHidden = this.isSeriesHidden(seriesName);
    (_a = this._toggleDataSeries) == null ? void 0 : _a.call(this, isSeriesHidden.realIndex, isSeriesHidden.isHidden);
    return isSeriesHidden.isHidden;
  }
  /**
   * @param {string} seriesName
   */
  showSeries(seriesName) {
    var _a;
    const isSeriesHidden = this.isSeriesHidden(seriesName);
    if (isSeriesHidden.isHidden) {
      (_a = this._toggleDataSeries) == null ? void 0 : _a.call(this, isSeriesHidden.realIndex, true);
    }
  }
  /**
   * @param {string} seriesName
   */
  hideSeries(seriesName) {
    var _a;
    const isSeriesHidden = this.isSeriesHidden(seriesName);
    if (!isSeriesHidden.isHidden) {
      (_a = this._toggleDataSeries) == null ? void 0 : _a.call(this, isSeriesHidden.realIndex, false);
    }
  }
  /**
   * Cheap pre-update reset for the data-replacement paths (updateSeries /
   * appendSeries). Clears the same bookkeeping resetSeries() clears (series
   * cache, previous paths, collapsed-series state) WITHOUT restoring
   * config.series from the initialSeries snapshot: the caller is about to
   * replace the series anyway (parseData assigns config.series = newSeries),
   * so materializing and cloning the snapshot per update is pure waste (two
   * O(n) deep clones per streaming tick at 50k points).
   */
  prepareDataUpdate() {
    const w2 = this.w;
    this.clearSeriesCache();
    w2.globals.previousPaths = [];
    w2.globals.collapsedSeries = [];
    w2.globals.ancillaryCollapsedSeries = [];
    w2.globals.collapsedSeriesIndices = [];
    w2.globals.ancillaryCollapsedSeriesIndices = [];
  }
  resetSeries(shouldUpdateChart = true, shouldResetZoom = true, shouldResetCollapsed = true) {
    var _a, _b;
    const w2 = this.w;
    this.clearSeriesCache();
    let series = Utils$1.clone(w2.globals.initialSeries);
    if (!Array.isArray(series)) {
      series = Utils$1.clone(w2.config.series) || [];
    }
    w2.globals.previousPaths = [];
    if (shouldResetCollapsed) {
      w2.globals.collapsedSeries = [];
      w2.globals.ancillaryCollapsedSeries = [];
      w2.globals.collapsedSeriesIndices = [];
      w2.globals.ancillaryCollapsedSeriesIndices = [];
    } else {
      series = this.emptyCollapsedSeries(series);
    }
    w2.config.series = series;
    if (shouldUpdateChart) {
      if (shouldResetZoom) {
        w2.interact.zoomed = false;
        (_a = this._revertDefaultAxisMinMax) == null ? void 0 : _a.call(this);
      }
      (_b = this._updateSeries) == null ? void 0 : _b.call(
        this,
        series,
        w2.config.chart.animations.dynamicAnimation.enabled
      );
    }
  }
  /**
   * @param {any[]} series
   */
  emptyCollapsedSeries(series) {
    const w2 = this.w;
    if (!Array.isArray(series)) return series;
    for (let i2 = 0; i2 < series.length; i2++) {
      if (w2.globals.collapsedSeriesIndices.indexOf(i2) > -1) {
        if (series[i2] && typeof series[i2] === "object") {
          series[i2].data = [];
        } else {
          series[i2] = 0;
        }
      }
    }
    return series;
  }
  /**
   * Series display names for the CURRENT `w.config` (post-merge), derived the
   * same way the parser does: an object series' own `name`, else the matching
   * `labels` entry (non-axis / pie / unit), else a generated `series-N`.
   * @returns {string[]}
   */
  _deriveSeriesNames() {
    const w2 = this.w;
    const series = w2.config.series || [];
    const labels = w2.config.labels || [];
    return series.map((s2, i2) => {
      if (s2 && typeof s2 === "object" && s2.name != null) return String(s2.name);
      return labels[i2] != null ? String(labels[i2]) : `series-${i2 + 1}`;
    });
  }
  /**
   * Re-apply legend-hidden (collapsed) series to a freshly-updated
   * `w.config.series`, matching BY CATEGORY NAME rather than index. This keeps a
   * hide alive across a data update that reorders or regroups categories - most
   * visibly a storyboard beat that supplies new series each scroll:
   *   - a category still present stays hidden (at its possibly-new index);
   *   - a category the update dropped/regrouped away is un-hidden (it no longer
   *     exists, so it must reappear as part of the new grouping).
   * Records without a stored name (older collapses) fall back to their index.
   */
  reconcileCollapsedByName() {
    const w2 = this.w;
    const gl = w2.globals;
    const newNames = this._deriveSeriesNames();
    const reconcile = (records) => {
      const nextRecords = [];
      const nextIndices = [];
      records.forEach((rec) => {
        const j = rec && rec.name != null ? newNames.indexOf(rec.name) : rec.index;
        if (j == null || j < 0 || j >= w2.config.series.length) return;
        const s2 = (
          /** @type {any} */
          w2.config.series[j]
        );
        rec.index = j;
        rec.data = gl.axisCharts ? s2 && s2.data ? s2.data.slice() : [] : s2;
        nextRecords.push(rec);
        nextIndices.push(j);
      });
      return { records: nextRecords, indices: nextIndices };
    };
    const main = reconcile(gl.collapsedSeries);
    gl.collapsedSeries = main.records;
    gl.collapsedSeriesIndices = main.indices;
    const anc = reconcile(gl.ancillaryCollapsedSeries);
    gl.ancillaryCollapsedSeries = anc.records;
    gl.ancillaryCollapsedSeriesIndices = anc.indices;
    gl.allSeriesCollapsed = gl.collapsedSeries.length + gl.ancillaryCollapsedSeries.length === w2.config.series.length;
    this.emptyCollapsedSeries(w2.config.series);
  }
  /**
   * @param {string} seriesName
   */
  /**
   * Bridge SVG series-dim state to the canvas renderer: SVG opacity classes
   * (legend-mouseover-inactive) don't touch the painted canvas series layer, so
   * repaint it with a matching per-series opacity. No-op unless the canvas
   * renderer is active. The renderer is mirrored on globals by RendererController
   * (Series has no ctx handle).
   * @param {{active:number, opacity:number}|null} dim
   */
  canvasRestyle(dim) {
    const r2 = this.w.globals.activeRenderer;
    if (r2 && r2.kind === "canvas" && typeof r2.restyle === "function") {
      r2.restyle(dim);
    }
  }
  /**
   * @param {string} seriesName
   */
  highlightSeries(seriesName) {
    var _a;
    const w2 = this.w;
    const targetElement = this.getSeriesByName(seriesName);
    const realIndex = parseInt(
      (_a = targetElement == null ? void 0 : targetElement.getAttribute("data:realIndex")) != null ? _a : "",
      10
    );
    const cacheKey = "highlightSeriesEls";
    let allSeriesEls = w2.globals.cachedSelectors[cacheKey];
    if (!allSeriesEls) {
      allSeriesEls = w2.dom.baseEl.querySelectorAll(
        `.apexcharts-series, .apexcharts-datalabels, .apexcharts-yaxis`
      );
      w2.globals.cachedSelectors[cacheKey] = allSeriesEls;
    }
    let seriesEl = null;
    let dataLabelEl = null;
    let yaxisEl = null;
    if (w2.globals.axisCharts || w2.config.chart.type === "radialBar") {
      if (w2.globals.axisCharts) {
        seriesEl = w2.dom.baseEl.querySelector(
          `.apexcharts-series[data\\:realIndex='${realIndex}']`
        );
        dataLabelEl = w2.dom.baseEl.querySelector(
          `.apexcharts-datalabels[data\\:realIndex='${realIndex}']`
        );
        const yaxisIndex = w2.globals.seriesYAxisReverseMap[realIndex];
        yaxisEl = w2.dom.baseEl.querySelector(
          `.apexcharts-yaxis[rel='${yaxisIndex}']`
        );
      } else {
        seriesEl = w2.dom.baseEl.querySelector(
          `.apexcharts-series[rel='${realIndex + 1}']`
        );
      }
    } else {
      seriesEl = w2.dom.baseEl.querySelector(
        `.apexcharts-series[rel='${realIndex + 1}'] path`
      );
    }
    for (let se2 = 0; se2 < allSeriesEls.length; se2++) {
      const serEl = (
        /** @type {Element} */
        allSeriesEls[se2]
      );
      serEl.classList.add(this.legendInactiveClass);
    }
    if (seriesEl) {
      if (!w2.globals.axisCharts) {
        const parentEl = (
          /** @type {Element} */
          seriesEl.parentNode
        );
        parentEl == null ? void 0 : parentEl.classList.remove(this.legendInactiveClass);
      }
      seriesEl.classList.remove(this.legendInactiveClass);
      if (dataLabelEl !== null) {
        dataLabelEl.classList.remove(this.legendInactiveClass);
      }
      if (yaxisEl !== null) {
        yaxisEl.classList.remove(this.legendInactiveClass);
      }
    } else {
      for (let se2 = 0; se2 < allSeriesEls.length; se2++) {
        const serEl = (
          /** @type {Element} */
          allSeriesEls[se2]
        );
        serEl.classList.remove(this.legendInactiveClass);
      }
    }
    this.canvasRestyle(
      seriesEl && !Number.isNaN(realIndex) ? { active: realIndex, opacity: 0.2 } : null
    );
  }
  /**
   * @param {Event} e
   * @param {any} targetElement
   */
  toggleSeriesOnHover(e2, targetElement) {
    const w2 = this.w;
    if (!targetElement) targetElement = e2.target;
    const allSeriesEls = w2.dom.baseEl.querySelectorAll(
      `.apexcharts-series, .apexcharts-datalabels, .apexcharts-yaxis`
    );
    if (e2.type === "mousemove") {
      const realIndex = parseInt(targetElement.getAttribute("rel"), 10) - 1;
      this.highlightSeries(w2.seriesData.seriesNames[realIndex]);
    } else if (e2.type === "mouseout") {
      for (let se2 = 0; se2 < allSeriesEls.length; se2++) {
        allSeriesEls[se2].classList.remove(this.legendInactiveClass);
      }
      this.canvasRestyle(null);
    }
  }
  /**
   * Dim every heatmap cell except those whose value falls inside the color
   * range at `rangeIndex`. Shared by the categorical legend (hover a legend
   * item) and the gradient legend (hover a band) — both supply an index into
   * `colorScale.ranges` plus an action, decoupling the highlight from any
   * particular DOM element / event shape.
   *
   * @param {number} rangeIndex index into `colorScale.ranges`
   * @param {'highlight'|'reset'} action
   */
  highlightRangeInSeries(rangeIndex, action) {
    const w2 = this.w;
    const allHeatMapElements = w2.dom.baseEl.getElementsByClassName(
      "apexcharts-heatmap-rect"
    );
    const toggleAllInactive = (op) => {
      for (let i2 = 0; i2 < allHeatMapElements.length; i2++) {
        const classList = (
          /** @type {any} */
          allHeatMapElements[i2].classList
        );
        if (typeof classList[op] === "function") {
          classList[op](this.legendInactiveClass);
        }
      }
    };
    if (action === "reset") {
      toggleAllInactive("remove");
      return;
    }
    const ranges = w2.config.plotOptions.heatmap.colorScale.ranges;
    const range = ranges && ranges[rangeIndex];
    if (!range) return;
    toggleAllInactive("add");
    for (let i2 = 0; i2 < allHeatMapElements.length; i2++) {
      const val = Number(allHeatMapElements[i2].getAttribute("val"));
      if (val >= range.from && val <= range.to) {
        allHeatMapElements[i2].classList.remove(this.legendInactiveClass);
      }
    }
  }
  /**
   * @param {string[]} chartTypes
   */
  getActiveConfigSeriesIndex(order = "asc", chartTypes = []) {
    const w2 = this.w;
    let activeIndex = 0;
    if (w2.config.series.length > 1) {
      const activeSeriesIndex = w2.config.series.map((s2, index) => {
        const checkChartType = () => {
          if (w2.globals.comboCharts) {
            return chartTypes.length === 0 || chartTypes.length && chartTypes.indexOf(
              /** @type {Record<string,any>} */
              w2.config.series[index].type
            ) > -1;
          }
          return true;
        };
        const hasData = (
          /** @type {any} */
          s2.data && /** @type {any} */
          s2.data.length > 0 && w2.globals.collapsedSeriesIndices.indexOf(index) === -1
        );
        return hasData && checkChartType() ? index : -1;
      });
      for (let a2 = order === "asc" ? 0 : activeSeriesIndex.length - 1; order === "asc" ? a2 < activeSeriesIndex.length : a2 >= 0; order === "asc" ? a2++ : a2--) {
        if (activeSeriesIndex[a2] !== -1) {
          activeIndex = activeSeriesIndex[a2];
          break;
        }
      }
    }
    return activeIndex;
  }
  /**
   * The highest active series index inside each series group, as an array
   * parallel to `w.labelData.seriesGroups`. Entries are -1 for a group whose
   * every series is collapsed or empty.
   *
   * Same activity test as `getActiveConfigSeriesIndex` (has data and is not
   * legend-collapsed), applied per group so grouped stacked bars can ask which
   * series caps each individual stack rather than the chart as a whole.
   * @param {string[]} chartTypes
   * @returns {number[]}
   */
  getActiveConfigSeriesIndexByGroup(chartTypes = []) {
    const w2 = this.w;
    const groups = w2.labelData.seriesGroups || [];
    return groups.map((group) => {
      let last = -1;
      w2.config.series.forEach((s2, i2) => {
        if (group.indexOf(w2.seriesData.seriesNames[i2]) === -1) return;
        if (w2.globals.comboCharts && chartTypes.length && chartTypes.indexOf(s2.type) === -1) {
          return;
        }
        const hasData = s2.data && s2.data.length > 0 && w2.globals.collapsedSeriesIndices.indexOf(i2) === -1;
        if (hasData) last = i2;
      });
      return last;
    });
  }
  getBarSeriesIndices() {
    const w2 = this.w;
    if (w2.globals.comboCharts) {
      return this.w.config.series.map((s2, i2) => {
        return s2.type === "bar" || s2.type === "column" ? i2 : -1;
      }).filter((i2) => {
        return i2 !== -1;
      });
    }
    return this.w.config.series.map((s2, i2) => {
      return i2;
    });
  }
  getPreviousPaths() {
    var _a, _b, _c, _d;
    const w2 = this.w;
    captureStreamFrame(w2);
    captureAxisChrome(w2);
    captureDataLabels(w2);
    if (!w2.globals.axisCharts) {
      w2.globals.previousPaths = w2.seriesData.series;
      return;
    }
    if (!Utils$1.elementExists(w2.dom.baseEl)) {
      w2.globals.previousPaths = [];
      return;
    }
    w2.globals.previousPaths = [];
    function pushPaths(seriesEls, i2, type) {
      const paths = seriesEls[i2].childNodes;
      const dArr = {
        type,
        paths: (
          /** @type {any[]} */
          []
        ),
        realIndex: seriesEls[i2].getAttribute("data:realIndex")
      };
      for (let j = 0; j < paths.length; j++) {
        if (paths[j].hasAttribute("pathTo")) {
          const d = paths[j].getAttribute("pathTo");
          dArr.paths.push({
            d,
            key: paths[j].getAttribute("data:pathKey"),
            fill: paths[j].getAttribute("fill")
          });
        }
      }
      w2.globals.previousPaths.push(dArr);
    }
    const getPaths = (chartType) => {
      return w2.dom.baseEl.querySelectorAll(
        `.apexcharts-${chartType}-series .apexcharts-series`
      );
    };
    const chartTypes = [
      "line",
      "area",
      "bar",
      "rangebar",
      "rangeArea",
      "candlestick",
      "radar"
    ];
    chartTypes.forEach((type) => {
      const paths = getPaths(type);
      for (let p2 = 0; p2 < paths.length; p2++) {
        pushPaths(paths, p2, type);
      }
    });
    const heatTreeSeries = w2.dom.baseEl.querySelectorAll(
      `.apexcharts-${w2.config.chart.type} .apexcharts-series`
    );
    if (heatTreeSeries.length > 0) {
      for (let h2 = 0; h2 < heatTreeSeries.length; h2++) {
        const seriesEls = w2.dom.baseEl.querySelectorAll(
          `.apexcharts-${w2.config.chart.type} .apexcharts-series[data\\:realIndex='${h2}'] rect`
        );
        const dArr = [];
        for (let i2 = 0; i2 < seriesEls.length; i2++) {
          const getAttr = (x) => {
            return (
              /** @type {Element} */
              seriesEls[i2].getAttribute(x)
            );
          };
          const rect = {
            x: parseFloat((_a = getAttr("x")) != null ? _a : "0"),
            y: parseFloat((_b = getAttr("y")) != null ? _b : "0"),
            width: parseFloat((_c = getAttr("width")) != null ? _c : "0"),
            height: parseFloat((_d = getAttr("height")) != null ? _d : "0")
          };
          dArr.push({
            rect,
            color: seriesEls[i2].getAttribute("color")
          });
        }
        w2.globals.previousPaths.push(dArr);
      }
    }
  }
  clearPreviousPaths() {
    const w2 = this.w;
    w2.globals.previousPaths = [];
    w2.globals.allSeriesCollapsed = false;
  }
  handleNoData() {
    const w2 = this.w;
    const me = this;
    const noDataOpts = w2.config.noData;
    const graphics = new Graphics(me.w);
    let x = w2.globals.svgWidth / 2;
    let y = w2.globals.svgHeight / 2;
    let textAnchor = "middle";
    w2.globals.noData = true;
    w2.globals.animationEnded = true;
    if (noDataOpts.align === "left") {
      x = 10;
      textAnchor = "start";
    } else if (noDataOpts.align === "right") {
      x = w2.globals.svgWidth - 10;
      textAnchor = "end";
    }
    if (noDataOpts.verticalAlign === "top") {
      y = 50;
    } else if (noDataOpts.verticalAlign === "bottom") {
      y = w2.globals.svgHeight - 50;
    }
    x = x + noDataOpts.offsetX;
    y = y + parseInt(noDataOpts.style.fontSize, 10) + 2 + noDataOpts.offsetY;
    if (noDataOpts.text !== void 0 && noDataOpts.text !== "") {
      const titleText = graphics.drawText({
        x,
        y,
        text: noDataOpts.text,
        textAnchor,
        fontSize: noDataOpts.style.fontSize,
        fontFamily: noDataOpts.style.fontFamily,
        foreColor: noDataOpts.style.color,
        opacity: 1,
        cssClass: "apexcharts-text-nodata"
      });
      w2.dom.Paper.add(titleText);
    }
  }
  // When user clicks on legends, the collapsed series is filled with [0,0,0,...,0]
  // This is because we don't want to alter the series' length as it is used at many places
  /**
   * @param {any[]} series
   */
  setNullSeriesToZeroValues(series) {
    const w2 = this.w;
    for (let sl = 0; sl < series.length; sl++) {
      if (series[sl].length === 0) {
        for (let j = 0; j < series[w2.globals.maxValsInArrayIndex].length; j++) {
          series[sl].push(0);
        }
      }
    }
    return series;
  }
  hasAllSeriesEqualX() {
    let equalLen = true;
    const w2 = this.w;
    const filteredSerX = this.filteredSeriesX();
    for (let i2 = 0; i2 < filteredSerX.length - 1; i2++) {
      if (filteredSerX[i2][0] !== filteredSerX[i2 + 1][0]) {
        equalLen = false;
        break;
      }
    }
    w2.globals.allSeriesHasEqualX = equalLen;
    return equalLen;
  }
  filteredSeriesX() {
    const w2 = this.w;
    const filteredSeriesX = w2.seriesData.seriesX.map(
      (ser) => ser.length > 0 ? ser : []
    );
    return filteredSeriesX;
  }
}
const TOKEN_MAP = {
  accent: "--apx-accent",
  fore: "--apx-fore",
  grid: "--apx-grid",
  surface: "--apx-surface"
};
const MAX_SERIES_TOKENS = 24;
function readTokens(w2) {
  if (!Environment.isBrowser()) return {};
  const el = w2.dom && (w2.dom.elWrap || w2.dom.baseEl) || null;
  if (!el) return {};
  const cs = BrowserAPIs.getComputedStyle(el);
  if (!cs || typeof /** @type {any} */
  cs.getPropertyValue !== "function") {
    return {};
  }
  const read = (name2) => {
    const v2 = (
      /** @type {any} */
      cs.getPropertyValue(name2)
    );
    return v2 ? String(v2).trim() : "";
  };
  const out = {};
  for (const key in TOKEN_MAP) {
    const v2 = read(
      /** @type {any} */
      TOKEN_MAP[key]
    );
    if (v2) out[key] = v2;
  }
  const series = [];
  for (let i2 = 1; i2 <= MAX_SERIES_TOKENS; i2++) {
    const v2 = read(`--apx-series-${i2}`);
    if (!v2) break;
    series.push(v2);
  }
  if (series.length) out.series = series;
  return out;
}
const THEME_KEY = "__apexcharts_themes__";
if (!/** @type {any} */
globalThis[THEME_KEY]) {
  globalThis[THEME_KEY] = {};
}
function getThemes() {
  return (
    /** @type {any} */
    globalThis[THEME_KEY]
  );
}
function registerTheme(name2, def) {
  if (!name2 || typeof name2 !== "string") {
    console.warn("ApexCharts: registerTheme requires a non-empty name.");
    return;
  }
  if (def != null && (typeof def !== "object" || Array.isArray(def))) {
    console.warn(
      `ApexCharts: registerTheme("${name2}") expects an object like { mode, palette, tokens, monochrome, accessibility }.`
    );
    return;
  }
  getThemes()[name2] = def || {};
}
function getTheme(name2) {
  if (!name2) return null;
  return getThemes()[name2] || null;
}
function unregisterTheme(name2) {
  if (!name2) return;
  delete getThemes()[name2];
}
const DEFAULT_FORECOLOR_LIGHT = "#373d3f";
const DEFAULT_FORECOLOR_DARK = "#f6f7f8";
const DEFAULT_AXIS_GRID = "#e0e0e0";
class Theme {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.colors = [];
    this.isColorFn = false;
    this.isHeatmapDistributed = this.checkHeatmapDistributed();
    this.isBarDistributed = this.checkBarDistributed();
    this._tokens = {};
    this._namedTheme = null;
  }
  checkHeatmapDistributed() {
    const { chart, plotOptions } = this.w.config;
    return chart.type === "treemap" && plotOptions.treemap && plotOptions.treemap.distributed || chart.type === "heatmap" && plotOptions.heatmap && plotOptions.heatmap.distributed;
  }
  checkBarDistributed() {
    const { chart, plotOptions } = this.w.config;
    return plotOptions.bar && plotOptions.bar.distributed && (chart.type === "bar" || chart.type === "rangeBar");
  }
  init() {
    this.setDefaultColors();
  }
  setDefaultColors() {
    var _a;
    const w2 = this.w;
    const utils = new Utils$1();
    this._namedTheme = getTheme(w2.config.theme.name);
    this._applyNamedThemeMode();
    w2.dom.elWrap.classList.add(
      `apexcharts-theme-${w2.config.theme.mode || "light"}`
    );
    this._applyModeDefaults();
    this._tokens = this._resolveTokens();
    this.applyTokenChrome(this._tokens);
    const colorBlindMode = (_a = w2.config.theme.accessibility) == null ? void 0 : _a.colorBlindMode;
    if (colorBlindMode) {
      w2.globals.colors = this.getColorBlindColors(colorBlindMode);
      this.applySeriesColors(w2.seriesData.seriesColors, w2.globals.colors);
      const defaultColors2 = w2.globals.colors.slice();
      this.pushExtraColors(w2.globals.colors);
      this.applyColorTypes(["fill", "stroke"], defaultColors2);
      this.applyDataLabelsColors(defaultColors2);
      this.applyRadarPolygonsColors();
      this.applyMarkersColors(defaultColors2);
      if (colorBlindMode === "highContrast") {
        w2.dom.elWrap.classList.add("apexcharts-high-contrast");
      }
      return;
    }
    const configColors = [...w2.config.colors || w2.config.fill.colors || []];
    w2.globals.colors = this.getColors(configColors);
    this.applySeriesColors(w2.seriesData.seriesColors, w2.globals.colors);
    if (w2.config.theme.monochrome.enabled) {
      w2.globals.colors = this.getMonochromeColors(
        w2.config.theme.monochrome,
        w2.seriesData.series,
        utils
      );
    }
    const defaultColors = w2.globals.colors.slice();
    this.pushExtraColors(w2.globals.colors);
    this.applyColorTypes(["fill", "stroke"], defaultColors);
    this.applyDataLabelsColors(defaultColors);
    this.applyRadarPolygonsColors();
    this.applyMarkersColors(defaultColors);
  }
  /**
   * Facet (#13): normalize the mode's concrete defaults (foreColor + palette +
   * tooltip theme) at render time. Only overwrites a value still at its
   * opposite-mode default sentinel, so an explicit user value or a value
   * already normalized by checkForDarkTheme/updateThemeOptions is untouched.
   */
  _applyModeDefaults() {
    const w2 = this.w;
    const mode = w2.config.theme.mode;
    if (mode === "dark") {
      if (w2.config.chart.foreColor === DEFAULT_FORECOLOR_LIGHT) {
        w2.config.chart.foreColor = DEFAULT_FORECOLOR_DARK;
      }
      if (w2.config.theme.palette === "palette1") {
        w2.config.theme.palette = "palette4";
      }
      if (w2.config.tooltip && w2.config.tooltip.theme !== "light") {
        w2.config.tooltip.theme = "dark";
      }
    } else if (mode === "light") {
      if (w2.config.chart.foreColor === DEFAULT_FORECOLOR_DARK) {
        w2.config.chart.foreColor = DEFAULT_FORECOLOR_LIGHT;
      }
      if (w2.config.theme.palette === "palette4") {
        w2.config.theme.palette = "palette1";
      }
    }
  }
  /**
   * Facet (#13): apply a registered named theme's mode / accessibility /
   * monochrome, each only when the user (or the OS watcher) has not set it, so
   * explicit config and `follow:'os'` both win over the named theme.
   */
  _applyNamedThemeMode() {
    const named = this._namedTheme;
    if (!named) return;
    const theme = this.w.config.theme;
    if (named.mode && !theme.mode) {
      theme.mode = named.mode;
    }
    if (named.accessibility && named.accessibility.colorBlindMode && !(theme.accessibility && theme.accessibility.colorBlindMode)) {
      theme.accessibility = theme.accessibility || {};
      theme.accessibility.colorBlindMode = named.accessibility.colorBlindMode;
    }
    if (named.monochrome && named.monochrome.enabled && !theme.monochrome.enabled) {
      theme.monochrome = __spreadValues(__spreadValues({}, theme.monochrome), named.monochrome);
    }
  }
  /**
   * Facet (#13): the effective token set. CSS `--apx-*` tokens (when enabled)
   * layer over the named theme's `tokens`, so a page-level token overrides a
   * registered brand default.
   * @returns {{accent?:string, fore?:string, grid?:string, surface?:string, series?:string[]}}
   */
  _resolveTokens() {
    const named = this._namedTheme && this._namedTheme.tokens || {};
    const css = this._shouldUseTokens() ? readTokens(this.w) : {};
    return __spreadValues(__spreadValues({}, named), css);
  }
  /**
   * Facet (#13): tokens are on unless explicitly disabled (`theme.tokens:false`).
   * `true` is the default (the legacy `'auto'` value is accepted and means the
   * same); `readTokens` returns only the tokens actually present, so absence
   * is a no-op.
   * @returns {boolean}
   */
  _shouldUseTokens() {
    return this.w.config.theme.tokens !== false;
  }
  /**
   * Facet (#13): overwrite chrome defaults with `--apx-*` tokens, but only where
   * the value still equals its built-in default (so explicit config wins).
   * @param {{fore?:string, grid?:string, surface?:string}} tokens
   */
  applyTokenChrome(tokens) {
    if (!tokens) return;
    const w2 = this.w;
    if (tokens.fore && (w2.config.chart.foreColor === DEFAULT_FORECOLOR_LIGHT || w2.config.chart.foreColor === DEFAULT_FORECOLOR_DARK)) {
      w2.config.chart.foreColor = tokens.fore;
    }
    if (tokens.grid) {
      if (w2.config.grid.borderColor === DEFAULT_AXIS_GRID) {
        w2.config.grid.borderColor = tokens.grid;
      }
      const applyAxis = (axis) => {
        if (!axis) return;
        if (axis.axisBorder && axis.axisBorder.color === DEFAULT_AXIS_GRID) {
          axis.axisBorder.color = tokens.grid;
        }
        if (axis.axisTicks && axis.axisTicks.color === DEFAULT_AXIS_GRID) {
          axis.axisTicks.color = tokens.grid;
        }
      };
      applyAxis(w2.config.xaxis);
      if (Array.isArray(w2.config.yaxis)) {
        w2.config.yaxis.forEach(applyAxis);
      } else {
        applyAxis(w2.config.yaxis);
      }
    }
    const appliedSurface = w2.globals.tokenSurface;
    const currentBg = w2.config.chart.background;
    const isOurs = !currentBg || currentBg === appliedSurface;
    if (tokens.surface) {
      if (isOurs) {
        w2.config.chart.background = tokens.surface;
        w2.globals.tokenSurface = tokens.surface;
        const paperNode = w2.dom.Paper && w2.dom.Paper.node;
        if (paperNode && paperNode.style) {
          paperNode.style.background = tokens.surface;
        }
      }
    } else if (appliedSurface && currentBg === appliedSurface) {
      w2.config.chart.background = "";
      w2.globals.tokenSurface = void 0;
      const paperNode = w2.dom.Paper && w2.dom.Paper.node;
      if (paperNode && paperNode.style) {
        paperNode.style.background = "";
      }
    }
  }
  /**
   * @param {any[]} configColors
   */
  getColors(configColors) {
    const w2 = this.w;
    if (!configColors || configColors.length === 0) {
      return this.predefined();
    }
    if (Array.isArray(configColors) && configColors.length > 0 && typeof configColors[0] === "function") {
      this.isColorFn = true;
      return w2.config.series.map((s2, i2) => {
        const c2 = configColors[i2] || configColors[0];
        return typeof c2 === "function" ? c2({
          value: w2.globals.axisCharts ? w2.seriesData.series[i2][0] || 0 : w2.seriesData.series[i2],
          seriesIndex: i2,
          dataPointIndex: i2,
          w: this.w
        }) : c2;
      });
    }
    return configColors;
  }
  /**
   * @param {any[]} seriesColors
   * @param {any[]} globalsColors
   */
  applySeriesColors(seriesColors, globalsColors) {
    seriesColors.forEach((c2, i2) => {
      if (c2) {
        globalsColors[i2] = c2;
      }
    });
  }
  /**
   * @param {Record<string, any>} monochrome
   * @param {any[]} series
   * @param {any} utils
   */
  getMonochromeColors(monochrome, series, utils) {
    const { color, shadeIntensity, shadeTo } = monochrome;
    const glsCnt = this.isBarDistributed || this.isHeatmapDistributed ? series[0].length * series.length : series.length;
    const part = 1 / (glsCnt / shadeIntensity);
    let percent = 0;
    return Array.from({ length: glsCnt }, () => {
      const newColor = shadeTo === "dark" ? utils.shadeColor(percent * -1, color) : utils.shadeColor(percent, color);
      percent += part;
      return newColor;
    });
  }
  /**
   * @param {string[]} colorTypes
   * @param {string[]} defaultColors
   */
  applyColorTypes(colorTypes, defaultColors) {
    const w2 = this.w;
    colorTypes.forEach((c2) => {
      w2.globals[c2].colors = w2.config[c2].colors === void 0 ? this.isColorFn ? w2.config.colors : defaultColors : w2.config[c2].colors.slice();
      this.pushExtraColors(
        /** @type {Record<string,any>} */
        w2.globals[c2].colors
      );
    });
  }
  /**
   * @param {string[]} defaultColors
   */
  applyDataLabelsColors(defaultColors) {
    const w2 = this.w;
    w2.globals.dataLabels.style.colors = w2.config.dataLabels.style.colors === void 0 ? defaultColors : w2.config.dataLabels.style.colors.slice();
    this.pushExtraColors(w2.globals.dataLabels.style.colors, 50);
  }
  applyRadarPolygonsColors() {
    const w2 = this.w;
    w2.globals.radarPolygons.fill.colors = w2.config.plotOptions.radar.polygons.fill.colors === void 0 ? [w2.config.theme.mode === "dark" ? "#343A3F" : "none"] : w2.config.plotOptions.radar.polygons.fill.colors.slice();
    this.pushExtraColors(w2.globals.radarPolygons.fill.colors, 20);
  }
  /**
   * @param {string[]} defaultColors
   */
  applyMarkersColors(defaultColors) {
    const w2 = this.w;
    w2.globals.markers.colors = w2.config.markers.colors === void 0 ? defaultColors : w2.config.markers.colors.slice();
    this.pushExtraColors(w2.globals.markers.colors);
  }
  /**
   * @param {any} colorSeries
   * @param {number} [length]
   * @param {boolean | null} [distributed]
   */
  pushExtraColors(colorSeries, length, distributed = null) {
    const w2 = this.w;
    let len = length || w2.seriesData.series.length;
    if (distributed === null) {
      distributed = this.isBarDistributed || this.isHeatmapDistributed || w2.config.chart.type === "heatmap" && w2.config.plotOptions.heatmap && w2.config.plotOptions.heatmap.colorScale.inverse;
    }
    if (distributed && w2.seriesData.series.length) {
      len = w2.seriesData.series[w2.globals.maxValsInArrayIndex].length * w2.seriesData.series.length;
    }
    if (colorSeries.length < len) {
      const diff = len - colorSeries.length;
      for (let i2 = 0; i2 < diff; i2++) {
        colorSeries.push(colorSeries[i2]);
      }
    }
  }
  /**
   * @param {'light' | 'dark'} mode
   */
  getColorBlindColors(mode) {
    const palettes = getThemePalettes();
    const map = {
      deuteranopia: palettes.cvdDeuteranopia,
      protanopia: palettes.cvdProtanopia,
      tritanopia: palettes.cvdTritanopia,
      highContrast: palettes.highContrast
    };
    return (
      /** @type {Record<string,any>} */
      /** @type {any} */
      (map[mode] || palettes.palette1).slice()
    );
  }
  /**
   * @param {Record<string, any>} options
   */
  updateThemeOptions(options2) {
    options2.chart = options2.chart || {};
    options2.tooltip = options2.tooltip || {};
    const mode = options2.theme.mode;
    const palette = mode === "dark" ? "palette4" : mode === "light" ? "palette1" : options2.theme.palette || "palette1";
    const foreColor = mode === "dark" ? "#f6f7f8" : mode === "light" ? "#373d3f" : options2.chart.foreColor || "#373d3f";
    options2.tooltip.theme = mode || "light";
    options2.chart.foreColor = foreColor;
    options2.theme.palette = palette;
    return options2;
  }
  predefined() {
    const palette = this.w.config.theme.palette;
    const palettes = getThemePalettes();
    const builtin = (
      /** @type {Record<string,any>} */
      palettes[palette] || palettes.palette1
    );
    const tokens = this._tokens || {};
    if (Array.isArray(tokens.series) && tokens.series.length) {
      return tokens.series.slice();
    }
    if (tokens.accent) {
      return [tokens.accent, ...builtin];
    }
    const named = this._namedTheme;
    if (named && Array.isArray(named.palette) && named.palette.length) {
      return named.palette.slice();
    }
    return builtin;
  }
}
class TitleSubtitle {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
  }
  draw() {
    this.drawTitleSubtitle("title");
    this.drawTitleSubtitle("subtitle");
  }
  /**
   * @param {'title' | 'subtitle'} type
   */
  drawTitleSubtitle(type) {
    const w2 = this.w;
    const tsConfig = type === "title" ? w2.config.title : w2.config.subtitle;
    let x = w2.globals.svgWidth / 2;
    let y = tsConfig.offsetY;
    let textAnchor = "middle";
    if (tsConfig.align === "left") {
      x = 10;
      textAnchor = "start";
    } else if (tsConfig.align === "right") {
      x = w2.globals.svgWidth - 10;
      textAnchor = "end";
    }
    x = x + tsConfig.offsetX;
    y = y + parseInt(tsConfig.style.fontSize, 10) + tsConfig.margin / 2;
    if (tsConfig.text !== void 0) {
      const graphics = new Graphics(this.w);
      const titleText = graphics.drawText({
        x,
        y,
        text: tsConfig.text,
        textAnchor,
        fontSize: tsConfig.style.fontSize,
        fontFamily: tsConfig.style.fontFamily,
        fontWeight: tsConfig.style.fontWeight,
        foreColor: tsConfig.style.color,
        opacity: 1
      });
      titleText.node.setAttribute("class", `apexcharts-${type}-text`);
      w2.dom.Paper.add(titleText);
    }
  }
}
let Helpers$4 = class Helpers {
  /**
   * @param {import('./Dimensions').default} dCtx
   */
  constructor(dCtx) {
    this.w = dCtx.w;
    this.dCtx = dCtx;
  }
  /**
   * Get Chart Title/Subtitle Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   * @param {string} type
   **/
  getTitleSubtitleCoords(type) {
    const w2 = this.w;
    let width = 0;
    let height = 0;
    const floating = type === "title" ? w2.config.title.floating : w2.config.subtitle.floating;
    const el = w2.dom.baseEl.querySelector(`.apexcharts-${type}-text`);
    if (el !== null && !floating) {
      const coord = el.getBoundingClientRect();
      width = coord.width;
      height = w2.globals.axisCharts ? coord.height + 5 : coord.height;
    }
    return {
      width,
      height
    };
  }
  getLegendsRect() {
    const w2 = this.w;
    const elLegendWrap = w2.dom.elLegendWrap;
    if (!w2.config.legend.height && (w2.config.legend.position === "top" || w2.config.legend.position === "bottom")) {
      if (elLegendWrap)
        elLegendWrap.style.maxHeight = w2.globals.svgHeight / 2 + "px";
    }
    const lgRect = (
      /** @type {any} */
      Object.assign({}, Utils$1.getBoundingClientRect(elLegendWrap))
    );
    if (elLegendWrap !== null && !w2.config.legend.floating && w2.config.legend.show) {
      this.dCtx.lgRect = {
        x: lgRect.x,
        y: lgRect.y,
        height: lgRect.height,
        width: lgRect.height === 0 ? 0 : lgRect.width
      };
    } else {
      this.dCtx.lgRect = {
        x: 0,
        y: 0,
        height: 0,
        width: 0
      };
    }
    if (w2.config.legend.position === "left" || w2.config.legend.position === "right") {
      if (this.dCtx.lgRect.width * 1.5 > w2.globals.svgWidth) {
        this.dCtx.lgRect.width = w2.globals.svgWidth / 1.5;
      }
    }
    return this.dCtx.lgRect;
  }
  /**
   * Get Y Axis Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   **/
  getDatalabelsRect() {
    const w2 = this.w;
    const allLabels = [];
    w2.config.series.forEach(
      (serie, seriesIndex) => {
        serie.data.forEach(
          (datum, dataPointIndex) => {
            const getText = (v2) => {
              return w2.config.dataLabels.formatter(v2, {
                seriesIndex,
                dataPointIndex,
                w: w2
              });
            };
            const labelText = getText(
              w2.seriesData.series[seriesIndex][dataPointIndex]
            );
            allLabels.push(labelText);
          }
        );
      }
    );
    const val = Utils$1.getLargestStringFromArr(allLabels);
    const graphics = new Graphics(this.w);
    const dataLabelsStyle = w2.config.dataLabels.style;
    const labelrect = graphics.getTextRects(
      val,
      parseInt(dataLabelsStyle.fontSize).toString(),
      dataLabelsStyle.fontFamily
    );
    return {
      width: labelrect.width * 1.05,
      height: labelrect.height
    };
  }
  /**
   * @param {any} val
   * @param {any[]} arr
   */
  getLargestStringFromMultiArr(val, arr) {
    const w2 = this.w;
    let valArr = val;
    if (w2.axisFlags.isMultiLineX) {
      const maxArrs = arr.map((xl) => {
        return Array.isArray(xl) ? xl.length : 1;
      });
      const maxArrLen = Math.max(...maxArrs);
      const maxArrIndex = maxArrs.indexOf(maxArrLen);
      valArr = arr[maxArrIndex];
    }
    return valArr;
  }
  /**
   * Vertical space a sparkline has to keep free inside its SVG so the series
   * stroke isn't clipped at the top / bottom edge.
   *
   * A stroke is centred on its path, so half of it hangs outside the plot
   * wherever the path runs along an edge. Reserving that half unconditionally
   * lifts the whole plot away from the SVG edges even when nothing is drawn
   * there, which shows up as a strip of empty space under an area fill or a
   * bar base (#5137). So reserve only what the ink can't absorb itself: where
   * the stroke traces the data points, the distance between the extreme datum
   * and the axis extreme already swallows part or all of the overhang. Fills
   * need nothing: area fills are drawn unstroked (see Line.js renderPaths).
   *
   * @returns {{ top: number, bottom: number }}
   **/
  getSparklineStrokeInset() {
    const w2 = this.w;
    const maxStrokeWidth = Array.isArray(w2.config.stroke.width) ? Math.max(...w2.config.stroke.width) : w2.config.stroke.width;
    const half = maxStrokeWidth / 2;
    if (!w2.config.stroke.show || !(half > 0)) {
      return { top: 0, bottom: 0 };
    }
    const yRange = w2.globals.maxY - w2.globals.minY;
    const extremes = this._getSeriesYExtremes();
    if (!this._strokeTracesDataPoints() || !(yRange > 0) || !extremes) {
      return { top: half, bottom: half };
    }
    const plotHeight = Math.max(w2.globals.svgHeight - maxStrokeWidth, 0);
    const roomAbove = plotHeight * (w2.globals.maxY - extremes.max) / yRange;
    const roomBelow = plotHeight * (extremes.min - w2.globals.minY) / yRange;
    return {
      top: Math.min(Math.max(half - roomAbove, 0), half),
      bottom: Math.min(Math.max(half - roomBelow, 0), half)
    };
  }
  /**
   * Whether every drawn series is one whose stroke follows the data points, so
   * the extreme datum marks the outermost ink. False for anything that strokes
   * to the baseline or fills the plot (bar, heatmap, ...) and for stacked
   * charts, where the ink is the cumulative total rather than the raw values.
   * @returns {boolean}
   **/
  _strokeTracesDataPoints() {
    const w2 = this.w;
    if (!w2.globals.axisCharts || w2.config.chart.stacked) return false;
    const tracesPoints = ["line", "area", "scatter"];
    return (
      /** @type {any[]} */
      w2.config.series.every(
        (s2) => tracesPoints.includes(s2.type || w2.config.chart.type)
      )
    );
  }
  /**
   * Min / max across every plotted y value, or null when nothing is plottable.
   * @returns {{ min: number, max: number } | null}
   **/
  _getSeriesYExtremes() {
    let min = Infinity;
    let max = -Infinity;
    this.w.seriesData.series.forEach((data) => {
      if (!Array.isArray(data)) return;
      data.forEach((val) => {
        if (!Number.isFinite(val)) return;
        if (val < min) min = val;
        if (val > max) max = val;
      });
    });
    return min === Infinity ? null : { min, max };
  }
};
class DimXAxis {
  /**
   * @param {import('./Dimensions').default} dCtx
   */
  constructor(dCtx) {
    this.w = dCtx.w;
    this.dCtx = dCtx;
  }
  /**
   * Get X Axis Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   **/
  getxAxisLabelsCoords() {
    const w2 = this.w;
    let xaxisLabels = w2.labelData.labels.slice();
    if (w2.config.xaxis.convertedCatToNumeric && xaxisLabels.length === 0) {
      xaxisLabels = w2.labelData.categoryLabels;
    }
    let rect;
    if (w2.labelData.timescaleLabels.length > 0) {
      const coords = this.getxAxisTimeScaleLabelsCoords();
      rect = {
        width: coords.width,
        height: coords.height
      };
      w2.layout.rotateXLabels = false;
    } else {
      this.dCtx.lgWidthForSideLegends = (w2.config.legend.position === "left" || w2.config.legend.position === "right") && !w2.config.legend.floating ? this.dCtx.lgRect.width : 0;
      const xlbFormatter = w2.formatters.xLabelFormatter;
      let val = Utils$1.getLargestStringFromArr(xaxisLabels);
      let valArr = this.dCtx.dimHelpers.getLargestStringFromMultiArr(
        val,
        xaxisLabels
      );
      if (w2.globals.isBarHorizontal) {
        val = w2.globals.yAxisScale[0].result.reduce(
          (a2, b2) => a2.length > b2.length ? a2 : b2,
          0
        );
        valArr = val;
      }
      const xFormat = new Formatters(this.w);
      const timestamp = val;
      val = xFormat.xLabelFormat(
        /** @type {Function} */
        xlbFormatter,
        val,
        timestamp,
        {
          i: void 0,
          dateFormatter: new DateTime(this.w).formatDate,
          w: w2
        }
      );
      valArr = xFormat.xLabelFormat(
        /** @type {Function} */
        xlbFormatter,
        valArr,
        timestamp,
        {
          i: void 0,
          dateFormatter: new DateTime(this.w).formatDate,
          w: w2
        }
      );
      if (w2.config.xaxis.convertedCatToNumeric && typeof val === "undefined" || String(val).trim() === "") {
        val = "1";
        valArr = val;
      }
      const graphics = new Graphics(this.w);
      let xLabelrect = graphics.getTextRects(
        val,
        w2.config.xaxis.labels.style.fontSize
      );
      let xArrLabelrect = xLabelrect;
      if (val !== valArr) {
        xArrLabelrect = graphics.getTextRects(
          valArr,
          w2.config.xaxis.labels.style.fontSize
        );
      }
      rect = {
        width: xLabelrect.width >= xArrLabelrect.width ? xLabelrect.width : xArrLabelrect.width,
        height: xLabelrect.height >= xArrLabelrect.height ? xLabelrect.height : xArrLabelrect.height
      };
      if (rect.width * xaxisLabels.length > w2.globals.svgWidth - this.dCtx.lgWidthForSideLegends - this.dCtx.yAxisWidth - this.dCtx.gridPad.left - this.dCtx.gridPad.right && w2.config.xaxis.labels.rotate !== 0 || w2.config.xaxis.labels.rotateAlways) {
        if (!w2.globals.isBarHorizontal) {
          w2.layout.rotateXLabels = true;
          const getRotatedTextRects = (text) => {
            return graphics.getTextRects(
              text,
              w2.config.xaxis.labels.style.fontSize,
              w2.config.xaxis.labels.style.fontFamily,
              `rotate(${w2.config.xaxis.labels.rotate} 0 0)`,
              false
            );
          };
          xLabelrect = getRotatedTextRects(val);
          if (val !== valArr) {
            xArrLabelrect = getRotatedTextRects(valArr);
          }
          rect.height = (xLabelrect.height > xArrLabelrect.height ? xLabelrect.height : xArrLabelrect.height) / 1.5;
          rect.width = xLabelrect.width > xArrLabelrect.width ? xLabelrect.width : xArrLabelrect.width;
        }
      } else {
        w2.layout.rotateXLabels = false;
      }
    }
    if (!w2.config.xaxis.labels.show) {
      rect = {
        width: 0,
        height: 0
      };
    }
    return {
      width: rect.width,
      height: rect.height
    };
  }
  /**
   * Get X Axis Label Group height
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   */
  getxAxisGroupLabelsCoords() {
    var _a;
    const w2 = this.w;
    if (!w2.labelData.hasXaxisGroups) {
      return { width: 0, height: 0 };
    }
    const fontSize = ((_a = w2.config.xaxis.group.style) == null ? void 0 : _a.fontSize) || w2.config.xaxis.labels.style.fontSize;
    const xaxisLabels = w2.labelData.groups.map(
      (g2) => g2.title
    );
    let rect;
    const val = Utils$1.getLargestStringFromArr(xaxisLabels);
    const valArr = this.dCtx.dimHelpers.getLargestStringFromMultiArr(
      val,
      xaxisLabels
    );
    const graphics = new Graphics(this.w);
    const xLabelrect = graphics.getTextRects(val, fontSize);
    let xArrLabelrect = xLabelrect;
    if (val !== valArr) {
      xArrLabelrect = graphics.getTextRects(valArr, fontSize);
    }
    rect = {
      width: xLabelrect.width >= xArrLabelrect.width ? xLabelrect.width : xArrLabelrect.width,
      height: xLabelrect.height >= xArrLabelrect.height ? xLabelrect.height : xArrLabelrect.height
    };
    if (!w2.config.xaxis.labels.show) {
      rect = {
        width: 0,
        height: 0
      };
    }
    return {
      width: rect.width,
      height: rect.height
    };
  }
  /**
   * Get X Axis Title Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   **/
  getxAxisTitleCoords() {
    const w2 = this.w;
    let width = 0;
    let height = 0;
    if (w2.config.xaxis.title.text !== void 0) {
      const graphics = new Graphics(this.w);
      const rect = graphics.getTextRects(
        w2.config.xaxis.title.text,
        w2.config.xaxis.title.style.fontSize
      );
      width = rect.width;
      height = rect.height;
    }
    return {
      width,
      height
    };
  }
  getxAxisTimeScaleLabelsCoords() {
    const w2 = this.w;
    this.dCtx.timescaleLabels = w2.labelData.timescaleLabels.slice();
    const labels = this.dCtx.timescaleLabels.map(
      (label) => label.value
    );
    const val = labels.reduce((a2, b2) => {
      if (typeof a2 === "undefined") {
        console.error(
          "You have possibly supplied invalid Date format. Please supply a valid JavaScript Date"
        );
        return 0;
      } else {
        return a2.length > b2.length ? a2 : b2;
      }
    }, 0);
    const graphics = new Graphics(this.w);
    const rect = graphics.getTextRects(
      val,
      w2.config.xaxis.labels.style.fontSize
    );
    const totalWidthRotated = rect.width * 1.05 * labels.length;
    if (totalWidthRotated > w2.layout.gridWidth && w2.config.xaxis.labels.rotate !== 0) {
      w2.globals.overlappingXLabels = true;
    }
    return rect;
  }
  // In certain cases, the last labels gets cropped in xaxis.
  // Hence, we add some additional padding based on the label length to avoid the last label being cropped or we don't draw it at all
  /**
   * @param {Record<string, any>} xaxisLabelCoords
   */
  additionalPaddingXLabels(xaxisLabelCoords) {
    const w2 = this.w;
    const gl = w2.globals;
    const cnf = w2.config;
    const xtype = cnf.xaxis.type;
    const lbWidth = xaxisLabelCoords.width;
    gl.skipLastTimelinelabel = false;
    gl.skipFirstTimelinelabel = false;
    const isBarOpposite = w2.config.yaxis[0].opposite && w2.globals.isBarHorizontal;
    const isCollapsed = (i2) => gl.collapsedSeriesIndices.indexOf(i2) !== -1;
    const rightPad = (yaxe) => {
      if (this.dCtx.timescaleLabels && this.dCtx.timescaleLabels.length) {
        const firstimescaleLabel = this.dCtx.timescaleLabels[0];
        const lastTimescaleLabel = this.dCtx.timescaleLabels[this.dCtx.timescaleLabels.length - 1];
        const lastLabelPosition = lastTimescaleLabel.position + lbWidth / 1.75 - this.dCtx.yAxisWidthRight;
        const firstLabelPosition = firstimescaleLabel.position - lbWidth / 1.75 + this.dCtx.yAxisWidthLeft;
        const lgRightRectWidth = w2.config.legend.position === "right" && this.dCtx.lgRect.width > 0 ? this.dCtx.lgRect.width : 0;
        if (lastLabelPosition > gl.svgWidth - w2.layout.translateX - lgRightRectWidth) {
          gl.skipLastTimelinelabel = true;
        }
        if (firstLabelPosition < -((!yaxe.show || yaxe.floating) && (cnf.chart.type === "bar" || cnf.chart.type === "candlestick" || cnf.chart.type === "rangeBar" || cnf.chart.type === "boxPlot" || cnf.chart.type === "violin") ? lbWidth / 1.75 : 10)) {
          gl.skipFirstTimelinelabel = true;
        }
      } else if (xtype === "datetime") {
        if (this.dCtx.gridPad.right < lbWidth && !w2.layout.rotateXLabels) {
          gl.skipLastTimelinelabel = true;
        }
      } else if (xtype !== "datetime") {
        if (this.dCtx.gridPad.right < lbWidth / 2 - this.dCtx.yAxisWidthRight && !w2.layout.rotateXLabels && !w2.config.xaxis.labels.trim) {
          this.dCtx.xPadRight = lbWidth / 2 + 1;
        }
      }
    };
    const padYAxe = (yaxe, i2) => {
      if (cnf.yaxis.length > 1 && isCollapsed(i2)) return;
      rightPad(yaxe);
    };
    cnf.yaxis.forEach((yaxe, i2) => {
      if (isBarOpposite) {
        if (this.dCtx.gridPad.left < lbWidth) {
          this.dCtx.xPadLeft = lbWidth / 2 + 1;
        }
        this.dCtx.xPadRight = lbWidth / 2 + 1;
      } else {
        padYAxe(yaxe, i2);
      }
    });
  }
}
class DimYAxis {
  /**
   * @param {import('./Dimensions').default} dCtx
   */
  constructor(dCtx) {
    this.w = dCtx.w;
    this.dCtx = dCtx;
  }
  /**
   * Get Y Axis Dimensions
   * @memberof Dimensions
   * @returns {Array<{width: number, height: number}>}
   **/
  getyAxisLabelsCoords() {
    const w2 = this.w;
    const width = 0;
    const height = 0;
    const ret = [];
    let labelPad = 10;
    const axesUtils = new AxesUtils(this.w, { theme: this.dCtx.theme, timeScale: this.dCtx.timeScale });
    w2.config.yaxis.map((yaxe, index) => {
      const formatterArgs = {
        seriesIndex: index,
        dataPointIndex: -1,
        w: w2
      };
      const yS = w2.globals.yAxisScale[index];
      let yAxisMinWidth = 0;
      if (!axesUtils.isYAxisHidden(index) && yaxe.labels.show && yaxe.labels.minWidth !== void 0)
        yAxisMinWidth = yaxe.labels.minWidth;
      if (!axesUtils.isYAxisHidden(index) && yaxe.labels.show && yS.result.length) {
        const lbFormatter = w2.formatters.yLabelFormatters[index];
        const minV = yS.niceMin === Number.MIN_VALUE ? 0 : yS.niceMin;
        let val = yS.result.reduce((acc, curr) => {
          var _a, _b;
          return ((_a = String(lbFormatter(acc, formatterArgs))) == null ? void 0 : _a.length) > ((_b = String(lbFormatter(curr, formatterArgs))) == null ? void 0 : _b.length) ? acc : curr;
        }, minV);
        val = lbFormatter(val, formatterArgs);
        let valArr = val;
        if (typeof val === "undefined" || val.length === 0) {
          val = yS.niceMax;
        }
        if (String(val).length === 1) {
          val = val + ".0";
          valArr = val;
        }
        if (w2.globals.isBarHorizontal) {
          labelPad = 0;
          const barYaxisLabels = w2.labelData.labels.slice();
          val = Utils$1.getLargestStringFromArr(barYaxisLabels);
          val = lbFormatter(val, { seriesIndex: index, dataPointIndex: -1, w: w2 });
          valArr = this.dCtx.dimHelpers.getLargestStringFromMultiArr(
            val,
            barYaxisLabels
          );
        }
        const graphics = new Graphics(this.w);
        const rotateStr = "rotate(".concat(yaxe.labels.rotate, " 0 0)");
        const rect = graphics.getTextRects(
          val,
          yaxe.labels.style.fontSize,
          yaxe.labels.style.fontFamily,
          rotateStr,
          false
        );
        let arrLabelrect = rect;
        if (val !== valArr) {
          arrLabelrect = graphics.getTextRects(
            valArr,
            yaxe.labels.style.fontSize,
            yaxe.labels.style.fontFamily,
            rotateStr,
            false
          );
        }
        ret.push({
          width: (yAxisMinWidth > arrLabelrect.width || yAxisMinWidth > rect.width ? yAxisMinWidth : arrLabelrect.width > rect.width ? arrLabelrect.width : rect.width) + labelPad,
          height: arrLabelrect.height > rect.height ? arrLabelrect.height : rect.height
        });
      } else {
        ret.push({
          width,
          height
        });
      }
    });
    return ret;
  }
  /**
   * Get Y Axis Dimensions
   * @memberof Dimensions
   * @returns {Array<{width: number, height: number}>}
   **/
  getyAxisTitleCoords() {
    const w2 = this.w;
    const ret = [];
    w2.config.yaxis.map((yaxe) => {
      if (yaxe.show && yaxe.title.text !== void 0) {
        const graphics = new Graphics(this.w);
        const rotateStr = "rotate(".concat(yaxe.title.rotate, " 0 0)");
        const rect = graphics.getTextRects(
          yaxe.title.text,
          yaxe.title.style.fontSize,
          yaxe.title.style.fontFamily,
          rotateStr,
          false
        );
        ret.push({
          width: rect.width,
          height: rect.height
        });
      } else {
        ret.push({
          width: 0,
          height: 0
        });
      }
    });
    return ret;
  }
  getTotalYAxisWidth() {
    const w2 = this.w;
    let yAxisWidth = 0;
    let yAxisWidthLeft = 0;
    let yAxisWidthRight = 0;
    const padding = w2.globals.yAxisScale.length > 1 ? 10 : 0;
    const axesUtils = new AxesUtils(this.w, { theme: this.dCtx.theme, timeScale: this.dCtx.timeScale });
    const isHiddenYAxis = function(index) {
      return w2.globals.ignoreYAxisIndexes.indexOf(index) > -1;
    };
    const padForLabelTitle = (coord, index) => {
      const floating = w2.config.yaxis[index].floating;
      let width = 0;
      if (coord.width > 0 && !floating) {
        width = coord.width + padding;
        if (isHiddenYAxis(index)) {
          width = width - coord.width - padding;
        }
      } else {
        width = floating || axesUtils.isYAxisHidden(index) ? 0 : 5;
      }
      w2.config.yaxis[index].opposite ? yAxisWidthRight = yAxisWidthRight + width : yAxisWidthLeft = yAxisWidthLeft + width;
      yAxisWidth = yAxisWidth + width;
    };
    w2.layout.yLabelsCoords.map((yLabelCoord, index) => {
      padForLabelTitle(yLabelCoord, index);
    });
    w2.layout.yTitleCoords.map((yTitleCoord, index) => {
      padForLabelTitle(yTitleCoord, index);
    });
    if (w2.globals.isBarHorizontal && !w2.config.yaxis[0].floating) {
      yAxisWidth = w2.layout.yLabelsCoords[0].width + w2.layout.yTitleCoords[0].width + 15;
    }
    this.dCtx.yAxisWidthLeft = yAxisWidthLeft;
    this.dCtx.yAxisWidthRight = yAxisWidthRight;
    return yAxisWidth;
  }
}
class DimGrid {
  /**
   * @param {import('./Dimensions').default} dCtx
   */
  constructor(dCtx) {
    this.w = dCtx.w;
    this.dCtx = dCtx;
  }
  /**
   * @param {number} gridWidth
   */
  gridPadForColumnsInNumericAxis(gridWidth) {
    const { w: w2 } = this;
    const { config: cnf, globals: gl } = w2;
    if (gl.noData || gl.collapsedSeries.length + gl.ancillaryCollapsedSeries.length === cnf.series.length) {
      return 0;
    }
    const hasBar = (type2) => ["bar", "rangeBar", "candlestick", "boxPlot", "violin"].includes(type2);
    const type = cnf.chart.type;
    let barWidth = 0;
    let seriesLen = hasBar(type) ? cnf.series.length : 1;
    if (gl.comboBarCount > 0) {
      seriesLen = gl.comboBarCount;
    }
    gl.collapsedSeries.forEach((c2) => {
      if (hasBar(c2.type)) {
        seriesLen -= 1;
      }
    });
    if (cnf.chart.stacked) {
      seriesLen = 1;
    }
    const barsPresent = hasBar(type) || gl.comboBarCount > 0;
    let xRange = Math.abs(gl.initialMaxX - gl.initialMinX);
    if (barsPresent && w2.axisFlags.isXNumeric && !gl.isBarHorizontal && seriesLen > 0 && xRange !== 0) {
      if (xRange <= 3) {
        xRange = gl.dataPoints;
      }
      const xRatio = xRange / gridWidth;
      let xDivision = gl.minXDiff && gl.minXDiff / xRatio > 0 ? gl.minXDiff / xRatio : 0;
      if (xDivision > gridWidth / 2) {
        xDivision /= 2;
      }
      barWidth = xDivision * parseInt(cnf.plotOptions.bar.columnWidth, 10) / 100;
      if (barWidth < 1) {
        barWidth = 1;
      }
      gl.barPadForNumericAxis = barWidth;
    }
    return barWidth;
  }
  /**
   * Reserve room to the right of the plot for stacked *total* dataLabels on a
   * 100% horizontal bar chart.
   *
   * The total label is placed just past the end of the stack. Under
   * `stackType: '100%'` every stack ends at the axis maximum, i.e. exactly at
   * the right edge of the plot, so the label was drawn outside the grid and
   * clipped by the SVG viewport. See #3579.
   *
   * Scoped to the 100% case on purpose: with ordinary stacking the axis
   * maximum is a rounded "nice" number that normally sits beyond the longest
   * stack, so there is already room and padding every such chart would move
   * layouts that render correctly today.
   *
   * Raises `xPadRight`, which narrows `gridWidth` without translating the plot
   * origin, so the y-axis and its labels stay put and only the bars get
   * shorter.
   */
  gridPadForStackedTotalDataLabels() {
    const { w: w2 } = this;
    const totalConfig = w2.config.plotOptions.bar.dataLabels.total;
    if (!w2.globals.isBarHorizontal || !w2.config.chart.stacked || w2.config.chart.stackType !== "100%" || !totalConfig.enabled) {
      return;
    }
    const totals = w2.seriesData.stackedSeriesTotals || [];
    if (!totals.length) return;
    const formatter = totalConfig.formatter || w2.config.dataLabels.formatter;
    const labels = totals.map(
      (val, j) => String(
        formatter ? formatter(val, __spreadProps(__spreadValues({}, w2), { seriesIndex: 0, dataPointIndex: j, w: w2 })) : val
      )
    );
    const graphics = new Graphics(w2);
    const rect = graphics.getTextRects(
      Utils$1.getLargestStringFromArr(labels),
      parseFloat(totalConfig.style.fontSize).toString(),
      totalConfig.style.fontFamily
    );
    const needed = rect.width + Math.abs(totalConfig.offsetX || 0) + 2;
    this.dCtx.xPadRight = Math.max(this.dCtx.xPadRight, needed);
  }
  gridPadFortitleSubtitle() {
    const { w: w2 } = this;
    const { globals: gl } = w2;
    let gridShrinkOffset = this.dCtx.isSparkline || !gl.axisCharts ? 0 : 10;
    const titleSubtitle = ["title", "subtitle"];
    titleSubtitle.forEach((t2) => {
      if (w2.config[t2].text !== void 0) {
        gridShrinkOffset += w2.config[t2].margin;
      } else {
        gridShrinkOffset += this.dCtx.isSparkline || !gl.axisCharts ? 0 : 5;
      }
    });
    if (w2.config.legend.show && w2.config.legend.position === "bottom" && !w2.config.legend.floating && !gl.axisCharts) {
      gridShrinkOffset += 10;
    }
    const titleCoords = this.dCtx.dimHelpers.getTitleSubtitleCoords("title");
    const subtitleCoords = this.dCtx.dimHelpers.getTitleSubtitleCoords("subtitle");
    this.dCtx.titleBlockPad = gridShrinkOffset;
    w2.layout.gridHeight -= titleCoords.height + subtitleCoords.height + gridShrinkOffset;
    w2.layout.translateY += titleCoords.height + subtitleCoords.height + gridShrinkOffset;
  }
  /**
   * @param {{width: number, height: number}[]} yTitleCoords
   * @param {{width: number, height: number}[]} yaxisLabelCoords
   */
  setGridXPosForDualYAxis(yTitleCoords, yaxisLabelCoords) {
    const { w: w2 } = this;
    const axesUtils = new AxesUtils(this.w, { theme: this.dCtx.theme, timeScale: this.dCtx.timeScale });
    w2.config.yaxis.forEach((yaxe, index) => {
      if (w2.globals.ignoreYAxisIndexes.indexOf(index) === -1 && !yaxe.floating && !axesUtils.isYAxisHidden(index)) {
        if (yaxe.opposite) {
          w2.layout.translateX -= yaxisLabelCoords[index].width + yTitleCoords[index].width + parseInt(yaxe.labels.style.fontSize, 10) / 1.2 + 12;
        }
        if (w2.layout.translateX < 2) {
          w2.layout.translateX = 2;
        }
      }
    });
  }
}
const XHTML$3 = "http://www.w3.org/1999/xhtml";
const BREADCRUMB_HEIGHT = 18;
const BREADCRUMB_HEIGHT_FULL = 23;
function breadcrumbConfig(w2, localCfg) {
  const shared = w2.config.drilldown && w2.config.drilldown.breadcrumb || {};
  return __spreadValues(__spreadValues({
    show: true,
    position: "top-left",
    separator: " / ",
    rootLabel: "All",
    offsetX: 0,
    offsetY: 0,
    formatter: void 0
  }, shared), localCfg || {});
}
function avoidChromeOverlap(w2, nav) {
  const chrome = (
    /** @type {Element[]} */
    [".apexcharts-title-text", ".apexcharts-subtitle-text"].map((s2) => w2.dom.baseEl.querySelector(s2)).filter((el) => el !== null)
  );
  if (!chrome.length) return;
  const wrapTop = w2.dom.elWrap.getBoundingClientRect().top;
  for (let pass = 0; pass < chrome.length + 1; pass++) {
    const nr = nav.getBoundingClientRect();
    const hit = chrome.find((el) => {
      const r2 = el.getBoundingClientRect();
      return nr.left < r2.right && nr.right > r2.left && nr.top < r2.bottom && nr.bottom > r2.top;
    });
    if (!hit) break;
    nav.style.top = `${hit.getBoundingClientRect().bottom - wrapTop + 4}px`;
  }
}
function breadcrumbCeiling(w2, nav) {
  const gridTop = w2.layout.translateY || 0;
  const elWrap = w2.dom.elWrap;
  if (!elWrap) return gridTop;
  const labels = w2.dom.baseEl.querySelectorAll(".apexcharts-yaxis-label");
  if (!labels.length) return gridTop;
  const wrapTop = elWrap.getBoundingClientRect().top;
  const navRect = nav.getBoundingClientRect();
  let ceiling = gridTop;
  for (let i2 = 0; i2 < labels.length; i2++) {
    const r2 = labels[i2].getBoundingClientRect();
    if (!r2.height) continue;
    if (r2.left >= navRect.right || r2.right <= navRect.left) continue;
    ceiling = Math.min(ceiling, r2.top - wrapTop);
  }
  return ceiling;
}
function placeInReservedBand(w2, ctx, nav, cfg) {
  var _a;
  const dimHelpers = (_a = ctx == null ? void 0 : ctx.dimensions) == null ? void 0 : _a.dimHelpers;
  const titleArea = dimHelpers ? dimHelpers.getTitleSubtitleCoords("title").height + dimHelpers.getTitleSubtitleCoords("subtitle").height : 0;
  const navH = nav.getBoundingClientRect().height || BREADCRUMB_HEIGHT;
  const offsetY = cfg && cfg.offsetY || 0;
  const ceiling = breadcrumbCeiling(w2, nav);
  if (ceiling - titleArea >= navH + 1) {
    nav.style.top = `${ceiling - navH - 1 + offsetY}px`;
    return true;
  }
  nav.style.top = `${titleArea + offsetY}px`;
  const dark = w2.config.theme.mode === "dark";
  nav.style.background = dark ? "rgba(20,24,30,0.82)" : "rgba(255,255,255,0.86)";
  nav.style.borderRadius = "4px";
  return false;
}
function clearBreadcrumb(w2) {
  const elWrap = w2.dom.elWrap;
  if (!elWrap) return;
  const existing = elWrap.querySelector(".apexcharts-breadcrumb");
  if (existing && existing.parentNode) existing.parentNode.removeChild(existing);
}
function renderBreadcrumb(w2, opts) {
  if (!Environment.isBrowser()) return null;
  const elWrap = w2.dom.elWrap;
  if (!elWrap) return null;
  clearBreadcrumb(w2);
  const cfg = opts.config || breadcrumbConfig(w2);
  if (cfg.show === false) return null;
  const crumbs = opts.crumbs || [];
  if (crumbs.length < 2) return null;
  const nav = BrowserAPIs.createElementNS(XHTML$3, "nav");
  nav.setAttribute("class", "apexcharts-breadcrumb");
  nav.setAttribute("aria-label", opts.ariaLabel || "Breadcrumb");
  positionBreadcrumb(nav, cfg);
  if (opts.compact) {
    nav.style.fontSize = "11px";
    nav.style.padding = "0 2px";
  }
  const separator = cfg.separator != null ? cfg.separator : " / ";
  crumbs.forEach((crumb, i2) => {
    var _a;
    if (i2 > 0) {
      const sep = BrowserAPIs.createElementNS(XHTML$3, "span");
      sep.setAttribute("class", "apexcharts-breadcrumb-separator");
      sep.setAttribute("aria-hidden", "true");
      sep.textContent = separator;
      nav.appendChild(sep);
    }
    let label = i2 === 0 ? (_a = cfg.rootLabel) != null ? _a : "All" : crumb.label;
    if (typeof cfg.formatter === "function") {
      label = cfg.formatter(label, {
        index: i2,
        depth: crumbs.length - 1,
        data: crumb.data
      });
    }
    if (i2 === crumbs.length - 1) {
      const cur = BrowserAPIs.createElementNS(XHTML$3, "span");
      cur.setAttribute(
        "class",
        "apexcharts-breadcrumb-item apexcharts-breadcrumb-current"
      );
      cur.setAttribute("aria-current", "page");
      cur.textContent = String(label);
      nav.appendChild(cur);
      return;
    }
    const btn = (
      /** @type {HTMLButtonElement} */
      BrowserAPIs.createElementNS(XHTML$3, "button")
    );
    btn.setAttribute("type", "button");
    btn.setAttribute("class", "apexcharts-breadcrumb-item");
    if (i2 === 0) {
      const arrow = BrowserAPIs.createElementNS(XHTML$3, "span");
      arrow.setAttribute("class", "apexcharts-breadcrumb-arrow");
      arrow.setAttribute("aria-hidden", "true");
      arrow.textContent = "←";
      btn.appendChild(arrow);
    }
    const text = BrowserAPIs.createElementNS(XHTML$3, "span");
    text.setAttribute("class", "apexcharts-breadcrumb-label");
    text.textContent = String(label);
    btn.appendChild(text);
    btn.addEventListener("click", () => opts.onNavigate(i2, crumb));
    nav.appendChild(btn);
  });
  elWrap.appendChild(nav);
  return nav;
}
function positionBreadcrumb(nav, cfg) {
  const ox = cfg.offsetX || 0;
  const oy = cfg.offsetY || 0;
  nav.style.position = "absolute";
  nav.style.top = oy + "px";
  if (cfg.position === "top-right") {
    nav.style.right = -ox + 3 + "px";
  } else {
    nav.style.left = ox + "px";
  }
}
class Dimensions {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.theme = ctx.theme;
    this.timeScale = ctx.timeScale;
    this.lgRect = /** @type {any} */
    {};
    this.yAxisWidth = 0;
    this.yAxisWidthLeft = 0;
    this.yAxisWidthRight = 0;
    this.xAxisHeight = 0;
    this.isSparkline = this.w.config.chart.sparkline.enabled;
    this.dimHelpers = new Helpers$4(this);
    this.dimYAxis = new DimYAxis(this);
    this.dimXAxis = new DimXAxis(this);
    this.dimGrid = new DimGrid(this);
    this.lgWidthForSideLegends = 0;
    this.gridPad = __spreadValues({}, this.w.config.grid.padding);
    this.xPadRight = 0;
    this.titleBlockPad = 0;
    this.xPadLeft = 0;
    this.datalabelsCoords = { width: 0, height: 0 };
    this.xAxisWidth = 0;
    this.timescaleLabels = [];
  }
  /**
   * @memberof Dimensions
   **/
  plotCoords() {
    const w2 = this.w;
    const gl = w2.globals;
    this.gridPad = __spreadValues({}, w2.config.grid.padding);
    this.lgRect = this.dimHelpers.getLegendsRect();
    this.datalabelsCoords = { width: 0, height: 0 };
    if (this.isSparkline) {
      if (this.w.globals.markers.largestSize > 0) {
        Object.entries(this.gridPad).forEach(([k, v2]) => {
          this.gridPad[k] = Math.max(
            v2,
            this.w.globals.markers.largestSize / 1.5
          );
        });
      }
      const strokeInset = this.dimHelpers.getSparklineStrokeInset();
      this.gridPad.top = Math.max(strokeInset.top, this.gridPad.top);
      this.gridPad.bottom = Math.max(strokeInset.bottom, this.gridPad.bottom);
    }
    if (gl.axisCharts) {
      this.setDimensionsForAxisCharts();
    } else {
      this.setDimensionsForNonAxisCharts();
    }
    this.dimGrid.gridPadFortitleSubtitle();
    this.gridPadForBreadcrumb();
    this.dimGrid.gridPadForStackedTotalDataLabels();
    w2.layout.gridHeight = w2.layout.gridHeight - this.gridPad.top - this.gridPad.bottom;
    w2.layout.gridWidth = w2.layout.gridWidth - this.gridPad.left - this.gridPad.right - this.xPadRight - this.xPadLeft;
    const barWidth = this.dimGrid.gridPadForColumnsInNumericAxis(
      w2.layout.gridWidth
    );
    w2.layout.gridWidth = w2.layout.gridWidth - barWidth * 2;
    w2.layout.translateX = w2.layout.translateX + this.gridPad.left + this.xPadLeft + (barWidth > 0 ? barWidth : 0);
    w2.layout.translateY = w2.layout.translateY + this.gridPad.top;
    return {
      // w.layout (future slice)
      layout: {
        gridHeight: w2.layout.gridHeight,
        gridWidth: w2.layout.gridWidth,
        translateX: w2.layout.translateX,
        translateY: w2.layout.translateY,
        translateXAxisX: w2.layout.translateXAxisX,
        translateXAxisY: w2.layout.translateXAxisY,
        rotateXLabels: w2.layout.rotateXLabels,
        xAxisHeight: w2.layout.xAxisHeight,
        xAxisLabelsHeight: w2.layout.xAxisLabelsHeight,
        xAxisGroupLabelsHeight: w2.layout.xAxisGroupLabelsHeight,
        xAxisLabelsWidth: w2.layout.xAxisLabelsWidth,
        yLabelsCoords: w2.layout.yLabelsCoords,
        yTitleCoords: w2.layout.yTitleCoords,
        gridPad: __spreadValues({}, this.gridPad)
      }
    };
  }
  /**
   * Reserve a strip above the plot for a navigation breadcrumb.
   *
   * A treemap fills its plot edge to edge, so unlike a sunburst - whose rings
   * leave the corners empty - an absolutely-positioned breadcrumb has nowhere
   * to float without covering a tile. Giving it real space is the only way it
   * never overlaps.
   *
   * Reserved whenever click-to-zoom is enabled, not only while zoomed in: the
   * strip appears and disappears as the reader navigates, and sizing the plot
   * around its presence would reflow every tile on each zoom.
   */
  gridPadForBreadcrumb() {
    var _a, _b, _c, _d, _e, _f;
    const w2 = this.w;
    const isTreemap = w2.config.chart.type === "treemap";
    if (isTreemap) {
      const zoom = (_b = (_a = w2.config.plotOptions) == null ? void 0 : _a.treemap) == null ? void 0 : _b.zoom;
      if (zoom && zoom.enabled) {
        if (breadcrumbConfig(w2, zoom.breadcrumb).show === false) return;
        this.gridPad.top += BREADCRUMB_HEIGHT + 4;
        return;
      }
    }
    if (!w2.globals.axisCharts) return;
    if (!this.ctx.drilldown) return;
    if (!w2.config.drilldown || !w2.config.drilldown.enabled) return;
    if (breadcrumbConfig(w2).show === false) return;
    const labelFs = parseFloat(String((_f = (_e = (_d = (_c = w2.config.yaxis) == null ? void 0 : _c[0]) == null ? void 0 : _d.labels) == null ? void 0 : _e.style) == null ? void 0 : _f.fontSize)) || 11;
    const yLabelOverhang = isTreemap ? 0 : Math.ceil(labelFs * LINE_HEIGHT_RATIO / 2);
    const needed = BREADCRUMB_HEIGHT_FULL + 1 + yLabelOverhang;
    const alreadyFree = isTreemap ? 0 : this.titleBlockPad || 0;
    this.gridPad.top += Math.max(0, needed - alreadyFree);
  }
  setDimensionsForAxisCharts() {
    const w2 = this.w;
    const gl = w2.globals;
    const yaxisLabelCoords = this.dimYAxis.getyAxisLabelsCoords();
    const yTitleCoords = this.dimYAxis.getyAxisTitleCoords();
    if (gl.isSlopeChart) {
      this.datalabelsCoords = this.dimHelpers.getDatalabelsRect();
    }
    w2.layout.yLabelsCoords = [];
    w2.layout.yTitleCoords = [];
    w2.config.yaxis.map((yaxe, index) => {
      w2.layout.yLabelsCoords.push({
        width: yaxisLabelCoords[index].width,
        index
      });
      w2.layout.yTitleCoords.push(
        /** @type {any} */
        {
          width: yTitleCoords[index].width,
          index
        }
      );
    });
    this.yAxisWidth = this.dimYAxis.getTotalYAxisWidth();
    const xaxisLabelCoords = this.dimXAxis.getxAxisLabelsCoords();
    const xaxisGroupLabelCoords = this.dimXAxis.getxAxisGroupLabelsCoords();
    const xtitleCoords = this.dimXAxis.getxAxisTitleCoords();
    this.conditionalChecksForAxisCoords(
      xaxisLabelCoords,
      xtitleCoords,
      xaxisGroupLabelCoords
    );
    w2.layout.translateXAxisY = w2.layout.rotateXLabels ? this.xAxisHeight / 8 : -4;
    w2.layout.translateXAxisX = w2.layout.rotateXLabels && w2.axisFlags.isXNumeric && w2.config.xaxis.labels.rotate <= -45 ? -this.xAxisWidth / 4 : 0;
    if (w2.globals.isBarHorizontal) {
      w2.layout.rotateXLabels = false;
      w2.layout.translateXAxisY = -1 * ((parseInt(w2.config.xaxis.labels.style.fontSize, 10) || 12) / 1.5);
    }
    w2.layout.translateXAxisY = w2.layout.translateXAxisY + w2.config.xaxis.labels.offsetY;
    w2.layout.translateXAxisX = w2.layout.translateXAxisX + w2.config.xaxis.labels.offsetX;
    let yAxisWidth = this.yAxisWidth;
    let xAxisHeight = this.xAxisHeight;
    w2.layout.xAxisLabelsHeight = this.xAxisHeight - xtitleCoords.height;
    w2.layout.xAxisGroupLabelsHeight = w2.layout.xAxisLabelsHeight - xaxisLabelCoords.height;
    w2.layout.xAxisLabelsWidth = this.xAxisWidth;
    w2.layout.xAxisHeight = this.xAxisHeight;
    let translateY = 10;
    if (w2.config.chart.type === "radar" || this.isSparkline) {
      yAxisWidth = 0;
      xAxisHeight = 0;
    }
    if (this.isSparkline) {
      this.lgRect = {
        height: 0,
        width: 0
      };
    }
    if (this.isSparkline || w2.config.chart.type === "treemap") {
      yAxisWidth = 0;
      xAxisHeight = 0;
      translateY = 0;
    }
    if (!this.isSparkline && w2.config.chart.type !== "treemap") {
      this.dimXAxis.additionalPaddingXLabels(xaxisLabelCoords);
    }
    const legendTopBottom = () => {
      w2.layout.translateX = yAxisWidth + this.datalabelsCoords.width;
      w2.layout.gridHeight = gl.svgHeight - this.lgRect.height - xAxisHeight - (!this.isSparkline && w2.config.chart.type !== "treemap" ? w2.layout.rotateXLabels ? 10 : 15 : 0);
      w2.layout.gridWidth = gl.svgWidth - yAxisWidth - this.datalabelsCoords.width * 2;
    };
    if (w2.config.xaxis.position === "top")
      translateY = w2.layout.xAxisHeight - w2.config.xaxis.axisTicks.height - 5;
    switch (w2.config.legend.position) {
      case "bottom":
        w2.layout.translateY = translateY;
        legendTopBottom();
        break;
      case "top":
        w2.layout.translateY = this.lgRect.height + translateY;
        legendTopBottom();
        break;
      case "left":
        w2.layout.translateY = translateY;
        w2.layout.translateX = this.lgRect.width + yAxisWidth + this.datalabelsCoords.width;
        w2.layout.gridHeight = gl.svgHeight - xAxisHeight - 12;
        w2.layout.gridWidth = gl.svgWidth - this.lgRect.width - yAxisWidth - this.datalabelsCoords.width * 2;
        break;
      case "right":
        w2.layout.translateY = translateY;
        w2.layout.translateX = yAxisWidth + this.datalabelsCoords.width;
        w2.layout.gridHeight = gl.svgHeight - xAxisHeight - 12;
        w2.layout.gridWidth = gl.svgWidth - this.lgRect.width - yAxisWidth - this.datalabelsCoords.width * 2 - 5;
        break;
      default:
        throw new Error("Legend position not supported");
    }
    this.dimGrid.setGridXPosForDualYAxis(yTitleCoords, yaxisLabelCoords);
    const objyAxis = new YAxis(this.w, {
      theme: this.theme,
      timeScale: this.timeScale
    });
    objyAxis.setYAxisXPosition(yaxisLabelCoords, yTitleCoords);
  }
  setDimensionsForNonAxisCharts() {
    const w2 = this.w;
    const gl = w2.globals;
    const cnf = w2.config;
    let xPad = 0;
    if (w2.config.legend.show && !w2.config.legend.floating) {
      xPad = 20;
    }
    if (cnf.chart.type === "unit") {
      const legendVisible = cnf.legend.show && !cnf.legend.floating;
      const pos = cnf.legend.position;
      let top = 0;
      let side = 0;
      if (legendVisible) {
        if (pos === "bottom" || pos === "top") {
          top = this.lgRect.height;
        } else {
          side = this.lgRect.width + xPad;
        }
      }
      w2.layout.gridWidth = gl.svgWidth - side;
      w2.layout.gridHeight = gl.svgHeight - top;
      w2.layout.translateX = pos === "left" ? side : 0;
      w2.layout.translateY = pos === "top" ? top : 0;
      return;
    }
    const type = cnf.chart.type === "sunburst" ? "sunburst" : cnf.chart.type === "pie" || cnf.chart.type === "polarArea" || cnf.chart.type === "donut" ? "pie" : "radialBar";
    const offY = cnf.plotOptions[type].offsetY;
    const offX = cnf.plotOptions[type].offsetX;
    if (!cnf.legend.show || cnf.legend.floating) {
      w2.layout.gridHeight = gl.svgHeight;
      const maxWidth = w2.dom.elWrap.getBoundingClientRect().width;
      w2.layout.gridWidth = Math.min(maxWidth, w2.layout.gridHeight);
      w2.layout.translateY = offY;
      w2.layout.translateX = offX + (gl.svgWidth - w2.layout.gridWidth) / 2;
      return;
    }
    switch (cnf.legend.position) {
      case "bottom":
        w2.layout.gridHeight = gl.svgHeight - this.lgRect.height;
        w2.layout.gridWidth = gl.svgWidth;
        w2.layout.translateY = offY - 10;
        w2.layout.translateX = offX + (gl.svgWidth - w2.layout.gridWidth) / 2;
        break;
      case "top":
        w2.layout.gridHeight = gl.svgHeight - this.lgRect.height;
        w2.layout.gridWidth = gl.svgWidth;
        w2.layout.translateY = this.lgRect.height + offY + 10;
        w2.layout.translateX = offX + (gl.svgWidth - w2.layout.gridWidth) / 2;
        break;
      case "left":
        w2.layout.gridWidth = gl.svgWidth - this.lgRect.width - xPad;
        w2.layout.gridHeight = cnf.chart.height !== "auto" ? gl.svgHeight : w2.layout.gridWidth;
        w2.layout.translateY = offY;
        w2.layout.translateX = offX + this.lgRect.width + xPad;
        break;
      case "right":
        w2.layout.gridWidth = gl.svgWidth - this.lgRect.width - xPad - 5;
        w2.layout.gridHeight = cnf.chart.height !== "auto" ? gl.svgHeight : w2.layout.gridWidth;
        w2.layout.translateY = offY;
        w2.layout.translateX = offX + 10;
        break;
      default:
        throw new Error("Legend position not supported");
    }
  }
  /**
   * @param {any} xaxisLabelCoords
   * @param {any} xtitleCoords
   * @param {any} xaxisGroupLabelCoords
   */
  conditionalChecksForAxisCoords(xaxisLabelCoords, xtitleCoords, xaxisGroupLabelCoords) {
    const w2 = this.w;
    const xAxisNum = w2.labelData.hasXaxisGroups ? 2 : 1;
    const baseXAxisHeight = xaxisGroupLabelCoords.height + xaxisLabelCoords.height + xtitleCoords.height;
    const xAxisHeightMultiplicate = w2.axisFlags.isMultiLineX ? 1.2 : LINE_HEIGHT_RATIO;
    const rotatedXAxisOffset = w2.layout.rotateXLabels ? 22 : 10;
    const rotatedXAxisLegendOffset = w2.layout.rotateXLabels && w2.config.legend.position === "bottom";
    const additionalOffset = rotatedXAxisLegendOffset ? 10 : 0;
    this.xAxisHeight = baseXAxisHeight * xAxisHeightMultiplicate + xAxisNum * rotatedXAxisOffset + additionalOffset;
    this.xAxisWidth = xaxisLabelCoords.width;
    if (this.xAxisHeight - xtitleCoords.height > w2.config.xaxis.labels.maxHeight) {
      this.xAxisHeight = w2.config.xaxis.labels.maxHeight;
    }
    if (w2.config.xaxis.labels.minHeight && this.xAxisHeight < w2.config.xaxis.labels.minHeight) {
      this.xAxisHeight = w2.config.xaxis.labels.minHeight;
    }
    if (w2.config.xaxis.floating) {
      this.xAxisHeight = 0;
    }
    let minYAxisWidth = 0;
    let maxYAxisWidth = 0;
    w2.config.yaxis.forEach((y) => {
      minYAxisWidth += y.labels.minWidth;
      maxYAxisWidth += y.labels.maxWidth;
    });
    if (this.yAxisWidth < minYAxisWidth) {
      this.yAxisWidth = minYAxisWidth;
    }
    if (this.yAxisWidth > maxYAxisWidth) {
      this.yAxisWidth = maxYAxisWidth;
    }
  }
}
const SECOND = 1e3;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;
const WEEK = 7 * DAY;
const APPROX_MONTH = 30 * DAY;
const APPROX_YEAR = 365 * DAY;
const MIN_ZOOM_DAYS = 10 / (24 * 60 * 60);
const TICK_LADDER = [
  { unit: "second", step: 1, approxMs: SECOND },
  { unit: "second", step: 5, approxMs: 5 * SECOND },
  { unit: "second", step: 15, approxMs: 15 * SECOND },
  { unit: "second", step: 30, approxMs: 30 * SECOND },
  { unit: "minute", step: 1, approxMs: MINUTE },
  { unit: "minute", step: 5, approxMs: 5 * MINUTE },
  { unit: "minute", step: 15, approxMs: 15 * MINUTE },
  { unit: "minute", step: 30, approxMs: 30 * MINUTE },
  { unit: "hour", step: 1, approxMs: HOUR },
  { unit: "hour", step: 3, approxMs: 3 * HOUR },
  { unit: "hour", step: 6, approxMs: 6 * HOUR },
  { unit: "hour", step: 12, approxMs: 12 * HOUR },
  { unit: "day", step: 1, approxMs: DAY },
  { unit: "day", step: 2, approxMs: 2 * DAY },
  { unit: "week", step: 1, approxMs: WEEK },
  { unit: "week", step: 2, approxMs: 2 * WEEK },
  { unit: "month", step: 1, approxMs: APPROX_MONTH },
  { unit: "month", step: 3, approxMs: 3 * APPROX_MONTH },
  { unit: "month", step: 6, approxMs: 6 * APPROX_MONTH },
  { unit: "year", step: 1, approxMs: APPROX_YEAR },
  { unit: "year", step: 2, approxMs: 2 * APPROX_YEAR },
  { unit: "year", step: 5, approxMs: 5 * APPROX_YEAR },
  { unit: "year", step: 10, approxMs: 10 * APPROX_YEAR },
  { unit: "year", step: 25, approxMs: 25 * APPROX_YEAR },
  { unit: "year", step: 50, approxMs: 50 * APPROX_YEAR },
  { unit: "year", step: 100, approxMs: 100 * APPROX_YEAR }
];
const DEFAULT_TICK_COUNT = 10;
class TimeScale {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.tickInterval = null;
    this.timeScaleArray = [];
    this.utc = w2.config.xaxis.labels.datetimeUTC;
  }
  /**
   * Compute raw ticks for a datetime axis spanning [minX, maxX]. Single
   * uniform stride — one unit, no promotion, no calendar-boundary overlay.
   * Context (year, day) is folded into each label's format string at
   * `formatDates` time, not split across promoted ticks.
   *
   * @param {number} minX
   * @param {number} maxX
   * @returns {Array<any>} raw ticks (pre-formatting). Pass to recalcDimensionsBasedOnFormat for the final timescaleLabels.
   */
  calculateTimeScaleTicks(minX, maxX) {
    const w2 = this.w;
    if (w2.globals.allSeriesCollapsed) {
      w2.labelData.labels = [];
      w2.labelData.timescaleLabels = [];
      this.timeScaleArray = [];
      return [];
    }
    const span = maxX - minX;
    const daysDiff = span / DAY;
    w2.interact.disableZoomIn = false;
    w2.interact.disableZoomOut = false;
    if (daysDiff < MIN_ZOOM_DAYS) {
      w2.interact.disableZoomIn = true;
    } else if (daysDiff > 5e4) {
      w2.interact.disableZoomOut = true;
    }
    const targetCount = Number.isFinite(w2.config.xaxis.tickAmount) ? (
      /** @type {number} */
      w2.config.xaxis.tickAmount
    ) : DEFAULT_TICK_COUNT;
    this.tickInterval = pickInterval(span, targetCount);
    const ticks = this.generateBaseTicks(minX, maxX, this.tickInterval);
    this.timeScaleArray = ticks;
    return ticks;
  }
  /**
   * Walk the interval stride from `minX` to `maxX`. Every tick carries
   * `unit: interval.unit` — no promotion, no snapping. Uniform spacing is
   * guaranteed.
   *
   * @param {number} minX
   * @param {number} maxX
   * @param {{ unit: string, step: number }} interval
   * @returns {Array<any>}
   */
  generateBaseTicks(minX, maxX, interval) {
    const w2 = this.w;
    const dt = new DateTime(w2);
    const isUTC = this.utc;
    const gridWidth = w2.layout.gridWidth;
    const span = maxX - minX;
    const ticks = [];
    let t2 = dt.ceilToBoundary(
      minX,
      /** @type {any} */
      interval.unit,
      interval.step,
      isUTC
    );
    let iter = 0;
    const MAX_ITER = 5e3;
    while (t2 <= maxX && iter < MAX_ITER) {
      const f = dt.getDateFields(t2, isUTC);
      const position = span > 0 ? (t2 - minX) / span * gridWidth : 0;
      ticks.push({
        timestamp: t2,
        position,
        unit: interval.unit,
        year: f.year,
        month: f.month + 1,
        day: f.date,
        hour: f.hour,
        minute: f.minute,
        second: f.second,
        value: t2
      });
      t2 = dt.addInterval(
        t2,
        /** @type {any} */
        interval.unit,
        interval.step,
        isUTC
      );
      iter++;
    }
    return ticks;
  }
  /**
   * Public entry called from Core.js after calculateTimeScaleTicks. Formats
   * the raw ticks into display labels, removes overlapping entries, writes
   * the result to `w.labelData.timescaleLabels`, and re-runs Dimensions to
   * lay out the grid based on the final label widths.
   *
   * @param {Array<any>} rawTicks
   */
  recalcDimensionsBasedOnFormat(rawTicks) {
    const w2 = this.w;
    const formatted = this.formatDates(rawTicks);
    const filtered = this.removeOverlappingTS(formatted);
    w2.labelData.timescaleLabels = filtered.slice();
    const dimensions = new Dimensions(this.w, this.ctx);
    const layoutState = dimensions.plotCoords();
    this.ctx._writeLayoutCoords(layoutState.layout);
  }
  /**
   * Format each raw tick into a display label. All ticks share one
   * effective format computed once from the interval unit and the data
   * range — when the range spans coarser units, the base format from
   * `datetimeFormatter[unit]` is automatically extended with the higher-
   * unit context (e.g. month-scale spanning years → `MMM yyyy`, hour-scale
   * spanning days → `dd MMM HH:mm`). A user-supplied `xaxis.labels.format`
   * overrides everything.
   *
   * @param {Array<any>} rawTicks
   * @returns {Array<any>}
   */
  formatDates(rawTicks) {
    const w2 = this.w;
    const dt = new DateTime(w2);
    const userFormat = w2.config.xaxis.labels.format;
    const dtFmt = w2.config.xaxis.labels.datetimeFormatter;
    const isUTC = this.utc;
    const pad = (n2, len = 2) => String(n2).padStart(len, "0");
    const effectiveFormat = userFormat ? userFormat : this._effectiveFormat(rawTicks, dtFmt);
    return rawTicks.map((tick) => {
      const date = dt.getDate(tick.timestamp);
      const value = dt.formatDate(date, effectiveFormat);
      const ds = `${tick.year}-${pad(tick.month)}-${pad(tick.day)}T${pad(tick.hour)}:${pad(tick.minute)}:${pad(tick.second)}.000${isUTC ? "Z" : ""}`;
      return {
        dateString: ds,
        position: tick.position,
        value,
        unit: tick.unit,
        year: tick.year,
        month: tick.month
      };
    });
  }
  /**
   * Pick the format string used for every tick this render. Folds coarser
   * context into the base `datetimeFormatter[unit]` when the data range
   * spans it. Skipped when the base format already references the higher
   * unit's tokens (so user customizations aren't doubled).
   *
   * @param {Array<any>} rawTicks
   * @param {Record<string, string>} dtFmt
   * @returns {string}
   */
  _effectiveFormat(rawTicks, dtFmt) {
    if (rawTicks.length === 0) return dtFmt.day || "dd MMM";
    const unit = this.tickInterval && this.tickInterval.unit || rawTicks[0].unit;
    const base = dtFmt[unit === "week" ? "day" : unit] || dtFmt.day || "dd MMM";
    const first = rawTicks[0];
    const last = rawTicks[rawTicks.length - 1];
    const spansYears = first.year !== last.year;
    const spansMonths = spansYears || first.month !== last.month;
    const spansDays = spansMonths || first.day !== last.day;
    const hasYearToken = /y/i.test(base);
    const hasMonthToken = /M/.test(base);
    const hasDayToken = /d/i.test(base);
    if (unit === "month" || unit === "week") {
      if (spansYears && !hasYearToken) return base + " yyyy";
      return base;
    }
    if (unit === "day") {
      if (spansYears && !hasYearToken) return base + " yyyy";
      return base;
    }
    if (unit === "hour" || unit === "minute" || unit === "second") {
      if (spansDays && !hasDayToken && !hasMonthToken) {
        const prefix = spansYears ? "dd MMM yyyy" : "dd MMM";
        return prefix + " " + base;
      }
      return base;
    }
    return base;
  }
  /**
   * Drop labels that would overlap their predecessor (when
   * `xaxis.labels.hideOverlappingLabels` is true). The first label is always
   * kept. Width is measured per-label unless all labels have the same string
   * length, in which case one measurement is reused.
   *
   * @param {Array<any>} arr
   * @returns {Array<any>}
   */
  removeOverlappingTS(arr) {
    if (arr.length === 0) return [];
    const w2 = this.w;
    const graphics = new Graphics(w2);
    let equalLabelLengthFlag = false;
    let constantLabelWidth;
    if (arr[0].value && arr.every((lb) => lb.value.length === arr[0].value.length)) {
      equalLabelLengthFlag = true;
      constantLabelWidth = graphics.getTextRects(
        arr[0].value,
        w2.config.xaxis.labels.style.fontSize
      ).width;
    }
    let lastDrawnIndex = 0;
    const filtered = arr.map((item, index) => {
      if (index === 0) return item;
      if (!w2.config.xaxis.labels.hideOverlappingLabels) return item;
      const prevLabelWidth = equalLabelLengthFlag ? (
        /** @type {number} */
        constantLabelWidth
      ) : graphics.getTextRects(
        arr[lastDrawnIndex].value,
        w2.config.xaxis.labels.style.fontSize
      ).width;
      const prevPos = arr[lastDrawnIndex].position;
      const pos = item.position;
      if (pos > prevPos + prevLabelWidth + 10) {
        lastDrawnIndex = index;
        return item;
      }
      return null;
    }).filter((f) => f !== null);
    return filtered;
  }
}
function pickInterval(span, targetCount) {
  if (!Number.isFinite(targetCount) || targetCount <= 0) {
    targetCount = DEFAULT_TICK_COUNT;
  }
  if (span <= 0) return TICK_LADDER[0];
  const targetMs = span / targetCount;
  let best = TICK_LADDER[0];
  let bestDist = Infinity;
  for (const interval of TICK_LADDER) {
    const dist = Math.abs(Math.log(interval.approxMs / targetMs));
    if (dist < bestDist) {
      bestDist = dist;
      best = interval;
    }
  }
  return best;
}
const REGISTRY_KEY$1 = "__apexcharts_registry__";
const CUSTOM_KEY = "__apexcharts_custom_types__";
if (!/** @type {any} */
globalThis[REGISTRY_KEY$1]) {
  globalThis[REGISTRY_KEY$1] = {};
}
if (!/** @type {any} */
globalThis[CUSTOM_KEY]) {
  globalThis[CUSTOM_KEY] = /* @__PURE__ */ new Set();
}
function getRegistry$1() {
  return (
    /** @type {any} */
    globalThis[REGISTRY_KEY$1]
  );
}
function getCustomTypes() {
  return (
    /** @type {any} */
    globalThis[CUSTOM_KEY]
  );
}
function markCustom(name2) {
  getCustomTypes().add(name2);
}
function isCustom(name2) {
  return getCustomTypes().has(name2);
}
function hasChartClass(type) {
  return !!getRegistry$1()[type];
}
function unregister(name2) {
  delete getRegistry$1()[name2];
  getCustomTypes().delete(name2);
}
function register(typeMap) {
  Object.assign(getRegistry$1(), typeMap);
}
function getChartClass(type) {
  const Cls = getRegistry$1()[type];
  if (!Cls) {
    throw new Error(
      `ApexCharts: chart type "${type}" is not registered. Import it via ApexCharts.use() or use the full apexcharts bundle.`
    );
  }
  return Cls;
}
class Core {
  /**
   * @param {Element} el
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(el, w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.el = el;
  }
  setupElements() {
    const { globals: gl, config: cnf } = this.w;
    const ct = cnf.chart.type;
    const xyChartsArrTypes = [
      "line",
      "area",
      "bar",
      "rangeBar",
      "rangeArea",
      "candlestick",
      "boxPlot",
      "violin",
      "scatter",
      "bubble"
    ];
    const axisChartsArrTypes = [
      ...xyChartsArrTypes,
      "radar",
      "heatmap",
      "treemap"
    ];
    const isCustomType = !axisChartsArrTypes.includes(ct) && !["pie", "donut", "polarArea", "radialBar"].includes(ct) && isCustom(ct);
    gl.axisCharts = axisChartsArrTypes.includes(ct) || isCustomType;
    gl.xyCharts = xyChartsArrTypes.includes(ct) || isCustomType;
    gl.isBarHorizontal = ["bar", "rangeBar", "boxPlot", "violin"].includes(ct) && cnf.plotOptions.bar.horizontal;
    gl.chartClass = `.apexcharts${gl.chartID}`;
    this.w.dom.baseEl = this.el;
    this.w.dom.elWrap = BrowserAPIs.createElementNS(
      "http://www.w3.org/1999/xhtml",
      "div"
    );
    Graphics.setAttrs(this.w.dom.elWrap, {
      id: gl.chartClass.substring(1),
      class: `apexcharts-canvas ${gl.chartClass.substring(1)}`
    });
    this.el.appendChild(this.w.dom.elWrap);
    const SVG2 = (
      /** @type {any} */
      globalThis.SVG
    );
    this.w.dom.Paper = SVG2().addTo(this.w.dom.elWrap);
    this.w.dom.Paper.attr({
      class: "apexcharts-svg",
      "xmlns:data": "ApexChartsNS",
      transform: `translate(${cnf.chart.offsetX}, ${cnf.chart.offsetY})`
    });
    this.w.dom.Paper.node.style.background = cnf.theme.mode === "dark" && !cnf.chart.background ? "#343A3F" : cnf.theme.mode === "light" && !cnf.chart.background ? "#fff" : cnf.chart.background;
    this.setSVGDimensions();
    this.w.dom.elLegendForeign = BrowserAPIs.createElementNS(
      SVGNS$2,
      "foreignObject"
    );
    Graphics.setAttrs(this.w.dom.elLegendForeign, {
      x: 0,
      y: 0,
      width: gl.svgWidth,
      height: gl.svgHeight
    });
    this.w.dom.elLegendWrap = BrowserAPIs.createElementNS(
      "http://www.w3.org/1999/xhtml",
      "div"
    );
    this.w.dom.elLegendWrap.classList.add("apexcharts-legend");
    this.w.dom.elWrap.appendChild(this.w.dom.elLegendWrap);
    this.w.dom.Paper.node.appendChild(this.w.dom.elLegendForeign);
    if (cnf.chart.accessibility.enabled && cnf.chart.accessibility.announcements.enabled) {
      const srStatus = BrowserAPIs.createElement("div");
      srStatus.className = "apexcharts-sr-status";
      srStatus.setAttribute("role", "status");
      srStatus.setAttribute("aria-live", "polite");
      srStatus.setAttribute("aria-atomic", "true");
      this.w.dom.elWrap.appendChild(srStatus);
    }
    if (cnf.chart.accessibility.enabled) {
      const ariaLabel = this.getAccessibleChartLabel();
      const svgRole = cnf.chart.accessibility.keyboard.enabled && cnf.chart.accessibility.keyboard.navigation.enabled ? "application" : "img";
      this.w.dom.Paper.attr({
        role: svgRole,
        "aria-label": ariaLabel
      });
      if (cnf.chart.accessibility.description) {
        const descEl = BrowserAPIs.createElementNS(SVGNS$2, "desc");
        descEl.textContent = cnf.chart.accessibility.description;
        this.w.dom.Paper.node.insertBefore(
          descEl,
          this.w.dom.elLegendForeign.nextSibling
        );
      }
    }
    this.w.dom.elGraphical = this.w.dom.Paper.group().attr({
      class: "apexcharts-inner apexcharts-graphical"
    });
    this.w.dom.elDefs = this.w.dom.Paper.defs();
    this.w.dom.Paper.add(this.w.dom.elGraphical);
    this.w.dom.elGraphical.add(this.w.dom.elDefs);
  }
  /**
   * Classify each series by its resolved chart type into per-type render
   * buckets (`{ series, i }`, plus `seriesRangeEnd` for rangeArea). Side
   * effects mirror the original inline code: sets `w.globals.columnSeries`,
   * folds `w.globals.comboCharts`, and warns on unsupported combinations (a
   * non-combo type mixed in, or horizontal bars in a combo). Extracted from
   * plotChartType to shrink it (audit C2).
   * @param {any[]} ser
   * @returns {{ seriesTypes: Record<string, any>, customBuckets: Record<string, {series: any[], i: number[]}> }}
   */
  _classifySeriesByType(ser) {
    const { w: w2 } = this;
    const { config: cnf, globals: gl } = w2;
    const seriesTypes = {
      line: { series: [], i: [] },
      area: { series: [], i: [] },
      scatter: { series: [], i: [] },
      bubble: { series: [], i: [] },
      bar: { series: [], i: [] },
      candlestick: { series: [], i: [] },
      boxPlot: { series: [], i: [] },
      violin: { series: [], i: [] },
      rangeBar: { series: [], i: [] },
      rangeArea: { series: [], seriesRangeEnd: [], i: [] }
    };
    const customBuckets = {};
    const chartType = cnf.chart.type || "line";
    let nonComboType = null;
    let comboCount = 0;
    this.w.seriesData.series.forEach((serie, st) => {
      var _a, _b;
      const seriesType = ((_a = ser[st]) == null ? void 0 : _a.type) === "column" ? "bar" : ((_b = ser[st]) == null ? void 0 : _b.type) || (chartType === "column" ? "bar" : chartType);
      const st_ = (
        /** @type {Record<string,any>} */
        seriesTypes
      );
      if (st_[seriesType]) {
        if (seriesType === "rangeArea") {
          st_[seriesType].series.push(this.w.rangeData.seriesRangeStart[st]);
          st_[seriesType].seriesRangeEnd.push(
            this.w.rangeData.seriesRangeEnd[st]
          );
        } else {
          st_[seriesType].series.push(serie);
        }
        st_[seriesType].i.push(st);
        if (seriesType === "bar") w2.globals.columnSeries = seriesTypes.bar;
      } else if ([
        "heatmap",
        "treemap",
        "pie",
        "donut",
        "polarArea",
        "radialBar",
        "radar",
        "unit",
        "sunburst"
      ].includes(seriesType)) {
        nonComboType = seriesType;
      } else if (isCustom(seriesType)) {
        if (!customBuckets[seriesType]) {
          customBuckets[seriesType] = { series: [], i: [] };
        }
        customBuckets[seriesType].series.push(serie);
        customBuckets[seriesType].i.push(st);
      } else {
        console.warn(
          `You have specified an unrecognized series type (${seriesType}).`
        );
      }
      if (chartType !== seriesType && seriesType !== "scatter") comboCount++;
    });
    if (comboCount > 0) {
      if (nonComboType) {
        console.warn(
          `Chart or series type ${nonComboType} cannot appear with other chart or series types.`
        );
      }
      if (seriesTypes.bar.series.length > 0 && cnf.plotOptions.bar.horizontal) {
        comboCount -= seriesTypes.bar.series.length;
        seriesTypes.bar = { series: [], i: [] };
        w2.globals.columnSeries = { series: [], i: [] };
        console.warn(
          "Horizontal bars are not supported in a mixed/combo chart. Please turn off `plotOptions.bar.horizontal`"
        );
      }
    }
    gl.comboCharts || (gl.comboCharts = comboCount > 0);
    return { seriesTypes, customBuckets };
  }
  /**
   * @param {any[]} ser
   * @param {import('../types/internal').XYRatios} xyRatios
   */
  plotChartType(ser, xyRatios) {
    const { w: w2, ctx } = this;
    const canvasMode = ctx.renderer && ctx.renderer.kind === "canvas";
    if (canvasMode) ctx.renderer.beginSeries();
    const { seriesTypes, customBuckets } = this._classifySeriesByType(ser);
    const renderers = this._instantiateSeriesRenderers(seriesTypes, xyRatios);
    const elGraph = this._assembleSeriesGraphics(
      seriesTypes,
      customBuckets,
      renderers,
      xyRatios
    );
    if (canvasMode) {
      const rr = (
        /** @type {any} */
        ctx.renderer
      );
      if (rr && rr._repaintHostInPlace) {
        rr._repaintHostInPlace = false;
        const wrapNode = this.w.dom.elGraphical.node.querySelector(
          ".apexcharts-canvas-series-wrap"
        );
        if (wrapNode && rr.canRepaintInPlace && rr.canRepaintInPlace()) {
          rr.repaintInPlace();
          const groups = Array.isArray(elGraph) ? elGraph : [elGraph];
          groups.forEach((g2) => {
            if (g2 && g2.node) wrapNode.appendChild(g2.node);
          });
          return [];
        }
      }
      const host = ctx.renderer.present();
      if (host) {
        const wrap = new Graphics(w2).group({
          class: "apexcharts-canvas-series-wrap"
        });
        wrap.add(host);
        const groups = Array.isArray(elGraph) ? elGraph : [elGraph];
        groups.forEach((g2) => {
          if (g2) wrap.add(g2);
        });
        return wrap;
      }
    }
    return elGraph;
  }
  /**
   * Lazily instantiate the shared series renderers the orchestration below
   * needs. getChartClass() is only called for types actually present, so a page
   * that registers just 'line' never triggers a lookup for 'candlestick' etc.
   * (tree-shaking). Returns the line/candlestick/violin renderers and sets
   * ctx.pie / ctx.rangeBar as a side effect (both may be null). Extracted from
   * plotChartType (audit C2).
   * @param {Record<string, any>} seriesTypes
   * @param {import('../types/internal').XYRatios} xyRatios
   * @returns {{ line: any, boxCandlestick: any, violin: any }}
   */
  _instantiateSeriesRenderers(seriesTypes, xyRatios) {
    const { w: w2, ctx } = this;
    const { config: cnf, globals: gl } = w2;
    const needsLine = seriesTypes.line.series.length > 0 || seriesTypes.area.series.length > 0 || seriesTypes.scatter.series.length > 0 || seriesTypes.bubble.series.length > 0 || seriesTypes.rangeArea.series.length > 0 || !gl.comboCharts && ["line", "area", "scatter", "bubble", "rangeArea"].includes(
      cnf.chart.type
    );
    const line = needsLine ? new (getChartClass("line"))(ctx.w, ctx, xyRatios) : null;
    const needsCandlestick = seriesTypes.candlestick.series.length > 0 || seriesTypes.boxPlot.series.length > 0 || !gl.comboCharts && ["candlestick", "boxPlot"].includes(cnf.chart.type);
    const boxCandlestick = needsCandlestick ? new (getChartClass("candlestick"))(ctx.w, ctx, xyRatios) : null;
    const needsViolin = seriesTypes.violin.series.length > 0 || !gl.comboCharts && cnf.chart.type === "violin";
    const violin = needsViolin ? new (getChartClass("violin"))(ctx.w, ctx, xyRatios) : null;
    const needsPie = !gl.comboCharts && ["pie", "donut", "polarArea"].includes(cnf.chart.type);
    ctx.pie = needsPie ? new (getChartClass("pie"))(ctx.w, ctx) : null;
    const needsRangeBar = seriesTypes.rangeBar.series.length > 0 || !gl.comboCharts && cnf.chart.type === "rangeBar";
    ctx.rangeBar = needsRangeBar ? new (getChartClass("rangeBar"))(ctx.w, ctx, xyRatios) : null;
    return { line, boxCandlestick, violin };
  }
  /**
   * Build the ordered list of series graphic groups (elGraph) from the
   * classified buckets. Combo charts layer per-type draws in z-order; a
   * single-type chart dispatches on cnf.chart.type. Sets ctx.bar for the
   * non-stacked bar paths (side effect, preserved). Extracted from
   * plotChartType (audit C2).
   * @param {Record<string, any>} seriesTypes
   * @param {Record<string, {series: any[], i: number[]}>} customBuckets
   * @param {{ line: any, boxCandlestick: any, violin: any }} renderers
   * @param {import('../types/internal').XYRatios} xyRatios
   * @returns {any}
   */
  _assembleSeriesGraphics(seriesTypes, customBuckets, renderers, xyRatios) {
    const { w: w2, ctx } = this;
    const { config: cnf, globals: gl } = w2;
    const { line, boxCandlestick, violin } = renderers;
    let elGraph = [];
    if (gl.comboCharts) {
      const coreUtils = new CoreUtils(this.w);
      if (seriesTypes.area.series.length > 0) {
        elGraph.push(
          ...coreUtils.drawSeriesByGroup(
            seriesTypes.area,
            gl.areaGroups,
            "area",
            line
          )
        );
      }
      if (seriesTypes.bar.series.length > 0) {
        if (cnf.chart.stacked) {
          const barStacked = new (getChartClass("barStacked"))(
            ctx.w,
            ctx,
            xyRatios
          );
          elGraph.push(
            barStacked.draw(seriesTypes.bar.series, seriesTypes.bar.i)
          );
        } else {
          ctx.bar = new (getChartClass("bar"))(ctx.w, ctx, xyRatios);
          elGraph.push(ctx.bar.draw(seriesTypes.bar.series, seriesTypes.bar.i));
        }
      }
      if (seriesTypes.rangeArea.series.length > 0) {
        elGraph.push(
          line.draw(
            seriesTypes.rangeArea.series,
            "rangeArea",
            seriesTypes.rangeArea.i,
            seriesTypes.rangeArea.seriesRangeEnd
          )
        );
      }
      if (seriesTypes.line.series.length > 0) {
        elGraph.push(
          ...coreUtils.drawSeriesByGroup(
            seriesTypes.line,
            gl.lineGroups,
            "line",
            line
          )
        );
      }
      if (seriesTypes.candlestick.series.length > 0) {
        elGraph.push(
          boxCandlestick.draw(
            seriesTypes.candlestick.series,
            "candlestick",
            seriesTypes.candlestick.i
          )
        );
      }
      if (seriesTypes.boxPlot.series.length > 0) {
        elGraph.push(
          boxCandlestick.draw(
            seriesTypes.boxPlot.series,
            "boxPlot",
            seriesTypes.boxPlot.i
          )
        );
      }
      if (seriesTypes.violin.series.length > 0) {
        elGraph.push(
          violin.draw(seriesTypes.violin.series, "violin", seriesTypes.violin.i)
        );
      }
      if (seriesTypes.rangeBar.series.length > 0) {
        elGraph.push(
          ctx.rangeBar.draw(
            seriesTypes.rangeBar.series,
            seriesTypes.rangeBar.i
          )
        );
      }
      if (seriesTypes.scatter.series.length > 0) {
        const scatterLine = new (getChartClass("line"))(
          ctx.w,
          ctx,
          xyRatios,
          true
        );
        elGraph.push(
          scatterLine.draw(
            seriesTypes.scatter.series,
            "scatter",
            seriesTypes.scatter.i
          )
        );
      }
      if (seriesTypes.bubble.series.length > 0) {
        const bubbleLine = new (getChartClass("line"))(
          ctx.w,
          ctx,
          xyRatios,
          true
        );
        elGraph.push(
          bubbleLine.draw(
            seriesTypes.bubble.series,
            "bubble",
            seriesTypes.bubble.i
          )
        );
      }
      Object.keys(customBuckets).forEach((cname) => {
        const bucket = customBuckets[cname];
        if (bucket.series.length > 0) {
          const cs = new (getChartClass(cname))(ctx.w, ctx, xyRatios);
          elGraph.push(cs.draw(bucket.series, cname, bucket.i));
        }
      });
    } else {
      const type = cnf.chart.type;
      switch (type) {
        case "line":
          elGraph = line.draw(this.w.seriesData.series, "line");
          break;
        case "area":
          elGraph = line.draw(this.w.seriesData.series, "area");
          break;
        case "bar":
          if (cnf.chart.stacked) {
            const barStacked = new (getChartClass("barStacked"))(
              ctx.w,
              ctx,
              xyRatios
            );
            elGraph = barStacked.draw(this.w.seriesData.series);
          } else {
            ctx.bar = new (getChartClass("bar"))(ctx.w, ctx, xyRatios);
            elGraph = ctx.bar.draw(this.w.seriesData.series);
          }
          break;
        case "candlestick":
          elGraph = boxCandlestick.draw(this.w.seriesData.series, "candlestick");
          break;
        case "boxPlot":
          elGraph = boxCandlestick.draw(this.w.seriesData.series, type);
          break;
        case "violin":
          elGraph = violin.draw(this.w.seriesData.series, "violin");
          break;
        case "rangeBar":
          elGraph = ctx.rangeBar.draw(this.w.seriesData.series);
          break;
        case "rangeArea":
          elGraph = line.draw(
            this.w.rangeData.seriesRangeStart,
            "rangeArea",
            void 0,
            this.w.rangeData.seriesRangeEnd
          );
          break;
        case "heatmap": {
          const heatmap = new (getChartClass("heatmap"))(ctx.w, ctx, xyRatios);
          elGraph = heatmap.draw(this.w.seriesData.series);
          break;
        }
        case "treemap": {
          const treemap = new (getChartClass("treemap"))(ctx.w, ctx);
          elGraph = treemap.draw(this.w.seriesData.series);
          break;
        }
        case "unit": {
          const unit = new (getChartClass("unit"))(ctx.w, ctx);
          elGraph = unit.draw(this.w.seriesData.series);
          break;
        }
        case "sunburst": {
          const sunburst = new (getChartClass("sunburst"))(ctx.w, ctx);
          elGraph = sunburst.draw(this.w.seriesData.series);
          break;
        }
        case "pie":
        case "donut":
        case "polarArea":
          elGraph = ctx.pie.draw(this.w.seriesData.series);
          break;
        case "radialBar": {
          const radialBar = new (getChartClass("radialBar"))(ctx.w, ctx);
          elGraph = radialBar.draw(this.w.seriesData.series);
          break;
        }
        case "radar": {
          const radar = new (getChartClass("radar"))(ctx.w, ctx);
          elGraph = radar.draw(this.w.seriesData.series);
          break;
        }
        default:
          if (isCustom(type)) {
            const cs = new (getChartClass(type))(ctx.w, ctx, xyRatios);
            elGraph = cs.draw(this.w.seriesData.series, type);
          } else {
            elGraph = line.draw(this.w.seriesData.series);
          }
      }
    }
    return elGraph;
  }
  /**
   * Extract the CSS unit suffix from a chart width/height config value
   * (`'100%'` -> `'%'`, `'300px'` -> `'px'`, `300`/`'300'` -> `''`). Kept as the
   * original digit-split idiom so results are identical for every input the
   * call sites already handle ('%', 'px', '', 'auto').
   * @param {string|number} value
   * @returns {string|undefined}
   */
  _extractDimensionUnit(value) {
    return String(value).split(/[0-9]+/g).pop();
  }
  setSVGDimensions() {
    var _a;
    const { globals: gl, config: cnf } = this.w;
    cnf.chart.width = cnf.chart.width || "100%";
    cnf.chart.height = cnf.chart.height || "auto";
    const rawWidth = cnf.chart.width;
    const rawHeight = cnf.chart.height;
    gl.svgWidth = NaN;
    gl.svgHeight = NaN;
    let elDim = Utils$1.getDimensions(this.el);
    const widthUnit = this._extractDimensionUnit(rawWidth);
    if (widthUnit === "%") {
      if (Utils$1.isNumber(elDim[0])) {
        if (elDim[0].width === 0) {
          elDim = Utils$1.getDimensions(this.el.parentNode);
        }
        gl.svgWidth = elDim[0] * parseInt(rawWidth, 10) / 100;
      }
    } else if (widthUnit === "px" || widthUnit === "") {
      gl.svgWidth = parseInt(rawWidth, 10);
    }
    const heightUnit = this._extractDimensionUnit(rawHeight);
    if (rawHeight !== "auto" && rawHeight !== "") {
      if (heightUnit === "%") {
        const elParentDim = Utils$1.getDimensions(this.el.parentNode);
        gl.svgHeight = elParentDim[1] * parseInt(rawHeight, 10) / 100;
      } else {
        gl.svgHeight = parseInt(rawHeight, 10);
      }
    } else {
      gl.svgHeight = gl.axisCharts ? gl.svgWidth / 1.61 : gl.svgWidth / 1.2;
    }
    gl.svgWidth = Math.max(gl.svgWidth, 0);
    gl.svgHeight = Math.max(gl.svgHeight, 0);
    Graphics.setAttrs(this.w.dom.Paper.node, {
      width: gl.svgWidth,
      height: gl.svgHeight
    });
    if (heightUnit !== "%" && Environment.isBrowser()) {
      const needsAxisPadding = gl.axisCharts && (cnf.grid.show || cnf.dataLabels.enabled || cnf.xaxis.labels.show || cnf.xaxis.axisBorder.show || cnf.xaxis.axisTicks.show || cnf.yaxis.some(
        (y) => y.show && (y.labels.show || y.axisBorder.show || y.axisTicks.show)
      ));
      const offsetY = cnf.chart.sparkline.enabled || !needsAxisPadding ? 0 : cnf.chart.parentHeightOffset;
      const paperNode = this.w.dom.Paper.node;
      if ((_a = paperNode.parentNode) == null ? void 0 : _a.parentNode) {
        paperNode.parentNode.parentNode.style.minHeight = `${gl.svgHeight + offsetY}px`;
      }
    }
    this.w.dom.elWrap.style.width = `${gl.svgWidth}px`;
    this.w.dom.elWrap.style.height = `${gl.svgHeight}px`;
    gl.lastResizeSignature = this.getResizeSignature();
  }
  /**
   * A stable fingerprint of the container measurements that actually feed the
   * chart's rendered dimensions, given the width/height config. Only inputs that
   * a resize can change are included: the element width when the width is a
   * percentage, and the parent height when the height is a percentage. A fully
   * pixel-sized chart therefore has a constant signature, so a resize never
   * forces a redraw; a percentage-sized chart's signature tracks the container.
   * The window-resize handler compares this against the last render to skip
   * redraws (and the entrance-animation teardown they cause) that aren't needed.
   * @returns {{ w: number, h: number }}
   */
  getResizeSignature() {
    const { config: cnf } = this.w;
    const rawWidth = (cnf.chart.width || "100%").toString().trim();
    const rawHeight = (cnf.chart.height || "auto").toString().trim();
    let w2 = 0;
    let h2 = 0;
    if (rawWidth.endsWith("%")) {
      let elDim = Utils$1.getDimensions(this.el);
      if (!elDim[0]) elDim = Utils$1.getDimensions(this.el.parentNode);
      w2 = elDim[0] || 0;
    }
    if (rawHeight.endsWith("%")) {
      h2 = Utils$1.getDimensions(this.el.parentNode)[1] || 0;
    }
    const sig = { w: Math.round(w2), h: Math.round(h2) };
    if (cnf.responsive && cnf.responsive.length && Environment.isBrowser()) {
      sig.iw = window.innerWidth;
    }
    return sig;
  }
  shiftGraphPosition() {
    const { globals: gl } = this.w;
    const { translateY: tY, translateX: tX } = gl;
    Graphics.setAttrs(this.w.dom.elGraphical.node, {
      transform: `translate(${tX}, ${tY})`
    });
  }
  resizeNonAxisCharts() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const { w: w2 } = this;
    const heightStr = w2.config.chart.height ? String(w2.config.chart.height) : "";
    const userSetFixedHeight = heightStr !== "" && heightStr !== "auto";
    const isPercentHeight = heightStr.includes("%");
    let legendHeight = 0;
    let offY = w2.config.chart.sparkline.enabled ? 1 : 15;
    offY += w2.layout.gridPad.bottom;
    if (["top", "bottom"].includes(w2.config.legend.position) && w2.config.legend.show && !w2.config.legend.floating) {
      legendHeight = ((_b = (_a = this.ctx.legend) == null ? void 0 : _a.legendHelpers.getLegendDimensions().clwh) != null ? _b : 0) + 7;
    }
    const el = w2.dom.baseEl.querySelector(
      ".apexcharts-radialbar, .apexcharts-pie, .apexcharts-sunburst"
    );
    const externalLabelMarginY = w2.globals.pieExternalLabelMarginY || 0;
    let chartInnerDimensions = externalLabelMarginY > 0 ? w2.globals.radialSize * 2 + externalLabelMarginY * 2 : w2.globals.radialSize * 2.05;
    const angleType = w2.config.chart.type === "sunburst" ? "sunburst" : w2.config.chart.type === "pie" || w2.config.chart.type === "donut" || w2.config.chart.type === "polarArea" ? "pie" : "radialBar";
    const radialAngleSpan = Math.abs(
      w2.config.plotOptions[angleType].endAngle - w2.config.plotOptions[angleType].startAngle
    );
    if (el && !w2.config.chart.sparkline.enabled && radialAngleSpan < 360) {
      const svgRect = Utils$1.getBoundingClientRect(this.w.dom.Paper.node);
      let arcTopFromSVGTop = Infinity;
      let arcBottomFromSVGTop = -Infinity;
      const accumulate = (node) => {
        var _a2, _b2, _c2, _d2;
        if ((_a2 = node.classList) == null ? void 0 : _a2.contains("apexcharts-radialbar-hollow")) {
          return;
        }
        const tag = (_c2 = (_b2 = node.tagName) == null ? void 0 : _b2.toLowerCase) == null ? void 0 : _c2.call(_b2);
        if (tag === "text" || tag === "tspan") return;
        const children = Array.from((_d2 = node.children) != null ? _d2 : []);
        if (children.length > 0) {
          children.forEach((c2) => accumulate(
            /** @type {Element} */
            c2
          ));
          return;
        }
        const r2 = Utils$1.getBoundingClientRect(node);
        const height = r2.bottom - r2.top;
        if (height > 0) {
          const top = r2.top - svgRect.top;
          const bottom = r2.bottom - svgRect.top;
          if (top < arcTopFromSVGTop) arcTopFromSVGTop = top;
          if (bottom > arcBottomFromSVGTop) arcBottomFromSVGTop = bottom;
        }
      };
      Array.from((_c = el.children) != null ? _c : []).forEach(
        (c2) => accumulate(
          /** @type {Element} */
          c2
        )
      );
      if (!Number.isFinite(arcTopFromSVGTop)) arcTopFromSVGTop = 0;
      if (!Number.isFinite(arcBottomFromSVGTop)) {
        const elRect = Utils$1.getBoundingClientRect(el);
        arcBottomFromSVGTop = elRect.bottom - svgRect.top;
      }
      const padding = Math.max(offY, w2.globals.radialSize * 0.2);
      const verticalShift = Math.max(padding - arcTopFromSVGTop, 0);
      if (verticalShift !== 0) {
        w2.layout.translateY = ((_d = w2.layout.translateY) != null ? _d : 0) + verticalShift;
        Graphics.setAttrs(this.w.dom.elGraphical.node, {
          transform: `translate(${(_e = w2.layout.translateX) != null ? _e : 0}, ${w2.layout.translateY})`
        });
        arcBottomFromSVGTop += verticalShift;
      }
      chartInnerDimensions = arcBottomFromSVGTop > 0 ? arcBottomFromSVGTop : w2.globals.radialSize * 2.05;
      const bottomPadding = Math.max(padding, arcTopFromSVGTop);
      const svgHeight = Math.ceil(
        chartInnerDimensions + legendHeight + bottomPadding
      );
      const chartOffsetY = (_f = w2.config.chart.offsetY) != null ? _f : 0;
      const elWrapHeight = svgHeight + Math.max(chartOffsetY, 0);
      if (!isPercentHeight) {
        if (this.w.dom.elLegendForeign) {
          this.w.dom.elLegendForeign.setAttribute(
            "height",
            String(elWrapHeight)
          );
        }
        this.w.dom.elWrap.style.height = `${elWrapHeight}px`;
        Graphics.setAttrs(this.w.dom.Paper.node, { height: svgHeight });
        if (Environment.isBrowser()) {
          const grandparent = (_g = this.w.dom.Paper.node.parentNode) == null ? void 0 : _g.parentNode;
          if (grandparent) {
            grandparent.style.minHeight = `${elWrapHeight}px`;
          }
        }
        w2.globals.svgHeight = svgHeight;
        if (w2.config.legend.position === "bottom" && w2.config.legend.show && !w2.config.legend.floating) {
          (_h = this.ctx.legend) == null ? void 0 : _h.setLegendWrapXY(20, 0);
        }
      }
      return;
    }
    const newHeight = Math.ceil(
      chartInnerDimensions + this.w.layout.translateY + legendHeight + offY
    );
    if (userSetFixedHeight) return;
    if (this.w.dom.elLegendForeign) {
      this.w.dom.elLegendForeign.setAttribute("height", String(newHeight));
    }
    this.w.dom.elWrap.style.height = `${newHeight}px`;
    Graphics.setAttrs(this.w.dom.Paper.node, { height: newHeight });
    if (Environment.isBrowser()) {
      const grandparent = (_i = this.w.dom.Paper.node.parentNode) == null ? void 0 : _i.parentNode;
      if (grandparent) {
        grandparent.style.minHeight = `${newHeight}px`;
      }
    }
  }
  coreCalculations() {
    new Range(this.w).init();
  }
  resetGlobals() {
    const resetxyValues = () => this.w.config.series.map(() => []);
    const globalObj = new Globals();
    const { globals: gl } = this.w;
    const parsingFlags = {
      dataWasParsed: this.w.axisFlags.dataWasParsed,
      originalSeries: gl.originalSeries
    };
    globalObj.initGlobalVars(gl);
    gl.seriesXvalues = resetxyValues();
    gl.seriesYvalues = resetxyValues();
    if (parsingFlags.dataWasParsed) {
      this.w.axisFlags.dataWasParsed = parsingFlags.dataWasParsed;
      gl.originalSeries = parsingFlags.originalSeries;
    }
  }
  isMultipleY() {
    if (Array.isArray(this.w.config.yaxis) && this.w.config.yaxis.length > 1) {
      this.w.globals.isMultipleYAxis = true;
      return true;
    }
    return false;
  }
  xySettings() {
    const { w: w2 } = this;
    let xyRatios = null;
    if (w2.globals.axisCharts) {
      if (w2.config.xaxis.crosshairs.position === "back") {
        new Crosshairs(this.w).drawXCrosshairs();
      }
      if (w2.config.yaxis[0].crosshairs.position === "back") {
        new Crosshairs(this.w).drawYCrosshairs();
      }
      if (w2.config.xaxis.type === "datetime" && w2.config.xaxis.labels.formatter === void 0) {
        this.ctx.timeScale = new TimeScale(this.w, this.ctx);
        let formattedTimeScale = [];
        if (isFinite(w2.globals.minX) && isFinite(w2.globals.maxX) && !w2.globals.isBarHorizontal) {
          formattedTimeScale = this.ctx.timeScale.calculateTimeScaleTicks(
            w2.globals.minX,
            w2.globals.maxX
          );
        } else if (w2.globals.isBarHorizontal) {
          formattedTimeScale = this.ctx.timeScale.calculateTimeScaleTicks(
            w2.globals.minY,
            w2.globals.maxY
          );
        }
        this.ctx.timeScale.recalcDimensionsBasedOnFormat(formattedTimeScale);
      }
      const coreUtils = new CoreUtils(this.w);
      xyRatios = coreUtils.getCalculatedRatios();
    }
    return xyRatios;
  }
  /**
   * @param {any} targetChart
   */
  updateSourceChart(targetChart) {
    this.ctx.w.interact.selection = void 0;
    this.ctx.updateHelpers._updateOptions(
      {
        chart: {
          selection: {
            xaxis: {
              min: targetChart.w.globals.minX,
              max: targetChart.w.globals.maxX
            }
          }
        }
      },
      false,
      false
    );
  }
  setupBrushHandler() {
    const { ctx, w: w2 } = this;
    if (!w2.config.chart.brush.enabled) return;
    if (typeof w2.config.chart.events.selection !== "function") {
      const targets = Array.isArray(w2.config.chart.brush.targets) ? w2.config.chart.brush.targets : [w2.config.chart.brush.target];
      targets.forEach((target) => {
        const targetChart = (
          /** @type {any} */
          ctx.constructor.getChartByID(
            target
          )
        );
        if (!targetChart) {
          console.warn(
            `ApexCharts: brush target "${target}" was not found. Ensure the target chart is rendered (and its chart.id matches) before the brush chart.`
          );
          return;
        }
        targetChart.w.globals.brushSource = this.ctx;
        if (typeof targetChart.w.config.chart.events.zoomed !== "function") {
          targetChart.w.config.chart.events.zoomed = () => this.updateSourceChart(targetChart);
        }
        if (typeof targetChart.w.config.chart.events.scrolled !== "function") {
          targetChart.w.config.chart.events.scrolled = () => (
            /**
             * @param {any} chart
             * @param {Event} e
             */
            this.updateSourceChart(targetChart)
          );
        }
      });
      w2.config.chart.events.selection = (chart, e2) => {
        targets.forEach((target) => {
          const targetChart = (
            /** @type {any} */
            ctx.constructor.getChartByID(
              target
            )
          );
          if (!targetChart) return;
          targetChart.ctx.updateHelpers._updateOptions(
            {
              xaxis: {
                min: e2.xaxis.min,
                max: e2.xaxis.max
              }
            },
            false,
            false,
            false,
            false
          );
        });
      };
    }
  }
  getAccessibleChartLabel() {
    const w2 = this.w;
    const cnf = w2.config;
    if (cnf.chart.accessibility && cnf.chart.accessibility.description) {
      return cnf.chart.accessibility.description;
    }
    const chartType = cnf.chart.type;
    const parts = [];
    if (cnf.title.text) {
      parts.push(`${cnf.title.text}. ${chartType} chart`);
      if (cnf.subtitle.text) parts.push(cnf.subtitle.text);
    } else {
      const namedSeries = (() => {
        if (Array.isArray(w2.seriesData.seriesNames) && w2.seriesData.seriesNames.length) {
          return w2.seriesData.seriesNames.filter(Boolean);
        }
        if (Array.isArray(cnf.series)) {
          return cnf.series.map((s2) => typeof s2 === "object" && s2 !== null ? s2.name : null).filter(Boolean);
        }
        return [];
      })();
      const seriesCount = w2.seriesData.series.length || (cnf.series ? cnf.series.length : 0);
      if (namedSeries.length) {
        parts.push(
          `${chartType} chart with ${seriesCount} data series: ${namedSeries.join(", ")}`
        );
      } else {
        parts.push(`${chartType} chart with ${seriesCount} data series`);
      }
    }
    return parts.join(". ");
  }
}
const TRANSFORM_KEY = "__apexcharts_series_transforms__";
if (!/** @type {any} */
globalThis[TRANSFORM_KEY]) {
  globalThis[TRANSFORM_KEY] = {};
}
function getTransforms() {
  return (
    /** @type {any} */
    globalThis[TRANSFORM_KEY]
  );
}
function registerSeriesTransform(name2, fn) {
  if (!name2 || typeof name2 !== "string") {
    console.warn(
      "ApexCharts: registerSeriesTransform requires a non-empty name."
    );
    return;
  }
  if (typeof fn !== "function") {
    console.warn(
      `ApexCharts: registerSeriesTransform("${name2}") expects a function (series, w) => series.`
    );
    return;
  }
  getTransforms()[name2] = fn;
}
function getSeriesTransform(name2) {
  if (!name2) return null;
  return getTransforms()[name2] || null;
}
function drilldownById(w2, id) {
  const dd = w2.config.drilldown;
  const list = dd && Array.isArray(dd.series) ? dd.series : [];
  return list.find((s2) => s2 && s2.id === id);
}
function toNode(w2, d, i2, paletteFromParent, parentKey, seenIds = null, opts = {}) {
  var _a, _b, _c;
  const isObj = d && typeof d === "object";
  const name2 = isObj ? (_b = (_a = d.x) != null ? _a : d.name) != null ? _b : "" : "";
  const value = isObj ? Number((_c = d.y) != null ? _c : d.value) : Number(d);
  const node = {
    name: String(name2),
    value: isNaN(value) ? null : value,
    color: isObj && d.color ? d.color : void 0,
    // Identity across data updates: the path of names (indexed so same-named
    // siblings stay distinct). Update animations morph matched keys in place.
    _key: `${parentKey}/${i2}:${name2}`
  };
  if (paletteFromParent && !node.color) {
    node.color = paletteFromParent[i2 % paletteFromParent.length];
  }
  if (opts.keepDatum) node._datum = d;
  if (isObj && Array.isArray(d.children) && d.children.length) {
    node.children = d.children.map(
      (c2, j) => toNode(w2, c2, j, null, node._key, seenIds, opts)
    );
  } else if (isObj && d.drilldown != null && opts.expandDrilldown !== false) {
    const visited = seenIds || /* @__PURE__ */ new Set();
    if (!visited.has(d.drilldown)) {
      const dd = drilldownById(w2, d.drilldown);
      if (dd && Array.isArray(dd.data) && dd.data.length) {
        const nextSeen = new Set(visited);
        nextSeen.add(d.drilldown);
        const palette = Array.isArray(dd.colors) ? dd.colors : null;
        node.children = dd.data.map(
          (c2, j) => toNode(w2, c2, j, palette, node._key, nextSeen, opts)
        );
      }
    }
  }
  return node;
}
function buildHierarchy(w2, opts = {}) {
  const cfgSeries = (
    /** @type {any} */
    w2.config.series
  );
  const first = cfgSeries && cfgSeries[0];
  const data = first && Array.isArray(first.data) ? first.data : cfgSeries;
  if (!Array.isArray(data)) return [];
  return data.map(
    (d, i2) => toNode(w2, d, i2, null, "", null, opts)
  );
}
function buildSeriesRoots(w2, series, opts = {}) {
  const cfgSeries = (
    /** @type {any} */
    series || w2.config.series
  );
  if (!Array.isArray(cfgSeries)) return [];
  return cfgSeries.map((s2, i2) => {
    var _a, _b;
    const data = s2 && Array.isArray(s2.data) ? s2.data : [];
    const key = `${i2}:${(_a = s2 == null ? void 0 : s2.name) != null ? _a : ""}`;
    const root = {
      name: String((_b = s2 == null ? void 0 : s2.name) != null ? _b : ""),
      value: null,
      color: (s2 == null ? void 0 : s2.color) || void 0,
      _key: key,
      _seriesIndex: i2,
      children: data.map(
        (d, j) => toNode(w2, d, j, null, key, null, opts)
      )
    };
    return root;
  });
}
function fillValues(node) {
  if (node.children && node.children.length) {
    node.children.forEach((c2) => fillValues(c2));
    if (node.value == null || isNaN(node.value)) {
      node.value = node.children.reduce(
        (s2, c2) => s2 + Math.max(0, c2.value || 0),
        0
      );
    }
  }
  if (node.value == null || isNaN(node.value)) node.value = 0;
}
function hasNesting(series, opts = {}) {
  if (!Array.isArray(series)) return false;
  const countDrilldown = opts.drilldown !== false;
  for (let i2 = 0; i2 < series.length; i2++) {
    const data = series[i2] && series[i2].data;
    if (!Array.isArray(data)) continue;
    for (let j = 0; j < data.length; j++) {
      const d = data[j];
      if (!d || typeof d !== "object") continue;
      if (Array.isArray(d.children) && d.children.length) return true;
      if (countDrilldown && d.drilldown != null) return true;
    }
  }
  return false;
}
function morphKey(key) {
  if (typeof key !== "string") return "";
  const i2 = key.indexOf("/");
  return i2 === -1 ? "" : key.slice(i2);
}
function drilldownAsLevels(w2) {
  var _a, _b, _c, _d;
  return !!((_d = (_c = (_b = (_a = w2 == null ? void 0 : w2.config) == null ? void 0 : _a.plotOptions) == null ? void 0 : _b.treemap) == null ? void 0 : _c.nested) == null ? void 0 : _d.drilldownAsLevels);
}
function isNestedTreemap(w2, series) {
  var _a, _b, _c;
  const nestedCfg = (_c = (_b = (_a = w2 == null ? void 0 : w2.config) == null ? void 0 : _a.plotOptions) == null ? void 0 : _b.treemap) == null ? void 0 : _c.nested;
  if (nestedCfg && nestedCfg.enabled === false) return false;
  return hasNesting(series, { drilldown: drilldownAsLevels(w2) });
}
function annotate(roots) {
  const leaves = [];
  let maxDepth = 0;
  roots.forEach((root, si) => {
    const seriesLeaves = [];
    const walk = (node, depth, parent) => {
      node._parent = parent;
      node._depth = depth;
      node._si = si;
      node._leaf = !(node.children && node.children.length);
      if (depth > maxDepth) maxDepth = depth;
      if (node._leaf) {
        node._di = seriesLeaves.length;
        seriesLeaves.push(node);
      } else {
        node._di = -1;
        node.children.forEach(
          (c2) => walk(c2, depth + 1, node)
        );
      }
    };
    walk(root, 0, null);
    leaves.push(seriesLeaves);
  });
  return { leaves, maxDepth };
}
function leafRow(node) {
  const d = node._datum;
  if (d && typeof d === "object") {
    const row = __spreadProps(__spreadValues({}, d), { x: node.name, y: node.value });
    delete row.children;
    return row;
  }
  return { x: node.name, y: node.value };
}
function resolveTreemapTree(w2, series) {
  const roots = buildSeriesRoots(w2, series, {
    keepDatum: true,
    expandDrilldown: drilldownAsLevels(w2)
  });
  roots.forEach(fillValues);
  const { leaves, maxDepth } = annotate(roots);
  const leafSeries = series.map((s2, i2) => __spreadProps(__spreadValues({}, s2), {
    data: (leaves[i2] || []).map(leafRow)
  }));
  return { roots, leafSeries, maxDepth };
}
function getTreemapRoots(w2) {
  const stashed = w2.globals.treemapRoots;
  if (stashed && stashed.length) {
    return {
      roots: stashed,
      maxDepth: w2.globals.treemapMaxDepth || 1,
      nested: true
    };
  }
  const roots = buildSeriesRoots(w2, w2.config.series, {
    keepDatum: true,
    expandDrilldown: false
  });
  roots.forEach(fillValues);
  const { maxDepth } = annotate(roots);
  return { roots, maxDepth, nested: false };
}
const RAW_SAMPLE_TYPES = ["histogram"];
class Data {
  /**
   * @param {import('../types/internal').ChartStateW} w
   */
  constructor(w2, { resetGlobals = () => {
  }, isMultipleY = () => {
  } } = {}) {
    this.w = w2;
    this.resetGlobals = resetGlobals;
    this.isMultipleY = isMultipleY;
    this.twoDSeries = [];
    this.threeDSeries = [];
    this.twoDSeriesX = [];
    this.seriesGoals = [];
    this._warnedMissingTransform = false;
    this.coreUtils = new CoreUtils(this.w);
    this.activeSeriesIndex = 0;
  }
  // Helper to get the first valid data point from the active series
  getFirstDataPoint() {
    const series = this.w.config.series;
    const sr = new Series(this.w);
    this.activeSeriesIndex = sr.getActiveConfigSeriesIndex();
    const activeItem = (
      /** @type {any} */
      series[this.activeSeriesIndex]
    );
    if (activeItem && activeItem.data && activeItem.data.length > 0 && activeItem.data[0] !== null && typeof activeItem.data[0] !== "undefined") {
      return activeItem.data[0];
    }
    return null;
  }
  isMultiFormat() {
    return this.isFormatXY() || this.isFormat2DArray();
  }
  // given format is [{x, y}, {x, y}]
  isFormatXY() {
    var _a;
    const firstDataPoint = this.getFirstDataPoint();
    if (!firstDataPoint || typeof firstDataPoint.x === "undefined") return false;
    const data = (
      /** @type {any} */
      (_a = this.w.config.series[this.activeSeriesIndex]) == null ? void 0 : _a.data
    );
    if (data) {
      const isXY = (pt) => pt && typeof pt.x !== "undefined";
      for (let k = 1; k < Math.min(3, data.length); k++) {
        if (isXY(data[k]) !== true) {
          console.warn(
            `ApexCharts: series data has mixed formats starting at index ${k}`
          );
          break;
        }
      }
    }
    return true;
  }
  // given format is [[x, y], [x, y]]
  isFormat2DArray() {
    const firstDataPoint = this.getFirstDataPoint();
    return firstDataPoint && Array.isArray(firstDataPoint);
  }
  /**
   * Typed single pass for the dominant [[x, y], ...] shape: scalar numeric or
   * null y, no z, no OHLC tuples. One monomorphic loop fills preallocated
   * x/y arrays and fuses the y-extrema scan that Range.getMinYMaxY would
   * otherwise repeat over every value (the extrema entry is ref+length
   * guarded, so any later reshaping of the series array simply falls back to
   * the scan). Returns false untouched on any non-conforming point so the
   * general loop below handles mixed/exotic data with unchanged output.
   * @param {any[]} data
   * @param {number} i
   * @returns {boolean}
   */
  _fast2DArrayParse(data, i2) {
    var _a, _b;
    const n2 = data.length;
    if (n2 === 0) return false;
    const ys = new Array(n2);
    const xs = new Array(n2);
    let maxY = -Number.MAX_VALUE;
    let lowestY = Number.MAX_VALUE;
    let negMinY = Infinity;
    let hasNulls = false;
    let yDec = 0;
    let xNumeric = true;
    let minX = Infinity;
    let maxX = -Infinity;
    let xSorted = true;
    let minXDiff = Infinity;
    let prevX = NaN;
    for (let j = 0; j < n2; j++) {
      const point = data[j];
      if (!Array.isArray(point) || point.length > 2) return false;
      const x = point[0];
      const y = point[1];
      if (xNumeric) {
        if (typeof x === "number") {
          if (x === x) {
            if (x < minX) minX = x;
            if (x > maxX) maxX = x;
          }
          const d = x - prevX;
          if (d > 0) {
            if (d < minXDiff) minXDiff = d;
          } else if (d < 0) {
            xSorted = false;
          }
          prevX = x;
        } else {
          xNumeric = false;
        }
      }
      if (typeof y === "number") {
        if (y === y && y !== Infinity && y !== -Infinity) {
          if (y > maxY) maxY = y;
          if (y < lowestY) lowestY = y;
          if (y < 0 && y < negMinY) negMinY = y;
          if (!Number.isInteger(y)) {
            const av = y < 0 ? -y : y;
            if (av >= 1e-6 && av < 1e21) {
              const str = "" + y;
              const dot = str.indexOf(".");
              const dec = dot === -1 ? 0 : str.length - dot - 1;
              if (dec > yDec) yDec = dec;
            } else {
              const nv = Utils$1.noExponents(y);
              if (Utils$1.isFloat(nv)) {
                yDec = Math.max(yDec, nv.toString().split(".")[1].length);
              }
            }
          }
        } else {
          hasNulls = true;
        }
      } else if (y === null) {
        hasNulls = true;
      } else {
        return false;
      }
      ys[j] = y;
      xs[j] = x;
    }
    this.twoDSeries = ys;
    this.twoDSeriesX = xs;
    this.w.axisFlags.dataFormatXNumeric = true;
    const extrema = (_b = (_a = this.w.seriesData)._parsedExtrema) != null ? _b : _a._parsedExtrema = [];
    extrema[i2] = {
      ref: ys,
      len: n2,
      maxY,
      lowestY,
      negMinY,
      hasNulls,
      yDec,
      xref: xs,
      xNumeric,
      minX,
      maxX,
      xSorted,
      minXDiff
    };
    return true;
  }
  /**
   * @param {any[]} ser
   * @param {number} i
   */
  handleFormat2DArray(ser, i2) {
    const cnf = this.w.config;
    const data = ser[i2].data;
    const isBoxPlot = cnf.chart.type === "boxPlot" || /** @type {any} */
    cnf.series[i2].type === "boxPlot";
    if (!isBoxPlot && cnf.xaxis.type !== "datetime" && this._fast2DArrayParse(data, i2)) {
      return;
    }
    for (let j = 0; j < data.length; j++) {
      const point = data[j];
      const x = point[0];
      const y = point[1];
      const z = point[2];
      if (typeof y !== "undefined") {
        if (Array.isArray(y) && y.length === 4 && !isBoxPlot) {
          this.twoDSeries.push(Utils$1.parseNumber(y[3]));
        } else if (point.length >= 5) {
          this.twoDSeries.push(Utils$1.parseNumber(point[4]));
        } else {
          this.twoDSeries.push(Utils$1.parseNumber(y));
        }
        this.w.axisFlags.dataFormatXNumeric = true;
      }
      if (cnf.xaxis.type === "datetime") {
        const ts = new Date(x).getTime();
        this.twoDSeriesX.push(ts);
      } else {
        this.twoDSeriesX.push(x);
      }
      if (typeof z !== "undefined") {
        this.threeDSeries.push(z);
        this.w.axisFlags.isDataXYZ = true;
      }
    }
  }
  /**
   * @param {any[]} ser
   * @param {number} i
   */
  handleFormatXY(ser, i2) {
    const cnf = this.w.config;
    const gl = this.w.globals;
    const dt = new DateTime(this.w);
    const data = ser[i2].data;
    let activeI = i2;
    if (gl.collapsedSeriesIndices.indexOf(i2) > -1) {
      activeI = this.activeSeriesIndex;
    }
    const activeData = ser[activeI].data;
    for (let j = 0; j < data.length; j++) {
      const point = data[j];
      if (typeof point.y !== "undefined") {
        const val = Array.isArray(point.y) ? Utils$1.parseNumber(point.y[point.y.length - 1]) : Utils$1.parseNumber(point.y);
        this.twoDSeries.push(val);
      }
      if (typeof this.seriesGoals[i2] === "undefined") {
        this.seriesGoals[i2] = [];
      }
      if (typeof point.goals !== "undefined" && Array.isArray(point.goals)) {
        this.seriesGoals[i2].push(point.goals);
      } else {
        this.seriesGoals[i2].push(null);
      }
      if (typeof point.z !== "undefined") {
        this.threeDSeries.push(point.z);
        this.w.axisFlags.isDataXYZ = true;
      }
    }
    for (let j = 0; j < activeData.length; j++) {
      const point = activeData[j];
      const x = point.x;
      const isXString = typeof x === "string";
      const isXArr = Array.isArray(x);
      const isXDate = !isXArr && !!dt.isValidDate(x);
      if (isXString || isXDate) {
        if (isXString || cnf.xaxis.convertedCatToNumeric) {
          const isRangeColumn = gl.isBarHorizontal && this.w.axisFlags.isRangeData;
          if (cnf.xaxis.type === "datetime" && !isRangeColumn) {
            this.twoDSeriesX.push(dt.parseDate(x));
          } else {
            this.fallbackToCategory = true;
            this.twoDSeriesX.push(x);
            if (!isNaN(x) && this.w.config.xaxis.type !== "category" && typeof x !== "string") {
              this.w.axisFlags.isXNumeric = true;
            }
          }
        } else {
          if (cnf.xaxis.type === "datetime") {
            this.twoDSeriesX.push(dt.parseDate(x.toString()));
          } else {
            this.w.axisFlags.dataFormatXNumeric = true;
            this.w.axisFlags.isXNumeric = true;
            this.twoDSeriesX.push(parseFloat(x));
          }
        }
      } else if (isXArr) {
        this.fallbackToCategory = true;
        this.twoDSeriesX.push(x);
      } else {
        this.w.axisFlags.isXNumeric = true;
        this.w.axisFlags.dataFormatXNumeric = true;
        this.twoDSeriesX.push(x);
      }
    }
  }
  /**
   * @param {any[]} ser
   * @param {number} i
   */
  handleRangeData(ser, i2) {
    let range = { start: [], end: [], rangeUniques: [] };
    if (this.isFormat2DArray()) {
      range = this.handleRangeDataFormat("array", ser, i2);
    } else if (this.isFormatXY()) {
      range = this.handleRangeDataFormat("xy", ser, i2);
    }
    this.w.rangeData.seriesRangeStart[i2] = range.start === void 0 ? [] : range.start;
    this.w.rangeData.seriesRangeEnd[i2] = range.end === void 0 ? [] : range.end;
    this.w.rangeData.seriesRange[i2] = range.rangeUniques;
    this.w.rangeData.seriesRange.forEach((sr) => {
      if (!sr) return;
      sr.forEach((sarr) => {
        const yItems = (
          /** @type {any} */
          sarr.y
        );
        const len = (
          /** @type {any[]} */
          yItems.length
        );
        if (len <= 1) return;
        for (let arri = 0; arri < len; arri++) {
          const arr = (
            /** @type {any} */
            yItems[arri]
          );
          const range1y1 = arr.y1;
          const range1y2 = arr.y2;
          for (let sri = arri + 1; sri < len; sri++) {
            const range2 = (
              /** @type {any} */
              yItems[sri]
            );
            const range2y1 = range2.y1;
            const range2y2 = range2.y2;
            if (range1y1 <= range2y2 && range2y1 <= range1y2) {
              const sarrAny = (
                /** @type {any} */
                sarr
              );
              sarrAny.overlaps.add(arr.rangeName);
              sarrAny.overlaps.add(range2.rangeName);
            }
          }
        }
      });
    });
    return range;
  }
  /**
   * Marks (#11) P3: fold a custom series' per-datum y-extent into the
   * range-data slice so both bounds drive the y-axis scale. When `yExtent` is
   * given it supplies the values a datum occupies (scalar or array => min/max
   * across them); otherwise the datum's `y` is used (array => first/last,
   * scalar => itself). The datum still carries a representative scalar `y`
   * (folded by handleFormatXY into seriesData.series) that gates Range.
   * @param {any[]} ser @param {number} i @param {Function|null} yExtent
   */
  handleCustomRangeData(ser, i2, yExtent) {
    const data = ser[i2].data || [];
    const start = [];
    const end = [];
    for (let j = 0; j < data.length; j++) {
      const datum = data[j];
      let lo;
      let hi;
      if (typeof yExtent === "function") {
        let ext = yExtent(datum, j);
        if (!Array.isArray(ext)) ext = [ext];
        const nums = ext.map((v2) => Utils$1.parseNumber(v2)).filter((v2) => v2 !== null && !isNaN(v2));
        lo = nums.length ? Math.min(...nums) : null;
        hi = nums.length ? Math.max(...nums) : null;
      } else {
        const y = datum == null ? null : datum.y;
        if (Array.isArray(y)) {
          lo = Utils$1.parseNumber(y[0]);
          hi = Utils$1.parseNumber(y[y.length - 1]);
        } else {
          lo = hi = Utils$1.parseNumber(y);
        }
      }
      start.push(lo);
      end.push(hi);
    }
    this.w.rangeData.seriesRangeStart[i2] = start;
    this.w.rangeData.seriesRangeEnd[i2] = end;
  }
  /**
   * @param {any[]} ser
   * @param {number} i
   */
  handleCandleStickBoxData(ser, i2) {
    let ohlc = { o: [], h: [], m: [], l: [], c: [] };
    if (this.isFormat2DArray()) {
      ohlc = this.handleCandleStickBoxDataFormat("array", ser, i2);
    } else if (this.isFormatXY()) {
      ohlc = this.handleCandleStickBoxDataFormat("xy", ser, i2);
    }
    this.w.candleData.seriesCandleO[i2] = ohlc.o;
    this.w.candleData.seriesCandleH[i2] = ohlc.h;
    this.w.candleData.seriesCandleM[i2] = ohlc.m;
    this.w.candleData.seriesCandleL[i2] = ohlc.l;
    this.w.candleData.seriesCandleC[i2] = ohlc.c;
    this.w.candleData.seriesBoxPoints[i2] = ohlc.points || [];
    return ohlc;
  }
  /**
   * Parse a violin series. Each data point carries a precomputed density
   * profile (the violin shape) and an array of raw observations (the jitter):
   *
   *   { x, y: { density: [[value, weight], ...], points: [v1, v2, ...] } }
   *
   * Array fallback form: [x, densityPairs, pointsArray].
   *
   * Density `weight` need not be normalized — Violin.js scales each violin by
   * its own maxWeight. The representative scalar pushed into the main series
   * (so generic code paths see a non-null y) is the density mode — the value
   * carrying the greatest weight.
   *
   * @param {any[]} ser
   * @param {number} i
   */
  handleViolinData(ser, i2) {
    var _a, _b, _c, _d, _e, _f;
    const w2 = this.w;
    const data = ser[i2].data;
    const densityArr = [];
    const pointsArr = [];
    const minArr = [];
    const maxArr = [];
    const placeholders = [];
    for (let j = 0; j < data.length; j++) {
      const d = data[j];
      const dens = (_c = (_b = (_a = d == null ? void 0 : d.y) == null ? void 0 : _a.density) != null ? _b : d == null ? void 0 : d[1]) != null ? _c : [];
      const pts = (_f = (_e = (_d = d == null ? void 0 : d.y) == null ? void 0 : _d.points) != null ? _e : d == null ? void 0 : d[2]) != null ? _f : [];
      const values = [];
      const weights = [];
      let maxWeight = 0;
      let modeValue = null;
      let minVal = Infinity;
      let maxVal = -Infinity;
      for (let k = 0; k < dens.length; k++) {
        const v2 = Utils$1.parseNumber(dens[k][0]);
        const wt = Utils$1.parseNumber(dens[k][1]);
        if (v2 === null || wt === null) continue;
        values.push(v2);
        weights.push(wt);
        if (wt > maxWeight) {
          maxWeight = wt;
          modeValue = v2;
        }
        if (v2 < minVal) minVal = v2;
        if (v2 > maxVal) maxVal = v2;
      }
      const cleanPts = [];
      for (let k = 0; k < pts.length; k++) {
        const p2 = Utils$1.parseNumber(pts[k]);
        if (p2 === null) continue;
        cleanPts.push(p2);
        if (p2 < minVal) minVal = p2;
        if (p2 > maxVal) maxVal = p2;
      }
      densityArr.push({ values, weights, maxWeight });
      pointsArr.push(cleanPts);
      minArr.push(minVal === Infinity ? 0 : minVal);
      maxArr.push(maxVal === -Infinity ? 0 : maxVal);
      placeholders.push(
        modeValue !== null ? modeValue : cleanPts.length ? cleanPts[Math.floor(cleanPts.length / 2)] : 0
      );
    }
    w2.violinData.seriesViolinDensity[i2] = densityArr;
    w2.violinData.seriesViolinPoints[i2] = pointsArr;
    w2.violinData.seriesViolinMin[i2] = minArr;
    w2.violinData.seriesViolinMax[i2] = maxArr;
    this.twoDSeries = placeholders;
  }
  /**
   * @param {string} format
   * @param {any[]} ser
   * @param {number} i
   */
  handleRangeDataFormat(format, ser, i2) {
    const rangeStart = [];
    const rangeEnd = [];
    const uniqueKeysMap = /* @__PURE__ */ new Map();
    const uniqueKeys = [];
    ser[i2].data.forEach((item) => {
      if (!uniqueKeysMap.has(item.x)) {
        const keyObj = {
          x: item.x,
          overlaps: /* @__PURE__ */ new Set(),
          y: []
        };
        uniqueKeysMap.set(item.x, keyObj);
        uniqueKeys.push(keyObj);
      }
    });
    if (format === "array") {
      for (let j = 0; j < ser[i2].data.length; j++) {
        if (Array.isArray(ser[i2].data[j])) {
          rangeStart.push(ser[i2].data[j][1][0]);
          rangeEnd.push(ser[i2].data[j][1][1]);
        } else {
          rangeStart.push(ser[i2].data[j]);
          rangeEnd.push(ser[i2].data[j]);
        }
      }
    } else if (format === "xy") {
      for (let j = 0; j < ser[i2].data.length; j++) {
        const isDataPoint2D = Array.isArray(ser[i2].data[j].y);
        const id = Utils$1.randomId();
        const x = ser[i2].data[j].x;
        const y = {
          y1: isDataPoint2D ? ser[i2].data[j].y[0] : ser[i2].data[j].y,
          y2: isDataPoint2D ? ser[i2].data[j].y[1] : ser[i2].data[j].y,
          rangeName: id
        };
        const gl = this.w.globals;
        if (!gl.seriesRangeName) gl.seriesRangeName = {};
        if (!gl.seriesRangeName[i2]) gl.seriesRangeName[i2] = {};
        gl.seriesRangeName[i2][j] = id;
        const keyObj = uniqueKeysMap.get(x);
        if (keyObj) {
          keyObj.y.push(y);
        }
        rangeStart.push(y.y1);
        rangeEnd.push(y.y2);
      }
    }
    return {
      start: rangeStart,
      end: rangeEnd,
      rangeUniques: uniqueKeys
    };
  }
  /**
   * @param {string} format
   * @param {any[]} ser
   * @param {number} i
   */
  handleCandleStickBoxDataFormat(format, ser, i2) {
    const w2 = this.w;
    const isBoxPlot = w2.config.chart.type === "boxPlot" || /** @type {Record<string,any>} */
    w2.config.series[i2].type === "boxPlot";
    const serO = [];
    const serH = [];
    const serM = [];
    const serL = [];
    const serC = [];
    const serPoints = [];
    const data = ser[i2].data;
    let getVals;
    if (format === "array") {
      const first = data[0];
      const isFlat = isBoxPlot && first && first.length === 6 || !isBoxPlot && first && first.length === 5;
      if (isFlat) {
        getVals = (d) => d.slice(1);
      } else {
        getVals = (d) => Array.isArray(d[1]) ? d[1] : [];
      }
    } else {
      getVals = (d) => Array.isArray(d.y) ? d.y : [];
    }
    for (let j = 0; j < data.length; j++) {
      const vals = getVals(data[j]);
      if (vals && vals.length >= 2) {
        serO.push(vals[0]);
        serH.push(vals[1]);
        if (isBoxPlot) {
          serM.push(vals[2]);
          serL.push(vals[3]);
          serC.push(vals[4]);
        } else {
          serL.push(vals[2]);
          serC.push(vals[3]);
        }
      }
      const pts = data[j] && /** @type {any} */
      data[j].points;
      serPoints.push(Array.isArray(pts) ? pts : []);
    }
    return {
      o: serO,
      h: serH,
      m: serM,
      l: serL,
      c: serC,
      points: serPoints
    };
  }
  /**
   * @param {any[]} ser
   */
  parseDataAxisCharts(ser) {
    var _a, _b, _c, _d, _e, _f;
    const cnf = this.w.config;
    const gl = this.w.globals;
    const dt = new DateTime(this.w);
    this.w.seriesData._parsedExtrema = [];
    const xlabels = cnf.labels.length > 0 ? cnf.labels.slice() : cnf.xaxis.categories.slice();
    this.w.axisFlags.isRangeBar = cnf.chart.type === "rangeBar" && gl.isBarHorizontal;
    this.w.labelData.hasXaxisGroups = cnf.xaxis.type === "category" && cnf.xaxis.group.groups.length > 0;
    if (this.w.labelData.hasXaxisGroups) {
      this.w.labelData.groups = cnf.xaxis.group.groups;
    }
    ser.forEach((s2, i2) => {
      if (s2.name !== void 0) {
        this.w.seriesData.seriesNames.push(s2.name);
      } else {
        this.w.seriesData.seriesNames.push(
          "series-" + parseInt(String(i2 + 1), 10)
        );
      }
    });
    this.coreUtils.setSeriesYAxisMappings();
    const buckets = [];
    const groups = [
      ...new Set(cnf.series.map((s2) => s2.group))
    ];
    cnf.series.forEach((s2, i2) => {
      const index = groups.indexOf(s2.group);
      if (!buckets[index]) buckets[index] = [];
      buckets[index].push(this.w.seriesData.seriesNames[i2]);
    });
    this.w.labelData.seriesGroups = buckets;
    const handleDates = () => {
      for (let j = 0; j < xlabels.length; j++) {
        if (typeof xlabels[j] === "string") {
          const isDate = dt.isValidDate(xlabels[j]);
          if (isDate) {
            this.twoDSeriesX.push(dt.parseDate(xlabels[j]));
          } else {
            throw new Error(
              "You have provided invalid Date format. Please provide a valid JavaScript Date"
            );
          }
        } else {
          this.twoDSeriesX.push(xlabels[j]);
        }
      }
    };
    for (let i2 = 0; i2 < ser.length; i2++) {
      this.twoDSeries = [];
      this.twoDSeriesX = [];
      this.threeDSeries = [];
      if (typeof ser[i2].data === "undefined") {
        console.error(
          "It is a possibility that you may have not included 'data' property in series."
        );
        ser[i2] = __spreadProps(__spreadValues({}, ser[i2]), { data: [] });
      }
      const dr = cnf.chart.dataReducer;
      const rawStash = (_b = (_a = gl.dataReducerRawSeries) == null ? void 0 : _a[i2]) == null ? void 0 : _b.data;
      if ((dr == null ? void 0 : dr.enabled) && this.isMultiFormat() && Array.isArray(rawStash) && rawStash.length > ((_c = dr.threshold) != null ? _c : 500)) {
        const targetPoints = (_d = dr.targetPoints) != null ? _d : 250;
        const xmin = cnf.xaxis.min;
        const xmax = cnf.xaxis.max;
        const windowed = xmin == null && xmax == null ? rawStash : Data.sliceByXRange(rawStash, xmin, xmax);
        let reduced = windowed;
        if (windowed.length > targetPoints) {
          const sampleY = !Array.isArray(windowed[0]) ? (_e = windowed[0]) == null ? void 0 : _e.y : (_f = windowed[0]) == null ? void 0 : _f[1];
          if (Array.isArray(sampleY)) {
            if (sampleY.length === 4) {
              reduced = Data.ohlcAggregate(windowed, targetPoints);
            } else if (sampleY.length === 2) {
              reduced = Data.rangeAggregate(windowed, targetPoints);
            }
          } else {
            reduced = Data.lttbDownsample(windowed, targetPoints);
          }
        }
        ser[i2] = __spreadProps(__spreadValues({}, ser[i2]), { data: reduced });
      }
      if (cnf.chart.type === "rangeBar" || cnf.chart.type === "rangeArea" || ser[i2].type === "rangeBar" || ser[i2].type === "rangeArea") {
        this.w.axisFlags.isRangeData = true;
        this.handleRangeData(ser, i2);
      }
      const customType = ser[i2].type || cnf.chart.type;
      if (isCustom(customType)) {
        const cls = (
          /** @type {any} */
          getChartClass(customType)
        );
        const yExtent = cls && cls.yExtent;
        if (cls && cls.dataType === "rangeXY" || typeof yExtent === "function") {
          this.w.axisFlags.isRangeData = true;
          this.handleCustomRangeData(ser, i2, yExtent);
        }
      }
      if (this.isMultiFormat()) {
        if (this.isFormat2DArray()) {
          this.handleFormat2DArray(ser, i2);
        } else if (this.isFormatXY()) {
          this.handleFormatXY(ser, i2);
        }
        if (cnf.chart.type === "candlestick" || ser[i2].type === "candlestick" || cnf.chart.type === "boxPlot" || ser[i2].type === "boxPlot") {
          this.handleCandleStickBoxData(ser, i2);
        }
        if (cnf.chart.type === "violin" || ser[i2].type === "violin") {
          this.handleViolinData(ser, i2);
        }
        this.w.seriesData.series.push(this.twoDSeries);
        this.w.labelData.labels.push(this.twoDSeriesX);
        this.w.seriesData.seriesX.push(this.twoDSeriesX);
        this.w.seriesData.seriesGoals = this.seriesGoals;
        if (i2 === this.activeSeriesIndex && !this.fallbackToCategory) {
          this.w.axisFlags.isXNumeric = true;
        }
      } else {
        if (cnf.xaxis.type === "datetime") {
          this.w.axisFlags.isXNumeric = true;
          handleDates();
          this.w.seriesData.seriesX.push(this.twoDSeriesX);
        } else if (cnf.xaxis.type === "numeric") {
          this.w.axisFlags.isXNumeric = true;
          if (xlabels.length > 0) {
            this.twoDSeriesX = xlabels;
            this.w.seriesData.seriesX.push(this.twoDSeriesX);
          }
        }
        this.w.labelData.labels.push(this.twoDSeriesX);
        const singleArray = ser[i2].data.map(
          (d) => Utils$1.parseNumber(d)
        );
        this.w.seriesData.series.push(singleArray);
      }
      this.w.seriesData.seriesZ.push(this.threeDSeries);
      if (ser[i2].color !== void 0) {
        this.w.seriesData.seriesColors.push(ser[i2].color);
      } else {
        this.w.seriesData.seriesColors.push(
          /** @type {any} */
          void 0
        );
      }
    }
    return this.w;
  }
  /**
   * @param {any[]} ser
   */
  parseDataNonAxisCharts(ser) {
    const cnf = this.w.config;
    this.w.seriesData.unitData = [];
    const hasOldFormat = Array.isArray(ser) && ser.every((s2) => typeof s2 === "number") && cnf.labels.length > 0;
    const hasNewFormat = Array.isArray(ser) && ser.some(
      (s2) => s2 && typeof s2 === "object" && s2.data || s2 && typeof s2 === "object" && s2.parsing
    );
    if (cnf.chart.type === "unit" && hasNewFormat && !hasOldFormat) {
      return this.parseUnitSeries(ser);
    }
    if (hasOldFormat && hasNewFormat) {
      console.warn(
        "ApexCharts: Both old format (numeric series + labels) and new format (series objects with data/parsing) detected. Using old format for backward compatibility."
      );
    }
    if (hasOldFormat) {
      this.w.seriesData.series = /** @type {any} */
      ser.slice();
      this.w.seriesData.seriesNames = cnf.labels.slice();
      for (let i2 = 0; i2 < this.w.seriesData.series.length; i2++) {
        if (this.w.seriesData.seriesNames[i2] === void 0) {
          this.w.seriesData.seriesNames.push("series-" + (i2 + 1));
        }
      }
      return this.w;
    }
    if (Array.isArray(ser) && ser.every((s2) => typeof s2 === "number")) {
      this.w.seriesData.series = /** @type {any} */
      ser.slice();
      this.w.seriesData.seriesNames = [];
      for (let i2 = 0; i2 < this.w.seriesData.series.length; i2++) {
        this.w.seriesData.seriesNames.push(cnf.labels[i2] || `series-${i2 + 1}`);
      }
      return this.w;
    }
    const processedData = this.extractPieDataFromSeries(ser);
    this.w.seriesData.series = processedData.values;
    this.w.seriesData.seriesNames = processedData.labels;
    if (cnf.chart.type === "radialBar") {
      this.w.seriesData.series = this.w.seriesData.series.map((val) => {
        const numVal = Utils$1.parseNumber(val);
        if (numVal > 100) {
          console.warn(
            `ApexCharts: RadialBar value ${numVal} > 100, consider using percentage values (0-100)`
          );
        }
        return numVal;
      });
    }
    for (let i2 = 0; i2 < this.w.seriesData.series.length; i2++) {
      if (this.w.seriesData.seriesNames[i2] === void 0) {
        this.w.seriesData.seriesNames.push("series-" + (i2 + 1));
      }
    }
    return this.w;
  }
  /**
   * Parse the unit chart's per-unit object form:
   *   series: [{ name, data: [datum, datum, ...] }, ...]
   * Each category's dot count is `data.length` (one dot per datum), and the
   * per-unit data is kept on `w.seriesData.unitData` so the renderer can colour
   * dots individually and the tooltip can show each unit's own info.
   * @param {any[]} ser
   * @returns {any} w
   */
  parseUnitSeries(ser) {
    const cnf = this.w.config;
    const series = [];
    const seriesNames = [];
    const unitData = [];
    ser.forEach((s2, i2) => {
      var _a;
      const data = s2 && Array.isArray(s2.data) ? s2.data : [];
      series.push(data.length);
      const name2 = s2 && s2.name !== void 0 && s2.name !== null ? s2.name : void 0;
      seriesNames.push((_a = name2 != null ? name2 : cnf.labels[i2]) != null ? _a : `series-${i2 + 1}`);
      unitData.push(data.slice());
    });
    this.w.seriesData.series = /** @type {any} */
    series;
    this.w.seriesData.seriesNames = seriesNames;
    this.w.seriesData.unitData = unitData;
    return this.w;
  }
  /**
   * Reset parsing flags to allow re-parsing of data during updates
   */
  resetParsingFlags() {
    const w2 = this.w;
    w2.axisFlags.dataWasParsed = false;
    w2.globals.originalSeries = null;
    if (w2.config.series) {
      w2.config.series.forEach((serie) => {
        if (
          /** @type {any} */
          serie.__apexParsed
        ) {
          delete /** @type {any} */
          serie.__apexParsed;
        }
      });
    }
  }
  /**
   * @param {any[]} ser
   */
  extractPieDataFromSeries(ser) {
    const values = [];
    const labels = [];
    if (!Array.isArray(ser)) {
      console.warn("ApexCharts: Expected array for series data");
      return { values: [], labels: [] };
    }
    if (ser.length === 0) {
      console.warn("ApexCharts: Empty series array");
      return { values: [], labels: [] };
    }
    const firstItem = ser[0];
    if (typeof firstItem === "object" && firstItem !== null && firstItem.data) {
      this.extractPieDataFromSeriesObjects(ser, values, labels);
    } else {
      console.warn(
        "ApexCharts: Unsupported series format for pie/donut/radialBar. Expected series objects with data property."
      );
      return { values: [], labels: [] };
    }
    return { values, labels };
  }
  // Extract data from series objects: [{ data: [...], parsing: {...} }]
  /**
   * @param {any[]} seriesArray
   * @param {any[]} values
   * @param {any[]} labels
   */
  extractPieDataFromSeriesObjects(seriesArray, values, labels) {
    seriesArray.forEach((serie, serieIndex) => {
      if (!serie.data || !Array.isArray(serie.data)) {
        console.warn(`ApexCharts: Series ${serieIndex} has no valid data array`);
        return;
      }
      serie.data.forEach((dataPoint) => {
        if (typeof dataPoint === "object" && dataPoint !== null) {
          if (dataPoint.x !== void 0 && dataPoint.y !== void 0) {
            labels.push(String(dataPoint.x));
            values.push(Utils$1.parseNumber(dataPoint.y));
          } else {
            console.warn(
              "ApexCharts: Invalid data point format for pie chart. Expected {x, y} format:",
              dataPoint
            );
          }
        } else {
          console.warn(
            "ApexCharts: Expected object data point, got:",
            typeof dataPoint
          );
        }
      });
    });
  }
  /** User possibly set string categories in xaxis.categories or labels prop
   * Or didn't set xaxis labels at all - in which case we manually do it.
   * If user passed series data as [[3, 2], [4, 5]] or [{ x: 3, y: 55 }],
   * this shouldn't be called
   * @param {any[]} ser - the series which user passed to the config
   */
  handleExternalLabelsData(ser) {
    const cnf = this.w.config;
    if (cnf.xaxis.categories.length > 0) {
      this.w.labelData.labels = cnf.xaxis.categories;
    } else if (cnf.labels.length > 0) {
      this.w.labelData.labels = cnf.labels.slice();
    } else if (this.fallbackToCategory) {
      this.w.labelData.labels = /** @type {string[]} */
      /** @type {unknown} */
      this.w.labelData.labels[0];
      if (this.w.rangeData.seriesRange.length) {
        this.w.rangeData.seriesRange.map((srt) => {
          srt.forEach((sr) => {
            if (this.w.labelData.labels.indexOf(sr.x) < 0 && sr.x) {
              this.w.labelData.labels.push(sr.x);
            }
          });
        });
        const _labels = this.w.labelData.labels;
        if (_labels.length > 0 && (typeof _labels[0] === "number" || typeof _labels[0] === "string")) {
          this.w.labelData.labels = [...new Set(_labels)];
        } else {
          const _seen = /* @__PURE__ */ new Map();
          for (const _label of _labels) {
            const _key = JSON.stringify(_label);
            if (!_seen.has(_key)) _seen.set(_key, _label);
          }
          this.w.labelData.labels = Array.from(_seen.values());
        }
      }
      if (cnf.xaxis.convertedCatToNumeric) {
        const defaults = new Defaults(cnf);
        defaults.convertCatToNumericXaxis(cnf, this.w.seriesData.seriesX[0]);
        this._generateExternalLabels(ser);
      }
    } else {
      this._generateExternalLabels(ser);
    }
  }
  /**
   * @param {any[]} ser
   */
  _generateExternalLabels(ser) {
    const gl = this.w.globals;
    const cnf = this.w.config;
    let labelArr = [];
    if (gl.axisCharts) {
      if (this.w.seriesData.series.length > 0) {
        if (this.isFormatXY()) {
          const seriesDataFiltered = cnf.series.map(
            (serie) => {
              const seen = /* @__PURE__ */ new Map();
              for (const point of serie.data) {
                if (!seen.has(point.x)) seen.set(point.x, point);
              }
              return Array.from(seen.values());
            }
          );
          const len = seriesDataFiltered.reduce(
            (p2, c2, i2, a2) => a2[p2].length > c2.length ? p2 : i2,
            0
          );
          for (let i2 = 0; i2 < seriesDataFiltered[len].length; i2++) {
            labelArr.push(i2 + 1);
          }
        } else {
          for (let i2 = 0; i2 < this.w.seriesData.series[gl.maxValsInArrayIndex].length; i2++) {
            labelArr.push(i2 + 1);
          }
        }
      }
      this.w.seriesData.seriesX = [];
      for (let i2 = 0; i2 < ser.length; i2++) {
        this.w.seriesData.seriesX.push(labelArr);
      }
      if (!this.w.globals.isBarHorizontal) {
        this.w.axisFlags.isXNumeric = true;
      }
    }
    if (labelArr.length === 0) {
      labelArr = gl.axisCharts ? [] : (
        /**
         * @param {Record<string, any>} gls
         * @param {number} glsi
         */
        this.w.seriesData.series.map((gls, glsi) => {
          return glsi + 1;
        })
      );
      for (let i2 = 0; i2 < ser.length; i2++) {
        this.w.seriesData.seriesX.push(labelArr);
      }
    }
    this.w.labelData.labels = /** @type {string[]} */
    /** @type {unknown} */
    labelArr;
    if (cnf.xaxis.convertedCatToNumeric) {
      this.w.labelData.categoryLabels = labelArr.map((l2) => {
        return cnf.xaxis.labels.formatter(l2);
      });
    }
    this.w.axisFlags.noLabelsProvided = true;
  }
  /**
   * @param {any[]} series
   */
  parseRawDataIfNeeded(series) {
    const cnf = this.w.config;
    const gl = this.w.globals;
    const globalParsing = cnf.parsing;
    if (this.w.axisFlags.dataWasParsed) {
      return series;
    }
    const hasGlobalParsing = !!(globalParsing && (globalParsing.x || globalParsing.y || globalParsing.z));
    const hasSeriesParsing = series.some(
      (s2) => s2.parsing && (s2.parsing.x || s2.parsing.y || s2.parsing.z)
    );
    if (!hasGlobalParsing && !hasSeriesParsing) {
      return series;
    }
    const processedSeries = series.map((serie, index) => {
      var _a, _b, _c, _d, _e;
      if (!serie.data || !Array.isArray(serie.data) || serie.data.length === 0) {
        return serie;
      }
      const effectiveParsing = {
        x: ((_a = serie.parsing) == null ? void 0 : _a.x) || (globalParsing == null ? void 0 : globalParsing.x),
        y: ((_b = serie.parsing) == null ? void 0 : _b.y) || (globalParsing == null ? void 0 : globalParsing.y),
        z: ((_c = serie.parsing) == null ? void 0 : _c.z) || (globalParsing == null ? void 0 : globalParsing.z)
      };
      if (!effectiveParsing.x && !effectiveParsing.y) {
        return serie;
      }
      const firstDataPoint = serie.data[0];
      if (typeof firstDataPoint === "object" && firstDataPoint !== null && (Object.prototype.hasOwnProperty.call(firstDataPoint, "x") || Object.prototype.hasOwnProperty.call(firstDataPoint, "y")) || Array.isArray(firstDataPoint)) {
        return serie;
      }
      if (!effectiveParsing.x || !effectiveParsing.y || Array.isArray(effectiveParsing.y) && effectiveParsing.y.length === 0) {
        const missing = [];
        if (!effectiveParsing.x) missing.push("x");
        if (!effectiveParsing.y || Array.isArray(effectiveParsing.y) && effectiveParsing.y.length === 0) {
          missing.push("y");
        }
        const seriesName = (_d = serie.name) != null ? _d : `series[${index}]`;
        console.warn(
          `ApexCharts [${this.w.globals.chartID}]: "${seriesName}" has a parseData config but is missing the '${missing.join(
            "', '"
          )}' field specification.`,
          { parsing: (_e = serie.parsing) != null ? _e : globalParsing }
        );
        return serie;
      }
      const transformedData = serie.data.map(
        (item, itemIndex) => {
          if (typeof item !== "object" || item === null) {
            console.warn(
              `ApexCharts: Series ${index}, data point ${itemIndex} is not an object, skipping parsing`
            );
            return item;
          }
          const x = this.getNestedValue(item, effectiveParsing.x);
          let y;
          let z = void 0;
          if (Array.isArray(effectiveParsing.y)) {
            const yValues = effectiveParsing.y.map(
              (fieldName) => this.getNestedValue(item, fieldName)
            );
            if (this.w.config.chart.type === "bubble") {
              if (yValues.length < 2) {
                console.warn(
                  `ApexCharts: series[${index}] bubble chart requires parseData.y to have at least 2 fields (y and z). Got: ${JSON.stringify(effectiveParsing.y)}`
                );
              }
              y = yValues[0];
            } else {
              y = yValues;
            }
          } else {
            y = this.getNestedValue(item, effectiveParsing.y);
          }
          if (effectiveParsing.z) {
            z = this.getNestedValue(item, effectiveParsing.z);
          }
          if (x === void 0) {
            console.warn(
              `ApexCharts: Series ${index}, data point ${itemIndex} missing field '${effectiveParsing.x}'`
            );
          }
          if (y === void 0) {
            console.warn(
              `ApexCharts: Series ${index}, data point ${itemIndex} missing field '${effectiveParsing.y}'`
            );
          }
          const result = { x, y, z: void 0 };
          if (this.w.config.chart.type === "bubble" && Array.isArray(effectiveParsing.y) && effectiveParsing.y.length === 2) {
            const zValue = this.getNestedValue(item, effectiveParsing.y[1]);
            if (zValue !== void 0) {
              result.z = zValue;
            }
          }
          if (z !== void 0) {
            result.z = z;
          }
          return result;
        }
      );
      return __spreadProps(__spreadValues({}, serie), {
        data: transformedData,
        __apexParsed: true
      });
    });
    this.w.axisFlags.dataWasParsed = true;
    if (!gl.originalSeries) {
      gl.originalSeries = Utils$1.clone(series);
    }
    return processedSeries;
  }
  /**
   * Get nested object value using dot notation path
   * @param {Object} obj - The object to search in
   * @param {string} path - Dot notation path (e.g., 'user.profile.name')
   * @returns {*} The value at the path, or undefined if not found
   */
  getNestedValue(obj, path) {
    if (!obj || typeof obj !== "object" || !path) {
      return void 0;
    }
    if (path.indexOf(".") === -1) {
      return (
        /** @type {any} */
        obj[path]
      );
    }
    const keys = path.split(".");
    let current = obj;
    for (let i2 = 0; i2 < keys.length; i2++) {
      if (current === null || current === void 0 || typeof current !== "object") {
        return void 0;
      }
      current = /** @type {any} */
      current[keys[i2]];
    }
    return current;
  }
  /**
   * Optional pre-parse series transform. A chart type whose series carries RAW
   * observations rather than the values it draws (a histogram's sample, and in
   * time a boxPlot's or a violin's) registers a transform through
   * `apexcharts/features/stats`. Core keeps only this lookup, so a bundle that
   * never asks for a raw-sample type never pays for the statistics.
   *
   * @param {any[]} ser
   * @returns {any[]}
   */
  applySeriesTransform(ser) {
    const cnf = this.w.config;
    const name2 = cnf.chart.requestedType || cnf.chart.type;
    const transform = getSeriesTransform(name2);
    if (transform) return transform(ser, this.w);
    if (!Array.isArray(ser) || RAW_SAMPLE_TYPES.indexOf(name2) === -1) return ser;
    if (!this._warnedMissingTransform) {
      this._warnedMissingTransform = true;
      console.warn(
        `ApexCharts: chart.type '${name2}' needs the stats feature. Add \`import 'apexcharts/features/stats'\`, or import from 'apexcharts/${name2}'.`
      );
    }
    return ser.map((s2) => __spreadProps(__spreadValues({}, s2), { data: [] }));
  }
  /**
   * Nested treemap: resolve a `children` hierarchy into the tree the renderer
   * lays out, and return the leaves as a flat series.
   *
   * Everything downstream of here addresses a treemap by `(seriesIndex,
   * dataPointIndex)` into a flat matrix, so the leaves are flattened in
   * depth-first order and `dataPointIndex` keeps meaning "the nth leaf of this
   * series". The tree itself goes on globals for the renderer.
   *
   * `cnf.series` is replaced with the flattened leaves further down parseData,
   * which is the only copy that survives — so the nested input is stashed on
   * the first parse and every later parse resolves from the stash, never from
   * the already-flattened view. That is the same contract the histogram's raw
   * observations and the downsampler's raw series use, and `_updateSeries`
   * clears all three when the user pushes new data.
   *
   * @param {any[]} ser
   * @returns {any[]}
   */
  flattenTreemapHierarchy(ser) {
    const w2 = this.w;
    const gl = w2.globals;
    if (w2.config.chart.type !== "treemap" || !Array.isArray(ser)) return ser;
    if (!gl.treemapRawSeries) {
      if (!isNestedTreemap(w2, ser)) {
        gl.treemapRoots = null;
        return ser;
      }
      gl.treemapRawSeries = ser.map((s2) => __spreadProps(__spreadValues({}, s2), {
        data: Array.isArray(s2 == null ? void 0 : s2.data) ? s2.data.slice() : s2 == null ? void 0 : s2.data
      }));
    }
    const { roots, leafSeries, maxDepth } = resolveTreemapTree(
      w2,
      gl.treemapRawSeries
    );
    gl.treemapRoots = roots;
    gl.treemapMaxDepth = maxDepth;
    return leafSeries;
  }
  /**
   * Scatter strip-plot support. When `plotOptions.scatter.jitter.enabled` and a
   * series carries compact `{ x: 'Category', y: [v1, v2, ...] }` data, expand
   * each observation into its own `{ x: bandIndex, y }` point (so every dot is a
   * first-class, hoverable marker) and frame the x-axis as evenly-spaced,
   * labelled bands. The reference per-point form (numeric x + `xaxis.categories`)
   * is reframed too, without expansion. Returns `ser` unchanged for non-scatter
   * charts and for plain numeric/datetime data (continuous overplotting jitter
   * is offset at render time instead).
   *
   * @param {any[]} ser
   * @returns {any[]}
   */
  expandScatterJitterData(ser) {
    var _a, _b;
    const cnf = this.w.config;
    const isScatter = cnf.chart.type === "scatter" || cnf.chart.type === "bubble";
    const jt = (_b = (_a = cnf.plotOptions) == null ? void 0 : _a.scatter) == null ? void 0 : _b.jitter;
    if (!isScatter || !jt || !jt.enabled || !Array.isArray(ser)) return ser;
    const hasArrayY = ser.some(
      (s2) => Array.isArray(s2 == null ? void 0 : s2.data) && s2.data.some(
        (d) => d && !Array.isArray(d) && Array.isArray(d.y)
      )
    );
    if (!hasArrayY) {
      if (cnf.xaxis.type !== "datetime") {
        if (Array.isArray(cnf.xaxis.categories) && cnf.xaxis.categories.length) {
          this._applyBandAxis(cnf.xaxis.categories.slice());
        } else if (Array.isArray(cnf.xaxis._scatterBandLabels) && cnf.xaxis._scatterBandLabels.length) {
          this._applyBandAxis(cnf.xaxis._scatterBandLabels);
        }
      }
      return ser;
    }
    const bandLabels = [];
    const bandIndex = /* @__PURE__ */ new Map();
    ser.forEach((s2) => {
      if (!Array.isArray(s2 == null ? void 0 : s2.data)) return;
      s2.data.forEach((d) => {
        if (d && Array.isArray(d.y)) {
          const key = String(d.x);
          if (!bandIndex.has(key)) {
            bandIndex.set(key, bandLabels.length);
            bandLabels.push(d.x);
          }
        }
      });
    });
    const maxPoints = jt.maxPoints || 5e3;
    const expanded = ser.map((s2) => {
      if (!Array.isArray(s2 == null ? void 0 : s2.data)) return s2;
      const out = [];
      s2.data.forEach((d) => {
        if (d && Array.isArray(d.y)) {
          const bi = bandIndex.get(String(d.x));
          const ys = d.y;
          const stride = ys.length > maxPoints ? Math.ceil(ys.length / maxPoints) : 1;
          for (let k = 0; k < ys.length; k += stride) {
            const yv = Utils$1.parseNumber(ys[k]);
            if (yv === null) continue;
            out.push({ x: bi, y: yv });
          }
        } else if (d && typeof d === "object" && !Array.isArray(d)) {
          const key = String(d.x);
          out.push({ x: bandIndex.has(key) ? bandIndex.get(key) : d.x, y: d.y });
        } else {
          out.push(d);
        }
      });
      return __spreadProps(__spreadValues({}, s2), { data: out });
    });
    this._applyBandAxis(bandLabels);
    return expanded;
  }
  /**
   * Frame the x-axis as N evenly-spaced bands (one per category) on a numeric
   * scale. Bands sit at integer positions 0..N-1; the range is padded by a full
   * band on each side (min -1, max N) so jittered dots never clip. Crucially the
   * range bounds and tick count are integers, so the ticks land exactly on the
   * band centers regardless of how the numeric scale "nices" the step (e.g. the
   * small-range reduction in Scales._adjustTicksForSmallRange triggered by a
   * y-axis formatter). Only fills in options the user hasn't set, so explicit
   * min/max/tickAmount/formatter still win. The exception is an interactive
   * zoom/pan window (w.interact.zoomed): its fractional bounds are snapped to
   * whole bands so tick labels stay on band centers and edge bands are never
   * half-cropped.
   *
   * @param {any[]} bandLabels
   */
  _applyBandAxis(bandLabels) {
    var _a;
    const xa = this.w.config.xaxis;
    const n2 = bandLabels.length;
    if (!n2) return;
    const owned = (
      /** @type {Record<string, boolean>} */
      xa._scatterBand = xa._scatterBand || {}
    );
    xa._scatterBandLabels = bandLabels.slice();
    xa.type = "numeric";
    if (((_a = this.w.interact) == null ? void 0 : _a.zoomed) && typeof xa.min === "number" && typeof xa.max === "number" && isFinite(xa.min) && isFinite(xa.max)) {
      const clampBand = (b2) => Math.max(0, Math.min(n2 - 1, b2));
      let first = clampBand(Math.round(xa.min + 0.49));
      let last = clampBand(Math.round(xa.max - 0.49));
      if (last < first) {
        first = last = clampBand(Math.round((xa.min + xa.max) / 2));
      }
      xa.min = first - 1;
      xa.max = last + 1;
      xa.tickAmount = last - first + 2;
      owned.min = true;
      owned.max = true;
      owned.tick = true;
    } else {
      if (xa.min == null || owned.min) {
        xa.min = -1;
        owned.min = true;
      }
      if (xa.max == null || owned.max) {
        xa.max = n2;
        owned.max = true;
      }
      if (xa.tickAmount == null || xa.tickAmount === "dataPoints" || owned.tick) {
        xa.tickAmount = n2 + 1;
        owned.tick = true;
      }
    }
    xa.labels = xa.labels || {};
    const existing = (
      /** @type {any} */
      xa.labels.formatter
    );
    if (typeof existing !== "function" || existing._scatterBand) {
      const fmt = (
        /** @type {any} */
        ((val) => {
          const r2 = Math.round(val);
          return Math.abs(val - r2) < 1e-6 && bandLabels[r2] !== void 0 ? bandLabels[r2] : "";
        })
      );
      fmt._scatterBand = true;
      xa.labels.formatter = fmt;
    }
  }
  // Segregate user provided data into appropriate vars
  /**
   * @param {any[]} ser
   */
  parseData(ser) {
    var _a, _b, _c, _d, _e, _f, _g;
    const w2 = this.w;
    const cnf = w2.config;
    const gl = w2.globals;
    ser = this.parseRawDataIfNeeded(ser);
    ser = this.applySeriesTransform(ser);
    ser = this.expandScatterJitterData(ser);
    ser = this.flattenTreemapHierarchy(ser);
    if (((_a = cnf.chart.dataReducer) == null ? void 0 : _a.enabled) && gl.axisCharts && !gl.dataReducerRawSeries) {
      gl.dataReducerRawSeries = ser.map((s2) => ({
        data: Array.isArray(s2 == null ? void 0 : s2.data) ? s2.data.slice() : s2 == null ? void 0 : s2.data
      }));
      let rawMinX = Infinity;
      let rawMaxX = -Infinity;
      for (const s2 of ser) {
        const d = s2 == null ? void 0 : s2.data;
        if (!Array.isArray(d) || d.length === 0) continue;
        const isXY = !Array.isArray(d[0]);
        const firstX = isXY ? (_b = d[0]) == null ? void 0 : _b.x : (_c = d[0]) == null ? void 0 : _c[0];
        const lastX = isXY ? (_d = d[d.length - 1]) == null ? void 0 : _d.x : (_e = d[d.length - 1]) == null ? void 0 : _e[0];
        if (typeof firstX === "number") rawMinX = Math.min(rawMinX, firstX);
        if (typeof lastX === "number") rawMaxX = Math.max(rawMaxX, lastX);
      }
      if (rawMinX !== Infinity) {
        gl.dataReducerRawMinX = rawMinX;
        gl.dataReducerRawMaxX = rawMaxX;
      }
    }
    if (gl.dataReducerRawSeries && ((_f = cnf.chart.dataReducer) == null ? void 0 : _f.enabled)) {
      ser = ser.map((s2) => __spreadValues({}, s2));
    }
    cnf.series = ser;
    if (gl.dataReducerRawSeries && ((_g = cnf.chart.dataReducer) == null ? void 0 : _g.enabled)) {
      const stash = gl.dataReducerRawSeries;
      gl.initialSeries = ser.map((s2, i2) => {
        var _a2, _b2, _c2;
        return __spreadProps(__spreadValues({}, s2), {
          data: (_c2 = (_b2 = (_a2 = stash[i2]) == null ? void 0 : _a2.data) == null ? void 0 : _b2.slice()) != null ? _c2 : s2.data
        });
      });
    } else if (gl.histogramRawSeries) {
      gl.initialSeries = gl.histogramRawSeries;
    } else if (gl.treemapRawSeries) {
      gl.initialSeries = gl.treemapRawSeries;
    } else {
      gl.initialSeries = ser;
    }
    this.excludeCollapsedSeriesInYAxis();
    this.fallbackToCategory = false;
    this.resetGlobals();
    this.isMultipleY();
    if (gl.axisCharts) {
      this.parseDataAxisCharts(ser);
      this.coreUtils.getLargestSeries();
    } else {
      this.parseDataNonAxisCharts(ser);
    }
    if (cnf.chart.stacked) {
      const series = new Series(this.w);
      this.w.seriesData.series = series.setNullSeriesToZeroValues(
        this.w.seriesData.series
      );
    }
    this.coreUtils.getSeriesTotals();
    if (gl.axisCharts) {
      Data._defineLazyResult(
        this.w.seriesData,
        "stackedSeriesTotals",
        () => this.coreUtils.getStackedSeriesTotals()
      );
      Data._defineLazyResult(
        this.w.seriesData,
        "stackedSeriesTotalsByGroups",
        () => this.coreUtils.getStackedSeriesTotalsByGroups()
      );
      Data._defineLazyResult(gl, "seriesPercent", () => {
        this.coreUtils.getPercentSeries();
        return gl.seriesPercent;
      });
    } else {
      this.coreUtils.getPercentSeries();
    }
    if (!this.w.axisFlags.dataFormatXNumeric && (!this.w.axisFlags.isXNumeric || cnf.xaxis.type === "numeric" && cnf.labels.length === 0 && cnf.xaxis.categories.length === 0)) {
      this.handleExternalLabelsData(ser);
    }
    const catLabels = this.coreUtils.getCategoryLabels(this.w.labelData.labels);
    for (let l2 = 0; l2 < catLabels.length; l2++) {
      if (Array.isArray(catLabels[l2])) {
        this.w.axisFlags.isMultiLineX = true;
        break;
      }
    }
    return {
      // w.seriesData (future slice)
      // initialSeries/originalSeries and the stacked totals are deliberately
      // ABSENT: they already live as lazy accessors on gl / w.seriesData, so
      // a snapshot field would either force their materialization (a deep
      // clone plus three O(n) passes per parse that most charts never need)
      // or, as a delegating getter, recurse into itself when a writer copies
      // it back onto the object it delegates to.
      seriesData: {
        series: this.w.seriesData.series,
        seriesNames: this.w.seriesData.seriesNames,
        seriesX: this.w.seriesData.seriesX,
        seriesZ: this.w.seriesData.seriesZ,
        seriesColors: this.w.seriesData.seriesColors,
        seriesGoals: this.w.seriesData.seriesGoals,
        unitData: this.w.seriesData.unitData,
        noLabelsProvided: this.w.axisFlags.noLabelsProvided
      },
      // w.rangeData (future slice)
      rangeData: {
        seriesRangeStart: this.w.rangeData.seriesRangeStart,
        seriesRangeEnd: this.w.rangeData.seriesRangeEnd,
        seriesRange: this.w.rangeData.seriesRange
      },
      // w.candleData (future slice)
      candleData: {
        seriesCandleO: this.w.candleData.seriesCandleO,
        seriesCandleH: this.w.candleData.seriesCandleH,
        seriesCandleM: this.w.candleData.seriesCandleM,
        seriesCandleL: this.w.candleData.seriesCandleL,
        seriesCandleC: this.w.candleData.seriesCandleC,
        seriesBoxPoints: this.w.candleData.seriesBoxPoints
      },
      // w.labelData (future slice)
      labelData: {
        labels: this.w.labelData.labels,
        categoryLabels: this.w.labelData.categoryLabels
      },
      // w.axisFlags (future slice)
      axisFlags: {
        isXNumeric: this.w.axisFlags.isXNumeric,
        dataFormatXNumeric: this.w.axisFlags.dataFormatXNumeric,
        isDataXYZ: this.w.axisFlags.isDataXYZ,
        isRangeData: this.w.axisFlags.isRangeData,
        isRangeBar: this.w.axisFlags.isRangeBar,
        isMultiLineX: this.w.axisFlags.isMultiLineX,
        dataWasParsed: this.w.axisFlags.dataWasParsed,
        hasXaxisGroups: this.w.labelData.hasXaxisGroups,
        groups: this.w.labelData.groups,
        seriesGroups: this.w.labelData.seriesGroups
      }
    };
  }
  /**
   * Slice a sorted-by-x series to a [xmin, xmax] window using binary search.
   *
   * Pads with one extra point on each side so lines extend cleanly to the
   * chart edges. Either bound may be null/undefined to disable that side.
   *
   * @param {any[]} data - Series data in [{x,y}] or [[x,y]] format, sorted by x.
   * @param {number|null|undefined} xmin
   * @param {number|null|undefined} xmax
   * @returns {any[]} Sliced array (new array, never the input reference).
   */
  /**
   * Define `key` on `obj` as a lazily computed property: `compute` runs on
   * first read after this call and its result is cached; assigning to the
   * property stores the assigned value directly (so code that writes the
   * field, like getPercentSeries, keeps working). Re-calling resets the cache
   * (used once per parse).
   * @param {any} obj
   * @param {string} key
   * @param {() => any} compute
   */
  static _defineLazyResult(obj, key, compute) {
    let has = false;
    let value;
    Object.defineProperty(obj, key, {
      configurable: true,
      enumerable: true,
      get() {
        if (!has) {
          has = true;
          value = compute();
        }
        return value;
      },
      set(v2) {
        has = true;
        value = v2;
      }
    });
  }
  /**
   * @param {any[]} data
   * @param {any} xmin
   * @param {any} xmax
   */
  static sliceByXRange(data, xmin, xmax) {
    const len = data.length;
    if (len === 0) return data;
    const isXY = !Array.isArray(data[0]);
    const getX = isXY ? (p2) => p2.x : (p2) => p2[0];
    let lo = 0;
    if (xmin != null) {
      let l2 = 0;
      let r2 = len - 1;
      while (l2 <= r2) {
        const m2 = l2 + r2 >> 1;
        if (getX(data[m2]) < xmin) l2 = m2 + 1;
        else r2 = m2 - 1;
      }
      lo = Math.max(0, l2 - 1);
    }
    let hi = len;
    if (xmax != null) {
      let l2 = 0;
      let r2 = len - 1;
      while (l2 <= r2) {
        const m2 = l2 + r2 >> 1;
        if (getX(data[m2]) > xmax) r2 = m2 - 1;
        else l2 = m2 + 1;
      }
      hi = Math.min(len, l2 + 1);
    }
    return lo === 0 && hi === len ? data.slice() : data.slice(lo, hi);
  }
  /**
   * Largest-Triangle-Three-Bucket (LTTB) downsampling.
   *
   * Reduces `data` to `targetPoints` points while preserving the visual shape
   * of the series as perceived by the human eye.
   *
   * @param {any[]} data   - Raw series data in [{x,y}] or [[x,y]] format.
   * @param {number} targetPoints - Desired output length (>= 3).
   * @returns {any[]} Downsampled array in the same format as the input.
   */
  static lttbDownsample(data, targetPoints) {
    const len = data.length;
    if (targetPoints >= len || targetPoints < 3) return data;
    const isXY = !Array.isArray(data[0]);
    const getX = isXY ? (p2) => p2.x : (p2) => p2[0];
    const getY = isXY ? (p2) => p2.y : (p2) => p2[1];
    const sampled = [];
    sampled.push(data[0]);
    const bucketSize = (len - 2) / (targetPoints - 2);
    let a2 = 0;
    for (let i2 = 0; i2 < targetPoints - 2; i2++) {
      const avgRangeStart = Math.floor((i2 + 1) * bucketSize) + 1;
      const avgRangeEnd = Math.min(Math.floor((i2 + 2) * bucketSize) + 1, len);
      let avgX = 0;
      let avgY = 0;
      const avgRangeLen = avgRangeEnd - avgRangeStart;
      for (let j = avgRangeStart; j < avgRangeEnd; j++) {
        avgX += getX(data[j]);
        avgY += getY(data[j]);
      }
      avgX /= avgRangeLen;
      avgY /= avgRangeLen;
      const rangeStart = Math.floor(i2 * bucketSize) + 1;
      const rangeEnd = Math.min(Math.floor((i2 + 1) * bucketSize) + 1, len);
      const pointAX = getX(data[a2]);
      const pointAY = getY(data[a2]);
      let maxArea = -1;
      let maxAreaIdx = rangeStart;
      for (let j = rangeStart; j < rangeEnd; j++) {
        const area = Math.abs(
          (pointAX - avgX) * (getY(data[j]) - pointAY) - (pointAX - getX(data[j])) * (avgY - pointAY)
        ) * 0.5;
        if (area > maxArea) {
          maxArea = area;
          maxAreaIdx = j;
        }
      }
      sampled.push(data[maxAreaIdx]);
      a2 = maxAreaIdx;
    }
    sampled.push(data[len - 1]);
    return sampled;
  }
  /**
   * OHLC-aware bucket aggregation for candlestick / OHLC series.
   *
   * Each point's y is a 4-tuple `[open, high, low, close]`. LTTB is unusable
   * here — it treats y as a scalar, so the triangle-area math degenerates and
   * silently discards the high/low extremes that *define* a candle. Instead we
   * split the series into `targetPoints` contiguous buckets and roll each up
   * into a single candle: open = first bucket open, close = last bucket close,
   * high = max of highs, low = min of lows. The x is the first point's x in the
   * bucket. Output keeps the input's format ([{x,y}] or [[x,y]]).
   *
   * @param {any[]} data         - Raw OHLC series in [{x,y:[o,h,l,c]}] or [[x,[o,h,l,c]]] format.
   * @param {number} targetPoints - Desired output length (>= 1).
   * @returns {any[]} Aggregated array in the same format as the input.
   */
  static ohlcAggregate(data, targetPoints) {
    const len = data.length;
    if (targetPoints >= len || targetPoints < 1) return data;
    const isXY = !Array.isArray(data[0]);
    const getX = isXY ? (p2) => p2.x : (p2) => p2[0];
    const getY = isXY ? (p2) => p2.y : (p2) => p2[1];
    const make = isXY ? (x, y) => ({ x, y }) : (x, y) => [x, y];
    const out = [];
    const bucketSize = len / targetPoints;
    for (let i2 = 0; i2 < targetPoints; i2++) {
      const start = Math.floor(i2 * bucketSize);
      const end = i2 === targetPoints - 1 ? len : Math.floor((i2 + 1) * bucketSize);
      if (end <= start) continue;
      const firstY = getY(data[start]);
      const open = firstY[0];
      let high = firstY[1];
      let low = firstY[2];
      let close = firstY[3];
      for (let j = start + 1; j < end; j++) {
        const y = getY(data[j]);
        if (y[1] > high) high = y[1];
        if (y[2] < low) low = y[2];
        close = y[3];
      }
      out.push(make(getX(data[start]), [open, high, low, close]));
    }
    return out;
  }
  /**
   * Bucket-aggregate 2-tuple range data (`y: [low, high]`, rangeArea/rangeBar)
   * into `targetPoints` points, the range analog of {@link ohlcAggregate}. Each
   * bucket emits `[min low, max high]` so the band's vertical extent is never
   * understated by downsampling (LTTB, built for scalar y, would drop these
   * extremes). Order-agnostic: the min/max scan both tuple slots, so it is
   * correct whether a point is stored `[low, high]` or `[high, low]`.
   *
   * Null bounds (e.g. an indicator's warm-up period) are ignored, not treated
   * as 0 — `Math.min(null, x)` would coerce to 0 and pin the band to the
   * baseline. A bucket with no finite bounds emits `[null, null]` so it renders
   * as a gap, matching the un-downsampled series.
   * @param {any[]} data
   * @param {number} targetPoints
   * @returns {any[]}
   */
  static rangeAggregate(data, targetPoints) {
    const len = data.length;
    if (targetPoints >= len || targetPoints < 1) return data;
    const isXY = !Array.isArray(data[0]);
    const getX = isXY ? (p2) => p2.x : (p2) => p2[0];
    const getY = isXY ? (p2) => p2.y : (p2) => p2[1];
    const make = isXY ? (x, y) => ({ x, y }) : (x, y) => [x, y];
    const out = [];
    const bucketSize = len / targetPoints;
    for (let i2 = 0; i2 < targetPoints; i2++) {
      const start = Math.floor(i2 * bucketSize);
      const end = i2 === targetPoints - 1 ? len : Math.floor((i2 + 1) * bucketSize);
      if (end <= start) continue;
      let low = Infinity;
      let high = -Infinity;
      for (let j = start; j < end; j++) {
        const y = getY(data[j]);
        if (y == null) continue;
        for (let k = 0; k < 2; k++) {
          const v2 = y[k];
          if (v2 == null || !isFinite(v2)) continue;
          if (v2 < low) low = v2;
          if (v2 > high) high = v2;
        }
      }
      out.push(
        make(getX(data[start]), low === Infinity ? [null, null] : [low, high])
      );
    }
    return out;
  }
  excludeCollapsedSeriesInYAxis() {
    const w2 = this.w;
    const yAxisIndexes = [];
    w2.globals.seriesYAxisMap.forEach((yAxisArr, yi) => {
      let collapsedCount = 0;
      yAxisArr.forEach((seriesIndex) => {
        if (w2.globals.collapsedSeriesIndices.indexOf(seriesIndex) !== -1) {
          collapsedCount++;
        }
      });
      if (collapsedCount > 0 && collapsedCount == yAxisArr.length) {
        yAxisIndexes.push(yi);
      }
    });
    w2.globals.ignoreYAxisIndexes = yAxisIndexes.map((x) => x);
  }
}
class UpdateHelpers {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
  }
  /**
   * private method to update Options.
   *
   * @param {Record<string, any>} options - A new config object can be passed which will be merged with the existing config object
   * @param {boolean} redraw - should redraw from beginning or should use existing paths and redraw from there
   * @param {boolean} animate - should animate or not on updating Options
   * @param {boolean} overwriteInitialConfig - should update the initial config or not
   */
  _updateOptions(options2, redraw = false, animate = true, updateSyncedCharts = true, overwriteInitialConfig = false) {
    return new Promise((resolve, reject) => {
      let charts = [this.ctx];
      if (updateSyncedCharts) {
        charts = this.ctx.getSyncedCharts();
      }
      if (this.w.globals.isExecCalled) {
        charts = [this.ctx];
        this.w.globals.isExecCalled = false;
      }
      charts.forEach((ch, chartIndex) => {
        var _a, _b;
        const w2 = ch.w;
        w2.globals.shouldAnimate = animate;
        if (!redraw) {
          w2.globals.resized = true;
          w2.globals.dataChanged = true;
          if (animate && w2.config.chart.animations.enabled) {
            ch.series.getPreviousPaths();
          }
        }
        const fromType = w2.config.chart.requestedType || w2.config.chart.type;
        if (animate && options2 && typeof options2 === "object") {
          const newType = (_a = options2 == null ? void 0 : options2.chart) == null ? void 0 : _a.type;
          if (newType && newType !== fromType) {
            (_b = ch.morphTypeChange) == null ? void 0 : _b.captureBeforeDestroy({
              fromType,
              toType: newType,
              newSeries: options2.series || w2.config.series
            });
          }
        }
        if (options2 && typeof options2 === "object") {
          ch.config = new Config(options2);
          const incomingType = options2.chart && options2.chart.type;
          const isAliasRequest = incomingType === "funnel" || incomingType === "pyramid" || incomingType === "gauge";
          const wasAlias = !!w2.config.chart.requestedType;
          if (incomingType && !isAliasRequest && wasAlias) {
            options2.chart = options2.chart || {};
            options2.chart.requestedType = incomingType;
            const prev = w2.config.chart.requestedType;
            if (prev === "funnel" || prev === "pyramid") {
              options2.plotOptions = options2.plotOptions || {};
              options2.plotOptions.bar = options2.plotOptions.bar || {};
              if (options2.plotOptions.bar.isFunnel === void 0) {
                options2.plotOptions.bar.isFunnel = false;
              }
              if (options2.plotOptions.bar.isPyramid === void 0) {
                options2.plotOptions.bar.isPyramid = false;
              }
            }
          }
          ch.config.normalizeAliasedChartType(options2);
          options2 = CoreUtils.extendArrayProps(ch.config, options2, w2);
          if (ch.w.globals.chartID !== this.w.globals.chartID) {
            delete options2.series;
            delete options2.yaxis;
          }
          w2.config = Utils$1.extend(w2.config, options2);
          Defaults.handOverTypeDefaults(w2.config, fromType, options2);
          if (overwriteInitialConfig) {
            w2.globals.lastXAxis = options2.xaxis ? Utils$1.clone(options2.xaxis) : [];
            w2.globals.lastYAxis = options2.yaxis ? Utils$1.clone(options2.yaxis) : [];
            w2.globals.initialConfig = Utils$1.extend({}, w2.config);
            w2.globals.initialSeries = w2.config.series;
          }
          if (options2.series && (w2.globals.collapsedSeriesIndices.length > 0 || w2.globals.ancillaryCollapsedSeriesIndices.length > 0)) {
            ch.series.reconcileCollapsedByName();
          }
        }
        return ch.update(options2).then(() => {
          if (chartIndex === charts.length - 1) {
            resolve(ch);
          }
        }).catch(reject);
      });
    });
  }
  /**
   * Private method to update Series.
   *
   * @param {any[]} newSeries - New series which will override the existing
   * @param {boolean} animate
   */
  _updateSeries(newSeries, animate, overwriteInitialSeries = false) {
    return new Promise((resolve, reject) => {
      const w2 = this.w;
      w2.globals.shouldAnimate = animate;
      w2.globals.dataChanged = true;
      const prevAxisScaleSig = JSON.stringify({
        y: (w2.globals.yAxisScale || []).map((s2) => s2 ? s2.result : null),
        xMin: w2.globals.minX,
        xMax: w2.globals.maxX
      });
      PerformanceCache.invalidateSelectors(w2);
      if (animate && w2.config.chart.animations.enabled) {
        this.ctx.series.getPreviousPaths();
      }
      const prevSeriesCount = w2.config.series.length;
      const prevDataLengths = w2.config.series.map(
        (s2) => {
          var _a, _b;
          return (_b = (_a = s2 == null ? void 0 : s2.data) == null ? void 0 : _a.length) != null ? _b : 0;
        }
      );
      if (overwriteInitialSeries) {
        w2.globals.dataReducerRawSeries = null;
        w2.globals.histogramRawSeries = null;
        w2.globals.treemapRawSeries = null;
      }
      this.ctx.data.resetParsingFlags();
      const parsedState = this.ctx.data.parseData(newSeries);
      this.ctx._writeParsedSeriesData(parsedState.seriesData);
      this.ctx._writeParsedRangeData(parsedState.rangeData);
      this.ctx._writeParsedCandleData(parsedState.candleData);
      this.ctx._writeParsedLabelData(parsedState.labelData);
      this.ctx._writeParsedAxisFlags(parsedState.axisFlags);
      if (overwriteInitialSeries) {
        if (w2.globals.initialConfig) {
          w2.globals.initialConfig.series = w2.config.series;
        }
        w2.globals.initialSeries = w2.config.series;
      }
      if (this._canUseFastPath(newSeries, prevSeriesCount, prevDataLengths, w2)) {
        return this.ctx.fastUpdate(animate, prevAxisScaleSig).then(() => {
          resolve(this.ctx);
        }).catch(reject);
      }
      if (this.ctx._updateStats) this.ctx._updateStats.full++;
      return this.ctx.update().then(() => {
        resolve(this.ctx);
      }).catch(reject);
    });
  }
  /**
   * Returns true if the data-only fast path can be used for this update.
   * Fast path skips rebuilding grid, axes, legend, annotations, and tooltip DOM.
   *
   * Requirements:
   * - Chart has been fully rendered (DOM exists)
   * - Axis chart (non-axis charts like pie always need full rebuild due to radial layout)
   * - Series count unchanged (grid column/row counts depend on it)
   * - Per-series data lengths unchanged (the fast path preserves the axis DOM,
   *   and a changed point count re-slots categories/ticks: with explicit
   *   categories the axis-scale signature can still match, leaving a stale
   *   ruler under the re-slotted marks; length changes also want the full
   *   render so enter/exit and axis transitions run)
   * - No series currently collapsing (collapsed series changes visible data range)
   * - Not a combo chart (combo charts mix types and need coordinated axis recalc)
   * - Not currently zoomed (zoomed charts have altered x-labels that need recalculation)
   * @param {any[]} newSeries
   * @param {number} prevSeriesCount
   * @param {number[]} prevDataLengths
   * @param {import('../../types/internal').ChartStateW} w
   */
  _canUseFastPath(newSeries, prevSeriesCount, prevDataLengths, w2) {
    if (!w2.dom.elGraphical) return false;
    if (!w2.globals.axisCharts) return false;
    if (newSeries.length !== prevSeriesCount) return false;
    if (newSeries.some(
      (s2, i2) => {
        var _a, _b;
        return ((_b = (_a = s2 == null ? void 0 : s2.data) == null ? void 0 : _a.length) != null ? _b : 0) !== prevDataLengths[i2];
      }
    )) {
      return false;
    }
    if (w2.globals.collapsedSeries.length > 0) return false;
    if (w2.globals.ancillaryCollapsedSeries.length > 0) return false;
    if (w2.globals.risingSeries.length > 0) return false;
    if (w2.globals.comboCharts) return false;
    if (w2.interact.zoomed) return false;
    return true;
  }
  /**
   * @param {any} s
   * @param {number} i
   */
  _extendSeries(s2, i2) {
    const w2 = this.w;
    const ser = w2.config.series[i2];
    return __spreadProps(__spreadValues(
      {},
      /** @type {Record<string,any>} */
      w2.config.series[i2]
    ), {
      name: s2.name ? s2.name : (
        /** @type {any} */
        ser == null ? void 0 : ser.name
      ),
      color: s2.color ? s2.color : (
        /** @type {any} */
        ser == null ? void 0 : ser.color
      ),
      type: s2.type ? s2.type : (
        /** @type {any} */
        ser == null ? void 0 : ser.type
      ),
      group: s2.group ? s2.group : (
        /** @type {any} */
        ser == null ? void 0 : ser.group
      ),
      hidden: typeof s2.hidden !== "undefined" ? s2.hidden : (
        /** @type {any} */
        ser == null ? void 0 : ser.hidden
      ),
      data: s2.data ? s2.data : (
        /** @type {any} */
        ser == null ? void 0 : ser.data
      ),
      zIndex: typeof s2.zIndex !== "undefined" ? s2.zIndex : i2
    });
  }
  /**
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   */
  toggleDataPointSelection(seriesIndex, dataPointIndex) {
    const w2 = this.w;
    let elPath = null;
    const parent = `.apexcharts-series[data\\:realIndex='${seriesIndex}']`;
    if (w2.globals.axisCharts) {
      elPath = w2.dom.Paper.findOne(
        `${parent} path[j='${dataPointIndex}'], ${parent} circle[j='${dataPointIndex}'], ${parent} rect[j='${dataPointIndex}']`
      );
    } else {
      if (typeof dataPointIndex === "undefined") {
        elPath = w2.dom.Paper.findOne(`${parent} path[j='${seriesIndex}']`);
        if (w2.config.chart.type === "pie" || w2.config.chart.type === "polarArea" || w2.config.chart.type === "donut") {
          this.ctx.pie.pieClicked(seriesIndex);
        }
      }
    }
    if (elPath) {
      const graphics = new Graphics(this.w);
      graphics.pathMouseDown(
        elPath,
        /** @type {any} */
        null
      );
    } else {
      console.warn("toggleDataPointSelection: Element not found");
      return null;
    }
    return elPath.node ? elPath.node : null;
  }
  /**
   * @param {Record<string, any>} options
   */
  forceXAxisUpdate(options2) {
    const w2 = this.w;
    const minmax = ["min", "max"];
    minmax.forEach((a2) => {
      if (typeof options2.xaxis[a2] !== "undefined") {
        w2.config.xaxis[a2] = options2.xaxis[a2];
        w2.globals.lastXAxis[a2] = options2.xaxis[a2];
      }
    });
    if (options2.xaxis.categories && options2.xaxis.categories.length) {
      w2.config.xaxis.categories = options2.xaxis.categories;
    }
    if (w2.config.xaxis.convertedCatToNumeric) {
      const defaults = new Defaults(options2);
      options2 = defaults.convertCatToNumericXaxis(options2, this.ctx);
    }
    return options2;
  }
  /**
   * @param {Record<string, any>} options
   */
  forceYAxisUpdate(options2) {
    if (options2.chart && options2.chart.stacked && options2.chart.stackType === "100%") {
      if (Array.isArray(options2.yaxis)) {
        options2.yaxis.forEach(
          (yaxe, index) => {
            options2.yaxis[index].min = 0;
            options2.yaxis[index].max = 100;
          }
        );
      } else {
        options2.yaxis.min = 0;
        options2.yaxis.max = 100;
      }
    }
    return options2;
  }
  /**
   * This function reverts the yaxis and xaxis min/max values to what it was when the chart was defined.
   * This function fixes an important bug where a user might load a new series after zooming in/out of previous series which resulted in wrong min/max
   * Also, this should never be called internally on zoom/pan - the reset should only happen when user calls the updateSeries() function externally
   * The function also accepts an object {xaxis, yaxis} which when present is set as the new xaxis/yaxis
   * @param {Record<string, any>} opts
   */
  revertDefaultAxisMinMax(opts) {
    const w2 = this.w;
    let xaxis = w2.globals.lastXAxis;
    let yaxis = w2.globals.lastYAxis;
    if (opts && opts.xaxis) {
      xaxis = opts.xaxis;
    }
    if (opts && opts.yaxis) {
      yaxis = opts.yaxis;
    }
    const _xaxis = (
      /** @type {any} */
      xaxis
    );
    w2.config.xaxis.min = _xaxis.min;
    w2.config.xaxis.max = _xaxis.max;
    const getLastYAxis = (index) => {
      if (typeof yaxis[index] !== "undefined") {
        const _y = (
          /** @type {any} */
          yaxis[index]
        );
        w2.config.yaxis[index].min = _y.min;
        w2.config.yaxis[index].max = _y.max;
      }
    };
    w2.config.yaxis.map((yaxe, index) => {
      if (w2.interact.zoomed) {
        getLastYAxis(index);
      } else {
        if (typeof yaxis[index] !== "undefined") {
          getLastYAxis(index);
        } else {
          if (typeof this.ctx.opts.yaxis[index] !== "undefined") {
            yaxe.min = this.ctx.opts.yaxis[index].min;
            yaxe.max = this.ctx.opts.yaxis[index].max;
          }
        }
      }
    });
  }
}
class AxisMapping {
  /**
   * Pixels per data-unit on the x-axis. Derived from `minX..maxX` so it is the
   * exact inverse used by both {@link dataXToPx} and {@link pxToDataX}.
   * @param {import('../types/internal').ChartStateW} w
   * @returns {number}
   */
  static xRatio(w2) {
    const gw = w2.layout.gridWidth || 1;
    return (w2.globals.maxX - w2.globals.minX) / gw;
  }
  /**
   * Data-x -> pixels from the plot origin (usable as an SVG `x` attribute).
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} dataX
   * @returns {number}
   */
  static dataXToPx(w2, dataX) {
    return (dataX - w2.globals.minX) / AxisMapping.xRatio(w2);
  }
  /**
   * Pixels from the plot origin -> data-x. Feed it `screenX - svgLeft - translateX`.
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} px
   * @returns {number}
   */
  static pxToDataX(w2, px) {
    return w2.globals.minX + px * AxisMapping.xRatio(w2);
  }
  /**
   * Client (screen) x -> pixels from the plot origin. The origin is the svg
   * element's left edge plus `translateX`, never the `.apexcharts-grid` box
   * (fact 2 above), so the result does not depend on what the grid happens to
   * render. `svgWidth` is the unscaled width the svg was drawn at, so the ratio
   * against the measured one is the CSS zoom of any container the chart sits in.
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} screenX
   * @returns {number}
   */
  static screenXToPlotPx(w2, screenX) {
    const baseEl = w2.dom.baseEl;
    const svg = baseEl && baseEl.querySelector(".apexcharts-svg");
    if (!svg) return screenX - w2.layout.translateX;
    const svgRect = svg.getBoundingClientRect();
    const zoom = w2.globals.svgWidth ? svgRect.width / w2.globals.svgWidth : 1;
    return (screenX - svgRect.left) / (zoom || 1) - w2.layout.translateX;
  }
}
class Utils2 {
  /**
   * @param {import('./Tooltip').default} tooltipContext
   */
  constructor(tooltipContext) {
    this.w = tooltipContext.w;
    this.ttCtx = tooltipContext;
  }
  /**
   ** When hovering over series, you need to capture which series is being hovered on.
   ** This function will return both capturedseries index as well as inner index of that series
   * @memberof Utils
   * @param {{ hoverArea: any, elGrid: any, clientX: any, clientY: any, context?: any }} opts
   */
  getNearestValues({ hoverArea, elGrid, clientX, clientY }) {
    var _a, _b;
    const w2 = this.w;
    const seriesBound = elGrid.getBoundingClientRect();
    const hoverWidth = w2.layout.gridWidth;
    const hoverHeight = seriesBound.height;
    let xDivisor = hoverWidth / (w2.globals.dataPoints - 1);
    const yDivisor = hoverHeight / w2.globals.dataPoints;
    const hasBars = this.hasBars();
    if ((w2.globals.comboCharts || hasBars) && !w2.config.xaxis.convertedCatToNumeric) {
      xDivisor = hoverWidth / w2.globals.dataPoints;
    }
    const hoverX = AxisMapping.screenXToPlotPx(w2, clientX);
    const hoverY = clientY - seriesBound.top;
    const edgePad = w2.globals.barPadForNumericAxis || 0;
    const notInRect = hoverX < -edgePad || hoverY < 0 || hoverX > hoverWidth + edgePad || hoverY > hoverHeight;
    if (notInRect) {
      hoverArea.classList.remove("hovering-zoom");
      hoverArea.classList.remove("hovering-pan");
    } else {
      if (w2.interact.zoomEnabled) {
        hoverArea.classList.remove("hovering-pan");
        hoverArea.classList.add("hovering-zoom");
      } else if (w2.interact.panEnabled) {
        hoverArea.classList.remove("hovering-zoom");
        hoverArea.classList.add("hovering-pan");
      }
    }
    let j = Math.round(hoverX / xDivisor);
    const jHorz = Math.floor(hoverY / yDivisor);
    if (hasBars && !w2.config.xaxis.convertedCatToNumeric) {
      j = Math.ceil(hoverX / xDivisor);
      j = j - 1;
    }
    let capturedSeries = null;
    let closest = null;
    let seriesXValArr = w2.globals.seriesXvalues.map(
      (seriesXVal) => {
        return seriesXVal.filter(
          (s2) => Utils$1.isNumber(s2)
        );
      }
    );
    const seriesYValArr = w2.globals.seriesYvalues.map(
      (seriesYVal) => {
        return seriesYVal.filter(
          (s2) => Utils$1.isNumber(s2)
        );
      }
    );
    if (w2.axisFlags.isXNumeric) {
      closest = this.closestInMultiArray(
        hoverX,
        hoverY,
        seriesXValArr,
        seriesYValArr
      );
      capturedSeries = closest.index;
      j = (_a = closest.j) != null ? _a : 0;
      if (capturedSeries !== null && w2.globals.hasNullValues) {
        seriesXValArr = w2.globals.seriesXvalues[capturedSeries];
        closest = this.closestInArray(hoverX, seriesXValArr);
        j = (_b = closest.j) != null ? _b : 0;
      }
    }
    w2.interact.capturedSeriesIndex = capturedSeries === null ? -1 : capturedSeries;
    if (!j || j < 1) j = 0;
    if (w2.globals.isBarHorizontal) {
      w2.interact.capturedDataPointIndex = jHorz;
    } else {
      w2.interact.capturedDataPointIndex = j;
    }
    return {
      capturedSeries,
      j: w2.globals.isBarHorizontal ? jHorz : j,
      hoverX,
      hoverY
    };
  }
  /**
   * @param {any[]} Xarrays
   */
  getFirstActiveXArray(Xarrays) {
    const w2 = this.w;
    let activeIndex = 0;
    const firstActiveSeriesIndex = Xarrays.map(
      (xarr, index) => {
        return xarr.length > 0 ? index : -1;
      }
    );
    for (let a2 = 0; a2 < firstActiveSeriesIndex.length; a2++) {
      if (firstActiveSeriesIndex[a2] !== -1 && w2.globals.collapsedSeriesIndices.indexOf(a2) === -1 && w2.globals.ancillaryCollapsedSeriesIndices.indexOf(a2) === -1) {
        activeIndex = firstActiveSeriesIndex[a2];
        break;
      }
    }
    return activeIndex;
  }
  /**
   * @param {number} hoverX
   * @param {number} hoverY
   * @param {any[]} Xarrays
   * @param {any[]} Yarrays
   */
  closestInMultiArray(hoverX, hoverY, Xarrays, Yarrays) {
    const w2 = this.w;
    const isActiveSeries = (seriesIndex) => {
      return w2.globals.collapsedSeriesIndices.indexOf(seriesIndex) === -1 && w2.globals.ancillaryCollapsedSeriesIndices.indexOf(seriesIndex) === -1;
    };
    const chartType = w2.config.chart.type;
    const isLineArea = !w2.globals.comboCharts && (chartType === "line" || chartType === "area");
    let closestDist = Infinity;
    let closestSeriesIndex = null;
    let closestPointIndex = null;
    if (w2.globals.allSeriesHasEqualX) {
      let bucketDistX = Infinity;
      for (let i2 = 0; i2 < Xarrays.length; i2++) {
        if (!isActiveSeries(i2)) continue;
        const xArr = Xarrays[i2];
        const yArr = Yarrays[i2];
        const len = Math.min(xArr.length, yArr.length);
        for (let j = 0; j < len; j++) {
          const distX = Math.abs(hoverX - xArr[j]);
          if (distX < bucketDistX) {
            bucketDistX = distX;
            closestPointIndex = j;
          }
        }
      }
      if (closestPointIndex !== null) {
        if (isLineArea) {
          let bestSegDist = Infinity;
          for (let i2 = 0; i2 < Xarrays.length; i2++) {
            if (!isActiveSeries(i2)) continue;
            const xArr = Xarrays[i2];
            const yArr = Yarrays[i2];
            const len = Math.min(xArr.length, yArr.length);
            if (len < 2) {
              const yVal = yArr[closestPointIndex];
              if (typeof yVal !== "number") continue;
              const d = Math.abs(hoverY - yVal);
              if (d < bestSegDist) {
                bestSegDist = d;
                closestSeriesIndex = i2;
              }
              continue;
            }
            for (let j = 0; j < len - 1; j++) {
              const seg = this._distanceToSegment(
                hoverX,
                hoverY,
                xArr[j],
                yArr[j],
                xArr[j + 1],
                yArr[j + 1]
              );
              if (seg.dist < bestSegDist) {
                bestSegDist = seg.dist;
                closestSeriesIndex = i2;
              }
            }
          }
        } else {
          let bestY = Infinity;
          for (let i2 = 0; i2 < Xarrays.length; i2++) {
            if (!isActiveSeries(i2)) continue;
            const yVal = Yarrays[i2][closestPointIndex];
            if (typeof yVal !== "number") continue;
            const distY = Math.abs(hoverY - yVal);
            if (distY < bestY) {
              bestY = distY;
              closestSeriesIndex = i2;
            }
          }
        }
      }
      return {
        index: closestSeriesIndex,
        j: closestPointIndex
      };
    }
    for (let i2 = 0; i2 < Xarrays.length; i2++) {
      if (!isActiveSeries(i2)) {
        continue;
      }
      const xArr = Xarrays[i2];
      const yArr = Yarrays[i2];
      const len = Math.min(xArr.length, yArr.length);
      if (isLineArea && len >= 2) {
        for (let j = 0; j < len - 1; j++) {
          const seg = this._distanceToSegment(
            hoverX,
            hoverY,
            xArr[j],
            yArr[j],
            xArr[j + 1],
            yArr[j + 1]
          );
          if (seg.dist < closestDist) {
            closestDist = seg.dist;
            closestSeriesIndex = i2;
            closestPointIndex = seg.t < 0.5 ? j : j + 1;
          }
        }
        continue;
      }
      for (let j = 0; j < len; j++) {
        const xVal = xArr[j];
        const distX = hoverX - xVal;
        const yVal = yArr[j];
        const distY = hoverY - yVal;
        const dist = Math.sqrt(distX * distX + distY * distY);
        if (dist < closestDist) {
          closestDist = dist;
          closestSeriesIndex = i2;
          closestPointIndex = j;
        }
      }
    }
    return {
      index: closestSeriesIndex,
      j: closestPointIndex
    };
  }
  /**
   * Perpendicular distance from point (px, py) to the line segment
   * (ax, ay) → (bx, by). Returns the distance plus the projection
   * parameter t (0 = at A, 1 = at B, clamped) so callers know which
   * endpoint the projection landed nearest.
   * @param {number} px
   * @param {number} py
   * @param {number} ax
   * @param {number} ay
   * @param {number} bx
   * @param {number} by
   * @returns {{ dist: number, t: number }}
   */
  _distanceToSegment(px, py, ax, ay, bx, by) {
    const dx = bx - ax;
    const dy = by - ay;
    const lenSq = dx * dx + dy * dy;
    let t2 = lenSq === 0 ? 0 : ((px - ax) * dx + (py - ay) * dy) / lenSq;
    if (t2 < 0) t2 = 0;
    else if (t2 > 1) t2 = 1;
    const cx = ax + t2 * dx;
    const cy = ay + t2 * dy;
    const ex = px - cx;
    const ey = py - cy;
    return { dist: Math.sqrt(ex * ex + ey * ey), t: t2 };
  }
  /**
   * @param {number} val
   * @param {any[]} arr
   */
  closestInArray(val, arr) {
    const curr = arr[0];
    let currIndex = null;
    let diff = Math.abs(val - curr);
    for (let i2 = 0; i2 < arr.length; i2++) {
      const newdiff = Math.abs(val - arr[i2]);
      if (newdiff < diff) {
        diff = newdiff;
        currIndex = i2;
      }
    }
    return {
      j: currIndex
    };
  }
  /**
   * When there are multiple series, it is possible to have different x values for each series.
   * But it may be possible in those multiple series, that there is same x value for 2 or more
   * series.
   * @memberof Utils
   * @param {number} j - the inner index of series (series[i][j])
   * @return {boolean}
   */
  isXoverlap(j) {
    const w2 = this.w;
    const xSameForAllSeriesJArr = [];
    const seriesX = w2.seriesData.seriesX.filter(
      (s2) => typeof s2[0] !== "undefined"
    );
    if (seriesX.length > 0) {
      for (let i2 = 0; i2 < seriesX.length - 1; i2++) {
        if (typeof seriesX[i2][j] !== "undefined" && typeof seriesX[i2 + 1][j] !== "undefined") {
          if (seriesX[i2][j] !== seriesX[i2 + 1][j]) {
            xSameForAllSeriesJArr.push("unEqual");
          }
        }
      }
    }
    if (xSameForAllSeriesJArr.length === 0) {
      return true;
    }
    return false;
  }
  isInitialSeriesSameLen() {
    var _a, _b, _c, _d;
    let sameLen = true;
    const initialSeries = (
      /** @type {any[]} */
      ((_b = (_a = this.w.globals._initialSeriesPeek) != null ? _a : this.w.globals.initialSeries) == null ? void 0 : _b.filter(
        /**
         * @param {Record<string, any>} s
         * @param {number} i
         */
        (s2, i2) => {
          var _a2;
          return !((_a2 = this.w.globals.collapsedSeriesIndices) == null ? void 0 : _a2.includes(i2));
        }
      )) || []
    );
    for (let i2 = 0; i2 < initialSeries.length - 1; i2++) {
      if (!((_c = initialSeries[i2]) == null ? void 0 : _c.data) || !((_d = initialSeries[i2 + 1]) == null ? void 0 : _d.data)) return true;
      if (initialSeries[i2].data.length !== initialSeries[i2 + 1].data.length) {
        sameLen = false;
        break;
      }
    }
    return sameLen;
  }
  /**
   * @param {any[]} allbars
   */
  getBarsHeight(allbars) {
    const bars = [...allbars];
    const totalHeight = bars.reduce((acc, bar) => acc + bar.getBBox().height, 0);
    return totalHeight;
  }
  /**
   * @param {number} capturedSeries
   */
  getElMarkers(capturedSeries) {
    if (typeof capturedSeries == "number") {
      return this.w.dom.baseEl.querySelectorAll(
        `.apexcharts-series[data\\:realIndex='${capturedSeries}'] .apexcharts-series-markers-wrap > *`
      );
    }
    return this.w.dom.baseEl.querySelectorAll(
      ".apexcharts-series-markers-wrap > *"
    );
  }
  getAllMarkers(filterCollapsed = false) {
    let markersWraps = (
      /** @type {any[]} */
      [
        ...this.w.dom.baseEl.querySelectorAll(".apexcharts-series-markers-wrap")
      ]
    );
    if (filterCollapsed) {
      markersWraps = markersWraps.filter((m2) => {
        const realIndex = Number(m2.getAttribute("data:realIndex"));
        return this.w.globals.collapsedSeriesIndices.indexOf(realIndex) === -1;
      });
    }
    markersWraps.sort((a2, b2) => {
      var indexA = Number(a2.getAttribute("data:realIndex"));
      var indexB = Number(b2.getAttribute("data:realIndex"));
      return indexB < indexA ? 1 : indexB > indexA ? -1 : 0;
    });
    const markers = [];
    markersWraps.forEach((m2) => {
      markers.push(m2.querySelector(".apexcharts-marker"));
    });
    return markers;
  }
  /**
   * @param {number} capturedSeries
   */
  hasMarkers(capturedSeries) {
    const markers = this.getElMarkers(capturedSeries);
    return markers.length > 0;
  }
  /**
   * @param {any} point
   * @param {number} size
   */
  getPathFromPoint(point, size) {
    const cx = Number(point.getAttribute("cx"));
    const cy = Number(point.getAttribute("cy"));
    const shape = point.getAttribute("shape");
    return new Graphics(this.w).getMarkerPath(cx, cy, shape, size);
  }
  getElBars() {
    return this.w.dom.baseEl.querySelectorAll(
      ".apexcharts-bar-series,  .apexcharts-candlestick-series, .apexcharts-boxPlot-series, .apexcharts-violin-series, .apexcharts-rangebar-series"
    );
  }
  hasBars() {
    const bars = this.getElBars();
    return bars.length > 0;
  }
  /**
   * @param {number} index
   */
  getHoverMarkerSize(index) {
    const w2 = this.w;
    let hoverSize = w2.config.markers.hover.size;
    if (hoverSize === void 0) {
      hoverSize = w2.globals.markers.size[index] + w2.config.markers.hover.sizeOffset;
    }
    return hoverSize;
  }
  /**
   * @param {string} state
   */
  toggleAllTooltipSeriesGroups(state) {
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    if (ttCtx.allTooltipSeriesGroups.length === 0) {
      ttCtx.allTooltipSeriesGroups = w2.dom.baseEl.querySelectorAll(
        ".apexcharts-tooltip-series-group"
      );
    }
    const allTooltipSeriesGroups = ttCtx.allTooltipSeriesGroups;
    for (let i2 = 0; i2 < allTooltipSeriesGroups.length; i2++) {
      if (state === "enable") {
        allTooltipSeriesGroups[i2].classList.add("apexcharts-active");
        allTooltipSeriesGroups[i2].style.display = w2.config.tooltip.items.display;
      } else {
        allTooltipSeriesGroups[i2].classList.remove("apexcharts-active");
        allTooltipSeriesGroups[i2].style.display = "none";
      }
    }
  }
}
class Labels {
  /**
   * @param {import('./Tooltip').default} tooltipContext
   */
  constructor(tooltipContext) {
    this.w = tooltipContext.w;
    this.ttCtx = tooltipContext;
    this.tooltipUtil = new Utils2(tooltipContext);
  }
  /** @param {{ shared?: boolean, ttItems?: any, i?: number, j?: any, y1?: any, y2?: any, e?: any }} opts */
  drawSeriesTexts({ shared = true, ttItems, i: i2 = 0, j = null, y1, y2, e: e2 }) {
    const w2 = this.w;
    if (w2.config.tooltip.custom !== void 0) {
      this.handleCustomTooltip({ i: i2, j, y1, y2, w: w2 });
    } else {
      this.toggleActiveInactiveSeries(shared, i2);
    }
    const values = this.getValuesToPrint({
      i: i2,
      j
    });
    this.printLabels({
      i: i2,
      j,
      values,
      ttItems,
      shared,
      e: e2
    });
    const tooltipEl = this.ttCtx.getElTooltip();
    if (tooltipEl) {
      this.ttCtx.tooltipRect.ttWidth = tooltipEl.getBoundingClientRect().width;
      this.ttCtx.tooltipRect.ttHeight = tooltipEl.getBoundingClientRect().height;
    }
  }
  /** @param {{i: any, j: any, values: any, ttItems: any, shared: any, e: any}} opts */
  printLabels({ i: i2, j, values, ttItems, shared, e: e2 }) {
    const w2 = this.w;
    const { xVal, zVal, xAxisTTVal } = values;
    const seriesLen = w2.seriesData.series.length;
    const basePColor = j !== null && w2.config.plotOptions.bar.distributed ? w2.globals.colors[j] : w2.globals.colors[i2];
    for (let t2 = 0; t2 < seriesLen; t2++) {
      const tIndex = w2.config.tooltip.inverseOrder ? seriesLen - 1 - t2 : t2;
      const row = this.computeSeriesRow({
        i: i2,
        j,
        t: t2,
        tIndex,
        shared,
        e: e2,
        basePColor
      });
      this.DOMHandling({
        i: i2,
        t: tIndex,
        j,
        ttItems,
        values: {
          val: row.val,
          goalVals: row.goalVals,
          xVal,
          xAxisTTVal,
          zVal
        },
        seriesName: row.seriesName,
        shared,
        pColor: row.pColor
      });
    }
  }
  /**
   * Compute the per-series row values (seriesName, val, goalVals, pColor)
   * for one iteration of the tooltip's series loop. Extracted from
   * printLabels() to keep the outer loop scannable.
   * @param {{i: number, j: any, t: number, tIndex: number, shared: boolean, e: any, basePColor: string}} opts
   */
  computeSeriesRow({ i: i2, j, tIndex, shared, e: e2, basePColor }) {
    const w2 = this.w;
    let f = this.getFormatters(i2);
    let pColor = basePColor;
    let val;
    let goalVals = (
      /** @type {any[]} */
      []
    );
    let seriesName = w2.config.chart.type === "treemap" ? f.yLbTitleFormatter(
      String(
        /** @type {any} */
        w2.config.series[i2].data[j].x
      ),
      {
        series: w2.seriesData.series,
        seriesIndex: i2,
        dataPointIndex: j,
        w: w2
      }
    ) : this.getSeriesName({
      fn: f.yLbTitleFormatter,
      index: i2,
      seriesIndex: i2,
      j
    });
    if (w2.globals.axisCharts) {
      if (shared) {
        f = this.getFormatters(tIndex);
        seriesName = this.getSeriesName({
          fn: f.yLbTitleFormatter,
          index: tIndex,
          seriesIndex: i2,
          j
        });
        pColor = w2.globals.colors[tIndex];
        val = this.formatYValue(f, tIndex, j);
        goalVals = this.formatGoalVals(f, tIndex, j);
      } else {
        pColor = this.resolvePatternColor(e2, pColor);
        val = this.formatYValue(f, i2, j);
        goalVals = this.formatGoalVals(f, i2, j);
      }
    }
    if (j === null) {
      val = f.yLbFormatter(w2.seriesData.series[i2], __spreadProps(__spreadValues({}, w2), {
        seriesIndex: i2,
        dataPointIndex: i2
      }));
    }
    return { seriesName, val, goalVals, pColor };
  }
  /**
   * Run the y-value formatter for a given (seriesIndex, dataPointIndex),
   * handling the range-data case (start - end concatenation).
   * @param {{yLbFormatter: Function}} f
   * @param {number} index
   * @param {any} j
   */
  formatYValue(f, index, j) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    if (w2.axisFlags.isRangeData) {
      return f.yLbFormatter((_b = (_a = w2.rangeData.seriesRangeStart) == null ? void 0 : _a[index]) == null ? void 0 : _b[j], {
        series: w2.rangeData.seriesRangeStart,
        seriesIndex: index,
        dataPointIndex: j,
        w: w2
      }) + " - " + f.yLbFormatter((_d = (_c = w2.rangeData.seriesRangeEnd) == null ? void 0 : _c[index]) == null ? void 0 : _d[j], {
        series: w2.rangeData.seriesRangeEnd,
        seriesIndex: index,
        dataPointIndex: j,
        w: w2
      });
    }
    return f.yLbFormatter(w2.seriesData.series[index][j], {
      series: w2.seriesData.series,
      seriesIndex: index,
      dataPointIndex: j,
      w: w2
    });
  }
  /**
   * Format the goal-line values attached to a given (seriesIndex, dataPointIndex).
   * Returns an empty array when no goals exist.
   * @param {{yLbFormatter: Function}} f
   * @param {number} index
   * @param {any} j
   */
  formatGoalVals(f, index, j) {
    var _a;
    const w2 = this.w;
    const goals = (_a = w2.seriesData.seriesGoals[index]) == null ? void 0 : _a[j];
    if (!Array.isArray(goals)) return [];
    return goals.map((goal) => ({
      attrs: goal,
      val: f.yLbFormatter(goal.value, {
        seriesIndex: index,
        dataPointIndex: j,
        w: w2
      })
    }));
  }
  /**
   * When the hovered element has a pattern fill (url(#…Pattern…)), reach
   * into the pattern's first child to pull a stroke color. Otherwise
   * return the raw fill attribute or the fallback.
   * @param {any} e
   * @param {string} fallback
   */
  resolvePatternColor(e2, fallback) {
    var _a, _b, _c;
    const w2 = this.w;
    const targetFill = (_a = e2 == null ? void 0 : e2.target) == null ? void 0 : _a.getAttribute("fill");
    if (!targetFill) return fallback;
    if (targetFill.indexOf("url") === -1) return targetFill;
    if (targetFill.indexOf("Pattern") === -1) return fallback;
    const patternEl = w2.dom.baseEl.querySelector(
      targetFill.substr(4).slice(0, -1)
    );
    return (_c = (_b = patternEl == null ? void 0 : patternEl.childNodes[0]) == null ? void 0 : _b.getAttribute("stroke")) != null ? _c : fallback;
  }
  /**
   * @param {number} i
   */
  getFormatters(i2) {
    const w2 = this.w;
    let yLbFormatter = w2.formatters.yLabelFormatters[i2];
    let yLbTitleFormatter;
    if (w2.formatters.ttVal !== void 0) {
      if (Array.isArray(w2.formatters.ttVal)) {
        yLbFormatter = /** @type {any} */
        w2.formatters.ttVal[i2] && /** @type {any} */
        w2.formatters.ttVal[i2].formatter;
        yLbTitleFormatter = /** @type {any} */
        w2.formatters.ttVal[i2] && /** @type {any} */
        w2.formatters.ttVal[i2].title && /** @type {any} */
        w2.formatters.ttVal[i2].title.formatter;
      } else {
        yLbFormatter = /** @type {any} */
        w2.formatters.ttVal.formatter;
        if (typeof /** @type {any} */
        w2.formatters.ttVal.title.formatter === "function") {
          yLbTitleFormatter = /** @type {any} */
          w2.formatters.ttVal.title.formatter;
        }
      }
    } else {
      yLbTitleFormatter = w2.config.tooltip.y.title.formatter;
    }
    if (typeof yLbFormatter !== "function") {
      if (w2.formatters.yLabelFormatters[0]) {
        yLbFormatter = w2.formatters.yLabelFormatters[0];
      } else {
        yLbFormatter = function(label) {
          return label;
        };
      }
    }
    if (typeof yLbTitleFormatter !== "function") {
      yLbTitleFormatter = function(label) {
        return label ? label + ": " : "";
      };
    }
    return {
      yLbFormatter,
      yLbTitleFormatter
    };
  }
  /** @param {{fn: any, index: any, seriesIndex: any, j: any}} opts */
  getSeriesName({ fn, index, seriesIndex, j }) {
    const w2 = this.w;
    return fn(String(w2.seriesData.seriesNames[index]), {
      series: w2.seriesData.series,
      seriesIndex,
      dataPointIndex: j,
      w: w2
    });
  }
  /** @param {{ t?: any, j?: any, i?: any, ttItems?: any, values?: any, seriesName?: any, shared?: any, pColor?: any }} opts */
  DOMHandling({ t: t2, j, ttItems, values, seriesName, shared, pColor }) {
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    const { val, goalVals, xVal, xAxisTTVal, zVal } = values;
    if (!ttItems || !ttItems[t2]) return;
    let ttItemsChildren = null;
    ttItemsChildren = ttItems[t2].children;
    if (w2.config.tooltip.fillSeriesColor) {
      ttItems[t2].style.backgroundColor = pColor;
      ttItemsChildren[0].style.display = "none";
    }
    if (ttCtx.showTooltipTitle) {
      if (ttCtx.tooltipTitle === null) {
        ttCtx.tooltipTitle = w2.dom.baseEl.querySelector(
          ".apexcharts-tooltip-title"
        );
      }
      if (ttCtx.tooltipTitle) {
        ttCtx.tooltipTitle.innerHTML = xVal;
      }
    }
    if (ttCtx.isXAxisTooltipEnabled) {
      if (ttCtx.xaxisTooltipText) {
        ttCtx.xaxisTooltipText.innerHTML = xAxisTTVal !== "" ? xAxisTTVal : xVal;
      }
    }
    const ttYLabel = ttItems[t2].querySelector(
      ".apexcharts-tooltip-text-y-label"
    );
    if (ttYLabel) {
      ttYLabel.innerHTML = seriesName ? seriesName : "";
    }
    const ttYVal = ttItems[t2].querySelector(".apexcharts-tooltip-text-y-value");
    if (ttYVal) {
      ttYVal.innerHTML = typeof val !== "undefined" ? val : "";
    }
    if (ttItemsChildren[0] && ttItemsChildren[0].classList.contains("apexcharts-tooltip-marker")) {
      if (w2.config.tooltip.marker.fillColors && Array.isArray(w2.config.tooltip.marker.fillColors)) {
        pColor = w2.config.tooltip.marker.fillColors[t2];
      }
      if (w2.config.tooltip.fillSeriesColor) {
        ttItemsChildren[0].style.backgroundColor = pColor;
      } else {
        ttItemsChildren[0].style.color = pColor;
      }
    }
    if (!w2.config.tooltip.marker.show) {
      ttItemsChildren[0].style.display = "none";
    }
    const ttGLabel = ttItems[t2].querySelector(
      ".apexcharts-tooltip-text-goals-label"
    );
    const ttGVal = ttItems[t2].querySelector(
      ".apexcharts-tooltip-text-goals-value"
    );
    if (goalVals.length && w2.seriesData.seriesGoals[t2]) {
      const createGoalsHtml = () => {
        let gLabels = "<div>";
        let gVals = "<div>";
        goalVals.forEach((goal) => {
          gLabels += ` <div style="display: flex"><span class="apexcharts-tooltip-marker" style="background-color: ${goal.attrs.strokeColor}; height: 3px; border-radius: 0; top: 5px;"></span> ${goal.attrs.name}</div>`;
          gVals += `<div>${goal.val}</div>`;
        });
        ttGLabel.innerHTML = gLabels + `</div>`;
        ttGVal.innerHTML = gVals + `</div>`;
      };
      if (shared) {
        if (w2.seriesData.seriesGoals[t2][j] && Array.isArray(w2.seriesData.seriesGoals[t2][j])) {
          createGoalsHtml();
        } else {
          ttGLabel.innerHTML = "";
          ttGVal.innerHTML = "";
        }
      } else {
        createGoalsHtml();
      }
    } else {
      ttGLabel.innerHTML = "";
      ttGVal.innerHTML = "";
    }
    if (zVal !== null) {
      const ttZLabel = ttItems[t2].querySelector(
        ".apexcharts-tooltip-text-z-label"
      );
      ttZLabel.innerHTML = w2.config.tooltip.z.title;
      const ttZVal = ttItems[t2].querySelector(
        ".apexcharts-tooltip-text-z-value"
      );
      ttZVal.innerHTML = typeof zVal !== "undefined" ? zVal : "";
    }
    if (shared && ttItemsChildren[0]) {
      if (w2.config.tooltip.hideEmptySeries) {
        const ttItemMarker = ttItems[t2].querySelector(
          ".apexcharts-tooltip-marker"
        );
        const ttItemText = ttItems[t2].querySelector(".apexcharts-tooltip-text");
        if (parseFloat(val) == 0) {
          ttItemMarker.style.display = "none";
          ttItemText.style.display = "none";
        } else {
          ttItemMarker.style.display = "block";
          ttItemText.style.display = "block";
        }
      }
      if (typeof val === "undefined" || val === null || w2.globals.ancillaryCollapsedSeriesIndices.indexOf(t2) > -1 || w2.globals.collapsedSeriesIndices.indexOf(t2) > -1 || Array.isArray(ttCtx.tConfig.enabledOnSeries) && ttCtx.tConfig.enabledOnSeries.indexOf(t2) === -1) {
        ttItemsChildren[0].parentNode.style.display = "none";
      } else {
        ttItemsChildren[0].parentNode.style.display = w2.config.tooltip.items.display;
      }
    } else {
      if (Array.isArray(ttCtx.tConfig.enabledOnSeries) && ttCtx.tConfig.enabledOnSeries.indexOf(t2) === -1) {
        ttItemsChildren[0].parentNode.style.display = "none";
      }
    }
  }
  /**
   * @param {boolean} shared
   * @param {number} i
   */
  toggleActiveInactiveSeries(shared, i2) {
    const w2 = this.w;
    if (shared) {
      this.tooltipUtil.toggleAllTooltipSeriesGroups("enable");
    } else {
      this.tooltipUtil.toggleAllTooltipSeriesGroups("disable");
      const firstTooltipSeriesGroup = w2.dom.baseEl.querySelector(
        `.apexcharts-tooltip-series-group-${i2}`
      );
      if (firstTooltipSeriesGroup) {
        const ftsGroup = (
          /** @type {HTMLElement} */
          firstTooltipSeriesGroup
        );
        ftsGroup.classList.add("apexcharts-active");
        ftsGroup.style.display = w2.config.tooltip.items.display;
      }
    }
  }
  /** @param {{i: any, j: any}} opts */
  getValuesToPrint({ i: i2, j }) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const w2 = this.w;
    const filteredSeriesX = w2.seriesData.seriesX.map(
      (ser) => ser.length > 0 ? ser : []
    );
    let xVal = "";
    let xAxisTTVal = "";
    let zVal = null;
    let val = null;
    const customFormatterOpts = {
      series: w2.seriesData.series,
      seriesIndex: i2,
      dataPointIndex: j,
      w: w2
    };
    const zFormatter = w2.formatters.ttZFormatter;
    if (j === null) {
      val = w2.seriesData.series[i2];
    } else {
      if (w2.axisFlags.isXNumeric && w2.config.chart.type !== "treemap") {
        xVal = filteredSeriesX[i2][j];
        if (filteredSeriesX[i2].length === 0) {
          const firstActiveSeriesIndex = this.tooltipUtil.getFirstActiveXArray(filteredSeriesX);
          xVal = filteredSeriesX[firstActiveSeriesIndex][j];
        }
      } else {
        const dataFormat = new Data(this.w);
        if (dataFormat.isFormatXY()) {
          xVal = typeof /** @type {any} */
          w2.config.series[i2].data[j] !== "undefined" ? (
            /** @type {any} */
            w2.config.series[i2].data[j].x
          ) : "";
        } else {
          xVal = typeof w2.labelData.labels[j] !== "undefined" ? w2.labelData.labels[j] : "";
        }
      }
    }
    const bufferXVal = xVal;
    if (w2.axisFlags.isXNumeric && w2.config.xaxis.type === "datetime") {
      const xFormat = new Formatters(this.w);
      xVal = xFormat.xLabelFormat(
        /** @type {Function} */
        w2.formatters.ttKeyFormatter,
        bufferXVal,
        bufferXVal,
        {
          i: void 0,
          dateFormatter: new DateTime(this.w).formatDate,
          w: this.w
        }
      );
    } else {
      if (w2.globals.isBarHorizontal) {
        xVal = w2.formatters.yLabelFormatters[0](bufferXVal, customFormatterOpts);
      } else {
        xVal = (_c = (_b = (_a = w2.formatters).xLabelFormatter) == null ? void 0 : _b.call(_a, bufferXVal, customFormatterOpts)) != null ? _c : bufferXVal;
      }
    }
    if (w2.config.tooltip.x.formatter !== void 0) {
      xVal = (_f = (_e = (_d = w2.formatters).ttKeyFormatter) == null ? void 0 : _e.call(_d, bufferXVal, customFormatterOpts)) != null ? _f : bufferXVal;
    }
    if (w2.seriesData.seriesZ.length > 0 && w2.seriesData.seriesZ[i2].length > 0) {
      zVal = zFormatter == null ? void 0 : zFormatter(w2.seriesData.seriesZ[i2][j], w2);
    }
    if (typeof w2.config.xaxis.tooltip.formatter === "function") {
      xAxisTTVal = (_h = (_g = w2.formatters).xaxisTooltipFormatter) == null ? void 0 : _h.call(
        _g,
        bufferXVal,
        customFormatterOpts
      );
    } else {
      xAxisTTVal = xVal;
    }
    return {
      val: Array.isArray(val) ? val.join(" ") : val,
      xVal: Array.isArray(xVal) ? xVal.join(" ") : xVal,
      xAxisTTVal: Array.isArray(xAxisTTVal) ? xAxisTTVal.join(" ") : xAxisTTVal,
      zVal
    };
  }
  /** @param {{i: any, j: any, y1: any, y2: any, w: any}} opts */
  handleCustomTooltip({ i: i2, j, y1, y2, w: w2 }) {
    const tooltipEl = this.ttCtx.getElTooltip();
    let fn = w2.config.tooltip.custom;
    if (Array.isArray(fn)) {
      fn = fn[i2];
    }
    if (typeof fn !== "function") return;
    const customTooltip = fn({
      series: w2.seriesData.series,
      seriesIndex: i2,
      dataPointIndex: j,
      y1,
      y2,
      w: w2
    });
    if (tooltipEl) {
      const arrowEl = tooltipEl.querySelector(".apexcharts-tooltip-arrow");
      if (typeof customTooltip === "string" || typeof customTooltip === "number") {
        tooltipEl.innerHTML = String(customTooltip);
      } else if (customTooltip != null && (customTooltip instanceof Element || typeof customTooltip.nodeName === "string")) {
        tooltipEl.innerHTML = "";
        tooltipEl.appendChild(customTooltip.cloneNode(true));
      }
      if (arrowEl) tooltipEl.appendChild(arrowEl);
    }
  }
}
const ARROW_TIP_OVERHANG = 7;
const POINT_TIP_GAP = 0;
class Position {
  /**
   * @param {import('./Tooltip').default} tooltipContext
   */
  constructor(tooltipContext) {
    this.ttCtx = tooltipContext;
    this.w = tooltipContext.w;
  }
  /**
   * This will move the crosshair (the vertical/horz line that moves along with mouse)
   * Along with this, this function also calls the xaxisMove function
   * @memberof Position
   * @param {number} cx - point's x position, wherever point's x is, you need to move crosshair
   * @param {number | null} [j]
   */
  moveXCrosshairs(cx, j = null) {
    const ttCtx = this.ttCtx;
    const w2 = this.w;
    const xcrosshairs = ttCtx.getElXCrosshairs();
    let x = cx - ttCtx.xcrosshairsWidth / 2;
    const tickAmount = w2.labelData.labels.slice().length;
    if (j !== null) {
      x = w2.layout.gridWidth / tickAmount * j;
    }
    if (xcrosshairs !== null && !w2.globals.isBarHorizontal) {
      xcrosshairs.setAttribute("x", String(x));
      xcrosshairs.setAttribute("x1", String(x));
      xcrosshairs.setAttribute("x2", String(x));
      xcrosshairs.setAttribute("y2", String(w2.layout.gridHeight));
      xcrosshairs.classList.add("apexcharts-active");
    }
    if (x < 0) {
      x = 0;
    }
    if (x > w2.layout.gridWidth) {
      x = w2.layout.gridWidth;
    }
    if (ttCtx.isXAxisTooltipEnabled) {
      let tx = x;
      if (w2.config.xaxis.crosshairs.width === "tickWidth" || w2.config.xaxis.crosshairs.width === "barWidth") {
        tx = x + ttCtx.xcrosshairsWidth / 2;
      }
      this.moveXAxisTooltip(tx);
    }
  }
  /**
   * This will move the crosshair (the vertical/horz line that moves along with mouse)
   * Along with this, this function also calls the xaxisMove function
   * @memberof Position
   * @param {number} cy - point's y position, wherever point's y is, you need to move crosshair
   */
  moveYCrosshairs(cy) {
    const ttCtx = this.ttCtx;
    if (ttCtx.ycrosshairs !== null) {
      Graphics.setAttrs(ttCtx.ycrosshairs, {
        y1: cy,
        y2: cy
      });
    }
    if (ttCtx.ycrosshairsHidden !== null) {
      Graphics.setAttrs(ttCtx.ycrosshairsHidden, {
        y1: cy,
        y2: cy
      });
    }
  }
  /**
   ** AxisTooltip is the small rectangle which appears on x axis with x value, when user moves
   * @memberof Position
   * @param {number} cx - point's x position, wherever point's x is, you need to move
   */
  moveXAxisTooltip(cx) {
    var _a, _b;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    if (ttCtx.xaxisTooltip !== null && ttCtx.xcrosshairsWidth !== 0) {
      ttCtx.xaxisTooltip.classList.add("apexcharts-active");
      const cy = ttCtx.xaxisOffY + w2.config.xaxis.tooltip.offsetY + w2.layout.translateY + 5 + w2.config.xaxis.offsetY;
      const xaxisTTText = ttCtx.xaxisTooltip.getBoundingClientRect();
      const xaxisTTTextWidth = xaxisTTText.width;
      cx = cx - xaxisTTTextWidth / 2;
      if (!isNaN(cx)) {
        cx = cx + w2.layout.translateX;
        const graphics = new Graphics(this.w);
        const textRect = graphics.getTextRects(
          (_b = (_a = ttCtx.xaxisTooltipText) == null ? void 0 : _a.innerHTML) != null ? _b : "",
          w2.config.xaxis.labels.style.fontSize
        );
        if (ttCtx.xaxisTooltipText) {
          ttCtx.xaxisTooltipText.style.minWidth = textRect.width + "px";
        }
        ttCtx.xaxisTooltip.style.left = cx + "px";
        ttCtx.xaxisTooltip.style.top = cy + "px";
      }
    }
  }
  /**
   * @param {number} index
   */
  moveYAxisTooltip(index) {
    var _a, _b;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    if (ttCtx.yaxisTTEls === null) {
      ttCtx.yaxisTTEls = /** @type {any[]} */
      [
        ...w2.dom.baseEl.querySelectorAll(".apexcharts-yaxistooltip")
      ];
    }
    const ycrosshairsHiddenRectY1 = parseInt(
      (_b = (_a = ttCtx.ycrosshairsHidden) == null ? void 0 : _a.getAttribute("y1")) != null ? _b : "0",
      10
    );
    let cy = w2.layout.translateY + ycrosshairsHiddenRectY1;
    if (ttCtx.yaxisTTEls) {
      const yAxisTTRect = ttCtx.yaxisTTEls[index].getBoundingClientRect();
      const yAxisTTHeight = yAxisTTRect.height;
      let cx;
      const labelsGroup = (
        /** @type {SVGGElement | null} */
        w2.dom.baseEl.querySelector(
          `.apexcharts-yaxis[rel='${index}'] .apexcharts-yaxis-texts-g`
        )
      );
      const elWrapRect = w2.dom.elWrap.getBoundingClientRect();
      if (labelsGroup) {
        const lr = labelsGroup.getBoundingClientRect();
        if (lr.width > 0) {
          const labelsCenterInElWrap = lr.left + lr.width / 2 - elWrapRect.left;
          cx = labelsCenterInElWrap - yAxisTTRect.width / 2;
        }
      }
      if (cx == null) {
        const GAP = 4;
        cx = w2.config.yaxis[index].opposite ? w2.globals.translateYAxisX[index] + GAP : w2.globals.translateYAxisX[index] - yAxisTTRect.width - GAP;
      }
      cy = cy - yAxisTTHeight / 2;
      if (w2.globals.ignoreYAxisIndexes.indexOf(index) === -1 && cy > 0 && cy < w2.layout.gridHeight) {
        ttCtx.yaxisTTEls[index].classList.add("apexcharts-active");
        ttCtx.yaxisTTEls[index].style.top = cy + "px";
        ttCtx.yaxisTTEls[index].style.left = cx + w2.config.yaxis[index].tooltip.offsetX + "px";
      } else {
        ttCtx.yaxisTTEls[index].classList.remove("apexcharts-active");
      }
    }
  }
  /**
   ** moves the whole tooltip by changing x, y attrs
   * @memberof Position
   * @param {number} cx - point's x position, wherever point's x is, you need to move tooltip
   * @param {number} cy - point's y position, wherever point's y is, you need to move tooltip
   * @param {number | null} [markerSize] - point's size
   */
  moveTooltip(cx, cy, markerSize = null) {
    const ttCtx = this.ttCtx;
    const tooltipEl = ttCtx.getElTooltip();
    if (!tooltipEl) return;
    const pos = this.computeTooltipPosition(cx, cy, markerSize);
    if (pos === null) return;
    this.applyTooltipPosition(tooltipEl, pos);
  }
  /**
   * Pure-ish (reads from `this.ttCtx` + `this.w` but performs no DOM writes)
   * computation of the tooltip box position, edge placement (for arrow),
   * and arrow vertical offset. Returns null when inputs are not numeric.
   *
   * @param {number} cx
   * @param {number} cy
   * @param {number | null} [markerSize]
   * @returns {{ x: number, y: number, placement: 'left'|'right', arrowY: number|null } | null}
   */
  computeTooltipPosition(cx, cy, markerSize = null) {
    var _a, _b, _c, _d, _e, _f, _g;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    const tooltipRect = ttCtx.tooltipRect;
    const arrowEnabled = !!w2.config.tooltip.arrow;
    const pointSize = markerSize !== null ? parseFloat(String(markerSize)) : 1;
    const ttH = tooltipRect.ttHeight || 0;
    const ttW = tooltipRect.ttWidth || 0;
    const cxNum = parseFloat(String(cx));
    const cyNum = parseFloat(String(cy));
    if (isNaN(cxNum) || isNaN(cyNum)) return null;
    const clearance = pointSize + (arrowEnabled ? ARROW_TIP_OVERHANG : 0) + POINT_TIP_GAP;
    let x = cxNum + clearance;
    const pointY = cyNum + w2.layout.translateY;
    let y = arrowEnabled ? pointY - ttH / 2 + pointSize / 2 : cyNum + pointSize / 2;
    let placement = "right";
    if (x > w2.layout.gridWidth / 2) {
      x = cxNum - ttW - clearance;
      placement = "left";
    }
    if (x > w2.layout.gridWidth - ttW - 10) {
      x = placement === "left" ? Math.min(w2.layout.gridWidth - ttW, x) : w2.layout.gridWidth - ttW;
    }
    if (x < -20) {
      x = -20;
    }
    if (w2.config.tooltip.followCursor) {
      const elGrid = ttCtx.getElGrid();
      if (!elGrid) return null;
      const seriesBound = elGrid.getBoundingClientRect();
      x = ttCtx.e.clientX - seriesBound.left;
      if (x > w2.layout.gridWidth / 2) {
        x = x - ttW;
        placement = "left";
      } else {
        placement = "right";
      }
      y = ttCtx.e.clientY + w2.layout.translateY - seriesBound.top;
      if (y > w2.layout.gridHeight / 2) {
        y = y - ttH;
      }
    } else {
      if (!w2.globals.isBarHorizontal) {
        if (arrowEnabled) {
          const gridTop = w2.layout.translateY;
          const gridBottom = w2.layout.translateY + w2.layout.gridHeight;
          if (y + ttH > gridBottom) {
            y = gridBottom - ttH;
          }
          if (y < gridTop) {
            y = gridTop;
          }
        } else {
          if (ttH / 2 + y > w2.layout.gridHeight) {
            y = w2.layout.gridHeight - ttH + w2.layout.translateY;
          }
        }
      }
    }
    if (isNaN(x)) return null;
    x = x + w2.layout.translateX;
    const a11y = (_b = (_a = w2.config) == null ? void 0 : _a.chart) == null ? void 0 : _b.accessibility;
    if ((a11y == null ? void 0 : a11y.enabled) && ((_d = (_c = a11y == null ? void 0 : a11y.keyboard) == null ? void 0 : _c.navigation) == null ? void 0 : _d.enabled) && ((_g = (_f = (_e = w2.dom) == null ? void 0 : _e.baseEl) == null ? void 0 : _f.querySelector) == null ? void 0 : _g.call(_f, ".apexcharts-keyboard-focused"))) {
      const refPointY = arrowEnabled ? pointY : cyNum;
      const margin = (pointSize || 1) + 12;
      const tooltipTop = y;
      const tooltipBottom = y + ttH;
      if (!isNaN(refPointY) && ttH > 0 && tooltipTop < refPointY + margin && tooltipBottom > refPointY - margin) {
        y = refPointY - ttH - margin;
        if (y < 0) {
          y = refPointY + margin;
        }
      }
    }
    let arrowY = null;
    if (arrowEnabled && ttH > 0) {
      const localY = pointY - y;
      const minArrowY = 10;
      const maxArrowY = ttH - 10;
      arrowY = Math.max(minArrowY, Math.min(maxArrowY, localY));
    }
    return { x, y, placement, arrowY };
  }
  /**
   * Single DOM-writer used by every positioning path on the main tooltip.
   * Replaces the duplicated `style.left/top` writes that previously lived
   * in Position.moveTooltip, Tooltip.drawFixedTooltipRect, and Intersect.
   *
   * @param {HTMLElement} tooltipEl
   * @param {{
   *   x: number,
   *   y: number,
   *   placement?: 'left'|'right'|'top'|'bottom',
   *   arrowY?: number|null,
   *   arrowX?: number|null,
   * }} pos
   */
  applyTooltipPosition(tooltipEl, pos) {
    if (!tooltipEl) return;
    const firstPaint = tooltipEl.dataset.positioned !== "true";
    if (firstPaint) {
      tooltipEl.style.transitionProperty = "none";
    }
    tooltipEl.style.left = pos.x + "px";
    tooltipEl.style.top = pos.y + "px";
    if (pos.placement) {
      tooltipEl.dataset.placement = pos.placement;
    }
    if (pos.arrowY != null) {
      tooltipEl.style.setProperty("--apx-tt-arrow-y", pos.arrowY + "px");
    }
    if (pos.arrowX != null) {
      tooltipEl.style.setProperty("--apx-tt-arrow-x", pos.arrowX + "px");
    }
    if (firstPaint) {
      void tooltipEl.offsetWidth;
      tooltipEl.dataset.positioned = "true";
      requestAnimationFrame(() => {
        tooltipEl.style.transitionProperty = "";
      });
    }
  }
  /**
   * @param {number} i
   * @param {number} j
   */
  moveMarkers(i2, j) {
    var _a;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    if (w2.globals.markers.size[i2] > 0) {
      const allPoints = w2.dom.baseEl.querySelectorAll(
        ` .apexcharts-series[data\\:realIndex='${i2}'] .apexcharts-marker`
      );
      for (let p2 = 0; p2 < allPoints.length; p2++) {
        if (parseInt((_a = allPoints[p2].getAttribute("rel")) != null ? _a : "0", 10) === j) {
          ttCtx.marker.resetPointsSize();
          ttCtx.marker.enlargeCurrentPoint(j, allPoints[p2]);
        }
      }
    } else {
      ttCtx.marker.resetPointsSize();
      this.moveDynamicPointOnHover(j, i2);
    }
  }
  // This function is used when you need to show markers/points only on hover -
  // DIFFERENT X VALUES in multiple series
  /**
   * @param {number} j
   * @param {number} capturedSeries
   */
  moveDynamicPointOnHover(j, capturedSeries) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    let cx = 0;
    let cy = 0;
    const graphics = new Graphics(this.w);
    const pointsArr = w2.globals.pointsArray;
    const hoverSize = ttCtx.tooltipUtil.getHoverMarkerSize(capturedSeries);
    const serType = (
      /** @type {any} */
      w2.config.series[capturedSeries].type
    );
    if (serType && (serType === "column" || serType === "candlestick" || serType === "boxPlot" || serType === "violin")) {
      return;
    }
    cx = (_b = (_a = pointsArr[capturedSeries]) == null ? void 0 : _a[j]) == null ? void 0 : _b[0];
    cy = ((_d = (_c = pointsArr[capturedSeries]) == null ? void 0 : _c[j]) == null ? void 0 : _d[1]) || 0;
    const point = w2.dom.baseEl.querySelector(
      `.apexcharts-series[data\\:realIndex='${capturedSeries}'] .apexcharts-series-markers path`
    );
    if (point && cy < w2.layout.gridHeight && cy > 0) {
      const shape = (_e = point.getAttribute("shape")) != null ? _e : "circle";
      const path = graphics.getMarkerPath(cx, cy, shape, hoverSize * 1.5);
      point.setAttribute("d", path);
    }
    this.moveXCrosshairs(cx);
    if (!ttCtx.fixedTooltip) {
      this.moveTooltip(cx, cy, hoverSize);
    }
  }
  // This function is used when you need to show markers/points only on hover -
  // SAME X VALUES in multiple series
  /**
   * @param {number} j
   */
  moveDynamicPointsOnHover(j) {
    var _a, _b;
    const ttCtx = this.ttCtx;
    const w2 = ttCtx.w;
    let cx = 0;
    let cy = 0;
    let activeSeries = 0;
    const pointsArr = w2.globals.pointsArray;
    const series = new Series(this.w);
    const graphics = new Graphics(this.w);
    activeSeries = series.getActiveConfigSeriesIndex("asc", [
      "line",
      "area",
      "scatter",
      "bubble"
    ]);
    const hoverSize = ttCtx.tooltipUtil.getHoverMarkerSize(activeSeries);
    if ((_a = pointsArr[activeSeries]) == null ? void 0 : _a[j]) {
      cx = pointsArr[activeSeries][j][0];
      cy = pointsArr[activeSeries][j][1];
    }
    if (isNaN(cx)) {
      return;
    }
    const points = ttCtx.tooltipUtil.getAllMarkers();
    if (points.length) {
      for (let p2 = 0; p2 < w2.seriesData.series.length; p2++) {
        const pointArr = pointsArr[p2];
        if (w2.globals.comboCharts) {
          if (typeof pointArr === "undefined") {
            points.splice(p2, 0, null);
          }
        }
        if (points[p2] && pointArr && pointArr.length) {
          let pcy = pointsArr[p2][j][1];
          let pcy2;
          points[p2].setAttribute("cx", cx);
          const shape = (_b = points[p2].getAttribute("shape")) != null ? _b : "circle";
          if (w2.config.chart.type === "rangeArea" && !w2.globals.comboCharts) {
            const rangeStartIndex = j + w2.seriesData.series[p2].length;
            pcy2 = pointsArr[p2][rangeStartIndex][1];
            const pcyDiff = Math.abs(pcy - pcy2) / 2;
            pcy = pcy - pcyDiff;
          }
          if (pcy !== null && !isNaN(pcy) && pcy < w2.layout.gridHeight + hoverSize && pcy + hoverSize > 0) {
            const path = graphics.getMarkerPath(cx, pcy, shape, hoverSize);
            points[p2].setAttribute("d", path);
          } else {
            points[p2].setAttribute("d", "");
          }
        }
      }
    }
    this.moveXCrosshairs(cx);
    if (!ttCtx.fixedTooltip) {
      this.moveTooltip(cx, cy || w2.layout.gridHeight, hoverSize);
    }
  }
  /**
   * @param {number} j
   * @param {number} capturedSeries
   */
  moveStickyTooltipOverBars(j, capturedSeries) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    let barLen = w2.globals.columnSeries ? (
      /** @type {any} */
      w2.globals.columnSeries.length
    ) : w2.seriesData.series.length;
    if (w2.config.chart.stacked) {
      barLen = w2.globals.barGroups.length;
    }
    let i2 = barLen >= 2 && barLen % 2 === 0 ? Math.floor(barLen / 2) : Math.floor(barLen / 2) + 1;
    if (w2.globals.isBarHorizontal) {
      const series = new Series(this.w);
      i2 = series.getActiveConfigSeriesIndex("desc") + 1;
    }
    let jBar = w2.dom.baseEl.querySelector(
      `.apexcharts-bar-series .apexcharts-series[rel='${i2}'] path[j='${j}'], .apexcharts-candlestick-series .apexcharts-series[rel='${i2}'] path[j='${j}'], .apexcharts-boxPlot-series .apexcharts-series[rel='${i2}'] path[j='${j}'], .apexcharts-violin-series .apexcharts-series[rel='${i2}'] path[j='${j}'], .apexcharts-rangebar-series .apexcharts-series[rel='${i2}'] path[j='${j}']`
    );
    if (!jBar && typeof capturedSeries === "number") {
      jBar = w2.dom.baseEl.querySelector(
        `.apexcharts-bar-series .apexcharts-series[data\\:realIndex='${capturedSeries}'] path[j='${j}'],
        .apexcharts-candlestick-series .apexcharts-series[data\\:realIndex='${capturedSeries}'] path[j='${j}'],
        .apexcharts-boxPlot-series .apexcharts-series[data\\:realIndex='${capturedSeries}'] path[j='${j}'],
        .apexcharts-violin-series .apexcharts-series[data\\:realIndex='${capturedSeries}'] path[j='${j}'],
        .apexcharts-rangebar-series .apexcharts-series[data\\:realIndex='${capturedSeries}'] path[j='${j}']`
      );
    }
    let bc = null;
    const bcc = (
      /** @type {any} */
      w2.globals.barCanvasCoords
    );
    if (!jBar && bcc) {
      bc = typeof capturedSeries === "number" && ((_a = bcc[capturedSeries]) == null ? void 0 : _a[j]) || null;
      if (!bc) {
        for (const key in bcc) {
          if ((_b = bcc[key]) == null ? void 0 : _b[j]) {
            bc = bcc[key][j];
            break;
          }
        }
      }
    }
    let bcx = jBar ? parseFloat((_c = jBar.getAttribute("cx")) != null ? _c : "0") : bc ? bc.cx : 0;
    let bcy = jBar ? parseFloat((_d = jBar.getAttribute("cy")) != null ? _d : "0") : bc ? bc.cy : 0;
    const bw = jBar ? parseFloat((_e = jBar.getAttribute("barWidth")) != null ? _e : "0") : bc ? bc.barWidth : 0;
    const elGrid = ttCtx.getElGrid();
    if (!elGrid) return;
    const seriesBound = elGrid.getBoundingClientRect();
    const isBoxOrCandle = jBar && (jBar.classList.contains("apexcharts-candlestick-area") || jBar.classList.contains("apexcharts-boxPlot-area"));
    if (w2.axisFlags.isXNumeric) {
      if (jBar && !isBoxOrCandle) {
        const center = this._datapointCenterXFromBars(j);
        if (center != null) {
          bcx = center;
        } else {
          bcx = bcx - (barLen % 2 !== 0 ? bw / 2 : 0);
        }
      }
      if (jBar && // fixes apexcharts.js#2354
      isBoxOrCandle) {
        bcx = bcx - bw / 2;
      }
    } else {
      if (!w2.globals.isBarHorizontal && !bc) {
        bcx = ttCtx.xAxisTicksPositions[j - 1] + ttCtx.dataPointsDividedWidth / 2;
        if (isNaN(bcx)) {
          bcx = ttCtx.xAxisTicksPositions[j] - ttCtx.dataPointsDividedWidth / 2;
        }
      }
    }
    if (!w2.globals.isBarHorizontal) {
      if (w2.config.tooltip.followCursor) {
        bcy = ttCtx.e.clientY - seriesBound.top - ttCtx.tooltipRect.ttHeight / 2;
      } else {
        if (bcy + ttCtx.tooltipRect.ttHeight + 15 > w2.layout.gridHeight) {
          bcy = w2.layout.gridHeight;
        }
      }
    } else {
      bcy = bcy - ttCtx.tooltipRect.ttHeight;
    }
    if (!w2.globals.isBarHorizontal) {
      this.moveXCrosshairs(bcx);
    }
    if (!ttCtx.fixedTooltip) {
      if (w2.globals.isBarHorizontal && !w2.config.tooltip.followCursor) {
        const placed = this.placeHorizontalSharedTooltip(j);
        if (placed) return;
      }
      this.moveTooltip(bcx, bcy || w2.layout.gridHeight);
    }
  }
  /**
   * Place tooltip above (or flipped: below) the union rect of all bars at
   * dataPointIndex `j` for horizontal-bar-likes. Returns true when a
   * Compute the true horizontal center of dataPointIndex `j` in grid-local
   * coords from the union of every visible bar's `getBoundingClientRect()`.
   * Used as a replacement for the (buggy on numeric/datetime xaxis) `cx`
   * attribute math in `moveStickyTooltipOverBars`. Returns null when no
   * usable bars are found.
   * @param {number} j
   * @returns {number | null}
   */
  _datapointCenterXFromBars(j) {
    var _a, _b;
    const w2 = this.w;
    const bars = w2.dom.baseEl.querySelectorAll(
      `.apexcharts-bar-series path[j='${j}'],.apexcharts-rangebar-series path[j='${j}']`
    );
    if (!bars.length) return null;
    let unionLeft = Infinity;
    let unionRight = -Infinity;
    for (const bar of bars) {
      const parent = (
        /** @type {Element|null} */
        bar.parentNode
      );
      if ((_b = (_a = parent == null ? void 0 : parent.classList) == null ? void 0 : _a.contains) == null ? void 0 : _b.call(_a, "apexcharts-series-collapsed")) continue;
      const r2 = (
        /** @type {Element} */
        bar.getBoundingClientRect()
      );
      if (r2.width === 0 && r2.height === 0) continue;
      if (r2.left < unionLeft) unionLeft = r2.left;
      if (r2.right > unionRight) unionRight = r2.right;
    }
    if (!isFinite(unionLeft)) return null;
    return AxisMapping.screenXToPlotPx(w2, (unionLeft + unionRight) / 2);
  }
  /**
   * Place tooltip above (or flipped: below) the union rect of all bars at
   * dataPointIndex `j` for horizontal-bar-likes. Returns true when a
   * placement was applied; false when no bars found (caller falls back).
   * @param {number} j
   * @returns {boolean}
   */
  placeHorizontalSharedTooltip(j) {
    var _a, _b;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    const tooltipEl = ttCtx.getElTooltip();
    if (!tooltipEl) return false;
    const elGrid = ttCtx.getElGrid();
    if (!elGrid) return false;
    const gridRect = elGrid.getBoundingClientRect();
    const bars = w2.dom.baseEl.querySelectorAll(
      `.apexcharts-bar-series path[j='${j}'],.apexcharts-rangebar-series path[j='${j}'],.apexcharts-boxPlot-series path[j='${j}']`
    );
    if (!bars.length) return false;
    let unionLeft = Infinity;
    let unionRight = -Infinity;
    let unionTop = Infinity;
    let unionBottom = -Infinity;
    for (const bar of bars) {
      const parent = (
        /** @type {Element|null} */
        bar.parentNode
      );
      if ((_b = (_a = parent == null ? void 0 : parent.classList) == null ? void 0 : _a.contains) == null ? void 0 : _b.call(_a, "apexcharts-series-collapsed")) continue;
      const r2 = (
        /** @type {Element} */
        bar.getBoundingClientRect()
      );
      if (r2.width === 0 && r2.height === 0) continue;
      if (r2.left < unionLeft) unionLeft = r2.left;
      if (r2.right > unionRight) unionRight = r2.right;
      if (r2.top < unionTop) unionTop = r2.top;
      if (r2.bottom > unionBottom) unionBottom = r2.bottom;
    }
    if (!isFinite(unionLeft)) return false;
    const ttW = ttCtx.tooltipRect.ttWidth || 0;
    const ttH = ttCtx.tooltipRect.ttHeight || 0;
    const rowCenterX = (unionLeft + unionRight) / 2 - gridRect.left + w2.layout.translateX;
    const rowTopElWrap = unionTop - gridRect.top + w2.layout.translateY;
    const rowBottomElWrap = unionBottom - gridRect.top + w2.layout.translateY;
    const gridTop = w2.layout.translateY;
    const gridBottom = w2.layout.translateY + w2.layout.gridHeight;
    const gridLeft = w2.layout.translateX;
    const gridRight = w2.layout.translateX + w2.layout.gridWidth;
    let placement = "top";
    let finalY = rowTopElWrap - ttH - ARROW_TIP_OVERHANG;
    if (finalY < gridTop) {
      const belowTop = rowBottomElWrap + ARROW_TIP_OVERHANG;
      if (belowTop + ttH <= gridBottom) {
        placement = "bottom";
        finalY = belowTop;
      }
    }
    let finalX = rowCenterX - ttW / 2;
    if (finalX < gridLeft) finalX = gridLeft;
    if (finalX + ttW > gridRight) finalX = gridRight - ttW;
    const arrowX = Math.max(10, Math.min(ttW - 10, rowCenterX - finalX));
    this.applyTooltipPosition(tooltipEl, {
      x: finalX,
      y: finalY,
      placement,
      arrowY: null,
      arrowX
    });
    return true;
  }
}
function renderMarkerSVG(shape) {
  const svg = (body) => `<svg viewBox="0 0 12 12" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">${body}</svg>`;
  switch (shape) {
    case "square":
    case "rect":
      return svg('<rect x="1" y="1" width="10" height="10" rx="1" fill="currentColor"/>');
    case "line":
      return svg('<rect x="0" y="5" width="12" height="2" rx="1" fill="currentColor"/>');
    case "diamond":
      return svg('<path d="M6 0.5 L11.5 6 L6 11.5 L0.5 6 Z" fill="currentColor"/>');
    case "triangle":
      return svg('<path d="M6 1 L11.2 10.5 L0.8 10.5 Z" fill="currentColor"/>');
    case "cross":
      return svg(
        '<path d="M2 2 L10 10 M10 2 L2 10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>'
      );
    case "plus":
      return svg(
        '<path d="M6 1 L6 11 M1 6 L11 6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>'
      );
    case "star":
      return svg(
        '<path d="M6 0.5 L7.5 4.4 L11.5 4.7 L8.4 7.2 L9.5 11.1 L6 8.9 L2.5 11.1 L3.6 7.2 L0.5 4.7 L4.5 4.4 Z" fill="currentColor"/>'
      );
    case "sparkle":
      return svg(
        '<path d="M6 0.5 L7 5 L11.5 6 L7 7 L6 11.5 L5 7 L0.5 6 L5 5 Z" fill="currentColor"/>'
      );
    case "circle":
    default:
      return svg('<circle cx="6" cy="6" r="5" fill="currentColor"/>');
  }
}
class Marker {
  /**
   * @param {import('./Tooltip').default} tooltipContext
   */
  constructor(tooltipContext) {
    this.w = tooltipContext.w;
    this.ttCtx = tooltipContext;
    this.ctx = tooltipContext.ctx;
    this.tooltipPosition = new Position(tooltipContext);
  }
  drawDynamicPoints() {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const marker = new Markers(this.w, this.ctx);
    const elsSeries = (
      /** @type {any[]} */
      [
        ...w2.dom.baseEl.querySelectorAll(".apexcharts-series")
      ]
    );
    if (w2.config.chart.stacked) {
      elsSeries.sort((a2, b2) => {
        return parseFloat(a2.getAttribute("data:realIndex")) - parseFloat(b2.getAttribute("data:realIndex"));
      });
    }
    for (let i2 = 0; i2 < elsSeries.length; i2++) {
      const pointsMain = elsSeries[i2].querySelector(
        `.apexcharts-series-markers-wrap`
      );
      if (pointsMain !== null) {
        let PointClasses = `apexcharts-marker w${(Math.random() + 1).toString(36).substring(4)}`;
        if ((w2.config.chart.type === "line" || w2.config.chart.type === "area") && !w2.globals.comboCharts && !w2.config.tooltip.intersect) {
          PointClasses += " no-pointer-events";
        }
        const elPointOptions = marker.getMarkerConfig({
          cssClass: PointClasses,
          seriesIndex: Number(pointsMain.getAttribute("data:realIndex"))
          // fixes apexcharts/apexcharts.js #1427
        });
        const point = graphics.drawMarker(0, 0, elPointOptions);
        point.node.setAttribute("default-marker-size", 0);
        const elPointsG = BrowserAPIs.createElementNS(SVGNS$2, "g");
        elPointsG.classList.add("apexcharts-series-markers");
        elPointsG.appendChild(point.node);
        pointsMain.appendChild(elPointsG);
      }
    }
  }
  /**
   * @param {any} rel
   * @param {any} point
   * @param {number | null} [x]
   * @param {number | null} [y]
   */
  enlargeCurrentPoint(rel, point, x = null, y = null) {
    const w2 = this.w;
    let appliedSize = w2.config.markers.hover.size;
    if (w2.config.chart.type !== "bubble") {
      appliedSize = this.newPointSize(rel, point);
    }
    let cx = point.getAttribute("cx");
    let cy = point.getAttribute("cy");
    if (x !== null && y !== null) {
      cx = x;
      cy = y;
    }
    this.tooltipPosition.moveXCrosshairs(cx);
    if (!this.fixedTooltip) {
      if (w2.config.chart.type === "radar") {
        const elGrid = this.ttCtx.getElGrid();
        if (!elGrid) return;
        const seriesBound = elGrid.getBoundingClientRect();
        cx = this.ttCtx.e.clientX - seriesBound.left;
      }
      this.tooltipPosition.moveTooltip(cx, cy, appliedSize);
    }
  }
  /**
   * @param {number} j
   */
  enlargePoints(j) {
    var _a, _b;
    const w2 = this.w;
    const me = this;
    const ttCtx = this.ttCtx;
    const col = j;
    const points = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-series:not(.apexcharts-series-collapsed) .apexcharts-marker"
    );
    let newSize = w2.config.markers.hover.size;
    for (let p2 = 0; p2 < points.length; p2++) {
      const rel = points[p2].getAttribute("rel");
      const index = points[p2].getAttribute("index");
      if (newSize === void 0) {
        newSize = w2.globals.markers.size[
          /** @type {any} */
          index
        ] + w2.config.markers.hover.sizeOffset;
      }
      if (col === parseInt(rel != null ? rel : "0", 10)) {
        me.newPointSize(col, points[p2]);
        const cx = (_a = points[p2].getAttribute("cx")) != null ? _a : "0";
        const cy = (_b = points[p2].getAttribute("cy")) != null ? _b : "0";
        me.tooltipPosition.moveXCrosshairs(parseFloat(cx));
        if (!ttCtx.fixedTooltip) {
          me.tooltipPosition.moveTooltip(
            parseFloat(cx),
            parseFloat(cy),
            newSize
          );
        }
      } else {
        me.oldPointSize(points[p2]);
      }
    }
  }
  /**
   * Resizes the hovered marker to its hover size and returns the size applied,
   * so the caller can position the tooltip clear of the enlarged dot. Undefined
   * when nothing was resized (a zero-size marker has nothing to clear).
   * @param {any} rel
   * @param {any} point
   * @returns {number | undefined}
   */
  newPointSize(rel, point) {
    const w2 = this.w;
    let newSize = w2.config.markers.hover.size;
    const elPoint = rel === 0 ? point.parentNode.firstChild : point.parentNode.lastChild;
    if (elPoint.getAttribute("default-marker-size") !== "0") {
      const index = parseInt(elPoint.getAttribute("index"), 10);
      if (newSize === void 0) {
        newSize = w2.globals.markers.size[index] + w2.config.markers.hover.sizeOffset;
      }
      if (newSize < 0) {
        newSize = 0;
      }
      const path = this.ttCtx.tooltipUtil.getPathFromPoint(point, newSize);
      point.setAttribute("d", path);
      return newSize;
    }
    return void 0;
  }
  /**
   * @param {any} point
   */
  oldPointSize(point) {
    const size = parseFloat(point.getAttribute("default-marker-size"));
    const path = this.ttCtx.tooltipUtil.getPathFromPoint(point, size);
    point.setAttribute("d", path);
  }
  resetPointsSize() {
    var _a;
    const w2 = this.w;
    const points = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-series:not(.apexcharts-series-collapsed) .apexcharts-marker"
    );
    for (let p2 = 0; p2 < points.length; p2++) {
      const size = parseFloat(
        (_a = points[p2].getAttribute("default-marker-size")) != null ? _a : "0"
      );
      if (Utils$1.isNumber(size) && size > 0) {
        const path = this.ttCtx.tooltipUtil.getPathFromPoint(points[p2], size);
        points[p2].setAttribute("d", path);
      } else {
        points[p2].setAttribute("d", "M0,0");
      }
    }
  }
}
class Intersect {
  /**
   * @param {import('./Tooltip').default} tooltipContext
   */
  constructor(tooltipContext) {
    this.w = tooltipContext.w;
    const w2 = this.w;
    this.ttCtx = tooltipContext;
    this.isVerticalGroupedRangeBar = !w2.globals.isBarHorizontal && w2.config.chart.type === "rangeBar" && w2.config.plotOptions.bar.rangeBarGroupRows;
  }
  // a helper function to get an element's attribute value
  /**
   * @param {Event} e
   * @param {string} attr
   */
  getAttr(e2, attr) {
    var _a;
    return parseFloat(
      /** @type {Element} */
      (_a = e2.target.getAttribute(attr)) != null ? _a : ""
    );
  }
  // handle tooltip for heatmaps and treemaps
  /** @param {{e: any, opt: any, x: any, y: any, type: any}} opts */
  handleHeatTreeTooltip({ e: e2, opt, x, y, type }) {
    var _a, _b;
    const ttCtx = this.ttCtx;
    const w2 = this.w;
    const renderer = w2.globals.activeRenderer;
    const canvasCells = type === "heatmap" && renderer && renderer.kind === "canvas" && typeof renderer.hitTest === "function";
    let i2, j, cx, cy, width, height;
    if (canvasCells) {
      const seriesBound = opt.elGrid.getBoundingClientRect();
      const clientX = e2.type === "touchmove" ? e2.touches[0].clientX : e2.clientX;
      const clientY = e2.type === "touchmove" ? e2.touches[0].clientY : e2.clientY;
      const hit = renderer.hitTest(
        clientX - seriesBound.left,
        clientY - seriesBound.top
      );
      if (!hit) {
        return { x, y, noHit: true };
      }
      i2 = hit.seriesIndex;
      j = hit.dataPointIndex;
      cx = hit.x;
      cy = hit.y;
      width = hit.width;
      height = hit.height;
    } else if (e2.target.classList.contains(`apexcharts-${type}-rect`)) {
      i2 = this.getAttr(e2, "i");
      j = this.getAttr(e2, "j");
      cx = this.getAttr(e2, "cx");
      cy = this.getAttr(e2, "cy");
      width = this.getAttr(e2, "width");
      height = this.getAttr(e2, "height");
    } else {
      return { x, y };
    }
    ttCtx.tooltipLabels.drawSeriesTexts({
      ttItems: opt.ttItems,
      i: i2,
      j,
      shared: false,
      e: e2
    });
    w2.interact.capturedSeriesIndex = i2;
    w2.interact.capturedDataPointIndex = j;
    ttCtx.tooltipPosition.moveXCrosshairs(cx + width / 2);
    const tooltipEl = ttCtx.getElTooltip();
    if (type === "heatmap" && w2.config.tooltip.arrow && !w2.config.tooltip.followCursor && tooltipEl) {
      const elGridRect = opt.elGrid.getBoundingClientRect();
      const elWrapRect = w2.dom.elWrap.getBoundingClientRect();
      const gridOffsetXInElWrap = elGridRect.left - elWrapRect.left;
      let clLeft, clTop, clRight, clBottom;
      if (canvasCells) {
        clLeft = cx;
        clTop = cy;
        clRight = cx + width;
        clBottom = cy + height;
      } else {
        const r2 = e2.target.getBoundingClientRect();
        clLeft = r2.left - elGridRect.left;
        clTop = r2.top - elGridRect.top;
        clRight = r2.right - elGridRect.left;
        clBottom = r2.bottom - elGridRect.top;
      }
      const ttW = ttCtx.tooltipRect.ttWidth || 0;
      const ttH = ttCtx.tooltipRect.ttHeight || 0;
      const cellCenterXInElWrap = (clLeft + clRight) / 2 + gridOffsetXInElWrap;
      const cellTopInElWrap = clTop + w2.layout.translateY;
      const cellBottomInElWrap = clBottom + w2.layout.translateY;
      const gridTop = w2.layout.translateY;
      const gridBottom = w2.layout.translateY + w2.layout.gridHeight;
      const gridLeft = gridOffsetXInElWrap;
      const gridRight = gridOffsetXInElWrap + w2.layout.gridWidth;
      let placement = "top";
      let finalY = cellTopInElWrap - ttH - ARROW_TIP_OVERHANG;
      if (finalY < gridTop) {
        const belowTop = cellBottomInElWrap + ARROW_TIP_OVERHANG;
        if (belowTop + ttH <= gridBottom) {
          placement = "bottom";
          finalY = belowTop;
        } else {
          finalY = gridTop;
        }
      }
      let finalX = cellCenterXInElWrap - ttW / 2;
      if (finalX < gridLeft) finalX = gridLeft;
      if (finalX + ttW > gridRight) finalX = gridRight - ttW;
      const arrowX = Math.max(10, Math.min(ttW - 10, cellCenterXInElWrap - finalX));
      ttCtx.tooltipPosition.applyTooltipPosition(tooltipEl, {
        x: finalX,
        y: finalY,
        placement,
        arrowY: null,
        arrowX
      });
      return { x: finalX, y: finalY, positioned: true };
    }
    x = cx + ttCtx.tooltipRect.ttWidth / 2 + width;
    y = cy + ttCtx.tooltipRect.ttHeight / 2 - height / 2;
    if (x > w2.layout.gridWidth / 2) {
      x = cx - ttCtx.tooltipRect.ttWidth / 2 + width;
    }
    if (ttCtx.w.config.tooltip.followCursor) {
      const seriesBound = w2.dom.elWrap.getBoundingClientRect();
      x = ((_a = w2.interact.clientX) != null ? _a : 0) - seriesBound.left - (x > w2.layout.gridWidth / 2 ? ttCtx.tooltipRect.ttWidth : 0);
      y = ((_b = w2.interact.clientY) != null ? _b : 0) - seriesBound.top - (y > w2.layout.gridHeight / 2 ? ttCtx.tooltipRect.ttHeight : 0);
    }
    return {
      x,
      y
    };
  }
  /**
   * handle tooltips for line/area/scatter charts where tooltip.intersect is true
   * when user hovers over the marker directly, this function is executed
   */
  /** @param {{e: any, opt: any, x: any, y: any}} opts */
  handleMarkerTooltip({ e: e2, opt, x, y }) {
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    let i2;
    let j;
    if (e2.target.classList.contains("apexcharts-marker")) {
      const cx = parseInt(opt.paths.getAttribute("cx"), 10);
      const cy = parseInt(opt.paths.getAttribute("cy"), 10);
      const val = parseFloat(opt.paths.getAttribute("val"));
      j = parseInt(opt.paths.getAttribute("rel"), 10);
      i2 = parseInt(
        opt.paths.parentNode.parentNode.parentNode.getAttribute("rel"),
        10
      ) - 1;
      if (ttCtx.intersect) {
        const el = Utils$1.findAncestor(opt.paths, "apexcharts-series");
        if (el) {
          i2 = parseInt(el.getAttribute("data:realIndex"), 10);
        }
      }
      ttCtx.tooltipLabels.drawSeriesTexts({
        ttItems: opt.ttItems,
        i: i2,
        j,
        shared: ttCtx.showOnIntersect ? false : w2.config.tooltip.shared,
        e: e2
      });
      if (e2.type === "mouseup") {
        ttCtx.markerClick(e2, i2, j);
      }
      w2.interact.capturedSeriesIndex = i2;
      w2.interact.capturedDataPointIndex = j;
      const arrowEnabled = !!w2.config.tooltip.arrow;
      x = cx;
      if (arrowEnabled) {
        y = cy;
      } else {
        y = cy + w2.layout.translateY - ttCtx.tooltipRect.ttHeight * 1.4;
        if (val < 0) {
          y = cy;
        }
      }
      if (ttCtx.w.config.tooltip.followCursor) {
        const elGrid = ttCtx.getElGrid();
        if (!elGrid) return { x, y };
        const seriesBound = elGrid.getBoundingClientRect();
        y = ttCtx.e.clientY + w2.layout.translateY - seriesBound.top;
      }
      ttCtx.marker.enlargeCurrentPoint(j, opt.paths, x, y);
    }
    return {
      x,
      y
    };
  }
  /**
   * handle tooltips for bar/column charts
   */
  /** @param {{e: any, opt: any}} opts */
  handleBarTooltip({ e: e2, opt }) {
    var _a, _b, _c;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    const tooltipEl = ttCtx.getElTooltip();
    let bx = 0;
    let x = 0;
    let y = 0;
    let i2 = 0;
    let strokeWidth;
    const barXY = this.getBarTooltipXY({
      e: e2,
      opt
    });
    if (barXY.j === null && barXY.barHeight === 0 && barXY.barWidth === 0) {
      return;
    }
    i2 = barXY.i;
    const j = barXY.j;
    w2.interact.capturedSeriesIndex = i2;
    w2.interact.capturedDataPointIndex = j !== null ? j : w2.interact.capturedDataPointIndex;
    if (w2.globals.isBarHorizontal && ttCtx.tooltipUtil.hasBars() || !w2.config.tooltip.shared) {
      x = barXY.x;
      y = barXY.y;
      strokeWidth = Array.isArray(w2.config.stroke.width) ? w2.config.stroke.width[i2] : w2.config.stroke.width;
      bx = x;
    } else {
      if (!w2.globals.comboCharts && !w2.config.tooltip.shared) {
        bx = bx / 2;
      }
    }
    if (isNaN(y)) {
      y = w2.globals.svgHeight - ttCtx.tooltipRect.ttHeight;
    }
    if (x + ttCtx.tooltipRect.ttWidth > w2.layout.gridWidth) {
      x = x - ttCtx.tooltipRect.ttWidth;
    } else if (x < 0) {
      x = 0;
    }
    if (ttCtx.w.config.tooltip.followCursor) {
      const elGrid = ttCtx.getElGrid();
      if (!elGrid) return;
    }
    if (ttCtx.tooltip === null) {
      ttCtx.tooltip = w2.dom.baseEl.querySelector(
        ".apexcharts-tooltip:not(.apexcharts-annotation-tooltip)"
      );
    }
    if (!w2.config.tooltip.shared) {
      if (w2.globals.comboBarCount > 0) {
        ttCtx.tooltipPosition.moveXCrosshairs(bx + strokeWidth / 2);
      } else {
        ttCtx.tooltipPosition.moveXCrosshairs(bx);
      }
    }
    if (!ttCtx.fixedTooltip && (!w2.config.tooltip.shared || w2.globals.isBarHorizontal && ttCtx.tooltipUtil.hasBars())) {
      y = y + w2.layout.translateY - ttCtx.tooltipRect.ttHeight / 2;
      if (tooltipEl) {
        const ttW = ttCtx.tooltipRect.ttWidth || 0;
        const ttH = ttCtx.tooltipRect.ttHeight || 0;
        const arrowEnabled = !!w2.config.tooltip.arrow;
        const { barAnchorXInGrid, barAnchorYInGrid, barRectInGrid } = barXY;
        const elGridRect = (_a = ttCtx.getElGrid()) == null ? void 0 : _a.getBoundingClientRect();
        const elWrapRect = w2.dom.elWrap.getBoundingClientRect();
        const gridOffsetXInElWrap = elGridRect ? elGridRect.left - elWrapRect.left : w2.layout.translateX;
        let placement;
        let arrowY = null;
        let arrowX = null;
        let finalX = x + gridOffsetXInElWrap;
        let finalY = y;
        if (arrowEnabled && w2.globals.isBarHorizontal && barRectInGrid != null) {
          const gridTop = w2.layout.translateY;
          const gridBottom = w2.layout.translateY + w2.layout.gridHeight;
          const gridLeft = gridOffsetXInElWrap;
          const gridRight = gridOffsetXInElWrap + w2.layout.gridWidth;
          const barCenterXInElWrap = (barRectInGrid.left + barRectInGrid.right) / 2 + gridOffsetXInElWrap;
          const barTopInElWrap = barRectInGrid.top + w2.layout.translateY;
          const barBottomInElWrap = barRectInGrid.bottom + w2.layout.translateY;
          let proposedTop = barTopInElWrap - ttH - ARROW_TIP_OVERHANG;
          placement = "top";
          if (proposedTop < gridTop) {
            const belowTop = barBottomInElWrap + ARROW_TIP_OVERHANG;
            if (belowTop + ttH <= gridBottom) {
              placement = "bottom";
              proposedTop = belowTop;
            }
          }
          finalY = proposedTop;
          finalX = barCenterXInElWrap - ttW / 2;
          if (finalX < gridLeft) finalX = gridLeft;
          if (finalX + ttW > gridRight) finalX = gridRight - ttW;
          arrowX = Math.max(
            10,
            Math.min(ttW - 10, barCenterXInElWrap - finalX)
          );
        } else if (arrowEnabled && barAnchorXInGrid != null && barAnchorYInGrid != null) {
          const barCenterXInElWrap = barAnchorXInGrid + gridOffsetXInElWrap;
          const gridCenterXInElWrap = gridOffsetXInElWrap + w2.layout.gridWidth / 2;
          const barLeftInElWrap = ((_b = barRectInGrid == null ? void 0 : barRectInGrid.left) != null ? _b : barAnchorXInGrid) + gridOffsetXInElWrap;
          const barRightInElWrap = ((_c = barRectInGrid == null ? void 0 : barRectInGrid.right) != null ? _c : barAnchorXInGrid) + gridOffsetXInElWrap;
          if (barCenterXInElWrap < gridCenterXInElWrap) {
            placement = "right";
            finalX = barRightInElWrap + ARROW_TIP_OVERHANG;
          } else {
            placement = "left";
            finalX = barLeftInElWrap - ttW - ARROW_TIP_OVERHANG;
          }
          if (barRectInGrid) {
            const barCenterYInElWrap = (barRectInGrid.top + barRectInGrid.bottom) / 2 + w2.layout.translateY;
            finalY = barCenterYInElWrap - ttH / 2;
            const gridTop = w2.layout.translateY;
            const gridBottom = w2.layout.translateY + w2.layout.gridHeight;
            if (finalY < gridTop) finalY = gridTop;
            if (finalY + ttH > gridBottom) finalY = gridBottom - ttH;
          }
          if (ttH > 0 && barRectInGrid) {
            const barCenterYInElWrap = (barRectInGrid.top + barRectInGrid.bottom) / 2 + w2.layout.translateY;
            arrowY = Math.max(
              10,
              Math.min(ttH - 10, barCenterYInElWrap - finalY)
            );
          }
        }
        ttCtx.tooltipPosition.applyTooltipPosition(tooltipEl, {
          x: finalX,
          y: finalY,
          placement,
          arrowY,
          arrowX
        });
      }
    }
  }
  /** @param {{e: any, opt: any}} opts */
  getBarTooltipXY({ e: e2, opt }) {
    const w2 = this.w;
    let j = null;
    const ttCtx = this.ttCtx;
    let i2 = 0;
    let x = 0;
    let y = 0;
    let barWidth = 0;
    let barHeight = 0;
    let barCx = null;
    let barCy = null;
    let barAnchorXInGrid = null;
    let barAnchorYInGrid = null;
    let barRectInGrid = null;
    const cl = e2.target.classList;
    if (cl.contains("apexcharts-bar-area") || cl.contains("apexcharts-candlestick-area") || cl.contains("apexcharts-boxPlot-area") || cl.contains("apexcharts-rangebar-area")) {
      const bar = e2.target;
      const barRect = bar.getBoundingClientRect();
      const seriesBound = opt.elGrid.getBoundingClientRect();
      const bh = barRect.height;
      barHeight = barRect.height;
      const bw = barRect.width;
      const cx = parseInt(bar.getAttribute("cx"), 10);
      const cy = parseInt(bar.getAttribute("cy"), 10);
      barCx = cx;
      barCy = cy;
      barWidth = parseFloat(bar.getAttribute("barWidth"));
      const rectLeftInGrid = barRect.left - seriesBound.left;
      const rectTopInGrid = barRect.top - seriesBound.top;
      const rectCenterXInGrid = rectLeftInGrid + bw / 2;
      const rectCenterYInGrid = rectTopInGrid + bh / 2;
      barAnchorXInGrid = rectCenterXInGrid;
      barAnchorYInGrid = w2.globals.isBarHorizontal ? rectCenterYInGrid : rectTopInGrid;
      barRectInGrid = {
        left: rectLeftInGrid,
        top: rectTopInGrid,
        right: rectLeftInGrid + bw,
        bottom: rectTopInGrid + bh
      };
      const clientX = e2.type === "touchmove" ? e2.touches[0].clientX : e2.clientX;
      j = parseInt(bar.getAttribute("j"), 10);
      i2 = parseInt(bar.parentNode.getAttribute("rel"), 10) - 1;
      const y1 = bar.getAttribute("data-range-y1");
      const y2 = bar.getAttribute("data-range-y2");
      if (w2.globals.comboCharts) {
        i2 = parseInt(bar.parentNode.getAttribute("data:realIndex"), 10);
      }
      const handleXForColumns = (x2) => {
        if (w2.axisFlags.isXNumeric) {
          x2 = cx - bw / 2;
        } else {
          if (this.isVerticalGroupedRangeBar) {
            x2 = cx + bw / 2;
          } else {
            x2 = cx - ttCtx.dataPointsDividedWidth + bw / 2;
          }
        }
        return x2;
      };
      const handleYForBars = () => {
        return cy - ttCtx.dataPointsDividedHeight + bh / 2 - ttCtx.tooltipRect.ttHeight / 2;
      };
      ttCtx.tooltipLabels.drawSeriesTexts({
        ttItems: opt.ttItems,
        i: i2,
        j,
        y1: y1 ? parseInt(y1, 10) : null,
        y2: y2 ? parseInt(y2, 10) : null,
        shared: ttCtx.showOnIntersect ? false : w2.config.tooltip.shared,
        e: e2
      });
      if (w2.config.tooltip.followCursor) {
        if (w2.globals.isBarHorizontal) {
          x = clientX - seriesBound.left + 15;
          y = handleYForBars();
        } else {
          x = handleXForColumns(x);
          y = e2.clientY - seriesBound.top - ttCtx.tooltipRect.ttHeight / 2 - 15;
        }
      } else {
        if (w2.globals.isBarHorizontal) {
          x = cx;
          if (ttCtx.xyRatios && x < ttCtx.xyRatios.baseLineInvertedY) {
            x = cx - ttCtx.tooltipRect.ttWidth;
          }
          y = handleYForBars();
        } else {
          x = handleXForColumns(x);
          y = cy;
        }
      }
    }
    return {
      x,
      y,
      barHeight,
      barWidth,
      i: i2,
      j,
      // SVG attribute values — left for any caller that still wants them.
      barCx,
      barCy,
      // Arrow anchor in grid-local coords (rect-derived; column→top,
      // horizontal→center). Used by handleBarTooltip to place the arrow
      // exactly on the bar's data point.
      barAnchorXInGrid,
      barAnchorYInGrid,
      // Full rendered bar rect (grid-local). Used for top/bottom
      // placement and flip-on-overflow detection.
      barRectInGrid
    };
  }
}
class AxesTooltip {
  /**
   * @param {import('./Tooltip').default} tooltipContext
   */
  constructor(tooltipContext) {
    this.w = tooltipContext.w;
    this.ttCtx = tooltipContext;
  }
  /**
   * This method adds the secondary tooltip which appears below x axis
   * @memberof Tooltip
   **/
  drawXaxisTooltip() {
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    const isBottom = w2.config.xaxis.position === "bottom";
    ttCtx.xaxisOffY = isBottom ? w2.layout.gridHeight + 1 : -w2.layout.xAxisHeight - w2.config.xaxis.axisTicks.height + 3;
    const tooltipCssClass = isBottom ? "apexcharts-xaxistooltip apexcharts-xaxistooltip-bottom" : "apexcharts-xaxistooltip apexcharts-xaxistooltip-top";
    const renderTo = w2.dom.elWrap;
    if (ttCtx.isXAxisTooltipEnabled) {
      const xaxisTooltip = w2.dom.baseEl.querySelector(
        ".apexcharts-xaxistooltip"
      );
      if (xaxisTooltip === null) {
        ttCtx.xaxisTooltip = BrowserAPIs.createElementNS(
          "http://www.w3.org/1999/xhtml",
          "div"
        );
        ttCtx.xaxisTooltip.setAttribute(
          "class",
          tooltipCssClass + " apexcharts-theme-" + w2.config.tooltip.theme
        );
        renderTo.appendChild(ttCtx.xaxisTooltip);
        ttCtx.xaxisTooltipText = BrowserAPIs.createElementNS(
          "http://www.w3.org/1999/xhtml",
          "div"
        );
        ttCtx.xaxisTooltipText.classList.add("apexcharts-xaxistooltip-text");
        ttCtx.xaxisTooltipText.style.fontFamily = w2.config.xaxis.tooltip.style.fontFamily || w2.config.chart.fontFamily;
        ttCtx.xaxisTooltipText.style.fontSize = w2.config.xaxis.tooltip.style.fontSize;
        ttCtx.xaxisTooltip.appendChild(ttCtx.xaxisTooltipText);
      }
    }
  }
  /**
   * This method adds the secondary tooltip which appears below x axis
   * @memberof Tooltip
   **/
  drawYaxisTooltip() {
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    for (let i2 = 0; i2 < w2.config.yaxis.length; i2++) {
      const isRight = w2.config.yaxis[i2].opposite || w2.config.yaxis[i2].crosshairs.opposite;
      ttCtx.yaxisOffX = isRight ? w2.layout.gridWidth + 1 : 1;
      const tooltipCssClass = isRight ? `apexcharts-yaxistooltip apexcharts-yaxistooltip-${i2} apexcharts-yaxistooltip-right` : `apexcharts-yaxistooltip apexcharts-yaxistooltip-${i2} apexcharts-yaxistooltip-left`;
      const renderTo = w2.dom.elWrap;
      const yaxisTooltip = w2.dom.baseEl.querySelector(
        `.apexcharts-yaxistooltip.apexcharts-yaxistooltip-${i2}`
      );
      if (yaxisTooltip === null) {
        ttCtx.yaxisTooltip = BrowserAPIs.createElementNS(
          "http://www.w3.org/1999/xhtml",
          "div"
        );
        ttCtx.yaxisTooltip.setAttribute(
          "class",
          tooltipCssClass + " apexcharts-theme-" + w2.config.tooltip.theme
        );
        renderTo.appendChild(ttCtx.yaxisTooltip);
        if (i2 === 0) ttCtx.yaxisTooltipText = [];
        ttCtx.yaxisTooltipText[i2] = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
        ttCtx.yaxisTooltipText[i2].classList.add(
          "apexcharts-yaxistooltip-text"
        );
        ttCtx.yaxisTooltip.appendChild(
          /** @type {any} */
          ttCtx.yaxisTooltipText[i2]
        );
      }
    }
  }
  /**
   * @memberof Tooltip
   **/
  setXCrosshairWidth() {
    var _a, _b;
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    const xcrosshairs = ttCtx.getElXCrosshairs();
    ttCtx.xcrosshairsWidth = parseInt(w2.config.xaxis.crosshairs.width, 10);
    if (!w2.globals.comboCharts) {
      if (w2.config.xaxis.crosshairs.width === "tickWidth") {
        const count = w2.labelData.labels.length;
        ttCtx.xcrosshairsWidth = w2.layout.gridWidth / count;
      } else if (w2.config.xaxis.crosshairs.width === "barWidth") {
        const bar = w2.dom.baseEl.querySelector(".apexcharts-bar-area");
        if (bar !== null) {
          const barWidth = parseFloat((_a = bar.getAttribute("barWidth")) != null ? _a : "0");
          ttCtx.xcrosshairsWidth = barWidth;
        } else {
          ttCtx.xcrosshairsWidth = 1;
        }
      }
    } else {
      const bar = w2.dom.baseEl.querySelector(".apexcharts-bar-area");
      if (bar !== null && w2.config.xaxis.crosshairs.width === "barWidth") {
        const barWidth = parseFloat((_b = bar.getAttribute("barWidth")) != null ? _b : "0");
        ttCtx.xcrosshairsWidth = barWidth;
      } else {
        if (w2.config.xaxis.crosshairs.width === "tickWidth") {
          const count = w2.labelData.labels.length;
          ttCtx.xcrosshairsWidth = w2.layout.gridWidth / count;
        }
      }
    }
    if (w2.globals.isBarHorizontal) {
      ttCtx.xcrosshairsWidth = 0;
    }
    if (xcrosshairs !== null && ttCtx.xcrosshairsWidth > 0) {
      xcrosshairs.setAttribute("width", String(ttCtx.xcrosshairsWidth));
    }
  }
  handleYCrosshair() {
    const w2 = this.w;
    const ttCtx = this.ttCtx;
    ttCtx.ycrosshairs = w2.dom.baseEl.querySelector(".apexcharts-ycrosshairs");
    ttCtx.ycrosshairsHidden = w2.dom.baseEl.querySelector(
      ".apexcharts-ycrosshairs-hidden"
    );
  }
  /**
   * @param {number} index
   * @param {number} clientY
   * @param {import('../../types/internal').XYRatios} xyRatios
   */
  drawYaxisTooltipText(index, clientY, xyRatios) {
    const ttCtx = this.ttCtx;
    const w2 = this.w;
    const gl = w2.globals;
    const yAxisSeriesArr = gl.seriesYAxisMap[index];
    if (ttCtx.yaxisTooltips[index] && yAxisSeriesArr.length > 0) {
      const lbFormatter = w2.formatters.yLabelFormatters[index];
      const elGrid = ttCtx.getElGrid();
      if (!elGrid) return;
      const seriesBound = elGrid.getBoundingClientRect();
      const seriesIndex = yAxisSeriesArr[0];
      let translationsIndex = 0;
      if (xyRatios.yRatio.length > 1) {
        translationsIndex = seriesIndex;
      }
      const hoverY = (clientY - seriesBound.top) * xyRatios.yRatio[translationsIndex];
      const height = gl.maxYArr[seriesIndex] - gl.minYArr[seriesIndex];
      let val = gl.minYArr[seriesIndex] + (height - hoverY);
      if (w2.config.yaxis[index].reversed) {
        val = gl.maxYArr[seriesIndex] - (height - hoverY);
      }
      ttCtx.tooltipPosition.moveYCrosshairs(clientY - seriesBound.top);
      ttCtx.yaxisTooltipText[index].innerHTML = lbFormatter(val);
      ttCtx.tooltipPosition.moveYAxisTooltip(index);
    }
  }
}
class Tooltip {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.tConfig = w2.config.tooltip;
    this.tooltipUtil = new Utils2(this);
    this.tooltipLabels = new Labels(this);
    this.tooltipPosition = new Position(this);
    this.marker = new Marker(this);
    this.intersect = new Intersect(this);
    this.axesTooltip = new AxesTooltip(this);
    this.showOnIntersect = this.tConfig.intersect;
    this.showTooltipTitle = this.tConfig.x.show;
    this.fixedTooltip = this.tConfig.fixed.enabled;
    this.xaxisTooltip = null;
    this.xaxisTooltipText = null;
    this.yaxisTooltip = null;
    this.yaxisTooltipText = null;
    this.yaxisTTEls = null;
    this.xaxisOffY = 0;
    this.yaxisOffX = 0;
    this.xcrosshairsWidth = 0;
    this.ycrosshairs = null;
    this.ycrosshairsHidden = null;
    this.tooltip = null;
    this.e = null;
    this.isBarShared = !w2.globals.isBarHorizontal && this.tConfig.shared;
    this.lastHoverTime = Date.now();
    this.dimensionUpdateScheduled = false;
    this.xyRatios = null;
    this.isXAxisTooltipEnabled = false;
    this.yaxisTooltips = [];
    this.allTooltipSeriesGroups = [];
    this.xAxisTicksPositions = null;
    this.dataPointsDividedHeight = 0;
    this.dataPointsDividedWidth = 0;
    this.tooltipTitle = null;
    this.legendLabels = null;
    this.ttItems = null;
    this.seriesBound = null;
    this.seriesHoverTimeout = void 0;
    this.clientX = 0;
    this.clientY = 0;
    this.barSeriesHeight = 0;
    this.tooltipRect = { x: 0, y: 0, ttWidth: 0, ttHeight: 0 };
  }
  setupDimensionCache() {
    const w2 = this.w;
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return;
    this.updateDimensionCache();
    if (typeof ResizeObserver !== "undefined" && !w2.globals.resizeObserver) {
      w2.globals.resizeObserver = new ResizeObserver(() => {
        if (!this.dimensionUpdateScheduled) {
          this.dimensionUpdateScheduled = true;
          requestAnimationFrame(() => {
            this.updateDimensionCache();
            this.dimensionUpdateScheduled = false;
          });
        }
      });
      w2.globals.resizeObserver.observe(tooltipEl);
    }
  }
  updateDimensionCache() {
    const w2 = this.w;
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return;
    const rect = tooltipEl.getBoundingClientRect();
    w2.globals.dimensionCache.tooltip = /** @type {any} */
    {
      width: rect.width,
      height: rect.height,
      lastUpdate: Date.now()
    };
  }
  getCachedDimensions() {
    const w2 = this.w;
    if (w2.globals.dimensionCache.tooltip) {
      const cache2 = (
        /** @type {Record<string,any>} */
        w2.globals.dimensionCache.tooltip
      );
      const age = Date.now() - cache2.lastUpdate;
      if (age < 1e3) {
        return {
          ttWidth: cache2.width,
          ttHeight: cache2.height
        };
      }
    }
    this.updateDimensionCache();
    const cache = (
      /** @type {Record<string,any>} */
      w2.globals.dimensionCache.tooltip
    );
    return cache ? {
      ttWidth: cache.width,
      ttHeight: cache.height
    } : { ttWidth: 0, ttHeight: 0 };
  }
  /**
   * @param {{ w: import('../../types/internal').ChartStateW }} [ctx]
   * @returns {HTMLElement | null}
   */
  getElTooltip(ctx) {
    if (!ctx) ctx = this;
    if (!ctx.w.dom.baseEl) return null;
    return (
      /** @type {HTMLElement | null} */
      ctx.w.dom.baseEl.querySelector(
        ".apexcharts-tooltip:not(.apexcharts-annotation-tooltip)"
      )
    );
  }
  getElXCrosshairs() {
    return this.w.dom.baseEl.querySelector(".apexcharts-xcrosshairs");
  }
  getElGrid() {
    return this.w.dom.baseEl.querySelector(".apexcharts-grid");
  }
  /**
   * @param {import('../../types/internal').XYRatios} xyRatios
   */
  drawTooltip(xyRatios) {
    const w2 = this.w;
    this.xyRatios = xyRatios;
    this.isXAxisTooltipEnabled = w2.config.xaxis.tooltip.enabled && w2.globals.axisCharts;
    this.yaxisTooltips = w2.config.yaxis.map((y) => {
      return y.show && y.tooltip.enabled && w2.globals.axisCharts ? true : false;
    });
    this.allTooltipSeriesGroups = [];
    if (!w2.globals.axisCharts) {
      this.showTooltipTitle = false;
    }
    const existingTooltip = this.getElTooltip();
    if (existingTooltip == null ? void 0 : existingTooltip.parentNode) {
      existingTooltip.parentNode.removeChild(existingTooltip);
    }
    this.tooltipTitle = null;
    const tooltipEl = BrowserAPIs.createElementNS(
      "http://www.w3.org/1999/xhtml",
      "div"
    );
    tooltipEl.classList.add("apexcharts-tooltip");
    if (w2.config.tooltip.cssClass) {
      tooltipEl.classList.add(w2.config.tooltip.cssClass);
    }
    tooltipEl.classList.add(`apexcharts-theme-${this.tConfig.theme || "light"}`);
    if (this.tConfig.fillSeriesColor) {
      tooltipEl.classList.add("apexcharts-tooltip-fill-series");
    }
    if (this.tConfig.style && this.tConfig.style.background) {
      tooltipEl.style.setProperty(
        "--apx-tt-bg",
        this.tConfig.style.background
      );
    }
    const isSharedMulti = this.tConfig.shared && w2.config.series.length > 1 && !w2.globals.isBarHorizontal && w2.config.chart.type !== "heatmap";
    const shouldDrawArrow = this.tConfig.arrow && !this.tConfig.followCursor && !this.tConfig.fixed.enabled && !isSharedMulti && !this.tConfig.fillSeriesColor && w2.globals.axisCharts;
    if (shouldDrawArrow) {
      const arrowEl = BrowserAPIs.createElementNS(
        "http://www.w3.org/1999/xhtml",
        "div"
      );
      arrowEl.classList.add("apexcharts-tooltip-arrow");
      tooltipEl.appendChild(arrowEl);
    }
    if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.announcements.enabled) {
      tooltipEl.setAttribute("role", "tooltip");
      tooltipEl.setAttribute("aria-live", "polite");
      tooltipEl.setAttribute("aria-atomic", "true");
      tooltipEl.setAttribute("aria-hidden", "true");
    }
    w2.dom.elWrap.appendChild(tooltipEl);
    if (w2.globals.axisCharts) {
      this.axesTooltip.drawXaxisTooltip();
      this.axesTooltip.drawYaxisTooltip();
      this.axesTooltip.setXCrosshairWidth();
      this.axesTooltip.handleYCrosshair();
      const xAxis = new XAxis(this.w, this.ctx, void 0);
      this.xAxisTicksPositions = xAxis.getXAxisTicksPositions();
    }
    if ((w2.globals.comboCharts || this.tConfig.intersect || w2.config.chart.type === "rangeBar") && !this.tConfig.shared) {
      this.showOnIntersect = true;
    }
    if (w2.config.markers.size === 0 || w2.globals.markers.largestSize === 0) {
      this.marker.drawDynamicPoints();
    }
    if (w2.globals.collapsedSeries.length === w2.seriesData.series.length) return;
    this.dataPointsDividedHeight = w2.layout.gridHeight / w2.globals.dataPoints;
    this.dataPointsDividedWidth = w2.layout.gridWidth / w2.globals.dataPoints;
    if (this.showTooltipTitle) {
      this.tooltipTitle = BrowserAPIs.createElementNS(
        "http://www.w3.org/1999/xhtml",
        "div"
      );
      this.tooltipTitle.classList.add("apexcharts-tooltip-title");
      this.tooltipTitle.style.fontFamily = this.tConfig.style.fontFamily || w2.config.chart.fontFamily;
      this.tooltipTitle.style.fontSize = this.tConfig.style.fontSize;
      tooltipEl.appendChild(this.tooltipTitle);
    }
    let ttItemsCnt = w2.seriesData.series.length;
    if ((w2.globals.xyCharts || w2.globals.comboCharts) && this.tConfig.shared) {
      if (!this.showOnIntersect) {
        ttItemsCnt = w2.seriesData.series.length;
      } else {
        ttItemsCnt = 1;
      }
    }
    this.legendLabels = w2.dom.baseEl.querySelectorAll(".apexcharts-legend-text");
    this.ttItems = this.createTTElements(ttItemsCnt);
    this.addSVGEvents();
    this.setupDimensionCache();
  }
  /**
   * @param {number} ttItemsCnt
   */
  createTTElements(ttItemsCnt) {
    const w2 = this.w;
    const ttItems = [];
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return ttItems;
    for (let i2 = 0; i2 < ttItemsCnt; i2++) {
      const gTxt = BrowserAPIs.createElementNS(
        "http://www.w3.org/1999/xhtml",
        "div"
      );
      gTxt.classList.add(
        "apexcharts-tooltip-series-group",
        `apexcharts-tooltip-series-group-${i2}`
      );
      gTxt.style.order = String(
        w2.config.tooltip.inverseOrder ? ttItemsCnt - i2 : i2 + 1
      );
      const point = BrowserAPIs.createElementNS(
        "http://www.w3.org/1999/xhtml",
        "span"
      );
      point.classList.add("apexcharts-tooltip-marker");
      if (w2.config.tooltip.fillSeriesColor) {
        point.style.backgroundColor = w2.globals.colors[i2];
      } else {
        point.style.color = w2.globals.colors[i2];
      }
      const mShape = w2.config.markers.shape;
      let shape = mShape;
      if (Array.isArray(mShape)) {
        shape = mShape[i2];
      }
      point.setAttribute("shape", shape);
      point.innerHTML = renderMarkerSVG(shape);
      gTxt.appendChild(point);
      const gYZ = BrowserAPIs.createElementNS(
        "http://www.w3.org/1999/xhtml",
        "div"
      );
      gYZ.classList.add("apexcharts-tooltip-text");
      gYZ.style.fontFamily = this.tConfig.style.fontFamily || w2.config.chart.fontFamily;
      gYZ.style.fontSize = this.tConfig.style.fontSize;
      ["y", "goals", "z"].forEach((g2) => {
        const gValText = BrowserAPIs.createElementNS(
          "http://www.w3.org/1999/xhtml",
          "div"
        );
        gValText.classList.add(`apexcharts-tooltip-${g2}-group`);
        const txtLabel = BrowserAPIs.createElementNS(
          "http://www.w3.org/1999/xhtml",
          "span"
        );
        txtLabel.classList.add(`apexcharts-tooltip-text-${g2}-label`);
        gValText.appendChild(txtLabel);
        const txtValue = BrowserAPIs.createElementNS(
          "http://www.w3.org/1999/xhtml",
          "span"
        );
        txtValue.classList.add(`apexcharts-tooltip-text-${g2}-value`);
        gValText.appendChild(txtValue);
        gYZ.appendChild(gValText);
      });
      gTxt.appendChild(gYZ);
      tooltipEl.appendChild(gTxt);
      ttItems.push(gTxt);
    }
    return ttItems;
  }
  addSVGEvents() {
    const w2 = this.w;
    const type = w2.config.chart.type;
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return;
    const commonBar = !!(type === "bar" || type === "candlestick" || type === "boxPlot" || type === "violin" || type === "rangeBar");
    const chartWithmarkers = type === "area" || type === "line" || type === "scatter" || type === "bubble" || type === "radar";
    const isPolarMarkerChart = chartWithmarkers && !w2.globals.xyCharts;
    const hoverArea = w2.dom.Paper.node;
    const elGrid = this.getElGrid();
    if (elGrid) {
      this.seriesBound = elGrid.getBoundingClientRect();
    }
    const tooltipY = [];
    const tooltipX = [];
    const seriesHoverParams = {
      hoverArea,
      elGrid,
      tooltipEl,
      tooltipY,
      tooltipX,
      ttItems: this.ttItems
    };
    let points;
    if (w2.globals.axisCharts) {
      if (chartWithmarkers) {
        points = w2.dom.baseEl.querySelectorAll(
          ".apexcharts-series[data\\:longestSeries='true'] .apexcharts-marker"
        );
      } else if (commonBar) {
        points = w2.dom.baseEl.querySelectorAll(
          ".apexcharts-series .apexcharts-bar-area, .apexcharts-series .apexcharts-candlestick-area, .apexcharts-series .apexcharts-boxPlot-area, .apexcharts-series .apexcharts-violin-area, .apexcharts-series .apexcharts-rangebar-area"
        );
      } else if (type === "heatmap" || type === "treemap") {
        points = w2.dom.baseEl.querySelectorAll(
          ".apexcharts-series .apexcharts-heatmap, .apexcharts-series .apexcharts-treemap"
        );
      }
      if (points && points.length) {
        for (let p2 = 0; p2 < points.length; p2++) {
          tooltipY.push(points[p2].getAttribute("cy"));
          tooltipX.push(points[p2].getAttribute("cx"));
        }
      }
    }
    const validSharedChartTypes = w2.globals.xyCharts && !this.showOnIntersect || w2.globals.comboCharts && !this.showOnIntersect || commonBar && this.tooltipUtil.hasBars() && this.tConfig.shared;
    if (validSharedChartTypes) {
      this.addPathsEventListeners([hoverArea], seriesHoverParams);
    } else if (commonBar && !w2.globals.comboCharts || chartWithmarkers && this.showOnIntersect || isPolarMarkerChart) {
      this.addDatapointEventsListeners(seriesHoverParams);
    } else if (type === "heatmap" && w2.globals.activeRenderer && w2.globals.activeRenderer.kind === "canvas") {
      this.addPathsEventListeners([hoverArea], seriesHoverParams);
    } else if (!w2.globals.axisCharts || type === "heatmap" || type === "treemap") {
      const seriesAll = w2.dom.baseEl.querySelectorAll(".apexcharts-series");
      this.addPathsEventListeners(seriesAll, seriesHoverParams);
    }
    if (this.showOnIntersect) {
      const lineAreaPoints = w2.dom.baseEl.querySelectorAll(
        ".apexcharts-line-series .apexcharts-marker, .apexcharts-area-series .apexcharts-marker"
      );
      if (lineAreaPoints.length > 0) {
        this.addPathsEventListeners(lineAreaPoints, seriesHoverParams);
      }
      if (this.tooltipUtil.hasBars() && !this.tConfig.shared) {
        this.addDatapointEventsListeners(seriesHoverParams);
      }
    }
  }
  drawFixedTooltipRect() {
    const w2 = this.w;
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return { x: 0, y: 0, ttWidth: 0, ttHeight: 0 };
    const tooltipRect = tooltipEl.getBoundingClientRect();
    const ttWidth = tooltipRect.width + 10;
    const ttHeight = tooltipRect.height + 10;
    let x = this.tConfig.fixed.offsetX;
    let y = this.tConfig.fixed.offsetY;
    const fixed = this.tConfig.fixed.position.toLowerCase();
    if (fixed.indexOf("right") > -1) {
      x = x + w2.globals.svgWidth - ttWidth + 10;
    }
    if (fixed.indexOf("bottom") > -1) {
      y = y + w2.globals.svgHeight - ttHeight - 10;
    }
    this.tooltipPosition.applyTooltipPosition(tooltipEl, { x, y });
    return {
      x,
      y,
      ttWidth,
      ttHeight
    };
  }
  /**
   * @param {Record<string, any>} seriesHoverParams
   */
  addDatapointEventsListeners(seriesHoverParams) {
    const w2 = this.w;
    const points = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-series-markers .apexcharts-marker, .apexcharts-bar-area, .apexcharts-candlestick-area, .apexcharts-boxPlot-area, .apexcharts-rangebar-area"
    );
    this.addPathsEventListeners(points, seriesHoverParams);
  }
  /**
   * @param {any} paths
   * @param {Record<string, any>} opts
   */
  addPathsEventListeners(paths, opts) {
    const self = this;
    for (let p2 = 0; p2 < paths.length; p2++) {
      const extendedOpts = {
        paths: paths[p2],
        tooltipEl: opts.tooltipEl,
        tooltipY: opts.tooltipY,
        tooltipX: opts.tooltipX,
        elGrid: opts.elGrid,
        hoverArea: opts.hoverArea,
        ttItems: opts.ttItems
      };
      const events = [
        "mousemove",
        "mouseup",
        "touchmove",
        "mouseout",
        "touchend"
      ];
      events.map((ev) => {
        return paths[p2].addEventListener(
          ev,
          self.onSeriesHover.bind(self, extendedOpts),
          { capture: false, passive: true }
        );
      });
    }
  }
  /*
   ** Check to see if the tooltips should be updated based on a mouse / touch event
   * @param {Record<string, any>} opt
   * @param {Event} e
   */
  /** @param {Record<string, any>} opt @param {any} e */
  onSeriesHover(opt, e2) {
    const targetDelay = 20;
    const timeSinceLastUpdate = Date.now() - this.lastHoverTime;
    if (timeSinceLastUpdate >= targetDelay) {
      this.seriesHover(opt, e2);
    } else {
      clearTimeout(this.seriesHoverTimeout);
      this.seriesHoverTimeout = setTimeout(() => {
        this.seriesHover(opt, e2);
      }, targetDelay - timeSinceLastUpdate);
    }
  }
  /*
   ** The actual series hover function
   * @param {Record<string, any>} opt
   * @param {Event} e
   */
  /** @param {Record<string, any>} opt @param {any} e */
  seriesHover(opt, e2) {
    if (this.w.globals.isDestroyed) return;
    this.lastHoverTime = Date.now();
    let chartGroups = [];
    const w2 = this.w;
    const isCfMember = (chart) => {
      var _a, _b, _c;
      const link = (_c = (_b = (_a = chart == null ? void 0 : chart.w) == null ? void 0 : _a.config) == null ? void 0 : _b.chart) == null ? void 0 : _c.link;
      return !!(link && typeof link.dimension === "function");
    };
    if (w2.config.chart.group && !isCfMember(this.ctx)) {
      chartGroups = this.ctx.getSyncedCharts().filter((ch) => !isCfMember(ch));
    }
    if (w2.globals.axisCharts && (w2.globals.minX === -Infinity && w2.globals.maxX === Infinity || w2.globals.dataPoints === 0)) {
      return;
    }
    if (chartGroups.length) {
      chartGroups.forEach((ch) => {
        const tooltipEl = this.getElTooltip(ch);
        const newOpts = {
          paths: opt.paths,
          tooltipEl,
          tooltipY: opt.tooltipY,
          tooltipX: opt.tooltipX,
          elGrid: opt.elGrid,
          hoverArea: opt.hoverArea,
          ttItems: ch.w.globals.tooltip.ttItems
        };
        if (ch.w.globals.minX === this.w.globals.minX && ch.w.globals.maxX === this.w.globals.maxX) {
          ch.w.globals.tooltip.seriesHoverByContext({
            chartCtx: ch,
            ttCtx: ch.w.globals.tooltip,
            opt: newOpts,
            e: e2
          });
        }
      });
    } else {
      this.seriesHoverByContext({
        chartCtx: this.ctx,
        ttCtx: this.w.globals.tooltip,
        opt,
        e: e2
      });
    }
  }
  /** @param {{chartCtx: any, ttCtx: any, opt: any, e: any}} opts */
  seriesHoverByContext({ chartCtx, ttCtx, opt, e: e2 }) {
    const w2 = chartCtx.w;
    const tooltipEl = this.getElTooltip(chartCtx);
    if (!tooltipEl) return;
    const cachedDims = ttCtx.getCachedDimensions();
    ttCtx.tooltipRect = {
      x: 0,
      y: 0,
      ttWidth: cachedDims.ttWidth,
      ttHeight: cachedDims.ttHeight
    };
    ttCtx.e = e2;
    if (ttCtx.tooltipUtil.hasBars() && !w2.globals.comboCharts && !ttCtx.isBarShared) {
      if (this.tConfig.onDatasetHover.highlightDataSeries) {
        const series = new Series(chartCtx.w);
        series.toggleSeriesOnHover(e2, e2.target.parentNode);
      }
    }
    if (w2.globals.axisCharts) {
      ttCtx.axisChartsTooltips({
        e: e2,
        opt,
        tooltipRect: ttCtx.tooltipRect
      });
    } else {
      ttCtx.nonAxisChartsTooltips({
        e: e2,
        opt,
        tooltipRect: ttCtx.tooltipRect
      });
    }
    if (ttCtx.fixedTooltip) {
      ttCtx.drawFixedTooltipRect();
    }
  }
  // tooltip handling for line/area/bar/columns/scatter
  /** @param {{e: any, opt: any}} opts */
  axisChartsTooltips({ e: e2, opt }) {
    var _a;
    const w2 = this.w;
    let x, y;
    if (!opt.elGrid) return;
    const seriesBound = opt.elGrid.getBoundingClientRect();
    const clientX = e2.type === "touchmove" ? e2.touches[0].clientX : e2.clientX;
    const clientY = e2.type === "touchmove" ? e2.touches[0].clientY : e2.clientY;
    this.clientY = clientY;
    this.clientX = clientX;
    w2.interact.capturedSeriesIndex = -1;
    w2.interact.capturedDataPointIndex = -1;
    if (clientY < seriesBound.top || clientY > seriesBound.top + seriesBound.height) {
      this.handleMouseOut(opt);
      return;
    }
    if (w2.dom.elWrap.querySelector(
      ".apexcharts-annotation-tooltip.apexcharts-active"
    )) {
      this.handleMouseOut(opt);
      return;
    }
    if (Array.isArray(this.tConfig.enabledOnSeries) && !w2.config.tooltip.shared) {
      const index = parseInt(opt.paths.getAttribute("index"), 10);
      if (this.tConfig.enabledOnSeries.indexOf(index) < 0) {
        this.handleMouseOut(opt);
        return;
      }
    }
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return;
    const xcrosshairs = this.getElXCrosshairs();
    let syncedCharts = [];
    if (w2.config.chart.group) {
      syncedCharts = this.ctx.getSyncedCharts();
    }
    const isStickyTooltip = w2.globals.xyCharts || w2.config.chart.type === "bar" && !w2.globals.isBarHorizontal && this.tooltipUtil.hasBars() && this.tConfig.shared || w2.globals.comboCharts && this.tooltipUtil.hasBars();
    if (e2.type === "mousemove" || e2.type === "touchmove" || e2.type === "mouseup") {
      if (w2.globals.collapsedSeries.length + w2.globals.ancillaryCollapsedSeries.length === w2.seriesData.series.length) {
        return;
      }
      if (xcrosshairs !== null) {
        xcrosshairs.classList.add("apexcharts-active");
      }
      const hasYAxisTooltip = (_a = this.yaxisTooltips) == null ? void 0 : _a.filter(
        (b2) => {
          return b2 === true;
        }
      );
      const _yc = (
        /** @type {any} */
        this.ycrosshairs
      );
      if (_yc !== null && (hasYAxisTooltip == null ? void 0 : hasYAxisTooltip.length)) {
        _yc.classList.add("apexcharts-active");
      }
      if (isStickyTooltip && !this.showOnIntersect || syncedCharts.length > 1) {
        this.handleStickyTooltip(e2, clientX, clientY, opt);
      } else {
        if (w2.config.chart.type === "heatmap" || w2.config.chart.type === "treemap") {
          const markerXY = this.intersect.handleHeatTreeTooltip({
            e: e2,
            opt,
            x,
            y,
            type: w2.config.chart.type
          });
          if (markerXY.noHit) {
            this.handleMouseOut(opt);
            return;
          }
          x = markerXY.x;
          y = markerXY.y;
          if (!markerXY.positioned) {
            tooltipEl.style.left = x + "px";
            tooltipEl.style.top = y + "px";
          }
        } else {
          if (this.tooltipUtil.hasBars()) {
            this.intersect.handleBarTooltip({
              e: e2,
              opt
            });
          }
          if (this.tooltipUtil.hasMarkers(0)) {
            this.intersect.handleMarkerTooltip({
              e: e2,
              opt,
              x,
              y
            });
          }
        }
      }
      if (this.yaxisTooltips && this.yaxisTooltips.length) {
        for (let yt = 0; yt < w2.config.yaxis.length; yt++) {
          this.axesTooltip.drawYaxisTooltipText(
            yt,
            clientY,
            /** @type {import('../../types/internal').XYRatios} */
            this.xyRatios
          );
        }
      }
      w2.dom.baseEl.classList.add("apexcharts-tooltip-active");
      opt.tooltipEl.classList.add("apexcharts-active");
      if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.announcements.enabled) {
        opt.tooltipEl.removeAttribute("aria-hidden");
      }
    } else if (e2.type === "mouseout" || e2.type === "touchend") {
      this.handleMouseOut(opt);
    }
  }
  /**
   * Where a pie / donut / polarArea slice wants its tooltip anchored, in
   * elWrap-relative pixels.
   *
   * Pie.js stamps the arc centroid on the path as `data:cx` / `data:cy`, in
   * the slice's OWN user space: below the inner group's translate, and below
   * the pie group's customScale. Reading those as if they were SVG-root
   * coordinates silently drops both, and the inner translate is exactly the
   * offset that centres a pie in a chart wider than it is tall, so on such a
   * chart the tooltip landed a couple of hundred pixels to the left of the
   * slice it described. The element's screen matrix accounts for every
   * ancestor transform at once, including the translate a slice picks up while
   * it is slid out on click.
   *
   * @param {any} el a slice path carrying data:cx / data:cy
   * @returns {{x: number, y: number} | null} null when it carries neither
   */
  getSliceAnchor(el) {
    var _a, _b;
    const w2 = this.w;
    const cx = parseFloat((_a = el == null ? void 0 : el.getAttribute("data:cx")) != null ? _a : "");
    const cy = parseFloat((_b = el == null ? void 0 : el.getAttribute("data:cy")) != null ? _b : "");
    if (isNaN(cx) || isNaN(cy)) return null;
    const wrapBound = w2.dom.elWrap.getBoundingClientRect();
    const ctm = typeof el.getScreenCTM === "function" ? el.getScreenCTM() : null;
    if (!ctm) {
      const svgBound = w2.dom.Paper.node.getBoundingClientRect();
      return {
        x: svgBound.left - wrapBound.left + cx,
        y: svgBound.top - wrapBound.top + cy
      };
    }
    return {
      x: ctm.a * cx + ctm.c * cy + ctm.e - wrapBound.left,
      y: ctm.b * cx + ctm.d * cy + ctm.f - wrapBound.top
    };
  }
  // tooltip handling for pie/donuts
  /** @param {{e: any, opt: any, tooltipRect: any}} opts */
  nonAxisChartsTooltips({ e: e2, opt, tooltipRect }) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const rel = opt.paths.getAttribute("rel");
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return;
    const seriesBound = w2.dom.elWrap.getBoundingClientRect();
    if (e2.type === "mousemove" || e2.type === "touchmove") {
      w2.dom.baseEl.classList.add("apexcharts-tooltip-active");
      tooltipEl.classList.add("apexcharts-active");
      if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.announcements.enabled) {
        tooltipEl.removeAttribute("aria-hidden");
      }
      if (w2.config.chart.type === "unit") {
        const unitDot = e2.target && typeof e2.target.closest === "function" ? e2.target.closest(".apexcharts-unit-area") : null;
        if (!unitDot) return;
        this.renderUnitTooltip(unitDot);
      } else {
        this.tooltipLabels.drawSeriesTexts({
          ttItems: opt.ttItems,
          i: parseInt(rel, 10) - 1,
          shared: false
        });
      }
      let x, y;
      const arcPath = opt.paths.querySelector("path[data\\:cx]") || opt.paths;
      const anchor = w2.config.tooltip.intersect ? this.getSliceAnchor(arcPath) : null;
      if (anchor) {
        x = anchor.x - tooltipRect.ttWidth / 2;
        y = anchor.y - tooltipRect.ttHeight - 10;
      } else {
        x = ((_a = w2.interact.clientX) != null ? _a : 0) - seriesBound.left - tooltipRect.ttWidth / 2;
        y = ((_b = w2.interact.clientY) != null ? _b : 0) - seriesBound.top - tooltipRect.ttHeight - 10;
      }
      tooltipEl.style.left = x + "px";
      tooltipEl.style.top = y + "px";
      if (w2.config.legend.tooltipHoverFormatter) {
        const legendFormatter = w2.config.legend.tooltipHoverFormatter;
        const i2 = rel - 1;
        const legendEl = (
          /** @type {HTMLElement | undefined} */
          (_c = this.legendLabels) == null ? void 0 : _c[i2]
        );
        if (!legendEl) return;
        const legendName = legendEl.getAttribute("data:default-text");
        const text = legendFormatter(legendName, {
          seriesIndex: i2,
          dataPointIndex: i2,
          w: w2
        });
        legendEl.innerHTML = text;
      }
    } else if (e2.type === "mouseout" || e2.type === "touchend") {
      tooltipEl.classList.remove("apexcharts-active");
      w2.dom.baseEl.classList.remove("apexcharts-tooltip-active");
      if (w2.config.legend.tooltipHoverFormatter) {
        (_d = this.legendLabels) == null ? void 0 : _d.forEach((l2) => {
          const defaultText = l2.getAttribute("data:default-text");
          l2.innerHTML = decodeURIComponent(
            defaultText != null ? defaultText : ""
          );
        });
      }
    }
  }
  /**
   * Fill the tooltip for one hovered unit-chart dot. Each dot carries `i` (its
   * category / series index) and `j` (its index within that category). The
   * default body reads "#<j+1> of <count>"; `plotOptions.unit.tooltip.formatter`
   * overrides just the body text (it is handed i/j so it can look up per-unit
   * data), and the global `tooltip.custom` still overrides the whole markup.
   * @param {Element} dotEl the hovered `.apexcharts-unit-area` node
   */
  renderUnitTooltip(dotEl) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const tooltipEl = this.getElTooltip();
    if (!tooltipEl) return;
    const i2 = parseInt(dotEl.getAttribute("i") || "0", 10);
    const j = parseInt(dotEl.getAttribute("j") || "0", 10);
    if (typeof w2.config.tooltip.custom === "function") {
      this.tooltipLabels.handleCustomTooltip({ i: i2, j, y1: null, y2: null, w: w2 });
      return;
    }
    const seriesName = w2.seriesData.seriesNames[i2] || `series-${i2 + 1}`;
    const value = Math.round(Number(w2.seriesData.series[i2]) || 0);
    const group = dotEl.parentNode;
    const count = group && group.querySelectorAll ? group.querySelectorAll(".apexcharts-unit-area").length : value;
    const unitOpts = w2.config.plotOptions.unit || {};
    const unitValue = unitOpts.unitValue > 0 ? unitOpts.unitValue : 1;
    const catData = w2.seriesData.unitData && w2.seriesData.unitData[i2];
    const datum = catData ? catData[j] : void 0;
    const datumObj = datum && typeof datum === "object" ? datum : null;
    const color = datumObj && datumObj.fillColor || w2.globals.colors && w2.globals.colors[i2] || "#008FFB";
    let body;
    const fmt = unitOpts.tooltip && unitOpts.tooltip.formatter;
    if (typeof fmt === "function") {
      body = fmt({
        seriesName,
        seriesIndex: i2,
        dataPointIndex: j,
        count,
        value,
        unitValue,
        datum,
        color,
        w: w2
      });
    } else if (datum !== void 0 && datum !== null) {
      const label = datumObj ? (_c = (_b = (_a = datumObj.name) != null ? _a : datumObj.label) != null ? _b : datumObj.x) != null ? _c : null : null;
      const dVal = datumObj ? (_e = (_d = datumObj.value) != null ? _d : datumObj.y) != null ? _e : null : datum;
      body = label != null && dVal != null ? `${label}: ${dVal}` : label != null ? String(label) : dVal != null ? String(dVal) : `#${(j + 1).toLocaleString()} of ${count.toLocaleString()}`;
    } else {
      body = `#${(j + 1).toLocaleString()} of ${count.toLocaleString()}`;
      if (unitValue !== 1) {
        body += ` &middot; ${unitValue.toLocaleString()} per dot`;
      }
    }
    const fontFamily = w2.config.chart.fontFamily || "inherit";
    const fontSize = w2.config.tooltip.style && w2.config.tooltip.style.fontSize || "12px";
    const arrowEl = tooltipEl.querySelector(".apexcharts-tooltip-arrow");
    tooltipEl.innerHTML = `<div class="apexcharts-tooltip-title" style="font-family: ${fontFamily}; font-size: ${fontSize};">${seriesName}</div><div class="apexcharts-tooltip-series-group apexcharts-active" style="display: flex;"><span class="apexcharts-tooltip-marker" style="background-color: ${color};"></span><div class="apexcharts-tooltip-text" style="font-family: ${fontFamily}; font-size: ${fontSize};"><div class="apexcharts-tooltip-y-group"><span class="apexcharts-tooltip-text-y-value">${body}</span></div></div></div>`;
    if (arrowEl) tooltipEl.appendChild(arrowEl);
    const rect = tooltipEl.getBoundingClientRect();
    this.tooltipRect.ttWidth = rect.width;
    this.tooltipRect.ttHeight = rect.height;
  }
  /**
   * @param {Event} e
   * @param {number} clientX
   * @param {number} clientY
   * @param {Record<string, any>} opt
   */
  handleStickyTooltip(e2, clientX, clientY, opt) {
    const w2 = this.w;
    const capj = this.tooltipUtil.getNearestValues({
      context: this,
      hoverArea: opt.hoverArea,
      elGrid: opt.elGrid,
      clientX,
      clientY
    });
    const j = capj.j;
    let capturedSeries = capj.capturedSeries;
    if (capturedSeries !== null && w2.globals.collapsedSeriesIndices.includes(capturedSeries != null ? capturedSeries : -1))
      capturedSeries = null;
    const edgePad = w2.globals.barPadForNumericAxis || 0;
    if (capj.hoverX < -edgePad || capj.hoverX > w2.layout.gridWidth + edgePad) {
      this.handleMouseOut(opt);
      return;
    }
    if (capturedSeries !== null) {
      this.handleStickyCapturedSeries(e2, capturedSeries != null ? capturedSeries : -1, opt, j != null ? j : 0);
    } else {
      if (this.tooltipUtil.isXoverlap(j != null ? j : 0) || w2.globals.isBarHorizontal) {
        const firstVisibleSeries = w2.seriesData.series.findIndex(
          /**
           * @param {any} s
           * @param {number} i
           */
          (s2, i2) => !w2.globals.collapsedSeriesIndices.includes(i2)
        );
        this.create(e2, this, firstVisibleSeries, j != null ? j : 0, opt.ttItems);
      }
    }
  }
  /**
   * @param {Event} e
   * @param {number} capturedSeries
   * @param {Record<string, any>} opt
   * @param {number} j
   */
  handleStickyCapturedSeries(e2, capturedSeries, opt, j) {
    const w2 = this.w;
    if (!this.tConfig.shared) {
      const ignoreNull = w2.seriesData.series[capturedSeries][j] === null;
      if (ignoreNull) {
        this.handleMouseOut(opt);
        return;
      }
    }
    if (typeof w2.seriesData.series[capturedSeries][j] !== "undefined") {
      if (this.tConfig.shared && this.tooltipUtil.isXoverlap(j) && this.tooltipUtil.isInitialSeriesSameLen()) {
        this.create(e2, this, capturedSeries, j, opt.ttItems);
      } else {
        this.create(e2, this, capturedSeries, j, opt.ttItems, false);
      }
    } else {
      if (this.tooltipUtil.isXoverlap(j)) {
        const firstVisibleSeries = w2.seriesData.series.findIndex(
          /**
           * @param {any} s
           * @param {number} i
           */
          (s2, i2) => !w2.globals.collapsedSeriesIndices.includes(i2)
        );
        this.create(e2, this, firstVisibleSeries, j, opt.ttItems);
      }
    }
  }
  deactivateHoverFilter() {
    const w2 = this.w;
    const graphics = new Graphics(this.w, this.ctx);
    const allPaths = w2.dom.Paper.find(`.apexcharts-bar-area`);
    for (let b2 = 0; b2 < allPaths.length; b2++) {
      graphics.pathMouseLeave(
        /** @type {any} */
        allPaths[b2],
        /** @type {any} */
        void 0
      );
    }
  }
  /**
   * @param {Record<string, any>} opt
   */
  handleMouseOut(opt) {
    var _a, _b;
    const w2 = this.w;
    const xcrosshairs = this.getElXCrosshairs();
    w2.dom.baseEl.classList.remove("apexcharts-tooltip-active");
    opt.tooltipEl.classList.remove("apexcharts-active");
    delete opt.tooltipEl.dataset.positioned;
    if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.announcements.enabled) {
      opt.tooltipEl.setAttribute("aria-hidden", "true");
    }
    this.deactivateHoverFilter();
    if (w2.config.chart.type !== "bubble") {
      this.marker.resetPointsSize();
    }
    if (xcrosshairs !== null) {
      xcrosshairs.classList.remove("apexcharts-active");
    }
    const _yc2 = (
      /** @type {any} */
      this.ycrosshairs
    );
    if (_yc2 !== null) {
      _yc2.classList.remove("apexcharts-active");
    }
    if (this.isXAxisTooltipEnabled) {
      (_a = this.xaxisTooltip) == null ? void 0 : _a.classList.remove("apexcharts-active");
    }
    if (this.yaxisTooltips && this.yaxisTooltips.length) {
      if (this.yaxisTTEls === null) {
        this.yaxisTTEls = /** @type {HTMLElement[]} */
        [
          ...w2.dom.baseEl.querySelectorAll(".apexcharts-yaxistooltip")
        ];
      }
      for (let i2 = 0; i2 < this.yaxisTTEls.length; i2++) {
        this.yaxisTTEls[i2].classList.remove("apexcharts-active");
      }
    }
    if (w2.config.legend.tooltipHoverFormatter) {
      (_b = this.legendLabels) == null ? void 0 : _b.forEach((l2) => {
        const defaultText = l2.getAttribute("data:default-text");
        l2.innerHTML = decodeURIComponent(
          defaultText != null ? defaultText : ""
        );
      });
    }
  }
  /**
   * @param {Event} e
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   */
  markerClick(e2, seriesIndex, dataPointIndex) {
    const w2 = this.w;
    if (typeof w2.config.chart.events.markerClick === "function") {
      w2.config.chart.events.markerClick(e2, this.ctx, {
        seriesIndex,
        dataPointIndex,
        w: w2
      });
    }
    this.ctx.events.fireEvent("markerClick", [
      e2,
      this.ctx,
      { seriesIndex, dataPointIndex, w: w2 }
    ]);
  }
  /**
   * Marks (#11): whether the chart's type (or any series' type) is a registered
   * custom series, whose marks live outside the built-in marker DOM.
   * @returns {boolean}
   */
  _hasCustomSeries() {
    const w2 = this.w;
    if (isCustom(w2.config.chart.type)) return true;
    const series = w2.config.series || [];
    return series.some(
      (s2) => s2 && s2.type && isCustom(s2.type)
    );
  }
  /**
   * @param {Event} e
   * @param {any} context
   * @param {number} capturedSeries
   * @param {number} j
   * @param {any} ttItems
   * @param {boolean | null} shared
   */
  create(e2, context, capturedSeries, j, ttItems, shared = null) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
    const w2 = this.w;
    const ttCtx = context;
    if (e2.type === "mouseup") {
      this.markerClick(e2, capturedSeries, j);
    }
    if (shared === null) shared = this.tConfig.shared;
    const hasMarkers = this.tooltipUtil.hasMarkers(capturedSeries);
    const canvasMode = ((_b = (_a = this.ctx) == null ? void 0 : _a.renderer) == null ? void 0 : _b.kind) === "canvas";
    const canvasNonBar = canvasMode && !this.tooltipUtil.hasBars();
    const marksMode = !canvasMode && !hasMarkers && !this.tooltipUtil.hasBars() && this._hasCustomSeries();
    const dynamicPoints = canvasNonBar || marksMode;
    const bars = this.tooltipUtil.getElBars();
    const handlePoints = () => {
      if (w2.globals.markers.largestSize > 0 && !canvasMode) {
        ttCtx.marker.enlargePoints(j);
      } else {
        ttCtx.tooltipPosition.moveDynamicPointsOnHover(j);
      }
    };
    if (w2.config.legend.tooltipHoverFormatter) {
      const legendFormatter = w2.config.legend.tooltipHoverFormatter;
      const els = (
        /** @type {HTMLElement[]} */
        Array.from((_c = this.legendLabels) != null ? _c : [])
      );
      els.forEach((l2) => {
        const legendName = l2.getAttribute("data:default-text");
        l2.innerHTML = decodeURIComponent(legendName != null ? legendName : "");
      });
      for (let i2 = 0; i2 < els.length; i2++) {
        const l2 = els[i2];
        const lsIndex = parseInt((_d = l2.getAttribute("i")) != null ? _d : "", 10);
        const legendName = decodeURIComponent(
          (_e = l2.getAttribute("data:default-text")) != null ? _e : ""
        );
        const text = legendFormatter(legendName, {
          seriesIndex: shared ? lsIndex : capturedSeries,
          dataPointIndex: j,
          w: w2
        });
        if (!shared) {
          l2.innerHTML = lsIndex === capturedSeries ? text : legendName;
          if (capturedSeries === lsIndex) {
            break;
          }
        } else {
          l2.innerHTML = w2.globals.collapsedSeriesIndices.indexOf(lsIndex) < 0 ? text : legendName;
        }
      }
    }
    const _rangeData = (
      /** @type {any} */
      w2.rangeData
    );
    const commonSeriesTextsParams = __spreadValues(__spreadValues({
      ttItems,
      i: capturedSeries,
      j
    }, ((_i = (_h = (_g = (_f = _rangeData.seriesRange) == null ? void 0 : _f[capturedSeries]) == null ? void 0 : _g[j]) == null ? void 0 : _h.y[0]) == null ? void 0 : _i.y1) !== void 0 && {
      y1: (_m = (_l = (_k = (_j = _rangeData.seriesRange) == null ? void 0 : _j[capturedSeries]) == null ? void 0 : _k[j]) == null ? void 0 : _l.y[0]) == null ? void 0 : _m.y1
    }), ((_q = (_p = (_o = (_n = _rangeData.seriesRange) == null ? void 0 : _n[capturedSeries]) == null ? void 0 : _o[j]) == null ? void 0 : _p.y[0]) == null ? void 0 : _q.y2) !== void 0 && {
      y2: (_u = (_t = (_s = (_r = _rangeData.seriesRange) == null ? void 0 : _r[capturedSeries]) == null ? void 0 : _s[j]) == null ? void 0 : _t.y[0]) == null ? void 0 : _u.y2
    });
    if (shared) {
      ttCtx.tooltipLabels.drawSeriesTexts(__spreadProps(__spreadValues({}, commonSeriesTextsParams), {
        shared: this.showOnIntersect ? false : this.tConfig.shared
      }));
      if (hasMarkers || dynamicPoints) {
        handlePoints();
      } else if (this.tooltipUtil.hasBars()) {
        if (canvasMode) {
          ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, capturedSeries);
        }
        this.barSeriesHeight = this.tooltipUtil.getBarsHeight(
          /** @type {any[]} */
          [...bars]
        );
        if (this.barSeriesHeight > 0) {
          const graphics = new Graphics(this.w, this.ctx);
          const paths = w2.dom.Paper.find(`.apexcharts-bar-area[j='${j}']`);
          this.deactivateHoverFilter();
          const points = ttCtx.tooltipUtil.getAllMarkers(true);
          if (points.length && !this.barSeriesHeight) {
            handlePoints();
          }
          ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, capturedSeries);
          for (let b2 = 0; b2 < paths.length; b2++) {
            graphics.pathMouseEnter(
              /** @type {any} */
              paths[b2],
              /** @type {any} */
              void 0
            );
          }
        }
      }
    } else {
      ttCtx.tooltipLabels.drawSeriesTexts(__spreadValues({
        shared: false
      }, commonSeriesTextsParams));
      if (this.tooltipUtil.hasBars()) {
        ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, capturedSeries);
      }
      if (hasMarkers) {
        ttCtx.tooltipPosition.moveMarkers(capturedSeries, j);
      } else if (dynamicPoints) {
        ttCtx.tooltipPosition.moveDynamicPointOnHover(j, capturedSeries);
      }
    }
  }
}
class SvgRenderer {
  /**
   * @param {any} w
   * @param {any} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.kind = "svg";
  }
  // ── lifecycle (SVG builds its layer via the existing plotChartType flow) ──
  beginSeries() {
  }
  present() {
    return null;
  }
  clear() {
  }
  // ── emit primitives (delegate to Graphics: the canvas renderer mirrors
  //    this exact surface) ──
  /** @param {any} attrs */
  group(attrs) {
    return this.ctx.graphics.group(attrs);
  }
  /** @param {any} opts */
  drawPath(opts) {
    return this.ctx.graphics.drawPath(opts);
  }
  /** @param {any[]} args */
  drawLine(...args) {
    return this.ctx.graphics.drawLine(...args);
  }
  /** @param {any[]} args */
  drawRect(...args) {
    return this.ctx.graphics.drawRect(...args);
  }
  /**
   * @param {number} r
   * @param {any} attrs
   */
  drawCircle(r2, attrs) {
    return this.ctx.graphics.drawCircle(r2, attrs);
  }
  /** @param {any} opts */
  drawText(opts) {
    return this.ctx.graphics.drawText(opts);
  }
  /**
   * A series mark path (animation-aware). Faithful passthrough to Graphics.
   * Note: the SVG emit path in the per-type draw() methods routes through
   * `seriesEmitter`, which returns the caller's own `Graphics` in SVG mode: so
   * this method is the interface contract surface (mirrored by the canvas
   * renderer), not the hot path.
   * @param {any} opts
   */
  renderPaths(opts) {
    return this.ctx.graphics.renderPaths(opts);
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {any} opts
   */
  drawMarker(x, y, opts = {}) {
    return this.ctx.graphics.drawMarker(x, y, opts);
  }
  // ── capabilities: SVG supports everything the interface enumerates ──
  /** @param {string} _feature */
  supports(_feature) {
    return true;
  }
  // ── interaction: the DOM does this natively in SVG mode ──
  hitTest() {
    return null;
  }
  restyle() {
  }
  // ── export: SVG serializes directly; no bitmap to composite ──
  toBitmap() {
    return null;
  }
  destroy() {
  }
}
const RENDERER_REGISTRY_KEY = "__apexcharts_renderers__";
function getRendererRegistry() {
  const g2 = (
    /** @type {any} */
    globalThis
  );
  if (!g2[RENDERER_REGISTRY_KEY]) g2[RENDERER_REGISTRY_KEY] = /* @__PURE__ */ new Map();
  return g2[RENDERER_REGISTRY_KEY];
}
class RendererController {
  /** Same Map as getRendererRegistry(); exposed for tests/tooling. */
  static get _rendererRegistry() {
    return getRendererRegistry();
  }
  /**
   * @param {string} kind
   * @param {(w: any, ctx: any) => any} factory
   */
  static registerRenderer(kind, factory) {
    getRendererRegistry().set(kind, factory);
  }
  /**
   * Remove a registered renderer backend (tests / hot-reload). Charts fall
   * back to SVG on their next resolve().
   * @param {string} kind
   */
  static unregisterRenderer(kind) {
    getRendererRegistry().delete(kind);
  }
  /**
   * @param {any} w
   * @param {any} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.svg = new SvgRenderer(w2, ctx);
    this.active = this.svg;
    this._activeKind = "svg";
    this._instances = {};
  }
  /**
   * The kind selection WANTS (before availability/fallback). Pure.
   * @returns {import('../renderers/Renderer').RendererKind}
   */
  _desiredKind() {
    const cfg = this.w.config.chart;
    const mode = cfg.renderer || "svg";
    if (!Environment.isBrowser()) return "svg";
    if (mode === "svg") return "svg";
    if (hasCanvasUnsupportedFeature(this.w)) return "svg";
    if (mode === "canvas") return "canvas";
    const marks = computeMarkCount(this.w);
    const threshold = cfg.rendererThreshold || 8e3;
    return marks >= threshold ? "canvas" : "svg";
  }
  /**
   * Resolve + instantiate the active renderer and set `ctx.renderer`. Falls
   * back to SVG (with a warning only when canvas was explicitly requested) if
   * the desired backend is not registered.
   * @returns {import('../renderers/Renderer').RendererKind}
   */
  resolve() {
    const mode = this.w.config.chart.renderer || "svg";
    const desired = this._desiredKind();
    if (desired !== "svg") {
      const factory = getRendererRegistry().get(desired);
      if (factory) {
        if (!this._instances[desired]) {
          this._instances[desired] = factory(this.w, this.ctx);
        }
        this.active = this._instances[desired];
        this._activeKind = desired;
        this.ctx.renderer = this.active;
        this.w.globals.activeRenderer = this.active;
        return this._activeKind;
      }
      if (mode === desired) {
        console.warn(
          `[apexcharts] renderer:"${desired}" requested but that renderer is not bundled (import 'apexcharts/features/renderer-${desired}'); falling back to SVG.`
        );
      }
    } else if (mode === "canvas" && hasCanvasUnsupportedFeature(this.w)) {
      console.warn(
        `[apexcharts] renderer:"canvas" requested but this chart uses a feature the canvas renderer does not render yet (gradient/pattern/image fill or a state color-matrix filter); falling back to SVG.`
      );
    }
    this.active = this.svg;
    this._activeKind = "svg";
    this.ctx.renderer = this.active;
    this.w.globals.activeRenderer = this.active;
    return this._activeKind;
  }
  /** @returns {import('../renderers/Renderer').RendererKind} */
  getActiveKind() {
    return this._activeKind;
  }
  /** Destroy the owned non-SVG renderer instances (full chart destroy). */
  teardown() {
    for (const kind in this._instances) {
      const r2 = this._instances[kind];
      if (r2 && typeof r2.destroy === "function") r2.destroy();
    }
    this._instances = {};
    this.active = this.svg;
    this._activeKind = "svg";
  }
}
class SVGElement {
  /**
   * @param {any} node
   */
  constructor(node) {
    this.node = node;
    if (node) {
      node.instance = this;
    }
    this._listeners = [];
    this._filter = null;
  }
  // ---- Attribute methods ----
  /**
   * @param {any} a
   * @param {any} [v]
   */
  attr(a2, v2) {
    if (typeof a2 === "string" && v2 === void 0) {
      return this.node.getAttribute(a2);
    }
    const attrs = typeof a2 === "string" ? { [a2]: v2 } : a2;
    for (const key in attrs) {
      let val = attrs[key];
      if (val === null) {
        this.node.removeAttribute(key);
      } else if (val !== void 0) {
        if (typeof val === "number" && isNaN(val)) val = 0;
        this.node.setAttribute(key, val);
      }
    }
    if (this.node.nodeName === "text" && attrs.x != null) {
      const tspans = this.node.querySelectorAll("tspan[data-newline]");
      for (let i2 = 0; i2 < tspans.length; i2++) {
        tspans[i2].setAttribute("x", attrs.x);
      }
    }
    return this;
  }
  /**
   * @param {Record<string, string>} styles
   */
  css(styles) {
    for (const k in styles) {
      this.node.style[k] = styles[k];
    }
    return this;
  }
  /**
   * @param {any} v
   */
  fill(v2) {
    if (typeof v2 === "object") {
      return this.attr(v2);
    }
    return this.attr("fill", v2);
  }
  /**
   * @param {any} v
   */
  stroke(v2) {
    if (typeof v2 === "object") {
      if (v2.color !== void 0) this.attr("stroke", v2.color);
      if (v2.width !== void 0) this.attr("stroke-width", v2.width);
      if (v2.dasharray !== void 0) this.attr("stroke-dasharray", v2.dasharray);
      if (v2.linecap !== void 0) this.attr("stroke-linecap", v2.linecap);
      if (v2.opacity !== void 0) this.attr("stroke-opacity", v2.opacity);
      return this;
    }
    return this.attr("stroke", v2);
  }
  /**
   * @param {number} w
   * @param {number} h
   */
  size(w2, h2) {
    return this.attr({ width: w2, height: h2 });
  }
  /**
   * @param {number} x
   * @param {number} y
   */
  move(x, y) {
    return this.attr({ x, y });
  }
  /**
   * @param {number} cx
   * @param {number} cy
   */
  center(cx, cy) {
    if (this.node.nodeName === "g") {
      const box = this.bbox();
      const dx = cx - (box.x + box.width / 2);
      const dy = cy - (box.y + box.height / 2);
      return this.attr("transform", `translate(${dx}, ${dy})`);
    }
    return this.attr({ cx, cy });
  }
  // ---- Tree operations ----
  /**
   * @param {any} child
   */
  add(child) {
    if (child && child.__isCanvasMark) return this;
    this.node.appendChild(child.node || child);
    return this;
  }
  /**
   * @param {any} parent
   */
  addTo(parent) {
    const p2 = parent.node || parent;
    p2.appendChild(this.node);
    return this;
  }
  remove() {
    if (this.node.parentNode) {
      this.node.parentNode.removeChild(this.node);
    }
    return this;
  }
  clear() {
    while (this.node.firstChild) {
      this.node.removeChild(this.node.firstChild);
    }
    return this;
  }
  // ---- Query ----
  /**
   * @param {string} selector
   */
  find(selector) {
    return Array.from(this.node.querySelectorAll(selector)).map(
      (n2) => n2.instance || new SVGElement(n2)
    );
  }
  /**
   * @param {string} selector
   */
  findOne(selector) {
    const n2 = this.node.querySelector(selector);
    return n2 ? n2.instance || new SVGElement(n2) : null;
  }
  // ---- Events ----
  /**
   * @param {Event} event
   * @param {Function} handler
   */
  on(event, handler) {
    const eventType = (
      /** @type {string} */
      /** @type {any} */
      event.split(
        "."
      )[0]
    );
    this._listeners.push({ event, eventType, handler });
    this.node.addEventListener(eventType, handler);
    return this;
  }
  /**
   * @param {Event} event
   * @param {Function} handler
   */
  off(event, handler) {
    if (!event && !handler) {
      this._listeners.forEach((l2) => {
        this.node.removeEventListener(l2.eventType, l2.handler);
      });
      this._listeners = [];
    } else if (event && !handler) {
      const eventType = (
        /** @type {string} */
        /** @type {any} */
        event.split(".")[0]
      );
      this._listeners = this._listeners.filter((l2) => {
        if (l2.eventType === eventType) {
          this.node.removeEventListener(l2.eventType, l2.handler);
          return false;
        }
        return true;
      });
    } else {
      const eventType = (
        /** @type {string} */
        /** @type {any} */
        event.split(".")[0]
      );
      this._listeners = this._listeners.filter((l2) => {
        if (l2.eventType === eventType && l2.handler === handler) {
          this.node.removeEventListener(l2.eventType, l2.handler);
          return false;
        }
        return true;
      });
    }
    return this;
  }
  // ---- Iteration ----
  /**
   * @param {Function} fn
   * @param {boolean} deep
   */
  each(fn, deep) {
    const children = Array.from(this.node.children);
    children.forEach((child) => {
      const inst = child.instance || new SVGElement(child);
      fn.call(inst);
      if (deep) inst.each(fn, deep);
    });
    return this;
  }
  // ---- CSS classes ----
  /**
   * @param {string} cls
   */
  removeClass(cls) {
    if (cls === "*") {
      this.node.removeAttribute("class");
    } else {
      this.node.classList.remove(cls);
    }
    return this;
  }
  // ---- Children ----
  children() {
    return Array.from(this.node.childNodes).filter((n2) => n2.nodeType === 1).map((n2) => n2.instance || new SVGElement(n2));
  }
  // ---- Visibility ----
  hide() {
    this.node.style.display = "none";
    return this;
  }
  show() {
    this.node.style.display = "";
    return this;
  }
  // ---- Measurement ----
  bbox() {
    if (typeof this.node.getBBox === "function") {
      try {
        return this.node.getBBox();
      } catch (e2) {
      }
    }
    return { x: 0, y: 0, width: 0, height: 0 };
  }
  // ---- Text-specific ----
  /**
   * @param {string} text
   */
  tspan(text) {
    const tspan = BrowserAPIs.createElementNS(
      "http://www.w3.org/2000/svg",
      "tspan"
    );
    tspan.textContent = text;
    this.node.appendChild(tspan);
    return new SVGElement(tspan);
  }
  // ---- Path-specific ----
  /**
   * @param {string} d
   */
  plot(d) {
    if (typeof d === "string") {
      this.attr("d", d);
    }
    return this;
  }
  // ---- Animation (overridden by SVGAnimation mixin) ----
  animate() {
    throw new Error("Animation module not loaded");
  }
  // ---- Filter methods (set up by SVGFilter module) ----
  // `filterWith`, `unfilter` and `filterer` are installed on the prototype by
  // installFilterMethods() (svg/index.js). This stub only exists so a call
  // before the filter module is installed fails with a clear message.
  filterWith() {
    throw new Error("Filter module not loaded");
  }
}
let gradientCounter = 0;
class SVGGradient extends SVGElement {
  /**
   * @param {any} container
   * @param {string} type
   * @param {object} builder
   */
  constructor(container, type, builder) {
    const tag = type === "radial" ? "radialGradient" : "linearGradient";
    const node = BrowserAPIs.createElementNS(SVGNS$2, tag);
    super(node);
    this._id = "SvgjsGradient" + ++gradientCounter;
    this.attr("id", this._id);
    if (typeof builder === "function") {
      builder(new StopBuilder(this));
    }
    let defs = container.node.querySelector("defs");
    if (!defs) {
      defs = BrowserAPIs.createElementNS(SVGNS$2, "defs");
      container.node.appendChild(defs);
    }
    defs.appendChild(this.node);
  }
  /**
   * @param {any} offset
   * @param {string} color
   * @param {number} opacity
   */
  stop(offset, color, opacity) {
    const s2 = BrowserAPIs.createElementNS(SVGNS$2, "stop");
    s2.setAttribute("offset", offset);
    s2.setAttribute("stop-color", color);
    if (opacity !== void 0) s2.setAttribute("stop-opacity", String(opacity));
    this.node.appendChild(s2);
    return this;
  }
  /**
   * @param {number} x
   * @param {number} y
   */
  from(x, y) {
    return this.attr({ x1: x, y1: y });
  }
  /**
   * @param {number} x
   * @param {number} y
   */
  to(x, y) {
    return this.attr({ x2: x, y2: y });
  }
  url() {
    return "url(#" + this._id + ")";
  }
  toString() {
    return this.url();
  }
  valueOf() {
    return this.url();
  }
  fill() {
    return this.url();
  }
}
class StopBuilder {
  /**
   * @param {any} gradient
   */
  constructor(gradient) {
    this.gradient = gradient;
  }
  /**
   * @param {any} offset
   * @param {string} color
   * @param {number} opacity
   */
  stop(offset, color, opacity) {
    this.gradient.stop(offset, color, opacity);
    return this;
  }
}
let patternCounter = 0;
class SVGPattern extends SVGElement {
  /**
   * @param {any} container
   * @param {number} w
   * @param {number} h
   * @param {Function} builder
   */
  constructor(container, w2, h2, builder) {
    const node = BrowserAPIs.createElementNS(SVGNS$2, "pattern");
    super(node);
    this._id = "SvgjsPattern" + ++patternCounter;
    this.attr({
      id: this._id,
      width: w2,
      height: h2,
      patternUnits: "userSpaceOnUse"
    });
    if (typeof builder === "function") {
      const patternContainer = new SVGContainer(this.node);
      builder(patternContainer);
    }
    let defs = container.node.querySelector("defs");
    if (!defs) {
      defs = BrowserAPIs.createElementNS(SVGNS$2, "defs");
      container.node.appendChild(defs);
    }
    defs.appendChild(this.node);
  }
  url() {
    return "url(#" + this._id + ")";
  }
  toString() {
    return this.url();
  }
  valueOf() {
    return this.url();
  }
  fill() {
    return this.url();
  }
}
class SVGContainer extends SVGElement {
  /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   */
  line(x1, y1, x2, y2) {
    const el = this._make("line");
    if (x1 !== void 0) {
      el.attr({ x1, y1, x2, y2 });
    }
    return el;
  }
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} h
   */
  rect(w2, h2) {
    const el = this._make("rect");
    if (w2 !== void 0) {
      el.attr({ width: w2, height: h2 });
    }
    return el;
  }
  /**
   * @param {number} d
   */
  circle(d) {
    const el = this._make("circle");
    if (d !== void 0) {
      el.attr({ r: d / 2, cx: d / 2, cy: d / 2 });
    }
    return el;
  }
  /**
   * @param {string} d
   */
  path(d) {
    const el = this._make("path");
    if (d) el.attr("d", d);
    return el;
  }
  /**
   * @param {string} pts
   */
  polygon(pts) {
    const el = this._make("polygon");
    if (pts) el.attr("points", pts);
    return el;
  }
  group() {
    return this._makeContainer("g");
  }
  defs() {
    return this._makeContainer("defs");
  }
  /**
   * @param {string} textContent
   */
  plain(textContent) {
    const node = BrowserAPIs.createElementNS(SVGNS$2, "text");
    node.textContent = textContent;
    const el = new SVGElement(node);
    this.node.appendChild(node);
    return el;
  }
  /**
   * @param {object} builder
   */
  text(builder) {
    const node = BrowserAPIs.createElementNS(SVGNS$2, "text");
    const el = new SVGElement(node);
    this.node.appendChild(node);
    if (typeof builder === "function") {
      builder(new TspanBuilder(node));
    }
    return el;
  }
  /**
   * @param {string} url
   * @param {Function} callback
   */
  image(url, callback) {
    const node = BrowserAPIs.createElementNS(SVGNS$2, "image");
    node.setAttributeNS("http://www.w3.org/1999/xlink", "href", url);
    const el = new SVGElement(node);
    this.node.appendChild(node);
    if (typeof callback === "function" && Environment.isBrowser()) {
      const img = new Image();
      img.onload = function() {
        el.size(img.width, img.height);
        callback.call(el, { width: img.width, height: img.height });
      };
      img.src = url;
    }
    return el;
  }
  /**
   * @param {string} type
   * @param {object} builder
   */
  gradient(type, builder) {
    return new SVGGradient(this, type, builder);
  }
  /**
   * @param {number} w
   * @param {number} h
   * @param {Function} builder
   */
  pattern(w2, h2, builder) {
    return new SVGPattern(this, w2, h2, builder);
  }
  /**
   * @param {string} tag
   */
  _make(tag) {
    const node = BrowserAPIs.createElementNS(SVGNS$2, tag);
    this.node.appendChild(node);
    return new SVGElement(node);
  }
  /**
   * @param {string} tag
   */
  _makeContainer(tag) {
    const node = BrowserAPIs.createElementNS(SVGNS$2, tag);
    this.node.appendChild(node);
    return new SVGContainer(node);
  }
}
class TspanBuilder {
  /**
   * @param {any} textNode
   */
  constructor(textNode) {
    this.textNode = textNode;
  }
  /**
   * @param {string} text
   */
  tspan(text) {
    const tspan = BrowserAPIs.createElementNS(SVGNS$2, "tspan");
    tspan.textContent = text;
    this.textNode.appendChild(tspan);
    return new TspanWrapper(tspan, this.textNode);
  }
}
class TspanWrapper {
  /**
   * @param {any} node
   * @param {any} textNode
   */
  constructor(node, textNode) {
    this.node = node;
    this.textNode = textNode;
  }
  newLine() {
    this.node.setAttribute("dy", "1.1em");
    this.node.dataset.newline = "1";
    return this;
  }
}
let filterCounter = 0;
class SVGFilter extends SVGElement {
  constructor() {
    const node = BrowserAPIs.createElementNS(SVGNS$2, "filter");
    super(node);
    this._id = "SvgjsFilter" + ++filterCounter;
    this.attr("id", this._id);
  }
}
class FilterBuilder {
  /**
   * @param {any} filter
   */
  constructor(filter) {
    this.filter = filter;
  }
  /**
   * @param {object} attrs
   */
  colorMatrix(attrs) {
    return this._primitive("feColorMatrix", attrs);
  }
  /**
   * @param {object} attrs
   */
  offset(attrs) {
    return this._primitive("feOffset", attrs);
  }
  /**
   * @param {object} attrs
   */
  gaussianBlur(attrs) {
    return this._primitive("feGaussianBlur", attrs);
  }
  /**
   * @param {object} attrs
   */
  flood(attrs) {
    return this._primitive("feFlood", attrs);
  }
  /**
   * @param {object} attrs
   */
  composite(attrs) {
    return this._primitive("feComposite", attrs);
  }
  /**
   * @param {string[]} sources
   */
  merge(sources) {
    const m2 = BrowserAPIs.createElementNS(SVGNS$2, "feMerge");
    sources.forEach((src) => {
      const mn = BrowserAPIs.createElementNS(SVGNS$2, "feMergeNode");
      mn.setAttribute("in", src);
      m2.appendChild(mn);
    });
    this.filter.node.appendChild(m2);
    return new SVGElement(m2);
  }
  /**
   * @param {string} tag
   * @param {Record<string, any>} attrs
   */
  _primitive(tag, attrs) {
    const el = BrowserAPIs.createElementNS(SVGNS$2, tag);
    for (const key in attrs) {
      el.setAttribute(key, attrs[key]);
    }
    this.filter.node.appendChild(el);
    return new SVGElement(el);
  }
}
function installFilterMethods(ElementClass) {
  ElementClass.prototype.filterWith = function(fn) {
    const filter = new SVGFilter();
    this._filter = filter;
    let svgRoot = this.node;
    while (svgRoot && svgRoot.nodeName !== "svg") {
      svgRoot = svgRoot.parentNode;
    }
    if (svgRoot) {
      let defs = svgRoot.querySelector("defs");
      if (!defs) {
        defs = BrowserAPIs.createElementNS(SVGNS$2, "defs");
        svgRoot.insertBefore(defs, svgRoot.firstChild);
      }
      defs.appendChild(filter.node);
    }
    fn(new FilterBuilder(filter));
    this.attr("filter", "url(#" + filter._id + ")");
    return this;
  };
  ElementClass.prototype.unfilter = function(all) {
    if (this._filter) {
      this.node.removeAttribute("filter");
      if (all && this._filter.node && this._filter.node.parentNode) {
        this._filter.node.parentNode.removeChild(this._filter.node);
      }
      this._filter = null;
    }
    return this;
  };
  ElementClass.prototype.filterer = function() {
    return this._filter;
  };
}
function installDraggable(ElementClass) {
  ElementClass.prototype.draggable = function(opts) {
    if (opts === false) {
      if (this._dragCleanup) {
        this._dragCleanup();
        this._dragCleanup = null;
      }
      return this;
    }
    const el = this;
    const constraints = opts || {};
    const onPointerDown = (e2) => {
      if (e2.button && e2.button !== 0) return;
      e2.stopPropagation();
      const isTouch = e2.type === "touchstart";
      const ev = isTouch ? e2.touches[0] : e2;
      const svgEl = el.node;
      const startAttrX = parseFloat(svgEl.getAttribute("x")) || 0;
      const startAttrY = parseFloat(svgEl.getAttribute("y")) || 0;
      const startClientX = ev.clientX;
      const startClientY = ev.clientY;
      const svgRoot = svgEl.ownerSVGElement;
      let ctm = null;
      if (svgRoot) {
        ctm = svgRoot.getScreenCTM();
      }
      const onMove = (me) => {
        const mev = me.type === "touchmove" ? me.touches[0] : me;
        let dx = mev.clientX - startClientX;
        let dy = mev.clientY - startClientY;
        if (ctm) {
          dx = dx / ctm.a;
          dy = dy / ctm.d;
        }
        let newX = startAttrX + dx;
        let newY = startAttrY + dy;
        const w2 = parseFloat(svgEl.getAttribute("width")) || 0;
        const h2 = parseFloat(svgEl.getAttribute("height")) || 0;
        if (constraints.minX !== void 0 && newX < constraints.minX)
          newX = constraints.minX;
        if (constraints.minY !== void 0 && newY < constraints.minY)
          newY = constraints.minY;
        if (constraints.maxX !== void 0 && newX + w2 > constraints.maxX)
          newX = constraints.maxX - w2;
        if (constraints.maxY !== void 0 && newY + h2 > constraints.maxY)
          newY = constraints.maxY - h2;
        const box = {
          x: newX,
          y: newY,
          w: w2,
          h: h2,
          x2: newX + w2,
          y2: newY + h2
        };
        const event = new CustomEvent("dragmove", {
          detail: {
            handler: {
              /**
               * @param {number} x
               * @param {number} y
               */
              move: function(x, y) {
                svgEl.setAttribute("x", x);
                svgEl.setAttribute("y", y);
              }
            },
            box
          }
        });
        svgEl.dispatchEvent(event);
      };
      const onUp = () => {
        if (Environment.isBrowser()) {
          document.removeEventListener("mousemove", onMove);
          document.removeEventListener("touchmove", onMove);
          document.removeEventListener("mouseup", onUp);
          document.removeEventListener("touchend", onUp);
        }
        el._activeDrag = null;
      };
      if (Environment.isBrowser()) {
        document.addEventListener("mousemove", onMove);
        document.addEventListener("touchmove", onMove);
        document.addEventListener("mouseup", onUp);
        document.addEventListener("touchend", onUp);
        el._activeDrag = { onMove, onUp };
      }
    };
    el.node.addEventListener("mousedown", onPointerDown);
    el.node.addEventListener("touchstart", onPointerDown);
    el._dragCleanup = () => {
      el.node.removeEventListener("mousedown", onPointerDown);
      el.node.removeEventListener("touchstart", onPointerDown);
      if (el._activeDrag && Environment.isBrowser()) {
        document.removeEventListener("mousemove", el._activeDrag.onMove);
        document.removeEventListener("touchmove", el._activeDrag.onMove);
        document.removeEventListener("mouseup", el._activeDrag.onUp);
        document.removeEventListener("touchend", el._activeDrag.onUp);
        el._activeDrag = null;
      }
    };
    return el;
  };
}
function installSelectable(ElementClass) {
  ElementClass.prototype.select = function(opts) {
    if (opts === false) {
      if (this._selectCleanup) {
        this._selectCleanup();
        this._selectCleanup = null;
      }
      return this;
    }
    const el = this;
    const { createHandle, updateHandle } = opts;
    const handleGroup = document.createElementNS(SVGNS$2, "g");
    handleGroup.setAttribute("class", "svg_select_points");
    const parent = el.node.parentNode;
    if (parent) {
      parent.appendChild(handleGroup);
    }
    const handles = {};
    const handleNames = ["t", "b", "l", "r", "lt", "rt", "lb", "rb"];
    handleNames.forEach((name2, index) => {
      const subGroup = new SVGContainer(document.createElementNS(SVGNS$2, "g"));
      handleGroup.appendChild(subGroup.node);
      const handle = createHandle(subGroup, [0, 0], index, [], name2);
      handles[name2] = { group: subGroup, handle };
    });
    const updatePositions = () => {
      const x = parseFloat(el.attr("x")) || 0;
      const y = parseFloat(el.attr("y")) || 0;
      const w2 = parseFloat(el.attr("width")) || 0;
      const h2 = parseFloat(el.attr("height")) || 0;
      const elTransform = el.node.getAttribute("transform");
      if (elTransform) {
        handleGroup.setAttribute("transform", elTransform);
      } else {
        handleGroup.removeAttribute("transform");
      }
      const positions = {
        t: [x + w2 / 2, y],
        b: [x + w2 / 2, y + h2],
        l: [x, y + h2 / 2],
        r: [x + w2, y + h2 / 2],
        lt: [x, y],
        rt: [x + w2, y],
        lb: [x, y + h2],
        rb: [x + w2, y + h2]
      };
      handleNames.forEach((name2) => {
        if (handles[name2] && positions[name2]) {
          updateHandle(handles[name2].group, positions[name2]);
        }
      });
    };
    updatePositions();
    el._selectHandles = handleGroup;
    el._selectHandlesMap = handles;
    el._updateSelectPositions = updatePositions;
    el._selectCleanup = () => {
      if (handleGroup.parentNode) {
        handleGroup.parentNode.removeChild(handleGroup);
      }
      el._selectHandles = null;
      el._selectHandlesMap = null;
      el._updateSelectPositions = null;
    };
    return el;
  };
  ElementClass.prototype.resize = function(enable) {
    if (enable === false) {
      if (this._resizeCleanup) {
        this._resizeCleanup();
        this._resizeCleanup = null;
      }
      return this;
    }
    const el = this;
    const handles = el._selectHandlesMap;
    if (!handles) return el;
    const cleanupFns = [];
    const makeHandleDraggable = (name2) => {
      const handleInfo = handles[name2];
      if (!handleInfo || !handleInfo.group || !handleInfo.group.node) return;
      const handleNode = handleInfo.group.node;
      const onPointerDown = (e2) => {
        if (e2.button && e2.button !== 0) return;
        e2.stopPropagation();
        const isTouch = e2.type === "touchstart";
        const ev = isTouch ? e2.touches[0] : e2;
        const startClientX = ev.clientX;
        const svgRoot = el.node.ownerSVGElement;
        let ctm = null;
        if (svgRoot) {
          ctm = svgRoot.getScreenCTM();
        }
        const startX = parseFloat(el.attr("x")) || 0;
        const startW = parseFloat(el.attr("width")) || 0;
        const onMove = (me) => {
          const mev = me.type === "touchmove" ? me.touches[0] : me;
          let dx = mev.clientX - startClientX;
          if (ctm) dx = dx / /** @type {any} */
          ctm.a;
          let newX = startX;
          let newW = startW;
          if (name2 === "l") {
            newX = startX + dx;
            newW = startW - dx;
          } else if (name2 === "r") {
            newW = startW + dx;
          }
          if (newW < 0) {
            newW = 0;
          }
          el.attr({ x: newX, width: newW });
          if (el._updateSelectPositions) {
            el._updateSelectPositions();
          }
          const event = new CustomEvent("resize", {
            detail: { el }
          });
          el.node.dispatchEvent(event);
        };
        const onUp = () => {
          if (Environment.isBrowser()) {
            document.removeEventListener("mousemove", onMove);
            document.removeEventListener("touchmove", onMove);
            document.removeEventListener("mouseup", onUp);
            document.removeEventListener("touchend", onUp);
          }
          const event = new CustomEvent("resize", {
            detail: { el }
          });
          el.node.dispatchEvent(event);
        };
        if (Environment.isBrowser()) {
          document.addEventListener("mousemove", onMove);
          document.addEventListener("touchmove", onMove);
          document.addEventListener("mouseup", onUp);
          document.addEventListener("touchend", onUp);
        }
      };
      handleNode.addEventListener("mousedown", onPointerDown);
      handleNode.addEventListener("touchstart", onPointerDown);
      cleanupFns.push(() => {
        handleNode.removeEventListener("mousedown", onPointerDown);
        handleNode.removeEventListener("touchstart", onPointerDown);
      });
    };
    makeHandleDraggable("l");
    makeHandleDraggable("r");
    el._resizeCleanup = () => {
      cleanupFns.forEach((fn) => fn());
    };
    return el;
  };
}
installFilterMethods(SVGElement);
installAnimationMethods(SVGElement);
installDraggable(SVGElement);
installSelectable(SVGElement);
function SVG() {
  const svgEl = BrowserAPIs.createElementNS(SVGNS$2, "svg");
  const svg = new SVGContainer(svgEl);
  svg.attr({ xmlns: SVGNS$2 });
  return svg;
}
SVG.xlink = "http://www.w3.org/1999/xlink";
if (Environment.isBrowser() && typeof window.SVG === "undefined") {
  window.SVG = SVG;
}
if (Environment.isBrowser()) {
  if (typeof window.SVG === "undefined") {
    window.SVG = SVG;
  }
  if (typeof window.Apex === "undefined") {
    window.Apex = {};
  }
} else {
  if (typeof global !== "undefined") {
    if (typeof /** @type {any} */
    global.Apex === "undefined") {
      global.Apex = {};
    }
    if (typeof /** @type {any} */
    global.SVG === "undefined") {
      global.SVG = SVG;
    }
  }
}
const _InitCtxVariables = class _InitCtxVariables {
  /**
   * Register one or more optional feature modules.
   *
   * @param {Record<string, new (w: object, ctx: object) => unknown>} featureMap
   *   Plain object mapping ctx property name → constructor.
   *
   * Example (called from src/features/legend.js):
   *   InitCtxVariables.registerFeatures({ legend: Legend })
   */
  static registerFeatures(featureMap) {
    for (const [key, Ctor] of Object.entries(featureMap)) {
      _InitCtxVariables._featureRegistry.set(key, Ctor);
    }
  }
  /**
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(ctx) {
    this.ctx = ctx;
    this.w = ctx.w;
  }
  initModules() {
    this.ctx.publicMethods = [
      "updateOptions",
      "updateSeries",
      "appendData",
      "appendSeries",
      "isSeriesHidden",
      "highlightSeries",
      "toggleSeries",
      "showSeries",
      "hideSeries",
      "setLocale",
      "resetSeries",
      "zoomX",
      "toggleDataPointSelection",
      "dataURI",
      "exportToCSV",
      "addXaxisAnnotation",
      "addYaxisAnnotation",
      "addPointAnnotation",
      "clearAnnotations",
      "removeAnnotation",
      "drillDown",
      "drillUp",
      "drillToRoot",
      "clearDrilldownCache",
      "paper",
      "getActiveRenderer",
      "destroy"
    ];
    this.ctx.eventList = [
      "click",
      "mousedown",
      "mousemove",
      "mouseleave",
      "touchstart",
      "touchmove",
      "touchleave",
      "mouseup",
      "touchend",
      "keydown",
      "keyup"
    ];
    this.ctx.animations = new Animations(this.w, this.ctx);
    this.ctx.axes = new Axes(this.w, this.ctx);
    this.ctx.core = new Core(this.ctx.el, this.w, this.ctx);
    this.ctx.config = new Config({});
    this.ctx.data = new Data(this.w, {
      resetGlobals: () => this.ctx.core.resetGlobals(),
      isMultipleY: () => this.ctx.core.isMultipleY()
    });
    this.ctx.grid = new Grid(this.w, this.ctx);
    this.ctx.graphics = new Graphics(this.w, this.ctx);
    this.ctx.coreUtils = new CoreUtils(this.w);
    this.ctx.crosshairs = new Crosshairs(this.w);
    this.ctx.events = new Events(this.w, this.ctx);
    this.ctx.fill = new Fill(this.w);
    this.ctx.localization = new Localization(this.w);
    this.ctx.options = new Options();
    this.ctx.responsive = new Responsive(this.w);
    this.ctx.series = new Series(this.w, {
      // legend may not be registered — guard with ?.
      toggleDataSeries: (...a2) => {
        var _a;
        return (_a = this.ctx.legend) == null ? void 0 : _a.legendHelpers.toggleDataSeries(...a2);
      },
      revertDefaultAxisMinMax: () => this.ctx.updateHelpers.revertDefaultAxisMinMax(),
      updateSeries: (...a2) => this.ctx.updateHelpers._updateSeries(...a2)
    });
    this.ctx.theme = new Theme(this.w);
    this.ctx.formatters = new Formatters(this.w);
    this.ctx.titleSubtitle = new TitleSubtitle(this.w);
    this.ctx.dimensions = new Dimensions(this.w, this.ctx);
    this.ctx.updateHelpers = new UpdateHelpers(this.w, this.ctx);
    this.ctx.rendererController = new RendererController(this.w, this.ctx);
    this.ctx.renderer = this.ctx.rendererController.active;
    const tooltipInstance = new Tooltip(this.w, this.ctx);
    this.w.globals.tooltip = tooltipInstance;
    Object.defineProperty(this.ctx, "tooltip", {
      get() {
        return this.w.globals.tooltip;
      },
      configurable: true
    });
    this._initOptionalModules();
  }
  /**
   * Instantiate optional feature modules from the registry.
   *
   * Modules that are not registered are set to null on ctx so that call sites
   * can safely use optional-chaining (ctx.tooltip?.drawTooltip(...)).
   *
   * Lazy-getter features (toolbar, zoomPanSelection, keyboardNavigation) are
   * installed as on-demand getters so they are only constructed if accessed,
   * and only if their constructor was registered.
   */
  _initOptionalModules() {
    const reg = _InitCtxVariables._featureRegistry;
    const w2 = this.w;
    const ctx = this.ctx;
    const ExportsCtor = reg.get("exports");
    ctx.exports = ExportsCtor ? new ExportsCtor(w2, ctx) : null;
    const LegendCtor = reg.get("legend");
    ctx.legend = LegendCtor ? new LegendCtor(w2, ctx) : null;
    const MorphCtor = reg.get("morphTypeChange");
    ctx.morphTypeChange = MorphCtor ? new MorphCtor(w2, ctx) : null;
    const DrilldownCtor = reg.get("drilldown");
    ctx.drilldown = DrilldownCtor ? new DrilldownCtor(w2, ctx) : null;
    const PerspectivesCtor = reg.get("perspectives");
    ctx.perspectives = PerspectivesCtor ? new PerspectivesCtor(w2, ctx) : null;
    const StoryboardCtor = reg.get("storyboard");
    ctx.storyboard = StoryboardCtor ? new StoryboardCtor(w2, ctx) : null;
    const HistoryCtor = reg.get("history");
    ctx.history = HistoryCtor ? new HistoryCtor(w2, ctx) : null;
    const LinkedViewsCtor = reg.get("linkedViews");
    ctx.linkedViews = LinkedViewsCtor ? new LinkedViewsCtor(w2, ctx) : null;
    const InkCtor = reg.get("ink");
    ctx.ink = InkCtor ? new InkCtor(w2, ctx) : null;
    const MeasureCtor = reg.get("measure");
    ctx.measure = MeasureCtor ? new MeasureCtor(w2, ctx) : null;
    const ContextMenuCtor = reg.get("contextMenu");
    ctx.contextMenu = ContextMenuCtor ? new ContextMenuCtor(w2, ctx) : null;
    const WeaveCtor = reg.get("weave");
    ctx.weave = WeaveCtor ? new WeaveCtor(w2, ctx) : null;
    const OSThemeCtor = reg.get("osThemeWatcher");
    ctx.osThemeWatcher = OSThemeCtor ? new OSThemeCtor(w2, ctx) : null;
    const ToolbarCtor = reg.get("toolbar");
    Object.defineProperty(ctx, "toolbar", {
      get() {
        var _a;
        if (!this._toolbar && ToolbarCtor)
          this._toolbar = new ToolbarCtor(w2, this);
        return (_a = this._toolbar) != null ? _a : null;
      },
      configurable: true
    });
    const ZoomPanCtor = reg.get("zoomPanSelection");
    Object.defineProperty(ctx, "zoomPanSelection", {
      get() {
        var _a;
        if (!this._zoomPanSelection && ZoomPanCtor)
          this._zoomPanSelection = new ZoomPanCtor(w2, this);
        return (_a = this._zoomPanSelection) != null ? _a : null;
      },
      configurable: true
    });
    const KeyboardCtor = reg.get("keyboardNavigation");
    Object.defineProperty(ctx, "keyboardNavigation", {
      get() {
        var _a;
        if (!this._keyboardNavigation && KeyboardCtor)
          this._keyboardNavigation = new KeyboardCtor(w2, this);
        return (_a = this._keyboardNavigation) != null ? _a : null;
      },
      configurable: true
    });
  }
};
/**
 * Registry of optional feature modules.
 *
 * Populated by ApexCharts.registerFeatures() (called from feature entry
 * files such as src/features/legend.js). Keys match the ctx property name
 * the module is stored under (e.g. 'legend', 'exports').
 *
 * Core modules that every chart needs are NOT in this registry — they are
 * always instantiated unconditionally in initModules().
 */
__publicField(_InitCtxVariables, "_featureRegistry", /* @__PURE__ */ new Map());
let InitCtxVariables = _InitCtxVariables;
class Destroy {
  /**
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(ctx) {
    this.ctx = ctx;
    this.w = ctx.w;
  }
  /**
   * @param {{ isUpdating: boolean }} opts
   */
  clear({ isUpdating }) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    (_a = this.ctx.weave) == null ? void 0 : _a.teardown(isUpdating);
    if (!isUpdating) {
      this.w.globals.isDestroyed = true;
    }
    if (this.ctx._zoomPanSelection) {
      this.ctx._zoomPanSelection.destroy();
    }
    if (this.ctx._toolbar) {
      this.ctx._toolbar.destroy();
    }
    if (this.w.globals.resizeObserver && typeof this.w.globals.resizeObserver.disconnect === "function") {
      this.w.globals.resizeObserver.disconnect();
      this.w.globals.resizeObserver = null;
    }
    PerformanceCache.invalidateAll(this.w);
    if (isUpdating) {
      this.ctx._zoomPanSelection = null;
      this.ctx._toolbar = null;
      (_b = this.ctx._keyboardNavigation) == null ? void 0 : _b.destroy();
      this.ctx._keyboardNavigation = null;
    } else {
      (_c = this.ctx.perspectives) == null ? void 0 : _c.teardown();
      this.ctx.perspectives = null;
      (_d = this.ctx.storyboard) == null ? void 0 : _d.teardown();
      this.ctx.storyboard = null;
      (_e = this.ctx.history) == null ? void 0 : _e.teardown();
      this.ctx.history = null;
      (_f = this.ctx.linkedViews) == null ? void 0 : _f.teardown();
      this.ctx.linkedViews = null;
      (_g = this.ctx.ink) == null ? void 0 : _g.teardown();
      this.ctx.ink = null;
      (_h = this.ctx.measure) == null ? void 0 : _h.teardown();
      this.ctx.measure = null;
      (_i = this.ctx.contextMenu) == null ? void 0 : _i.teardown();
      this.ctx.contextMenu = null;
      (_j = this.ctx.osThemeWatcher) == null ? void 0 : _j.teardown();
      this.ctx.osThemeWatcher = null;
      this.ctx.weave = null;
      (_l = (_k = this.ctx.rendererController) == null ? void 0 : _k.teardown) == null ? void 0 : _l.call(_k);
      this.ctx.rendererController = null;
      this.ctx.renderer = null;
      this.ctx.drilldown = null;
      this.ctx.morphTypeChange = null;
      this.ctx.exports = null;
      this.ctx.animations = null;
      this.ctx.axes = null;
      this.ctx.annotations = null;
      this.ctx.core = null;
      this.ctx.data = null;
      this.ctx.grid = null;
      this.ctx.series = null;
      this.ctx.responsive = null;
      this.ctx.theme = null;
      this.ctx.formatters = null;
      this.ctx.titleSubtitle = null;
      this.ctx.legend = null;
      this.ctx.dimensions = null;
      this.ctx.options = null;
      this.ctx.crosshairs = null;
      this.ctx._zoomPanSelection = null;
      this.ctx.updateHelpers = null;
      this.ctx._toolbar = null;
      this.ctx.localization = null;
      this.ctx._keyboardNavigation = null;
      this.ctx.w.globals.tooltip = null;
    }
    this.clearDomElements({ isUpdating });
  }
  /**
   * @param {any} draw
   */
  killSVG(draw) {
    draw.each(
      /** @this {any} */
      function() {
        this.removeClass("*");
        this.off();
      },
      true
    );
    draw.clear();
  }
  /**
   * @param {{ isUpdating: boolean }} opts
   */
  clearDomElements({ isUpdating }) {
    const domEls = (
      /** @type {any} */
      this.w.dom
    );
    if (Environment.isBrowser() && domEls.Paper) {
      const elSVG = domEls.Paper.node;
      if (elSVG.parentNode && elSVG.parentNode.parentNode && !isUpdating) {
        elSVG.parentNode.parentNode.style.minHeight = "unset";
      }
      const baseEl = domEls.baseEl;
      if (baseEl) {
        this.ctx.eventList.forEach((event) => {
          baseEl.removeEventListener(event, this.ctx.events.documentEvent);
        });
      }
      if (this.ctx.el !== null) {
        while (this.ctx.el.firstChild) {
          this.ctx.el.removeChild(this.ctx.el.firstChild);
        }
      }
      this.killSVG(domEls.Paper);
      domEls.Paper.remove();
    }
    domEls.Paper = null;
    domEls.elWrap = null;
    domEls.elGraphical = null;
    domEls.elLegendWrap = null;
    domEls.elLegendForeign = null;
    domEls.baseEl = null;
    domEls.elGridRect = null;
    domEls.elGridRectMask = null;
    domEls.elGridRectBarMask = null;
    domEls.elGridRectMarkerMask = null;
    domEls.elForecastMask = null;
    domEls.elNonForecastMask = null;
    domEls.elDefs = null;
  }
}
const LAYOUT_KEY = "__apexcharts_unit_layouts__";
if (!/** @type {any} */
globalThis[LAYOUT_KEY]) {
  globalThis[LAYOUT_KEY] = {};
}
function getLayouts() {
  return (
    /** @type {any} */
    globalThis[LAYOUT_KEY]
  );
}
function registerUnitLayout(name2, fn) {
  if (!name2 || typeof name2 !== "string") {
    console.warn("ApexCharts: registerUnitLayout requires a non-empty name.");
    return;
  }
  if (typeof fn !== "function") {
    console.warn(
      `ApexCharts: registerUnitLayout("${name2}") expects a function (objects, rect) => [{id, x, y}].`
    );
    return;
  }
  getLayouts()[name2] = fn;
}
function getUnitLayout(name2) {
  if (!name2) return null;
  return getLayouts()[name2] || null;
}
function unregisterUnitLayout(name2) {
  delete getLayouts()[name2];
}
const ROW_SOURCE_KEY = "__apexcharts_row_sources__";
if (!/** @type {any} */
globalThis[ROW_SOURCE_KEY]) {
  globalThis[ROW_SOURCE_KEY] = {};
}
function getSources() {
  return (
    /** @type {any} */
    globalThis[ROW_SOURCE_KEY]
  );
}
function registerRowSource(name2, fn) {
  if (!name2 || typeof name2 !== "string") {
    console.warn("ApexCharts: registerRowSource requires a non-empty name.");
    return;
  }
  if (typeof fn !== "function") {
    console.warn(
      `ApexCharts: registerRowSource("${name2}") expects a function (w, opts) => series.`
    );
    return;
  }
  getSources()[name2] = fn;
}
function getRowSource(name2) {
  if (!name2) return null;
  return getSources()[name2] || null;
}
function unregisterRowSource(name2) {
  delete getSources()[name2];
}
function rowSourceFor(w2) {
  const cnf = w2 && w2.config && w2.config.chart;
  if (!cnf) return null;
  return getRowSource(cnf.requestedType) || getRowSource(cnf.type);
}
const REGISTRY_KEY = "__apexcharts_plugins__";
function getRegistry() {
  const g2 = (
    /** @type {any} */
    globalThis
  );
  if (!g2[REGISTRY_KEY]) g2[REGISTRY_KEY] = {};
  return g2[REGISTRY_KEY];
}
function registerPlugin(def) {
  if (!def || typeof def.name !== "string" || typeof def.setup !== "function") {
    console.error(
      "[apexcharts] registerPlugin: a plugin needs a { name, setup } shape."
    );
    return;
  }
  getRegistry()[def.name] = def;
}
function getPlugin(name2) {
  return getRegistry()[name2] || null;
}
function unregisterPlugin(name2) {
  delete getRegistry()[name2];
}
const ros = /* @__PURE__ */ new WeakMap();
function addResizeListener(el, fn) {
  if (Environment.isSSR()) return;
  let called = false;
  if (el.nodeType !== Node.DOCUMENT_FRAGMENT_NODE) {
    const elRect = el.getBoundingClientRect();
    if (el.style.display === "none" || elRect.width === 0) {
      called = true;
    }
  }
  const ro = new ResizeObserver((r2) => {
    if (called) {
      fn.call(el, r2);
    }
    called = true;
  });
  if (el.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
    Array.from(el.children).forEach((c2) => ro.observe(c2));
  } else {
    ro.observe(el);
  }
  ros.set(fn, ro);
}
function removeResizeListener(el, fn) {
  if (Environment.isSSR()) return;
  const ro = ros.get(fn);
  if (ro) {
    ro.disconnect();
    ros.delete(fn);
  }
}
const apexCSS = '@keyframes opaque {\n  0% {\n    opacity: 0\n  }\n\n  to {\n    opacity: 1\n  }\n}\n\n@keyframes resizeanim {\n\n  0%,\n  to {\n    opacity: 0\n  }\n}\n\n.apexcharts-canvas {\n  position: relative;\n  direction: ltr !important;\n  user-select: none;\n  /* Focus indicator colour. Themes override below. */\n  --apexcharts-focus-color: #008FFB;\n}\n\n/* Dark theme & high-contrast: brighter focus colour for sufficient contrast. */\n.apexcharts-canvas .apexcharts-theme-dark,\n.apexcharts-theme-dark.apexcharts-canvas {\n  --apexcharts-focus-color: #FFD500;\n}\n.apexcharts-canvas.apexcharts-high-contrast,\n.apexcharts-high-contrast.apexcharts-canvas {\n  --apexcharts-focus-color: #FFFF00;\n}\n\n/* Visually-hidden aria-live status region (WCAG 4.1.3 Status Messages). */\n.apexcharts-sr-status {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  padding: 0;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0, 0, 0, 0);\n  white-space: nowrap;\n  border: 0;\n}\n\n/* Respect OS-level reduced-motion preference (WCAG 2.3.3). */\n@media (prefers-reduced-motion: reduce) {\n  .apexcharts-canvas *,\n  .apexcharts-canvas *::before,\n  .apexcharts-canvas *::after {\n    animation-duration: 0.01ms !important;\n    animation-iteration-count: 1 !important;\n    transition-duration: 0.01ms !important;\n  }\n}\n\n.apexcharts-canvas ::-webkit-scrollbar {\n  -webkit-appearance: none;\n  width: 6px\n}\n\n.apexcharts-canvas ::-webkit-scrollbar-thumb {\n  border-radius: 4px;\n  background-color: rgba(0, 0, 0, .5);\n  box-shadow: 0 0 1px rgba(255, 255, 255, .5);\n  -webkit-box-shadow: 0 0 1px rgba(255, 255, 255, .5)\n}\n\n.apexcharts-inner {\n  position: relative\n}\n\n.apexcharts-text tspan {\n  font-family: inherit\n}\n\nrect.legend-mouseover-inactive,\n.legend-mouseover-inactive rect,\n.legend-mouseover-inactive path,\n.legend-mouseover-inactive circle,\n.legend-mouseover-inactive line,\n.legend-mouseover-inactive text.apexcharts-yaxis-title-text,\n.legend-mouseover-inactive text.apexcharts-yaxis-label {\n  transition: .15s ease all;\n  opacity: .2\n}\n\n/* Linked Views (#4): per-mark crossfilter dim. Applied to individual data\n   marks (not whole series) whose x is outside the brushed range. Opacity is\n   overridable per chart via the --apx-cf-dim custom property. */\n.apexcharts-crossfilter-dimmed {\n  transition: opacity .25s ease;\n  opacity: var(--apx-cf-dim, .2)\n}\n\n/* Linked Views (#4): default styling for the built-in crossfilter data table\n   (cf.dataTable). Deliberately light so host styles can override. */\n.apexcharts-cf-table {\n  border-collapse: collapse;\n  width: 100%;\n  font-size: 13px;\n}\n.apexcharts-cf-table caption {\n  caption-side: bottom;\n  text-align: right;\n  padding: 6px 2px;\n  font-size: 12px;\n  opacity: .7\n}\n.apexcharts-cf-table th,\n.apexcharts-cf-table td {\n  padding: 6px 10px;\n  text-align: left;\n  border-bottom: 1px solid rgba(0, 0, 0, .08)\n}\n.apexcharts-cf-table th {\n  font-weight: 600;\n  border-bottom-width: 2px\n}\n.apexcharts-cf-table tbody tr:hover {\n  background: rgba(99, 102, 241, .06)\n}\n\n/* Measure ruler (#18): measure / delta ruler.\n   Theme via these classes or the --apx-measure-* custom properties below\n   (config `chart.measure.colors` overrides both). The ruler group also carries\n   a direction class: apexcharts-measure-up | -down | -flat.\n   Element classes:\n     .apexcharts-measure-band     shaded span band\n     .apexcharts-measure-vline    vertical guide lines\n     .apexcharts-measure-line     free-mode diagonal line\n     .apexcharts-measure-label-bg readout box     .apexcharts-measure-label text\n   Colors are applied as SVG presentation attributes, so any rule you write on\n   these classes overrides them. */\n.apexcharts-canvas {\n  --apx-measure-up: #16a34a;\n  --apx-measure-down: #dc2626;\n  --apx-measure-neutral: #64748b;\n  --apx-measure-guide: #94a3b8;\n}\n.apexcharts-measure-capture {\n  cursor: crosshair;\n}\n\n/* Radial Actions (#chrome): right-click context menu. Theme via these classes\n   or the --apx-menu-* custom properties. */\n.apexcharts-canvas {\n  --apx-menu-bg: #ffffff;\n  --apx-menu-fg: #1e293b;\n  --apx-menu-border: #e2e8f0;\n  --apx-menu-hover: #f1f5f9;\n  --apx-menu-shadow: rgba(15, 23, 42, 0.18);\n}\n.apexcharts-context-menu {\n  min-width: 168px;\n  padding: 4px;\n  border-radius: 8px;\n  background: var(--apx-menu-bg);\n  border: 1px solid var(--apx-menu-border);\n  box-shadow: 0 6px 22px var(--apx-menu-shadow);\n  font-family: Helvetica, Arial, sans-serif;\n  font-size: 13px;\n  z-index: 20;\n  user-select: none;\n}\n.apexcharts-context-menu-item {\n  display: block;\n  width: 100%;\n  box-sizing: border-box;\n  text-align: left;\n  padding: 7px 12px;\n  border: 0;\n  border-radius: 5px;\n  background: transparent;\n  color: var(--apx-menu-fg);\n  font: inherit;\n  cursor: pointer;\n}\n.apexcharts-context-menu-item:hover,\n.apexcharts-context-menu-item--active {\n  background: var(--apx-menu-hover);\n}\n.apexcharts-context-menu-item:focus {\n  outline: none;\n}\n\n/* Ink Layer (#7): the floating note editor card, opened by clicking an\n   ink-managed annotation. Theme via these classes or the --apx-ink-* vars. */\n.apexcharts-canvas {\n  --apx-ink-card-bg: #ffffff;\n  --apx-ink-card-fg: #1e293b;\n  --apx-ink-card-border: #e2e8f0;\n  --apx-ink-card-hover: #f1f5f9;\n  --apx-ink-card-accent: #6366f1;\n  --apx-ink-card-shadow: rgba(15, 23, 42, 0.18);\n}\n.apexcharts-ink-card {\n  position: absolute;\n  z-index: 25;\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n  padding: 8px;\n  border-radius: 8px;\n  background: var(--apx-ink-card-bg);\n  border: 1px solid var(--apx-ink-card-border);\n  box-shadow: 0 6px 22px var(--apx-ink-card-shadow);\n  font-family: Helvetica, Arial, sans-serif;\n  font-size: 12px;\n  color: var(--apx-ink-card-fg);\n  user-select: none;\n}\n.apexcharts-ink-card-row {\n  display: flex;\n  align-items: center;\n  gap: 4px;\n}\n.apexcharts-ink-card input.apexcharts-ink-editor {\n  flex: 1 1 auto;\n  width: 150px;\n  min-width: 0;\n  box-sizing: border-box;\n  padding: 4px 6px;\n  font: inherit;\n  color: inherit;\n  background: transparent;\n  border: 1px solid var(--apx-ink-card-border);\n  border-radius: 5px;\n}\n.apexcharts-ink-card input.apexcharts-ink-editor:focus {\n  outline: none;\n  border-color: var(--apx-ink-card-accent);\n}\n.apexcharts-ink-btn {\n  flex: 0 0 auto;\n  width: 24px;\n  height: 24px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  padding: 0;\n  border: 0;\n  border-radius: 5px;\n  background: transparent;\n  color: inherit;\n  font: inherit;\n  font-size: 12px;\n  line-height: 1;\n  cursor: pointer;\n}\n.apexcharts-ink-btn:hover,\n.apexcharts-ink-btn--active {\n  background: var(--apx-ink-card-hover);\n}\n.apexcharts-ink-btn:focus-visible,\n.apexcharts-ink-swatch:focus-visible {\n  outline: 2px solid var(--apx-ink-card-accent);\n  outline-offset: 1px;\n}\n.apexcharts-ink-btn--bold {\n  font-weight: 700;\n}\n.apexcharts-ink-btn--delete:hover {\n  color: #dc2626;\n}\n.apexcharts-ink-swatch {\n  flex: 0 0 auto;\n  width: 16px;\n  height: 16px;\n  padding: 0;\n  border: 1px solid rgba(100, 116, 139, 0.45);\n  border-radius: 50%;\n  cursor: pointer;\n}\n.apexcharts-ink-swatch--active {\n  box-shadow:\n    0 0 0 2px var(--apx-ink-card-bg),\n    0 0 0 4px var(--apx-ink-card-accent);\n}\n.apexcharts-ink-sep {\n  flex: 0 0 auto;\n  width: 1px;\n  height: 16px;\n  margin: 0 2px;\n  background: var(--apx-ink-card-border);\n}\n.apexcharts-ink-cardlabel {\n  flex: 0 0 auto;\n  font-size: 10px;\n  letter-spacing: 0.4px;\n  text-transform: uppercase;\n  opacity: 0.65;\n  margin-right: 2px;\n}\n.apexcharts-ink-marker-size {\n  flex: 0 0 auto;\n  min-width: 16px;\n  text-align: center;\n  font-variant-numeric: tabular-nums;\n}\n\n.apexcharts-legend-text {\n  padding-left: 15px;\n  margin-left: -15px;\n}\n\n.apexcharts-legend-series[role="button"]:focus {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: 2px;\n}\n\n.apexcharts-legend-series[role="button"]:focus:not(:focus-visible) {\n  outline: none;\n}\n\n.apexcharts-legend-series[role="button"]:focus-visible {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: 2px;\n}\n\n.apexcharts-series-collapsed {\n  opacity: 0\n}\n\n.apexcharts-canvas svg:focus:not(:focus-visible) {\n  outline: none;\n}\n\n/* Keyboard navigation focus indicator on SVG data elements.\n   SVG elements don\'t support CSS outline, so we use stroke. */\n.apexcharts-bar-area.apexcharts-keyboard-focused,\n.apexcharts-candlestick-area.apexcharts-keyboard-focused,\n.apexcharts-boxPlot-area.apexcharts-keyboard-focused,\n.apexcharts-rangebar-area.apexcharts-keyboard-focused,\n.apexcharts-pie-area.apexcharts-keyboard-focused,\n.apexcharts-heatmap-rect.apexcharts-keyboard-focused,\n.apexcharts-treemap-rect.apexcharts-keyboard-focused {\n  stroke: var(--apexcharts-focus-color, #008FFB);\n  stroke-width: 2;\n  stroke-opacity: 1;\n}\n\n.apexcharts-tooltip {\n  --apx-tt-bg: #ffffff;\n  /* Shared by the body and the arrow\'s two outward facets, so the\n   * hairline reads as one continuous outline around the whole shape.\n   * Keep it strong enough to survive on its own: the shadow below is\n   * elevation, not edge definition. */\n  --apx-tt-border: rgba(15, 23, 42, 0.12);\n  /* Elevation, in three layers: a tight contact shadow that anchors the\n   * bottom edge, a directional key shadow for the lift, and a wide\n   * ambient one that grounds the whole box. Each is weaker and more\n   * diffuse than the last.\n   *\n   * A tooltip is unusual in that it floats over *data*, so reach costs\n   * more than it does on a page: every pixel the shadow travels tints a\n   * bar or a line the reader is trying to compare. These numbers are\n   * tuned to keep the near-edge contrast that reads as elevation while\n   * dropping the long low haze that only muddies the plot.\n   *\n   * Note there is deliberately no `0 0 0 1px` ring layer. That used to\n   * stand in for edge definition back when --apx-tt-border was barely\n   * visible; now that the border is a real hairline (and the arrow\n   * shares it) a ring only double-draws the outline, and being spread\n   * rather than offset it leaked ink upward too, flattening the lift.\n   *\n   * `--apx-tt-shadow-dir` flips the whole stack\'s Y in one place — see\n   * the `[data-placement="bottom"]` rule further down. */\n  --apx-tt-shadow-dir: 1;\n  --apx-tt-shadow: 0 calc(var(--apx-tt-shadow-dir) * 1px) 2px rgba(15, 23, 42, 0.06), 0 calc(var(--apx-tt-shadow-dir) * 4px) 8px -2px rgba(15, 23, 42, 0.10), 0 calc(var(--apx-tt-shadow-dir) * 12px) 20px -8px rgba(15, 23, 42, 0.14);\n  --apx-tt-arrow-bg: var(--apx-tt-bg);\n  --apx-tt-color: #0f172a;\n  --apx-tt-color-muted: rgba(15, 23, 42, 0.55);\n  border-radius: 8px;\n  background: var(--apx-tt-bg);\n  border: 1px solid var(--apx-tt-border);\n  box-shadow: var(--apx-tt-shadow);\n  color: var(--apx-tt-color);\n  cursor: default;\n  font-size: 13px;\n  left: 0;\n  top: 0;\n  opacity: 0;\n  pointer-events: none;\n  position: absolute;\n  display: flex;\n  flex-direction: column;\n  padding: 2px 0;\n  white-space: nowrap;\n  z-index: 12;\n  transition: opacity .12s ease\n}\n\n/* While the tooltip is visible, smoothly animate position changes\n * between data points. Kept short (160 ms) and ease-out so it stays\n * responsive — too long would feel laggy when sweeping across many\n * points fast. The position transition is only attached after the\n * first paint (Position.applyTooltipPosition flips `data-positioned`\n * once the tooltip has been placed) so the *first* show doesn\'t slide\n * the tooltip in from the previously-stale (0,0) coordinates. */\n.apexcharts-tooltip.apexcharts-active {\n  opacity: 1;\n  transition: opacity .12s ease\n}\n.apexcharts-tooltip.apexcharts-active[data-positioned="true"] {\n  transition: opacity .12s ease, left .16s ease-out, top .16s ease-out\n}\n\n.apexcharts-tooltip.apexcharts-theme-light {\n  /* defaults already set above; class kept for backward-compat selectors */\n}\n\n.apexcharts-tooltip.apexcharts-theme-dark {\n  --apx-tt-bg: #1c1c1f;\n  --apx-tt-border: rgba(255, 255, 255, 0.16);\n  /* Dark needs more alpha than light to register at all, but not as much\n   * as it used to: the light rim above now carries the edge, so the\n   * shadow is free to be pure elevation instead of doubling as an\n   * outline. Same geometry as light, heavier ink. */\n  --apx-tt-shadow: 0 calc(var(--apx-tt-shadow-dir) * 1px) 2px rgba(0, 0, 0, 0.24), 0 calc(var(--apx-tt-shadow-dir) * 4px) 8px -2px rgba(0, 0, 0, 0.30), 0 calc(var(--apx-tt-shadow-dir) * 12px) 20px -8px rgba(0, 0, 0, 0.38);\n  --apx-tt-color: #f3f4f6;\n  --apx-tt-color-muted: rgba(243, 244, 246, 0.55);\n}\n\n.apexcharts-tooltip * {\n  font-family: inherit\n}\n\n/* Point-annotation hover tooltip (apexcharts/apexcharts.js#2424). Reuses the\n * glass body/border/shadow from `.apexcharts-tooltip` but holds free-form\n * content, so it needs its own padding, wrapping and a sane max width. */\n.apexcharts-tooltip.apexcharts-annotation-tooltip {\n  padding: 6px 10px;\n  max-width: 240px;\n  white-space: normal;\n  line-height: 1.4;\n  pointer-events: none;\n  z-index: 13\n}\n\n.apexcharts-tooltip-title {\n  padding: 8px 12px 4px;\n  font-size: 12px;\n  font-weight: 600;\n  letter-spacing: 0.01em;\n  color: var(--apx-tt-color-muted);\n  background: transparent;\n  border-bottom: none;\n  margin-bottom: 0\n}\n\n.apexcharts-tooltip.apexcharts-theme-light .apexcharts-tooltip-title,\n.apexcharts-tooltip.apexcharts-theme-dark .apexcharts-tooltip-title {\n  background: transparent;\n  border-bottom: none\n}\n\n/* `fillSeriesColor`: each series-group already paints itself with the\n * series colour. Drop the glass body entirely (transparent bg, no\n * border, no backdrop-filter, no padding) and clip the coloured\n * series-group(s) to the tooltip\'s rounded corners so they fill the\n * shell edge-to-edge. Text inside the coloured group is forced to\n * white for contrast. */\n.apexcharts-tooltip.apexcharts-tooltip-fill-series {\n  background: transparent;\n  -webkit-backdrop-filter: none;\n  backdrop-filter: none;\n  border: none;\n  padding: 0;\n  overflow: hidden;\n  color: #fff\n}\n\n.apexcharts-tooltip.apexcharts-tooltip-fill-series .apexcharts-tooltip-title {\n  background: rgba(0, 0, 0, 0.22);\n  color: #fff;\n  opacity: 1;\n  padding: 6px 12px\n}\n\n.apexcharts-tooltip.apexcharts-tooltip-fill-series .apexcharts-tooltip-series-group {\n  color: #fff\n}\n\n/* Arrow connector — a 45°-rotated square straddling the body\'s edge, so\n * the body\'s 1px border runs continuously out across the arrow and back.\n * The two facets that face away from the tooltip carry the border; the\n * two that face into it carry none, and the square\'s opaque fill covers\n * the segment of the body\'s own border it sits on, hiding the seam.\n *\n * This is why it\'s a rotated square and not a triangle: `clip-path`\n * erases `border` and `box-shadow` along with everything outside the\n * polygon, which left `filter: drop-shadow` as the only way to suggest\n * an edge — and a drop-shadow can only ever blur one, never draw a\n * hairline. Nothing here needs a filter.\n *\n * Geometry: a square of side S rotated 45° reaches S/√2 from its centre\n * to each corner, so S = 10px gives the ~7px tip overhang that\n * ARROW_TIP_OVERHANG assumes (tooltip/constants.js) over a ~14px base.\n * The offsets park the square\'s *centre* 1px outside the padding box\n * (-6px = -1px border - 10px/2), i.e. exactly on the body\'s border line,\n * so the two borders meet end to end instead of overlapping or gapping.\n * `box-sizing` must be border-box or the bordered sides would grow the\n * square asymmetrically and knock its centre off that line. */\n.apexcharts-tooltip-arrow {\n  position: absolute;\n  box-sizing: border-box;\n  width: 10px;\n  height: 10px;\n  background: var(--apx-tt-arrow-bg);\n  transform: rotate(45deg);\n  pointer-events: none;\n  top: calc(var(--apx-tt-arrow-y, 50%) - 5px)\n}\n\n/* Which two sides face outward depends on the placement. Under\n * `rotate(45deg)` the square\'s bottom-left corner swings to the left,\n * top-right to the right, top-left to the top and bottom-right to the\n * bottom — so the pair of borders below is always the two sharing the\n * corner that ends up as the tip. */\n.apexcharts-tooltip[data-placement="right"] .apexcharts-tooltip-arrow {\n  left: -6px;\n  border-left: 1px solid var(--apx-tt-border);\n  border-bottom: 1px solid var(--apx-tt-border)\n}\n\n.apexcharts-tooltip[data-placement="left"] .apexcharts-tooltip-arrow {\n  right: -6px;\n  border-top: 1px solid var(--apx-tt-border);\n  border-right: 1px solid var(--apx-tt-border)\n}\n\n/* Vertical arrow variants: tooltip is above/below the data point and the\n * arrow points down/up. The base rule above uses `--apx-tt-arrow-y` for\n * left/right placement; for top/bottom we centre on `--apx-tt-arrow-x`\n * instead (set by applyTooltipPosition). */\n.apexcharts-tooltip[data-placement="top"] .apexcharts-tooltip-arrow,\n.apexcharts-tooltip[data-placement="bottom"] .apexcharts-tooltip-arrow {\n  top: auto;\n  left: calc(var(--apx-tt-arrow-x, 50%) - 5px)\n}\n\n.apexcharts-tooltip[data-placement="top"] .apexcharts-tooltip-arrow {\n  bottom: -6px;\n  border-right: 1px solid var(--apx-tt-border);\n  border-bottom: 1px solid var(--apx-tt-border)\n}\n\n.apexcharts-tooltip[data-placement="bottom"] .apexcharts-tooltip-arrow {\n  top: -6px;\n  border-top: 1px solid var(--apx-tt-border);\n  border-left: 1px solid var(--apx-tt-border)\n}\n\n/* When the tooltip is flipped below the data point, the default\n * downward-biased shadow leaves its top edge undefined. Negating the\n * direction casts the whole elevation upward instead, so the shadow\n * falls between the tooltip and the mark above it. One multiplier flips\n * all three layers together; the arrow needs no counterpart, since its\n * border doesn\'t depend on light direction. */\n.apexcharts-tooltip[data-placement="bottom"] {\n  --apx-tt-shadow-dir: -1\n}\n\n.apexcharts-tooltip-text-goals-value,\n.apexcharts-tooltip-text-y-value,\n.apexcharts-tooltip-text-z-value {\n  display: inline-block;\n  margin-left: 5px;\n  font-weight: 600\n}\n\n.apexcharts-tooltip-text-goals-label:empty,\n.apexcharts-tooltip-text-goals-value:empty,\n.apexcharts-tooltip-text-y-label:empty,\n.apexcharts-tooltip-text-y-value:empty,\n.apexcharts-tooltip-text-z-value:empty,\n.apexcharts-tooltip-title:empty {\n  display: none\n}\n\n.apexcharts-tooltip-text-goals-label,\n.apexcharts-tooltip-text-goals-value {\n  padding: 6px 0 5px\n}\n\n.apexcharts-tooltip-goals-group,\n.apexcharts-tooltip-text-goals-label,\n.apexcharts-tooltip-text-goals-value {\n  display: flex\n}\n\n.apexcharts-tooltip-text-goals-label:not(:empty),\n.apexcharts-tooltip-text-goals-value:not(:empty) {\n  margin-top: -6px\n}\n\n.apexcharts-tooltip-marker {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  position: relative;\n  width: 12px;\n  height: 12px;\n  margin-right: 6px;\n  vertical-align: middle;\n  color: inherit;\n}\n\n.apexcharts-tooltip-marker svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.apexcharts-tooltip-series-group {\n  padding: 4px 12px;\n  display: none;\n  gap: 8px;\n  text-align: left;\n  justify-content: left;\n  align-items: center\n}\n\n.apexcharts-tooltip-series-group.apexcharts-active .apexcharts-tooltip-marker {\n  opacity: 1\n}\n\n.apexcharts-tooltip-series-group.apexcharts-active:last-child,\n.apexcharts-tooltip-series-group:last-child {\n  padding-bottom: 8px\n}\n\n.apexcharts-tooltip-y-group {\n  padding: 6px 0 5px\n}\n\n.apexcharts-custom-tooltip,\n.apexcharts-tooltip-box {\n  padding: 4px 8px\n}\n\n.apexcharts-tooltip-boxPlot {\n  display: flex;\n  flex-direction: column-reverse\n}\n\n.apexcharts-tooltip-box>div {\n  margin: 4px 0\n}\n\n.apexcharts-tooltip-box span.value {\n  font-weight: 700\n}\n\n.apexcharts-tooltip-rangebar {\n  padding: 5px 8px\n}\n\n.apexcharts-tooltip-rangebar .category {\n  font-weight: 600;\n  color: #777\n}\n\n.apexcharts-tooltip-rangebar .series-name {\n  font-weight: 700;\n  display: block;\n  margin-bottom: 5px\n}\n\n/* X/Y axis tooltips — small popovers that label the crosshair on the\n * axes. Restyled to match the modern data-tooltip palette: solid white\n * body with a subtle border + soft drop-shadow, smaller font, rounded\n * corners. The arrows still use the CSS border-triangle technique\n * (cheap, crisp at small sizes); their colours flow from CSS variables\n * so light/dark themes only need one override per axis. */\n.apexcharts-xaxistooltip,\n.apexcharts-yaxistooltip {\n  --apx-axt-bg: #ffffff;\n  --apx-axt-border: rgba(15, 23, 42, 0.08);\n  --apx-axt-color: #0f172a;\n  --apx-axt-shadow: 0 4px 12px -4px rgba(15, 23, 42, 0.18), 0 1px 3px -1px rgba(15, 23, 42, 0.12);\n  opacity: 0;\n  pointer-events: none;\n  color: var(--apx-axt-color);\n  font-size: 12px;\n  font-weight: 500;\n  text-align: center;\n  border-radius: 6px;\n  position: absolute;\n  z-index: 10;\n  background: var(--apx-axt-bg);\n  border: 1px solid var(--apx-axt-border);\n  box-shadow: var(--apx-axt-shadow)\n}\n\n.apexcharts-xaxistooltip.apexcharts-theme-dark,\n.apexcharts-yaxistooltip.apexcharts-theme-dark {\n  --apx-axt-bg: #1c1c1f;\n  --apx-axt-border: rgba(255, 255, 255, 0.1);\n  --apx-axt-color: #f3f4f6;\n  --apx-axt-shadow: 0 4px 12px -4px rgba(0, 0, 0, 0.55), 0 1px 3px -1px rgba(0, 0, 0, 0.45)\n}\n\n.apexcharts-xaxistooltip {\n  padding: 4px 8px;\n  transition: .15s ease all\n}\n\n.apexcharts-xaxistooltip:after,\n.apexcharts-xaxistooltip:before {\n  left: 50%;\n  border: solid transparent;\n  content: " ";\n  height: 0;\n  width: 0;\n  position: absolute;\n  pointer-events: none\n}\n\n/* :before paints the 1px border outline of the triangle (slightly larger\n * than :after); :after sits inside and paints the fill — leaves a 1px\n * ring of :before visible at the edges. */\n.apexcharts-xaxistooltip:after {\n  border-color: transparent;\n  border-width: 5px;\n  margin-left: -5px\n}\n\n.apexcharts-xaxistooltip:before {\n  border-color: transparent;\n  border-width: 6px;\n  margin-left: -6px\n}\n\n.apexcharts-xaxistooltip-bottom:after,\n.apexcharts-xaxistooltip-bottom:before {\n  bottom: 100%\n}\n\n.apexcharts-xaxistooltip-top:after,\n.apexcharts-xaxistooltip-top:before {\n  top: 100%\n}\n\n.apexcharts-xaxistooltip-bottom:after {\n  border-bottom-color: var(--apx-axt-bg)\n}\n\n.apexcharts-xaxistooltip-bottom:before {\n  border-bottom-color: var(--apx-axt-border)\n}\n\n.apexcharts-xaxistooltip-top:after {\n  border-top-color: var(--apx-axt-bg)\n}\n\n.apexcharts-xaxistooltip-top:before {\n  border-top-color: var(--apx-axt-border)\n}\n\n.apexcharts-xaxistooltip.apexcharts-active {\n  opacity: 1;\n  transition: .15s ease all\n}\n\n.apexcharts-yaxistooltip {\n  padding: 3px 8px\n}\n\n.apexcharts-yaxistooltip:after,\n.apexcharts-yaxistooltip:before {\n  top: 50%;\n  border: solid transparent;\n  content: " ";\n  height: 0;\n  width: 0;\n  position: absolute;\n  pointer-events: none\n}\n\n.apexcharts-yaxistooltip:after {\n  border-color: transparent;\n  border-width: 5px;\n  margin-top: -5px\n}\n\n.apexcharts-yaxistooltip:before {\n  border-color: transparent;\n  border-width: 6px;\n  margin-top: -6px\n}\n\n.apexcharts-yaxistooltip-left:after,\n.apexcharts-yaxistooltip-left:before {\n  left: 100%\n}\n\n.apexcharts-yaxistooltip-right:after,\n.apexcharts-yaxistooltip-right:before {\n  right: 100%\n}\n\n.apexcharts-yaxistooltip-left:after {\n  border-left-color: var(--apx-axt-bg)\n}\n\n.apexcharts-yaxistooltip-left:before {\n  border-left-color: var(--apx-axt-border)\n}\n\n.apexcharts-yaxistooltip-right:after {\n  border-right-color: var(--apx-axt-bg)\n}\n\n.apexcharts-yaxistooltip-right:before {\n  border-right-color: var(--apx-axt-border)\n}\n\n.apexcharts-yaxistooltip.apexcharts-active {\n  opacity: 1\n}\n\n.apexcharts-yaxistooltip-hidden {\n  display: none\n}\n\n.apexcharts-xcrosshairs,\n.apexcharts-ycrosshairs {\n  pointer-events: none;\n  opacity: 0;\n  transition: .15s ease all\n}\n\n.apexcharts-xcrosshairs.apexcharts-active,\n.apexcharts-ycrosshairs.apexcharts-active {\n  opacity: 1;\n  transition: .15s ease all\n}\n\n.apexcharts-ycrosshairs-hidden {\n  opacity: 0\n}\n\n.apexcharts-selection-rect {\n  cursor: move\n}\n\n.svg_select_shape {\n  stroke-width: 1;\n  stroke-dasharray: 10 10;\n  stroke: black;\n  stroke-opacity: 0.1;\n  pointer-events: none;\n  fill: none;\n}\n\n.svg_select_handle {\n  stroke-width: 3;\n  stroke: black;\n  fill: none;\n}\n\n.svg_select_handle_r {\n  cursor: e-resize;\n}\n\n.svg_select_handle_l {\n  cursor: w-resize;\n}\n\n.apexcharts-svg.apexcharts-zoomable.hovering-zoom {\n  cursor: crosshair\n}\n\n.apexcharts-svg.apexcharts-zoomable.hovering-pan {\n  cursor: move\n}\n\n.apexcharts-menu-icon,\n.apexcharts-measure-icon,\n.apexcharts-pan-icon,\n.apexcharts-reset-icon,\n.apexcharts-selection-icon,\n.apexcharts-toolbar-custom-icon,\n.apexcharts-zoom-icon,\n.apexcharts-zoomin-icon,\n.apexcharts-zoomout-icon {\n  cursor: pointer;\n  /* WCAG 2.5.8 Target Size (Minimum): 24×24 CSS px hit target. */\n  width: 26px;\n  height: 24px;\n  line-height: 24px;\n  color: #6e8192;\n  text-align: center;\n  /* Reset native <button> chrome — these are styled via SVG icons. */\n  padding: 0;\n  margin: 0;\n  background: transparent;\n  border: 0;\n  border-radius: 5px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  transition: background-color .12s ease, color .12s ease;\n}\n\n.apexcharts-menu-icon svg,\n.apexcharts-measure-icon svg,\n.apexcharts-pan-icon svg,\n.apexcharts-reset-icon svg,\n.apexcharts-selection-icon svg,\n.apexcharts-zoom-icon svg,\n.apexcharts-zoomin-icon svg,\n.apexcharts-zoomout-icon svg {\n  width: 18px;\n  height: 18px;\n  fill: none;\n  stroke: currentColor;\n  stroke-width: 2;\n  stroke-linecap: round;\n  stroke-linejoin: round\n}\n\n.apexcharts-theme-dark .apexcharts-menu-icon,\n.apexcharts-theme-dark .apexcharts-measure-icon,\n.apexcharts-theme-dark .apexcharts-pan-icon,\n.apexcharts-theme-dark .apexcharts-reset-icon,\n.apexcharts-theme-dark .apexcharts-selection-icon,\n.apexcharts-theme-dark .apexcharts-toolbar-custom-icon,\n.apexcharts-theme-dark .apexcharts-zoom-icon,\n.apexcharts-theme-dark .apexcharts-zoomin-icon,\n.apexcharts-theme-dark .apexcharts-zoomout-icon {\n  color: #d4d6dc\n}\n\n.apexcharts-canvas .apexcharts-measure-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-pan-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-reset-zoom-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-selection-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-zoom-icon.apexcharts-selected {\n  background: rgba(0, 143, 251, 0.12);\n  color: #008ffb\n}\n\n.apexcharts-theme-light .apexcharts-menu-icon:hover,\n.apexcharts-theme-light .apexcharts-measure-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-pan-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-reset-icon:hover,\n.apexcharts-theme-light .apexcharts-selection-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-zoom-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-zoomin-icon:hover,\n.apexcharts-theme-light .apexcharts-zoomout-icon:hover {\n  background: rgba(15, 23, 42, 0.06);\n  color: #1f2937\n}\n\n.apexcharts-theme-dark .apexcharts-menu-icon:hover,\n.apexcharts-theme-dark .apexcharts-measure-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-pan-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-reset-icon:hover,\n.apexcharts-theme-dark .apexcharts-selection-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-zoom-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-zoomin-icon:hover,\n.apexcharts-theme-dark .apexcharts-zoomout-icon:hover {\n  background: rgba(255, 255, 255, 0.08);\n  color: #fff\n}\n\n.apexcharts-menu-icon,\n.apexcharts-selection-icon {\n  position: relative\n}\n\n.apexcharts-toolbar {\n  position: absolute;\n  z-index: 11;\n  display: inline-flex;\n  align-items: center;\n  gap: 1px;\n  padding: 3px;\n  border-radius: 8px;\n  background: rgba(255, 255, 255, 0.85);\n  backdrop-filter: blur(8px);\n  -webkit-backdrop-filter: blur(8px);\n}\n\n.apexcharts-theme-dark .apexcharts-toolbar {\n  background: rgba(28, 28, 31, 0.82);\n}\n\n.apexcharts-menu {\n  background: rgba(255, 255, 255, 0.95);\n  backdrop-filter: blur(8px);\n  -webkit-backdrop-filter: blur(8px);\n  position: absolute;\n  top: calc(100% + 4px);\n  border: 1px solid rgba(15, 23, 42, 0.08);\n  border-radius: 8px;\n  padding: 4px;\n  right: 0;\n  opacity: 0;\n  min-width: 120px;\n  transition: opacity .15s ease, transform .15s ease;\n  transform: translateY(-2px);\n  pointer-events: none;\n  box-shadow: 0 4px 16px -4px rgba(15, 23, 42, 0.12), 0 2px 4px -1px rgba(15, 23, 42, 0.06)\n}\n\n.apexcharts-menu.apexcharts-menu-open {\n  opacity: 1;\n  transform: translateY(0);\n  pointer-events: all\n}\n\n.apexcharts-menu-item {\n  padding: 6px 9px;\n  font-size: 12px;\n  border-radius: 5px;\n  cursor: pointer\n}\n\n.apexcharts-theme-light .apexcharts-menu-item:hover {\n  background: rgba(15, 23, 42, 0.06)\n}\n\n.apexcharts-theme-dark .apexcharts-menu {\n  background: rgba(28, 28, 31, 0.92);\n  border-color: rgba(255, 255, 255, 0.08);\n  color: #f3f4f6;\n  box-shadow: 0 4px 16px -4px rgba(0, 0, 0, 0.5), 0 2px 4px -1px rgba(0, 0, 0, 0.4)\n}\n\n.apexcharts-theme-dark .apexcharts-menu-item:hover {\n  background: rgba(255, 255, 255, 0.08)\n}\n\n@media screen and (min-width:768px) {\n  .apexcharts-canvas:hover .apexcharts-toolbar {\n    opacity: 1\n  }\n}\n\n/* Toolbar keyboard accessibility: show toolbar when any button inside it is focused */\n.apexcharts-toolbar:focus-within {\n  opacity: 1\n}\n\n/* Focus indicator for toolbar icon buttons */\n.apexcharts-menu-icon:focus-visible,\n.apexcharts-measure-icon:focus-visible,\n.apexcharts-pan-icon:focus-visible,\n.apexcharts-reset-icon:focus-visible,\n.apexcharts-selection-icon:focus-visible,\n.apexcharts-toolbar-custom-icon:focus-visible,\n.apexcharts-zoom-icon:focus-visible,\n.apexcharts-zoomin-icon:focus-visible,\n.apexcharts-zoomout-icon:focus-visible {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: 1px;\n  border-radius: 5px\n}\n\n/* Focus indicator for hamburger menu items */\n.apexcharts-menu-item:focus-visible {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: -2px;\n  background: #eee\n}\n\n.apexcharts-canvas .apexcharts-element-hidden,\n.apexcharts-datalabel.apexcharts-element-hidden,\n.apexcharts-hide .apexcharts-series-points {\n  opacity: 0;\n}\n\n.apexcharts-hidden-element-shown {\n  opacity: 1;\n  transition: 0.25s ease all;\n}\n\n.apexcharts-datalabel,\n.apexcharts-datalabel-label,\n.apexcharts-datalabel-value,\n.apexcharts-datalabels,\n.apexcharts-pie-label,\n.apexcharts-pie-name-label,\n.apexcharts-pie-name-label-group,\n.apexcharts-pie-label-connector {\n  cursor: default;\n  pointer-events: none\n}\n\n.apexcharts-pie-label-connector {\n  fill: none\n}\n\n.apexcharts-pie-label-delay {\n  opacity: 0;\n  animation-name: opaque;\n  animation-duration: .3s;\n  animation-fill-mode: forwards;\n  animation-timing-function: ease\n}\n\n.apexcharts-radialbar-label {\n  cursor: pointer;\n}\n\n.apexcharts-annotation-rect,\n.apexcharts-area-series .apexcharts-area,\n.apexcharts-gridline,\n.apexcharts-line,\n.apexcharts-point-annotation-label,\n.apexcharts-radar-series path:not(.apexcharts-marker),\n.apexcharts-radar-series polygon,\n.apexcharts-toolbar svg,\n.apexcharts-tooltip .apexcharts-marker,\n.apexcharts-xaxis-annotation-label,\n.apexcharts-yaxis-annotation-label,\n.apexcharts-zoom-rect,\n.no-pointer-events {\n  pointer-events: none\n}\n\n.apexcharts-tooltip-active .apexcharts-marker {\n  transition: .15s ease all\n}\n\n.apexcharts-radar-series .apexcharts-yaxis {\n  pointer-events: none;\n}\n\n.resize-triggers {\n  animation: 1ms resizeanim;\n  visibility: hidden;\n  opacity: 0;\n  height: 100%;\n  width: 100%;\n  overflow: hidden\n}\n\n.contract-trigger:before,\n.resize-triggers,\n.resize-triggers>div {\n  content: " ";\n  display: block;\n  position: absolute;\n  top: 0;\n  left: 0\n}\n\n.resize-triggers>div {\n  height: 100%;\n  width: 100%;\n  background: #eee;\n  overflow: auto\n}\n\n.contract-trigger:before {\n  overflow: hidden;\n  width: 200%;\n  height: 200%\n}\n\n.apexcharts-bar-goals-markers {\n  pointer-events: none\n}\n\n.apexcharts-bar-shadows {\n  pointer-events: none\n}\n\n.apexcharts-rangebar-goals-markers {\n  pointer-events: none\n}\n\n.apexcharts-drilldown-target {\n  cursor: pointer\n}\n\n.apexcharts-breadcrumb {\n  position: absolute;\n  z-index: 11;\n  display: inline-flex;\n  align-items: center;\n  gap: 2px;\n  font-size: 12px;\n  font-family: inherit;\n  padding: 2px 4px\n}\n\n.apexcharts-breadcrumb-item {\n  background: transparent;\n  border: none;\n  padding: 2px 6px;\n  border-radius: 3px;\n  font: inherit;\n  color: inherit;\n  cursor: pointer;\n  line-height: 1.2\n}\n\n.apexcharts-breadcrumb-item:hover:not(.apexcharts-breadcrumb-current) {\n  background: rgba(0, 0, 0, 0.08)\n}\n\n.apexcharts-breadcrumb-arrow {\n  margin-right: 4px;\n  font-weight: 600;\n  user-select: none\n}\n\n.apexcharts-breadcrumb-current {\n  cursor: default;\n  font-weight: 600;\n  opacity: 0.85\n}\n\n.apexcharts-breadcrumb-separator {\n  opacity: 0.5;\n  user-select: none\n}\n\n.apexcharts-theme-dark .apexcharts-breadcrumb-item:hover:not(.apexcharts-breadcrumb-current) {\n  background: rgba(255, 255, 255, 0.12)\n}\n\n.apexcharts-drilldown-loading {\n  position: absolute;\n  inset: 0;\n  z-index: 12;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 10px;\n  font-size: 13px;\n  font-family: inherit;\n  color: inherit;\n  background: rgba(255, 255, 255, 0.62);\n  /* The chart underneath stays interactive-looking but must not take clicks\n     while a level is resolving, or a second drill can start mid-fetch. */\n  cursor: progress\n}\n\n.apexcharts-drilldown-loading-spinner {\n  width: 26px;\n  height: 26px;\n  border-radius: 50%;\n  border: 2.5px solid rgba(0, 0, 0, 0.16);\n  border-top-color: rgba(0, 0, 0, 0.55);\n  animation: apexcharts-drilldown-spin 0.7s linear infinite\n}\n\n.apexcharts-drilldown-loading-text {\n  opacity: 0.8\n}\n\n.apexcharts-theme-dark .apexcharts-drilldown-loading {\n  background: rgba(30, 30, 30, 0.62)\n}\n\n.apexcharts-theme-dark .apexcharts-drilldown-loading-spinner {\n  border-color: rgba(255, 255, 255, 0.22);\n  border-top-color: rgba(255, 255, 255, 0.7)\n}\n\n@keyframes apexcharts-drilldown-spin {\n  to {\n    transform: rotate(360deg)\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .apexcharts-drilldown-loading-spinner {\n    animation: apexcharts-drilldown-pulse 1.4s ease-in-out infinite\n  }\n\n  @keyframes apexcharts-drilldown-pulse {\n    0%, 100% {\n      opacity: 0.35\n    }\n\n    50% {\n      opacity: 1\n    }\n  }\n}\n\n.apexcharts-disable-transitions * {\n  transition: none !important;\n}';
const e = globalThis.console;
function t(t2) {
  e.error(t2);
}
function s(t2) {
  e.warn(t2);
}
const i = "APEX-", n = /* @__PURE__ */ new Date("2027-07-31T00:00:00Z"), r = "__apex_license_v1__";
function a() {
  const e2 = globalThis;
  let t2 = e2[r];
  return t2 || (t2 = { key: null, listeners: /* @__PURE__ */ new Set(), result: null }, e2[r] = t2), t2;
}
const l = class {
  static get licenseKey() {
    return a().key;
  }
  static set licenseKey(e2) {
    a().key = e2;
  }
  static get listeners() {
    return a().listeners;
  }
  static get validationResult() {
    return a().result;
  }
  static set validationResult(e2) {
    a().result = e2;
  }
  static getKey() {
    return this.licenseKey;
  }
  static getLicenseStatus() {
    return this.licenseKey ? (this.validationResult = this.validateKey(this.licenseKey), this.validationResult) : { expired: false, signatureVerified: false, valid: false };
  }
  static isKeyValid(e2) {
    return !!e2 && this.validateKey(e2).valid;
  }
  static isLicenseValid() {
    return this.getLicenseStatus().valid;
  }
  static onChange(e2) {
    return this.listeners.add(e2), () => {
      this.listeners.delete(e2);
    };
  }
  static setLicense(e2) {
    var _a;
    var i2;
    if (!e2) return this.licenseKey = null, void this.publish({ expired: false, signatureVerified: false, valid: false });
    const n2 = this.validateKey(e2);
    n2.valid || e2 === this.licenseKey || !(null == (i2 = this.validationResult) ? void 0 : i2.valid) ? (this.licenseKey = e2, this.publish(n2), n2.valid || t(`[Apex] ${n2.message}`)) : s(`[Apex] Ignoring license key: ${(_a = n2.message) != null ? _a : "it is not valid"} A valid license is already active on this page.`);
  }
  static validateKey(e2) {
    const t2 = this.parseKey(e2), s2 = this.validateStructure(e2, t2);
    if (!s2.valid || !(null == t2 ? void 0 : t2.signature)) return s2;
    const i2 = this.verdicts.get(e2);
    return false === i2 ? { data: t2.data, expired: false, message: "Invalid license key. The license signature does not verify.", signatureVerified: true, valid: false } : (void 0 === i2 && this.verifySignature(e2, t2, s2), __spreadProps(__spreadValues({}, s2), { signatureVerified: true === i2 }));
  }
  static _resetSignatureState() {
    this.verdicts.clear(), this.verifying.clear(), this.warnedUnverifiable = false, this.epoch++;
  }
  static base64ToBytes(e2) {
    const t2 = e2.replace(/-/g, "+").replace(/_/g, "/"), s2 = t2.padEnd(4 * Math.ceil(t2.length / 4), "="), i2 = globalThis.atob;
    if ("function" != typeof i2) throw new Error("no base64 decoder available");
    const n2 = i2(s2), r2 = new Uint8Array(n2.length);
    for (let e3 = 0; e3 < n2.length; e3++) r2[e3] = n2.charCodeAt(e3);
    return r2;
  }
  static canonicalPayload(e2) {
    const t2 = e2.domains && e2.domains.length > 0 ? e2.domains.join(",") : "";
    return `v1|${e2.issueDate}|${e2.expiryDate}|${e2.plan}|${t2}`;
  }
  static notify(e2) {
    for (const t2 of this.listeners) try {
      t2(e2);
    } catch (e3) {
    }
  }
  static parseKey(e2) {
    if ("string" != typeof e2 || !e2.startsWith(i)) return null;
    const t2 = e2.slice(5);
    if (!t2) return null;
    try {
      const e3 = new TextDecoder().decode(this.base64ToBytes(t2)), s2 = JSON.parse(e3);
      return s2.issueDate && s2.expiryDate && s2.plan ? { data: { domains: Array.isArray(s2.domains) ? s2.domains : void 0, expiryDate: s2.expiryDate, issueDate: s2.issueDate, plan: s2.plan, valid: true }, signature: "string" == typeof s2.sig && s2.sig ? s2.sig : null } : null;
    } catch (e3) {
      return null;
    }
  }
  static publish(e2) {
    this.validationResult = e2, this.notify(e2);
  }
  static validateStructure(e2, t2) {
    const s2 = (e3) => ({ expired: false, message: e3, signatureVerified: false, valid: false });
    if ("string" != typeof e2 || !e2.startsWith(i)) return s2('Invalid license key format. License key must start with "APEX-".');
    if (!t2) return s2("Invalid license key. Unable to decode license data.");
    const { data: r2, signature: a2 } = t2;
    if (!a2 && /* @__PURE__ */ new Date() >= n) return s2("This license key is in the old unsigned format, which is no longer accepted. Please request a replacement key.");
    if (new Date(r2.expiryDate) < /* @__PURE__ */ new Date()) return { data: r2, expired: true, message: `License expired on ${r2.expiryDate}. Please renew your license.`, signatureVerified: false, valid: false };
    if (r2.domains && r2.domains.length > 0) {
      const e3 = "undefined" == typeof location ? "" : location.hostname;
      if (!r2.domains.some(((t3) => e3 === t3 || e3.endsWith(`.${t3}`)))) return { data: r2, expired: false, message: `License is not valid for this domain (${e3}). Allowed domains: ${r2.domains.join(", ")}.`, signatureVerified: false, valid: false };
    }
    return { data: r2, expired: false, signatureVerified: false, valid: true };
  }
  static verifySignature(e2, i2, n2) {
    return __async(this, null, function* () {
      var r2;
      if (this.verifying.has(e2) || this.verdicts.has(e2)) return;
      this.verifying.add(e2);
      const a2 = this.epoch, l2 = null == (r2 = globalThis.crypto) ? void 0 : r2.subtle;
      if (!l2 || 0 === this.publicKeysSpki.length) return this.verifying.delete(e2), void (this.warnedUnverifiable || (this.warnedUnverifiable = true, s(l2 ? "[Apex] No license signing key is configured in this build, so license signatures cannot be verified." : "[Apex] Web Crypto is unavailable (a secure context is required), so the license signature cannot be verified.")));
      const o2 = new TextEncoder().encode(this.canonicalPayload(i2.data));
      let c2 = false;
      for (const e3 of this.publicKeysSpki) {
        try {
          const t2 = yield l2.importKey("spki", this.base64ToBytes(e3), { name: "ECDSA", namedCurve: "P-256" }, false, ["verify"]);
          c2 = yield l2.verify({ hash: "SHA-256", name: "ECDSA" }, t2, this.base64ToBytes(i2.signature), o2);
        } catch (e4) {
          c2 = false;
        }
        if (c2) break;
      }
      if (this.verifying.delete(e2), this.epoch !== a2) return;
      if (this.verdicts.set(e2, c2), c2) {
        const t2 = __spreadProps(__spreadValues({}, n2), { signatureVerified: true });
        return void (this.licenseKey === e2 ? this.publish(t2) : this.notify(t2));
      }
      const h2 = "Invalid license key. The license signature does not verify.", d = { data: i2.data, expired: false, message: h2, signatureVerified: true, valid: false };
      this.licenseKey === e2 ? this.publish(d) : this.notify(d), t(`[Apex] ${h2}`);
    });
  }
};
l.publicKeysSpki = ["MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEQIaK9UMD6n0oR/FIy8QdL0uSzKMQlf1BB+tOrji4/WuHsyRNxeDhVykoSsNURozMi1xhmqWvBH1L//xIfugTPA=="], l.verdicts = /* @__PURE__ */ new Map(), l.verifying = /* @__PURE__ */ new Set(), l.warnedUnverifiable = false, l.epoch = 0;
let o = l;
const c = class {
  static applyStyles(e2) {
    Object.assign(e2.style, this.CRITICAL_STYLES, { backgroundImage: this.createWatermarkPattern(), backgroundRepeat: "repeat" });
  }
  static node(e2) {
    return e2 ? e2.querySelector(`[${this.WATERMARK_ATTR}]`) : null;
  }
  static add(e2, t2) {
    return e2 && "undefined" != typeof document ? (this.setManaged(e2, t2), this.paint(e2)) : null;
  }
  static exists(e2) {
    return !!this.node(e2);
  }
  static remove(e2, t2) {
    e2 && (this.setManaged(e2, t2), this.erase(e2));
  }
  static untrack(e2) {
    this.managed.delete(e2);
  }
  static createWatermarkPattern() {
    const e2 = this.WATERMARK_TEXT;
    return `url("data:image/svg+xml,${encodeURIComponent(`
      <svg xmlns="http://www.w3.org/2000/svg" width="300" height="200">
        <text
          x="50%"
          y="50%"
          dominant-baseline="middle"
          text-anchor="middle"
          font-family="-apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif"
          font-size="18"
          font-weight="600"
          fill="rgba(134, 134, 134, 0.1)"
          transform="rotate(-35, 100, 60)"
        >${e2}</text>
      </svg>
    `.trim())}")`;
  }
  static erase(e2) {
    var t2;
    null == (t2 = this.node(e2)) || t2.remove();
  }
  static paint(e2) {
    let t2 = this.node(e2);
    return t2 || (t2 = document.createElement("div"), t2.setAttribute(this.WATERMARK_ATTR, ""), e2.appendChild(t2)), this.applyStyles(t2), "function" == typeof getComputedStyle && "static" === getComputedStyle(e2).position && (e2.style.position = "relative"), t2;
  }
  static reconcile() {
    const e2 = o.isLicenseValid();
    for (const t2 of this.managed) t2.isConnected ? e2 ? this.erase(t2) : this.paint(t2) : this.managed.delete(t2);
  }
  static setManaged(e2, t2) {
    false !== (null == t2 ? void 0 : t2.manage) ? this.track(e2) : this.managed.delete(e2);
  }
  static track(e2) {
    this.managed.add(e2), this.subscribed || (this.subscribed = true, o.onChange((() => {
      this.reconcile();
    })));
  }
};
c.WATERMARK_ATTR = "data-apexcharts-watermark", c.WATERMARK_TEXT = "APEXCHARTS", c.ATTR = "data-apexcharts-watermark", c.CRITICAL_STYLES = { bottom: "0", display: "block", left: "0", msUserSelect: "none", opacity: "1", pointerEvents: "none", position: "absolute", right: "0", top: "0", userSelect: "none", visibility: "visible", webkitUserSelect: "none", zIndex: "10000" }, c.managed = /* @__PURE__ */ new Set(), c.subscribed = false;
let h = c;
const p = 0.05, m = 0.05, g = 1 / 45;
function v(e2, t2, s2, i2) {
  const n2 = { value: e2, velocity: 0, target: e2, stiffness: t2, damping: s2 };
  return n2;
}
function b(e2, t2) {
  if (t2 <= 0) return M(e2);
  let s2 = t2;
  for (; s2 > 0; ) {
    const t3 = Math.min(s2, g), i2 = -e2.stiffness * (e2.value - e2.target) - e2.damping * e2.velocity;
    e2.velocity += i2 * t3, e2.value += e2.velocity * t3, s2 -= t3;
  }
  return !!M(e2) && (e2.value = e2.target, e2.velocity = 0, true);
}
function w(e2, t2) {
  e2.target = t2;
}
function M(e2) {
  var _a, _b;
  const t2 = (_a = e2.restVelocity) != null ? _a : p, s2 = (_b = e2.restDisplacement) != null ? _b : m;
  return Math.abs(e2.velocity) < t2 && Math.abs(e2.value - e2.target) < s2;
}
const S = { crisp: [210, 26], gentle: [120, 20], snappy: [320, 30] };
function L(e2) {
  var _a;
  return (_a = S[e2 != null ? e2 : "crisp"]) != null ? _a : S.crisp;
}
const X = "__apexcharts_crossfilters__";
function Y(e2) {
  if (!Number.isFinite(e2)) return e2;
  const t2 = Number(e2.toPrecision(12));
  return Object.is(t2, -0) ? 0 : t2;
}
function Z(e2) {
  return "number" == typeof e2 && Number.isFinite(e2);
}
function J(e2) {
  if ("function" == typeof e2) return e2;
  if (e2 && "object" == typeof e2) {
    if ("string" == typeof e2.sum) {
      const t2 = e2.sum;
      return (e3) => e3.reduce(((e4, s2) => e4 + (Number(s2[t2]) || 0)), 0);
    }
    if ("string" == typeof e2.avg) {
      const t2 = e2.avg;
      return (e3) => e3.length ? e3.reduce(((e4, s2) => e4 + (Number(s2[t2]) || 0)), 0) / e3.length : 0;
    }
    if ("string" == typeof e2.min) {
      const t2 = e2.min;
      return (e3) => e3.length ? Math.min(...e3.map(((e4) => Number(e4[t2]) || 0))) : 0;
    }
    if ("string" == typeof e2.max) {
      const t2 = e2.max;
      return (e3) => e3.length ? Math.max(...e3.map(((e4) => Number(e4[t2]) || 0))) : 0;
    }
  }
  return (e3) => e3.length;
}
function G(e2, t2) {
  return "function" == typeof t2 ? e2.slice().sort(t2) : "asc" === t2 ? e2.slice().sort(((e3, t3) => e3 > t3 ? 1 : e3 < t3 ? -1 : 0)) : "desc" === t2 ? e2.slice().sort(((e3, t3) => e3 < t3 ? 1 : e3 > t3 ? -1 : 0)) : e2;
}
function ee(e2, t2) {
  if (!Z(e2)) return -1;
  const s2 = t2.length - 1;
  if (e2 < t2[0] || e2 > t2[s2]) return -1;
  if (e2 === t2[s2]) return s2 - 1;
  for (let i2 = 0; i2 < s2; i2++) if (e2 >= t2[i2] && e2 < t2[i2 + 1]) return i2;
  return -1;
}
function te(e2) {
  const t2 = [];
  for (let s2 = 0; s2 < e2.length - 1; s2++) t2.push(Y((e2[s2] + e2[s2 + 1]) / 2));
  return t2;
}
class se {
  constructor(e2, t2) {
    this.dims = /* @__PURE__ */ new Map(), this.listeners = /* @__PURE__ */ new Map(), this.id = e2, this.records = Array.isArray(t2) ? t2 : [];
  }
  static store() {
    const e2 = globalThis;
    return e2[X] || (e2[X] = /* @__PURE__ */ new Map()), e2[X];
  }
  static getOrCreate(e2) {
    if (!e2 || "string" != typeof e2.id) throw new Error("Crossfilter.getOrCreate requires an { id } string.");
    const t2 = se.store(), s2 = t2.get(e2.id);
    if (s2) return e2.records && s2.setRecords(e2.records), s2;
    const i2 = new se(e2.id, e2.records);
    return t2.set(e2.id, i2), i2;
  }
  static get(e2) {
    return se.store().get(e2) || null;
  }
  setRecords(e2) {
    return this.records = Array.isArray(e2) ? e2 : [], this.dims.forEach(((e3) => this.recomputeDomain(e3))), this.emit("records", this.state()), this.emit("change", this.state()), this;
  }
  registerDimension(e2, t2) {
    if (!t2 || "function" != typeof t2.dimension) throw new Error(`crossfilter.registerDimension("${e2}") needs a dimension function.`);
    const s2 = t2.type || (t2.bins ? "range" : "category"), i2 = { accessor: t2.dimension, reducer: J(t2.reduce), type: s2, bins: t2.bins, order: t2.order, filter: null, labels: [], edges: null, xLabels: [], yLabels: [] };
    return this.dims.set(e2, i2), this.recomputeDomain(i2), null != t2.filter && this.setFilterOn(i2, t2.filter), this;
  }
  hasDimension(e2) {
    return this.dims.has(e2);
  }
  removeDimension(e2) {
    const t2 = this.dims.get(e2), s2 = !!t2 && this.hasFilter(t2);
    return this.dims.delete(e2), s2 && this.emit("change", this.state()), this;
  }
  recomputeDomain(e2) {
    if ("matrix" === e2.type) {
      const t2 = (function(e3, t3, s2) {
        const i2 = /* @__PURE__ */ new Set(), n2 = /* @__PURE__ */ new Set(), r2 = [], a2 = [];
        for (let s3 = 0; s3 < e3.length; s3++) {
          const l2 = t3(e3[s3]);
          if (!l2) continue;
          const o2 = l2[0], c2 = l2[1];
          null == o2 || i2.has(o2) || (i2.add(o2), r2.push(o2)), null == c2 || n2.has(c2) || (n2.add(c2), a2.push(c2));
        }
        return { xLabels: G(r2, s2), yLabels: G(a2, s2) };
      })(this.records, e2.accessor, e2.order);
      return e2.xLabels = t2.xLabels, e2.yLabels = t2.yLabels, void (e2.edges = null);
    }
    if ("range" === e2.type) return e2.edges = (function(e3, t2, s2) {
      if (s2 && Array.isArray(s2.thresholds) && s2.thresholds.length >= 2) {
        const e4 = Array.from(new Set(s2.thresholds.filter(Z))).sort(((e5, t3) => e5 - t3));
        return e4.length >= 2 ? e4.map(Y) : [0, 1];
      }
      let i2 = 1 / 0, n2 = -1 / 0;
      for (let s3 = 0; s3 < e3.length; s3++) {
        const r3 = t2(e3[s3]);
        Z(r3) && (r3 < i2 && (i2 = r3), r3 > n2 && (n2 = r3));
      }
      if (i2 === 1 / 0) return [0, 1];
      if (i2 === n2) {
        const e4 = Math.abs(i2) > 0 ? Math.abs(i2) : 1;
        return [Y(i2), Y(i2 + e4)];
      }
      if (s2 && Z(s2.width) && s2.width > 0) {
        const e4 = s2.width, t3 = Math.floor(i2 / e4) * e4;
        let r3 = Math.ceil(n2 / e4) * e4;
        r3 <= t3 && (r3 = t3 + e4);
        const a3 = Math.max(1, Math.round((r3 - t3) / e4)), l3 = new Array(a3 + 1);
        for (let s3 = 0; s3 <= a3; s3++) l3[s3] = Y(t3 + s3 * e4);
        return l3;
      }
      const r2 = s2 && Z(s2.count) && s2.count >= 1 ? Math.floor(s2.count) : 30, a2 = (n2 - i2) / r2, l2 = new Array(r2 + 1);
      for (let e4 = 0; e4 <= r2; e4++) l2[e4] = Y(i2 + e4 * a2);
      return l2[r2] = Y(n2), l2;
    })(this.records, e2.accessor, e2.bins), void (e2.labels = te(e2.edges));
    if (e2.labels = (function(e3, t2, s2) {
      const i2 = /* @__PURE__ */ new Set(), n2 = [];
      for (let s3 = 0; s3 < e3.length; s3++) {
        const r2 = t2(e3[s3]);
        null != r2 && (i2.has(r2) || (i2.add(r2), n2.push(r2)));
      }
      return G(n2, s2);
    })(this.records, e2.accessor, e2.order), e2.edges = null, e2.filter instanceof Set) {
      const t2 = new Set(e2.labels);
      Array.from(e2.filter).forEach(((s2) => {
        t2.has(s2) || e2.filter.delete(s2);
      }));
    }
  }
  filter(e2, t2) {
    const s2 = this.dims.get(e2);
    return s2 ? (this.setFilterOn(s2, t2), this.emit("change", this.state()), this) : this;
  }
  toggleKey(e2, t2) {
    const s2 = this.dims.get(e2);
    if (!s2 || "category" !== s2.type) return this;
    s2.filter instanceof Set || (s2.filter = /* @__PURE__ */ new Set());
    const i2 = s2.filter;
    return i2.has(t2) ? i2.delete(t2) : i2.add(t2), 0 === i2.size && (s2.filter = null), this.emit("change", this.state()), this;
  }
  setFilterOn(e2, t2) {
    if (null == t2) return void (e2.filter = null);
    if ("range" === e2.type) {
      if (Array.isArray(t2) && 2 === t2.length && t2.every(Z)) {
        const [s3, i2] = t2;
        e2.filter = [Math.min(s3, i2), Math.max(s3, i2)];
      } else e2.filter = null;
      return;
    }
    const s2 = new Set(t2);
    e2.filter = s2.size ? s2 : null;
  }
  clear(e2) {
    const t2 = this.dims.get(e2);
    return t2 && (t2.filter = null), this.emit("change", this.state()), this;
  }
  reset() {
    return this.dims.forEach(((e2) => {
      e2.filter = null;
    })), this.emit("change", this.state()), this;
  }
  hasFilter(e2) {
    return null != e2.filter && (!(e2.filter instanceof Set) || e2.filter.size > 0);
  }
  passes(e2, t2) {
    if (!this.hasFilter(e2)) return true;
    const s2 = e2.accessor(t2);
    if (e2.filter instanceof Set) return e2.filter.has(s2);
    if (!Z(s2)) return false;
    const [i2, n2] = e2.filter;
    return s2 >= i2 && s2 <= n2;
  }
  filteredRecords(e2) {
    const t2 = [];
    return this.dims.forEach(((s2, i2) => {
      i2 !== e2 && this.hasFilter(s2) && t2.push(s2);
    })), 0 === t2.length ? this.records : this.records.filter(((e3) => t2.every(((t3) => this.passes(t3, e3)))));
  }
  filteredRows() {
    return this.filteredRecords(null);
  }
  aggregateFor(e2) {
    const t2 = this.dims.get(e2);
    if (!t2) return { type: "category", labels: [], values: [], keys: [] };
    const s2 = this.filteredRecords(e2);
    if ("matrix" === t2.type) {
      const e3 = new Map(t2.xLabels.map(((e4, t3) => [e4, t3]))), i3 = new Map(t2.yLabels.map(((e4, t3) => [e4, t3]))), n2 = t2.yLabels.map((() => t2.xLabels.map((() => []))));
      for (let r2 = 0; r2 < s2.length; r2++) {
        const a2 = t2.accessor(s2[r2]);
        if (!a2) continue;
        const l2 = e3.get(a2[0]), o2 = i3.get(a2[1]);
        null != l2 && null != o2 && n2[o2][l2].push(s2[r2]);
      }
      return { type: "matrix", xLabels: t2.xLabels.slice(), yLabels: t2.yLabels.slice(), matrix: n2.map(((e4) => e4.map(((e5) => t2.reducer(e5))))) };
    }
    if ("range" === t2.type) {
      const e3 = t2.edges || [0, 1], i3 = e3.length - 1, n2 = Array.from({ length: i3 }, (() => []));
      for (let i4 = 0; i4 < s2.length; i4++) {
        const r2 = ee(t2.accessor(s2[i4]), e3);
        r2 >= 0 && n2[r2].push(s2[i4]);
      }
      return { type: "range", labels: te(e3), values: n2.map(((e4) => t2.reducer(e4))), keys: n2.map(((t3, s3) => [e3[s3], e3[s3 + 1]])), edges: e3 };
    }
    const i2 = /* @__PURE__ */ new Map();
    t2.labels.forEach(((e3) => i2.set(e3, [])));
    for (let e3 = 0; e3 < s2.length; e3++) {
      const n2 = i2.get(t2.accessor(s2[e3]));
      n2 && n2.push(s2[e3]);
    }
    return { type: "category", labels: t2.labels.slice(), values: t2.labels.map(((e3) => t2.reducer(i2.get(e3) || []))), keys: t2.labels.slice() };
  }
  aggregateAll() {
    const e2 = {};
    return this.dims.forEach(((t2, s2) => {
      e2[s2] = this.aggregateFor(s2);
    })), e2;
  }
  state() {
    const e2 = {};
    return this.dims.forEach(((t2, s2) => {
      this.hasFilter(t2) && (e2[s2] = t2.filter instanceof Set ? Array.from(t2.filter) : t2.filter.slice());
    })), { filters: e2, filteredCount: this.filteredRows().length, total: this.records.length };
  }
  filterOf(e2) {
    const t2 = this.dims.get(e2);
    return t2 && this.hasFilter(t2) ? t2.filter instanceof Set ? new Set(t2.filter) : t2.filter.slice() : null;
  }
  on(e2, t2) {
    let s2 = this.listeners.get(e2);
    return s2 || (s2 = /* @__PURE__ */ new Set(), this.listeners.set(e2, s2)), s2.add(t2), () => this.off(e2, t2);
  }
  off(e2, t2) {
    var s2;
    return null == (s2 = this.listeners.get(e2)) || s2.delete(t2), this;
  }
  emit(e2, t2) {
    var s2;
    null == (s2 = this.listeners.get(e2)) || s2.forEach(((e3) => {
      try {
        e3(t2);
      } catch (e4) {
      }
    }));
  }
  static esc(e2) {
    return String(null == e2 ? "" : e2).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
  }
  resolveColumns(e2) {
    if (Array.isArray(e2) && e2.length) return e2.map(((e3) => "string" == typeof e3 ? { field: e3, label: e3 } : { field: e3.field, label: e3.label || e3.field, format: e3.format }));
    const t2 = this.records[0];
    return (t2 ? Object.keys(t2) : []).map(((e3) => ({ field: e3, label: e3 })));
  }
  tableHTML(e2, t2, s2) {
    const i2 = "<thead><tr>" + e2.map(((e3) => `<th>${se.esc(e3.label)}</th>`)).join("") + "</tr></thead>", n2 = "<tbody>" + t2.map(((t3) => "<tr>" + e2.map(((e3) => {
      const s3 = t3[e3.field], i3 = e3.format ? e3.format(s3, t3) : s3;
      return `<td>${se.esc(i3)}</td>`;
    })).join("") + "</tr>")).join("") + "</tbody>";
    return `<table class="apexcharts-cf-table">${`<caption>${t2.length} of ${s2} rows</caption>`}${i2}${n2}</table>`;
  }
  dataTable(e2, t2) {
    if (!e2) return { refresh() {
    }, destroy() {
    } };
    const s2 = t2 || {}, i2 = this.resolveColumns(s2.columns), n2 = s2.pageSize || 0, r2 = s2.page || 0, a2 = () => {
      const t3 = this.filteredRows(), s3 = n2 ? t3.slice(r2 * n2, r2 * n2 + n2) : t3;
      e2.innerHTML = this.tableHTML(i2, s3, t3.length);
    };
    a2();
    const l2 = this.on("change", a2);
    return { refresh: a2, destroy: () => {
      l2(), e2.innerHTML = "";
    } };
  }
  destroy() {
    se.store().delete(this.id), this.dims.clear(), this.listeners.clear(), this.records = [];
  }
}
const PRICING_URL = "https://apexcharts.com/pricing";
let _perspectivesTokenDecoded = false;
const enforced = /* @__PURE__ */ new Set();
function untrackChart(ctx) {
  enforced.delete(ctx);
}
function markPerspectivesTokenDecoded() {
  _perspectivesTokenDecoded = true;
  reevaluateLicenseAcrossCharts();
}
function premiumFeaturesInUse(w2, ctx) {
  const chart = w2 && w2.config && w2.config.chart || {};
  const used = [];
  if (chart.type === "unit") used.push("unit");
  if (ctx.storyboard && ctx.storyboard._used) used.push("storyboard");
  const link = chart.link;
  if (ctx.linkedViews && link && (link.enabled === true || typeof link.dimension === "function")) {
    used.push("link");
  }
  if (ctx.ink && chart.ink && chart.ink.enabled === true) used.push("ink");
  if (ctx.measure && chart.measure && chart.measure.enabled === true) {
    used.push("measure");
  }
  if (ctx.contextMenu && chart.contextMenu && chart.contextMenu.enabled === true) {
    used.push("context-menu");
  }
  if (ctx.perspectives && (ctx.perspectives._used || _perspectivesTokenDecoded)) {
    used.push("perspectives");
  }
  if (ctx.history && chart.history && chart.history.enabled === true) {
    used.push("history");
  }
  return used;
}
function resolveKey(w2) {
  const perChart = w2 && w2.config && w2.config.chart && w2.config.chart.license;
  if (perChart) return perChart;
  const singleton = o.getKey();
  if (singleton) return singleton;
  const apex = Environment.getApex();
  if (apex && apex.license) return apex.license;
  return null;
}
const PREMIUM_PLANS = /* @__PURE__ */ new Set(["premium", "enterprise"]);
function licensedForPremium(key) {
  if (!key) return false;
  const result = o.validateKey(key);
  if (!result.valid) return false;
  const plan = result.data && result.data.plan;
  return typeof plan === "string" && PREMIUM_PLANS.has(plan.toLowerCase());
}
function reinstateWatermark(ctx, elWrap) {
  const node = h.add(elWrap, { manage: false });
  if (!node || typeof MutationObserver === "undefined") return;
  if (ctx._wmNodeObserver && ctx._wmObservedNode === node) return;
  if (ctx._wmNodeObserver) ctx._wmNodeObserver.disconnect();
  const nodeObs = new MutationObserver(() => {
    const n2 = h.node(elWrap);
    if (!n2) return;
    nodeObs.disconnect();
    h.applyStyles(n2);
    nodeObs.takeRecords();
    nodeObs.observe(n2, { attributes: true, attributeFilter: ["style"] });
  });
  nodeObs.observe(node, { attributes: true, attributeFilter: ["style"] });
  ctx._wmNodeObserver = nodeObs;
  ctx._wmObservedNode = node;
}
function addWatermark(ctx, elWrap) {
  reinstateWatermark(ctx, elWrap);
  if (typeof MutationObserver === "undefined" || ctx._wmWrapObserver) return;
  const wrapObs = new MutationObserver(() => {
    if (!h.node(elWrap)) reinstateWatermark(ctx, elWrap);
  });
  wrapObs.observe(elWrap, { childList: true });
  ctx._wmWrapObserver = wrapObs;
}
function teardownWatermark(ctx, elWrap) {
  if (ctx._wmWrapObserver) {
    ctx._wmWrapObserver.disconnect();
    ctx._wmWrapObserver = null;
  }
  if (ctx._wmNodeObserver) {
    ctx._wmNodeObserver.disconnect();
    ctx._wmNodeObserver = null;
  }
  ctx._wmObservedNode = null;
  const wrap = elWrap || ctx.w && ctx.w.dom && ctx.w.dom.elWrap;
  if (wrap) h.remove(wrap, { manage: false });
}
function notifyTrial(ctx, key, features) {
  if (ctx._premiumLicenseNotified) return;
  ctx._premiumLicenseNotified = true;
  const many = features.length > 1;
  if (!key) {
    console.warn(
      `[ApexCharts] Premium feature${many ? "s" : ""} in use (${features.join(", ")}) without a license. Running in trial mode with a watermark. Get a license: ${PRICING_URL}`
    );
    return;
  }
  const result = o.validateKey(key);
  if (result.valid) {
    const plan = result.data && result.data.plan || "current";
    console.warn(
      `[ApexCharts] Premium feature${many ? "s" : ""} in use (${features.join(", ")}) require a Premium or Enterprise license; the ${plan} plan does not include ${many ? "them" : "it"}. Running in trial mode with a watermark. Upgrade: ${PRICING_URL}`
    );
    return;
  }
  if (key !== o.getKey()) {
    console.error(`[Apex] ${result.message}`);
  }
}
function enforceLicense(w2, ctx) {
  try {
    if (!Environment.isBrowser()) return;
    if (w2 && w2.globals && w2.globals.isDestroyed) {
      enforced.delete(ctx);
      return;
    }
    const elWrap = w2 && w2.dom && w2.dom.elWrap;
    if (!elWrap) return;
    const features = premiumFeaturesInUse(w2, ctx);
    if (features.length === 0) {
      enforced.delete(ctx);
      teardownWatermark(ctx, elWrap);
      return;
    }
    enforced.add(ctx);
    const key = resolveKey(w2);
    if (licensedForPremium(key)) {
      teardownWatermark(ctx, elWrap);
      return;
    }
    addWatermark(ctx, elWrap);
    notifyTrial(ctx, key, features);
  } catch (e2) {
  }
}
function reevaluateLicenseAcrossCharts() {
  if (!Environment.isBrowser()) return;
  const visited = /* @__PURE__ */ new Set();
  const apex = Environment.getApex();
  const instances = apex && apex._chartInstances;
  if (Array.isArray(instances)) {
    instances.forEach((entry) => {
      const chart = entry && entry.chart;
      if (chart && chart.w && !chart.w.globals.isDestroyed) {
        visited.add(chart);
        enforceLicense(chart.w, chart);
      }
    });
  }
  Array.from(enforced).forEach((ctx) => {
    const w2 = ctx && ctx.w;
    const elWrap = w2 && w2.dom && w2.dom.elWrap;
    if (!w2 || w2.globals.isDestroyed || !elWrap || elWrap.isConnected === false) {
      enforced.delete(ctx);
      return;
    }
    if (visited.has(ctx)) return;
    enforceLicense(w2, ctx);
  });
}
o.onChange(reevaluateLicenseAcrossCharts);
const _ApexCharts = class _ApexCharts {
  /**
   * Creates a new ApexCharts instance.
   *
   * @param {HTMLElement} el - The DOM element to render the chart into.
   * @param {ApexOptions} opts - Chart configuration options.
   */
  constructor(el, opts) {
    // Module properties set dynamically by InitCtxVariables.initModules().
    // Declared as typed class fields so @ts-check resolves them throughout the
    // class body without errors. Each field typed as `any` since the modules are
    // plain objects whose specific shapes are not yet typed.
    /** @type {any} */
    __publicField(this, "core");
    /** @type {any} */
    __publicField(this, "responsive");
    /** @type {any} */
    __publicField(this, "axes");
    /** @type {any} */
    __publicField(this, "grid");
    /** @type {any} */
    __publicField(this, "graphics");
    /** @type {any} */
    __publicField(this, "coreUtils");
    /** @type {any} */
    __publicField(this, "crosshairs");
    /** @type {any} */
    __publicField(this, "events");
    /** @type {any} */
    __publicField(this, "fill");
    /** @type {any} */
    __publicField(this, "localization");
    /** @type {any} */
    __publicField(this, "options");
    /** @type {any} */
    __publicField(this, "series");
    /** @type {any} */
    __publicField(this, "theme");
    /** @type {any} */
    __publicField(this, "formatters");
    /** @type {any} */
    __publicField(this, "titleSubtitle");
    /** @type {any} */
    __publicField(this, "dimensions");
    /** @type {any} */
    __publicField(this, "updateHelpers");
    /** @type {any} */
    __publicField(this, "tooltip");
    /** @type {any} */
    __publicField(this, "data");
    /** @type {any} */
    __publicField(this, "animations");
    /** @type {any} */
    __publicField(this, "exports");
    /** @type {any} */
    __publicField(this, "legend");
    /** @type {any} */
    __publicField(this, "toolbar");
    /** @type {any} */
    __publicField(this, "zoomPanSelection");
    /** @type {any} */
    __publicField(this, "keyboardNavigation");
    /** @type {any} */
    __publicField(this, "annotations");
    /** @type {any} */
    __publicField(this, "morphTypeChange");
    /** @type {any} */
    __publicField(this, "timeScale");
    /** @type {any} */
    __publicField(this, "_keyboardNavigation");
    /** @type {any} */
    __publicField(this, "_zoomPanSelection");
    /** @type {any} */
    __publicField(this, "windowResizeHandler");
    /** @type {any} */
    __publicField(this, "parentResizeHandler");
    /** @type {string[]} */
    __publicField(this, "publicMethods", []);
    /** @type {string[]} */
    __publicField(this, "eventList", []);
    /** @type {Promise<any> | null} */
    __publicField(this, "_renderPromise", null);
    /** @type {any} */
    __publicField(this, "config");
    /** @type {any} */
    __publicField(this, "perspectives");
    /** @type {any} */
    __publicField(this, "storyboard");
    /** @type {any} */
    __publicField(this, "history");
    /** @type {any} */
    __publicField(this, "linkedViews");
    /** @type {any} */
    __publicField(this, "ink");
    /** @type {any} */
    __publicField(this, "measure");
    /** @type {any} */
    __publicField(this, "contextMenu");
    /** @type {any} */
    __publicField(this, "weave");
    /** @type {any} */
    __publicField(this, "renderer");
    /** @type {any} */
    __publicField(this, "rendererController");
    this.opts = opts;
    this.ctx = this;
    this.w = new Base(opts).init();
    this.el = el;
    this.w.globals.cuid = Utils$1.randomId();
    this.w.globals.chartID = this.w.config.chart.id ? Utils$1.escapeString(this.w.config.chart.id) : this.w.globals.cuid;
    applyAnimationPolicy(this.w);
    const initCtx = new InitCtxVariables(this);
    initCtx.initModules();
    this.lastUpdateOptions = null;
    this._updateStats = { fast: 0, fastWithAxes: 0, full: 0 };
    this.create = this.create.bind(this);
    if (Environment.isBrowser()) {
      this.windowResizeHandler = this._windowResizeHandler.bind(this);
      this.parentResizeHandler = this._parentResizeCallback.bind(this);
    }
  }
  /**
   * Renders the chart. Must be called once after construction.
   *
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after mount.
   */
  render() {
    var _a, _b;
    if (!((_b = (_a = this.w) == null ? void 0 : _a.config) == null ? void 0 : _b.chart)) {
      return Promise.reject(
        new Error(
          "ApexCharts: chart configuration is missing or invalid. Ensure the options object includes a `chart` property."
        )
      );
    }
    if (this._renderPromise) return this._renderPromise;
    const renderPromise = new Promise((resolve, reject) => {
      var _a2;
      if (Utils$1.elementExists(this.el)) {
        if (typeof Apex._chartInstances === "undefined") {
          Apex._chartInstances = [];
        }
        if (this.w.config.chart.id) {
          Apex._chartInstances.push({
            id: this.w.globals.chartID,
            group: this.w.config.chart.group,
            chart: this
          });
        }
        this.setLocale(this.w.config.chart.defaultLocale);
        const beforeMount = this.w.config.chart.events.beforeMount;
        if (typeof beforeMount === "function") {
          beforeMount(this, this.w);
        }
        this.events.fireEvent("beforeMount", [this, this.w]);
        if (Environment.isBrowser()) {
          window.addEventListener("resize", this.windowResizeHandler);
          addResizeListener(
            /** @type {HTMLElement} */
            this.el.parentNode,
            this.parentResizeHandler
          );
          const rootNode = (
            /** @type {any} */
            this.el.getRootNode && this.el.getRootNode()
          );
          const inShadowRoot = Utils$1.is("ShadowRoot", rootNode);
          const doc = this.el.ownerDocument;
          let css = inShadowRoot ? rootNode.getElementById("apexcharts-css") : doc.getElementById("apexcharts-css");
          if (!css) {
            css = BrowserAPIs.createElementNS(
              "http://www.w3.org/1999/xhtml",
              "style"
            );
            css.id = "apexcharts-css";
            css.textContent = apexCSS;
            const nonce = ((_a2 = this.opts.chart) == null ? void 0 : _a2.nonce) || this.w.config.chart.nonce;
            if (nonce) {
              css.setAttribute("nonce", nonce);
            }
            if (inShadowRoot) {
              rootNode.prepend(css);
            } else if (this.w.config.chart.injectStyleSheet !== false) {
              doc.head.appendChild(css);
            }
          }
        }
        const graphData = this.create(this.w.config.series, {});
        if (!graphData) return resolve(this);
        this.mount(graphData).then(() => {
          if (typeof this.w.config.chart.events.mounted === "function") {
            this.w.config.chart.events.mounted(this, this.w);
          }
          this.events.fireEvent("mounted", [this, this.w]);
          resolve(graphData);
        }).catch((e2) => {
          var _a3, _b2;
          const enriched = e2 instanceof Error ? e2 : new Error(String(e2));
          const err = (
            /** @type {any} */
            enriched
          );
          err.chartId = (_b2 = (_a3 = this.w) == null ? void 0 : _a3.globals) == null ? void 0 : _b2.chartID;
          err.el = this.el;
          reject(enriched);
        });
      } else {
        reject(new Error("Element not found"));
      }
    });
    this._renderPromise = renderPromise;
    renderPromise.catch(() => {
      if (this._renderPromise === renderPromise) this._renderPromise = null;
    });
    return renderPromise;
  }
  /**
   * @param {any[]} ser
   * @param {object} opts
   */
  create(ser, opts) {
    var _a, _b, _c, _d, _e, _f;
    const w2 = this.w;
    if (!this.core) {
      const initCtx = new InitCtxVariables(this);
      initCtx.initModules();
    }
    const gl = this.w.globals;
    gl.noData = false;
    gl.animationEnded = false;
    if (!Utils$1.elementExists(this.el)) {
      gl.animationEnded = true;
      return null;
    }
    this.responsive.checkResponsiveConfig(opts);
    applyAnimationPolicy(w2);
    if (w2.config.xaxis.convertedCatToNumeric) {
      const defaults = new Defaults(w2.config);
      defaults.convertCatToNumericXaxis(w2.config, this.ctx);
    }
    this.core.setupElements();
    if (w2.config.chart.type === "treemap") {
      w2.config.grid.show = false;
      w2.config.yaxis[0].show = false;
    }
    if (gl.svgWidth === 0) {
      gl.animationEnded = true;
      return null;
    }
    let series = ser;
    ser.forEach((s2, realIndex) => {
      if (s2.hidden) {
        series = this.legend.legendHelpers.getSeriesAfterCollapsing({
          realIndex
        });
      }
    });
    const combo = CoreUtils.checkComboSeries(series, w2.config.chart.type);
    gl.comboCharts = combo.comboCharts;
    gl.comboBarCount = combo.comboBarCount;
    const allSeriesAreEmpty = series.every((s2) => s2.data && s2.data.length === 0);
    if (series.length === 0 || allSeriesAreEmpty && gl.collapsedSeries.length < 1) {
      this.series.handleNoData();
    }
    if (Environment.isBrowser()) {
      this.events.setupEventHandlers();
    }
    const parsedState = this.data.parseData(series);
    this._writeParsedSeriesData(parsedState.seriesData);
    this._writeParsedRangeData(parsedState.rangeData);
    this._writeParsedCandleData(parsedState.candleData);
    this._writeParsedLabelData(parsedState.labelData);
    this._writeParsedAxisFlags(parsedState.axisFlags);
    (_a = this.rendererController) == null ? void 0 : _a.resolve();
    (_b = this.weave) == null ? void 0 : _b.dispatch("afterParse");
    this.theme.init();
    const markers = new Markers(this.w, this);
    markers.setGlobalMarkerSize();
    this.formatters.setLabelFormatters();
    this.titleSubtitle.draw();
    if (!gl.noData || gl.collapsedSeries.length === w2.seriesData.series.length || w2.config.legend.showForSingleSeries) {
      (_c = this.legend) == null ? void 0 : _c.init();
    }
    this.series.hasAllSeriesEqualX();
    if (gl.axisCharts) {
      this.core.coreCalculations();
      if (w2.config.xaxis.type !== "category") {
        this.formatters.setLabelFormatters();
      }
      if (this.ctx.toolbar) {
        this.ctx.toolbar.minX = w2.globals.minX;
        this.ctx.toolbar.maxX = w2.globals.maxX;
      }
    }
    this.formatters.heatmapLabelFormatters();
    const coreUtils = new CoreUtils(this.w);
    coreUtils.getLargestMarkerSize();
    const layoutState = this.dimensions.plotCoords();
    this._writeLayoutCoords(layoutState.layout);
    const xyRatios = this.core.xySettings();
    (_d = this.weave) == null ? void 0 : _d.dispatch("afterScales", { xyRatios });
    this.grid.createGridMask();
    const elGraph = this.core.plotChartType(series, xyRatios);
    const dataLabels = new DataLabels(this.w, this);
    dataLabels.bringForward();
    if (w2.config.dataLabels.background.enabled) {
      dataLabels.dataLabelsBackground();
    }
    this.core.shiftGraphPosition();
    (_f = (_e = this.legend) == null ? void 0 : _e.heatmapGradientLegend) == null ? void 0 : _f.repositionToPlot();
    if (w2.globals.dataPoints > 50) {
      w2.dom.elWrap.classList.add("apexcharts-disable-transitions");
    }
    const dim = {
      plot: {
        left: w2.layout.translateX,
        top: w2.layout.translateY,
        width: w2.layout.gridWidth,
        height: w2.layout.gridHeight
      }
    };
    return {
      elGraph,
      xyRatios,
      dimensions: dim
    };
  }
  /**
   * @param {any} graphData
   */
  mount(graphData = null) {
    const me = this;
    const w2 = me.w;
    return new Promise((resolve, reject) => {
      var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
      if (me.el === null) {
        return reject(
          new Error("Not enough data to display or target element not found")
        );
      } else if (w2.globals.allSeriesCollapsed) {
        me.series.handleNoData();
      }
      me.grid = new Grid(me.w, me);
      const elgrid = me.grid.drawGrid();
      const AnnotationsCtor = InitCtxVariables._featureRegistry.get("annotations");
      me.annotations = AnnotationsCtor ? new AnnotationsCtor(me.w, {
        theme: me.theme,
        timeScale: me.timeScale
      }) : null;
      (_a = me.annotations) == null ? void 0 : _a.drawImageAnnos();
      (_b = me.annotations) == null ? void 0 : _b.drawTextAnnos();
      if (w2.config.grid.position === "back") {
        if (elgrid) {
          w2.dom.elGraphical.add(elgrid.el);
        }
        if ((_c = elgrid == null ? void 0 : elgrid.elGridBorders) == null ? void 0 : _c.node) {
          w2.dom.elGraphical.add(elgrid.elGridBorders);
        }
      }
      if (Array.isArray(graphData.elGraph)) {
        for (let g2 = 0; g2 < graphData.elGraph.length; g2++) {
          w2.dom.elGraphical.add(graphData.elGraph[g2]);
        }
      } else {
        w2.dom.elGraphical.add(graphData.elGraph);
      }
      if (w2.config.grid.position === "front") {
        if (elgrid) {
          w2.dom.elGraphical.add(elgrid.el);
        }
        if ((_d = elgrid == null ? void 0 : elgrid.elGridBorders) == null ? void 0 : _d.node) {
          w2.dom.elGraphical.add(elgrid.elGridBorders);
        }
      }
      if (w2.config.xaxis.crosshairs.position === "front") {
        me.crosshairs.drawXCrosshairs();
      }
      if (w2.config.yaxis[0].crosshairs.position === "front") {
        me.crosshairs.drawYCrosshairs();
      }
      if (w2.config.chart.type !== "treemap") {
        me.axes.drawAxis(w2.config.chart.type, elgrid);
      }
      const xAxis = new XAxis(this.w, this.ctx, elgrid);
      const yaxis = new YAxis(
        this.w,
        { theme: this.theme, timeScale: this.timeScale },
        elgrid
      );
      if (elgrid !== null) {
        xAxis.xAxisLabelCorrections();
        yaxis.setYAxisTextAlignments();
        w2.config.yaxis.map((yaxe, index) => {
          if (w2.globals.ignoreYAxisIndexes.indexOf(index) === -1) {
            yaxis.yAxisTitleRotate(index, yaxe.opposite);
          }
        });
      }
      (_e = me.annotations) == null ? void 0 : _e.drawAxesAnnotations();
      if (!w2.globals.noData) {
        if (Environment.isBrowser() && w2.config.tooltip.enabled && !w2.globals.noData) {
          (_f = me.w.globals.tooltip) == null ? void 0 : _f.drawTooltip(graphData.xyRatios);
        }
        if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.keyboard.enabled && w2.config.chart.accessibility.keyboard.navigation.enabled) {
          (_g = me.keyboardNavigation) == null ? void 0 : _g.init();
        }
        if (Environment.isBrowser() && w2.globals.axisCharts && (w2.axisFlags.isXNumeric || /** @type {Record<string,any>} */
        w2.config.xaxis.convertedCatToNumeric || w2.axisFlags.isRangeBar)) {
          if (w2.config.chart.zoom.enabled || w2.config.chart.selection && w2.config.chart.selection.enabled || // @ts-ignore — chart.pan is an internal toolbar config property
          w2.config.chart.pan && w2.config.chart.pan.enabled) {
            (_h = me.zoomPanSelection) == null ? void 0 : _h.init({
              xyRatios: graphData.xyRatios
            });
          }
        } else {
          const tools = w2.config.chart.toolbar.tools;
          const toolsArr = [
            "zoom",
            "zoomin",
            "zoomout",
            "selection",
            "pan",
            "reset"
          ];
          toolsArr.forEach((t2) => {
            tools[t2] = false;
          });
        }
        if (w2.config.chart.toolbar.show && !w2.globals.allSeriesCollapsed) {
          (_i = me.toolbar) == null ? void 0 : _i.createToolbar();
        }
      }
      (_j = me.weave) == null ? void 0 : _j.dispatch("draw", {
        pass: "full",
        xyRatios: graphData == null ? void 0 : graphData.xyRatios
      });
      if (w2.globals.memory.methodsToExec.length > 0) {
        w2.globals.memory.methodsToExec.forEach((fn) => {
          fn.method(fn.params, false, fn.context);
        });
      }
      if (!w2.globals.axisCharts && !w2.globals.noData) {
        me.core.resizeNonAxisCharts();
      }
      enforceLicense(w2, me);
      resolve(me);
    });
  }
  /**
   * Destroys the chart instance, removes all DOM elements and event listeners.
   * After calling this, the instance should not be used again.
   */
  destroy() {
    var _a;
    this._renderPromise = null;
    if (Environment.isBrowser()) {
      window.removeEventListener("resize", this.windowResizeHandler);
      removeResizeListener(
        /** @type {Element} */
        this.el.parentNode,
        this.parentResizeHandler
      );
      clearTimeout((_a = this.w.globals.resizeTimer) != null ? _a : void 0);
    }
    const chartID = this.w.config.chart.id;
    if (chartID && Array.isArray(Apex._chartInstances)) {
      Apex._chartInstances.forEach(
        (c2, i2) => {
          if (c2.id === Utils$1.escapeString(chartID)) {
            Apex._chartInstances.splice(i2, 1);
          }
        }
      );
    }
    if (this._keyboardNavigation) {
      this._keyboardNavigation.destroy();
    }
    teardownWatermark(this);
    untrackChart(this);
    new Destroy(this.ctx).clear({ isUpdating: false });
  }
  /**
   * Merges new options into the existing config and re-renders the chart.
   *
   * @param {ApexOptions} options - Partial config object merged with the existing config.
   * @param {boolean} [redraw=false] - When true, redraws the chart from scratch instead of animating from previous paths.
   * @param {boolean} [animate=true] - Whether to animate the update.
   * @param {boolean} [updateSyncedCharts=true] - Whether to propagate the update to charts in the same group.
   * @param {boolean} [overwriteInitialConfig=true] - When true, replaces the stored initial config used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */
  updateOptions(options2, redraw = false, animate = true, updateSyncedCharts = true, overwriteInitialConfig = true) {
    const w2 = this.w;
    if (options2 && "series" in options2 && !Array.isArray(options2.series)) {
      console.warn(
        "ApexCharts: updateOptions() ignored `series` because it is not an array."
      );
      options2 = __spreadValues({}, options2);
      delete options2.series;
    }
    w2.interact.selection = void 0;
    if (this.lastUpdateOptions) {
      if (Utils$1.shallowEqual(this.lastUpdateOptions, options2)) {
        return Promise.resolve(this);
      }
      if (options2.series && this.lastUpdateOptions.series && !_ApexCharts._optionsTooBigToCompare(options2)) {
        if (JSON.stringify(this.lastUpdateOptions.series) === JSON.stringify(options2.series)) {
          const optionsWithoutSeries = __spreadValues({}, options2);
          const lastWithoutSeries = __spreadValues({}, this.lastUpdateOptions);
          delete optionsWithoutSeries.series;
          delete lastWithoutSeries.series;
          if (Utils$1.shallowEqual(optionsWithoutSeries, lastWithoutSeries)) {
            return Promise.resolve(this);
          }
        }
      }
    }
    if (options2.series) {
      this.data.resetParsingFlags();
      this.series.resetSeries(false, true, false);
      if (options2.series.length && options2.series[0].data) {
        options2.series = options2.series.map(
          (s2, i2) => {
            return this.updateHelpers._extendSeries(s2, i2);
          }
        );
      }
      this.updateHelpers.revertDefaultAxisMinMax();
    }
    if (options2.xaxis) {
      options2 = this.updateHelpers.forceXAxisUpdate(options2);
    }
    if (options2.yaxis) {
      options2 = this.updateHelpers.forceYAxisUpdate(options2);
    }
    if (w2.globals.collapsedSeriesIndices.length > 0) {
      this.series.clearPreviousPaths();
    }
    if (options2.theme) {
      options2 = this.theme.updateThemeOptions(options2);
    }
    return this.updateHelpers._updateOptions(
      options2,
      redraw,
      animate,
      updateSyncedCharts,
      overwriteInitialConfig
    );
  }
  /**
   * Replaces the chart's series data and re-renders.
   *
   * @param {ApexAxisChartSeries | ApexNonAxisChartSeries} [newSeries=[]] - The replacement series array.
   * @param {boolean} [animate=true] - Whether to animate the update.
   * @param {boolean} [overwriteInitialSeries=true] - When true, replaces the stored initial series used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */
  updateSeries(newSeries = [], animate = true, overwriteInitialSeries = true) {
    if (!Array.isArray(newSeries)) {
      console.warn(
        "ApexCharts: updateSeries() ignored the call because the series is not an array."
      );
      return Promise.resolve(this);
    }
    this.data.resetParsingFlags();
    this.series.prepareDataUpdate();
    this.updateHelpers.revertDefaultAxisMinMax();
    return this.updateHelpers._updateSeries(
      newSeries,
      animate,
      overwriteInitialSeries
    );
  }
  /**
   * Appends a new series to the existing series array and re-renders.
   *
   * @param {ApexAxisChartSeries[0] | ApexNonAxisChartSeries} newSerie - The series object to append.
   * @param {boolean} [animate=true] - Whether to animate the update.
   * @param {boolean} [overwriteInitialSeries=true] - When true, replaces the stored initial series used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */
  appendSeries(newSerie, animate = true, overwriteInitialSeries = true) {
    this.data.resetParsingFlags();
    const newSeries = this.w.config.series.slice();
    newSeries.push(
      /** @type {any} */
      newSerie
    );
    this.series.prepareDataUpdate();
    this.updateHelpers.revertDefaultAxisMinMax();
    return this.updateHelpers._updateSeries(
      newSeries,
      animate,
      overwriteInitialSeries
    );
  }
  /**
   * Appends data points to existing series without replacing them.
   * Each element of `newData` corresponds to the series at the same index.
   *
   * @param {Array<{ data: any[] }>} newData - Data to append, in the same shape as series[].data.
   * @param {boolean} [overwriteInitialSeries=true] - When true, updates the stored initial series used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */
  appendData(newData, overwriteInitialSeries = true) {
    const me = this;
    me.data.resetParsingFlags();
    me.w.globals.dataChanged = true;
    if (me.w.config.chart.animations.enabled) {
      me.series.getPreviousPaths();
    }
    const histRaw = me.w.globals.histogramRawSeries;
    if (histRaw) {
      for (let i2 = 0; i2 < histRaw.length; i2++) {
        const src = (
          /** @type {any} */
          newData[i2]
        );
        if (src && Array.isArray(src.data) && Array.isArray(histRaw[i2].data)) {
          for (let j = 0; j < src.data.length; j++) {
            histRaw[i2].data.push(src.data[j]);
          }
        }
      }
      return this.update();
    }
    const newSeries = me.w.config.series.slice();
    for (let i2 = 0; i2 < newSeries.length; i2++) {
      if (newData[i2] !== null && typeof newData[i2] !== "undefined") {
        const srcSerie = (
          /** @type {any} */
          newData[i2]
        );
        const dstSerie = (
          /** @type {any} */
          newSeries[i2]
        );
        for (let j = 0; j < srcSerie.data.length; j++) {
          dstSerie.data.push(srcSerie.data[j]);
        }
      }
    }
    trimStreamingSeries(newSeries, me.w);
    me.w.config.series = newSeries;
    if (overwriteInitialSeries) {
      me.w.globals.initialSeries = me.w.config.series;
    }
    return this.update();
  }
  /**
   * True when an options object carries enough series data that a
   * JSON.stringify equality check (and the Utils.clone needed to store it for
   * later comparison) would cost more than the re-render it tries to avoid.
   * @param {any} options
   * @returns {boolean}
   */
  static _optionsTooBigToCompare(options2) {
    const series = options2 && options2.series;
    if (!Array.isArray(series)) return false;
    let points = 0;
    for (let i2 = 0; i2 < series.length; i2++) {
      const d = series[i2] && series[i2].data;
      points += Array.isArray(d) ? d.length : 1;
      if (points > 1e3) return true;
    }
    return false;
  }
  /**
   * @param {object} [options]
   */
  update(options2) {
    return new Promise((resolve, reject) => {
      if (options2 && this.lastUpdateOptions && !_ApexCharts._optionsTooBigToCompare(options2) && JSON.stringify(this.lastUpdateOptions) === JSON.stringify(options2)) {
        return resolve(this);
      }
      this.lastUpdateOptions = options2 && !_ApexCharts._optionsTooBigToCompare(options2) ? Utils$1.clone(options2) : null;
      new Destroy(this.ctx).clear({ isUpdating: true });
      const graphData = this.create(this.w.config.series, options2 != null ? options2 : {});
      if (!graphData) return resolve(this);
      this.mount(graphData).then(() => {
        var _a;
        (_a = this.morphTypeChange) == null ? void 0 : _a.applyChromeFade();
        applyAxisTransition(this.w);
        applyDataLabelTransition(this.w);
        if (typeof this.w.config.chart.events.updated === "function") {
          this.w.config.chart.events.updated(this, this.w);
        }
        this.events.fireEvent("updated", [this, this.w]);
        this.w.globals.isDirty = true;
        resolve(this);
      }).catch((e2) => {
        reject(e2);
      });
    });
  }
  /**
   * Redraws the scale-dependent chrome (grid lines, x-axis, y-axes) IN PLACE
   * within the frozen layout after a data-only update changed the axis
   * domain. The rebuilt groups replace the old nodes positionally, so z-order
   * (grid back/front) is preserved without re-running mount. Legend,
   * annotations containers, toolbar, defs/masks, and the plot geometry are
   * untouched.
   *
   * Returns false when the refresh cannot faithfully reproduce the chart and
   * the caller must fall back to a full render:
   * - horizontal bar charts (inversed axes draw through a different path)
   * - charts with annotations or ink notes (their positions are scale-bound
   *   and are laid out by the full render)
   * - the new y labels no longer fit the width reserved at layout time
   *
   * @param {any} _xyRatios
   * @returns {boolean} true when the chrome was refreshed in place
   */
  _fastAxisChromeRefresh(_xyRatios) {
    const w2 = this.w;
    const gl = w2.globals;
    this._fastAxisBailReason = "";
    try {
      if (gl.isBarHorizontal) {
        this._fastAxisBailReason = "barHorizontal";
        return false;
      }
      if (w2.config.chart.sparkline.enabled) return true;
      const a2 = w2.config.annotations;
      if (a2 && (a2.yaxis && a2.yaxis.length || a2.xaxis && a2.xaxis.length || a2.points && a2.points.length || a2.texts && a2.texts.length || a2.images && a2.images.length)) {
        this._fastAxisBailReason = "annotations";
        return false;
      }
      if (w2.config.chart.ink && w2.config.chart.ink.enabled) {
        this._fastAxisBailReason = "ink";
        return false;
      }
      const dim = this.dimensions;
      if (!dim || !dim.dimYAxis) {
        this._fastAxisBailReason = "noDimensions";
        return false;
      }
      const prevYLabelsCoords = w2.layout.yLabelsCoords;
      const prevYTitleCoords = w2.layout.yTitleCoords;
      const yaxisLabelCoords = dim.dimYAxis.getyAxisLabelsCoords();
      const yTitleCoords = dim.dimYAxis.getyAxisTitleCoords();
      w2.layout.yLabelsCoords = [];
      w2.layout.yTitleCoords = [];
      w2.config.yaxis.map((_yaxe, index) => {
        w2.layout.yLabelsCoords.push({
          width: yaxisLabelCoords[index].width,
          index
        });
        w2.layout.yTitleCoords.push(
          /** @type {any} */
          { width: yTitleCoords[index].width, index }
        );
      });
      const newYAxisWidth = dim.dimYAxis.getTotalYAxisWidth();
      if (newYAxisWidth > dim.yAxisWidth + 2) {
        w2.layout.yLabelsCoords = prevYLabelsCoords;
        w2.layout.yTitleCoords = prevYTitleCoords;
        this._fastAxisBailReason = `labelWidth ${newYAxisWidth} > ${dim.yAxisWidth}`;
        return false;
      }
      const innerEl = w2.dom.elGraphical.node;
      const oldGrid = innerEl.querySelector(".apexcharts-grid");
      const oldGridBorders = innerEl.querySelector(".apexcharts-grid-borders");
      if (!oldGrid) {
        this._fastAxisBailReason = "missingGridNode";
        return false;
      }
      const gridParent = oldGrid.parentNode;
      const gridNext = oldGridBorders ? oldGridBorders.nextSibling : oldGrid.nextSibling;
      oldGrid.remove();
      if (oldGridBorders) oldGridBorders.remove();
      innerEl.querySelectorAll(".apexcharts-xaxis-tick").forEach((t2) => t2.remove());
      this.grid = new Grid(w2, this);
      const elgrid = this.grid.drawGrid();
      if (elgrid && elgrid.el) {
        gridParent.insertBefore(elgrid.el.node, gridNext);
        if (elgrid.elGridBorders && elgrid.elGridBorders.node) {
          gridParent.insertBefore(elgrid.elGridBorders.node, gridNext);
        }
      }
      const xAxis = new XAxis(this.w, this.ctx, elgrid);
      const oldXaxis = innerEl.querySelector(".apexcharts-xaxis");
      if (oldXaxis) {
        const xParent = oldXaxis.parentNode;
        const xNext = oldXaxis.nextSibling;
        oldXaxis.remove();
        const elXaxis = xAxis.drawXaxis();
        xParent.insertBefore(elXaxis.node, xNext);
      }
      const yAxis = new YAxis(
        this.w,
        { theme: this.theme, timeScale: this.timeScale },
        elgrid
      );
      for (let index = 0; index < w2.config.yaxis.length; index++) {
        if (gl.ignoreYAxisIndexes.indexOf(index) !== -1) continue;
        const oldY = w2.dom.baseEl.querySelector(
          `.apexcharts-yaxis[rel='${index}']`
        );
        if (!oldY) {
          this._fastAxisBailReason = "missingYAxisNode";
          return false;
        }
        const yParent = oldY.parentNode;
        if (!yParent) {
          this._fastAxisBailReason = "missingYAxisParent";
          return false;
        }
        const yNext = oldY.nextSibling;
        oldY.remove();
        const elYaxis = yAxis.drawYaxis(index);
        yParent.insertBefore(elYaxis.node, yNext);
      }
      if (elgrid !== null) {
        xAxis.xAxisLabelCorrections();
        yAxis.setYAxisTextAlignments();
        w2.config.yaxis.map((yaxe, index) => {
          if (gl.ignoreYAxisIndexes.indexOf(index) === -1) {
            yAxis.yAxisTitleRotate(index, yaxe.opposite);
          }
        });
      }
      return true;
    } catch (e2) {
      this._fastAxisBailReason = "error: " + (e2 && /** @type {any} */
      e2.message);
      return false;
    }
  }
  /**
   * Fast update path for data-only series changes.
   *
   * Skips rebuilding grid, axes, dimensions, legend, annotations, tooltip DOM,
   * and toolbar. Only recalculates scales and replots the series paths.
   * Called automatically by _updateSeries() when the fast path is eligible.
   *
   * @param {boolean} animate - Whether to animate the update.
   * @param {string} [prevAxisScaleSig] - Signature of the on-screen axis scale
   *   captured by _updateSeries() before parseData recomputed bounds. When the
   *   recomputed scale differs, the fast path can't repaint the ruler in place,
   *   so it delegates to a full render. Omitted -> the check is skipped.
   * @returns {Promise<ApexCharts>} Resolves with the chart instance.
   */
  fastUpdate(animate, prevAxisScaleSig) {
    return new Promise((resolve, reject) => {
      var _a, _b, _c;
      try {
        const w2 = this.w;
        const gl = w2.globals;
        gl.shouldAnimate = animate;
        gl.dataChanged = true;
        gl.animationEnded = false;
        PerformanceCache.invalidateSelectors(w2);
        const gl2 = w2.globals;
        gl2.maxY = -Number.MAX_VALUE;
        gl2.minY = Number.MIN_VALUE;
        gl2.minYArr = [];
        gl2.maxYArr = [];
        gl2.maxX = -Number.MAX_VALUE;
        gl2.minX = Number.MAX_VALUE;
        gl2.initialMaxX = -Number.MAX_VALUE;
        gl2.initialMinX = Number.MAX_VALUE;
        gl2.yAxisScale = [];
        gl2.xAxisScale = null;
        gl2.xAxisTicksPositions = [];
        gl2.xRange = 0;
        gl2.yRange = [];
        gl2.zRange = 0;
        gl2.xTickAmount = 0;
        gl2.multiAxisTickAmount = 0;
        gl2.pointsArray = [];
        gl2.barCanvasCoords = null;
        gl2.dataLabelsRects = [];
        gl2.lastDrawnDataLabelsIndexes = [];
        gl2.textRectsCache = /* @__PURE__ */ new Map();
        gl2.domCache = /* @__PURE__ */ new Map();
        gl2.cachedSelectors = {};
        gl2.disableZoomIn = false;
        gl2.disableZoomOut = false;
        if (gl.axisCharts) {
          this.core.coreCalculations();
          if (w2.config.xaxis.type !== "category") {
            this.formatters.setLabelFormatters();
          }
        }
        this.formatters.heatmapLabelFormatters();
        const xyRatios = this.core.xySettings();
        if (this._zoomPanSelection) this._zoomPanSelection.xyRatios = xyRatios;
        const newAxisScaleSig = JSON.stringify({
          y: (gl.yAxisScale || []).map((s2) => s2 ? s2.result : null),
          xMin: gl.minX,
          xMax: gl.maxX
        });
        const scaleChanged = gl.axisCharts && prevAxisScaleSig != null && newAxisScaleSig !== prevAxisScaleSig;
        if (scaleChanged && !this._fastAxisChromeRefresh(xyRatios)) {
          this._updateStats.full++;
          return this.update().then(() => resolve(this)).catch(reject);
        }
        if (scaleChanged) {
          this._updateStats.fastWithAxes++;
        } else {
          this._updateStats.fast++;
        }
        (_a = this.weave) == null ? void 0 : _a.dispatch("afterScales", { pass: "fast", xyRatios });
        const rr = (
          /** @type {any} */
          this.ctx.renderer
        );
        const reuseCanvasHost = !!(rr && rr.kind === "canvas" && rr.canRepaintInPlace && rr.canRepaintInPlace());
        if (reuseCanvasHost) rr._repaintHostInPlace = true;
        const innerEl = w2.dom.elGraphical.node;
        const toRemove = innerEl.querySelectorAll(
          (reuseCanvasHost ? "" : ".apexcharts-canvas-series-wrap, ") + ".apexcharts-plot-series, .apexcharts-series, .apexcharts-datalabels, .apexcharts-datalabels-background"
        );
        toRemove.forEach(
          (el) => {
            var _a2;
            return (_a2 = el.parentNode) == null ? void 0 : _a2.removeChild(el);
          }
        );
        const elGraph = this.core.plotChartType(w2.config.series, xyRatios);
        const gridEl = innerEl.querySelector(".apexcharts-grid");
        const xaxisEl = innerEl.querySelector(".apexcharts-xaxis");
        const graphs = Array.isArray(elGraph) ? elGraph : [elGraph];
        const anchor = gridEl && w2.config.grid.position === "front" ? gridEl : xaxisEl;
        if (anchor) {
          graphs.forEach((g2) => {
            const node = g2 && g2.node ? g2.node : g2;
            if (node) innerEl.insertBefore(node, anchor);
          });
        } else {
          graphs.forEach((g2) => {
            w2.dom.elGraphical.add(g2);
          });
        }
        const dataLabels = new DataLabels(w2, this);
        dataLabels.bringForward();
        if (w2.config.dataLabels.background.enabled) {
          dataLabels.dataLabelsBackground();
        }
        if (Environment.isBrowser() && w2.config.tooltip.enabled && !gl.noData) {
          (_b = w2.globals.tooltip) == null ? void 0 : _b.drawTooltip(xyRatios);
        }
        (_c = this.weave) == null ? void 0 : _c.dispatch("draw", { pass: "fast", xyRatios });
        if (typeof w2.config.chart.events.updated === "function") {
          w2.config.chart.events.updated(this, w2);
        }
        this.events.fireEvent("updated", [this, w2]);
        enforceLicense(w2, this);
        gl.isDirty = true;
        resolve(this);
      } catch (e2) {
        reject(e2);
      }
    });
  }
  /**
   * Returns all charts in the same `chart.group` (including this instance),
   * used to synchronise zoom/pan across grouped charts.
   *
   * @returns {ApexCharts[]}
   */
  getSyncedCharts() {
    const group = (
      /** @type {ApexCharts[]} */
      this.getGroupedCharts()
    );
    group.splice(0, 0, this);
    return group;
  }
  /**
   * Returns all charts in the same `chart.group`, excluding this instance.
   * Used internally to apply hover/zoom effects to sibling charts.
   *
   * @returns {ApexCharts[]}
   */
  getGroupedCharts() {
    return Apex._chartInstances.filter(
      (ch) => this !== ch.chart && !!this.w.config.chart.group && this.w.config.chart.group === ch.group
    ).map((ch) => ch.chart);
  }
  /**
   * Retrieves a rendered chart instance by its `chart.id` config value.
   *
   * @param {string} id - The chart ID set via `chart.id` in options.
   * @returns {ApexCharts | undefined}
   */
  static getChartByID(id) {
    const chartId = Utils$1.escapeString(id);
    if (!Apex._chartInstances) return void 0;
    const c2 = Apex._chartInstances.filter(
      (ch) => ch.id === chartId
    )[0];
    return c2 && c2.chart;
  }
  /**
   * Scans the document for elements with a `data-apexcharts` attribute and
   * `data-options` JSON, then renders a chart in each one automatically.
   * Useful for non-framework HTML pages.
   */
  static initOnLoad() {
    var _a;
    const els = document.querySelectorAll("[data-apexcharts]");
    for (let i2 = 0; i2 < els.length; i2++) {
      const el = (
        /** @type {HTMLElement} */
        els[i2]
      );
      const options2 = JSON.parse((_a = els[i2].getAttribute("data-options")) != null ? _a : "");
      const apexChart = new _ApexCharts(el, options2);
      apexChart.render();
    }
  }
  /**
   * This static method allows users to call chart methods without necessarily from the
   * instance of the chart in case user has assigned chartID to the targeted chart.
   * The chartID is used for mapping the instance stored in Apex._chartInstances global variable
   *
   * This is helpful in cases when you don't have reference of the chart instance
   * easily and need to call the method from anywhere.
   * For eg, in React/Vue applications when you have many parent/child components,
   * and need easy reference to other charts for performing dynamic operations
   *
   * @param {string} chartID - The unique identifier which will be used to call methods
   * on that chart instance
   * @param {string} fn - The method name to call
   * @param {...any} opts - The parameters which are accepted in the original method will be passed here in the same order.
   */
  static exec(chartID, fn, ...opts) {
    const chart = this.getChartByID(chartID);
    if (!chart) return;
    chart.w.globals.isExecCalled = true;
    let ret = null;
    if (chart.publicMethods.indexOf(fn) !== -1) {
      ret = /** @type {any} */
      chart[fn](...opts);
    }
    return ret;
  }
  /**
   * Deep-merges `source` into `target` and returns the result.
   * Thin wrapper around the internal `Utils.extend` utility.
   *
   * @param {object} target
   * @param {object} source
   * @returns {object}
   */
  static merge(target, source) {
    return Utils$1.extend(target, source);
  }
  static getThemePalettes() {
    return getThemePalettes();
  }
  /**
   * Register additional chart types. Used by sub-entry points so that only
   * the types they include are bundled.
   *
   * @param {Record<string, new (...args: any[]) => any>} typeMap  e.g. { line: Line, area: Line }
   */
  static use(typeMap) {
    register(typeMap);
  }
  /**
   * Register optional feature modules (Exports, Legend, Toolbar,
   * ZoomPanSelection, KeyboardNavigation, Annotations).
   *
   * Call this before rendering any chart. Feature entry files (e.g.
   * `apexcharts/features/legend`) call this automatically when imported.
   * Note: Tooltip is part of core and does not need to be registered.
   *
   * @param {Record<string, new (...args: any[]) => any>} featureMap  e.g. { legend: Legend, exports: Exports }
   */
  static registerFeatures(featureMap) {
    InitCtxVariables.registerFeatures(featureMap);
  }
  /**
   * Set the license key that unlocks the premium features (storyboard, link /
   * crossfilter, ink, measure, contextMenu, perspectives, history). Without a
   * valid key those features still work but the chart shows an "APEXCHARTS"
   * trial watermark; a valid key removes it. Keys are shared across the whole
   * ApexCharts family (apexgantt, apextree, apexsankey, apex-grid-enterprise,
   * apexstock), so one customer key works everywhere.
   *
   * Call before render(). The watermark is re-evaluated on every render/update,
   * so a late setLicense(validKey) followed by chart.update() clears it.
   *
   * Precedence per chart: `chart.license` (most specific) -> this key ->
   * `window.Apex.license` -> unlicensed (trial).
   *
   * @param {string} key  the `APEX-<base64(JSON)>` license key
   * @returns {typeof ApexCharts}
   */
  static setLicense(key) {
    o.setLicense(key);
    reevaluateLicenseAcrossCharts();
    return _ApexCharts;
  }
  /**
   * Register a Weave plugin definition (a plain { name, setup } object).
   * Lives in core so plugins can always be registered; they only activate when
   * the Weave host is bundled (`import 'apexcharts/features/weave'`, included in
   * the full bundle) and listed in a chart's `plugins` config.
   *
   * @param {{ name: string, apiVersion?: number, setup: Function, destroy?: Function }} def
   * @returns {typeof ApexCharts}
   */
  static registerPlugin(def) {
    registerPlugin(def);
    return _ApexCharts;
  }
  /**
   * Remove a registered Weave plugin definition. Charts already holding an
   * active instance keep it until their plugins config changes or they are
   * destroyed; the name simply stops resolving for new activations. Intended
   * for tests and hot-reload flows.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */
  static unregisterPlugin(name2) {
    unregisterPlugin(name2);
    return _ApexCharts;
  }
  /**
   * Register a non-SVG series renderer (Strata #2). SVG is built in; the canvas
   * backend registers itself via `import 'apexcharts/features/renderer-canvas'`.
   * When a `kind` is not registered, selection falls back to SVG.
   *
   * @param {string} kind  e.g. 'canvas'
   * @param {(w: any, ctx: any) => any} factory  returns a Renderer instance
   */
  static registerRenderer(kind, factory) {
    RendererController.registerRenderer(kind, factory);
  }
  /**
   * Register a custom series type (Marks #11): a `{ renderItem }` definition
   * that draws primitives (path/line/rect/circle/text) per datum. Requires the
   * Marks feature to be bundled (`import 'apexcharts/features/marks'`, included
   * in the full bundle); without it this warns and no-ops. Once registered, use
   * it via `series[].type` or `chart.type`.
   *
   * @param {string} name  the type name, e.g. 'dumbbell'
   * @param {{ renderItem: Function, dataType?: string, yExtent?: Function, tooltip?: Function }} def
   * @returns {typeof ApexCharts}
   */
  static registerSeriesType(name2, def) {
    const factory = (
      /** @type {any} */
      _ApexCharts._customSeriesFactory
    );
    if (!factory) {
      console.warn(
        `[apexcharts] registerSeriesType("${name2}") requires the Marks feature: import 'apexcharts/features/marks'.`
      );
      return _ApexCharts;
    }
    if (!def || typeof def.renderItem !== "function") {
      console.warn(
        `[apexcharts] registerSeriesType("${name2}") needs a def with a renderItem() function.`
      );
      return _ApexCharts;
    }
    if (hasChartClass(name2) && !isCustom(name2)) {
      console.warn(
        `[apexcharts] registerSeriesType("${name2}") would override the built-in "${name2}" chart type; pick another name.`
      );
      return _ApexCharts;
    }
    register({ [name2]: factory(name2, def) });
    markCustom(name2);
    return _ApexCharts;
  }
  /**
   * Remove a custom series type registered via registerSeriesType. Built-in
   * chart types cannot be unregistered. Intended for tests and hot-reload.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */
  static unregisterSeriesType(name2) {
    if (isCustom(name2)) unregister(name2);
    return _ApexCharts;
  }
  /**
   * Facet (#13): register a named theme (palette + design tokens + mode)
   * referenceable via `theme: { name }`. The theme sits below explicit config
   * and CSS `--apx-*` tokens, above the built-in palette/mode defaults.
   *
   * @param {string} name  the theme name, e.g. 'brand'
   * @param {any} def  { mode?, palette?, tokens?, monochrome?, accessibility? }
   * @returns {typeof ApexCharts}
   */
  static registerTheme(name2, def) {
    registerTheme(name2, def);
    return _ApexCharts;
  }
  /**
   * Remove a theme registered via registerTheme. Charts referencing it by
   * `theme.name` fall back to the built-in defaults on their next render.
   * Intended for tests and hot-reload flows.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */
  static unregisterTheme(name2) {
    unregisterTheme(name2);
    return _ApexCharts;
  }
  /**
   * Cadence (#6): register a named easing function referenceable via
   * `chart.animations.easing: '<name>'`. `fn` maps linear progress t in [0,1]
   * to eased progress (back/elastic curves may overshoot 1).
   *
   * @param {string} name  the easing name, e.g. 'bounce'
   * @param {(t:number)=>number} fn
   * @returns {typeof ApexCharts}
   */
  static registerEasing(name2, fn) {
    registerEasing(name2, fn);
    return _ApexCharts;
  }
  /**
   * Register a named unit-chart layout, referenceable via
   * `plotOptions.unit.positions: '<name>'` with `plotOptions.unit.layout:
   * 'custom'`.
   *
   * A layout is objects in, positions out: `(objects, rect) => [{id, x, y,
   * r?}]`, in plot pixels. It knows nothing about animation, because the engine
   * already tweens position, radius and colour and already keeps each mark's
   * identity across a relayout. That is what lets an arrangement the engine
   * cannot know about - a country silhouette, a hex grid, a timeline, a
   * projection supplied by ApexMaps - be a plugin rather than a core change.
   *
   * Marks whose id the layout omits animate out; ids matching no mark are
   * ignored.
   *
   * @param {string} name  the layout name, e.g. 'silhouette'
   * @param {(objects: any[], rect: {x:number,y:number,width:number,height:number}) => any[]} fn
   * @returns {typeof ApexCharts}
   */
  static registerUnitLayout(name2, fn) {
    registerUnitLayout(name2, fn);
    return _ApexCharts;
  }
  /**
   * Remove a layout registered via registerUnitLayout. Charts referencing it by
   * name fall back to the grouped layout on their next render.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */
  static unregisterUnitLayout(name2) {
    unregisterUnitLayout(name2);
    return _ApexCharts;
  }
  /**
   * Register a row source: given a chart's state, what rows is each of its
   * marks standing for?
   *
   * Most marks cannot answer. An ordinary bar aggregates rows the library never
   * saw. The types that can are the ones whose series carries raw observations
   * (histogram, boxPlot, violin), and their sources ship with the statistics in
   * `apexcharts/features/stats`; core keeps only the lookup.
   *
   * The function returns a unit-chart series (one cluster per mark, one datum
   * per row) in the marks' own draw order, or null. See RowSourceRegistry for
   * why that order is a contract rather than a convention.
   *
   * @param {string} name  chart type name, matched against `chart.requestedType` then `chart.type`
   * @param {(w: any, opts?: any) => any[] | null} fn
   * @returns {typeof ApexCharts}
   */
  static registerRowSource(name2, fn) {
    registerRowSource(name2, fn);
    return _ApexCharts;
  }
  /**
   * Remove a row source registered via registerRowSource.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */
  static unregisterRowSource(name2) {
    unregisterRowSource(name2);
    return _ApexCharts;
  }
  /**
   * The rows behind this chart's marks, as a unit-chart series.
   *
   * A histogram bar stands for the observations it counted, a box for the
   * sample it summarises. This hands them back as one cluster per mark, so a
   * mark can come apart into its own rows:
   *
   *     chart.updateOptions({ chart: { type: 'unit' }, series: chart.rowSeries() })
   *
   * With the morph feature loaded, each dot then leaves from the part of the
   * mark that was standing for it, and collapsing back is the inverse.
   *
   * Returns null when the chart's type cannot name its rows, or when
   * `apexcharts/features/stats` (which carries the sources for the types that
   * can) is not loaded.
   *
   * @param {{ maxRows?: number }} [opts] `maxRows` caps the dots produced
   *   (default 3000, matching the jitter overlay); past it every cluster is
   *   thinned by one shared stride so their relative sizes survive.
   * @returns {any[]|null}
   */
  rowSeries(opts) {
    const source = rowSourceFor(this.w);
    if (!source) return null;
    return source(this.w, opts) || null;
  }
  /**
   * Linked Views (#4) Phase 2: get-or-create a crossfilter coordinator by id.
   * Register one shared record set, then let each chart declare a dimension +
   * reduction under `chart.link`. Selecting in one chart re-aggregates the
   * others over the filtered subset.
   *
   * Lives in core (always callable) but the engine ships in the `link` feature
   * (`import 'apexcharts/features/link'`, included in the full bundle); without
   * it this warns and returns null so the engine shakes out when unused.
   *
   * @param {{ id: string, records?: any[] }} opts
   * @returns {any} the coordinator handle, or null if the feature is absent
   */
  static crossfilter(opts) {
    if (!opts || typeof opts.id !== "string") {
      throw new Error("ApexCharts.crossfilter requires an { id } string.");
    }
    const factory = (
      /** @type {any} */
      _ApexCharts._crossfilterFactory
    );
    if (!factory) {
      console.warn(
        `[apexcharts] ApexCharts.crossfilter(...) requires the link feature: import 'apexcharts/features/link'.`
      );
      return null;
    }
    const coordinator = factory(opts);
    reevaluateLicenseAcrossCharts();
    return coordinator;
  }
  /**
   * Look up an existing crossfilter coordinator by id (null if none / feature
   * absent).
   * @param {string} id
   * @returns {any}
   */
  static getCrossfilter(id) {
    const get = (
      /** @type {any} */
      _ApexCharts._crossfilterGet
    );
    return get ? get(id) : null;
  }
  /**
   * Linked Views (#4): clear crossfilter dimming across this chart and every
   * chart in its `chart.group`. No-op unless the `link` feature is bundled.
   */
  clearCrossfilter() {
    var _a;
    (_a = this.linkedViews) == null ? void 0 : _a.clearGroup();
  }
  /**
   * Measure ruler (#18): arm a sticky measure-ruler mode (drag A->B on the
   * plot to read dx/dy/%change/slope). Alternatively hold the measure key
   * (chart.measure.key, default 'm') and drag. No-op unless the `measure`
   * feature is bundled and chart.measure.enabled.
   */
  startMeasure() {
    var _a;
    (_a = this.measure) == null ? void 0 : _a.startMeasure();
  }
  /** Measure ruler (#18): leave measure mode. */
  stopMeasure() {
    var _a;
    (_a = this.measure) == null ? void 0 : _a.stopMeasure();
  }
  /** Measure ruler (#18): remove all pinned measure rulers. */
  clearMeasures() {
    var _a;
    (_a = this.measure) == null ? void 0 : _a.clearMeasures();
  }
  /**
   * Toggles (show/hide) the series identified by name.
   * Mirrors a click on the corresponding legend item.
   *
   * @param {string} seriesName
   * @returns {object | undefined} The collapsed series object, if now hidden.
   */
  toggleSeries(seriesName) {
    return this.series.toggleSeries(seriesName);
  }
  /**
   * Highlights or un-highlights a series when the user hovers a legend item.
   * Called internally by the legend; not typically called by consumers.
   *
   * @param {MouseEvent} e
   * @param {HTMLElement} targetElement - The legend marker element being hovered.
   */
  highlightSeriesOnLegendHover(e2, targetElement) {
    return this.series.toggleSeriesOnHover(e2, targetElement);
  }
  /**
   * Makes a previously hidden series visible and re-renders.
   *
   * @param {string} seriesName
   */
  showSeries(seriesName) {
    this.series.showSeries(seriesName);
  }
  /**
   * Hides a visible series and re-renders.
   *
   * @param {string} seriesName
   */
  hideSeries(seriesName) {
    this.series.hideSeries(seriesName);
  }
  /**
   * Highlights (dims all other series) the series identified by name.
   *
   * @param {string} seriesName
   */
  highlightSeries(seriesName) {
    this.series.highlightSeries(seriesName);
  }
  /**
   * Returns whether the series identified by name is currently hidden.
   *
   * @param {string} seriesName
   * @returns {boolean}
   */
  isSeriesHidden(seriesName) {
    return this.series.isSeriesHidden(seriesName);
  }
  /**
   * Resets the chart to the initial series and optionally the initial zoom level.
   *
   * @param {boolean} [shouldUpdateChart=true] - When true, triggers a re-render.
   * @param {boolean} [shouldResetZoom=true] - When true, restores the initial zoom level.
   */
  resetSeries(shouldUpdateChart = true, shouldResetZoom = true) {
    this.series.resetSeries(shouldUpdateChart, shouldResetZoom);
  }
  /**
   * Subscribes to a chart event by name.
   * Supported event names mirror the `chart.events` option keys
   * (e.g. `'mounted'`, `'updated'`, `'dataPointMouseEnter'`).
   *
   * @param {string} name - Event name.
   * @param {Function} handler - Callback invoked when the event fires.
   */
  addEventListener(name2, handler) {
    this.events.addEventListener(name2, handler);
  }
  /**
   * Removes a previously registered event listener.
   *
   * @param {string} name - Event name.
   * @param {Function} handler - The exact function reference passed to addEventListener.
   */
  removeEventListener(name2, handler) {
    this.events.removeEventListener(name2, handler);
  }
  /**
   * Adds an x-axis annotation dynamically after render.
   *
   * @param {XAxisAnnotations} opts - Annotation configuration.
   * @param {boolean} [pushToMemory=true] - When true, the annotation persists across re-renders.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */
  addXaxisAnnotation(opts, pushToMemory = true, context = void 0) {
    var _a;
    let me = (
      /** @type {ApexCharts} */
      /** @type {unknown} */
      this
    );
    if (context) {
      me = context;
    }
    (_a = me.annotations) == null ? void 0 : _a.addXaxisAnnotationExternal(opts, pushToMemory, me);
  }
  /**
   * Adds a y-axis annotation dynamically after render.
   *
   * @param {YAxisAnnotations} opts - Annotation configuration.
   * @param {boolean} [pushToMemory=true] - When true, the annotation persists across re-renders.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */
  addYaxisAnnotation(opts, pushToMemory = true, context = void 0) {
    var _a;
    let me = (
      /** @type {ApexCharts} */
      /** @type {unknown} */
      this
    );
    if (context) {
      me = context;
    }
    (_a = me.annotations) == null ? void 0 : _a.addYaxisAnnotationExternal(opts, pushToMemory, me);
  }
  /**
   * Adds a point annotation dynamically after render.
   *
   * @param {PointAnnotations} opts - Annotation configuration.
   * @param {boolean} [pushToMemory=true] - When true, the annotation persists across re-renders.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */
  addPointAnnotation(opts, pushToMemory = true, context = void 0) {
    var _a;
    let me = (
      /** @type {ApexCharts} */
      /** @type {unknown} */
      this
    );
    if (context) {
      me = context;
    }
    (_a = me.annotations) == null ? void 0 : _a.addPointAnnotationExternal(opts, pushToMemory, me);
  }
  /**
   * Removes all annotations from the chart.
   *
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */
  clearAnnotations(context = void 0) {
    var _a;
    let me = (
      /** @type {ApexCharts} */
      /** @type {unknown} */
      this
    );
    if (context) {
      me = context;
    }
    me.lastUpdateOptions = null;
    (_a = me.annotations) == null ? void 0 : _a.clearAnnotations(me);
  }
  /**
   * Removes a specific annotation by its `id`.
   *
   * @param {string} id - The annotation id as set in the annotation config.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */
  removeAnnotation(id, context = void 0) {
    var _a;
    let me = (
      /** @type {ApexCharts} */
      /** @type {unknown} */
      this
    );
    if (context) {
      me = context;
    }
    me.lastUpdateOptions = null;
    (_a = me.annotations) == null ? void 0 : _a.removeAnnotation(me, id);
  }
  /**
   * Returns the inner SVG group element that contains all chart graphics.
   *
   * @returns {Element | null}
   */
  getChartArea() {
    const el = this.w.dom.baseEl.querySelector(".apexcharts-inner");
    return el;
  }
  /**
   * Returns the sum of all data points whose x value falls within [minX, maxX].
   *
   * @param {number} minX
   * @param {number} maxX
   * @returns {number[]} One total per series.
   */
  getSeriesTotalXRange(minX, maxX) {
    return this.coreUtils.getSeriesTotalsXRange(minX, maxX);
  }
  /**
   * Returns the highest y value in the specified series.
   *
   * @param {number} [seriesIndex=0]
   * @returns {number}
   */
  getHighestValueInSeries(seriesIndex = 0) {
    const range = new Range(this.w);
    return range.getMinYMaxY(seriesIndex).highestY;
  }
  /**
   * Returns the lowest y value in the specified series.
   *
   * @param {number} [seriesIndex=0]
   * @returns {number}
   */
  getLowestValueInSeries(seriesIndex = 0) {
    const range = new Range(this.w);
    return range.getMinYMaxY(seriesIndex).lowestY;
  }
  /**
   * Returns the sum of each series (the totals used for percentage calculations).
   *
   * @returns {number[]}
   */
  getSeriesTotal() {
    return this.w.globals.seriesTotals;
  }
  /**
   * Returns a curated snapshot of chart state for use in formatters, events,
   * and external integrations. Prefer this over accessing `chart.w` directly.
   *
   * The shape of this object is stable and versioned. `chart.w` is internal
   * and will be restricted in a future major version.
   */
  getState() {
    const w2 = this.w;
    const gl = w2.globals;
    return {
      // Series data — computed/parsed form used for rendering
      series: w2.seriesData.series,
      seriesNames: w2.seriesData.seriesNames,
      colors: gl.colors,
      labels: w2.labelData.labels,
      seriesTotals: gl.seriesTotals,
      seriesPercent: gl.seriesPercent,
      seriesXvalues: gl.seriesXvalues,
      seriesYvalues: gl.seriesYvalues,
      // Axis bounds — updated after each render
      minX: gl.minX,
      maxX: gl.maxX,
      minY: gl.minY,
      maxY: gl.maxY,
      minYArr: gl.minYArr,
      maxYArr: gl.maxYArr,
      minXDiff: gl.minXDiff,
      dataPoints: gl.dataPoints,
      // Axis scale objects — computed tick/scale results
      xAxisScale: gl.xAxisScale,
      yAxisScale: gl.yAxisScale,
      xTickAmount: gl.xTickAmount,
      // Axis type flags
      isXNumeric: w2.axisFlags.isXNumeric,
      // Multi-axis series mapping
      seriesYAxisMap: gl.seriesYAxisMap,
      seriesYAxisReverseMap: gl.seriesYAxisReverseMap,
      // Chart dimensions — updated after each render/resize
      svgWidth: gl.svgWidth,
      svgHeight: gl.svgHeight,
      gridWidth: w2.layout.gridWidth,
      gridHeight: w2.layout.gridHeight,
      // Interactive state
      selectedDataPoints: w2.interact.selectedDataPoints,
      collapsedSeriesIndices: gl.collapsedSeriesIndices,
      zoomed: w2.interact.zoomed,
      // Chart-type-specific series data (null when not applicable)
      seriesX: w2.seriesData.seriesX,
      seriesZ: w2.seriesData.seriesZ,
      seriesCandleO: w2.candleData.seriesCandleO,
      seriesCandleH: w2.candleData.seriesCandleH,
      seriesCandleM: w2.candleData.seriesCandleM,
      seriesCandleL: w2.candleData.seriesCandleL,
      seriesCandleC: w2.candleData.seriesCandleC,
      seriesRangeStart: w2.rangeData.seriesRangeStart,
      seriesRangeEnd: w2.rangeData.seriesRangeEnd,
      seriesGoals: w2.seriesData.seriesGoals
    };
  }
  /**
   * Programmatically selects or deselects a data point.
   * Equivalent to a user click on the data point.
   *
   * @param {number} seriesIndex - Zero-based series index.
   * @param {number} [dataPointIndex] - Zero-based data point index within the series.
   * @returns {number[][] | null} Updated selectedDataPoints array, or null.
   */
  toggleDataPointSelection(seriesIndex, dataPointIndex) {
    return this.updateHelpers.toggleDataPointSelection(
      seriesIndex,
      dataPointIndex
    );
  }
  /**
   * Programmatically zooms the x-axis to the given range.
   * Requires zoom to be enabled (`chart.zoom.enabled: true`).
   *
   * @param {number} min - The minimum x value (timestamp or numeric).
   * @param {number} max - The maximum x value (timestamp or numeric).
   */
  zoomX(min, max) {
    var _a;
    (_a = this.ctx.toolbar) == null ? void 0 : _a.zoomUpdateOptions(min, max);
  }
  /**
   * Switches the active locale, updating all locale-dependent labels (toolbar tooltips, month names, etc.).
   *
   * @param {string} localeName - Must match a locale name defined in `chart.locales`.
   */
  setLocale(localeName) {
    this.localization.setCurrentLocaleValues(localeName);
  }
  /**
   * Exports the chart to a PNG or SVG data URI.
   * Requires the Exports feature: `import 'apexcharts/features/exports'`.
   *
   * @param {{ scale?: number, width?: number }} [options]
   * @returns {Promise<{ imgURI: string } | { blob: Blob }>}
   */
  dataURI(options2) {
    if (!this.ctx.exports)
      throw new Error(
        "apexcharts: Exports feature is not registered. Import apexcharts/features/exports."
      );
    return this.ctx.exports.dataURI(options2);
  }
  /**
   * Returns the chart's SVG markup as a string, optionally scaled.
   * Requires the Exports feature: `import 'apexcharts/features/exports'`.
   *
   * @param {number} [scale=1]
   * @returns {Promise<string>}
   */
  getSvgString(scale) {
    if (!this.ctx.exports)
      throw new Error(
        "apexcharts: Exports feature is not registered. Import apexcharts/features/exports."
      );
    return this.ctx.exports.getSvgString(scale);
  }
  /**
   * Triggers a CSV download of the chart's data.
   * Requires the Exports feature: `import 'apexcharts/features/exports'`.
   *
   * @param {{ series?: any, fileName?: string, columnDelimiter?: string, lineDelimiter?: string }} [options]
   */
  exportToCSV(options2 = {}) {
    if (!this.ctx.exports)
      throw new Error(
        "apexcharts: Exports feature is not registered. Import apexcharts/features/exports."
      );
    return this.ctx.exports.exportToCSV(options2);
  }
  paper() {
    return this.w.dom.Paper;
  }
  /**
   * Returns the active series renderer for the last render: `'svg'` (default)
   * or `'canvas'` (Strata #2). `'auto'`/`'canvas'` resolve to `'svg'` unless the
   * canvas renderer feature is bundled and no canvas-unsupported feature is in
   * use. See `chart.renderer` / `chart.rendererThreshold`.
   *
   * @returns {'svg' | 'canvas' | 'gpu'}
   */
  getActiveRenderer() {
    return this.rendererController ? this.rendererController.getActiveKind() : "svg";
  }
  /**
   * Facet (#13): re-resolve the `--apx-*` design tokens and re-render.
   *
   * Tokens are read from the CSS cascade once per render, so a runtime change
   * that is NOT an OS color-scheme flip (e.g. the host app swaps its own
   * design-system theme by toggling a class or setting style properties) is
   * invisible until the next render, and `updateOptions({})` is memoized away.
   * This busts the memo and re-renders, picking up the current token values.
   * @returns {Promise<any>}
   */
  refreshTokens() {
    this.lastUpdateOptions = null;
    return this.update();
  }
  /**
   * Drills into the child level referenced by `id` (a `chart.drilldown.series` entry).
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @param {string|number} id - The drilldown series id to navigate into.
   * @returns {Promise<ApexCharts>}
   */
  drillDown(id) {
    if (!this.ctx.drilldown)
      throw new Error(
        "apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown."
      );
    return this.ctx.drilldown.drillDown(id);
  }
  /**
   * Navigates back one drilldown level.
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @returns {Promise<ApexCharts>}
   */
  drillUp() {
    if (!this.ctx.drilldown)
      throw new Error(
        "apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown."
      );
    return this.ctx.drilldown.drillUp();
  }
  /**
   * Navigates back to the root drilldown level.
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @returns {Promise<ApexCharts>}
   */
  drillToRoot() {
    if (!this.ctx.drilldown)
      throw new Error(
        "apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown."
      );
    return this.ctx.drilldown.drillToRoot();
  }
  /**
   * Drops levels cached from `drilldown.onDrillDown`, so the next drill re-runs
   * the resolver. Call it when the data behind an already-drilled chart changes.
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @param {string|number} [id] - A single level id, or every level when omitted.
   * @returns {ApexCharts}
   */
  clearDrilldownCache(id) {
    if (!this.ctx.drilldown)
      throw new Error(
        "apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown."
      );
    return this.ctx.drilldown.clearCache(id);
  }
  // ─── Slice write-back stubs ─────────────────────────────────────────────────
  /**
   * Copy own DATA properties of a parse-state slice onto a live w.* slice.
   * Never Object.assign here: several w.* fields (and, historically, snapshot
   * fields) are lazy accessors, and [[Get]]-ing an accessor while copying
   * forces its deferred computation (a deep initialSeries clone plus O(n)
   * stacked-totals passes on every render/update) and can replace the live
   * accessor with a materialized value for the life of the instance.
   * @param {any} target
   * @param {any} slice
   */
  static _writeDataProps(target, slice) {
    for (const key of Object.keys(slice)) {
      const d = Object.getOwnPropertyDescriptor(slice, key);
      if (d && "value" in d) target[key] = d.value;
    }
  }
  /**
   * @param {Partial<import('./types/internal').SeriesData>} slice
   */
  _writeParsedSeriesData(slice) {
    _ApexCharts._writeDataProps(this.w.seriesData, slice);
  }
  /**
   * @param {Partial<import('./types/internal').RangeData>} slice
   */
  _writeParsedRangeData(slice) {
    _ApexCharts._writeDataProps(this.w.rangeData, slice);
  }
  /**
   * @param {Partial<import('./types/internal').CandleData>} slice
   */
  _writeParsedCandleData(slice) {
    _ApexCharts._writeDataProps(this.w.candleData, slice);
  }
  /**
   * @param {Partial<import('./types/internal').LabelData>} slice
   */
  _writeParsedLabelData(slice) {
    _ApexCharts._writeDataProps(this.w.labelData, slice);
  }
  /**
   * @param {Partial<import('./types/internal').AxisFlags>} slice
   */
  _writeParsedAxisFlags(slice) {
    _ApexCharts._writeDataProps(this.w.axisFlags, slice);
  }
  /**
   * @param {Partial<import('./types/internal').LayoutCoords>} slice
   */
  _writeLayoutCoords(slice) {
    _ApexCharts._writeDataProps(this.w.layout, slice);
  }
  _parentResizeCallback() {
    if (this.w.globals.animationEnded && this.w.config.chart.redrawOnParentResize) {
      this._windowResize();
    }
  }
  /**
   * Handle window resize and re-draw the whole chart.
   */
  _windowResize() {
    this.w.globals.resizeTimer = window.setTimeout(() => {
      const gl = this.w.globals;
      if (this.core && gl.lastResizeSignature) {
        const sig = this.core.getResizeSignature();
        if (sig.w === gl.lastResizeSignature.w && sig.h === gl.lastResizeSignature.h && sig.iw === gl.lastResizeSignature.iw) {
          return;
        }
      }
      gl.resized = true;
      gl.dataChanged = false;
      this.ctx.update();
    }, 150);
  }
  _windowResizeHandler() {
    var _a;
    clearTimeout((_a = this.w.globals.resizeTimer) != null ? _a : void 0);
    let { redrawOnWindowResize: redraw } = this.w.config.chart;
    if (typeof redraw === "function") {
      redraw = /** @type {any} */
      redraw();
    }
    redraw && this._windowResize();
  }
};
/**
 * Static Perspectives helpers (decode/fromURL), populated by the perspectives
 * feature when imported (`import 'apexcharts/features/perspectives'`); null
 * otherwise. Declared here as a placeholder so core stays free of the
 * Perspectives module while the assignment in the feature file type-checks.
 * @type {any}
 */
__publicField(_ApexCharts, "perspectives", null);
let ApexCharts = _ApexCharts;
class Exports {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
  }
  /**
   * @param {string} svgString
   */
  svgStringToNode(svgString) {
    const parser = new DOMParser();
    const svgDoc = parser.parseFromString(svgString, "image/svg+xml");
    return svgDoc.documentElement;
  }
  /**
   * @param {any} svg
   * @param {number} scale
   */
  scaleSvgNode(svg, scale) {
    const svgWidth = parseFloat(svg.getAttributeNS(null, "width"));
    const svgHeight = parseFloat(svg.getAttributeNS(null, "height"));
    svg.setAttributeNS(null, "width", svgWidth * scale);
    svg.setAttributeNS(null, "height", svgHeight * scale);
    svg.setAttributeNS(null, "viewBox", "0 0 " + svgWidth + " " + svgHeight);
  }
  /**
   * Inline any Strata canvas series layer into the clone as an SVG `<image>`.
   * A serialized `<canvas>` loses its bitmap, so a canvas-mode export would drop
   * the series; an `<image>` carrying the canvas `toDataURL()` preserves it in
   * place. Because it replaces the `<foreignObject>` at the same DOM position,
   * the grid-behind / annotations-in-front z-order is retained automatically.
   * No-op in SVG mode (no series canvas present).
   * @param {any} clonedNode the cloned elWrap about to be serialized
   */
  inlineCanvasLayers(clonedNode) {
    const w2 = this.w;
    const XLINK = "http://www.w3.org/1999/xlink";
    const origCanvases = w2.dom.elWrap.querySelectorAll(
      ".apexcharts-series-canvas"
    );
    if (!origCanvases.length) return;
    const clonedFOs = clonedNode.querySelectorAll(".apexcharts-canvas-series");
    for (let i2 = 0; i2 < origCanvases.length && i2 < clonedFOs.length; i2++) {
      let dataURL;
      try {
        dataURL = /** @type {HTMLCanvasElement} */
        origCanvases[i2].toDataURL();
      } catch (e2) {
        continue;
      }
      const fo = clonedFOs[i2];
      const img = document.createElementNS(SVGNS$2, "image");
      img.setAttribute("x", fo.getAttribute("x") || "0");
      img.setAttribute("y", fo.getAttribute("y") || "0");
      img.setAttribute("width", fo.getAttribute("width") || "0");
      img.setAttribute("height", fo.getAttribute("height") || "0");
      img.setAttribute("href", dataURL);
      img.setAttributeNS(XLINK, "xlink:href", dataURL);
      if (fo.parentNode) fo.parentNode.replaceChild(img, fo);
    }
  }
  /**
   * `querySelectorAll` as a typed array. Both HTML and SVG elements carry
   * `style` / `classList`, but `NodeListOf<Element>` does not.
   * @param {ParentNode} root
   * @param {string} selector
   * @returns {Array<HTMLElement | SVGElement>}
   */
  queryStyleable(root, selector) {
    return (
      /** @type {Array<HTMLElement | SVGElement>} */
      Array.prototype.slice.call(root.querySelectorAll(selector))
    );
  }
  /**
   * Applies `styles` only where the element has no inline value for that
   * property yet.
   *
   * The rules being re-applied here came from a stylesheet, so anything a
   * module set inline has to keep winning exactly like it does in the live
   * DOM: `legend.fontSize` (Legend.js) and the heatmap gradient legend's
   * deliberate `display`/`overflow`/`padding` overrides on the legend wrap
   * (HeatmapGradientLegend.js) would otherwise be clobbered in the export.
   * @param {HTMLElement | SVGElement} el
   * @param {Record<string, string>} styles
   */
  setStyleDefaults(el, styles) {
    Object.keys(styles).forEach((prop) => {
      if (el.style.getPropertyValue(prop) === "") {
        el.style.setProperty(prop, styles[prop]);
      }
    });
  }
  /**
   * Re-applies, as inline styles on the clone, the rules that used to reach
   * the exported SVG through an injected `<style>` block. A strict
   * Content-Security-Policy without `'unsafe-inline'` blocks that block and
   * breaks the export, so the export must not depend on one. See #5146.
   *
   * Mirrors `src/assets/apexcharts-legend.css`. Interaction-only rules
   * (`cursor`, `pointer-events`) are carried over for parity even though they
   * do nothing in a static image; the layout rules are what matter.
   * @param {HTMLElement} clonedNode the cloned elWrap about to be serialized
   */
  applyExportStyles(clonedNode) {
    const w2 = this.w;
    this.queryStyleable(clonedNode, "style").forEach((el) => el.remove());
    this.queryStyleable(
      clonedNode,
      [
        ".apexcharts-tooltip",
        ".apexcharts-toolbar",
        ".apexcharts-xaxistooltip",
        ".apexcharts-yaxistooltip",
        ".apexcharts-xcrosshairs",
        ".apexcharts-ycrosshairs",
        ".apexcharts-zoom-rect",
        ".apexcharts-selection-rect"
      ].join(", ")
    ).forEach((el) => {
      el.style.setProperty("display", "none", "important");
    });
    this.queryStyleable(clonedNode, ".apexcharts-flip-y").forEach((el) => {
      this.setStyleDefaults(el, {
        transform: "scaleY(-1) translateY(-100%)",
        "transform-origin": "top",
        "transform-box": "fill-box"
      });
    });
    this.queryStyleable(clonedNode, ".apexcharts-flip-x").forEach((el) => {
      this.setStyleDefaults(el, {
        transform: "scaleX(-1)",
        "transform-origin": "center",
        "transform-box": "fill-box"
      });
    });
    if (!w2.config.legend.show || !w2.dom.elLegendWrap || !w2.dom.elLegendWrap.children.length) {
      return;
    }
    this.queryStyleable(clonedNode, ".apexcharts-legend").forEach((el) => {
      this.setStyleDefaults(el, {
        display: "flex",
        overflow: "auto",
        padding: "0 10px"
      });
      const cl = el.classList;
      const isSide = cl.contains("apx-legend-position-left") || cl.contains("apx-legend-position-right");
      const isTopOrBottom = cl.contains("apx-legend-position-top") || cl.contains("apx-legend-position-bottom");
      if (cl.contains("apexcharts-legend-group-horizontal")) {
        this.setStyleDefaults(el, { "flex-direction": "column" });
      }
      if (isSide) {
        this.setStyleDefaults(el, { "flex-direction": "column", bottom: "0" });
      }
      if (isTopOrBottom) {
        this.setStyleDefaults(el, { "flex-wrap": "wrap" });
      }
      if (isSide || isTopOrBottom && cl.contains("apexcharts-align-left")) {
        this.setStyleDefaults(el, {
          "justify-content": "flex-start",
          "align-items": "flex-start"
        });
      } else if (isTopOrBottom && cl.contains("apexcharts-align-center")) {
        this.setStyleDefaults(el, {
          "justify-content": "center",
          "align-items": "center"
        });
      } else if (isTopOrBottom && cl.contains("apexcharts-align-right")) {
        this.setStyleDefaults(el, {
          "justify-content": "flex-end",
          "align-items": "flex-end"
        });
      }
    });
    this.queryStyleable(clonedNode, ".apexcharts-legend-group").forEach(
      (el) => {
        this.setStyleDefaults(el, { display: "flex" });
      }
    );
    this.queryStyleable(
      clonedNode,
      ".apexcharts-legend-group-vertical"
    ).forEach((el) => {
      this.setStyleDefaults(el, { "flex-direction": "column-reverse" });
    });
    this.queryStyleable(clonedNode, ".apexcharts-legend-series").forEach(
      (el) => {
        this.setStyleDefaults(el, {
          cursor: el.classList.contains("apexcharts-no-click") ? "auto" : "pointer",
          "line-height": "normal",
          display: "flex",
          "align-items": "center"
        });
      }
    );
    this.queryStyleable(clonedNode, ".apexcharts-legend-text").forEach((el) => {
      this.setStyleDefaults(el, {
        position: "relative",
        "font-size": "14px"
      });
    });
    this.queryStyleable(clonedNode, ".apexcharts-legend-marker").forEach(
      (el) => {
        this.setStyleDefaults(el, {
          position: "relative",
          display: "flex",
          "align-items": "center",
          "justify-content": "center",
          cursor: "pointer",
          "margin-right": "1px"
        });
      }
    );
    this.queryStyleable(clonedNode, ".apexcharts-inactive-legend").forEach(
      (el) => {
        this.setStyleDefaults(el, { opacity: "0.45" });
      }
    );
    this.queryStyleable(
      clonedNode,
      ".apexcharts-legend .apexcharts-hidden-zero-series, .apexcharts-legend .apexcharts-hidden-null-series"
    ).forEach((el) => {
      el.style.setProperty("display", "none", "important");
    });
  }
  /**
   * The colour a raster export paints under the chart.
   *
   * A PNG needs an opaque base, so `dataURI` fills the canvas before drawing
   * the SVG onto it. That fill used to be `#fff` whenever `chart.background`
   * was unset *or* `'transparent'`, which is wrong for a dark theme:
   * `theme.mode: 'dark'` moves `chart.foreColor` to a near-white `#f6f7f8` but
   * leaves the background alone, so `background: 'transparent'` produced
   * near-white labels, axes and legend on white — a PNG that looked like it had
   * lost all its text. See #2920.
   *
   * The unset-background case was already covered: `Core.setupElements` paints
   * the SVG paper `#343A3F` for a dark theme, and the clone carries that inline
   * style into the export. `'transparent'` is the gap, because it makes that
   * paper style transparent too and nothing is left to cover the white fill.
   *
   * An explicit non-transparent `chart.background` still wins, including one
   * written by the Facet `--apx-surface` token (Theme assigns it into the same
   * field).
   * @returns {string}
   */
  resolveExportBackground() {
    const w2 = this.w;
    const bg = w2.config.chart.background;
    if (bg && bg !== "transparent") return bg;
    return w2.config.theme.mode === "dark" ? "#343A3F" : "#fff";
  }
  /**
   * Font families the rendered chart actually paints with.
   *
   * Read off the live DOM rather than the config: computed styles resolve
   * `chart.fontFamily`, every per-element `style.fontFamily` override, and any
   * family the chart inherits from the page, none of which are reliably
   * enumerable from config alone. The clone is not in the document, so its
   * computed styles would come back empty.
   * @returns {Set<string>}
   */
  collectFontFamilies() {
    const families = /* @__PURE__ */ new Set();
    const w2 = this.w;
    if (!Environment.isBrowser() || !w2.dom.elWrap) return families;
    const els = this.queryStyleable(
      w2.dom.elWrap,
      "text, tspan, .apexcharts-legend-text, .apexcharts-title-text, .apexcharts-subtitle-text"
    );
    const all = [w2.dom.elWrap, ...els];
    all.forEach((el) => {
      const cs = (
        /** @type {any} */
        BrowserAPIs.getComputedStyle(
          /** @type {any} */
          el
        )
      );
      const ff = cs && cs.fontFamily;
      if (!ff) return;
      ff.split(",").forEach((name2) => {
        const clean = name2.trim().replace(/^['"]|['"]$/g, "");
        if (clean) families.add(clean.toLowerCase());
      });
    });
    return families;
  }
  /**
   * Every `@font-face` rule reachable from the document, as `{ family, css }`.
   *
   * Same-origin sheets are read through `cssRules`. A cross-origin sheet throws
   * on that access, so it is re-fetched by href and its `@font-face` blocks are
   * pulled out of the text: that is the path that matters in practice, since
   * hosted webfonts (the subject of #3617) are exactly the cross-origin case.
   * @returns {Promise<Array<{family: string, css: string}>>}
   */
  collectFontFaceRules() {
    if (!Environment.isBrowser()) return Promise.resolve([]);
    const found = [];
    const remote = [];
    const pushFromText = (cssText) => {
      const blocks = cssText.match(/@font-face\s*\{[^}]*\}/gi) || [];
      blocks.forEach((css) => {
        const m2 = css.match(/font-family\s*:\s*([^;}]+)/i);
        if (!m2) return;
        const family = m2[1].trim().replace(/^['"]|['"]$/g, "");
        found.push({ family: family.toLowerCase(), css });
      });
    };
    const sheets = Array.from(document.styleSheets || []);
    sheets.forEach((sheet) => {
      let rules = null;
      try {
        rules = sheet.cssRules;
      } catch (e2) {
        rules = null;
      }
      if (rules) {
        Array.from(rules).forEach((rule) => {
          if (rule.type === 5 && rule.cssText) pushFromText(rule.cssText);
        });
        return;
      }
      if (sheet.href) {
        remote.push(
          fetch(sheet.href).then((r2) => r2.ok ? r2.text() : "").then(pushFromText).catch(() => {
          })
        );
      }
    });
    return Promise.all(remote).then(() => found);
  }
  /**
   * Inline the `@font-face` rules for the families the chart uses, with the
   * font files themselves as base64 data URIs, into the exported SVG.
   *
   * Needed because the export is a standalone document: rasterizing it through
   * `<img src="data:image/svg+xml,...">` gives it no access to the page's
   * stylesheets *or* its loaded fonts, and an SVG-as-image may not fetch
   * external resources at all. Without this the text silently reflows into a
   * generic fallback face. See #3617.
   *
   * `@font-face` only exists as a stylesheet construct, so unlike the rules in
   * `applyExportStyles` this cannot be expressed as inline styles and has to
   * ship as a `<style>` element. A page whose CSP forbids inline styles will
   * drop it and fall back to today's behaviour, which is why the whole thing is
   * best-effort: any failure leaves the export exactly as it was.
   * @param {any} svgNode the parsed outer <svg> about to be serialized
   * @returns {Promise<void>}
   */
  embedFonts(svgNode) {
    const w2 = this.w;
    if (!Environment.isBrowser() || !w2.config.chart.toolbar.export.embedFonts || typeof fetch !== "function") {
      return Promise.resolve();
    }
    const used = this.collectFontFamilies();
    if (!used.size) return Promise.resolve();
    return this.collectFontFaceRules().then((faces) => {
      const wanted = faces.filter((f) => used.has(f.family));
      if (!wanted.length) return Promise.resolve([]);
      return Promise.all(
        wanted.map(
          (face) => this.inlineFontFaceUrls(face.css).catch(() => null)
        )
      );
    }).then((cssBlocks) => {
      const css = (cssBlocks || []).filter(Boolean).join("\n");
      if (!css) return;
      const style = document.createElementNS(SVGNS$2, "style");
      style.textContent = css;
      svgNode.insertBefore(style, svgNode.firstChild);
    }).catch(() => {
    });
  }
  /**
   * Replace every remote `url(...)` in one `@font-face` block with a base64
   * data URI. Resolves to null if no url could be fetched, so the caller can
   * drop a block that would only reference unreachable files.
   * @param {string} css
   * @returns {Promise<string | null>}
   */
  inlineFontFaceUrls(css) {
    const urls = [];
    const re = /url\(\s*(['"]?)([^'")]+)\1\s*\)/gi;
    let m2;
    while ((m2 = re.exec(css)) !== null) {
      if (!m2[2].startsWith("data:")) urls.push(m2[2]);
    }
    if (!urls.length) return Promise.resolve(css.includes("data:") ? css : null);
    return Promise.all(
      urls.map(
        (url) => this.fetchAsDataUri(url).then((dataUri) => ({ url, dataUri })).catch(() => ({ url, dataUri: null }))
      )
    ).then((results) => {
      let out = css;
      let replaced = 0;
      results.forEach(({ url, dataUri }) => {
        if (!dataUri) return;
        out = out.split(url).join(dataUri);
        replaced++;
      });
      return replaced ? out : null;
    });
  }
  /**
   * Fetch a binary asset as a base64 data URI.
   *
   * Uses `fetch` rather than the `<img>`+canvas route in `getBase64FromUrl`:
   * that route only works for raster images and taints the canvas for any
   * response without CORS headers, whereas this works for fonts too and fails
   * cleanly when CORS denies it.
   * @param {string} url
   * @returns {Promise<string>}
   */
  fetchAsDataUri(url) {
    if (typeof fetch !== "function" || typeof btoa !== "function") {
      return Promise.reject(new Error("fetch unavailable"));
    }
    return fetch(url).then((res) => {
      if (!res.ok) throw new Error(`HTTP ${res.status} for ${url}`);
      const type = res.headers.get("content-type") || "application/octet-stream";
      return res.arrayBuffer().then((buf) => {
        const bytes = new Uint8Array(buf);
        let binary = "";
        const CHUNK = 32768;
        for (let i2 = 0; i2 < bytes.length; i2 += CHUNK) {
          binary += String.fromCharCode.apply(
            null,
            /** @type {any} */
            bytes.subarray(i2, i2 + CHUNK)
          );
        }
        return `data:${type};base64,${btoa(binary)}`;
      });
    });
  }
  /**
   * @param {number} [_scale]
   */
  getSvgString(_scale) {
    return new Promise((resolve) => {
      const w2 = this.w;
      let scale = _scale || w2.config.chart.toolbar.export.scale || w2.config.chart.toolbar.export.width / w2.globals.svgWidth;
      if (!scale) {
        scale = 1;
      }
      const width = w2.globals.svgWidth * scale;
      const height = w2.globals.svgHeight * scale;
      const clonedNode = (
        /** @type {HTMLElement} */
        w2.dom.elWrap.cloneNode(true)
      );
      clonedNode.style.width = width + "px";
      clonedNode.style.height = height + "px";
      this.inlineCanvasLayers(clonedNode);
      this.applyExportStyles(clonedNode);
      const serializedNode = new XMLSerializer().serializeToString(clonedNode);
      let svgString = `
        <svg xmlns="http://www.w3.org/2000/svg"
          version="1.1"
          xmlns:xlink="http://www.w3.org/1999/xlink"
          class="apexcharts-svg"
          xmlns:data="ApexChartsNS"
          transform="translate(0, 0)"
          width="${w2.globals.svgWidth}px" height="${w2.globals.svgHeight}px">
          <foreignObject width="100%" height="100%">
            <div xmlns="http://www.w3.org/1999/xhtml" style="width:${width}px; height:${height}px;">
              ${serializedNode}
            </div>
          </foreignObject>
        </svg>
      `;
      const svgNode = this.svgStringToNode(svgString);
      if (scale !== 1) {
        this.scaleSvgNode(svgNode, scale);
      }
      Promise.all([
        this.convertImagesToBase64(svgNode),
        this.embedFonts(svgNode)
      ]).then(() => {
        svgString = new XMLSerializer().serializeToString(svgNode);
        resolve(svgString.replace(/&nbsp;/g, "&#160;"));
      });
    });
  }
  /**
   * Turn every remote `<image>` in the export into an inline data URI.
   *
   * This is not an optimisation: an SVG rasterized through `<img src="data:...">`
   * is not allowed to fetch external resources, so any href left pointing at a
   * URL vanishes from the PNG entirely. Image annotations and `hollow.image`
   * were disappearing from downloads for exactly this reason. See #3170.
   *
   * Two things were missing before. `SVGContainer.image()` writes `xlink:href`,
   * but Strata's inlined canvas layers and hand-authored `customSVG` markup use
   * the plain `href` form, and only the namespaced attribute was being read.
   * And conversion went through `<img>`+canvas, which taints (and therefore
   * throws) for any response without CORS headers, so a cross-origin icon,
   * the common case, silently failed. `fetch` is tried first and only falls
   * back to the canvas route, which still helps for a same-origin image on a
   * page whose CSP blocks `connect-src`.
   * @param {any} svgNode
   */
  convertImagesToBase64(svgNode) {
    const XLINK = "http://www.w3.org/1999/xlink";
    const images = svgNode.getElementsByTagName("image");
    const promises = Array.from(images).map((img) => {
      const nsHref = img.getAttributeNS(XLINK, "href");
      const plainHref = img.getAttribute("href");
      const href = nsHref || plainHref;
      if (!href || href.startsWith("data:")) return Promise.resolve();
      const write = (base64) => {
        if (nsHref) img.setAttributeNS(XLINK, "href", base64);
        if (plainHref || !nsHref) img.setAttribute("href", base64);
      };
      return this.fetchAsDataUri(href).then(write).catch(
        () => this.getBase64FromUrl(href).then(write).catch((error) => {
          console.error("Error converting image to base64:", error);
        })
      );
    });
    return Promise.all(promises);
  }
  /**
   * @param {string} url
   */
  getBase64FromUrl(url) {
    if (Environment.isSSR()) return Promise.resolve(url);
    return new Promise((resolve, reject) => {
      const img = new Image();
      img.crossOrigin = "Anonymous";
      img.onload = () => {
        const canvas = document.createElement("canvas");
        canvas.width = img.width;
        canvas.height = img.height;
        const ctx = canvas.getContext("2d");
        if (ctx) ctx.drawImage(img, 0, 0);
        resolve(canvas.toDataURL());
      };
      img.onerror = reject;
      img.src = url;
    });
  }
  svgUrl() {
    return new Promise((resolve) => {
      this.getSvgString().then((svgData) => {
        const svgBlob = new Blob([svgData], {
          type: "image/svg+xml;charset=utf-8"
        });
        resolve(URL.createObjectURL(svgBlob));
      });
    });
  }
  /**
   * @param {Record<string, any> | undefined} options
   */
  dataURI(options2) {
    if (Environment.isSSR()) return Promise.resolve({ imgURI: "" });
    return new Promise((resolve) => {
      const w2 = this.w;
      const scale = options2 ? options2.scale || options2.width / w2.globals.svgWidth : 1;
      const canvas = document.createElement("canvas");
      canvas.width = w2.globals.svgWidth * scale;
      canvas.height = parseInt(w2.dom.elWrap.style.height, 10) * scale;
      const canvasBg = this.resolveExportBackground();
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      ctx.fillStyle = canvasBg;
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      this.getSvgString(scale).then((svgData) => {
        const svgUrl = "data:image/svg+xml," + encodeURIComponent(svgData);
        const img = new Image();
        img.crossOrigin = "anonymous";
        img.onload = () => {
          ctx.drawImage(img, 0, 0);
          const edgeCanvas = canvas;
          if (edgeCanvas.msToBlob) {
            const blob = edgeCanvas.msToBlob();
            resolve({ blob });
          } else {
            const imgURI = canvas.toDataURL("image/png");
            resolve({ imgURI });
          }
        };
        img.src = svgUrl;
      });
    });
  }
  exportToSVG() {
    this.svgUrl().then((url) => {
      this.triggerDownload(
        url,
        this.w.config.chart.toolbar.export.svg.filename,
        ".svg"
      );
    });
  }
  exportToPng() {
    const scale = this.w.config.chart.toolbar.export.scale;
    const width = this.w.config.chart.toolbar.export.width;
    const option = scale ? { scale } : width ? { width } : void 0;
    this.dataURI(option).then(({ imgURI, blob }) => {
      if (blob) {
        navigator.msSaveOrOpenBlob(blob, this.w.globals.chartID + ".png");
      } else {
        this.triggerDownload(
          imgURI,
          this.w.config.chart.toolbar.export.png.filename,
          ".png"
        );
      }
    });
  }
  /** @param {{ series?: any, fileName?: any, columnDelimiter?: string, lineDelimiter?: string }} opts */
  exportToCSV({
    series,
    fileName,
    columnDelimiter = ",",
    lineDelimiter = "\n"
  }) {
    const w2 = this.w;
    if (!series) series = w2.config.series;
    let columns = [];
    const rows = [];
    let result = "";
    const universalBOM = "\uFEFF";
    const gSeries = w2.seriesData.series.map((s2, i2) => {
      return w2.globals.collapsedSeriesIndices.indexOf(i2) === -1 ? s2 : [];
    });
    const csvSafe = (val) => {
      if (val == null || Utils$1.isNumber(val)) return val;
      const s2 = String(val);
      return /^[=+\-@\t\r]/.test(s2) ? `'${s2}` : s2;
    };
    const getFormattedCategory = (cat) => {
      if (typeof w2.config.chart.toolbar.export.csv.categoryFormatter === "function") {
        return w2.config.chart.toolbar.export.csv.categoryFormatter(cat);
      }
      if (w2.config.xaxis.type === "datetime" && String(cat).length >= 10) {
        return new Date(cat).toDateString();
      }
      return Utils$1.isNumber(cat) ? cat : csvSafe(cat.split(columnDelimiter).join(""));
    };
    const getFormattedValue = (value) => {
      return typeof w2.config.chart.toolbar.export.csv.valueFormatter === "function" ? w2.config.chart.toolbar.export.csv.valueFormatter(value) : csvSafe(value);
    };
    const seriesMaxDataLength = Math.max(
      ...series.map((s2) => {
        return s2.data ? s2.data.length : 0;
      })
    );
    const dataFormat = new Data(this.w);
    const axesUtils = new AxesUtils(this.w, {
      theme: this.ctx.theme,
      timeScale: this.ctx.timeScale
    });
    const getCat = (i2) => {
      let cat = "";
      if (!w2.globals.axisCharts) {
        cat = w2.config.labels[i2];
      } else {
        if (w2.config.xaxis.type === "category" || w2.config.xaxis.convertedCatToNumeric) {
          if (w2.globals.isBarHorizontal) {
            const lbFormatter = w2.formatters.yLabelFormatters[0];
            const sr = new Series(this.ctx.w);
            const activeSeries = sr.getActiveConfigSeriesIndex();
            cat = lbFormatter(w2.labelData.labels[i2], {
              seriesIndex: activeSeries,
              dataPointIndex: i2,
              w: w2
            });
          } else {
            cat = axesUtils.getLabel(
              w2.labelData.labels,
              w2.labelData.timescaleLabels,
              0,
              i2
            ).text;
          }
        }
        if (w2.config.xaxis.type === "datetime") {
          if (w2.config.xaxis.categories.length) {
            cat = w2.config.xaxis.categories[i2];
          } else if (w2.config.labels.length) {
            cat = w2.config.labels[i2];
          }
        }
      }
      if (cat === null) return "nullvalue";
      if (Array.isArray(cat)) {
        cat = cat.join(" ");
      }
      return Utils$1.isNumber(cat) ? cat : cat.split(columnDelimiter).join("");
    };
    const getEmptyDataForCsvColumn = () => {
      return [...Array(seriesMaxDataLength)].map(() => "");
    };
    const handleAxisRowsColumns = (s2, sI) => {
      var _a, _b, _c, _d, _e, _f;
      if (columns.length && sI === 0) {
        rows.push(columns.join(columnDelimiter));
      }
      if (s2.data) {
        const rowData = s2.data.length ? s2.data : getEmptyDataForCsvColumn();
        for (let i2 = 0; i2 < rowData.length; i2++) {
          columns = [];
          let cat = getCat(i2);
          if (cat === "nullvalue") continue;
          if (!cat) {
            if (dataFormat.isFormatXY()) {
              cat = series[sI].data[i2].x;
            } else if (dataFormat.isFormat2DArray()) {
              cat = series[sI].data[i2] ? series[sI].data[i2][0] : "";
            }
          }
          if (sI === 0) {
            columns.push(getFormattedCategory(cat));
            for (let ci = 0; ci < w2.seriesData.series.length; ci++) {
              const value = dataFormat.isFormatXY() ? (_a = series[ci].data[i2]) == null ? void 0 : _a.y : gSeries[ci][i2];
              columns.push(getFormattedValue(value));
            }
          }
          if (w2.config.chart.type === "candlestick" || s2.type && s2.type === "candlestick") {
            columns.pop();
            columns.push(w2.candleData.seriesCandleO[sI][i2]);
            columns.push(w2.candleData.seriesCandleH[sI][i2]);
            columns.push(w2.candleData.seriesCandleL[sI][i2]);
            columns.push(w2.candleData.seriesCandleC[sI][i2]);
          }
          if (w2.config.chart.type === "boxPlot" || s2.type && s2.type === "boxPlot") {
            columns.pop();
            columns.push(w2.candleData.seriesCandleO[sI][i2]);
            columns.push(w2.candleData.seriesCandleH[sI][i2]);
            columns.push(w2.candleData.seriesCandleM[sI][i2]);
            columns.push(w2.candleData.seriesCandleL[sI][i2]);
            columns.push(w2.candleData.seriesCandleC[sI][i2]);
          }
          if (w2.config.chart.type === "rangeBar") {
            columns.pop();
            columns.push(w2.rangeData.seriesRangeStart[sI][i2]);
            columns.push(w2.rangeData.seriesRangeEnd[sI][i2]);
          }
          if (w2.config.chart.type === "violin" || s2.type && s2.type === "violin") {
            columns.pop();
            columns.push((_b = w2.violinData.seriesViolinMin[sI]) == null ? void 0 : _b[i2]);
            columns.push((_c = w2.violinData.seriesViolinMax[sI]) == null ? void 0 : _c[i2]);
            columns.push((_f = (_e = (_d = w2.violinData.seriesViolinPoints[sI]) == null ? void 0 : _d[i2]) == null ? void 0 : _e.length) != null ? _f : 0);
          }
          if (columns.length) {
            rows.push(columns.join(columnDelimiter));
          }
        }
      }
    };
    const handleUnequalXValues = () => {
      const categories = /* @__PURE__ */ new Set();
      const data = {};
      series.forEach((s2, sI) => {
        s2 == null ? void 0 : s2.data.forEach((dataItem) => {
          let cat, value;
          if (dataFormat.isFormatXY()) {
            cat = dataItem.x;
            value = dataItem.y;
          } else if (dataFormat.isFormat2DArray()) {
            cat = dataItem[0];
            value = dataItem[1];
          } else {
            return;
          }
          if (!/** @type {Record<string,any>} */
          data[cat]) {
            data[cat] = Array(
              series.length
            ).fill("");
          }
          data[cat][sI] = getFormattedValue(value);
          categories.add(cat);
        });
      });
      if (columns.length) {
        rows.push(columns.join(columnDelimiter));
      }
      Array.from(categories).sort().forEach((cat) => {
        const values = (
          /** @type {Record<string,any>} */
          data[cat]
        );
        rows.push([getFormattedCategory(cat), ...values].join(columnDelimiter));
      });
    };
    columns.push(w2.config.chart.toolbar.export.csv.headerCategory);
    if (w2.config.chart.type === "boxPlot") {
      columns.push("minimum");
      columns.push("q1");
      columns.push("median");
      columns.push("q3");
      columns.push("maximum");
    } else if (w2.config.chart.type === "candlestick") {
      columns.push("open");
      columns.push("high");
      columns.push("low");
      columns.push("close");
    } else if (w2.config.chart.type === "rangeBar") {
      columns.push("minimum");
      columns.push("maximum");
    } else if (w2.config.chart.type === "violin") {
      columns.push("minimum");
      columns.push("maximum");
      columns.push("observations");
    } else {
      series.map((s2, sI) => {
        const sname = (s2.name ? s2.name : `series-${sI}`) + "";
        if (w2.globals.axisCharts) {
          columns.push(
            sname.split(columnDelimiter).join("") ? sname.split(columnDelimiter).join("") : `series-${sI}`
          );
        }
      });
    }
    if (!w2.globals.axisCharts) {
      columns.push(w2.config.chart.toolbar.export.csv.headerValue);
      rows.push(columns.join(columnDelimiter));
    }
    if (!w2.globals.allSeriesHasEqualX && w2.globals.axisCharts && !w2.config.xaxis.categories.length && !w2.config.labels.length) {
      handleUnequalXValues();
    } else {
      series.map((s2, sI) => {
        if (w2.globals.axisCharts) {
          handleAxisRowsColumns(s2, sI);
        } else {
          columns = [];
          columns.push(getFormattedCategory(w2.labelData.labels[sI]));
          columns.push(getFormattedValue(gSeries[sI]));
          rows.push(columns.join(columnDelimiter));
        }
      });
    }
    result += rows.join(lineDelimiter);
    this.triggerDownload(
      "data:text/csv; charset=utf-8," + encodeURIComponent(universalBOM + result),
      fileName ? fileName : w2.config.chart.toolbar.export.csv.filename,
      ".csv"
    );
  }
  /**
   * @param {string} href
   * @param {string} filename
   * @param {string} ext
   */
  triggerDownload(href, filename, ext) {
    if (Environment.isSSR()) return;
    const downloadLink = document.createElement("a");
    downloadLink.href = href;
    downloadLink.download = (filename ? filename : this.w.globals.chartID) + ext;
    document.body.appendChild(downloadLink);
    downloadLink.click();
    document.body.removeChild(downloadLink);
  }
}
ApexCharts.registerFeatures({ exports: Exports });
const apexchartsLegendCSS = ".apexcharts-flip-y {\n  transform: scaleY(-1) translateY(-100%);\n  transform-origin: top;\n  transform-box: fill-box;\n}\n.apexcharts-flip-x {\n  transform: scaleX(-1);\n  transform-origin: center;\n  transform-box: fill-box;\n}\n.apexcharts-legend {\n  display: flex;\n  overflow: auto;\n  padding: 0 10px;\n}\n.apexcharts-legend.apexcharts-legend-group-horizontal {\n  flex-direction: column;\n}\n.apexcharts-legend-group {\n  display: flex;\n}\n.apexcharts-legend-group-vertical {\n  flex-direction: column-reverse;\n}\n.apexcharts-legend.apx-legend-position-bottom, .apexcharts-legend.apx-legend-position-top {\n  flex-wrap: wrap\n}\n.apexcharts-legend.apx-legend-position-right, .apexcharts-legend.apx-legend-position-left {\n  flex-direction: column;\n  bottom: 0;\n}\n.apexcharts-legend.apx-legend-position-bottom.apexcharts-align-left, .apexcharts-legend.apx-legend-position-top.apexcharts-align-left, .apexcharts-legend.apx-legend-position-right, .apexcharts-legend.apx-legend-position-left {\n  justify-content: flex-start;\n  align-items: flex-start;\n}\n.apexcharts-legend.apx-legend-position-bottom.apexcharts-align-center, .apexcharts-legend.apx-legend-position-top.apexcharts-align-center {\n  justify-content: center;\n  align-items: center;\n}\n.apexcharts-legend.apx-legend-position-bottom.apexcharts-align-right, .apexcharts-legend.apx-legend-position-top.apexcharts-align-right {\n  justify-content: flex-end;\n  align-items: flex-end;\n}\n.apexcharts-legend-series {\n  cursor: pointer;\n  line-height: normal;\n  display: flex;\n  align-items: center;\n}\n.apexcharts-legend-text {\n  position: relative;\n  font-size: 14px;\n}\n.apexcharts-legend-text *, .apexcharts-legend-marker * {\n  pointer-events: none;\n}\n.apexcharts-legend-marker {\n  position: relative;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  cursor: pointer;\n  margin-right: 1px;\n}\n\n.apexcharts-legend-series.apexcharts-no-click {\n  cursor: auto;\n}\n.apexcharts-legend .apexcharts-hidden-zero-series, .apexcharts-legend .apexcharts-hidden-null-series {\n  display: none !important;\n}\n.apexcharts-inactive-legend {\n  opacity: 0.45;\n} ";
let Helpers$3 = class Helpers2 {
  /**
   * @param {import('./Legend').default} lgCtx
   */
  constructor(lgCtx) {
    this.w = lgCtx.w;
    this.lgCtx = lgCtx;
  }
  getLegendStyles() {
    if (Environment.isSSR()) return null;
    const stylesheet = document.createElement("style");
    stylesheet.setAttribute("type", "text/css");
    const nonce = this.w.config.chart.nonce;
    if (nonce) {
      stylesheet.setAttribute("nonce", nonce);
    }
    const rule = document.createTextNode(apexchartsLegendCSS);
    stylesheet.appendChild(rule);
    return stylesheet;
  }
  getLegendDimensions() {
    const w2 = this.w;
    const currLegendsWrap = w2.dom.baseEl.querySelector(".apexcharts-legend");
    if (!currLegendsWrap) {
      return { clwh: 0, clww: 0 };
    }
    const { width: currLegendsWrapWidth, height: currLegendsWrapHeight } = currLegendsWrap.getBoundingClientRect();
    return {
      clwh: currLegendsWrapHeight,
      clww: currLegendsWrapWidth
    };
  }
  appendToForeignObject() {
    var _a;
    const legendStyles = this.getLegendStyles();
    if (this.w.config.chart.injectStyleSheet !== false && legendStyles) {
      (_a = this.w.dom.elLegendForeign) == null ? void 0 : _a.appendChild(legendStyles);
    }
  }
  /**
   * @param {number} seriesCnt
   * @param {boolean} isHidden
   */
  toggleDataSeries(seriesCnt, isHidden) {
    var _a, _b, _c;
    const w2 = this.w;
    if (w2.globals.axisCharts || w2.config.chart.type === "radialBar") {
      w2.globals.resized = true;
      let seriesEl = null;
      let realIndex = null;
      w2.globals.risingSeries = [];
      if (w2.globals.axisCharts) {
        seriesEl = (_a = Array.prototype.find.call(
          w2.dom.baseEl.querySelectorAll(".apexcharts-series"),
          (el) => el.getAttribute("data:realIndex") === String(seriesCnt)
        )) != null ? _a : null;
        if (!seriesEl) return;
        realIndex = parseInt((_b = seriesEl.getAttribute("data:realIndex")) != null ? _b : "", 10);
      } else {
        seriesEl = w2.dom.baseEl.querySelector(
          `.apexcharts-series[rel='${seriesCnt + 1}']`
        );
        if (!seriesEl) return;
        realIndex = parseInt((_c = seriesEl.getAttribute("rel")) != null ? _c : "", 10) - 1;
      }
      if (isHidden) {
        const seriesToMakeVisible = [
          {
            cs: w2.globals.collapsedSeries,
            csi: w2.globals.collapsedSeriesIndices
          },
          {
            cs: w2.globals.ancillaryCollapsedSeries,
            csi: w2.globals.ancillaryCollapsedSeriesIndices
          }
        ];
        seriesToMakeVisible.forEach((r2) => {
          const cs = (
            /** @type {any} */
            r2.cs
          );
          const csi = (
            /** @type {any} */
            r2.csi
          );
          this.riseCollapsedSeries(
            cs,
            csi,
            /** @type {number} */
            realIndex
          );
        });
      } else {
        this.hideSeries({ seriesEl, realIndex });
      }
      if (w2.config.chart.accessibility.enabled) {
        const legendItem = w2.dom.baseEl.querySelector(
          `.apexcharts-legend-series[rel="${seriesCnt + 1}"]`
        );
        if (legendItem) {
          const isCollapsed = w2.globals.collapsedSeriesIndices.includes(realIndex) || w2.globals.ancillaryCollapsedSeriesIndices.includes(realIndex);
          legendItem.setAttribute(
            "aria-pressed",
            isCollapsed ? "true" : "false"
          );
          const legendTextEl = legendItem.querySelector(
            ".apexcharts-legend-text"
          );
          const seriesName = legendTextEl ? legendTextEl.textContent : w2.seriesData.seriesNames[seriesCnt];
          const statusText = isCollapsed ? "hidden" : "visible";
          legendItem.setAttribute(
            "aria-label",
            `${seriesName}, ${statusText}. Press Enter or Space to toggle.`
          );
        }
      }
    } else {
      w2.globals.resized = true;
      w2.globals.risingSeries = [];
      if (isHidden) {
        this.riseCollapsedSeries(
          w2.globals.collapsedSeries,
          w2.globals.collapsedSeriesIndices,
          seriesCnt
        );
      } else {
        const series = this.getSeriesAfterCollapsing({ realIndex: seriesCnt });
        this.lgCtx.updateSeries(
          series,
          w2.config.chart.animations.dynamicAnimation.enabled
        );
      }
      if (w2.config.chart.accessibility.enabled) {
        const legendItem = w2.dom.baseEl.querySelector(
          `.apexcharts-legend-series[rel="${seriesCnt + 1}"]`
        );
        if (legendItem) {
          const isCollapsed = w2.globals.collapsedSeriesIndices.includes(seriesCnt);
          legendItem.setAttribute(
            "aria-pressed",
            isCollapsed ? "true" : "false"
          );
          const legendTextEl = legendItem.querySelector(
            ".apexcharts-legend-text"
          );
          const seriesName = legendTextEl ? legendTextEl.textContent : w2.seriesData.seriesNames[seriesCnt];
          const statusText = isCollapsed ? "hidden" : "visible";
          legendItem.setAttribute(
            "aria-label",
            `${seriesName}, ${statusText}. Press Enter or Space to toggle.`
          );
        }
      }
    }
  }
  /**
   * Non-axis "slice" container. A pie/donut/polarArea slice is normally a
   * top-level series element (numeric form: `series = [n, n, n]`), but object
   * form (`series = [{ data: [{ x, y, drilldown }, ...] }]`, which pie/donut
   * drilldown requires) packs every slice as a data point inside a single
   * series. Return the array that actually holds the slice values so a
   * slice/legend index addresses the right thing. Scoped to the pie family so
   * unit charts (which share this non-axis path) are untouched.
   * @param {any[]} series
   * @returns {any[]}
   */
  _nonAxisSliceContainer(series) {
    const type = this.w.config.chart.type;
    if ((type === "pie" || type === "donut" || type === "polarArea" || type === "sunburst") && series.length === 1 && series[0] && typeof series[0] === "object" && Array.isArray(series[0].data)) {
      return series[0].data;
    }
    return series;
  }
  /**
   * Read a non-axis slice value (handles `{ x, y }` data points and plain
   * numbers).
   * @param {any} sliceEntry
   * @returns {number}
   */
  _readSliceValue(sliceEntry) {
    if (this.w.config.chart.type === "unit" && sliceEntry && Array.isArray(sliceEntry.data)) {
      return sliceEntry.data;
    }
    return sliceEntry && typeof sliceEntry === "object" ? sliceEntry.y : sliceEntry;
  }
  /**
   * Write a non-axis slice value in place, preserving `{ x, drilldown, ... }`
   * on object data points.
   * @param {any[]} container
   * @param {number} i
   * @param {number} value
   */
  _writeSliceValue(container, i2, value) {
    const entry = container[i2];
    if (this.w.config.chart.type === "unit" && entry && Array.isArray(entry.data)) {
      entry.data = Array.isArray(value) ? value : [];
      return;
    }
    if (entry && typeof entry === "object") {
      entry.y = value;
    } else {
      container[i2] = value;
    }
  }
  /** @param {{realIndex: any}} opts */
  getSeriesAfterCollapsing({ realIndex }) {
    var _a, _b;
    const w2 = this.w;
    const gl = w2.globals;
    const series = Utils$1.clone(w2.config.series);
    if (gl.axisCharts) {
      const yaxis = w2.config.yaxis[gl.seriesYAxisReverseMap[realIndex]];
      const collapseData = {
        index: realIndex,
        data: series[realIndex].data.slice(),
        type: series[realIndex].type || w2.config.chart.type,
        // The category name pins the hide across a data update that reorders or
        // regroups categories (e.g. a storyboard beat): the collapse is
        // reconciled by name, not index. See Series.reconcileCollapsedByName.
        name: (gl.seriesNames || [])[realIndex]
      };
      if (yaxis && yaxis.show && yaxis.showAlways) {
        if (gl.ancillaryCollapsedSeriesIndices.indexOf(realIndex) < 0) {
          gl.ancillaryCollapsedSeries.push(collapseData);
          gl.ancillaryCollapsedSeriesIndices.push(realIndex);
        }
      } else {
        if (gl.collapsedSeriesIndices.indexOf(realIndex) < 0) {
          gl.collapsedSeries.push(collapseData);
          gl.collapsedSeriesIndices.push(realIndex);
          const removeIndexOfRising = gl.risingSeries.indexOf(realIndex);
          gl.risingSeries.splice(removeIndexOfRising, 1);
        }
      }
    } else {
      if (gl.collapsedSeriesIndices.indexOf(realIndex) < 0) {
        const container = this._nonAxisSliceContainer(series);
        gl.collapsedSeries.push({
          index: realIndex,
          // Store the original slice VALUE so it can be restored on rise. In
          // object form this is a data point's `y`, not the whole series entry.
          data: this._readSliceValue(container[realIndex]),
          type: (
            /** @type {any} */
            (_b = (_a = w2.config.series[realIndex]) == null ? void 0 : _a.type) != null ? _b : "line"
          ),
          // Pin the hide by category name so it survives a regroup (see above).
          name: (gl.seriesNames || [])[realIndex]
        });
        gl.collapsedSeriesIndices.push(realIndex);
      }
    }
    const seriesCount = gl.axisCharts ? w2.config.series.length : this._nonAxisSliceContainer(series).length;
    gl.allSeriesCollapsed = gl.collapsedSeries.length + gl.ancillaryCollapsedSeries.length === seriesCount;
    return this._getSeriesBasedOnCollapsedState(series);
  }
  /** @param {{seriesEl: any, realIndex: any}} opts */
  hideSeries({ seriesEl, realIndex }) {
    const w2 = this.w;
    const series = this.getSeriesAfterCollapsing({
      realIndex
    });
    const seriesChildren = seriesEl.childNodes;
    for (let sc = 0; sc < seriesChildren.length; sc++) {
      if (seriesChildren[sc].classList.contains("apexcharts-series-markers-wrap")) {
        if (seriesChildren[sc].classList.contains("apexcharts-hide")) {
          seriesChildren[sc].classList.remove("apexcharts-hide");
        } else {
          seriesChildren[sc].classList.add("apexcharts-hide");
        }
      }
    }
    this.lgCtx.updateSeries(
      series,
      w2.config.chart.animations.dynamicAnimation.enabled
    );
  }
  /**
   * @param {any[]} collapsedSeries
   * @param {number[]} seriesIndices
   * @param {number} realIndex
   */
  riseCollapsedSeries(collapsedSeries, seriesIndices, realIndex) {
    const w2 = this.w;
    let series = Utils$1.clone(w2.config.series);
    if (collapsedSeries.length > 0) {
      for (let c2 = 0; c2 < collapsedSeries.length; c2++) {
        if (collapsedSeries[c2].index === realIndex) {
          if (w2.globals.axisCharts) {
            series[realIndex].data = collapsedSeries[c2].data.slice();
            series[realIndex].hidden = false;
          } else {
            const container = this._nonAxisSliceContainer(series);
            this._writeSliceValue(container, realIndex, collapsedSeries[c2].data);
          }
          collapsedSeries.splice(c2, 1);
          seriesIndices.splice(c2, 1);
          w2.globals.risingSeries.push(realIndex);
          c2--;
        }
      }
      series = this._getSeriesBasedOnCollapsedState(series);
      this.lgCtx.updateSeries(
        series,
        w2.config.chart.animations.dynamicAnimation.enabled
      );
    }
  }
  /**
   * @param {any[]} series
   */
  _getSeriesBasedOnCollapsedState(series) {
    const w2 = this.w;
    let collapsed = 0;
    if (w2.globals.axisCharts) {
      series.forEach((s2, sI) => {
        if (!(w2.globals.collapsedSeriesIndices.indexOf(sI) < 0 && w2.globals.ancillaryCollapsedSeriesIndices.indexOf(sI) < 0)) {
          series[sI].data = [];
          collapsed++;
        }
      });
    } else {
      const container = this._nonAxisSliceContainer(series);
      container.forEach((s2, sI) => {
        if (!(w2.globals.collapsedSeriesIndices.indexOf(sI) < 0)) {
          this._writeSliceValue(container, sI, 0);
          collapsed++;
        }
      });
    }
    const seriesCount = w2.globals.axisCharts ? series.length : this._nonAxisSliceContainer(series).length;
    w2.globals.allSeriesCollapsed = collapsed === seriesCount;
    return series;
  }
};
const DEFAULT_DIVERGING = ["#cf4d3f", "#8f9499", "#26a75b"];
const lerp$1 = (a2, b2, t2) => a2 + (b2 - a2) * t2;
function toHexPair(n2) {
  const v2 = Math.max(0, Math.min(255, Math.round(n2)));
  return v2.toString(16).padStart(2, "0");
}
function mixColors(c1, c2, t2) {
  const a2 = Utils$1.parseHex(normalizeHex(c1));
  const b2 = Utils$1.parseHex(normalizeHex(c2));
  if (!a2 || !b2) return c1;
  return "#" + toHexPair(lerp$1(a2[0], b2[0], t2)) + toHexPair(lerp$1(a2[1], b2[1], t2)) + toHexPair(lerp$1(a2[2], b2[2], t2));
}
function normalizeHex(c2) {
  if (typeof c2 !== "string") return "#000000";
  if (Utils$1.isColorHex(c2)) return c2;
  const asHex = Utils$1.rgb2hex(c2);
  return asHex || "#000000";
}
function colorValueOf(w2, i2, j) {
  const series = (
    /** @type {any} */
    w2.config.series[i2]
  );
  const datum = series && Array.isArray(series.data) ? series.data[j] : null;
  return colorValueOfDatum(w2, datum, i2, j);
}
function colorValueOfDatum(w2, datum, i2, j) {
  var _a, _b, _c;
  if (!datum || typeof datum !== "object") return null;
  const accessor = (_c = (_b = (_a = w2.config.plotOptions) == null ? void 0 : _a.treemap) == null ? void 0 : _b.colorScale) == null ? void 0 : _c.colorValue;
  let raw;
  if (typeof accessor === "function") {
    raw = accessor(datum, { seriesIndex: i2, dataPointIndex: j, w: w2 });
  } else if (typeof accessor === "string") {
    raw = datum[accessor];
  } else {
    raw = datum.colorValue;
  }
  if (raw == null) return null;
  const n2 = Number(raw);
  return Number.isFinite(n2) ? n2 : null;
}
function resolveStops(cfg, min, max, midpoint) {
  if (Array.isArray(cfg.stops) && cfg.stops.length >= 2) {
    return cfg.stops.filter((s2) => s2 && Number.isFinite(Number(s2.value))).map((s2) => ({
      value: Number(s2.value),
      color: normalizeHex(s2.color)
    })).sort(
      (a2, b2) => a2.value - b2.value
    );
  }
  const colors = (Array.isArray(cfg.colors) && cfg.colors.length >= 2 ? cfg.colors : DEFAULT_DIVERGING).map(normalizeHex);
  const n2 = colors.length;
  if (midpoint != null && n2 >= 3) {
    const mid = Math.floor((n2 - 1) / 2);
    const out = [];
    for (let k = 0; k <= mid; k++) {
      out.push({ value: lerp$1(min, midpoint, k / mid), color: colors[k] });
    }
    for (let k = mid + 1; k < n2; k++) {
      out.push({
        value: lerp$1(midpoint, max, (k - mid) / (n2 - 1 - mid)),
        color: colors[k]
      });
    }
    return out;
  }
  return colors.map((c2, k) => ({
    value: lerp$1(min, max, k / (n2 - 1)),
    color: c2
  }));
}
function buildContinuousScale(w2) {
  var _a, _b, _c;
  const cs = (_c = (_b = (_a = w2.config) == null ? void 0 : _a.plotOptions) == null ? void 0 : _b.treemap) == null ? void 0 : _c.colorScale;
  const cfg = cs && cs.gradient;
  if (!cfg) return null;
  if (cfg.enabled === false) return null;
  const series = (
    /** @type {any} */
    w2.config.series || []
  );
  let dataMin = Infinity;
  let dataMax = -Infinity;
  let found = false;
  for (let i2 = 0; i2 < series.length; i2++) {
    const data = series[i2] && series[i2].data;
    if (!Array.isArray(data)) continue;
    for (let j = 0; j < data.length; j++) {
      const v2 = colorValueOfDatum(w2, data[j], i2, j);
      if (v2 == null) continue;
      found = true;
      if (v2 < dataMin) dataMin = v2;
      if (v2 > dataMax) dataMax = v2;
    }
  }
  if (!found && cfg.enabled !== true) return null;
  if (!Number.isFinite(dataMin)) {
    dataMin = 0;
    dataMax = 0;
  }
  let min = Number.isFinite(Number(cfg.min)) ? Number(cfg.min) : dataMin;
  let max = Number.isFinite(Number(cfg.max)) ? Number(cfg.max) : dataMax;
  let midpoint = null;
  if (cfg.midpoint === null) {
    midpoint = null;
  } else if (Number.isFinite(Number(cfg.midpoint))) {
    midpoint = Number(cfg.midpoint);
  } else if (min < 0 && max > 0) {
    midpoint = 0;
  }
  if (midpoint != null && cfg.symmetric !== false && !Number.isFinite(Number(cfg.min)) && !Number.isFinite(Number(cfg.max))) {
    const reach = Math.max(Math.abs(min - midpoint), Math.abs(max - midpoint));
    min = midpoint - reach;
    max = midpoint + reach;
  }
  if (max === min) {
    min -= 0.5;
    max += 0.5;
  }
  const stops = resolveStops(cfg, min, max, midpoint);
  if (stops.length < 2) return null;
  const at = (v2) => {
    if (!Number.isFinite(v2)) return stops[Math.floor(stops.length / 2)].color;
    if (v2 <= stops[0].value) return stops[0].color;
    const last = stops[stops.length - 1];
    if (v2 >= last.value) return last.color;
    for (let k = 1; k < stops.length; k++) {
      const hi = stops[k];
      if (v2 <= hi.value) {
        const lo = stops[k - 1];
        const span2 = hi.value - lo.value;
        const t2 = span2 === 0 ? 0 : (v2 - lo.value) / span2;
        return mixColors(lo.color, hi.color, t2);
      }
    }
    return last.color;
  };
  const span = max - min;
  const legendStops = stops.map((s2) => ({
    percent: span === 0 ? 0 : (s2.value - min) / span,
    color: s2.color
  }));
  return { min, max, midpoint, stops, at, legendStops };
}
function readableOn(bg) {
  const hex = normalizeHex(bg);
  return Utils$1.getContrastRatio(hex, "#ffffff") >= Utils$1.getContrastRatio(hex, "#000000") ? "#ffffff" : "#000000";
}
const SVG_NS = "http://www.w3.org/2000/svg";
class HeatmapGradientLegend {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.svgEl = null;
    this.arrowEl = null;
    this.hoverValueEl = null;
    this._min = 0;
    this._max = 0;
    this._geom = null;
    this._bandHitEls = [];
    this._activeBandIndex = -1;
    this._onCellEnter = this._onCellEnter.bind(this);
    this._onCellLeave = this._onCellLeave.bind(this);
    this._onBandEnter = this._onBandEnter.bind(this);
    this._onBandLeave = this._onBandLeave.bind(this);
  }
  /** Default value formatter for min/max labels and the hover tooltip. */
  _getFormatter() {
    const cfg = this._cfg();
    if (typeof cfg.formatter === "function") return cfg.formatter;
    return (v2) => {
      if (!Number.isFinite(v2)) return String(v2);
      const abs = Math.abs(v2);
      if (abs >= 1e3) return v2.toFixed(0);
      if (abs >= 10) return v2.toFixed(1);
      return v2.toFixed(2);
    };
  }
  /**
   * The colorScale of whichever chart type is being drawn. Every chart type
   * that encodes a value as colour carries the same `colorScale` shape, so one
   * strip serves them all rather than a near-copy per type.
   * @param {any} w
   */
  static colorScaleOf(w2) {
    var _a, _b, _c, _d, _e;
    const type = (_b = (_a = w2 == null ? void 0 : w2.config) == null ? void 0 : _a.chart) == null ? void 0 : _b.type;
    if (!type) return null;
    return ((_e = (_d = (_c = w2 == null ? void 0 : w2.config) == null ? void 0 : _c.plotOptions) == null ? void 0 : _d[type]) == null ? void 0 : _e.colorScale) || null;
  }
  /**
   * @param {any} w
   */
  static configFor(w2) {
    const cs = HeatmapGradientLegend.colorScaleOf(w2);
    return cs && cs.gradientLegend || null;
  }
  /** This instance's gradient-legend config. */
  _cfg() {
    return HeatmapGradientLegend.configFor(this.w) || {};
  }
  /**
   * True when the user has opted into the gradient legend variant.
   * @param {any} w
   */
  static isEnabled(w2) {
    if (!HeatmapGradientLegend.supports(w2)) return false;
    const cfg = HeatmapGradientLegend.configFor(w2);
    return !!(cfg && cfg.enabled);
  }
  /**
   * Chart types this legend can serve: those that encode a value as colour
   * through a `colorScale`. Everything else gets the categorical legend.
   * @param {any} w
   */
  static supports(w2) {
    var _a, _b;
    const type = (_b = (_a = w2 == null ? void 0 : w2.config) == null ? void 0 : _a.chart) == null ? void 0 : _b.type;
    return type === "heatmap" || type === "treemap";
  }
  /**
   * Build the gradient legend DOM into `elLegendWrap`.
   * Caller is responsible for clearing the wrap first.
   */
  draw() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const w2 = this.w;
    const elLegendWrap = (
      /** @type {HTMLElement} */
      w2.dom.elLegendWrap
    );
    if (!elLegendWrap) return;
    const cfg = this._cfg();
    const position = w2.config.legend.position;
    const isVertical = position === "left" || position === "right";
    const arrowSize = (_b = (_a = cfg.arrow) == null ? void 0 : _a.size) != null ? _b : 8;
    const arrowGutter = arrowSize + 4;
    const labelPadAlongStrip = cfg.showLabels ? 28 : 4;
    const labelPadAcrossStrip = cfg.showLabels ? 20 : 4;
    const minLabelWidth = cfg.showLabels ? 44 : 0;
    const stripLength = this._resolveStripLength(isVertical ? cfg.height : cfg.width, isVertical);
    const stripThickness = cfg.thickness;
    const svgWidth = isVertical ? Math.max(stripThickness + arrowGutter + 4, minLabelWidth) : stripLength + labelPadAlongStrip * 2;
    const svgHeight = isVertical ? stripLength + labelPadAcrossStrip * 2 : stripThickness + arrowGutter + 4;
    const verticalGroupWidth = stripThickness + arrowGutter;
    const verticalGroupLeftPad = (svgWidth - verticalGroupWidth) / 2;
    const stripX = isVertical ? position === "left" ? verticalGroupLeftPad : verticalGroupLeftPad + arrowGutter : labelPadAlongStrip;
    const stripY = isVertical ? labelPadAcrossStrip : position === "top" ? arrowGutter : 4;
    const svg = BrowserAPIs.createElementNS(SVG_NS, "svg");
    svg.setAttribute(
      "class",
      "apexcharts-heatmap-gradient-legend apexcharts-gradient-legend"
    );
    svg.setAttribute("width", String(svgWidth));
    svg.setAttribute("height", String(svgHeight));
    svg.setAttribute("overflow", "visible");
    const defs = BrowserAPIs.createElementNS(SVG_NS, "defs");
    const gradId = `apexcharts-heatmap-gradient-${w2.globals.cuid}`;
    const linearGrad = BrowserAPIs.createElementNS(SVG_NS, "linearGradient");
    linearGrad.setAttribute("id", gradId);
    if (isVertical) {
      linearGrad.setAttribute("x1", "0");
      linearGrad.setAttribute("y1", "1");
      linearGrad.setAttribute("x2", "0");
      linearGrad.setAttribute("y2", "0");
    } else {
      linearGrad.setAttribute("x1", "0");
      linearGrad.setAttribute("y1", "0");
      linearGrad.setAttribute("x2", "1");
      linearGrad.setAttribute("y2", "0");
    }
    const { min, max, stops, bands } = this._computeStops();
    this._min = min;
    this._max = max;
    stops.forEach((s2) => {
      const stopEl = BrowserAPIs.createElementNS(SVG_NS, "stop");
      stopEl.setAttribute("offset", `${(s2.percent * 100).toFixed(2)}%`);
      stopEl.setAttribute("stop-color", s2.color);
      linearGrad.appendChild(stopEl);
    });
    defs.appendChild(linearGrad);
    svg.appendChild(defs);
    const rect = BrowserAPIs.createElementNS(SVG_NS, "rect");
    rect.setAttribute("x", String(stripX));
    rect.setAttribute("y", String(stripY));
    rect.setAttribute("width", String(isVertical ? stripThickness : stripLength));
    rect.setAttribute("height", String(isVertical ? stripLength : stripThickness));
    rect.setAttribute("rx", "2");
    rect.setAttribute("fill", `url(#${gradId})`);
    svg.appendChild(rect);
    if (cfg.showLabels) {
      const labelColor = ((_c = cfg.labelStyle) == null ? void 0 : _c.colors) || (Array.isArray(w2.config.legend.labels.colors) ? w2.config.legend.labels.colors[0] : w2.config.legend.labels.colors) || w2.config.chart.foreColor;
      const labelFontSize = ((_d = cfg.labelStyle) == null ? void 0 : _d.fontSize) || "11px";
      const labelFontFamily = ((_e = cfg.labelStyle) == null ? void 0 : _e.fontFamily) || w2.config.chart.fontFamily;
      const fmt = this._getFormatter();
      const makeLabel = (text, x, y, anchor) => {
        const t2 = BrowserAPIs.createElementNS(SVG_NS, "text");
        t2.setAttribute("x", String(x));
        t2.setAttribute("y", String(y));
        t2.setAttribute("text-anchor", anchor);
        t2.setAttribute("dominant-baseline", "middle");
        t2.setAttribute("fill", labelColor);
        t2.setAttribute("font-size", labelFontSize);
        if (labelFontFamily) t2.setAttribute("font-family", labelFontFamily);
        t2.textContent = String(text);
        return t2;
      };
      if (isVertical) {
        const midX = stripX + stripThickness / 2;
        svg.appendChild(makeLabel(fmt(min), midX, stripY + stripLength + 10, "middle"));
        svg.appendChild(makeLabel(fmt(max), midX, stripY - 10, "middle"));
      } else {
        const midY = stripY + stripThickness / 2;
        svg.appendChild(makeLabel(fmt(min), stripX - 6, midY, "end"));
        svg.appendChild(makeLabel(fmt(max), stripX + stripLength + 6, midY, "start"));
      }
    }
    const arrowColor = ((_f = cfg.arrow) == null ? void 0 : _f.color) || w2.config.chart.foreColor;
    const arrow = this._buildArrow(arrowSize, arrowColor, position);
    svg.appendChild(arrow);
    this.arrowEl = arrow;
    this._bandHitEls = [];
    if (w2.config.legend.onItemHover.highlightDataSeries && bands.length > 0) {
      bands.forEach((b2) => {
        const hit = BrowserAPIs.createElementNS(SVG_NS, "rect");
        if (isVertical) {
          const yTop = stripY + stripLength - b2.p2 * stripLength;
          const yBot = stripY + stripLength - b2.p1 * stripLength;
          hit.setAttribute("x", String(stripX));
          hit.setAttribute("y", String(yTop));
          hit.setAttribute("width", String(stripThickness));
          hit.setAttribute("height", String(Math.max(0, yBot - yTop)));
        } else {
          hit.setAttribute("x", String(stripX + b2.p1 * stripLength));
          hit.setAttribute("y", String(stripY));
          hit.setAttribute(
            "width",
            String(Math.max(0, (b2.p2 - b2.p1) * stripLength))
          );
          hit.setAttribute("height", String(stripThickness));
        }
        hit.setAttribute("fill", "transparent");
        hit.setAttribute("class", "apexcharts-heatmap-gradient-band");
        hit.setAttribute("data:range-index", String(b2.index));
        hit.style.cursor = "pointer";
        svg.appendChild(hit);
        this._bandHitEls.push(hit);
      });
    }
    this._geom = {
      isVertical,
      position,
      stripX,
      stripY,
      stripLength,
      stripThickness,
      arrowSize,
      svgWidth,
      svgHeight
    };
    if (cfg.showHoverValue) {
      const tt = BrowserAPIs.createElement("div");
      tt.classList.add("apexcharts-heatmap-gradient-legend-value");
      tt.style.position = "absolute";
      tt.style.fontSize = ((_g = cfg.labelStyle) == null ? void 0 : _g.fontSize) || "11px";
      tt.style.fontFamily = ((_h = cfg.labelStyle) == null ? void 0 : _h.fontFamily) || w2.config.chart.fontFamily || "";
      tt.style.color = w2.config.chart.foreColor;
      tt.style.background = "rgba(0,0,0,0.65)";
      tt.style.color = "#fff";
      tt.style.padding = "2px 6px";
      tt.style.borderRadius = "3px";
      tt.style.pointerEvents = "none";
      tt.style.whiteSpace = "nowrap";
      tt.style.opacity = "0";
      tt.style.transition = "opacity 120ms ease";
      this.hoverValueEl = tt;
    }
    elLegendWrap.classList.add("apexcharts-heatmap-gradient-legend-wrap");
    elLegendWrap.classList.add(
      "apx-legend-position-" + position
    );
    elLegendWrap.appendChild(svg);
    if (this.hoverValueEl) elLegendWrap.appendChild(this.hoverValueEl);
    this.svgEl = svg;
    this._applyWrapAlignment(elLegendWrap, position, isVertical, svgWidth, svgHeight);
    this._attachHoverListeners();
    this._attachBandHoverListeners();
  }
  /**
   * Resolve a configured length (number = px, string ending in '%' =
   * percentage of the chart's SVG width/height) to a pixel length.
   * @param {number|string} value
   * @param {boolean} isVertical
   * @returns {number}
   */
  _resolveStripLength(value, isVertical) {
    const w2 = this.w;
    const basis = isVertical ? w2.globals.svgHeight || w2.config.chart.height || 300 : w2.globals.svgWidth || w2.config.chart.width || 600;
    if (typeof value === "string") {
      const trimmed = value.trim();
      if (trimmed.endsWith("%")) {
        const pct = parseFloat(trimmed) || 0;
        return Math.max(20, basis * pct / 100);
      }
      const n2 = parseFloat(trimmed);
      return Number.isFinite(n2) ? n2 : 200;
    }
    if (typeof value === "number" && Number.isFinite(value)) return value;
    return 200;
  }
  /**
   * Position the legend wrap and align the gradient strip within it. The
   * wrap spans the chart's long axis (full width for top/bottom; full
   * height for left/right) and uses flexbox to honor the `align` config.
   * Bypasses the standard `setLegendWrapXY` which sizes the wrap to its
   * content.
   * @param {HTMLElement} elLegendWrap
   * @param {'top'|'right'|'bottom'|'left'} position
   * @param {boolean} isVertical
   * @param {number} svgWidth
   * @param {number} svgHeight
   */
  _applyWrapAlignment(elLegendWrap, position, isVertical, svgWidth, svgHeight) {
    const w2 = this.w;
    const cfg = this._cfg();
    const align = cfg.align || "center";
    const edgePad = 12;
    const chartWidth = w2.globals.svgWidth || w2.config.chart.width || 600;
    const chartHeight = w2.globals.svgHeight || w2.config.chart.height || 300;
    const userOffsetX = w2.config.legend.offsetX || 0;
    const userOffsetY = w2.config.legend.offsetY || 0;
    elLegendWrap.style.position = "absolute";
    elLegendWrap.style.display = "block";
    elLegendWrap.style.overflow = "visible";
    elLegendWrap.style.padding = "0";
    elLegendWrap.style.width = svgWidth + "px";
    elLegendWrap.style.height = svgHeight + "px";
    elLegendWrap.style.right = "auto";
    elLegendWrap.style.bottom = "auto";
    if (isVertical) {
      const availableHeight = chartHeight - svgHeight - edgePad * 2;
      let y;
      if (align === "start") y = edgePad;
      else if (align === "end") y = edgePad + Math.max(0, availableHeight);
      else y = edgePad + Math.max(0, availableHeight) / 2;
      elLegendWrap.style.top = y + userOffsetY + "px";
      if (position === "left") {
        elLegendWrap.style.left = edgePad + userOffsetX + "px";
      } else {
        elLegendWrap.style.left = chartWidth - svgWidth - edgePad + userOffsetX + "px";
      }
    } else {
      const availableWidth = chartWidth - svgWidth - edgePad * 2;
      let x;
      if (align === "start") x = edgePad;
      else if (align === "end") x = edgePad + Math.max(0, availableWidth);
      else x = edgePad + Math.max(0, availableWidth) / 2;
      elLegendWrap.style.left = x + userOffsetX + "px";
      if (position === "top") {
        elLegendWrap.style.top = edgePad + userOffsetY + "px";
      } else {
        elLegendWrap.style.top = chartHeight - svgHeight - edgePad + userOffsetY + "px";
      }
    }
  }
  /**
   * Re-position the strip once the final layout is known.
   *
   * `_applyWrapAlignment` (called during `draw()`, before `plotCoords()`) can
   * only pin to the chart's outer edge. This runs after layout — when
   * `translateX/Y`, `gridWidth/Height` and `xAxisHeight` are populated — and:
   *   - centers the strip within its reserved band on the perpendicular axis
   *     (between the title and the plot for `top`; the x-axis and the chart
   *     bottom for `bottom`; the chart edge and the plot for `left`/`right`),
   *     so the slack is split evenly instead of dumped on one side, and
   *   - aligns it along the plot's own extent (so `align: 'center'` centers
   *     over the heatmap, not the whole canvas).
   * Honors `legend.offsetX/offsetY` for user nudging. Safe to call repeatedly.
   */
  repositionToPlot() {
    var _a, _b;
    if (!Environment.isBrowser()) return;
    const w2 = this.w;
    const g2 = w2.globals;
    const wrap = (
      /** @type {HTMLElement} */
      w2.dom.elLegendWrap
    );
    if (!wrap || !this._geom) return;
    if (!Number.isFinite(g2.gridWidth) || !Number.isFinite(g2.gridHeight)) return;
    const { isVertical, position, svgWidth, svgHeight, stripX, stripY, stripThickness } = this._geom;
    const align = this._cfg().align || "center";
    const ox = w2.config.legend.offsetX || 0;
    const oy = w2.config.legend.offsetY || 0;
    const dimHelpers = (_b = (_a = this.ctx) == null ? void 0 : _a.dimensions) == null ? void 0 : _b.dimHelpers;
    const titleArea = dimHelpers ? dimHelpers.getTitleSubtitleCoords("title").height + dimHelpers.getTitleSubtitleCoords("subtitle").height : 0;
    const xAxisArea = w2.layout.xAxisHeight || 0;
    const alongOffset = (extent, size) => {
      const avail = Math.max(0, extent - size);
      if (align === "start") return 0;
      if (align === "end") return avail;
      return avail / 2;
    };
    if (isVertical) {
      wrap.style.top = g2.translateY + alongOffset(g2.gridHeight, svgHeight) + oy + "px";
      const bandStart = position === "left" ? 0 : g2.translateX + g2.gridWidth;
      const bandEnd = position === "left" ? g2.translateX : g2.svgWidth;
      const stripCenter = (bandStart + bandEnd) / 2;
      wrap.style.left = stripCenter - stripX - stripThickness / 2 + ox + "px";
    } else {
      wrap.style.left = g2.translateX + alongOffset(g2.gridWidth, svgWidth) + ox + "px";
      const bandStart = position === "top" ? titleArea : g2.translateY + g2.gridHeight + xAxisArea;
      const bandEnd = position === "top" ? g2.translateY : g2.svgHeight;
      const stripCenter = (bandStart + bandEnd) / 2;
      wrap.style.top = stripCenter - stripY - stripThickness / 2 + oy + "px";
    }
    BrowserAPIs.requestAnimationFrame(() => this._enforceMinPlotGap());
  }
  /**
   * Guarantee a minimum gap between the strip's chart-facing edge and the plot.
   * Measured in viewport space (immune to the wrap↔SVG coordinate offset) and
   * applied as a *relative* shift to the wrap's current position, so it only
   * nudges a strip that ended up too close — placements with ample room are
   * left exactly where centering put them. Runs post-paint (see caller).
   */
  _enforceMinPlotGap() {
    const w2 = this.w;
    const wrap = (
      /** @type {HTMLElement} */
      w2.dom.elLegendWrap
    );
    const strip = this.svgEl && this.svgEl.querySelector("rect");
    const grid = w2.dom.baseEl.querySelector(".apexcharts-grid");
    if (!wrap || !strip || !grid || !this._geom) return;
    const s2 = strip.getBoundingClientRect();
    const gr = grid.getBoundingClientRect();
    if (!s2.width || !s2.height || !gr.width || !gr.height) return;
    const MIN_GAP = 16;
    const { isVertical, position } = this._geom;
    if (isVertical) {
      const gap = position === "left" ? gr.left - s2.right : s2.left - gr.right;
      if (gap < MIN_GAP) {
        const curLeft = parseFloat(wrap.style.left) || 0;
        const shift = MIN_GAP - gap;
        wrap.style.left = curLeft + (position === "left" ? -shift : shift) + "px";
      }
    } else {
      const gap = position === "top" ? gr.top - s2.bottom : s2.top - gr.bottom;
      if (gap < MIN_GAP) {
        const curTop = parseFloat(wrap.style.top) || 0;
        const shift = MIN_GAP - gap;
        wrap.style.top = curTop + (position === "top" ? -shift : shift) + "px";
      }
    }
  }
  /**
   * Tear down listeners (called before re-render).
   */
  destroy() {
    var _a, _b, _c, _d, _e, _f, _g;
    for (let i2 = 0; i2 < this._bandHitEls.length; i2++) {
      const el = this._bandHitEls[i2];
      (_a = el.removeEventListener) == null ? void 0 : _a.call(el, "mousemove", this._onBandEnter);
      (_b = el.removeEventListener) == null ? void 0 : _b.call(el, "mouseout", this._onBandLeave);
    }
    this._bandHitEls = [];
    this._activeBandIndex = -1;
    if (!((_c = this.ctx) == null ? void 0 : _c.events)) return;
    try {
      (_e = (_d = this.ctx.events).removeEventListener) == null ? void 0 : _e.call(
        _d,
        "dataPointMouseEnter",
        this._onCellEnter
      );
      (_g = (_f = this.ctx.events).removeEventListener) == null ? void 0 : _g.call(
        _f,
        "dataPointMouseLeave",
        this._onCellLeave
      );
    } catch (_) {
    }
  }
  /** Wire mousemove/mouseout on each per-band hit-region (ranges mode). */
  _attachBandHoverListeners() {
    if (!Environment.isBrowser()) return;
    for (let i2 = 0; i2 < this._bandHitEls.length; i2++) {
      const el = this._bandHitEls[i2];
      el.addEventListener("mousemove", this._onBandEnter);
      el.addEventListener("mouseout", this._onBandLeave);
    }
  }
  /**
   * Hovering a gradient band highlights its cells and dims the rest. Guarded
   * so the repeated mousemove stream only re-applies on an actual band change.
   * @param {Event} e
   */
  _onBandEnter(e2) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const target = (
      /** @type {Element} */
      e2.currentTarget
    );
    const idx = parseInt((_a = target.getAttribute("data:range-index")) != null ? _a : "-1", 10);
    if (idx < 0 || idx === this._activeBandIndex) return;
    this._activeBandIndex = idx;
    (_d = (_c = (_b = this.ctx) == null ? void 0 : _b.events) == null ? void 0 : _c.fireEvent) == null ? void 0 : _d.call(_c, "legendHover", [this.ctx, idx, w2]);
    new Series(w2).highlightRangeInSeries(idx, "highlight");
  }
  /** Leaving a band clears the highlight. */
  _onBandLeave() {
    if (this._activeBandIndex < 0) return;
    const idx = this._activeBandIndex;
    this._activeBandIndex = -1;
    new Series(this.w).highlightRangeInSeries(idx, "reset");
  }
  _attachHoverListeners() {
    var _a, _b;
    if (!Environment.isBrowser()) return;
    if (!((_b = (_a = this.ctx) == null ? void 0 : _a.events) == null ? void 0 : _b.addEventListener)) return;
    this.ctx.events.addEventListener(
      "dataPointMouseEnter",
      this._onCellEnter
    );
    this.ctx.events.addEventListener(
      "dataPointMouseLeave",
      this._onCellLeave
    );
  }
  /**
   * dataPointMouseEnter fires as `(e, ctx, { seriesIndex, dataPointIndex, w })`.
   * Graphics._fireEvent forwards listener args in the same shape.
   * @param {...any} args
   */
  _onCellEnter(...args) {
    var _a, _b, _c;
    const w2 = this.w;
    if (!this.arrowEl) return;
    const opts = args[args.length - 1];
    if (!opts || typeof opts !== "object") return;
    const i2 = opts.seriesIndex;
    const j = opts.dataPointIndex;
    if (typeof i2 !== "number" || typeof j !== "number") return;
    if (!HeatmapGradientLegend.supports(w2)) return;
    let val;
    if (this._continuous) {
      val = colorValueOf(w2, i2, j);
    } else {
      val = (_c = (_b = (_a = w2.seriesData) == null ? void 0 : _a.series) == null ? void 0 : _b[i2]) == null ? void 0 : _c[j];
    }
    if (val == null || Number.isNaN(val)) return;
    this._positionArrow(val);
  }
  _onCellLeave() {
    if (!this.arrowEl) return;
    this.arrowEl.setAttribute("opacity", "0");
    if (this.hoverValueEl) {
      this.hoverValueEl.style.opacity = "0";
    }
  }
  /**
   * Move the arrow to the position corresponding to `val` along the strip.
   * @param {number} val
   */
  _positionArrow(val) {
    if (!this.arrowEl || !this._geom) return;
    const { isVertical, position, stripX, stripY, stripLength, stripThickness, arrowSize } = this._geom;
    const min = this._min;
    const max = this._max;
    const span = max - min;
    let pct;
    if (span === 0) {
      pct = 0.5;
    } else {
      pct = (val - min) / span;
    }
    if (pct < 0) pct = 0;
    if (pct > 1) pct = 1;
    if (isVertical) {
      const yCenter = stripY + stripLength - pct * stripLength;
      let tipX, baseX;
      if (position === "left") {
        tipX = stripX + stripThickness;
        baseX = tipX + arrowSize;
      } else {
        tipX = stripX;
        baseX = tipX - arrowSize;
      }
      const points = [
        `${tipX},${yCenter}`,
        `${baseX},${yCenter - arrowSize / 2}`,
        `${baseX},${yCenter + arrowSize / 2}`
      ].join(" ");
      this.arrowEl.setAttribute("points", points);
    } else {
      const xCenter = stripX + pct * stripLength;
      let tipY, baseY;
      if (position === "top") {
        tipY = stripY + stripThickness;
        baseY = tipY + arrowSize;
      } else {
        tipY = stripY;
        baseY = tipY - arrowSize;
      }
      const points = [
        `${xCenter},${tipY}`,
        `${xCenter - arrowSize / 2},${baseY}`,
        `${xCenter + arrowSize / 2},${baseY}`
      ].join(" ");
      this.arrowEl.setAttribute("points", points);
    }
    this.arrowEl.setAttribute("opacity", "1");
    if (this.hoverValueEl) {
      const fmt = this._getFormatter();
      this.hoverValueEl.textContent = fmt(val);
      if (isVertical) {
        const yCenter = stripY + stripLength - pct * stripLength;
        if (position === "left") {
          this.hoverValueEl.style.left = `${stripX + stripThickness + arrowSize + 8}px`;
        } else {
          this.hoverValueEl.style.left = `${stripX - arrowSize - 8}px`;
          this.hoverValueEl.style.transform = "translateX(-100%)";
        }
        this.hoverValueEl.style.top = `${yCenter - 9}px`;
      } else {
        const xCenter = stripX + pct * stripLength;
        this.hoverValueEl.style.left = `${xCenter}px`;
        this.hoverValueEl.style.transform = "translateX(-50%)";
        if (position === "top") {
          this.hoverValueEl.style.top = `${stripY + stripThickness + arrowSize + 8}px`;
        } else {
          this.hoverValueEl.style.top = `${stripY - arrowSize - 18}px`;
        }
      }
      this.hoverValueEl.style.opacity = "1";
    }
  }
  /**
   * @param {number} size
   * @param {string} color
   * @param {'top'|'right'|'bottom'|'left'} _position
   */
  _buildArrow(size, color, _position) {
    const polygon = BrowserAPIs.createElementNS(SVG_NS, "polygon");
    polygon.setAttribute("fill", color);
    polygon.setAttribute("opacity", "0");
    polygon.setAttribute("class", "apexcharts-heatmap-gradient-arrow");
    polygon.setAttribute("points", "0,0 0,0 0,0");
    polygon.setAttribute("pointer-events", "none");
    return polygon;
  }
  /**
   * Build gradient stops + return effective min/max.
   * - If `colorScale.ranges` is set, stops are placed at each range boundary
   *   so the gradient reflects the user's discrete palette.
   * - Otherwise, samples N stops from the same shadeColor function the cells
   *   use, so the strip visually matches the heatmap.
   * @returns {{ min: number, max: number, stops: Array<{percent:number,color:string}>, bands: Array<{index:number,p1:number,p2:number}> }}
   */
  _computeStops() {
    var _a, _b;
    const w2 = this.w;
    const cs = HeatmapGradientLegend.colorScaleOf(w2) || {};
    const cfg = this._cfg();
    const continuous = buildContinuousScale(w2);
    if (continuous) {
      this._continuous = true;
      return {
        min: continuous.min,
        max: continuous.max,
        stops: continuous.legendStops,
        bands: []
      };
    }
    this._continuous = false;
    let dataMin = Infinity;
    let dataMax = -Infinity;
    const rows = ((_a = w2.seriesData) == null ? void 0 : _a.series) || [];
    for (let i2 = 0; i2 < rows.length; i2++) {
      const row = rows[i2];
      if (!row) continue;
      for (let j = 0; j < row.length; j++) {
        const v2 = row[j];
        if (v2 == null || Number.isNaN(v2)) continue;
        if (v2 < dataMin) dataMin = v2;
        if (v2 > dataMax) dataMax = v2;
      }
    }
    if (!Number.isFinite(dataMin)) dataMin = 0;
    if (!Number.isFinite(dataMax)) dataMax = 0;
    let min = dataMin;
    let max = dataMax;
    if (typeof cs.min !== "undefined") {
      min = cs.min < dataMin ? cs.min : dataMin;
    }
    if (typeof cs.max !== "undefined") {
      max = cs.max > dataMax ? cs.max : dataMax;
    }
    const stops = [];
    const bands = [];
    if (cs.ranges && cs.ranges.length > 0) {
      const ranges = cs.ranges.map((r2, originalIndex) => __spreadProps(__spreadValues({}, r2), {
        _originalIndex: originalIndex
      })).sort((a2, b2) => a2.from - b2.from);
      const lo = ranges[0].from;
      const hi = ranges[ranges.length - 1].to;
      min = lo;
      max = hi;
      const span = hi - lo || 1;
      ranges.forEach((r2) => {
        const p1 = (r2.from - lo) / span;
        const p2 = (r2.to - lo) / span;
        stops.push({ percent: (p1 + p2) / 2, color: r2.color });
        bands.push({ index: r2._originalIndex, p1, p2 });
      });
    } else {
      const baseColor = w2.globals.colors[0] || "#008FFB";
      const utils = new Utils$1();
      const plot = w2.config.plotOptions[w2.config.chart.type] || {};
      const shadeIntensity = (_b = plot.shadeIntensity) != null ? _b : 0.5;
      const hasNegs = (
        /** @type {any} */
        w2.globals.hasNegs
      );
      const n2 = Math.max(2, cfg.stops || 16);
      for (let s2 = 0; s2 < n2; s2++) {
        const t2 = s2 / (n2 - 1);
        const v2 = min + t2 * (max - min);
        const total = Math.abs(max) + Math.abs(min);
        const percent_v = total === 0 ? 0 : 100 * v2 / total;
        let colorShadePercent;
        if (hasNegs) {
          if (plot.reverseNegativeShade) {
            colorShadePercent = percent_v < 0 ? percent_v / 100 * (shadeIntensity * 1.25) : (1 - percent_v / 100) * (shadeIntensity * 1.25);
          } else {
            colorShadePercent = percent_v <= 0 ? 1 - (1 + percent_v / 100) * shadeIntensity : (1 - percent_v / 100) * shadeIntensity;
          }
        } else {
          colorShadePercent = 1 - percent_v / 100;
        }
        if (colorShadePercent > 1) colorShadePercent = 1;
        if (colorShadePercent < -1) colorShadePercent = -1;
        const shaded = plot.enableShades ? utils.shadeColor(
          w2.config.theme.mode === "dark" ? colorShadePercent * -1 : colorShadePercent,
          baseColor
        ) : baseColor;
        stops.push({ percent: t2, color: shaded });
      }
    }
    return { min, max, stops, bands };
  }
}
class Legend {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.updateSeries = (...a2) => ctx.updateHelpers._updateSeries(...a2);
    this.onLegendClick = this.onLegendClick.bind(this);
    this.onLegendHovered = this.onLegendHovered.bind(this);
    this.isBarsDistributed = this.w.config.chart.type === "bar" && this.w.config.plotOptions.bar.distributed && this.w.config.series.length === 1;
    this.legendHelpers = new Helpers$3(this);
  }
  init() {
    const w2 = this.w;
    const gl = w2.globals;
    const cnf = w2.config;
    this.isBarsDistributed = cnf.chart.type === "bar" && cnf.plotOptions.bar.distributed && cnf.series.length === 1;
    const showLegendAlways = cnf.legend.showForSingleSeries && this.w.seriesData.series.length === 1 || this.isBarsDistributed || // Heatmap legends are colorScale-driven (discrete ranges or the
    // gradient strip), not series-driven, so they must render even for a
    // single-row heatmap.
    cnf.chart.type === "heatmap" || // Same for a treemap once it has a gradient strip: a nested treemap is
    // usually one series, and the strip describes the colour metric rather
    // than the series.
    HeatmapGradientLegend.isEnabled(w2) || this.w.seriesData.series.length > 1;
    this.legendHelpers.appendToForeignObject();
    if ((showLegendAlways || !gl.axisCharts) && cnf.legend.show) {
      const elLegendWrap = (
        /** @type {HTMLElement} */
        w2.dom.elLegendWrap
      );
      while (elLegendWrap.firstChild) {
        elLegendWrap.removeChild(elLegendWrap.firstChild);
      }
      if (this.heatmapGradientLegend) {
        this.heatmapGradientLegend.destroy();
        this.heatmapGradientLegend = null;
      }
      if (HeatmapGradientLegend.isEnabled(w2)) {
        this.heatmapGradientLegend = new HeatmapGradientLegend(w2, this.ctx);
        this.heatmapGradientLegend.draw();
      } else {
        this.drawLegends();
        if (cnf.legend.position === "bottom" || cnf.legend.position === "top") {
          this.legendAlignHorizontal();
        } else if (cnf.legend.position === "right" || cnf.legend.position === "left") {
          this.legendAlignVertical();
        }
      }
    }
  }
  createLegendMarker({ i: i2, fillcolor }) {
    const w2 = this.w;
    const elMarker = BrowserAPIs.createElement("span");
    elMarker.classList.add("apexcharts-legend-marker");
    const mShape = w2.config.legend.markers.shape || w2.config.markers.shape;
    let shape = mShape;
    if (Array.isArray(mShape)) {
      shape = mShape[i2];
    }
    const mSize = Array.isArray(w2.config.legend.markers.size) ? parseFloat(w2.config.legend.markers.size[i2]) : parseFloat(w2.config.legend.markers.size);
    const mOffsetX = Array.isArray(w2.config.legend.markers.offsetX) ? parseFloat(w2.config.legend.markers.offsetX[i2]) : parseFloat(w2.config.legend.markers.offsetX);
    const mOffsetY = Array.isArray(w2.config.legend.markers.offsetY) ? parseFloat(w2.config.legend.markers.offsetY[i2]) : parseFloat(w2.config.legend.markers.offsetY);
    const mBorderWidth = Array.isArray(w2.config.legend.markers.strokeWidth) ? parseFloat(w2.config.legend.markers.strokeWidth[i2]) : parseFloat(w2.config.legend.markers.strokeWidth);
    const mStyle = elMarker.style;
    mStyle.height = (mSize + mBorderWidth) * 2 + "px";
    mStyle.width = (mSize + mBorderWidth) * 2 + "px";
    mStyle.left = mOffsetX + "px";
    mStyle.top = mOffsetY + "px";
    if (w2.config.legend.markers.customHTML) {
      mStyle.background = "transparent";
      mStyle.color = fillcolor[i2];
      if (Array.isArray(w2.config.legend.markers.customHTML)) {
        if (w2.config.legend.markers.customHTML[i2]) {
          elMarker.innerHTML = w2.config.legend.markers.customHTML[i2]();
        }
      } else {
        elMarker.innerHTML = w2.config.legend.markers.customHTML();
      }
    } else {
      const markers = new Markers(this.ctx.w, this.ctx);
      const markerConfig = markers.getMarkerConfig({
        cssClass: `apexcharts-legend-marker apexcharts-marker apexcharts-marker-${shape}`,
        seriesIndex: i2,
        strokeWidth: mBorderWidth,
        size: mSize
      });
      const SVGLib = Environment.isBrowser() ? (
        /** @type {any} */
        window.SVG
      ) : (
        /** @type {any} */
        global.SVG
      );
      const SVGMarker = SVGLib().addTo(elMarker).size("100%", "100%");
      const marker = new Graphics(this.w).drawMarker(0, 0, __spreadProps(__spreadValues({}, markerConfig), {
        pointFillColor: Array.isArray(fillcolor) ? fillcolor[i2] : markerConfig.pointFillColor,
        shape
      }));
      const shapesEls = w2.dom.Paper.find(
        ".apexcharts-legend-marker.apexcharts-marker"
      );
      shapesEls.forEach((shapeEl) => {
        if (shapeEl.node.classList.contains("apexcharts-marker-triangle")) {
          shapeEl.node.style.transform = "translate(50%, 45%)";
        } else {
          shapeEl.node.style.transform = "translate(50%, 50%)";
        }
      });
      SVGMarker.add(marker);
    }
    return elMarker;
  }
  drawLegends() {
    var _a;
    const me = this;
    const w2 = this.w;
    const elLegendWrap = (
      /** @type {HTMLElement} */
      w2.dom.elLegendWrap
    );
    const fontFamily = w2.config.legend.fontFamily;
    let legendNames = w2.seriesData.seriesNames;
    let fillcolor = w2.config.legend.markers.fillColors ? w2.config.legend.markers.fillColors.slice() : w2.globals.colors.slice();
    if (w2.config.chart.type === "heatmap") {
      const ranges = w2.config.plotOptions.heatmap.colorScale.ranges;
      legendNames = ranges.map((colorScale) => {
        return colorScale.name ? colorScale.name : colorScale.from + " - " + colorScale.to;
      });
      fillcolor = ranges.map((color) => color.color);
    } else if (this.isBarsDistributed) {
      legendNames = w2.labelData.labels.slice();
    }
    if (w2.config.legend.customLegendItems.length) {
      legendNames = w2.config.legend.customLegendItems;
    }
    const legendFormatter = w2.formatters.legendFormatter;
    const isLegendInversed = w2.config.legend.inverseOrder;
    const legendGroups = [];
    if (w2.labelData.seriesGroups.length > 1 && w2.config.legend.clusterGroupedSeries) {
      w2.labelData.seriesGroups.forEach((_, gi) => {
        legendGroups[gi] = BrowserAPIs.createElement("div");
        legendGroups[gi].classList.add(
          "apexcharts-legend-group",
          `apexcharts-legend-group-${gi}`
        );
        if (w2.config.legend.clusterGroupedSeriesOrientation === "horizontal") {
          elLegendWrap.classList.add("apexcharts-legend-group-horizontal");
        } else {
          legendGroups[gi].classList.add("apexcharts-legend-group-vertical");
        }
      });
    }
    for (let i2 = isLegendInversed ? legendNames.length - 1 : 0; isLegendInversed ? i2 >= 0 : i2 <= legendNames.length - 1; isLegendInversed ? i2-- : i2++) {
      const text = legendFormatter(legendNames[i2], { seriesIndex: i2, w: w2 });
      let collapsedSeries = false;
      let ancillaryCollapsedSeries = false;
      if (w2.globals.collapsedSeries.length > 0) {
        for (let c2 = 0; c2 < w2.globals.collapsedSeries.length; c2++) {
          if (w2.globals.collapsedSeries[c2].index === i2) {
            collapsedSeries = true;
          }
        }
      }
      if (w2.globals.ancillaryCollapsedSeriesIndices.length > 0) {
        for (let c2 = 0; c2 < w2.globals.ancillaryCollapsedSeriesIndices.length; c2++) {
          if (w2.globals.ancillaryCollapsedSeriesIndices[c2] === i2) {
            ancillaryCollapsedSeries = true;
          }
        }
      }
      const elMarker = this.createLegendMarker({ i: i2, fillcolor });
      Graphics.setAttrs(elMarker, {
        rel: i2 + 1,
        "data:collapsed": collapsedSeries || ancillaryCollapsedSeries
      });
      if (collapsedSeries || ancillaryCollapsedSeries) {
        elMarker.classList.add("apexcharts-inactive-legend");
      }
      const elLegend = BrowserAPIs.createElement("div");
      if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.keyboard.enabled) {
        elLegend.setAttribute("role", "button");
        elLegend.setAttribute("tabindex", "0");
        const seriesName = Array.isArray(text) ? text.join(" ") : text;
        const isCollapsed = collapsedSeries || ancillaryCollapsedSeries;
        const statusText = isCollapsed ? "hidden" : "visible";
        elLegend.setAttribute(
          "aria-label",
          `${seriesName}, ${statusText}. Press Enter or Space to toggle.`
        );
        elLegend.setAttribute("aria-pressed", isCollapsed ? "true" : "false");
      }
      const elLegendText = BrowserAPIs.createElement("span");
      elLegendText.classList.add("apexcharts-legend-text");
      elLegendText.innerHTML = Array.isArray(text) ? text.join(" ") : text;
      let textColor = w2.config.legend.labels.useSeriesColors ? w2.globals.colors[i2] : Array.isArray(w2.config.legend.labels.colors) ? (_a = w2.config.legend.labels.colors) == null ? void 0 : _a[i2] : w2.config.legend.labels.colors;
      if (!textColor) {
        textColor = w2.config.chart.foreColor;
      }
      elLegendText.style.color = textColor;
      elLegendText.style.fontSize = w2.config.legend.fontSize;
      elLegendText.style.fontWeight = w2.config.legend.fontWeight;
      elLegendText.style.fontFamily = fontFamily || w2.config.chart.fontFamily;
      Graphics.setAttrs(elLegendText, {
        rel: i2 + 1,
        i: i2,
        "data:default-text": encodeURIComponent(text),
        "data:collapsed": collapsedSeries || ancillaryCollapsedSeries
      });
      elLegend.appendChild(elMarker);
      elLegend.appendChild(elLegendText);
      const coreUtils = new CoreUtils(this.w);
      if (!w2.config.legend.showForZeroSeries) {
        const total = coreUtils.getSeriesTotalByIndex(i2);
        if (total === 0 && coreUtils.seriesHaveSameValues(i2) && !coreUtils.isSeriesNull(i2) && w2.globals.collapsedSeriesIndices.indexOf(i2) === -1 && w2.globals.ancillaryCollapsedSeriesIndices.indexOf(i2) === -1) {
          elLegend.classList.add("apexcharts-hidden-zero-series");
        }
      }
      if (!w2.config.legend.showForNullSeries) {
        if (coreUtils.isSeriesNull(i2) && w2.globals.collapsedSeriesIndices.indexOf(i2) === -1 && w2.globals.ancillaryCollapsedSeriesIndices.indexOf(i2) === -1) {
          elLegend.classList.add("apexcharts-hidden-null-series");
        }
      }
      if (legendGroups.length) {
        w2.labelData.seriesGroups.forEach((group, gi) => {
          var _a2, _b;
          if (group.includes(
            /** @type {Record<string,any>} */
            (_b = (_a2 = w2.config.series[i2]) == null ? void 0 : _a2.name) != null ? _b : ""
          )) {
            elLegendWrap.appendChild(legendGroups[gi]);
            legendGroups[gi].appendChild(elLegend);
          }
        });
      } else {
        elLegendWrap.appendChild(elLegend);
      }
      elLegendWrap.classList.add(
        `apexcharts-align-${w2.config.legend.horizontalAlign}`
      );
      elLegendWrap.classList.add(
        "apx-legend-position-" + w2.config.legend.position
      );
      elLegend.classList.add("apexcharts-legend-series");
      elLegend.style.margin = `${w2.config.legend.itemMargin.vertical}px ${w2.config.legend.itemMargin.horizontal}px`;
      elLegendWrap.style.width = w2.config.legend.width ? w2.config.legend.width + "px" : "";
      elLegendWrap.style.height = w2.config.legend.height ? w2.config.legend.height + "px" : "";
      Graphics.setAttrs(elLegend, {
        rel: i2 + 1,
        seriesName: Utils$1.escapeString(legendNames[i2]),
        "data:collapsed": collapsedSeries || ancillaryCollapsedSeries
      });
      if (collapsedSeries || ancillaryCollapsedSeries) {
        elLegend.classList.add("apexcharts-inactive-legend");
      }
      if (!w2.config.legend.onItemClick.toggleDataSeries) {
        elLegend.classList.add("apexcharts-no-click");
      }
    }
    w2.dom.elWrap.addEventListener("click", me.onLegendClick, true);
    if (w2.config.legend.onItemHover.highlightDataSeries && w2.config.legend.customLegendItems.length === 0) {
      w2.dom.elWrap.addEventListener("mousemove", me.onLegendHovered, true);
      w2.dom.elWrap.addEventListener("mouseout", me.onLegendHovered, true);
    }
    if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.keyboard.enabled) {
      w2.dom.elWrap.addEventListener(
        "keydown",
        me.onLegendKeyDown.bind(me),
        true
      );
    }
  }
  /**
   * @param {number} offsetX
   * @param {number} offsetY
   */
  setLegendWrapXY(offsetX, offsetY) {
    const w2 = this.w;
    const elLegendWrap = (
      /** @type {HTMLElement} */
      w2.dom.elLegendWrap
    );
    const legendHeight = elLegendWrap.clientHeight;
    let x = 0;
    let y = 0;
    if (w2.config.legend.position === "bottom") {
      y = w2.globals.svgHeight - Math.min(legendHeight, w2.globals.svgHeight / 2) - 5;
    } else if (w2.config.legend.position === "top") {
      const dim = new Dimensions(this.w, this.ctx);
      const titleH = dim.dimHelpers.getTitleSubtitleCoords("title").height;
      const subtitleH = dim.dimHelpers.getTitleSubtitleCoords("subtitle").height;
      y = (titleH > 0 ? titleH - 10 : 0) + (subtitleH > 0 ? subtitleH - 10 : 0);
    }
    elLegendWrap.style.position = "absolute";
    x = x + offsetX + w2.config.legend.offsetX;
    y = y + offsetY + w2.config.legend.offsetY;
    elLegendWrap.style.left = x + "px";
    elLegendWrap.style.top = y + "px";
    if (w2.config.legend.position === "right") {
      elLegendWrap.style.left = "auto";
      elLegendWrap.style.right = 25 + w2.config.legend.offsetX + "px";
    }
    const fixedHeigthWidth = (
      /** @type {const} */
      ["width", "height"]
    );
    fixedHeigthWidth.forEach((hw) => {
      if (elLegendWrap && elLegendWrap.style[hw]) {
        elLegendWrap.style[hw] = parseInt(String(w2.config.legend[hw]), 10) + "px";
      }
    });
  }
  legendAlignHorizontal() {
    const w2 = this.w;
    const elLegendWrap = (
      /** @type {HTMLElement} */
      w2.dom.elLegendWrap
    );
    elLegendWrap.style.right = "0";
    const dimensions = new Dimensions(this.w, this.ctx);
    const titleRect = dimensions.dimHelpers.getTitleSubtitleCoords("title");
    const subtitleRect = dimensions.dimHelpers.getTitleSubtitleCoords("subtitle");
    const offsetX = 20;
    let offsetY = 0;
    if (w2.config.legend.position === "top") {
      offsetY = titleRect.height + subtitleRect.height + w2.config.title.margin + w2.config.subtitle.margin - 10;
    }
    this.setLegendWrapXY(offsetX, offsetY);
  }
  legendAlignVertical() {
    const w2 = this.w;
    const lRect = this.legendHelpers.getLegendDimensions();
    const offsetY = 20;
    let offsetX = 0;
    if (w2.config.legend.position === "left") {
      offsetX = 20;
    }
    if (w2.config.legend.position === "right") {
      offsetX = w2.globals.svgWidth - lRect.clww - 10;
    }
    this.setLegendWrapXY(offsetX, offsetY);
  }
  /**
   * @param {MouseEvent} e
   */
  onLegendHovered(e2) {
    var _a;
    const w2 = this.w;
    const target = (
      /** @type {Element} */
      e2.target
    );
    const hoverOverLegend = target.classList.contains("apexcharts-legend-series") || target.classList.contains("apexcharts-legend-text") || target.classList.contains("apexcharts-legend-marker");
    if (w2.config.chart.type !== "heatmap" && !this.isBarsDistributed) {
      if (!target.classList.contains("apexcharts-inactive-legend") && hoverOverLegend) {
        const series = new Series(this.ctx.w);
        series.toggleSeriesOnHover(e2, target);
      }
    } else {
      if (hoverOverLegend) {
        const seriesCnt = parseInt((_a = target.getAttribute("rel")) != null ? _a : "0", 10) - 1;
        this.ctx.events.fireEvent("legendHover", [this.ctx, seriesCnt, this.w]);
        const series = new Series(this.ctx.w);
        if (e2.type === "mousemove") {
          series.highlightRangeInSeries(seriesCnt, "highlight");
        } else if (e2.type === "mouseout") {
          series.highlightRangeInSeries(seriesCnt, "reset");
        }
      }
    }
  }
  /**
   * @param {KeyboardEvent} e
   */
  onLegendKeyDown(e2) {
    const me = this;
    const w2 = this.w;
    const target = (
      /** @type {Element} */
      e2.target
    );
    const isLegendItem = target.classList.contains("apexcharts-legend-series") || target.classList.contains("apexcharts-legend-text") || target.classList.contains("apexcharts-legend-marker");
    if (!isLegendItem) return;
    if (e2.key === "Enter" || e2.key === " ") {
      e2.preventDefault();
      const rel = target.getAttribute("rel");
      me.onLegendClick(e2);
      if (rel !== null && w2.config.legend.onItemClick.toggleDataSeries) {
        requestAnimationFrame(() => {
          const restored = w2.dom.baseEl.querySelector(
            `.apexcharts-legend-series[rel="${rel}"]`
          );
          if (restored) restored.focus();
        });
      }
    }
  }
  /**
   * @param {Event} e
   */
  onLegendClick(e2) {
    var _a;
    const w2 = this.w;
    const target = (
      /** @type {Element} */
      e2.target
    );
    if (w2.config.legend.customLegendItems.length) return;
    if (target.classList.contains("apexcharts-legend-series") || target.classList.contains("apexcharts-legend-text") || target.classList.contains("apexcharts-legend-marker")) {
      const seriesCnt = parseInt((_a = target.getAttribute("rel")) != null ? _a : "0", 10) - 1;
      const isHidden = target.getAttribute("data:collapsed") === "true";
      const legendClick = this.w.config.chart.events.legendClick;
      if (typeof legendClick === "function") {
        legendClick(this.ctx, seriesCnt, this.w);
      }
      this.ctx.events.fireEvent("legendClick", [this.ctx, seriesCnt, this.w]);
      const markerClick = this.w.config.legend.markers.onClick;
      if (typeof markerClick === "function" && target.classList.contains("apexcharts-legend-marker")) {
        markerClick(this.ctx, seriesCnt, this.w);
        this.ctx.events.fireEvent("legendMarkerClick", [
          this.ctx,
          seriesCnt,
          this.w
        ]);
      }
      const clickAllowed = w2.config.chart.type !== "treemap" && w2.config.chart.type !== "heatmap" && !this.isBarsDistributed;
      if (clickAllowed && w2.config.legend.onItemClick.toggleDataSeries) {
        this.legendHelpers.toggleDataSeries(seriesCnt, isHidden);
      }
    }
  }
}
ApexCharts.registerFeatures({ legend: Legend });
const icoPan = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M5 9 2 12l3 3"/>\n    <path d="M9 5l3-3 3 3"/>\n    <path d="M15 19l-3 3-3-3"/>\n    <path d="M19 9l3 3-3 3"/>\n    <path d="M2 12h20"/>\n    <path d="M12 2v20"/>\n</svg>\n';
const icoZoom = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <circle cx="11" cy="11" r="7"/>\n    <path d="m21 21-4.3-4.3M8 11h6M11 8v6"/>\n</svg>\n';
const icoReset = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/>\n    <path d="M3 3v5h5"/>\n</svg>\n';
const icoZoomIn = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M12 5v14M5 12h14"/>\n</svg>\n';
const icoZoomOut = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M5 12h14"/>\n</svg>\n';
const icoSelect = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M5 3a2 2 0 0 0-2 2"/>\n    <path d="M19 3a2 2 0 0 1 2 2"/>\n    <path d="M21 19a2 2 0 0 1-2 2"/>\n    <path d="M5 21a2 2 0 0 1-2-2"/>\n    <path d="M9 3h1M14 3h1M9 21h1M14 21h1M3 9v1M3 14v1M21 9v1M21 14v1"/>\n</svg>\n';
const icoMeasure = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M21.3 15.3a2.4 2.4 0 0 1 0 3.4l-2.6 2.6a2.4 2.4 0 0 1-3.4 0L2.7 8.7a2.4 2.4 0 0 1 0-3.4l2.6-2.6a2.4 2.4 0 0 1 3.4 0Z"/>\n    <path d="m14.5 12.5 2-2"/>\n    <path d="m11.5 9.5 2-2"/>\n    <path d="m8.5 6.5 2-2"/>\n    <path d="m17.5 15.5 2-2"/>\n</svg>\n';
const icoMenu = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M4 6h16M4 12h16M4 18h16"/>\n</svg>\n';
class Toolbar {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.ev = this.w.config.chart.events;
    this.selectedClass = "apexcharts-selected";
    this.localeValues = this.w.globals.locale.toolbar;
    this.minX = w2.globals.minX;
    this.maxX = w2.globals.maxX;
    this.elZoom = null;
    this.elZoomIn = null;
    this.elZoomOut = null;
    this.elPan = null;
    this.elSelection = null;
    this.elMeasure = null;
    this.elZoomReset = null;
    this.elMenuIcon = null;
    this.elMenu = null;
    this.elMenuItems = [];
    this.t = null;
  }
  createToolbar() {
    var _a, _b;
    const w2 = this.w;
    const createDiv = () => {
      return BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
    };
    const createBtn = () => {
      const btn = (
        /** @type {HTMLButtonElement} */
        BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "button")
      );
      btn.setAttribute("type", "button");
      return btn;
    };
    const elToolbarWrap = createDiv();
    elToolbarWrap.setAttribute("class", "apexcharts-toolbar");
    elToolbarWrap.style.top = w2.config.chart.toolbar.offsetY + "px";
    elToolbarWrap.style.right = -w2.config.chart.toolbar.offsetX + 3 + "px";
    w2.dom.elWrap.appendChild(elToolbarWrap);
    this.elZoom = createBtn();
    this.elZoomIn = createBtn();
    this.elZoomOut = createBtn();
    this.elPan = createBtn();
    this.elSelection = createBtn();
    this.elMeasure = createBtn();
    this.elZoomReset = createBtn();
    this.elMenuIcon = createBtn();
    this.elMenu = createDiv();
    this.elCustomIcons = [];
    this.t = w2.config.chart.toolbar.tools;
    if (Array.isArray(this.t.customIcons)) {
      for (let i2 = 0; i2 < this.t.customIcons.length; i2++) {
        this.elCustomIcons.push(createBtn());
      }
    }
    const toolbarControls = [];
    const appendZoomControl = (type, el, ico) => {
      const tool = type.toLowerCase();
      if (this.t[tool] && w2.config.chart.zoom.enabled) {
        toolbarControls.push({
          el,
          icon: typeof this.t[tool] === "string" ? this.t[tool] : ico,
          title: (
            /** @type {any} */
            this.localeValues[type]
          ),
          class: `apexcharts-${tool}-icon`
        });
      }
    };
    appendZoomControl("zoomIn", this.elZoomIn, icoZoomIn);
    appendZoomControl("zoomOut", this.elZoomOut, icoZoomOut);
    const zoomSelectionCtrls = (z) => {
      if (this.t[z] && w2.config.chart[z].enabled) {
        toolbarControls.push({
          el: z === "zoom" ? this.elZoom : this.elSelection,
          icon: typeof this.t[z] === "string" ? this.t[z] : z === "zoom" ? icoZoom : icoSelect,
          title: (
            /** @type {any} */
            this.localeValues[z === "zoom" ? "selectionZoom" : "selection"]
          ),
          class: `apexcharts-${z}-icon`
        });
      }
    };
    zoomSelectionCtrls("zoom");
    zoomSelectionCtrls("selection");
    if (this.t.pan && w2.config.chart.zoom.enabled) {
      toolbarControls.push({
        el: this.elPan,
        icon: typeof this.t.pan === "string" ? this.t.pan : icoPan,
        title: this.localeValues.pan,
        class: "apexcharts-pan-icon"
      });
    }
    if (this.t.measure && w2.config.chart.measure && w2.config.chart.measure.enabled) {
      toolbarControls.push({
        el: this.elMeasure,
        icon: typeof this.t.measure === "string" ? this.t.measure : icoMeasure,
        title: (
          /** @type {any} */
          this.localeValues.measure || "Measure"
        ),
        class: "apexcharts-measure-icon"
      });
    }
    appendZoomControl("reset", this.elZoomReset, icoReset);
    if (this.t.download) {
      toolbarControls.push({
        el: this.elMenuIcon,
        icon: typeof this.t.download === "string" ? this.t.download : icoMenu,
        title: this.localeValues.menu,
        class: "apexcharts-menu-icon"
      });
    }
    for (let i2 = 0; i2 < this.elCustomIcons.length; i2++) {
      toolbarControls.push({
        el: this.elCustomIcons[i2],
        icon: this.t.customIcons[i2].icon,
        title: this.t.customIcons[i2].title,
        index: this.t.customIcons[i2].index,
        class: "apexcharts-toolbar-custom-icon " + this.t.customIcons[i2].class
      });
    }
    toolbarControls.forEach((t2, index) => {
      if (t2.index) {
        Utils$1.moveIndexInArray(toolbarControls, index, t2.index);
      }
    });
    for (let i2 = 0; i2 < toolbarControls.length; i2++) {
      Graphics.setAttrs(toolbarControls[i2].el, {
        class: toolbarControls[i2].class,
        title: toolbarControls[i2].title,
        "aria-label": toolbarControls[i2].title
      });
      toolbarControls[i2].el.innerHTML = toolbarControls[i2].icon;
      elToolbarWrap.appendChild(toolbarControls[i2].el);
    }
    if (this.elZoom.parentNode) {
      this.elZoom.setAttribute("aria-pressed", String(!!w2.interact.zoomEnabled));
    }
    if (this.elSelection.parentNode) {
      this.elSelection.setAttribute(
        "aria-pressed",
        String(!!w2.interact.selectionEnabled)
      );
    }
    if (this.elPan.parentNode) {
      this.elPan.setAttribute("aria-pressed", String(!!w2.interact.panEnabled));
    }
    if (this.elMeasure.parentNode) {
      this.elMeasure.setAttribute(
        "aria-pressed",
        String(!!w2.interact.measureEnabled)
      );
    }
    if (this.elMenuIcon.parentNode) {
      this.elMenuIcon.setAttribute("aria-haspopup", "true");
      this.elMenuIcon.setAttribute("aria-expanded", "false");
    }
    this._createHamburgerMenu(elToolbarWrap);
    if (w2.interact.zoomEnabled) {
      this.elZoom.classList.add(this.selectedClass);
    } else if (w2.interact.panEnabled) {
      this.elPan.classList.add(this.selectedClass);
    } else if (w2.interact.selectionEnabled) {
      this.elSelection.classList.add(this.selectedClass);
    } else if (w2.interact.measureEnabled && this.elMeasure) {
      this.elMeasure.classList.add(this.selectedClass);
      (_b = (_a = this.ctx.measure) == null ? void 0 : _a.startMeasure) == null ? void 0 : _b.call(_a);
    }
    this.addToolbarEventListeners();
  }
  /**
   * @param {Element} parent
   */
  _createHamburgerMenu(parent) {
    this.elMenuItems = [];
    parent.appendChild(
      /** @type {Node} */
      this.elMenu
    );
    Graphics.setAttrs(this.elMenu, {
      class: "apexcharts-menu",
      role: "menu"
    });
    const menuItems = [
      {
        name: "exportSVG",
        title: this.localeValues.exportToSVG
      },
      {
        name: "exportPNG",
        title: this.localeValues.exportToPNG
      },
      {
        name: "exportCSV",
        title: this.localeValues.exportToCSV
      }
    ];
    for (let i2 = 0; i2 < menuItems.length; i2++) {
      this.elMenuItems.push(
        BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div")
      );
      this.elMenuItems[i2].innerHTML = menuItems[i2].title;
      Graphics.setAttrs(this.elMenuItems[i2], {
        class: `apexcharts-menu-item ${menuItems[i2].name}`,
        title: menuItems[i2].title,
        role: "menuitem",
        tabindex: "-1"
      });
      this.elMenu.appendChild(this.elMenuItems[i2]);
    }
  }
  addToolbarEventListeners() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    (_a = this.elZoomReset) == null ? void 0 : _a.addEventListener("click", this.handleZoomReset.bind(this));
    (_b = this.elSelection) == null ? void 0 : _b.addEventListener(
      "click",
      this.toggleZoomSelection.bind(this, "selection")
    );
    (_c = this.elZoom) == null ? void 0 : _c.addEventListener(
      "click",
      this.toggleZoomSelection.bind(this, "zoom")
    );
    (_d = this.elZoomIn) == null ? void 0 : _d.addEventListener("click", this.handleZoomIn.bind(this));
    (_e = this.elZoomOut) == null ? void 0 : _e.addEventListener("click", this.handleZoomOut.bind(this));
    (_f = this.elPan) == null ? void 0 : _f.addEventListener("click", this.togglePanning.bind(this));
    (_g = this.elMeasure) == null ? void 0 : _g.addEventListener("click", this.toggleMeasure.bind(this));
    (_h = this.elMenuIcon) == null ? void 0 : _h.addEventListener("click", this.toggleMenu.bind(this));
    this.elMenuItems.forEach((m2) => {
      if (m2.classList.contains("exportSVG")) {
        m2.addEventListener("click", this.handleDownload.bind(this, "svg"));
      } else if (m2.classList.contains("exportPNG")) {
        m2.addEventListener("click", this.handleDownload.bind(this, "png"));
      } else if (m2.classList.contains("exportCSV")) {
        m2.addEventListener("click", this.handleDownload.bind(this, "csv"));
      }
    });
    for (let i2 = 0; i2 < this.t.customIcons.length; i2++) {
      this.elCustomIcons[i2].addEventListener(
        "click",
        this.t.customIcons[i2].click.bind(this, this.ctx, this.ctx.w)
      );
    }
    const toolbarButtons = [
      this.elZoomReset,
      this.elSelection,
      this.elZoom,
      this.elZoomIn,
      this.elZoomOut,
      this.elPan,
      this.elMeasure,
      this.elMenuIcon,
      ...this.elCustomIcons
    ];
    toolbarButtons.forEach((btn) => {
      btn.addEventListener("keydown", (e2) => {
        if (e2.key === "Enter" || e2.key === " ") {
          e2.preventDefault();
          const btnClass = btn.className;
          btn.click();
          requestAnimationFrame(() => {
            const baseEl = this.w.dom.baseEl;
            if (!baseEl) return;
            const apexClass = btnClass.split(" ").find((c2) => c2.startsWith("apexcharts-"));
            if (!apexClass) return;
            const restored = baseEl.querySelector(`.${apexClass}`);
            if (restored) restored.focus();
          });
        }
      });
    });
    (_i = this.elMenuIcon) == null ? void 0 : _i.addEventListener(
      "keydown",
      (e2) => {
        var _a2;
        if (e2.key === "ArrowDown" || e2.key === "ArrowUp") {
          e2.preventDefault();
          if (!((_a2 = this.elMenu) == null ? void 0 : _a2.classList.contains("apexcharts-menu-open"))) {
            this.toggleMenu();
          }
          window.setTimeout(() => {
            const idx = e2.key === "ArrowDown" ? 0 : this.elMenuItems.length - 1;
            if (this.elMenuItems[idx])
              this.elMenuItems[idx].focus();
          }, 20);
        }
      }
    );
    this.elMenuItems.forEach((m2, idx) => {
      m2.addEventListener("keydown", (e2) => {
        var _a2;
        if (e2.key === "ArrowDown") {
          e2.preventDefault();
          const next = this.elMenuItems[idx + 1] || this.elMenuItems[0];
          next.focus();
        } else if (e2.key === "ArrowUp") {
          e2.preventDefault();
          const prev = this.elMenuItems[idx - 1] || this.elMenuItems[this.elMenuItems.length - 1];
          prev.focus();
        } else if (e2.key === "Escape" || e2.key === "Tab") {
          this._closeMenu();
          (_a2 = this.elMenuIcon) == null ? void 0 : _a2.focus();
          if (e2.key === "Tab") ;
          else {
            e2.preventDefault();
          }
        } else if (e2.key === "Enter" || e2.key === " ") {
          e2.preventDefault();
          m2.click();
        }
      });
    });
  }
  /**
   * @param {string} type
   */
  toggleZoomSelection(type) {
    const charts = this.ctx.getSyncedCharts();
    charts.forEach((ch) => {
      const tb = ch.ctx.toolbar;
      const enabledType = type === "selection" ? "selectionEnabled" : "zoomEnabled";
      const wasEnabled = !!ch.w.globals[enabledType];
      tb.toggleOtherControls();
      const el = type === "selection" ? tb.elSelection : tb.elZoom;
      if (!wasEnabled) {
        ch.w.globals[enabledType] = true;
        el.classList.add(tb.selectedClass);
      }
      el.setAttribute("aria-pressed", String(!!ch.w.globals[enabledType]));
    });
  }
  /**
   * Toggle the measure ruler tool. Mutually exclusive with zoom/pan/selection
   * (toggleOtherControls deselects those and disarms any active measure), so a
   * fresh enable arms the ruler via the Measure module's sticky mode.
   */
  toggleMeasure() {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const enabling = !w2.interact.measureEnabled;
    this.toggleOtherControls();
    if (enabling) {
      w2.interact.measureEnabled = true;
      (_a = this.elMeasure) == null ? void 0 : _a.classList.add(this.selectedClass);
      (_c = (_b = this.ctx.measure) == null ? void 0 : _b.startMeasure) == null ? void 0 : _c.call(_b);
    }
    (_d = this.elMeasure) == null ? void 0 : _d.setAttribute(
      "aria-pressed",
      String(w2.interact.measureEnabled)
    );
  }
  getToolbarIconsReference() {
    const w2 = this.w;
    if (!this.elZoom) {
      this.elZoom = w2.dom.baseEl.querySelector(".apexcharts-zoom-icon");
    }
    if (!this.elPan) {
      this.elPan = w2.dom.baseEl.querySelector(".apexcharts-pan-icon");
    }
    if (!this.elSelection) {
      this.elSelection = w2.dom.baseEl.querySelector(
        ".apexcharts-selection-icon"
      );
    }
    if (!this.elMeasure) {
      this.elMeasure = w2.dom.baseEl.querySelector(".apexcharts-measure-icon");
    }
  }
  /**
   * @param {string} type
   */
  enableZoomPanFromToolbar(type) {
    this.toggleOtherControls();
    type === "pan" ? this.w.interact.panEnabled = true : this.w.interact.zoomEnabled = true;
    const el = type === "pan" ? this.elPan : this.elZoom;
    const el2 = type === "pan" ? this.elZoom : this.elPan;
    if (el) {
      el.classList.add(this.selectedClass);
    }
    if (el2) {
      el2.classList.remove(this.selectedClass);
    }
  }
  togglePanning() {
    const charts = this.ctx.getSyncedCharts();
    charts.forEach((ch) => {
      const tb = ch.ctx.toolbar;
      const wasEnabled = !!ch.w.interact.panEnabled;
      tb.toggleOtherControls();
      if (!wasEnabled) {
        ch.w.interact.panEnabled = true;
        tb.elPan.classList.add(tb.selectedClass);
      }
      tb.elPan.setAttribute("aria-pressed", String(!!ch.w.interact.panEnabled));
    });
  }
  toggleOtherControls() {
    var _a, _b, _c;
    const w2 = this.w;
    w2.interact.panEnabled = false;
    w2.interact.zoomEnabled = false;
    w2.interact.selectionEnabled = false;
    if (w2.interact.measureEnabled) {
      w2.interact.measureEnabled = false;
      (_b = (_a = this.ctx.measure) == null ? void 0 : _a.stopMeasure) == null ? void 0 : _b.call(_a);
      (_c = this.elMeasure) == null ? void 0 : _c.setAttribute("aria-pressed", "false");
    }
    this.getToolbarIconsReference();
    const toggleEls = [this.elPan, this.elSelection, this.elZoom, this.elMeasure];
    toggleEls.forEach((el) => {
      if (el) {
        el.classList.remove(this.selectedClass);
      }
    });
  }
  /**
   * Read the current x-range from globals at click time.
   * Toolbar instance is kept alive across updates (Phase 8 lazy
   * instantiation), so cached this.minX/maxX go stale after a zoom.
   * @returns {{minX: number, maxX: number}}
   */
  _currentXRange() {
    const w2 = this.w;
    if (w2.axisFlags.isRangeBar) {
      return { minX: w2.globals.minY, maxX: w2.globals.maxY };
    }
    return { minX: w2.globals.minX, maxX: w2.globals.maxX };
  }
  handleZoomIn() {
    const w2 = this.w;
    const { minX, maxX } = this._currentXRange();
    this.minX = minX;
    this.maxX = maxX;
    const centerX = (minX + maxX) / 2;
    const newMinX = (minX + centerX) / 2;
    const newMaxX = (maxX + centerX) / 2;
    const newMinXMaxX = this._getNewMinXMaxX(newMinX, newMaxX);
    if (!w2.interact.disableZoomIn) {
      this.zoomUpdateOptions(newMinXMaxX.minX, newMinXMaxX.maxX);
    }
  }
  handleZoomOut() {
    const w2 = this.w;
    const { minX, maxX } = this._currentXRange();
    this.minX = minX;
    this.maxX = maxX;
    if (w2.config.xaxis.type === "datetime" && new Date(minX).getUTCFullYear() < 1e3) {
      return;
    }
    const centerX = (minX + maxX) / 2;
    const newMinX = minX - (centerX - minX);
    const newMaxX = maxX - (centerX - maxX);
    const newMinXMaxX = this._getNewMinXMaxX(newMinX, newMaxX);
    if (!w2.interact.disableZoomOut) {
      this.zoomUpdateOptions(newMinXMaxX.minX, newMinXMaxX.maxX);
    }
  }
  /**
   * @param {number} newMinX
   * @param {number} newMaxX
   */
  _getNewMinXMaxX(newMinX, newMaxX) {
    const shouldFloor = this.w.config.xaxis.convertedCatToNumeric;
    return {
      minX: shouldFloor ? Math.floor(newMinX) : newMinX,
      maxX: shouldFloor ? Math.floor(newMaxX) : newMaxX
    };
  }
  /**
   * @param {number} newMinX
   * @param {number} newMaxX
   */
  zoomUpdateOptions(newMinX, newMaxX) {
    const w2 = this.w;
    if (newMinX === void 0 && newMaxX === void 0) {
      this.handleZoomReset();
      return;
    }
    if (w2.config.xaxis.convertedCatToNumeric) {
      if (newMinX < 1) {
        newMinX = 1;
        newMaxX = w2.globals.dataPoints;
      }
      if (newMaxX - newMinX < 2) {
        return;
      }
    }
    let xaxis = {
      min: newMinX,
      max: newMaxX
    };
    const beforeZoomRange = this.getBeforeZoomRange(
      xaxis,
      /** @type {any} */
      void 0
    );
    if (beforeZoomRange) {
      xaxis = beforeZoomRange.xaxis;
    }
    const options2 = {
      xaxis
    };
    if (!w2.globals.initialConfig) return;
    const yaxis = Utils$1.clone(w2.globals.initialConfig.yaxis);
    if (!w2.config.chart.group) {
      options2.yaxis = yaxis;
    }
    this.w.interact.zoomed = true;
    this.ctx.updateHelpers._updateOptions(
      options2,
      false,
      this.w.config.chart.animations.dynamicAnimation.enabled
    );
    this.zoomCallback(xaxis, yaxis);
  }
  /**
   * @param {Record<string, any>} xaxis
   * @param {Record<string, any>} yaxis
   */
  zoomCallback(xaxis, yaxis) {
    if (typeof this.ev.zoomed === "function") {
      this.ev.zoomed(this.ctx, { xaxis, yaxis });
      this.ctx.events.fireEvent("zoomed", { xaxis, yaxis });
    }
  }
  /**
   * @param {Record<string, any>} xaxis
   * @param {Record<string, any>} yaxis
   */
  getBeforeZoomRange(xaxis, yaxis) {
    let newRange = null;
    if (typeof this.ev.beforeZoom === "function") {
      newRange = this.ev.beforeZoom(this, { xaxis, yaxis });
    }
    return newRange;
  }
  toggleMenu() {
    window.setTimeout(() => {
      var _a, _b, _c;
      if ((_a = this.elMenu) == null ? void 0 : _a.classList.contains("apexcharts-menu-open")) {
        this._closeMenu();
      } else {
        (_b = this.elMenu) == null ? void 0 : _b.classList.add("apexcharts-menu-open");
        (_c = this.elMenuIcon) == null ? void 0 : _c.setAttribute("aria-expanded", "true");
      }
    }, 0);
  }
  _closeMenu() {
    var _a, _b;
    (_a = this.elMenu) == null ? void 0 : _a.classList.remove("apexcharts-menu-open");
    (_b = this.elMenuIcon) == null ? void 0 : _b.setAttribute("aria-expanded", "false");
  }
  /**
   * @param {string} type
   */
  handleDownload(type) {
    const w2 = this.w;
    const exprt = new Exports(this.w, this.ctx);
    switch (type) {
      case "svg":
        exprt.exportToSVG();
        break;
      case "png":
        exprt.exportToPng();
        break;
      case "csv":
        exprt.exportToCSV({
          series: w2.config.series,
          columnDelimiter: w2.config.chart.toolbar.export.csv.columnDelimiter
        });
        break;
    }
  }
  handleZoomReset() {
    const charts = this.ctx.getSyncedCharts();
    charts.forEach((ch) => {
      const w2 = ch.w;
      if (!w2.interact.zoomed) return;
      w2.globals.lastXAxis.min = w2.globals.initialConfig.xaxis.min;
      w2.globals.lastXAxis.max = w2.globals.initialConfig.xaxis.max;
      ch.updateHelpers.revertDefaultAxisMinMax();
      if (typeof w2.config.chart.events.beforeResetZoom === "function") {
        const resetZoomRange = w2.config.chart.events.beforeResetZoom(ch, w2);
        if (resetZoomRange) {
          ch.updateHelpers.revertDefaultAxisMinMax(resetZoomRange);
        }
      }
      if (typeof w2.config.chart.events.zoomed === "function") {
        ch.ctx.toolbar.zoomCallback({
          min: w2.config.xaxis.min,
          max: w2.config.xaxis.max
        });
      }
      const series = ch.ctx.series.emptyCollapsedSeries(
        Utils$1.clone(w2.globals.initialSeries)
      );
      ch.updateHelpers._updateSeries(
        series,
        w2.config.chart.animations.dynamicAnimation.enabled
      );
      w2.interact.zoomed = false;
    });
  }
  destroy() {
    this.elZoom = null;
    this.elZoomIn = null;
    this.elZoomOut = null;
    this.elPan = null;
    this.elSelection = null;
    this.elMeasure = null;
    this.elZoomReset = null;
    this.elMenuIcon = null;
  }
}
const WHEEL_ZOOM_PIXELS_PER_2X = 240;
const INERTIA_MIN_RELEASE_VELOCITY = 0.05;
const INERTIA_DEFAULT_FRICTION = 0.92;
const INERTIA_STOP_VELOCITY = 0.02;
const FRAME_MS_60FPS = 16.6667;
const PAN_NUDGE_DIVISOR = 15;
class ZoomPanSelection extends Toolbar {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    super(w2, ctx);
    this.w = w2;
    this.ctx = ctx;
    this.dragged = false;
    this.graphics = new Graphics(this.w);
    this.eventList = [
      "mousedown",
      "mouseleave",
      "mousemove",
      "touchstart",
      "touchmove",
      "mouseup",
      "touchend"
    ];
    this.clientX = 0;
    this.clientY = 0;
    this.startX = 0;
    this.endX = 0;
    this.dragX = 0;
    this.startY = 0;
    this.endY = 0;
    this.dragY = 0;
    this.moveDirection = "none";
  }
  /** @param {{xyRatios: any}} opts */
  init({ xyRatios }) {
    const w2 = this.w;
    const me = this;
    this.xyRatios = xyRatios;
    this.zoomRect = this.graphics.drawRect(0, 0, 0, 0);
    this.selectionRect = this.graphics.drawRect(0, 0, 0, 0);
    this.constraints = new Box(0, 0, w2.layout.gridWidth, w2.layout.gridHeight);
    this.zoomRect.node.classList.add("apexcharts-zoom-rect");
    this.selectionRect.node.classList.add("apexcharts-selection-rect");
    w2.dom.Paper.add(this.zoomRect);
    w2.dom.Paper.add(this.selectionRect);
    if (w2.config.chart.selection.type === "x") {
      this.slDraggableRect = this.selectionRect.draggable({
        minX: 0,
        minY: 0,
        maxX: w2.layout.gridWidth,
        maxY: w2.layout.gridHeight
      }).on("dragmove.namespace", this.selectionDragging.bind(this, "dragging"));
    } else if (w2.config.chart.selection.type === "y") {
      this.slDraggableRect = this.selectionRect.draggable({
        minX: 0,
        maxX: w2.layout.gridWidth
      }).on("dragmove.namespace", this.selectionDragging.bind(this, "dragging"));
    } else {
      this.slDraggableRect = this.selectionRect.draggable().on("dragmove.namespace", this.selectionDragging.bind(this, "dragging"));
    }
    this.preselectedSelection();
    this.hoverArea = /** @type {Element} */
    w2.dom.baseEl.querySelector(`${w2.globals.chartClass} .apexcharts-svg`);
    if (!this.hoverArea) return;
    this.hoverArea.classList.add("apexcharts-zoomable");
    this.eventList.forEach((event) => {
      var _a;
      (_a = this.hoverArea) == null ? void 0 : _a.addEventListener(
        event,
        me.svgMouseEvents.bind(me, xyRatios),
        {
          capture: false,
          passive: true
        }
      );
    });
    if (this._wheelZoomEnabled()) {
      this.hoverArea.addEventListener("wheel", me.mouseWheelEvent.bind(me), {
        capture: false,
        passive: false
      });
    }
    if (this._momentumEnabled()) {
      ["touchstart", "touchmove", "touchend", "touchcancel"].forEach(
        (event) => {
          var _a;
          (_a = this.hoverArea) == null ? void 0 : _a.addEventListener(event, me.momentumTouch.bind(me), {
            capture: false,
            passive: false
          });
        }
      );
    }
  }
  // remove the event listeners which were previously added on hover area
  destroy() {
    if (this.slDraggableRect) {
      this.slDraggableRect.draggable(false);
      this.slDraggableRect.off();
      this.selectionRect.off();
    }
    this.selectionRect = null;
    this.zoomRect = null;
  }
  /**
   * @param {import('../types/internal').XYRatios} xyRatios
   * @param {any} e
   */
  svgMouseEvents(xyRatios, e2) {
    const w2 = this.w;
    const toolbar = this.ctx.toolbar;
    if (w2.interact.momentum && w2.interact.momentum.busy) return;
    if (this._momentumEnabled() && e2.touches && e2.touches.length > 1) {
      return;
    }
    const zoomtype = w2.interact.zoomEnabled ? w2.config.chart.zoom.type : w2.config.chart.selection.type;
    const autoSelected = w2.config.chart.toolbar.autoSelected;
    if (autoSelected !== "measure") {
      if (e2.shiftKey) {
        w2.interact.shiftWasPressed = true;
        toolbar.enableZoomPanFromToolbar(autoSelected === "pan" ? "zoom" : "pan");
      } else {
        if (w2.interact.shiftWasPressed) {
          toolbar.enableZoomPanFromToolbar(autoSelected);
          w2.interact.shiftWasPressed = false;
        }
      }
    }
    if (!e2.target) return;
    const tc = e2.target.classList;
    let pc;
    if (e2.target.parentNode && e2.target.parentNode !== null) {
      pc = e2.target.parentNode.classList;
    }
    const falsePositives = tc.contains("apexcharts-legend-marker") || tc.contains("apexcharts-legend-text") || pc && pc.contains("apexcharts-toolbar");
    if (falsePositives) return;
    this.clientX = e2.type === "touchmove" || e2.type === "touchstart" ? e2.touches[0].clientX : e2.type === "touchend" ? e2.changedTouches[0].clientX : e2.clientX;
    this.clientY = e2.type === "touchmove" || e2.type === "touchstart" ? e2.touches[0].clientY : e2.type === "touchend" ? e2.changedTouches[0].clientY : e2.clientY;
    if (e2.type === "mousedown" && e2.which === 1 || e2.type === "touchstart") {
      const gridRectDim = this._gridRect();
      if (!gridRectDim) return;
      this.startX = this._screenXToPlotPx(this.clientX);
      this.startY = this.clientY - gridRectDim.top;
      this.dragged = false;
      this.w.interact.mousedown = true;
    }
    if (e2.type === "mousemove" && e2.which === 1 || e2.type === "touchmove") {
      this.dragged = true;
      if (w2.interact.panEnabled) {
        w2.interact.selection = null;
        if (this.w.interact.mousedown) {
          this.panDragging({
            context: this,
            zoomtype,
            xyRatios: this.xyRatios
          });
        }
      } else {
        if (this.w.interact.mousedown && w2.interact.zoomEnabled || this.w.interact.mousedown && w2.interact.selectionEnabled) {
          this.selection = this.selectionDrawing({
            context: this,
            zoomtype
          });
        }
      }
    }
    if (e2.type === "mouseup" || e2.type === "touchend" || e2.type === "mouseleave") {
      this.handleMouseUp({ zoomtype });
    }
    this.makeSelectionRectDraggable();
  }
  /** @param {{ zoomtype?: any, isResized?: any }} opts */
  handleMouseUp({ zoomtype, isResized }) {
    const w2 = this.w;
    const gridRectDim = this._gridRect();
    if (gridRectDim && (this.w.interact.mousedown || isResized)) {
      this.endX = this._screenXToPlotPx(this.clientX);
      this.endY = this.clientY - gridRectDim.top;
      this.dragX = Math.abs(this.endX - this.startX);
      this.dragY = Math.abs(this.endY - this.startY);
      if (w2.interact.zoomEnabled || w2.interact.selectionEnabled) {
        this.selectionDrawn({
          context: this,
          zoomtype
        });
      }
    }
    if (w2.interact.zoomEnabled) {
      this.hideSelectionRect(this.selectionRect);
    }
    this.dragged = false;
    this.w.interact.mousedown = false;
  }
  // ---------------------------------------------------------------------------
  // Wheel zoom: continuous, cursor-anchored zoom on mouse wheel / trackpad.
  //
  // Each wheel event multiplies a pending zoom factor scaled to its deltaY (so
  // a trackpad's stream of tiny deltas and a discrete wheel's ±100 notches both
  // feel proportional), and the accumulated factor is applied at most once per
  // animation frame through the same immediate, animation-free fast path the
  // touch pinch uses (_applyXRange). Deliberately instant, trading-chart style:
  // no per-step morph and no easing between steps (an animated variant was
  // tried and rejected). The original implementation instead ran a fixed
  // 0.5x/1.5x animated update at most once per 400ms and dropped every wheel
  // event in between, which read as lag on continuous scrolling.
  //
  // Like Momentum (see the comment above momentumTouch), applying a frame
  // triggers _updateOptions, which destroys and recreates this instance
  // mid-gesture, so all wheel-gesture state lives on w.interact.wheel rather
  // than on the instance.
  // ---------------------------------------------------------------------------
  /**
   * A wheel or pinch zoom is an incidental gesture: the viewer can land in a
   * zoomed window without meaning to (a page scroll over the chart, a two-finger
   * swipe), so it is only offered when there is a way back out of it. The only
   * built-in way back is the toolbar's reset button, hence 'auto' (the default
   * for both allowMouseWheelZoom and pinch) resolves against that button being
   * present. A page that builds its own reset control sets the option to true
   * and gets the gesture with no toolbar. Drag-to-zoom is deliberate, so it is
   * not gated this way.
   *
   * @param {boolean|'auto'} setting
   */
  _incidentalZoomEnabled(setting) {
    var _a, _b, _c;
    const c2 = this.w.config.chart;
    if (!c2.zoom || !c2.zoom.enabled) return false;
    if (setting !== "auto") return !!setting;
    return !!(((_a = c2.toolbar) == null ? void 0 : _a.show) && ((_c = (_b = c2.toolbar) == null ? void 0 : _b.tools) == null ? void 0 : _c.reset));
  }
  _wheelZoomEnabled() {
    const { zoom } = this.w.config.chart;
    return this._incidentalZoomEnabled(zoom && zoom.allowMouseWheelZoom);
  }
  /** Lazily-created, re-render-surviving wheel-gesture state. */
  _wheel() {
    const it = this.w.interact;
    if (!it.wheel) {
      it.wheel = {
        factor: 1,
        clientX: 0,
        /** @type {number|null} */
        rafId: null,
        /** @type {any} */
        endTimer: null
      };
    }
    return it.wheel;
  }
  /**
   * @param {any} e
   */
  mouseWheelEvent(e2) {
    e2.preventDefault();
    const st = this._wheel();
    let dy = e2.deltaY;
    if (e2.deltaMode === 1) dy *= 33;
    else if (e2.deltaMode === 2) dy *= 330;
    st.factor *= Math.pow(2, dy / WHEEL_ZOOM_PIXELS_PER_2X);
    st.clientX = e2.clientX;
    if (st.rafId == null) {
      st.rafId = requestAnimationFrame(() => this._applyWheelZoom());
    }
    if (st.endTimer) clearTimeout(st.endTimer);
    st.endTimer = setTimeout(() => this._endWheelZoom(), 150);
  }
  /**
   * Apply the zoom factor accumulated since the last animation frame, keeping
   * the data value under the cursor pinned (both zooming in and out).
   */
  _applyWheelZoom() {
    const w2 = this.w;
    const st = this._wheel();
    st.rafId = null;
    const scale = st.factor;
    st.factor = 1;
    if (scale === 1 || w2.globals.isDestroyed) return;
    const gridRectDim = this._gridRect();
    if (!gridRectDim || !gridRectDim.width) return;
    const { min, max } = this._currentXWindow();
    const range = max - min;
    const mouseX = Math.min(
      Math.max((st.clientX - gridRectDim.left) / gridRectDim.width, 0),
      1
    );
    let newRange = range * scale;
    const bounds = this._clampBounds();
    if (bounds) {
      const minXDiff = w2.globals.minXDiff > 0 && isFinite(w2.globals.minXDiff) ? w2.globals.minXDiff : 0;
      const minRange = Math.max(minXDiff * 2, (bounds.max - bounds.min) * 1e-6);
      if (newRange < minRange) newRange = minRange;
      if (newRange > bounds.max - bounds.min) newRange = bounds.max - bounds.min;
    }
    const anchor = min + mouseX * range;
    let newMinX = anchor - mouseX * newRange;
    let newMaxX = newMinX + newRange;
    const eps = range * 1e-9;
    if (Math.abs(newMinX - min) < eps && Math.abs(newMaxX - max) < eps) return;
    if (isNaN(newMinX) || isNaN(newMaxX)) return;
    const beforeZoomRange = this.getBeforeZoomRange(
      { min: newMinX, max: newMaxX },
      /** @type {any} */
      void 0
    );
    if (beforeZoomRange && beforeZoomRange.xaxis) {
      newMinX = beforeZoomRange.xaxis.min;
      newMaxX = beforeZoomRange.xaxis.max;
    }
    this._applyXRange(newMinX, newMaxX, true);
  }
  /** Fire the zoomed callback once the wheel gesture settles (mirrors _endPinch). */
  _endWheelZoom() {
    const w2 = this.w;
    const st = this._wheel();
    st.endTimer = null;
    if (w2.globals.isDestroyed || !w2.interact.zoomed) return;
    const { min, max } = this._currentXWindow();
    const yaxis = w2.globals.initialConfig ? Utils$1.clone(w2.globals.initialConfig.yaxis) : [];
    const toolbar = this.ctx.toolbar;
    if (toolbar) toolbar.zoomCallback({ min, max }, yaxis);
  }
  makeSelectionRectDraggable() {
    const w2 = this.w;
    if (!this.selectionRect) return;
    const rectDim = this.selectionRect.node.getBoundingClientRect();
    if (rectDim.width > 0 && rectDim.height > 0) {
      this.selectionRect.select(false).resize(false);
      this.selectionRect.select({
        createRot: () => {
        },
        updateRot: () => {
        },
        createHandle: (group, p2, index, pointArr, handleName) => {
          if (handleName === "l" || handleName === "r")
            return group.circle(8).css({ "stroke-width": 1, stroke: "#333", fill: "#fff" });
          return group.circle(0);
        },
        updateHandle: (group, p2) => {
          return group.center(p2[0], p2[1]);
        }
      }).resize().on("resize", () => {
        var _a;
        if (w2.interact.selectionEnabled) {
          w2.interact.selection = {
            x: parseFloat(this.selectionRect.node.getAttribute("x")),
            y: parseFloat(this.selectionRect.node.getAttribute("y")),
            width: parseFloat(this.selectionRect.node.getAttribute("width")),
            height: parseFloat(this.selectionRect.node.getAttribute("height"))
          };
          clearTimeout((_a = this.w.globals.selectionResizeTimer) != null ? _a : void 0);
          this.w.globals.selectionResizeTimer = window.setTimeout(() => {
            this._emitSelectionFromRect();
          }, 30);
        } else {
          const zoomtype = w2.interact.zoomEnabled ? w2.config.chart.zoom.type : w2.config.chart.selection.type;
          this.handleMouseUp({ zoomtype, isResized: true });
        }
      });
    }
  }
  preselectedSelection() {
    const w2 = this.w;
    const xyRatios = this.xyRatios;
    if (!w2.interact.zoomEnabled) {
      if (typeof w2.interact.selection !== "undefined" && w2.interact.selection !== null) {
        this.drawSelectionRect(__spreadProps(__spreadValues({}, w2.interact.selection), {
          translateX: w2.layout.translateX,
          translateY: w2.layout.translateY
        }));
      } else {
        if (w2.config.chart.selection.xaxis.min !== void 0 && w2.config.chart.selection.xaxis.max !== void 0) {
          let x = AxisMapping.dataXToPx(w2, w2.config.chart.selection.xaxis.min);
          let width = AxisMapping.dataXToPx(w2, w2.config.chart.selection.xaxis.max) - x;
          if (w2.axisFlags.isRangeBar) {
            x = (w2.config.chart.selection.xaxis.min - w2.globals.yAxisScale[0].niceMin) / xyRatios.invertedYRatio;
            width = (w2.config.chart.selection.xaxis.max - w2.config.chart.selection.xaxis.min) / xyRatios.invertedYRatio;
          }
          const selectionRect = {
            x,
            y: 0,
            width,
            height: w2.layout.gridHeight,
            translateX: w2.layout.translateX,
            translateY: w2.layout.translateY,
            selectionEnabled: true
          };
          this.drawSelectionRect(selectionRect);
          this.makeSelectionRectDraggable();
          if (typeof w2.config.chart.events.selection === "function") {
            w2.config.chart.events.selection(this.ctx, {
              xaxis: {
                min: w2.config.chart.selection.xaxis.min,
                max: w2.config.chart.selection.xaxis.max
              },
              yaxis: {}
            });
          }
        }
      }
    }
  }
  /** @param {{x: any, y: any, width: any, height: any, translateX: any, translateY: any}} opts */
  drawSelectionRect({ x, y, width, height, translateX = 0, translateY = 0 }) {
    const w2 = this.w;
    const zoomRect = this.zoomRect;
    const selectionRect = this.selectionRect;
    if (this.dragged || w2.interact.selection !== null) {
      const scalingAttrs = {
        transform: "translate(" + translateX + ", " + translateY + ")"
      };
      if (w2.interact.zoomEnabled && this.dragged) {
        if (width < 0) width = 1;
        zoomRect.attr({
          x,
          y,
          width,
          height,
          fill: w2.config.chart.zoom.zoomedArea.fill.color,
          "fill-opacity": w2.config.chart.zoom.zoomedArea.fill.opacity,
          stroke: w2.config.chart.zoom.zoomedArea.stroke.color,
          "stroke-width": w2.config.chart.zoom.zoomedArea.stroke.width,
          "stroke-opacity": w2.config.chart.zoom.zoomedArea.stroke.opacity
        });
        Graphics.setAttrs(zoomRect.node, scalingAttrs);
      }
      if (w2.interact.selectionEnabled) {
        selectionRect.attr({
          x,
          y,
          width: width > 0 ? width : 0,
          height: height > 0 ? height : 0,
          fill: w2.config.chart.selection.fill.color,
          "fill-opacity": w2.config.chart.selection.fill.opacity,
          stroke: w2.config.chart.selection.stroke.color,
          "stroke-width": w2.config.chart.selection.stroke.width,
          "stroke-dasharray": w2.config.chart.selection.stroke.dashArray,
          "stroke-opacity": w2.config.chart.selection.stroke.opacity
        });
        Graphics.setAttrs(selectionRect.node, scalingAttrs);
      }
    }
  }
  /**
   * @param {any} rect
   */
  hideSelectionRect(rect) {
    if (rect) {
      rect.attr({
        x: 0,
        y: 0,
        width: 0,
        height: 0
      });
    }
  }
  selectionDrawing({ context, zoomtype }) {
    const w2 = this.w;
    const me = context;
    const gridRectDim = this._gridRect();
    if (!gridRectDim) return;
    const startX = me.startX - 1;
    const startY = me.startY;
    let inversedX = false;
    let inversedY = false;
    const left = this._screenXToPlotPx(me.clientX);
    const top = me.clientY - gridRectDim.top;
    let selectionWidth = left - startX;
    let selectionHeight = top - startY;
    let selectionRect = {
      translateX: w2.layout.translateX,
      translateY: w2.layout.translateY
    };
    if (Math.abs(selectionWidth + startX) > w2.layout.gridWidth) {
      selectionWidth = w2.layout.gridWidth - startX;
    } else if (left < 0) {
      selectionWidth = startX;
    }
    if (startX > left) {
      inversedX = true;
      selectionWidth = Math.abs(selectionWidth);
    }
    if (startY > top) {
      inversedY = true;
      selectionHeight = Math.abs(selectionHeight);
    }
    if (zoomtype === "x") {
      selectionRect = {
        x: inversedX ? startX - selectionWidth : startX,
        y: 0,
        width: selectionWidth,
        height: w2.layout.gridHeight
      };
    } else if (zoomtype === "y") {
      selectionRect = {
        x: 0,
        y: inversedY ? startY - selectionHeight : startY,
        width: w2.layout.gridWidth,
        height: selectionHeight
      };
    } else {
      selectionRect = {
        x: inversedX ? startX - selectionWidth : startX,
        y: inversedY ? startY - selectionHeight : startY,
        width: selectionWidth,
        height: selectionHeight
      };
    }
    selectionRect = __spreadProps(__spreadValues({}, selectionRect), {
      translateX: w2.layout.translateX,
      translateY: w2.layout.translateY
    });
    me.drawSelectionRect(selectionRect);
    me.selectionDragging("resizing");
    return selectionRect;
  }
  /**
   * @param {string} type
   * @param {CustomEvent} e
   */
  selectionDragging(type, e2) {
    var _a;
    const w2 = this.w;
    if (!e2) return;
    e2.preventDefault();
    const { handler, box } = e2.detail;
    const constraints = (
      /** @type {any} */
      this.constraints
    );
    let { x, y } = box;
    if (x < constraints.x) {
      x = constraints.x;
    }
    if (y < constraints.y) {
      y = constraints.y;
    }
    if (box.x2 > constraints.x2) {
      x = constraints.x2 - box.w;
    }
    if (box.y2 > constraints.y2) {
      y = constraints.y2 - box.h;
    }
    handler.move(x, y);
    const selRect = this.selectionRect;
    let timerInterval = 0;
    if (type === "resizing") {
      timerInterval = 30;
    }
    const getSelAttr = (attr) => {
      return parseFloat(selRect.node.getAttribute(attr));
    };
    const draggedProps = {
      x: getSelAttr("x"),
      y: getSelAttr("y"),
      width: getSelAttr("width"),
      height: getSelAttr("height")
    };
    w2.interact.selection = draggedProps;
    const link = w2.config.chart.link;
    const linkActive = !!(link && (link.enabled || typeof link.dimension === "function"));
    if ((typeof w2.config.chart.events.selection === "function" || linkActive) && w2.interact.selectionEnabled) {
      clearTimeout((_a = this.w.globals.selectionResizeTimer) != null ? _a : void 0);
      this.w.globals.selectionResizeTimer = window.setTimeout(() => {
        this._emitSelectionFromRect();
      }, timerInterval);
    }
  }
  /**
   * Recompute the reported x/y range from the CURRENT persistent selection rect
   * (via the shared AxisMapping) and notify listeners: chart.events.selection,
   * brushScrolled, and the crossfilter coordinator. Shared by the rect-body drag
   * (selectionDragging) and the handle resize (makeSelectionRectDraggable) so
   * every gesture re-reports through ONE mapping and the reported range always
   * matches the rect the user sees. No dragged/threshold gate: reaching here
   * already means the user moved or resized the persistent rect.
   */
  _emitSelectionFromRect() {
    var _a;
    const w2 = this.w;
    if (!w2.interact.selectionEnabled) return;
    const link = w2.config.chart.link;
    const linkActive = !!(link && (link.enabled || typeof link.dimension === "function"));
    if (typeof w2.config.chart.events.selection !== "function" && !linkActive) {
      return;
    }
    const gridRectDim = this._gridRect();
    if (!gridRectDim) return;
    const selectionRect = this.selectionRect.node.getBoundingClientRect();
    const xyRatios = this.xyRatios;
    let minX, maxX, minY, maxY;
    const relLeft = this._screenXToPlotPx(selectionRect.left);
    const relRight = this._screenXToPlotPx(selectionRect.right);
    if (!w2.axisFlags.isRangeBar) {
      if (!w2.globals.xAxisScale) return;
      minX = AxisMapping.pxToDataX(w2, relLeft);
      maxX = AxisMapping.pxToDataX(w2, relRight);
      minY = w2.globals.yAxisScale[0].niceMin + (gridRectDim.bottom - selectionRect.bottom) * xyRatios.yRatio[0];
      maxY = w2.globals.yAxisScale[0].niceMax - (selectionRect.top - gridRectDim.top) * xyRatios.yRatio[0];
    } else {
      minX = w2.globals.yAxisScale[0].niceMin + relLeft * xyRatios.invertedYRatio;
      maxX = w2.globals.yAxisScale[0].niceMin + relRight * xyRatios.invertedYRatio;
      minY = 0;
      maxY = 1;
    }
    const xyAxis = {
      xaxis: { min: minX, max: maxX },
      yaxis: { min: minY, max: maxY }
    };
    if (typeof w2.config.chart.events.selection === "function") {
      w2.config.chart.events.selection(this.ctx, xyAxis);
    }
    if (w2.config.chart.brush.enabled && w2.config.chart.events.brushScrolled !== void 0) {
      w2.config.chart.events.brushScrolled(this.ctx, xyAxis);
    }
    (_a = this.ctx.linkedViews) == null ? void 0 : _a.onSourceSelection(xyAxis.xaxis);
  }
  /** @param {{context: any, zoomtype: any}} opts */
  selectionDrawn({ context, zoomtype }) {
    var _a;
    const w2 = this.w;
    const me = context;
    const xyRatios = this.xyRatios;
    const toolbar = this.ctx.toolbar;
    const selRect = w2.interact.zoomEnabled ? me.zoomRect.node.getBoundingClientRect() : me.selectionRect.node.getBoundingClientRect();
    const gridRectDim = me._gridRect();
    if (!gridRectDim) return;
    const localStartX = this._screenXToPlotPx(selRect.left);
    const localEndX = this._screenXToPlotPx(selRect.right);
    const localStartY = selRect.top - gridRectDim.top;
    const localEndY = selRect.bottom - gridRectDim.top;
    let xLowestValue, xHighestValue;
    if (!w2.axisFlags.isRangeBar) {
      xLowestValue = AxisMapping.pxToDataX(w2, localStartX);
      xHighestValue = AxisMapping.pxToDataX(w2, localEndX);
    } else {
      xLowestValue = w2.globals.yAxisScale[0].niceMin + localStartX * xyRatios.invertedYRatio;
      xHighestValue = w2.globals.yAxisScale[0].niceMin + localEndX * xyRatios.invertedYRatio;
    }
    const yHighestValue = [];
    const yLowestValue = [];
    w2.config.yaxis.forEach((yaxe, index) => {
      const seriesIndex = w2.globals.seriesYAxisMap[index][0];
      const highestVal = w2.globals.yAxisScale[index].niceMax - xyRatios.yRatio[seriesIndex] * localStartY;
      const lowestVal = w2.globals.yAxisScale[index].niceMax - xyRatios.yRatio[seriesIndex] * localEndY;
      yHighestValue.push(highestVal);
      yLowestValue.push(lowestVal);
    });
    if (me.dragged && (me.dragX > 10 || me.dragY > 10) && xLowestValue !== xHighestValue) {
      if (w2.interact.zoomEnabled) {
        if (!w2.globals.initialConfig) return;
        let yaxis = Utils$1.clone(w2.globals.initialConfig.yaxis);
        let xaxis = Utils$1.clone(w2.globals.initialConfig.xaxis);
        w2.interact.zoomed = true;
        if (w2.config.xaxis.convertedCatToNumeric) {
          xLowestValue = Math.floor(xLowestValue);
          xHighestValue = Math.floor(xHighestValue);
          if (xLowestValue < 1) {
            xLowestValue = 1;
            xHighestValue = w2.globals.dataPoints;
          }
          if (xHighestValue - xLowestValue < 2) {
            xHighestValue = xLowestValue + 1;
          }
        }
        if (zoomtype === "xy" || zoomtype === "x") {
          xaxis = {
            min: xLowestValue,
            max: xHighestValue
          };
        }
        if (zoomtype === "xy" || zoomtype === "y") {
          yaxis.forEach((yaxe, index) => {
            yaxis[index].min = yLowestValue[index];
            yaxis[index].max = yHighestValue[index];
          });
        }
        if (toolbar) {
          const beforeZoomRange = toolbar.getBeforeZoomRange(xaxis, yaxis);
          if (beforeZoomRange) {
            xaxis = beforeZoomRange.xaxis ? beforeZoomRange.xaxis : xaxis;
            yaxis = beforeZoomRange.yaxis ? beforeZoomRange.yaxis : yaxis;
          }
        }
        const options2 = {
          xaxis
        };
        if (!w2.config.chart.group) {
          options2.yaxis = yaxis;
        }
        me.ctx.updateHelpers._updateOptions(
          options2,
          false,
          me.w.config.chart.animations.dynamicAnimation.enabled
        );
        if (typeof w2.config.chart.events.zoomed === "function") {
          toolbar.zoomCallback(xaxis, yaxis);
        }
      } else if (w2.interact.selectionEnabled) {
        let yaxis = null;
        let xaxis = null;
        xaxis = {
          min: xLowestValue,
          max: xHighestValue
        };
        if (zoomtype === "xy" || zoomtype === "y") {
          const yaxisCopy = (
            /** @type {ApexYAxis[]} */
            Utils$1.clone(w2.config.yaxis)
          );
          yaxis = yaxisCopy;
          yaxisCopy.forEach((yaxe, index) => {
            yaxisCopy[index].min = yLowestValue[index];
            yaxisCopy[index].max = yHighestValue[index];
          });
        }
        w2.interact.selection = me.selection;
        if (typeof w2.config.chart.events.selection === "function") {
          w2.config.chart.events.selection(me.ctx, {
            xaxis,
            yaxis
          });
        }
        (_a = me.ctx.linkedViews) == null ? void 0 : _a.onSourceSelection(xaxis);
      }
    }
  }
  /** @param {{ context?: any, zoomtype?: any, xyRatios?: any }} opts */
  panDragging({ context }) {
    var _a;
    const w2 = this.w;
    const me = context;
    if (typeof w2.interact.lastClientPosition.x !== "undefined") {
      const deltaX = w2.interact.lastClientPosition.x - me.clientX;
      const deltaY = ((_a = w2.interact.lastClientPosition.y) != null ? _a : 0) - me.clientY;
      if (Math.abs(deltaX) > Math.abs(deltaY) && deltaX > 0) {
        this.moveDirection = "left";
      } else if (Math.abs(deltaX) > Math.abs(deltaY) && deltaX < 0) {
        this.moveDirection = "right";
      } else if (Math.abs(deltaY) > Math.abs(deltaX) && deltaY > 0) {
        this.moveDirection = "up";
      } else if (Math.abs(deltaY) > Math.abs(deltaX) && deltaY < 0) {
        this.moveDirection = "down";
      }
    }
    w2.interact.lastClientPosition = {
      x: me.clientX,
      y: me.clientY
    };
    const xLowestValue = w2.axisFlags.isRangeBar ? w2.globals.minY : w2.globals.minX;
    const xHighestValue = w2.axisFlags.isRangeBar ? w2.globals.maxY : w2.globals.maxX;
    me.panScrolled(xLowestValue, xHighestValue);
  }
  // delayedPanScrolled() {
  //   const w = this.w
  //   let newMinX = w.globals.minX
  //   let newMaxX = w.globals.maxX
  //   const centerX = (w.globals.maxX - w.globals.minX) / 2
  //   if (this.moveDirection === 'left') {
  //     newMinX = w.globals.minX + centerX
  //     newMaxX = w.globals.maxX + centerX
  //   } else if (this.moveDirection === 'right') {
  //     newMinX = w.globals.minX - centerX
  //     newMaxX = w.globals.maxX - centerX
  //   }
  //   newMinX = Math.floor(newMinX)
  //   newMaxX = Math.floor(newMaxX)
  //   this.updateScrolledChart(
  //     { xaxis: { min: newMinX, max: newMaxX } },
  //     newMinX,
  //     newMaxX
  //   )
  // }
  /**
   * @param {number} xLowestValue
   * @param {number} xHighestValue
   */
  panScrolled(xLowestValue, xHighestValue) {
    var _a, _b;
    const w2 = this.w;
    const xyRatios = this.xyRatios;
    if (!w2.globals.initialConfig) return;
    const yaxis = Utils$1.clone(w2.globals.initialConfig.yaxis);
    let xRatio = xyRatios.xRatio;
    let minX = w2.globals.minX;
    let maxX = w2.globals.maxX;
    if (w2.axisFlags.isRangeBar) {
      xRatio = xyRatios.invertedYRatio;
      minX = w2.globals.minY;
      maxX = w2.globals.maxY;
    }
    if (this.moveDirection === "left") {
      xLowestValue = minX + w2.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
      xHighestValue = maxX + w2.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
    } else if (this.moveDirection === "right") {
      xLowestValue = minX - w2.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
      xHighestValue = maxX - w2.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
    }
    if (!w2.axisFlags.isRangeBar) {
      const clampMin = (_a = w2.globals.dataReducerRawMinX) != null ? _a : w2.globals.initialMinX;
      const clampMax = (_b = w2.globals.dataReducerRawMaxX) != null ? _b : w2.globals.initialMaxX;
      if (xLowestValue < clampMin || xHighestValue > clampMax) {
        xLowestValue = minX;
        xHighestValue = maxX;
      }
    }
    const xaxis = {
      min: xLowestValue,
      max: xHighestValue
    };
    const options2 = {
      xaxis
    };
    if (!w2.config.chart.group) {
      options2.yaxis = yaxis;
    }
    this.updateScrolledChart(options2, xLowestValue, xHighestValue);
  }
  /**
   * @param {object} options
   * @param {number} xLowestValue
   * @param {number} xHighestValue
   */
  updateScrolledChart(options2, xLowestValue, xHighestValue) {
    const w2 = this.w;
    this.ctx.updateHelpers._updateOptions(options2, false, false);
    if (typeof w2.config.chart.events.scrolled === "function") {
      const args = {
        xaxis: {
          min: xLowestValue,
          max: xHighestValue
        }
      };
      w2.config.chart.events.scrolled(this.ctx, args);
      this.ctx.events.fireEvent("scrolled", args);
    }
  }
  // ---------------------------------------------------------------------------
  // Momentum: multi-touch pinch-zoom, two-finger pan and kinetic inertia.
  //
  // Every _updateOptions destroys and recreates this instance, and applying a
  // gesture frame IS an _updateOptions, so the gesture must not depend on the
  // instance surviving. All runtime state lives on w.interact.momentum (the
  // interaction slice that persists across re-renders, like the crude pan's
  // lastClientPosition). The instance that received touchstart keeps driving
  // the gesture off the persistent state; inertia is a self-contained rAF loop
  // that stops on w.globals.isDestroyed (a real destroy) rather than being
  // cancelled by the per-update destroy().
  // ---------------------------------------------------------------------------
  _momentumEnabled() {
    return this._pinchEnabled() || this._panInertiaEnabled();
  }
  _pinchEnabled() {
    return this._incidentalZoomEnabled(this.w.config.chart.zoom.pinch);
  }
  _panInertiaEnabled() {
    const c2 = this.w.config.chart;
    return !!(c2.pan && c2.pan.inertia);
  }
  /** Lazily-created, re-render-surviving gesture state on the interaction slice. */
  _m() {
    const it = this.w.interact;
    if (!it.momentum) {
      it.momentum = {
        busy: false,
        /** @type {any} */
        pinch: null,
        /** @type {any} */
        panState: null,
        /** @type {{x:number,t:number}[]} */
        samples: [],
        /** @type {number|null} */
        inertiaRAF: null
      };
    }
    return it.momentum;
  }
  /** Current x data-window (rangeBars carry the datetime domain on y). */
  _currentXWindow() {
    const w2 = this.w;
    return w2.axisFlags.isRangeBar ? { min: w2.globals.minY, max: w2.globals.maxY } : { min: w2.globals.minX, max: w2.globals.maxX };
  }
  /** Live grid rect from the current DOM. Never cache the grid node on the
   * instance: a full render replaces this whole instance, but the fast update
   * path (fastUpdate/_fastAxisChromeRefresh) keeps the instance while swapping
   * the grid node, and a cached node would go stale (detached nodes report an
   * all-zero bounding rect, silently corrupting selection geometry). */
  _gridRect() {
    const baseEl = this.w.dom.baseEl;
    const grid = baseEl && baseEl.querySelector(".apexcharts-grid");
    return grid ? grid.getBoundingClientRect() : null;
  }
  /**
   * Convert an absolute (client) x pixel to the plot-origin coordinate space
   * that bar placement and the selection rect transform both use:
   * `screenX - svgLeft - translateX`. This is the ONLY correct reference for the
   * numeric/datetime x mapping (see AxisMapping): do NOT measure from the
   * `.apexcharts-grid` box and subtract barPadForNumericAxis, because on a
   * numeric bar chart that box extends barPad to the LEFT of the plot origin, so
   * the two corrections are a fragile pair that only cancels while the grid box
   * happens to extend exactly barPad. Anchoring on translateX (the same origin
   * the bars use) is stable regardless of grid padding.
   * @param {number} screenX
   * @returns {number}
   */
  _screenXToPlotPx(screenX) {
    return AxisMapping.screenXToPlotPx(this.w, screenX);
  }
  /**
   * Raw data bounds to clamp against. When zoom-aware downsampling is active,
   * the raw stash tracks the full domain; fall back to the initial window.
   * Returns null for rangeBars (no raw-x clamp available).
   * @returns {{min:number, max:number}|null}
   */
  _clampBounds() {
    var _a, _b;
    const w2 = this.w;
    if (w2.axisFlags.isRangeBar) return null;
    return {
      min: (_a = w2.globals.dataReducerRawMinX) != null ? _a : w2.globals.initialMinX,
      max: (_b = w2.globals.dataReducerRawMaxX) != null ? _b : w2.globals.initialMaxX
    };
  }
  /**
   * Apply an x-window immediately (no animation), mirroring panScrolled but
   * pixel-accurate: clamp to the raw bounds (preserving window width so a pan
   * stops flush at the edge rather than shrinking), floor for category axes,
   * then route through the fast _updateOptions path.
   * @param {number} newMinX @param {number} newMaxX @param {boolean} isZoom
   * @returns {{minX:number, maxX:number}|false} applied window, or false if rejected
   */
  _applyXRange(newMinX, newMaxX, isZoom) {
    const w2 = this.w;
    if (!w2.globals.initialConfig) return false;
    const cur = this._currentXWindow();
    const zoomingOut = isZoom && newMaxX - newMinX > cur.max - cur.min;
    const bounds = this._clampBounds();
    if (bounds) {
      const range = newMaxX - newMinX;
      if (newMinX < bounds.min) {
        newMinX = bounds.min;
        newMaxX = newMinX + range;
      }
      if (newMaxX > bounds.max) {
        newMaxX = bounds.max;
        newMinX = newMaxX - range;
      }
      if (newMinX < bounds.min) newMinX = bounds.min;
    }
    if (w2.config.xaxis.convertedCatToNumeric) {
      newMinX = Math.floor(newMinX);
      newMaxX = zoomingOut ? Math.ceil(newMaxX) : Math.floor(newMaxX);
      if (newMinX < 1) newMinX = 1;
      if (bounds && newMaxX > bounds.max) newMaxX = Math.floor(bounds.max);
      if (newMaxX - newMinX < 2) return false;
    }
    if (!(newMaxX > newMinX)) return false;
    const options2 = { xaxis: { min: newMinX, max: newMaxX } };
    if (!w2.config.chart.group) {
      options2.yaxis = Utils$1.clone(w2.globals.initialConfig.yaxis);
    }
    if (isZoom) w2.interact.zoomed = true;
    this.ctx.updateHelpers._updateOptions(options2, false, false);
    return { minX: newMinX, maxX: newMaxX };
  }
  _cancelInertia() {
    const m2 = this._m();
    if (m2.inertiaRAF != null) {
      cancelAnimationFrame(m2.inertiaRAF);
      m2.inertiaRAF = null;
    }
  }
  _fireScrolled() {
    const w2 = this.w;
    if (typeof w2.config.chart.events.scrolled !== "function") return;
    const { min, max } = this._currentXWindow();
    const args = { xaxis: { min, max } };
    w2.config.chart.events.scrolled(this.ctx, args);
    this.ctx.events.fireEvent("scrolled", args);
  }
  /** @param {number} x @param {number} t */
  _pushSample(x, t2) {
    const s2 = this._m().samples;
    s2.push({ x, t: t2 });
    while (s2.length > 6) s2.shift();
  }
  /**
   * Single passive:false handler for all touch phases. Two fingers => pinch /
   * two-finger pan (zoom). One finger, in pan mode => kinetic pan with inertia.
   * @param {any} e
   */
  momentumTouch(e2) {
    const w2 = this.w;
    const m2 = this._m();
    const type = e2.type;
    if (type === "touchstart") {
      this._cancelInertia();
      const gridRectDim = this._gridRect();
      if (!gridRectDim) return;
      if (e2.touches.length >= 2 && this._pinchEnabled()) {
        e2.preventDefault();
        m2.busy = true;
        m2.panState = null;
        this._beginPinch(e2, gridRectDim);
      } else if (e2.touches.length === 1 && this._panInertiaEnabled() && w2.interact.panEnabled) {
        m2.busy = true;
        m2.pinch = null;
        const t2 = e2.touches[0];
        const win = this._currentXWindow();
        const gw = w2.layout.gridWidth || 1;
        m2.panState = {
          startX: t2.clientX,
          startY: t2.clientY,
          axis: null,
          // decided on first move (rails)
          minX0: win.min,
          maxX0: win.max,
          ratio0: (win.max - win.min) / gw
        };
        m2.samples = [{ x: t2.clientX, t: e2.timeStamp }];
      }
      return;
    }
    if (type === "touchmove") {
      if (m2.pinch && e2.touches.length >= 2) {
        e2.preventDefault();
        this._movePinch(e2);
      } else if (m2.panState && e2.touches.length === 1) {
        this._movePan(e2);
      }
      return;
    }
    if (m2.pinch) {
      if (e2.touches.length < 2) this._endPinch();
    } else if (m2.panState) {
      if (e2.touches.length === 0) this._endPan();
    }
    if (e2.touches.length === 0) {
      w2.interact.mousedown = false;
      this.dragged = false;
      if (m2.inertiaRAF == null && !m2.pinch && !m2.panState) {
        m2.busy = false;
      }
    }
  }
  /** @param {any} e @param {DOMRect} gridRectDim */
  _beginPinch(e2, gridRectDim) {
    const w2 = this.w;
    const t0 = e2.touches[0];
    const t1 = e2.touches[1];
    const dist = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY) || 1;
    const cx = (t0.clientX + t1.clientX) / 2 - gridRectDim.left - w2.globals.barPadForNumericAxis;
    const { min, max } = this._currentXWindow();
    this._m().pinch = {
      d0: dist,
      cx0: cx,
      minX0: min,
      maxX0: max,
      gridWidth: w2.layout.gridWidth || 1
    };
  }
  /** @param {any} e */
  _movePinch(e2) {
    const w2 = this.w;
    const p2 = this._m().pinch;
    if (!p2) return;
    const gridRectDim = this._gridRect();
    if (!gridRectDim) return;
    const t0 = e2.touches[0];
    const t1 = e2.touches[1];
    const dist = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY) || 1;
    const cx = (t0.clientX + t1.clientX) / 2 - gridRectDim.left - w2.globals.barPadForNumericAxis;
    const range0 = p2.maxX0 - p2.minX0;
    const newRange = range0 * (p2.d0 / dist);
    const anchorData = p2.minX0 + p2.cx0 / p2.gridWidth * range0;
    let newMinX = anchorData - cx / p2.gridWidth * newRange;
    let newMaxX = newMinX + newRange;
    const bounds = this._clampBounds();
    if (bounds) {
      const minXDiff = w2.globals.minXDiff > 0 && isFinite(w2.globals.minXDiff) ? w2.globals.minXDiff : 0;
      const minRange = Math.max(minXDiff * 2, (bounds.max - bounds.min) * 1e-6);
      if (newMaxX - newMinX < minRange) {
        const mid = (newMinX + newMaxX) / 2;
        newMinX = mid - minRange / 2;
        newMaxX = mid + minRange / 2;
      }
    }
    this._applyXRange(newMinX, newMaxX, true);
  }
  _endPinch() {
    const w2 = this.w;
    const m2 = this._m();
    m2.pinch = null;
    const { min, max } = this._currentXWindow();
    const xaxis = { min, max };
    const yaxis = w2.globals.initialConfig ? Utils$1.clone(w2.globals.initialConfig.yaxis) : [];
    const toolbar = this.ctx.toolbar;
    if (toolbar) toolbar.zoomCallback(xaxis, yaxis);
  }
  /** @param {any} e */
  _movePan(e2) {
    const m2 = this._m();
    const s2 = m2.panState;
    const t2 = e2.touches[0];
    if (!s2.axis) {
      const dx = Math.abs(t2.clientX - s2.startX);
      const dy = Math.abs(t2.clientY - s2.startY);
      if (dx < 6 && dy < 6) {
        this._pushSample(t2.clientX, e2.timeStamp);
        return;
      }
      if (dy > dx) {
        m2.busy = false;
        m2.panState = null;
        return;
      }
      s2.axis = "x";
    }
    if (s2.axis !== "x") return;
    e2.preventDefault();
    const totalDeltaPx = t2.clientX - s2.startX;
    const deltaData = totalDeltaPx * s2.ratio0;
    this._pushSample(t2.clientX, e2.timeStamp);
    this._applyXRange(s2.minX0 - deltaData, s2.maxX0 - deltaData, false);
  }
  _endPan() {
    const m2 = this._m();
    const s2 = m2.panState;
    m2.panState = null;
    let vel = 0;
    const samples = m2.samples;
    if (samples.length >= 2) {
      const a2 = samples[0];
      const b2 = samples[samples.length - 1];
      const dt = b2.t - a2.t;
      if (dt > 0) vel = (b2.x - a2.x) / dt;
    }
    m2.samples = [];
    if (s2 && s2.axis === "x" && this._panInertiaEnabled() && Math.abs(vel) > INERTIA_MIN_RELEASE_VELOCITY) {
      this._startInertia(vel);
    } else {
      m2.busy = false;
      this._fireScrolled();
    }
  }
  /**
   * Kinetic glide after a one-finger pan release: decay the velocity by
   * `friction` each frame and shift the window, stopping at the data edge
   * (clamp, not elastic overshoot). The loop is w-driven, so it keeps running
   * across the re-renders each frame triggers and stops only on a real destroy.
   * @param {number} vel0 px/ms, sign is the finger direction
   */
  _startInertia(vel0) {
    const w2 = this.w;
    const m2 = this._m();
    const cfgFriction = w2.config.chart.pan && w2.config.chart.pan.friction;
    const friction = typeof cfgFriction === "number" ? Math.min(Math.max(cfgFriction, 0.5), 0.999) : INERTIA_DEFAULT_FRICTION;
    let vel = vel0;
    let lastT = null;
    m2.busy = true;
    const step = (ts) => {
      if (w2.globals.isDestroyed) {
        m2.inertiaRAF = null;
        m2.busy = false;
        return;
      }
      if (lastT == null) {
        lastT = ts;
        m2.inertiaRAF = requestAnimationFrame(step);
        return;
      }
      const dt = ts - lastT;
      lastT = ts;
      vel *= Math.pow(friction, dt / FRAME_MS_60FPS);
      if (Math.abs(vel) < INERTIA_STOP_VELOCITY) {
        m2.inertiaRAF = null;
        m2.busy = false;
        this._fireScrolled();
        return;
      }
      const win = this._currentXWindow();
      const gw = w2.layout.gridWidth || 1;
      const ratio = (win.max - win.min) / gw;
      const deltaData = vel * dt * ratio;
      const applied = this._applyXRange(
        win.min - deltaData,
        win.max - deltaData,
        false
      );
      const bounds = this._clampBounds();
      const hitEdge = !applied || bounds && (deltaData > 0 && applied.minX <= bounds.min + (bounds.max - bounds.min) * 1e-6 || deltaData < 0 && applied.maxX >= bounds.max - (bounds.max - bounds.min) * 1e-6);
      if (hitEdge) {
        m2.inertiaRAF = null;
        m2.busy = false;
        this._fireScrolled();
        return;
      }
      m2.inertiaRAF = requestAnimationFrame(step);
    };
    m2.inertiaRAF = requestAnimationFrame(step);
  }
}
ApexCharts.registerFeatures({
  toolbar: Toolbar,
  zoomPanSelection: ZoomPanSelection
});
let Helpers$2 = class Helpers3 {
  /**
   * @param {import('./Annotations').default} annoCtx
   */
  constructor(annoCtx) {
    this.w = annoCtx.w;
    this.annoCtx = annoCtx;
  }
  /**
   * @param {Record<string, any>} anno
   * @param {number | null} [annoIndex]
   */
  setOrientations(anno, annoIndex = null) {
    var _a, _b;
    const w2 = this.w;
    if (anno.label.orientation === "vertical") {
      const i2 = annoIndex !== null ? annoIndex : 0;
      const xAnno = w2.dom.baseEl.querySelector(
        `.apexcharts-xaxis-annotations .apexcharts-xaxis-annotation-label[rel='${i2}']`
      );
      if (xAnno !== null) {
        const xAnnoCoord = (
          /** @type {SVGGraphicsElement} */
          xAnno.getBBox()
        );
        xAnno.setAttribute(
          "x",
          String(
            parseFloat((_a = xAnno.getAttribute("x")) != null ? _a : "0") - xAnnoCoord.height + 4
          )
        );
        const yOffset = anno.label.position === "top" ? xAnnoCoord.width : -xAnnoCoord.width;
        xAnno.setAttribute(
          "y",
          String(parseFloat((_b = xAnno.getAttribute("y")) != null ? _b : "0") + yOffset)
        );
        const { x, y } = this.annoCtx.graphics.rotateAroundCenter(xAnno);
        xAnno.setAttribute("transform", `rotate(-90 ${x} ${y})`);
      }
    }
  }
  /**
   * @param {any} annoEl
   * @param {Record<string, any>} anno
   */
  addBackgroundToAnno(annoEl, anno) {
    const w2 = this.w;
    if (!annoEl || !anno.label.text || !String(anno.label.text).trim()) {
      return null;
    }
    const gridEl = w2.dom.baseEl.querySelector(".apexcharts-grid");
    if (!gridEl) return null;
    const elGridRect = gridEl.getBoundingClientRect();
    const gridBBox = (
      /** @type {SVGGraphicsElement} */
      gridEl.getBBox()
    );
    const zoom = elGridRect.width / gridBBox.width || 1;
    const coords = annoEl.getBoundingClientRect();
    let {
      left: pleft,
      right: pright,
      top: ptop,
      bottom: pbottom
    } = anno.label.style.padding;
    if (anno.label.orientation === "vertical") {
      [ptop, pbottom, pleft, pright] = [pleft, pright, ptop, pbottom];
    }
    const gridLeft = elGridRect.left - gridBBox.x * zoom;
    const gridTop = elGridRect.top - gridBBox.y * zoom;
    const x1 = (coords.left - gridLeft) / zoom - pleft;
    const y1 = (coords.top - gridTop) / zoom - ptop;
    const elRect = this.annoCtx.graphics.drawRect(
      x1,
      y1,
      coords.width / zoom + pleft + pright,
      coords.height / zoom + ptop + pbottom,
      anno.label.borderRadius,
      anno.label.style.background,
      1,
      anno.label.borderWidth,
      anno.label.borderColor,
      0
    );
    if (anno.id) {
      elRect.node.classList.add(anno.id);
    }
    return elRect;
  }
  annotationsBackground() {
    const w2 = this.w;
    const add = (anno, i2, type) => {
      const annoLabel = w2.dom.baseEl.querySelector(
        `.apexcharts-${type}-annotations .apexcharts-${type}-annotation-label[rel='${i2}']`
      );
      if (annoLabel) {
        const parent = annoLabel.parentNode;
        const elRect = this.addBackgroundToAnno(annoLabel, anno);
        if (elRect) {
          parent == null ? void 0 : parent.insertBefore(elRect.node, annoLabel);
          const labelX = annoLabel.getAttribute("x");
          if (labelX !== null) {
            applyProgressiveReveal(elRect, parseFloat(labelX), w2);
          }
          if (anno.label.mouseEnter) {
            elRect.node.addEventListener(
              "mouseenter",
              anno.label.mouseEnter.bind(this, anno)
            );
          }
          if (anno.label.mouseLeave) {
            elRect.node.addEventListener(
              "mouseleave",
              anno.label.mouseLeave.bind(this, anno)
            );
          }
          if (anno.label.click) {
            elRect.node.addEventListener(
              "click",
              anno.label.click.bind(this, anno)
            );
          }
        }
      }
    };
    w2.config.annotations.xaxis.forEach(
      (anno, i2) => add(anno, i2, "xaxis")
    );
    w2.config.annotations.yaxis.forEach(
      (anno, i2) => add(anno, i2, "yaxis")
    );
    w2.config.annotations.points.forEach(
      (anno, i2) => add(anno, i2, "point")
    );
  }
  /**
   * @param {string} type
   * @param {Record<string, any>} anno
   */
  getY1Y2(type, anno) {
    var _a, _b;
    const w2 = this.w;
    const y = type === "y1" ? anno.y : anno.y2;
    let yP;
    let clipped = false;
    if (this.annoCtx.invertAxis) {
      const labels = w2.config.xaxis.convertedCatToNumeric ? w2.labelData.categoryLabels : w2.labelData.labels;
      const catIndex = labels.indexOf(y);
      const xLabel = w2.dom.baseEl.querySelector(
        `.apexcharts-yaxis-texts-g text:nth-child(${catIndex + 1})`
      );
      yP = xLabel ? parseFloat((_a = xLabel.getAttribute("y")) != null ? _a : "0") : (w2.layout.gridHeight / labels.length - 1) * (catIndex + 1) - w2.globals.barHeight;
      if (anno.seriesIndex !== void 0 && w2.globals.barHeight) {
        yP -= w2.globals.barHeight / 2 * (w2.seriesData.series.length - 1) - w2.globals.barHeight * anno.seriesIndex;
      }
    } else {
      const yAxisMap = w2.globals.seriesYAxisMap[anno.yAxisIndex];
      if (!yAxisMap || yAxisMap[0] == null || !w2.config.yaxis[anno.yAxisIndex]) {
        return { yP: 0, clipped: true };
      }
      const seriesIndex = yAxisMap[0];
      const yPos = w2.config.yaxis[anno.yAxisIndex].logarithmic ? new CoreUtils(this.w).getLogVal(
        w2.config.yaxis[anno.yAxisIndex].logBase,
        y,
        seriesIndex
      ) / /** @type {any} */
      w2.globals.yLogRatio[seriesIndex] : (y - w2.globals.minYArr[seriesIndex]) / (w2.globals.yRange[seriesIndex] / w2.layout.gridHeight);
      yP = w2.layout.gridHeight - Math.min(Math.max(yPos, 0), w2.layout.gridHeight);
      clipped = yPos > w2.layout.gridHeight || yPos < 0;
      if (anno.marker && (anno.y === void 0 || anno.y === null)) {
        yP = 0;
      }
      if ((_b = w2.config.yaxis[anno.yAxisIndex]) == null ? void 0 : _b.reversed) {
        yP = yPos;
      }
    }
    if (typeof y === "string" && y.includes("px")) {
      yP = parseFloat(y);
    }
    return { yP, clipped };
  }
  /**
   * @param {string} type
   * @param {Record<string, any>} anno
   */
  getX1X2(type, anno) {
    const w2 = this.w;
    const x = type === "x1" ? anno.x : anno.x2;
    const min = this.annoCtx.invertAxis ? w2.globals.minY : w2.globals.minX;
    const max = this.annoCtx.invertAxis ? w2.globals.maxY : w2.globals.maxX;
    const range = this.annoCtx.invertAxis ? w2.globals.yRange[0] : w2.globals.xRange;
    let clipped = false;
    let xP = this.annoCtx.inversedReversedAxis ? (max - x) / (range / w2.layout.gridWidth) : (x - min) / (range / w2.layout.gridWidth);
    if ((w2.config.xaxis.type === "category" || w2.config.xaxis.convertedCatToNumeric) && !this.annoCtx.invertAxis && !w2.axisFlags.dataFormatXNumeric) {
      if (!w2.config.chart.sparkline.enabled) {
        xP = this.getStringX(x);
      }
    }
    if (typeof x === "string" && x.includes("px")) {
      xP = parseFloat(x);
    }
    if ((x === void 0 || x === null) && anno.marker) {
      xP = w2.layout.gridWidth;
    }
    if (anno.seriesIndex !== void 0 && w2.globals.barWidth && !this.annoCtx.invertAxis) {
      xP -= w2.globals.barWidth / 2 * (w2.seriesData.series.length - 1) - w2.globals.barWidth * anno.seriesIndex;
    }
    if (typeof xP !== "number") {
      xP = 0;
      clipped = true;
    }
    if (parseFloat(xP.toFixed(10)) > parseFloat(w2.layout.gridWidth.toFixed(10))) {
      xP = w2.layout.gridWidth;
      clipped = true;
    } else if (xP < 0) {
      xP = 0;
      clipped = true;
    }
    return { x: xP, clipped };
  }
  /**
   * @param {number} x
   */
  getStringX(x) {
    var _a;
    const w2 = this.w;
    let rX = x;
    if (w2.config.xaxis.convertedCatToNumeric && w2.labelData.categoryLabels.length) {
      const strX = String(x);
      x = w2.labelData.categoryLabels.findIndex(
        (l2) => String(l2) === strX
      ) + 1;
    }
    const catIndex = w2.labelData.labels.map(
      (item) => Array.isArray(item) ? item.join(" ") : item
    ).indexOf(x);
    const xLabel = w2.dom.baseEl.querySelector(
      `.apexcharts-xaxis-texts-g text:nth-child(${catIndex + 1})`
    );
    if (xLabel) {
      rX = parseFloat((_a = xLabel.getAttribute("x")) != null ? _a : "0");
    }
    return rX;
  }
};
class XAnnotations {
  /**
   * @param {import('./Annotations').default} annoCtx
   */
  constructor(annoCtx) {
    this.w = annoCtx.w;
    this.annoCtx = annoCtx;
    this.invertAxis = this.annoCtx.invertAxis;
    this.helpers = new Helpers$2(this.annoCtx);
  }
  /**
   * @param {XAxisAnnotations} anno
   * @param {Element} parent
   * @param {number} index
   */
  addXaxisAnnotation(anno, parent, index) {
    const w2 = this.w;
    const result = this.helpers.getX1X2("x1", anno);
    let x1 = result.x;
    const clipX1 = result.clipped;
    let clipX2 = true;
    let x2;
    const text = anno.label.text;
    const strokeDashArray = anno.strokeDashArray;
    if (!Utils$1.isNumber(x1)) return;
    if (anno.x2 === null || typeof anno.x2 === "undefined") {
      if (!clipX1) {
        const line = this.annoCtx.graphics.drawLine(
          x1 + anno.offsetX,
          // x1
          0 + anno.offsetY,
          // y1
          x1 + anno.offsetX,
          // x2
          w2.layout.gridHeight + anno.offsetY,
          // y2
          anno.borderColor,
          // lineColor
          strokeDashArray,
          //dashArray
          anno.borderWidth
        );
        parent.appendChild(line.node);
        if (anno.id) {
          line.node.classList.add(anno.id);
        }
        applyProgressiveReveal(line, x1 + anno.offsetX, w2);
      }
    } else {
      const result2 = this.helpers.getX1X2("x2", anno);
      x2 = result2.x;
      clipX2 = result2.clipped;
      if (x2 < x1) {
        const temp = x1;
        x1 = x2;
        x2 = temp;
      }
      const rect = this.annoCtx.graphics.drawRect(
        x1 + anno.offsetX,
        // x1
        0 + anno.offsetY,
        // y1
        x2 - x1,
        // x2
        w2.layout.gridHeight + anno.offsetY,
        // y2
        0,
        // radius
        anno.fillColor,
        // color
        anno.opacity,
        // opacity,
        1,
        // strokeWidth
        anno.borderColor,
        // strokeColor
        strokeDashArray
        // stokeDashArray
      );
      rect.node.classList.add("apexcharts-annotation-rect");
      rect.attr("clip-path", `url(#gridRectMask${w2.globals.cuid})`);
      parent.appendChild(rect.node);
      if (anno.id) {
        rect.node.classList.add(anno.id);
      }
      applyProgressiveReveal(rect, x1 + anno.offsetX, w2);
    }
    if (!(clipX1 && clipX2)) {
      const textRects = this.annoCtx.graphics.getTextRects(
        text,
        anno.label.style.fontSize
      );
      const textY = anno.label.position === "top" ? 4 : anno.label.position === "center" ? w2.layout.gridHeight / 2 + (anno.label.orientation === "vertical" ? textRects.width / 2 : 0) : w2.layout.gridHeight;
      const elText = this.annoCtx.graphics.drawText({
        x: x1 + anno.label.offsetX,
        y: textY + anno.label.offsetY - (anno.label.orientation === "vertical" ? anno.label.position === "top" ? textRects.width / 2 - 12 : -textRects.width / 2 : 0),
        text,
        textAnchor: anno.label.textAnchor,
        fontSize: anno.label.style.fontSize,
        fontFamily: anno.label.style.fontFamily,
        fontWeight: anno.label.style.fontWeight,
        foreColor: anno.label.style.color,
        cssClass: `apexcharts-xaxis-annotation-label ${anno.label.style.cssClass} ${anno.id ? anno.id : ""}`
      });
      elText.attr({
        rel: index
      });
      parent.appendChild(elText.node);
      applyProgressiveReveal(elText, x1 + anno.label.offsetX, w2);
      this.annoCtx.helpers.setOrientations(anno, index);
    }
  }
  drawXAxisAnnotations() {
    const w2 = this.w;
    const elg = this.annoCtx.graphics.group({
      class: "apexcharts-xaxis-annotations"
    });
    w2.config.annotations.xaxis.map(
      (anno, index) => {
        this.addXaxisAnnotation(anno, elg.node, index);
      }
    );
    return elg;
  }
}
class YAnnotations {
  /**
   * @param {import('./Annotations').default} annoCtx
   */
  constructor(annoCtx) {
    this.w = annoCtx.w;
    this.annoCtx = annoCtx;
    this.helpers = new Helpers$2(this.annoCtx);
    this.axesUtils = new AxesUtils(this.annoCtx.w, {
      theme: this.annoCtx.theme,
      timeScale: this.annoCtx.timeScale
    });
  }
  /**
   * @param {YAxisAnnotations} anno
   * @param {Element} parent
   * @param {number} index
   */
  addYaxisAnnotation(anno, parent, index) {
    const w2 = this.w;
    const strokeDashArray = anno.strokeDashArray;
    let result = this.helpers.getY1Y2("y1", anno);
    let y1 = result.yP;
    const clipY1 = result.clipped;
    let y2;
    let clipY2 = true;
    let drawn = false;
    const text = anno.label.text;
    if (anno.y2 === null || typeof anno.y2 === "undefined") {
      if (!clipY1) {
        drawn = true;
        const line = this.annoCtx.graphics.drawLine(
          0 + anno.offsetX,
          // x1
          y1 + anno.offsetY,
          // y1
          this._getYAxisAnnotationWidth(anno),
          // x2
          y1 + anno.offsetY,
          // y2
          anno.borderColor,
          // lineColor
          strokeDashArray,
          // dashArray
          anno.borderWidth
        );
        parent.appendChild(line.node);
        if (anno.id) {
          line.node.classList.add(anno.id);
        }
      }
    } else {
      result = this.helpers.getY1Y2("y2", anno);
      y2 = result.yP;
      clipY2 = result.clipped;
      if (y2 > y1) {
        const temp = y1;
        y1 = y2;
        y2 = temp;
      }
      if (!(clipY1 && clipY2)) {
        drawn = true;
        const rect = this.annoCtx.graphics.drawRect(
          0 + anno.offsetX,
          // x1
          y2 + anno.offsetY,
          // y1
          this._getYAxisAnnotationWidth(anno),
          // x2
          y1 - y2,
          // y2
          0,
          // radius
          anno.fillColor,
          // color
          anno.opacity,
          // opacity,
          1,
          // strokeWidth
          anno.borderColor,
          // strokeColor
          strokeDashArray
          // stokeDashArray
        );
        rect.node.classList.add("apexcharts-annotation-rect");
        rect.attr("clip-path", `url(#gridRectMask${w2.globals.cuid})`);
        parent.appendChild(rect.node);
        if (anno.id) {
          rect.node.classList.add(anno.id);
        }
      }
    }
    if (drawn) {
      const textX = anno.label.position === "right" ? w2.layout.gridWidth : anno.label.position === "center" ? w2.layout.gridWidth / 2 : 0;
      const elText = this.annoCtx.graphics.drawText({
        x: textX + anno.label.offsetX,
        y: (y2 != null ? y2 : y1) + anno.label.offsetY - 3,
        text,
        textAnchor: anno.label.textAnchor,
        fontSize: anno.label.style.fontSize,
        fontFamily: anno.label.style.fontFamily,
        fontWeight: anno.label.style.fontWeight,
        foreColor: anno.label.style.color,
        cssClass: `apexcharts-yaxis-annotation-label ${anno.label.style.cssClass} ${anno.id ? anno.id : ""}`
      });
      elText.attr({
        rel: index
      });
      parent.appendChild(elText.node);
    }
  }
  /**
   * @param {YAxisAnnotations} anno
   */
  _getYAxisAnnotationWidth(anno) {
    const w2 = this.w;
    let width = w2.layout.gridWidth;
    if (anno.width.indexOf("%") > -1) {
      width = w2.layout.gridWidth * parseInt(anno.width, 10) / 100;
    } else {
      width = parseInt(anno.width, 10);
    }
    return width + anno.offsetX;
  }
  drawYAxisAnnotations() {
    const w2 = this.w;
    const elg = this.annoCtx.graphics.group({
      class: "apexcharts-yaxis-annotations"
    });
    w2.config.annotations.yaxis.forEach(
      (anno, index) => {
        anno.yAxisIndex = this.axesUtils.translateYAxisIndex(anno.yAxisIndex);
        if (!(this.axesUtils.isYAxisHidden(anno.yAxisIndex) && this.axesUtils.yAxisAllSeriesCollapsed(anno.yAxisIndex))) {
          this.addYaxisAnnotation(anno, elg.node, index);
        }
      }
    );
    return elg;
  }
}
class PointAnnotations {
  /**
   * @param {import('./Annotations').default} annoCtx
   */
  constructor(annoCtx) {
    this.w = annoCtx.w;
    this.annoCtx = annoCtx;
    this.helpers = new Helpers$2(this.annoCtx);
  }
  /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */
  addPointAnnotation(anno, parent, index) {
    const w2 = this.w;
    if (w2.globals.collapsedSeriesIndices.indexOf(anno.seriesIndex) > -1) {
      return;
    }
    const resultX = this.helpers.getX1X2("x1", anno);
    const x = resultX.x;
    const clipX = resultX.clipped;
    const resultY = this.helpers.getY1Y2("y1", anno);
    const y = resultY.yP;
    const clipY = resultY.clipped;
    if (!Utils$1.isNumber(x)) return;
    if (!(clipY || clipX)) {
      const optsPoints = {
        pSize: anno.marker.size,
        pointStrokeWidth: anno.marker.strokeWidth,
        pointFillColor: anno.marker.fillColor,
        pointStrokeColor: anno.marker.strokeColor,
        shape: anno.marker.shape,
        pRadius: anno.marker.radius,
        class: `apexcharts-point-annotation-marker ${anno.marker.cssClass} ${anno.id ? anno.id : ""}`
      };
      let point = this.annoCtx.graphics.drawMarker(
        x + anno.marker.offsetX,
        y + anno.marker.offsetY,
        optsPoints
      );
      parent.appendChild(point.node);
      const tooltipTargets = [point.node];
      applyProgressiveReveal(point, x, w2);
      const text = anno.label.text ? anno.label.text : "";
      const elText = this.annoCtx.graphics.drawText({
        x: x + anno.label.offsetX,
        y: y + anno.label.offsetY - anno.marker.size - parseFloat(anno.label.style.fontSize) / 1.6,
        text,
        textAnchor: anno.label.textAnchor,
        fontSize: anno.label.style.fontSize,
        fontFamily: anno.label.style.fontFamily,
        fontWeight: anno.label.style.fontWeight,
        foreColor: anno.label.style.color,
        cssClass: `apexcharts-point-annotation-label ${anno.label.style.cssClass} ${anno.id ? anno.id : ""}`
      });
      elText.attr({
        rel: index
      });
      parent.appendChild(elText.node);
      applyProgressiveReveal(elText, x, w2);
      if (anno.customSVG.SVG) {
        const g2 = this.annoCtx.graphics.group({
          class: "apexcharts-point-annotations-custom-svg " + anno.customSVG.cssClass
        });
        g2.attr({
          transform: `translate(${x + anno.customSVG.offsetX}, ${y + anno.customSVG.offsetY})`
        });
        g2.node.innerHTML = anno.customSVG.SVG;
        parent.appendChild(g2.node);
        tooltipTargets.push(g2.node);
      }
      if (anno.image.path) {
        const imgWidth = anno.image.width ? anno.image.width : 20;
        const imgHeight = anno.image.height ? anno.image.height : 20;
        point = this.annoCtx.addImage({
          x: x + anno.image.offsetX - imgWidth / 2,
          y: y + anno.image.offsetY - imgHeight / 2,
          width: imgWidth,
          height: imgHeight,
          path: anno.image.path,
          appendTo: ".apexcharts-point-annotations"
        });
        tooltipTargets.push(point.node);
      }
      if (anno.tooltip && anno.tooltip.enabled) {
        tooltipTargets.forEach((node) => {
          node.addEventListener("mouseenter", () => {
            this.showPointTooltip(anno, node);
          });
          node.addEventListener("mouseleave", () => {
            this.hidePointTooltip();
          });
        });
      }
      if (anno.mouseEnter) {
        point.node.addEventListener(
          "mouseenter",
          anno.mouseEnter.bind(this, anno)
        );
      }
      if (anno.mouseLeave) {
        point.node.addEventListener(
          "mouseleave",
          anno.mouseLeave.bind(this, anno)
        );
      }
      if (anno.click) {
        point.node.addEventListener("click", anno.click.bind(this, anno));
      }
    }
  }
  /**
   * Lazily create (once per chart) and return the shared HTML element used to
   * render point-annotation tooltips. Reuses the `.apexcharts-tooltip` glass
   * styling; the `.apexcharts-annotation-tooltip` modifier adds padding and
   * text wrapping for free-form content.
   * @returns {HTMLElement}
   */
  getPointTooltipEl() {
    const w2 = this.w;
    let el = (
      /** @type {HTMLElement | null} */
      w2.dom.elWrap.querySelector(".apexcharts-annotation-tooltip")
    );
    if (!el) {
      el = /** @type {HTMLElement} */
      BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
      el.classList.add("apexcharts-tooltip", "apexcharts-annotation-tooltip");
      w2.dom.elWrap.appendChild(el);
    }
    return el;
  }
  /**
   * Resolve the tooltip markup for a point annotation. Precedence:
   * `tooltip.formatter` (fn) -> `tooltip.text` -> `label.text`. Arrays are
   * joined with line breaks.
   * @param {Record<string, any>} anno
   * @returns {string}
   */
  getPointTooltipContent(anno) {
    const w2 = this.w;
    const tt = anno.tooltip || {};
    if (typeof tt.formatter === "function") {
      return tt.formatter({
        annotation: anno,
        seriesIndex: anno.seriesIndex,
        id: anno.id,
        w: w2
      });
    }
    let content = tt.text != null ? tt.text : anno.label && anno.label.text;
    if (Array.isArray(content)) {
      content = content.join("<br/>");
    }
    return content == null ? "" : String(content);
  }
  /**
   * @param {Record<string, any>} anno
   * @param {Element} targetNode the hovered marker / image / custom-SVG node
   */
  showPointTooltip(anno, targetNode) {
    const w2 = this.w;
    const content = this.getPointTooltipContent(anno);
    if (!content) return;
    const el = this.getPointTooltipEl();
    el.innerHTML = content;
    const theme = anno.tooltip.theme || w2.config.tooltip.theme || "light";
    el.classList.remove("apexcharts-theme-light", "apexcharts-theme-dark");
    el.classList.add(`apexcharts-theme-${theme}`);
    el.classList.add("apexcharts-active");
    const wrapRect = w2.dom.elWrap.getBoundingClientRect();
    const markRect = targetNode.getBoundingClientRect();
    const ttRect = el.getBoundingClientRect();
    const offsetX = anno.tooltip.offsetX || 0;
    const offsetY = anno.tooltip.offsetY || 0;
    let left = markRect.left - wrapRect.left + markRect.width / 2 - ttRect.width / 2;
    let top = markRect.top - wrapRect.top - ttRect.height - 10;
    left = Math.max(0, Math.min(left, wrapRect.width - ttRect.width));
    if (top < 0) {
      top = markRect.top - wrapRect.top + markRect.height + 10;
    }
    el.style.left = left + offsetX + "px";
    el.style.top = top + offsetY + "px";
  }
  hidePointTooltip() {
    const el = (
      /** @type {HTMLElement | null} */
      this.w.dom.elWrap.querySelector(".apexcharts-annotation-tooltip")
    );
    if (el) {
      el.classList.remove("apexcharts-active");
    }
  }
  drawPointAnnotations() {
    const w2 = this.w;
    const elg = this.annoCtx.graphics.group({
      class: "apexcharts-point-annotations"
    });
    w2.config.annotations.points.map(
      (anno, index) => {
        this.addPointAnnotation(anno, elg.node, index);
      }
    );
    return elg;
  }
}
class Annotations {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   */
  constructor(w2, { theme = null, timeScale = null } = {}) {
    this.w = w2;
    this.theme = theme;
    this.timeScale = timeScale;
    this.invertAxis = void 0;
    this.inversedReversedAxis = void 0;
    this.graphics = new Graphics(this.w);
    if (this.w.globals.isBarHorizontal) {
      this.invertAxis = true;
    }
    this.helpers = new Helpers$2(this);
    this.xAxisAnnotations = new XAnnotations(this);
    this.yAxisAnnotations = new YAnnotations(this);
    this.pointsAnnotations = new PointAnnotations(this);
    if (this.w.globals.isBarHorizontal && this.w.config.yaxis[0].reversed) {
      this.inversedReversedAxis = true;
    }
    this.xDivision = this.w.layout.gridWidth / this.w.globals.dataPoints;
  }
  drawAxesAnnotations() {
    const w2 = this.w;
    if (w2.globals.axisCharts && w2.globals.dataPoints) {
      const yAnnotations = this.yAxisAnnotations.drawYAxisAnnotations();
      const xAnnotations = this.xAxisAnnotations.drawXAxisAnnotations();
      const pointAnnotations = this.pointsAnnotations.drawPointAnnotations();
      const initialAnim = w2.config.chart.animations.enabled;
      const annoArray = [yAnnotations, xAnnotations, pointAnnotations];
      const annoElArray = [
        xAnnotations.node,
        yAnnotations.node,
        pointAnnotations.node
      ];
      const progressiveAnnos = w2.config.chart.type === "line" || w2.config.chart.type === "area" || w2.config.chart.type === "rangeArea";
      const skipGroupHide = [progressiveAnnos, false, progressiveAnnos];
      for (let i2 = 0; i2 < 3; i2++) {
        w2.dom.elGraphical.add(annoArray[i2]);
        if (initialAnim && !w2.globals.resized && !w2.globals.dataChanged) {
          if (w2.config.chart.type !== "scatter" && w2.config.chart.type !== "bubble" && w2.globals.dataPoints > 1 && !skipGroupHide[i2]) {
            annoElArray[i2].classList.add("apexcharts-element-hidden");
          }
        }
        w2.globals.delayedElements.push({ el: annoElArray[i2], index: 0 });
      }
      this.helpers.annotationsBackground();
    }
  }
  drawImageAnnos() {
    const w2 = this.w;
    w2.config.annotations.images.map((s2) => {
      this.addImage(s2);
    });
  }
  drawTextAnnos() {
    const w2 = this.w;
    w2.config.annotations.texts.map((t2) => {
      this.addText(t2);
    });
  }
  /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */
  addXaxisAnnotation(anno, parent, index) {
    this.xAxisAnnotations.addXaxisAnnotation(anno, parent, index);
  }
  /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */
  addYaxisAnnotation(anno, parent, index) {
    this.yAxisAnnotations.addYaxisAnnotation(anno, parent, index);
  }
  /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */
  addPointAnnotation(anno, parent, index) {
    this.pointsAnnotations.addPointAnnotation(anno, parent, index);
  }
  /**
   * @param {Record<string, any>} params
   */
  addText(params) {
    const {
      x,
      y,
      text,
      textAnchor,
      foreColor,
      fontSize,
      fontFamily,
      fontWeight,
      cssClass,
      backgroundColor,
      borderWidth,
      strokeDashArray,
      borderRadius,
      borderColor,
      appendTo = ".apexcharts-svg",
      paddingLeft = 4,
      paddingRight = 4,
      paddingBottom = 2,
      paddingTop = 2
    } = params;
    const w2 = this.w;
    const elText = this.graphics.drawText({
      x,
      y,
      text,
      textAnchor: textAnchor || "start",
      fontSize: fontSize || "12px",
      fontWeight: fontWeight || "regular",
      fontFamily: fontFamily || w2.config.chart.fontFamily,
      foreColor: foreColor || w2.config.chart.foreColor,
      cssClass: "apexcharts-text " + cssClass ? cssClass : ""
    });
    const parent = w2.dom.baseEl.querySelector(appendTo);
    if (parent) {
      parent.appendChild(elText.node);
    }
    const textRect = elText.bbox();
    if (text) {
      const elRect = this.graphics.drawRect(
        textRect.x - paddingLeft,
        textRect.y - paddingTop,
        textRect.width + paddingLeft + paddingRight,
        textRect.height + paddingBottom + paddingTop,
        borderRadius,
        backgroundColor ? backgroundColor : "transparent",
        1,
        borderWidth,
        borderColor,
        strokeDashArray
      );
      parent.insertBefore(elRect.node, elText.node);
    }
  }
  /**
   * @param {Record<string, any>} params
   */
  addImage(params) {
    const w2 = this.w;
    const {
      path,
      x = 0,
      y = 0,
      width = 20,
      height = 20,
      appendTo = ".apexcharts-svg"
    } = params;
    const img = w2.dom.Paper.image(path);
    img.size(width, height).move(x, y);
    const parent = w2.dom.baseEl.querySelector(appendTo);
    if (parent) {
      parent.appendChild(img.node);
    }
    return img;
  }
  // The addXaxisAnnotation method requires a parent class, and user calling this method externally on the chart instance may not specify parent, hence a different method
  /**
   * @param {Record<string, any>} params
   * @param {boolean} pushToMemory
   * @param {any} context
   */
  addXaxisAnnotationExternal(params, pushToMemory, context) {
    this.addAnnotationExternal({
      params,
      pushToMemory,
      context,
      type: "xaxis",
      contextMethod: context.addXaxisAnnotation
    });
    return context;
  }
  /**
   * @param {Record<string, any>} params
   * @param {boolean} pushToMemory
   * @param {any} context
   */
  addYaxisAnnotationExternal(params, pushToMemory, context) {
    this.addAnnotationExternal({
      params,
      pushToMemory,
      context,
      type: "yaxis",
      contextMethod: context.addYaxisAnnotation
    });
    return context;
  }
  /**
   * @param {Record<string, any>} params
   * @param {boolean} pushToMemory
   * @param {any} context
   */
  addPointAnnotationExternal(params, pushToMemory, context) {
    if (typeof this.invertAxis === "undefined") {
      this.invertAxis = context.w.globals.isBarHorizontal;
    }
    this.addAnnotationExternal({
      params,
      pushToMemory,
      context,
      type: "point",
      contextMethod: context.addPointAnnotation
    });
    return context;
  }
  /** @param {{params: any, pushToMemory: any, context: any, type: any, contextMethod: any}} opts */
  addAnnotationExternal({
    params,
    pushToMemory,
    context,
    type,
    contextMethod
  }) {
    const me = context;
    const w2 = me.w;
    const parent = w2.dom.baseEl.querySelector(`.apexcharts-${type}-annotations`);
    const index = parent.childNodes.length + 1;
    const options2 = new Options();
    const axesAnno = Object.assign(
      {},
      type === "xaxis" ? options2.xAxisAnnotation : type === "yaxis" ? options2.yAxisAnnotation : options2.pointAnnotation
    );
    const anno = Utils$1.extend(axesAnno, params);
    switch (type) {
      case "xaxis":
        this.addXaxisAnnotation(anno, parent, index);
        break;
      case "yaxis":
        this.addYaxisAnnotation(anno, parent, index);
        break;
      case "point":
        this.addPointAnnotation(anno, parent, index);
        break;
    }
    const axesAnnoLabel = w2.dom.baseEl.querySelector(
      `.apexcharts-${type}-annotations .apexcharts-${type}-annotation-label[rel='${index}']`
    );
    const elRect = this.helpers.addBackgroundToAnno(axesAnnoLabel, anno);
    if (elRect) {
      parent.insertBefore(elRect.node, axesAnnoLabel);
    }
    if (pushToMemory) {
      w2.globals.memory.methodsToExec.push({
        context: me,
        id: anno.id ? anno.id : Utils$1.randomId(),
        method: contextMethod,
        label: "addAnnotation",
        params
      });
    }
    return context;
  }
  /**
   * Remove the shared point-annotation hover tooltip node.
   *
   * `hidePointTooltip` is wired only to the marker's `mouseleave`; if the marker
   * is torn down while hovered (clearAnnotations / removeAnnotation / a redraw),
   * that never fires and the tooltip is left `.apexcharts-active`. A stale active
   * annotation tooltip then permanently suppresses the series tooltip (see the
   * guard in Tooltip.drawTooltip added by b1369f5ab). Removing the node clears
   * both the ghost box and the stale state; it is recreated on the next hover.
   * @param {any} w
   */
  _removeAnnotationTooltip(w2) {
    const el = w2.dom.elWrap && w2.dom.elWrap.querySelector(".apexcharts-annotation-tooltip");
    if (el && el.parentNode) {
      el.parentNode.removeChild(el);
    }
  }
  /**
   * @param {import('../../types/internal').ChartContext} ctx
   */
  clearAnnotations(ctx) {
    const w2 = ctx.w;
    this._removeAnnotationTooltip(w2);
    const annos = w2.dom.baseEl.querySelectorAll(
      ".apexcharts-yaxis-annotations, .apexcharts-xaxis-annotations, .apexcharts-point-annotations"
    );
    for (let i2 = w2.globals.memory.methodsToExec.length - 1; i2 >= 0; i2--) {
      if (w2.globals.memory.methodsToExec[i2].label === "addText" || w2.globals.memory.methodsToExec[i2].label === "addAnnotation") {
        w2.globals.memory.methodsToExec.splice(i2, 1);
      }
    }
    Array.prototype.forEach.call(annos, (a2) => {
      while (a2.firstChild) {
        a2.removeChild(a2.firstChild);
      }
    });
  }
  /**
   * @param {import('../../types/internal').ChartContext} ctx
   * @param {string} id
   */
  removeAnnotation(ctx, id) {
    const w2 = ctx.w;
    this._removeAnnotationTooltip(w2);
    const annos = w2.dom.baseEl.querySelectorAll(`.${id}`);
    if (annos) {
      w2.globals.memory.methodsToExec.map((m2, i2) => {
        if (m2.id === id) {
          w2.globals.memory.methodsToExec.splice(i2, 1);
        }
      });
      Object.keys(w2.config.annotations).forEach((key) => {
        const annotationArray = w2.config.annotations[key];
        if (Array.isArray(annotationArray)) {
          w2.config.annotations[key] = annotationArray.filter((m2) => m2.id !== id);
        }
      });
      Array.prototype.forEach.call(annos, (a2) => {
        a2.parentElement.removeChild(a2);
      });
    }
  }
}
ApexCharts.registerFeatures({ annotations: Annotations });
class KeyboardNavigation {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.seriesIndex = 0;
    this.dataPointIndex = 0;
    this.active = false;
    this._tooltipDismissed = false;
    this._focusedEl = null;
    this._hoveredBarEl = null;
    this._enlargedScatterMarker = null;
    this._onKeyDown = this._onKeyDown.bind(this);
    this._onFocus = this._onFocus.bind(this);
    this._onBlur = this._onBlur.bind(this);
    this._onLegendClick = this._onLegendClick.bind(this);
    this._onPointerDown = this._onPointerDown.bind(this);
    this._lastPointerDownAt = 0;
  }
  // ─── Public API ───────────────────────────────────────────────────────────
  /**
   * Called after the chart and tooltip have been fully rendered.
   * Attaches event listeners and makes the SVG keyboard-focusable.
   */
  init() {
    const w2 = this.w;
    const svgEl = w2.dom.Paper.node;
    if (!svgEl) return;
    svgEl.setAttribute("tabindex", "0");
    svgEl.addEventListener("focus", this._onFocus);
    svgEl.addEventListener("blur", this._onBlur);
    svgEl.addEventListener("mousedown", this._onPointerDown, { capture: true });
    svgEl.addEventListener("pointerdown", this._onPointerDown, {
      capture: true
    });
    svgEl.addEventListener("touchstart", this._onPointerDown, {
      capture: true,
      passive: true
    });
    svgEl.addEventListener("keydown", this._onKeyDown, { passive: false });
    this.ctx.events.addEventListener("legendClick", this._onLegendClick);
  }
  /**
   * Removes all event listeners. Called from chart.destroy().
   */
  destroy() {
    const w2 = this.w;
    const svgEl = w2.dom.Paper && w2.dom.Paper.node;
    this.ctx.events.removeEventListener("legendClick", this._onLegendClick);
    if (!svgEl) return;
    svgEl.removeEventListener("focus", this._onFocus);
    svgEl.removeEventListener("blur", this._onBlur);
    svgEl.removeEventListener("keydown", this._onKeyDown);
    svgEl.removeEventListener(
      "mousedown",
      this._onPointerDown,
      /** @type {any} */
      { capture: true }
    );
    svgEl.removeEventListener(
      "pointerdown",
      this._onPointerDown,
      /** @type {any} */
      { capture: true }
    );
    svgEl.removeEventListener(
      "touchstart",
      this._onPointerDown,
      /** @type {any} */
      { capture: true }
    );
  }
  // Records the timestamp of the most recent pointer-down inside the SVG.
  // `_onFocus` reads this to distinguish keyboard-driven focus (no recent
  // pointer activity) from mouse-driven focus (pointer event within the
  // last 100 ms). Stays a no-op for keyboard users.
  _onPointerDown() {
    this._lastPointerDownAt = Date.now();
  }
  /**
   * Called from Events.js keydown handler. Navigation keys are already handled
   * by the direct SVG listener (which can call preventDefault). This entry
   * point is intentionally a no-op — Events.js still fires the public keyDown
   * callback and fireEvent('keydown') independently.
   * @param {Event} _e
   */
  handleKey(_e) {
  }
  // ─── Focus / blur ─────────────────────────────────────────────────────────
  _onFocus() {
    if (!this._isNavEnabled()) return;
    if (Date.now() - this._lastPointerDownAt < 100) {
      return;
    }
    this.active = true;
    this._clampCursor();
    this._snapToVisibleRange();
    this._showCurrentPoint();
  }
  _onBlur() {
    this.active = false;
    this._tooltipDismissed = false;
    this._hideFocus();
  }
  // Called when the user clicks a legend item (collapse/expand a series).
  // Hide the keyboard-nav tooltip — the chart is about to re-render and the
  // current position may no longer be valid.
  _onLegendClick() {
    if (!this.active) return;
    this.active = false;
    this._hideFocus();
  }
  // ─── Key handler ──────────────────────────────────────────────────────────
  /**
   * @param {KeyboardEvent} e
   */
  _onKeyDown(e2) {
    var _a, _b, _c;
    if (!this._isNavEnabled() || !this.active) return;
    if (e2.shiftKey && (e2.key === "ArrowRight" || e2.key === "ArrowLeft") && this._canPan()) {
      e2.preventDefault();
      this._panBy(e2.key === "ArrowRight" ? 1 : -1);
      return;
    }
    switch (e2.key) {
      case "ArrowRight":
        e2.preventDefault();
        this._move(0, 1);
        break;
      case "ArrowLeft":
        e2.preventDefault();
        this._move(0, -1);
        break;
      case "ArrowUp":
        e2.preventDefault();
        this._move(-1, 0);
        break;
      case "ArrowDown":
        e2.preventDefault();
        this._move(1, 0);
        break;
      case "Home":
        e2.preventDefault();
        this.dataPointIndex = 0;
        this._skipNullForward();
        this._showCurrentPoint();
        break;
      case "End":
        e2.preventDefault();
        this.dataPointIndex = this._getDataPointCount(this.seriesIndex) - 1;
        this._skipNullBackward();
        this._showCurrentPoint();
        break;
      case "Enter":
      case " ":
        e2.preventDefault();
        this._fireClick();
        break;
      case "+":
      case "=":
        if (this._canZoom()) {
          e2.preventDefault();
          (_a = this.ctx.toolbar) == null ? void 0 : _a.handleZoomIn();
          this._announce("Zoomed in");
        }
        break;
      case "-":
      case "_":
        if (this._canZoom()) {
          e2.preventDefault();
          (_b = this.ctx.toolbar) == null ? void 0 : _b.handleZoomOut();
          this._announce("Zoomed out");
        }
        break;
      case "0":
        if (this._canZoom() && this.w.interact.zoomed) {
          e2.preventDefault();
          (_c = this.ctx.toolbar) == null ? void 0 : _c.handleZoomReset();
          this._announce("Zoom reset");
        }
        break;
      case "Escape":
        e2.preventDefault();
        if (!this._tooltipDismissed) {
          this._tooltipDismissed = true;
          this._hideFocus();
        } else {
          this.active = false;
          this._tooltipDismissed = false;
          this._hideFocus();
        }
        break;
    }
  }
  // ─── Zoom / pan (keyboard alternatives for drag gestures) ─────────────────
  _canZoom() {
    const w2 = this.w;
    return Boolean(
      w2.globals.axisCharts && w2.config.chart.zoom && w2.config.chart.zoom.enabled
    );
  }
  _canPan() {
    return this._canZoom();
  }
  /**
   * Shift the visible x-range by ~10% in the given direction.
   * @param {number} direction +1 = right, -1 = left
   */
  _panBy(direction) {
    const w2 = this.w;
    const toolbar = this.ctx.toolbar;
    if (!toolbar) return;
    const minX = Number(w2.globals.minX);
    const maxX = Number(w2.globals.maxX);
    if (!isFinite(minX) || !isFinite(maxX) || minX === maxX) return;
    const span = maxX - minX;
    const step = span * 0.1 * direction;
    toolbar.zoomUpdateOptions(minX + step, maxX + step);
    this._announce(direction > 0 ? "Panned right" : "Panned left");
  }
  // ─── Navigation ───────────────────────────────────────────────────────────
  /**
   * @param {number} dSeries
   * @param {number} dPoint
   */
  _move(dSeries, dPoint) {
    const w2 = this.w;
    const wrapAround = w2.config.chart.accessibility.keyboard.navigation.wrapAround;
    if (dSeries !== 0) {
      const ttCtx = this.w.globals.tooltip;
      if (ttCtx && ttCtx.tConfig && ttCtx.tConfig.shared) {
        const j = this.dataPointIndex;
        const isActuallyShared = ttCtx.tooltipUtil && ttCtx.tooltipUtil.isXoverlap(j) && ttCtx.tooltipUtil.isInitialSeriesSameLen();
        if (isActuallyShared) return;
      }
      const total = this._getSeriesCount();
      let si = this.seriesIndex + dSeries;
      let attempts = 0;
      while (attempts < total) {
        if (si < 0) si = wrapAround ? total - 1 : 0;
        if (si >= total) si = wrapAround ? 0 : total - 1;
        if (!w2.globals.collapsedSeriesIndices.includes(si)) break;
        si += dSeries;
        attempts++;
      }
      this.seriesIndex = si;
      const dpCount = this._getDataPointCount(si);
      if (this.dataPointIndex >= dpCount) {
        this.dataPointIndex = dpCount - 1;
      }
    }
    if (dPoint !== 0) {
      const dpCount = this._getDataPointCount(this.seriesIndex);
      let di = this.dataPointIndex + dPoint;
      if (di < 0) di = wrapAround ? dpCount - 1 : 0;
      if (di >= dpCount) di = wrapAround ? 0 : dpCount - 1;
      this.dataPointIndex = di;
      if (dPoint > 0) {
        this._skipNullForward();
      } else {
        this._skipNullBackward();
      }
      if (!this._isDataPointVisible(this.seriesIndex, this.dataPointIndex)) {
        this._snapToVisibleRangeInDirection(dPoint);
      }
    }
    this._showCurrentPoint();
  }
  /** Advance dataPointIndex forward past any nulls */
  _skipNullForward() {
    const w2 = this.w;
    const si = this.seriesIndex;
    const dpCount = this._getDataPointCount(si);
    let di = this.dataPointIndex;
    let attempts = 0;
    if (!Array.isArray(w2.seriesData.series[si])) return;
    while (attempts < dpCount && w2.seriesData.series[si][di] === null) {
      di = (di + 1) % dpCount;
      attempts++;
    }
    this.dataPointIndex = di;
  }
  /** Retreat dataPointIndex backward past any nulls */
  _skipNullBackward() {
    const w2 = this.w;
    const si = this.seriesIndex;
    const dpCount = this._getDataPointCount(si);
    let di = this.dataPointIndex;
    let attempts = 0;
    if (!Array.isArray(w2.seriesData.series[si])) return;
    while (attempts < dpCount && w2.seriesData.series[si][di] === null) {
      di = (di - 1 + dpCount) % dpCount;
      attempts++;
    }
    this.dataPointIndex = di;
  }
  // ─── Display ──────────────────────────────────────────────────────────────
  _showCurrentPoint() {
    const { seriesIndex: i2, dataPointIndex: j } = this;
    const w2 = this.w;
    const ttCtx = w2.globals.tooltip;
    if (!ttCtx || !ttCtx.ttItems) return;
    w2.interact.capturedSeriesIndex = i2;
    w2.interact.capturedDataPointIndex = j;
    this._applyFocusClass(i2, j);
    this._showTooltip(
      i2,
      j,
      /** @type {any} */
      ttCtx
    );
  }
  _hideFocus() {
    const w2 = this.w;
    const ttCtx = (
      /** @type {any} */
      w2.globals.tooltip
    );
    this._removeFocusClass();
    this._leaveHoveredBar();
    if (!ttCtx) return;
    if (ttCtx.marker) {
      ttCtx.marker.resetPointsSize();
    }
    this._enlargedScatterMarker = null;
    const tooltipEl = ttCtx.getElTooltip();
    if (tooltipEl) {
      tooltipEl.classList.remove("apexcharts-active");
      if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.announcements.enabled) {
        tooltipEl.setAttribute("aria-hidden", "true");
      }
    }
    w2.dom.baseEl.classList.remove("apexcharts-tooltip-active");
    const xcrosshairs = ttCtx.getElXCrosshairs();
    if (xcrosshairs) xcrosshairs.classList.remove("apexcharts-active");
  }
  // ─── Tooltip display per chart type ───────────────────────────────────────
  /**
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */
  _showTooltip(i2, j, ttCtx) {
    const w2 = this.w;
    const type = w2.config.chart.type;
    const tooltipEl = ttCtx.getElTooltip();
    if (!tooltipEl) return;
    const cachedDims = ttCtx.getCachedDimensions();
    ttCtx.tooltipRect = {
      x: 0,
      y: 0,
      ttWidth: cachedDims.ttWidth || 0,
      ttHeight: cachedDims.ttHeight || 0
    };
    this._setSyntheticEvent(i2, j, ttCtx);
    w2.dom.baseEl.classList.add("apexcharts-tooltip-active");
    tooltipEl.classList.add("apexcharts-active");
    if (w2.config.chart.accessibility.enabled && w2.config.chart.accessibility.announcements.enabled) {
      tooltipEl.removeAttribute("aria-hidden");
    }
    if (type === "pie" || type === "donut" || type === "polarArea") {
      this._showTooltipNonAxis(i2, j, ttCtx, tooltipEl);
    } else if (type === "radialBar") {
      this._showTooltipRadialBar(i2, j, ttCtx, tooltipEl);
    } else if (type === "heatmap" || type === "treemap") {
      this._showTooltipHeatTree(i2, j, ttCtx, tooltipEl, type);
    } else if (type === "bar" || type === "candlestick" || type === "boxPlot" || type === "violin" || type === "rangeBar") {
      this._showTooltipBar(i2, j, ttCtx);
    } else {
      this._showTooltipAxisLine(i2, j, ttCtx);
    }
  }
  /**
   * Set ttCtx.e to a synthetic mouse-event-like object whose clientX/Y point
   * to the centre of the current data-point element.  This ensures that any
   * positioning helper that reads ttCtx.e (followCursor path in moveTooltip,
   * moveStickyTooltipOverBars, moveDynamicPointsOnHover, etc.) gets valid
   * coordinates rather than crashing on undefined.
   *
   * For chart types that don't have a concrete SVG element per data point
   * (pie, radialBar) we fall back to the SVG centre.
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */
  _setSyntheticEvent(i2, j, ttCtx) {
    const w2 = this.w;
    const type = w2.config.chart.type;
    let clientX = 0;
    let clientY = 0;
    const el = this._getFocusableElement(i2, j);
    if (el) {
      const rect = el.getBoundingClientRect();
      clientX = rect.left + rect.width / 2;
      clientY = rect.top + rect.height / 2;
    } else if (w2.globals.pointsArray && w2.globals.pointsArray[i2] && w2.globals.pointsArray[i2][j]) {
      const pt = w2.globals.pointsArray[i2][j];
      const elGrid = ttCtx.getElGrid && ttCtx.getElGrid();
      if (elGrid) {
        const gridRect = elGrid.getBoundingClientRect();
        clientX = gridRect.left + (pt[0] || 0);
        clientY = gridRect.top + (pt[1] || 0);
      }
    } else {
      const svgEl = w2.dom.Paper && w2.dom.Paper.node;
      if (svgEl) {
        const svgRect = svgEl.getBoundingClientRect();
        clientX = svgRect.left + svgRect.width / 2;
        clientY = svgRect.top + svgRect.height / 2;
      }
    }
    if (type === "line" || type === "area" || type === "rangeArea" || type === "scatter" || type === "bubble" || type === "radar") {
      if (w2.globals.pointsArray && w2.globals.pointsArray[i2] && w2.globals.pointsArray[i2][j]) {
        const pt = w2.globals.pointsArray[i2][j];
        const elGrid = ttCtx.getElGrid && ttCtx.getElGrid();
        if (elGrid) {
          const gridRect = elGrid.getBoundingClientRect();
          clientX = gridRect.left + (pt[0] || 0);
          clientY = gridRect.top + (pt[1] || 0);
        }
      }
    }
    ttCtx.e = { type: "mousemove", clientX, clientY };
  }
  /**
   * bar / column / candlestick / boxPlot / rangeBar
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */
  _showTooltipBar(i2, j, ttCtx) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const shared = ttCtx.tConfig.shared && (ttCtx.tooltipUtil.isXoverlap(j) || w2.globals.isBarHorizontal) && ttCtx.tooltipUtil.isInitialSeriesSameLen();
    const rangeData = (
      /** @type {any} */
      (_d = (_c = (_b = (_a = w2.rangeData.seriesRange) == null ? void 0 : _a[i2]) == null ? void 0 : _b[j]) == null ? void 0 : _c.y) == null ? void 0 : _d[0]
    );
    ttCtx.tooltipLabels.drawSeriesTexts(__spreadProps(__spreadValues(__spreadValues({
      ttItems: ttCtx.ttItems,
      i: i2,
      j
    }, (rangeData == null ? void 0 : rangeData.y1) !== void 0 && { y1: rangeData.y1 }), (rangeData == null ? void 0 : rangeData.y2) !== void 0 && { y2: rangeData.y2 }), {
      shared
    }));
    const parent = `.apexcharts-series[data\\:realIndex='${i2}']`;
    const elPath = w2.dom.Paper.findOne(
      `${parent} path[j='${j}'], ${parent} circle[j='${j}'], ${parent} rect[j='${j}']`
    );
    if (elPath) {
      this._leaveHoveredBar();
      const graphics = new Graphics(this.w, this.ctx);
      graphics.pathMouseEnter(elPath, null);
      this._hoveredBarEl = elPath;
    }
    if (w2.globals.isBarHorizontal) {
      const barDomEl = elPath && elPath.node;
      if (barDomEl) {
        const wrapRect = w2.dom.elWrap.getBoundingClientRect();
        const barRect = barDomEl.getBoundingClientRect();
        const barCx = barRect.left - wrapRect.left;
        const barCy = barRect.top - wrapRect.top;
        const bh = barRect.height;
        const bw = barRect.width;
        const ttWidth = ttCtx.tooltipRect.ttWidth || 0;
        const ttHeight = ttCtx.tooltipRect.ttHeight || 0;
        const y = barCy + bh / 2 - ttHeight / 2;
        let x = barCx + bw;
        const baselineX = ttCtx.xyRatios && ttCtx.xyRatios.baseLineInvertedY != null ? ttCtx.xyRatios.baseLineInvertedY : wrapRect.width / 2;
        if (barCx < baselineX) {
          x = barCx - ttWidth;
        }
        const tooltipEl = ttCtx.getElTooltip();
        if (tooltipEl) {
          tooltipEl.style.left = x + "px";
          tooltipEl.style.top = y + "px";
        }
      }
    } else {
      ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, i2);
    }
  }
  /**
   * line / area / scatter / bubble / radar / rangeArea
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */
  _showTooltipAxisLine(i2, j, ttCtx) {
    const w2 = this.w;
    const type = w2.config.chart.type;
    const sharedConfigured = ttCtx.tConfig.shared;
    const shared = sharedConfigured && ttCtx.tooltipUtil.isXoverlap(j) && ttCtx.tooltipUtil.isInitialSeriesSameLen();
    ttCtx.tooltipLabels.drawSeriesTexts({
      ttItems: ttCtx.ttItems,
      i: i2,
      j,
      shared
    });
    const isScatterLike = type === "scatter" || type === "bubble";
    const hasVisibleMarkers = w2.globals.markers.largestSize > 0;
    if (isScatterLike) {
      this._showScatterBubblePoint(i2, j, ttCtx);
    } else if (hasVisibleMarkers) {
      if (shared) {
        ttCtx.marker.enlargePoints(j);
      } else {
        ttCtx.tooltipPosition.moveDynamicPointOnHover(j, i2);
      }
    } else if (shared) {
      ttCtx.tooltipPosition.moveDynamicPointsOnHover(j);
    } else {
      ttCtx.tooltipPosition.moveDynamicPointOnHover(j, i2);
    }
  }
  /**
   * Scatter / bubble: find the specific marker element for (seriesIndex i,
   * dataPointIndex j), resize only that element, and position the tooltip at
   * its coordinates — mirroring what Position.moveMarkers does for mouse hover.
   *
   * Unlike enlargePoints(j) which queries ALL series for rel===j (causing
   * multiple bubbles to enlarge and tooltip to land on the wrong one), this
   * method queries by both series index AND data-point index for precision.
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */
  _showScatterBubblePoint(i2, j, ttCtx) {
    const baseEl = this.w.dom.baseEl;
    if (this._enlargedScatterMarker) {
      ttCtx.marker.oldPointSize(this._enlargedScatterMarker);
      this._enlargedScatterMarker = null;
    }
    const seriesEl = baseEl.querySelector(
      `.apexcharts-series[data\\:realIndex='${i2}']`
    );
    if (!seriesEl) return;
    const markerEl = seriesEl.querySelector(`.apexcharts-marker[rel='${j}']`);
    if (!markerEl) return;
    ttCtx.marker.enlargeCurrentPoint(j, markerEl);
    this._enlargedScatterMarker = markerEl;
  }
  /**
   * pie / donut / polarArea
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   * @param {HTMLElement} tooltipEl
   */
  _showTooltipNonAxis(i2, j, ttCtx, tooltipEl) {
    const w2 = this.w;
    ttCtx.tooltipLabels.drawSeriesTexts({
      ttItems: ttCtx.ttItems,
      i: j,
      shared: false
    });
    const tooltipBound = tooltipEl.getBoundingClientRect();
    const ttWidth = tooltipBound.width || ttCtx.tooltipRect.ttWidth || 0;
    const ttHeight = tooltipBound.height || ttCtx.tooltipRect.ttHeight || 0;
    const sliceEl = w2.dom.baseEl.querySelector(`.apexcharts-pie-area[j='${j}']`);
    const anchor = ttCtx.getSliceAnchor(sliceEl);
    if (anchor) {
      tooltipEl.style.left = anchor.x - ttWidth / 2 + "px";
      tooltipEl.style.top = anchor.y - ttHeight - 10 + "px";
    }
  }
  /**
   * radialBar — one ring per series, single value each
   * @param {number} i
   * @param {any} _j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   * @param {HTMLElement} tooltipEl
   */
  _showTooltipRadialBar(i2, _j, ttCtx, tooltipEl) {
    var _a;
    const w2 = this.w;
    ttCtx.tooltipLabels.drawSeriesTexts({
      ttItems: ttCtx.ttItems,
      i: i2,
      shared: false
    });
    const { ttWidth = 0, ttHeight = 0 } = ttCtx.getCachedDimensions();
    const arcEl = w2.dom.baseEl.querySelector(
      `.apexcharts-radialbar-series[data\\:realIndex='${i2}'] path`
    );
    if (arcEl) {
      const angle = parseFloat((_a = arcEl.getAttribute("data:angle")) != null ? _a : "") || 0;
      const initialAngle = w2.config.plotOptions.radialBar.startAngle || 0;
      const midAngle = initialAngle + angle / 2;
      const centerX = w2.layout.gridWidth / 2;
      const centerY = w2.layout.gridHeight / 2;
      const radialSize = w2.globals.radialSize || Math.min(w2.layout.gridWidth, w2.layout.gridHeight) / 2;
      const seriesCount = w2.seriesData.series.length;
      const trackSize = radialSize / Math.max(seriesCount, 1);
      const outerRadius = radialSize - i2 * trackSize;
      const innerRadius = outerRadius - trackSize;
      const ringRadius = (outerRadius + innerRadius) / 2;
      const centroid = Utils$1.polarToCartesian(
        centerX,
        centerY,
        ringRadius,
        midAngle
      );
      const x = centroid.x + (w2.layout.translateX || 0);
      const y = centroid.y + (w2.layout.translateY || 0);
      tooltipEl.style.left = x - ttWidth / 2 + "px";
      tooltipEl.style.top = y - ttHeight - 10 + "px";
    }
  }
  /**
   * heatmap / treemap — position tooltip using element bounding rect
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   * @param {HTMLElement} tooltipEl
   * @param {string} type
   */
  _showTooltipHeatTree(i2, j, ttCtx, tooltipEl, type) {
    var _a, _b;
    const w2 = this.w;
    ttCtx.tooltipLabels.drawSeriesTexts({
      ttItems: ttCtx.ttItems,
      i: i2,
      j,
      shared: false
    });
    const tooltipRect = tooltipEl.getBoundingClientRect();
    const ttWidth = tooltipRect.width || ttCtx.tooltipRect.ttWidth || 0;
    const ttHeight = tooltipRect.height || ttCtx.tooltipRect.ttHeight || 0;
    const rectClass = type === "heatmap" ? "apexcharts-heatmap-rect" : "apexcharts-treemap-rect";
    const cell = w2.dom.baseEl.querySelector(`.${rectClass}[i='${i2}'][j='${j}']`);
    if (cell) {
      const wrapRect = w2.dom.elWrap.getBoundingClientRect();
      const cellRect = cell.getBoundingClientRect();
      const cellCx = cellRect.left - wrapRect.left;
      const cellCy = cellRect.top - wrapRect.top;
      const cellWidth = cellRect.width;
      const cellHeight = cellRect.height;
      const cx = parseFloat((_a = cell.getAttribute("cx")) != null ? _a : "");
      const cellWidthAttr = parseFloat((_b = cell.getAttribute("width")) != null ? _b : "");
      ttCtx.tooltipPosition.moveXCrosshairs(cx + cellWidthAttr / 2);
      let x = cellCx + cellWidth + ttWidth / 2;
      const y = cellCy + cellHeight / 2 - ttHeight / 2;
      if (cellCx + cellWidth > w2.layout.gridWidth / 2) {
        x = cellCx - ttWidth / 2;
      }
      tooltipEl.style.left = x + "px";
      tooltipEl.style.top = y + "px";
    }
  }
  // ─── Focus class management ───────────────────────────────────────────────
  /**
   * @param {number} i
   * @param {number} j
   */
  _applyFocusClass(i2, j) {
    this._removeFocusClass();
    const el = this._getFocusableElement(i2, j);
    if (el) {
      el.classList.add("apexcharts-keyboard-focused");
      el.setAttribute("role", "img");
      const label = this._buildPointLabel(i2, j);
      if (label) el.setAttribute("aria-label", label);
      this._focusedEl = el;
    }
  }
  _removeFocusClass() {
    if (this._focusedEl) {
      this._focusedEl.classList.remove("apexcharts-keyboard-focused");
      this._focusedEl.removeAttribute("role");
      this._focusedEl.removeAttribute("aria-label");
      this._focusedEl = null;
    }
  }
  /**
   * Build an accessible label for the data point at (i, j) using the same
   * formatters the visible tooltip / axis labels use, so SR output matches
   * the visual presentation.
   * @param {number} i
   * @param {number} j
   * @returns {string}
   */
  _buildPointLabel(i2, j) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const w2 = this.w;
    const type = w2.config.chart.type;
    const seriesNames = w2.seriesData.seriesNames || [];
    const series = w2.seriesData.series || [];
    if (type === "pie" || type === "donut" || type === "polarArea") {
      const sliceLabel = (_b = ((_a = w2.labelData) == null ? void 0 : _a.labels) && w2.labelData.labels[j]) != null ? _b : "";
      const value = Array.isArray(series) ? series[j] : "";
      return sliceLabel ? `${sliceLabel}: ${value}` : `${value}`;
    }
    if (type === "radialBar") {
      const seriesName2 = seriesNames[i2] || `Series ${i2 + 1}`;
      const value = Array.isArray(series) ? series[i2] : "";
      return `${seriesName2}: ${value}`;
    }
    const seriesName = seriesNames[i2] || `Series ${i2 + 1}`;
    const row = Array.isArray(series[i2]) ? series[i2] : [];
    const rawValue = row[j];
    let formattedValue = rawValue == null ? "" : String(rawValue);
    const yFormatter = (_d = (_c = w2.formatters) == null ? void 0 : _c.yLabelFormatters) == null ? void 0 : _d[i2];
    if (typeof yFormatter === "function") {
      try {
        formattedValue = yFormatter(rawValue, {
          seriesIndex: i2,
          dataPointIndex: j,
          w: w2
        });
      } catch (e2) {
      }
    }
    let category = "";
    const categoryLabels = (_e = w2.labelData) == null ? void 0 : _e.categoryLabels;
    const seriesX = (_g = (_f = w2.seriesData) == null ? void 0 : _f.seriesX) == null ? void 0 : _g[i2];
    if (Array.isArray(categoryLabels) && categoryLabels[j] != null) {
      category = String(categoryLabels[j]);
    } else if (Array.isArray(seriesX) && seriesX[j] != null) {
      const xFormatter = (_h = w2.formatters) == null ? void 0 : _h.xLabelFormatter;
      if (typeof xFormatter === "function") {
        try {
          category = String(
            xFormatter(seriesX[j], { seriesIndex: i2, dataPointIndex: j, w: w2 })
          );
        } catch (e2) {
          category = String(seriesX[j]);
        }
      } else {
        category = String(seriesX[j]);
      }
    }
    return category ? `${seriesName}: ${formattedValue}, ${category}` : `${seriesName}: ${formattedValue}`;
  }
  _leaveHoveredBar() {
    if (this._hoveredBarEl) {
      const graphics = new Graphics(this.w, this.ctx);
      graphics.pathMouseLeave(this._hoveredBarEl, null);
      this._hoveredBarEl = null;
    }
  }
  /**
   * @param {number} i
   * @param {number} j
   */
  _getFocusableElement(i2, j) {
    const w2 = this.w;
    const type = w2.config.chart.type;
    const baseEl = w2.dom.baseEl;
    if (type === "pie" || type === "donut" || type === "polarArea") {
      return baseEl.querySelector(`.apexcharts-pie-area[j='${j}']`);
    }
    if (type === "heatmap") {
      return baseEl.querySelector(
        `.apexcharts-heatmap-rect[i='${i2}'][j='${j}']`
      );
    }
    if (type === "treemap") {
      return baseEl.querySelector(
        `.apexcharts-treemap-rect[i='${i2}'][j='${j}']`
      );
    }
    if (type === "radialBar") {
      return baseEl.querySelector(
        `.apexcharts-radialbar-series[data\\:realIndex='${i2}'] path`
      );
    }
    if (type === "bar" || type === "candlestick" || type === "boxPlot" || type === "violin" || type === "rangeBar") {
      return baseEl.querySelector(
        `.apexcharts-series[data\\:realIndex='${i2}'] path[j='${j}']`
      );
    }
    const marker = baseEl.querySelector(
      `.apexcharts-series[data\\:realIndex='${i2}'] .apexcharts-marker[rel='${j}']`
    );
    return marker || null;
  }
  // ─── Click / Enter ────────────────────────────────────────────────────────
  _fireClick() {
    const w2 = this.w;
    const ttCtx = w2.globals.tooltip;
    if (!ttCtx) return;
    const syntheticEvent = {
      type: "mouseup",
      clientX: 0,
      clientY: 0
    };
    ttCtx.markerClick(syntheticEvent, this.seriesIndex, this.dataPointIndex);
  }
  // ─── Helpers ──────────────────────────────────────────────────────────────
  _isNavEnabled() {
    const a11y = this.w.config.chart.accessibility;
    return a11y.enabled && a11y.keyboard.enabled && a11y.keyboard.navigation.enabled;
  }
  _getSeriesCount() {
    const w2 = this.w;
    const type = w2.config.chart.type;
    if (type === "pie" || type === "donut" || type === "polarArea") {
      return 1;
    }
    return w2.seriesData.series.length;
  }
  /**
   * @param {number} si
   */
  _getDataPointCount(si) {
    const w2 = this.w;
    const type = w2.config.chart.type;
    if (type === "pie" || type === "donut" || type === "polarArea") {
      return w2.seriesData.series.length;
    }
    const series = w2.seriesData.series;
    return series[si] && Array.isArray(series[si]) ? series[si].length : 0;
  }
  _clampCursor() {
    const seriesCount = this._getSeriesCount();
    if (this.seriesIndex >= seriesCount) this.seriesIndex = seriesCount - 1;
    if (this.seriesIndex < 0) this.seriesIndex = 0;
    const dpCount = this._getDataPointCount(this.seriesIndex);
    if (this.dataPointIndex >= dpCount) this.dataPointIndex = dpCount - 1;
    if (this.dataPointIndex < 0) this.dataPointIndex = 0;
  }
  /**
   * When the chart is zoomed in, the current dataPointIndex may point to a
   * data point that is outside the visible viewport. Snap the cursor to the
   * first data point whose x-value falls within [minX, maxX].
   *
   * Only adjusts when w.seriesData.seriesX is populated (numeric/datetime axes).
   * Category-only charts (seriesX entries are strings or auto-indices) are
   * unaffected — all points are always visible.
   */
  _snapToVisibleRange() {
    const w2 = this.w;
    const gl = w2.globals;
    const si = this.seriesIndex;
    if (!w2.interact.zoomed) return;
    const seriesX = w2.seriesData.seriesX && w2.seriesData.seriesX[si];
    if (!seriesX || !seriesX.length) return;
    const minX = gl.minX;
    const maxX = gl.maxX;
    if (minX === void 0 || maxX === void 0) return;
    const currentX = seriesX[this.dataPointIndex];
    if (currentX >= minX && currentX <= maxX) return;
    const dpCount = seriesX.length;
    for (let di = 0; di < dpCount; di++) {
      if (seriesX[di] >= minX && seriesX[di] <= maxX) {
        this.dataPointIndex = di;
        return;
      }
    }
  }
  /**
   * Snap to the nearest visible data point in the given navigation direction.
   * direction > 0 → find the first visible point (left boundary of zoomed range)
   * direction < 0 → find the last visible point (right boundary of zoomed range)
   * @param {number} direction
   */
  _snapToVisibleRangeInDirection(direction) {
    const w2 = this.w;
    const gl = w2.globals;
    const si = this.seriesIndex;
    const seriesX = w2.seriesData.seriesX && w2.seriesData.seriesX[si];
    if (!seriesX || !seriesX.length) return;
    const minX = gl.minX;
    const maxX = gl.maxX;
    if (minX === void 0 || maxX === void 0) return;
    const dpCount = seriesX.length;
    if (direction >= 0) {
      for (let di = 0; di < dpCount; di++) {
        if (seriesX[di] >= minX && seriesX[di] <= maxX) {
          this.dataPointIndex = di;
          return;
        }
      }
    } else {
      for (let di = dpCount - 1; di >= 0; di--) {
        if (seriesX[di] >= minX && seriesX[di] <= maxX) {
          this.dataPointIndex = di;
          return;
        }
      }
    }
  }
  /**
   * Check whether the data point at (si, di) is within the current visible
   * x-axis range. Used to skip out-of-viewport points during keyboard nav.
   * @param {number} si
   * @param {number} di
   */
  _isDataPointVisible(si, di) {
    const w2 = this.w;
    const gl = w2.globals;
    if (!w2.interact.zoomed) return true;
    const seriesX = w2.seriesData.seriesX && w2.seriesData.seriesX[si];
    if (!seriesX) return true;
    const x = seriesX[di];
    if (x === void 0) return true;
    return x >= gl.minX && x <= gl.maxX;
  }
  /**
   * Push a short status message to the visually-hidden aria-live region so
   * screen readers announce zoom / pan / reset events that have no inherent
   * tooltip update. Silently no-op if the region is missing or announcements
   * are disabled.
   * @param {string} message
   */
  _announce(message) {
    const w2 = this.w;
    if (!w2.config.chart.accessibility.announcements.enabled) return;
    const baseEl = w2.dom.baseEl;
    if (!baseEl) return;
    const region = baseEl.querySelector(".apexcharts-sr-status");
    if (!region) return;
    region.textContent = "";
    setTimeout(() => {
      region.textContent = message;
    }, 0);
  }
}
ApexCharts.registerFeatures({ keyboardNavigation: KeyboardNavigation });
function gridDivideRect(bbox, count) {
  if (!(count > 0)) return [];
  if (count === 1) return [__spreadValues({}, bbox)];
  const horizontal = bbox.width >= bbox.height;
  const rowExtent = horizontal ? bbox.width : bbox.height;
  const colExtent = horizontal ? bbox.height : bbox.width;
  const ratio = colExtent > 0 ? rowExtent / colExtent : count;
  let rows = Math.max(1, Math.ceil(Math.sqrt(ratio * count)));
  if (rows > count) rows = count;
  const baseCols = Math.floor(count / rows);
  let remainder = count - baseCols * rows;
  const cells = [];
  const rowSize = rowExtent / rows;
  let rowStart = 0;
  for (let r2 = 0; r2 < rows; r2++) {
    const cols = baseCols + (remainder > 0 ? 1 : 0);
    if (remainder > 0) remainder--;
    const colSize = cols > 0 ? colExtent / cols : 0;
    for (let c2 = 0; c2 < cols; c2++) {
      cells.push(
        horizontal ? {
          x: bbox.x + rowStart,
          y: bbox.y + c2 * colSize,
          width: rowSize,
          height: colSize
        } : {
          x: bbox.x + c2 * colSize,
          y: bbox.y + rowStart,
          width: colSize,
          height: rowSize
        }
      );
    }
    rowStart += rowSize;
  }
  return cells;
}
function gridDivideShape(bbox, count, intervalsAt) {
  if (!(count > 0)) return [];
  const horizontal = bbox.width >= bbox.height;
  const rowExtent = horizontal ? bbox.width : bbox.height;
  const colExtent = horizontal ? bbox.height : bbox.width;
  const minorLo = horizontal ? bbox.y : bbox.x;
  const minorHi = minorLo + colExtent;
  const ratio = colExtent > 0 ? rowExtent / colExtent : count;
  let rows = Math.max(
    Math.ceil(Math.sqrt(ratio * count)),
    Math.ceil(rowExtent / 16)
  );
  if (!(rows >= 1)) rows = 1;
  if (rows > count) rows = count;
  const baseCols = Math.floor(count / rows);
  let remainder = count - baseCols * rows;
  const cells = [];
  const rowSize = rowExtent / rows;
  let rowStart = horizontal ? bbox.x : bbox.y;
  for (let r2 = 0; r2 < rows; r2++) {
    const cols = baseCols + (remainder > 0 ? 1 : 0);
    if (remainder > 0) remainder--;
    const spans = [];
    const raw = intervalsAt(rowStart, rowStart + rowSize, horizontal);
    if (Array.isArray(raw)) {
      for (let s2 = 0; s2 < raw.length; s2++) {
        const iv = raw[s2];
        if (!iv) continue;
        const lo = Math.max(minorLo, Math.min(iv[0], iv[1]));
        const hi = Math.min(minorHi, Math.max(iv[0], iv[1]));
        if (hi > lo) spans.push([lo, hi]);
      }
    }
    if (!spans.length) spans.push([minorLo, minorHi]);
    const totalLen = spans.reduce((a2, s2) => a2 + (s2[1] - s2[0]), 0);
    const exact = spans.map(
      (s2) => totalLen > 0 ? (s2[1] - s2[0]) / totalLen * cols : cols / spans.length
    );
    const share = exact.map((v2) => Math.floor(v2));
    let used = share.reduce((a2, b2) => a2 + b2, 0);
    const byFrac = exact.map((v2, i2) => ({ i: i2, frac: v2 - Math.floor(v2) })).sort((a2, b2) => b2.frac - a2.frac);
    for (let k = 0; used < cols; k++, used++) {
      share[byFrac[k % byFrac.length].i]++;
    }
    for (let s2 = 0; s2 < spans.length; s2++) {
      const n2 = share[s2];
      if (n2 <= 0) continue;
      const lo = spans[s2][0];
      const colSize = (spans[s2][1] - lo) / n2;
      for (let c2 = 0; c2 < n2; c2++) {
        cells.push(
          horizontal ? { x: rowStart, y: lo + c2 * colSize, width: rowSize, height: colSize } : { x: lo + c2 * colSize, y: rowStart, width: colSize, height: rowSize }
        );
      }
    }
    rowStart += rowSize;
  }
  return cells;
}
function hilbertIndex(x, y, minX, minY, maxX, maxY) {
  let ix = maxX === minX ? 0 : Math.round(32767 * ((x - minX) / (maxX - minX)));
  let iy = maxY === minY ? 0 : Math.round(32767 * ((y - minY) / (maxY - minY)));
  let d = 0;
  for (let s2 = 32768; s2 >= 1; s2 /= 2) {
    const rx = (ix & s2) > 0 ? 1 : 0;
    const ry = (iy & s2) > 0 ? 1 : 0;
    d += s2 * s2 * (3 * rx ^ ry);
    if (ry === 0) {
      if (rx === 1) {
        ix = s2 - 1 - ix;
        iy = s2 - 1 - iy;
      }
      const t2 = ix;
      ix = iy;
      iy = t2;
    }
  }
  return d;
}
function sortByHilbert(items, getXY) {
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  const pts = items.map((it) => {
    const [x, y] = getXY(it);
    if (x < minX) minX = x;
    if (x > maxX) maxX = x;
    if (y < minY) minY = y;
    if (y > maxY) maxY = y;
    return [x, y];
  });
  return items.map((item, k) => ({
    item,
    d: hilbertIndex(pts[k][0], pts[k][1], minX, minY, maxX, maxY)
  })).sort((a2, b2) => a2.d - b2.d).map((e2) => e2.item);
}
function parseColor(str) {
  if (!str || typeof str !== "string") return null;
  const s2 = str.trim();
  if (s2[0] === "#") {
    const hex = s2.slice(1);
    if (hex.length === 3) {
      return [
        parseInt(hex[0] + hex[0], 16),
        parseInt(hex[1] + hex[1], 16),
        parseInt(hex[2] + hex[2], 16),
        1
      ];
    }
    if (hex.length === 6 || hex.length === 8) {
      return [
        parseInt(hex.slice(0, 2), 16),
        parseInt(hex.slice(2, 4), 16),
        parseInt(hex.slice(4, 6), 16),
        hex.length === 8 ? parseInt(hex.slice(6, 8), 16) / 255 : 1
      ];
    }
    return null;
  }
  const m2 = s2.match(/^rgba?\(([^)]+)\)$/i);
  if (m2) {
    const parts = m2[1].split(",").map((p2) => parseFloat(p2));
    if (parts.length < 3 || parts.some((v2) => !isFinite(v2))) return null;
    return [parts[0], parts[1], parts[2], parts.length > 3 ? parts[3] : 1];
  }
  return null;
}
function makeColorLerp(from, to) {
  const a2 = parseColor(from);
  const b2 = parseColor(to);
  if (!a2 || !b2) return null;
  return (t2) => {
    const r2 = Math.round(a2[0] + (b2[0] - a2[0]) * t2);
    const g2 = Math.round(a2[1] + (b2[1] - a2[1]) * t2);
    const bl = Math.round(a2[2] + (b2[2] - a2[2]) * t2);
    const al = a2[3] + (b2[3] - a2[3]) * t2;
    return al >= 1 ? `rgb(${r2},${g2},${bl})` : `rgba(${r2},${g2},${bl},${al})`;
  };
}
function easeInOutCubic$1(t2) {
  return t2 < 0.5 ? 4 * t2 * t2 * t2 : 1 - Math.pow(-2 * t2 + 2, 3) / 2;
}
function runPieceTween({ pieces, duration, onPieceDone, onAllDone }) {
  let cancelled = false;
  const start = Date.now();
  const dur = Math.max(1, duration);
  const write = (p2, e2) => {
    const f = p2.from;
    const t2 = p2.to;
    const el = p2.el;
    el.setAttribute("x", String(f.x + (t2.x - f.x) * e2));
    el.setAttribute("y", String(f.y + (t2.y - f.y) * e2));
    el.setAttribute("width", String(Math.max(0, f.width + (t2.width - f.width) * e2)));
    el.setAttribute("height", String(Math.max(0, f.height + (t2.height - f.height) * e2)));
    el.setAttribute("rx", String(Math.max(0, f.rx + (t2.rx - f.rx) * e2)));
    if (p2.fill) el.setAttribute("fill", p2.fill(e2));
    else if (e2 >= 1 && p2.fillEnd) el.setAttribute("fill", p2.fillEnd);
  };
  const frame = () => {
    if (cancelled) return;
    const elapsed = Date.now() - start;
    let live = false;
    for (let k = 0; k < pieces.length; k++) {
      const p2 = pieces[k];
      if (
        /** @type {any} */
        p2._done
      ) continue;
      const raw = (elapsed - p2.delay) / dur;
      if (raw < 1) live = true;
      if (raw <= 0) continue;
      const t2 = Math.min(1, raw);
      write(p2, easeInOutCubic$1(t2));
      if (t2 >= 1) {
        p2._done = true;
        if (onPieceDone) onPieceDone(p2);
      }
    }
    if (live) BrowserAPIs.requestAnimationFrame(frame);
    else if (onAllDone) onAllDone();
  };
  BrowserAPIs.requestAnimationFrame(frame);
  return () => {
    cancelled = true;
  };
}
const BAR_FAMILY = /* @__PURE__ */ new Set(["bar", "funnel", "pyramid", "histogram"]);
const RADIAL_FAMILY = /* @__PURE__ */ new Set(["pie", "donut", "polarArea", "radialBar", "gauge"]);
const UNIT_FAMILY = /* @__PURE__ */ new Set(["unit", "waffle"]);
const PARTITION_FAMILY = /* @__PURE__ */ new Set(["treemap", "sunburst"]);
const SUMMARY_FAMILY = /* @__PURE__ */ new Set(["boxPlot", "violin"]);
const GHOST_FADE_FRACTION = 0.55;
const PIECE_BUDGET = 1500;
const PIECE_STAGGER_MAX = 300;
function familyOf(type) {
  if (BAR_FAMILY.has(type)) return "bar";
  if (RADIAL_FAMILY.has(type)) return "radial";
  if (UNIT_FAMILY.has(type)) return "unit";
  if (PARTITION_FAMILY.has(type)) return "partition";
  if (SUMMARY_FAMILY.has(type)) return "summary";
  return null;
}
class MorphTypeChange {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this._snapshot = null;
    this._ghost = null;
    this._pieceLayer = null;
    this._pieceCancel = null;
  }
  /**
   * @param {string} fromType
   * @param {string} toType
   * @returns {boolean}
   */
  canMorphTypes(fromType, toType) {
    if (fromType === toType) return false;
    const ff = familyOf(fromType);
    const tf = familyOf(toType);
    if (!ff || !tf) return false;
    if (ff === "partition" !== (tf === "partition")) {
      return ff !== "unit" && tf !== "unit";
    }
    return true;
  }
  /**
   * @param {string} fromType
   * @param {string} toType
   * @param {any} newSeries
   * @returns {boolean}
   */
  isCompatibleSeriesShape(fromType, toType, newSeries) {
    if (!Array.isArray(newSeries) || newSeries.length === 0) return false;
    const ff = familyOf(fromType);
    const tf = familyOf(toType);
    if (tf === "unit") {
      if (newSeries.every((v2) => typeof v2 === "number")) return true;
      return newSeries.every(
        (s2) => s2 && typeof s2 === "object" && Array.isArray(s2.data)
      );
    }
    if (tf === "partition") {
      return true;
    }
    if (tf === "radial") {
      if (newSeries.every((v2) => typeof v2 === "number")) return true;
      return newSeries.length === 1 && newSeries[0] && typeof newSeries[0] === "object" && Array.isArray(newSeries[0].data);
    }
    if (tf === "bar" || tf === "summary") {
      return newSeries.every(
        (s2) => s2 && typeof s2 === "object" && Array.isArray(s2.data)
      );
    }
    return ff !== null && tf !== null;
  }
  /**
   * Capture the live DOM of the *current* (outgoing) chart and stash it on
   * this module. Called from `apexcharts._updateOptions` before the config
   * merge that flips `chart.type`.
   *
   * Returns true if a morph is queued — caller doesn't need the value, but
   * tests use it.
   *
   * @param {{ fromType: string, toType: string, newSeries: any }} args
   * @returns {boolean}
   */
  captureBeforeDestroy({ fromType, toType, newSeries }) {
    this._snapshot = null;
    this._removeGhost();
    this._cancelPieces();
    if (!Environment.isBrowser()) return false;
    const animCfg = this.w.config.chart.animations;
    if (!animCfg || animCfg.enabled === false) return false;
    if (animCfg.chartTypeMorph && animCfg.chartTypeMorph.enabled === false)
      return false;
    if (animCfg.respectReducedMotion && prefersReducedMotion()) return false;
    if (!this.canMorphTypes(fromType, toType)) return false;
    if (!this.isCompatibleSeriesShape(fromType, toType, newSeries)) return false;
    const { marks, branches, unitDots } = this._captureFromDOM(fromType);
    if (!marks.length) return false;
    const mapping = this._buildMapping(
      marks,
      fromType,
      toType,
      newSeries,
      branches
    );
    if (mapping.size === 0) return false;
    this._snapshot = {
      fromType,
      toType,
      mapping,
      oldLayout: {
        translateX: this.w.layout.translateX || 0,
        translateY: this.w.layout.translateY || 0
      }
    };
    const ff = familyOf(fromType);
    const tf = familyOf(toType);
    const canShape = this._canProbePaths();
    const pieceFamilies = ff === "bar" || ff === "summary" || ff === "radial" && canShape;
    if (tf === "unit" && pieceFamilies) {
      const total = this._countUnitSeries(newSeries);
      this._snapshot.pieceOut = total > 0 && total <= PIECE_BUDGET;
    } else if (ff === "unit" && (tf === "bar" || tf === "summary" || tf === "radial" && canShape)) {
      let total = 0;
      unitDots.forEach((list) => {
        total += list.length;
      });
      if (total > 0 && total <= PIECE_BUDGET) {
        this._snapshot.pieceIn = true;
        this._snapshot.sourceDots = unitDots;
        const keyOrder = [];
        if (tf === "radial") {
          (Array.isArray(newSeries) ? newSeries : []).forEach(
            (_v, i2) => {
              keyOrder.push(`${i2}:0`);
            }
          );
        } else {
          (Array.isArray(newSeries) ? newSeries : []).forEach(
            (s2, seriesIdx) => {
              const data = s2 && Array.isArray(s2.data) ? s2.data : [];
              for (let j = 0; j < data.length; j++) {
                keyOrder.push(`${seriesIdx}:${j}`);
              }
            }
          );
        }
        this._snapshot.keyOrder = keyOrder;
      }
    }
    if (this._needsGhost(fromType, toType) && !this._snapshot.pieceOut && !this._snapshot.pieceIn) {
      this._captureGhost();
    }
    this.w.globals.previousPaths = [];
    return true;
  }
  /**
   * Whether the outgoing marks need an exit animation of their own.
   *
   * Most pairs do not. bar → pie hands every wedge the exact `d` of the bar it
   * replaces, and treemap → sunburst does the same for its tiles: the outgoing
   * mark IS the incoming mark's first frame, so it never needs to leave, and
   * drawing a copy of it would only double the image at t=0.
   *
   * The unit pairs are the exception, in both directions, because the
   * correspondence is not 1:1. Going in, one bar becomes N dots, so the bar has
   * no successor to become. Coming out, N dots become one bar: the bar does
   * grow from the cloud's footprint, but no individual dot has anywhere to go.
   * Either way something on screen simply stops existing, which is exactly the
   * hard cut that made these pairs read as "the old chart vanished and the new
   * one animated" rather than as a morph.
   *
   * @param {string} fromType
   * @param {string} toType
   * @returns {boolean}
   */
  _needsGhost(fromType, toType) {
    return familyOf(fromType) === "unit" || familyOf(toType) === "unit";
  }
  /**
   * Take a detached copy of the outgoing chart's marks, to be mounted over the
   * incoming chart once it exists (see `_mountGhost`).
   *
   * The whole `<svg>` is cloned and the chrome then removed from the copy,
   * rather than lifting the series groups out on their own: every mark's
   * position depends on the transforms of the groups above it, and cloning
   * from the root is what keeps those intact without re-deriving any geometry.
   *
   * The chrome is dropped because `applyChromeFade` already fades the incoming
   * axes, grid and legend in from zero. Keeping the outgoing set as well would
   * put two sets of axis labels on screen at half opacity each.
   */
  _captureGhost() {
    var _a, _b;
    const paper = (_a = this.w.dom) == null ? void 0 : _a.Paper;
    const node = paper && paper.node;
    if (!node || typeof node.cloneNode !== "function") return;
    const clone = node.cloneNode(true);
    const drop = [
      ".apexcharts-xaxis",
      ".apexcharts-yaxis",
      ".apexcharts-grid",
      ".apexcharts-gridlines-horizontal",
      ".apexcharts-gridlines-vertical",
      ".apexcharts-legend",
      ".apexcharts-title-text",
      ".apexcharts-subtitle-text",
      ".apexcharts-annotations",
      ".apexcharts-zoom-rect",
      ".apexcharts-selection-rect",
      ".apexcharts-xcrosshairs",
      ".apexcharts-ycrosshairs"
    ];
    if (typeof clone.querySelectorAll === "function") {
      drop.forEach((sel) => {
        clone.querySelectorAll(sel).forEach((el) => {
          if (el.parentNode) el.parentNode.removeChild(el);
        });
      });
    }
    if (typeof clone.querySelectorAll === "function") {
      clone.querySelectorAll("[id]").forEach((el) => {
        el.removeAttribute("id");
      });
    }
    (_b = clone.removeAttribute) == null ? void 0 : _b.call(clone, "id");
    this._ghost = clone;
  }
  /**
   * Mount the captured copy over the newly-rendered chart and fade it out.
   *
   * It goes ON TOP of the live svg, which is what makes both directions read
   * as one motion rather than as a swap. Going into a unit chart the bars
   * dissolve and the dots are uncovered already in flight, having left from
   * inside the bar that held them. Coming out of one, the dots are still there
   * to fade while the bar grows underneath them; behind the incoming mark they
   * would be hidden on the first frame, because that mark starts out exactly
   * the size of the cloud it is replacing.
   *
   * The fade runs over a fraction of the morph so the outgoing marks are gone
   * before the incoming ones settle. Holding them for the full duration leaves
   * two charts overlapping right at the moment the eye is reading the final
   * shape, which looks like a rendering fault rather than a transition.
   */
  _mountGhost() {
    var _a, _b, _c;
    const ghost = this._ghost;
    if (!ghost || !Environment.isBrowser()) return;
    const wrap = (_a = this.w.dom) == null ? void 0 : _a.elWrap;
    if (!wrap || typeof wrap.appendChild !== "function") {
      this._ghost = null;
      return;
    }
    const style = ghost.style;
    if (style) {
      style.position = "absolute";
      style.left = "0";
      style.top = "0";
      style.background = "transparent";
      style.pointerEvents = "none";
      style.opacity = "1";
    }
    (_b = ghost.setAttribute) == null ? void 0 : _b.call(ghost, "aria-hidden", "true");
    (_c = ghost.setAttribute) == null ? void 0 : _c.call(ghost, "class", "apexcharts-morph-ghost");
    wrap.appendChild(ghost);
    const speed = this.getSpeed();
    const fade = Math.max(120, Math.round(speed * GHOST_FADE_FRACTION));
    BrowserAPIs.requestAnimationFrame(() => {
      if (!style) return;
      style.transition = `opacity ${fade}ms ease-in`;
      style.opacity = "0";
    });
    setTimeout(() => this._removeGhost(), fade + 60);
  }
  /** Detach the ghost if one is mounted. Safe to call at any time. */
  _removeGhost() {
    const ghost = this._ghost;
    this._ghost = null;
    if (ghost && ghost.parentNode) ghost.parentNode.removeChild(ghost);
  }
  /* ------------------------------------------------------------------ *
   * The piece layer (see MorphPieces for the geometry).
   * ------------------------------------------------------------------ */
  /**
   * Object count of an incoming unit series: one datum per dot in the object
   * form. The numeric form ([3, 5]) scales values by `plotOptions.unit
   * .unitValue`, which is not resolvable pre-merge, so it counts as zero and
   * keeps the burst-and-ghost behaviour.
   *
   * @param {any} newSeries
   * @returns {number}
   */
  _countUnitSeries(newSeries) {
    if (!Array.isArray(newSeries)) return 0;
    let total = 0;
    for (const s2 of newSeries) {
      if (!s2 || typeof s2 !== "object" || !Array.isArray(s2.data)) return 0;
      total += s2.data.length;
    }
    return total;
  }
  /**
   * Whether the incoming unit chart should hold its dots for the piece layer:
   * render them at their final slots, hidden, and let the pieces do the
   * flying. The reveal happens per dot as its piece lands.
   *
   * Consulted by the unit renderer during its draw, which runs after
   * `captureBeforeDestroy` and before `applyChromeFade`, so the decision was
   * already made from the same series the renderer is now drawing.
   *
   * @returns {boolean}
   */
  usesPieceTakeover() {
    return !!(this._snapshot && this._snapshot.pieceOut);
  }
  /**
   * Whether the piece layer claims the incoming mark at (realIndex, j): a
   * source cluster's dots will fly to it and tile it, so it must render
   * hidden and reveal only when its mosaic is complete. Consulted by the bar
   * renderer (boxPlot and violin render through it).
   *
   * @param {number|string} realIndex
   * @param {number|string} j
   * @returns {boolean}
   */
  claimsTargetMark(realIndex, j) {
    return !!(this._snapshot && this._snapshot.pieceIn && this._snapshot.mapping.has(`${realIndex}:${j}`));
  }
  /**
   * Create the overlay group the pieces are driven in. It lives INSIDE the
   * new chart's elGraphical so every coordinate matches the marks' own local
   * space, and it never takes a pointer event.
   * @returns {any} the <g> node, or null
   */
  _makePieceLayer() {
    var _a, _b;
    const graph = (_a = this.w.dom) == null ? void 0 : _a.elGraphical;
    const host = graph && graph.node;
    if (!host || typeof host.appendChild !== "function") return null;
    const g2 = BrowserAPIs.createElementNS("http://www.w3.org/2000/svg", "g");
    if (!g2) return null;
    g2.setAttribute("class", "apexcharts-morph-pieces");
    g2.setAttribute("pointer-events", "none");
    const cuid = (_b = this.w.globals) == null ? void 0 : _b.cuid;
    if (cuid) g2.setAttribute("clip-path", `url(#gridRectBarMask${cuid})`);
    host.appendChild(g2);
    this._pieceLayer = g2;
    return g2;
  }
  /**
   * Reveal everything a piece takeover hid, whether or not the pieces ran.
   * The attribute is plain (no namespace colon) so it stays selectable
   * everywhere.
   */
  _revealPieceHidden() {
    var _a;
    const baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
    if (!baseEl || typeof baseEl.querySelectorAll !== "function") return;
    baseEl.querySelectorAll("[data-piece-hidden]").forEach(
      (el) => {
        el.removeAttribute("opacity");
        el.removeAttribute("data-piece-hidden");
      }
    );
  }
  /** Stop the piece run, drop the overlay, and reveal anything still hidden. */
  _cancelPieces() {
    if (this._pieceCancel) {
      this._pieceCancel();
      this._pieceCancel = null;
    }
    const layer = this._pieceLayer;
    this._pieceLayer = null;
    if (layer && layer.parentNode) layer.parentNode.removeChild(layer);
    this._revealPieceHidden();
  }
  /**
   * Whether this environment can hit-test path geometry at all. Decided
   * before the ghost clone is taken, because a family whose cells are only
   * honest when probed (radial) must keep the fade rather than fall back to a
   * rectangular grid it cannot justify. jsdom answers no.
   * @returns {boolean}
   */
  _canProbePaths() {
    if (!Environment.isBrowser()) return false;
    const probe = BrowserAPIs.createElementNS(
      "http://www.w3.org/2000/svg",
      "path"
    );
    return !!probe && typeof /** @type {any} */
    probe.isPointInFill === "function";
  }
  /**
   * A per-band ink prober over a mark's path, for gridDivideShape: given a
   * major-axis band it measures where the mark actually has ink across the
   * minor axis, so a violin's cells follow its density outline, a boxPlot's
   * whisker rows collapse to slivers, and a wedge's rows stop at the wedge
   * instead of spanning the bounding box (which stamped a rectangle over the
   * mark at frame one).
   *
   * The probe path is mounted (hidden) inside the piece layer so its user
   * space is exactly the space the cells are laid out in. The stroke test
   * catches zero-area subpaths (a boxPlot's whisker line has no fill to
   * hit), and its width is what a whisker's slivers will measure.
   *
   * Returns null when the environment cannot hit-test path geometry (jsdom);
   * the caller then keeps the plain grid.
   *
   * @param {string} d - path data, in piece-layer coordinates
   * @param {{x:number,y:number,width:number,height:number}} bbox
   * @param {any} layer - the mounted piece layer
   * @returns {{ intervalsAt: (bandLo: number, bandHi: number, horizontal: boolean) => Array<[number, number]> | null, dispose: () => void } | null}
   */
  _makeExtentProber(d, bbox, layer) {
    const doc = layer.ownerDocument;
    const probe = doc.createElementNS("http://www.w3.org/2000/svg", "path");
    probe.setAttribute("d", d);
    probe.setAttribute("fill", "#000");
    probe.setAttribute("stroke", "#000");
    probe.setAttribute("stroke-width", "3");
    probe.setAttribute("visibility", "hidden");
    layer.appendChild(probe);
    const svg = probe.ownerSVGElement;
    if (typeof /** @type {any} */
    probe.isPointInFill !== "function" || !svg || typeof svg.createSVGPoint !== "function") {
      layer.removeChild(probe);
      return null;
    }
    const pt = svg.createSVGPoint();
    const hit = (x, y) => {
      pt.x = x;
      pt.y = y;
      const p2 = (
        /** @type {any} */
        probe
      );
      return p2.isPointInFill(pt) || typeof p2.isPointInStroke === "function" && p2.isPointInStroke(pt);
    };
    const SCAN = 48;
    const intervalsAt = (bandLo, bandHi, horizontal) => {
      const lo = horizontal ? bbox.y : bbox.x;
      const hi = lo + (horizontal ? bbox.height : bbox.width);
      if (!(hi > lo)) return null;
      const at = (v2, major) => horizontal ? hit(major, v2) : hit(v2, major);
      const majors = [
        bandLo + (bandHi - bandLo) * 0.1,
        (bandLo + bandHi) / 2,
        bandHi - (bandHi - bandLo) * 0.1
      ];
      const edge = (inside, outside, major) => {
        let a2 = outside;
        let b2 = inside;
        for (let it = 0; it < 6; it++) {
          const m2 = (a2 + b2) / 2;
          if (at(m2, major)) b2 = m2;
          else a2 = m2;
        }
        return (a2 + b2) / 2;
      };
      const step = (hi - lo) / SCAN;
      const proof = new Array(SCAN + 1).fill(null);
      let any = false;
      for (const major of majors) {
        for (let s2 = 0; s2 <= SCAN; s2++) {
          if (proof[s2] !== null) continue;
          if (at(lo + s2 * step, major)) {
            proof[s2] = major;
            any = true;
          }
        }
      }
      if (!any) return null;
      const out = [];
      let runStart = -1;
      for (let s2 = 0; s2 <= SCAN + 1; s2++) {
        const inside = s2 <= SCAN && proof[s2] !== null;
        if (inside && runStart < 0) runStart = s2;
        if (!inside && runStart >= 0) {
          const last = s2 - 1;
          const major = (
            /** @type {number} */
            proof[runStart]
          );
          const left = runStart === 0 ? lo : edge(lo + runStart * step, lo + (runStart - 1) * step, major);
          const right = last === SCAN ? hi : edge(
            lo + last * step,
            lo + (last + 1) * step,
            /** @type {number} */
            proof[last]
          );
          if (right > left) out.push([left, right]);
          runStart = -1;
        }
      }
      return out.length ? out : null;
    };
    return {
      intervalsAt,
      dispose: () => {
        if (probe.parentNode) probe.parentNode.removeChild(probe);
      }
    };
  }
  /**
   * mark -> objects. Cut each captured mark into one cell per dot and fly
   * every cell to its dot, corners rounding off and fill blending on the way.
   * The real dots (rendered hidden by the unit chart, see usesPieceTakeover)
   * are revealed one by one as their piece lands, so the handoff is
   * geometrically exact and nothing ever fades.
   */
  _separatePieces() {
    var _a;
    const snap = this._snapshot;
    const baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
    if (!snap || !baseEl) return this._revealPieceHidden();
    const byCluster = /* @__PURE__ */ new Map();
    let total = 0;
    baseEl.querySelectorAll(".apexcharts-unit-area").forEach((dot) => {
      var _a2, _b, _c, _d, _e, _f, _g;
      const i2 = parseInt((_a2 = dot.getAttribute("i")) != null ? _a2 : "", 10);
      if (isNaN(i2)) return;
      const cxAttr = dot.getAttribute("cx");
      let x;
      let y;
      let r2 = 3;
      if (cxAttr != null) {
        x = parseFloat(cxAttr);
        y = parseFloat((_b = dot.getAttribute("cy")) != null ? _b : "");
        r2 = parseFloat((_c = dot.getAttribute("r")) != null ? _c : "3") || 3;
      } else {
        const wAttr = parseFloat((_d = dot.getAttribute("width")) != null ? _d : "0") || 0;
        const hAttr = parseFloat((_e = dot.getAttribute("height")) != null ? _e : "0") || 0;
        x = parseFloat((_f = dot.getAttribute("x")) != null ? _f : "") + wAttr / 2;
        y = parseFloat((_g = dot.getAttribute("y")) != null ? _g : "") + hAttr / 2;
        r2 = Math.max(wAttr, hAttr) / 2 || 3;
      }
      if (!isFinite(x) || !isFinite(y)) return;
      let list = byCluster.get(i2);
      if (!list) {
        list = [];
        byCluster.set(i2, list);
      }
      list.push({ el: dot, x, y, r: r2, fill: dot.getAttribute("fill") });
      total++;
    });
    if (total === 0 || total > PIECE_BUDGET) return this._revealPieceHidden();
    const layer = this._makePieceLayer();
    if (!layer) return this._revealPieceHidden();
    const pieces = [];
    const doc = layer.ownerDocument;
    const sourceFam = familyOf(snap.fromType);
    const shapedSource = sourceFam === "summary" || sourceFam === "radial";
    Array.from(byCluster.keys()).sort((a2, b2) => a2 - b2).forEach((i2) => {
      var _a2;
      const dots = (
        /** @type {any[]} */
        byCluster.get(i2)
      );
      const box = this.getInitialBBoxFor(i2);
      const entry = snap.mapping.get(`${i2}:0`);
      if (!box || !entry) {
        dots.forEach((d) => {
          d.el.removeAttribute("opacity");
          d.el.removeAttribute("data-piece-hidden");
        });
        return;
      }
      const markFill = entry.fill && entry.fill.indexOf("url(") !== 0 ? entry.fill : ((_a2 = this.w.globals.colors) == null ? void 0 : _a2[i2]) || dots[0].fill;
      let prober = null;
      if (shapedSource) {
        const shifted = this.getInitialPathFor(i2, 0);
        if (shifted) prober = this._makeExtentProber(shifted, box, layer);
      }
      const divided = prober ? gridDivideShape(box, dots.length, prober.intervalsAt) : gridDivideRect(box, dots.length);
      if (prober) prober.dispose();
      const cells = sortByHilbert(divided, (c2) => [
        c2.x + c2.width / 2,
        c2.y + c2.height / 2
      ]);
      const ordered = sortByHilbert(dots, (d) => [d.x, d.y]);
      for (let k = 0; k < ordered.length; k++) {
        const cell = cells[k];
        const dot = ordered[k];
        const el = doc.createElementNS("http://www.w3.org/2000/svg", "rect");
        el.setAttribute("data-i", String(i2));
        el.setAttribute("x", String(cell.x));
        el.setAttribute("y", String(cell.y));
        el.setAttribute("width", String(cell.width));
        el.setAttribute("height", String(cell.height));
        el.setAttribute("rx", "0");
        el.setAttribute("fill", String(markFill));
        layer.appendChild(el);
        pieces.push({
          el,
          from: { x: cell.x, y: cell.y, width: cell.width, height: cell.height, rx: 0 },
          to: {
            x: dot.x - dot.r,
            y: dot.y - dot.r,
            width: dot.r * 2,
            height: dot.r * 2,
            rx: dot.r
          },
          fill: makeColorLerp(markFill, dot.fill),
          fillEnd: dot.fill,
          delay: 0,
          meta: { dotEl: dot.el }
        });
      }
    });
    if (!pieces.length) return this._cancelPieces();
    this._runPieces(pieces, (piece) => {
      const dotEl = piece.meta.dotEl;
      dotEl.removeAttribute("opacity");
      dotEl.removeAttribute("data-piece-hidden");
      if (piece.el.parentNode) piece.el.parentNode.removeChild(piece.el);
    });
  }
  /**
   * objects -> mark. Each captured outgoing dot flies to one cell of the
   * incoming mark, squaring off and blending towards the mark's fill; the
   * mark itself (rendered hidden, see claimsTargetMark) is revealed the
   * moment its last piece lands and the mosaic is complete, which is also the
   * moment the seams disappear.
   */
  _combinePieces() {
    var _a, _b;
    const snap = this._snapshot;
    const baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
    if (!snap || !snap.sourceDots || !snap.keyOrder || !baseEl) {
      return this._revealPieceHidden();
    }
    const targets = this._collectTargetMarks(snap.toType);
    if (!targets.size) return this._revealPieceHidden();
    const dx = snap.oldLayout.translateX - (this.w.layout.translateX || 0);
    const dy = snap.oldLayout.translateY - (this.w.layout.translateY || 0);
    const clusterIdx = Array.from(snap.sourceDots.keys()).sort((a2, b2) => a2 - b2);
    const layer = this._makePieceLayer();
    if (!layer) return this._revealPieceHidden();
    const doc = layer.ownerDocument;
    const pieces = [];
    const targetFam = familyOf(snap.toType);
    const shapedTarget = targetFam === "summary" || targetFam === "radial";
    for (let k = 0; k < clusterIdx.length; k++) {
      const dots = (
        /** @type {any[]} */
        snap.sourceDots.get(clusterIdx[k])
      );
      const key = snap.keyOrder[k];
      const target = key ? targets.get(key) : null;
      if (!target || !dots || !dots.length) {
        if (target) {
          target.els.forEach((el) => {
            el.removeAttribute("opacity");
            el.removeAttribute("data-piece-hidden");
          });
        }
        continue;
      }
      const markFill = target.fill && target.fill.indexOf("url(") !== 0 ? target.fill : ((_b = this.w.globals.colors) == null ? void 0 : _b[target.realIndex]) || dots[0].fill;
      let prober = null;
      if (shapedTarget) {
        const d = target.d || target.els.map(
          (p2) => p2.getAttribute("pathTo") || p2.getAttribute("d")
        ).filter(Boolean).join(" ");
        if (d) prober = this._makeExtentProber(d, target.bbox, layer);
      }
      const divided = prober ? gridDivideShape(target.bbox, dots.length, prober.intervalsAt) : gridDivideRect(target.bbox, dots.length);
      if (prober) prober.dispose();
      const cells = sortByHilbert(divided, (c2) => [
        c2.x + c2.width / 2,
        c2.y + c2.height / 2
      ]);
      const ordered = sortByHilbert(dots, (d) => [d.x, d.y]);
      const markState = { remaining: ordered.length, els: target.els, tiles: (
        /** @type {any[]} */
        []
      ) };
      for (let m2 = 0; m2 < ordered.length; m2++) {
        const dot = ordered[m2];
        const cell = cells[m2];
        const el = doc.createElementNS("http://www.w3.org/2000/svg", "rect");
        const fx = dot.x + dx - dot.r;
        const fy = dot.y + dy - dot.r;
        el.setAttribute("data-key", key);
        el.setAttribute("x", String(fx));
        el.setAttribute("y", String(fy));
        el.setAttribute("width", String(dot.r * 2));
        el.setAttribute("height", String(dot.r * 2));
        el.setAttribute("rx", String(dot.r));
        el.setAttribute("fill", String(dot.fill || markFill));
        layer.appendChild(el);
        markState.tiles.push(el);
        pieces.push({
          el,
          from: { x: fx, y: fy, width: dot.r * 2, height: dot.r * 2, rx: dot.r },
          to: { x: cell.x, y: cell.y, width: cell.width, height: cell.height, rx: 0 },
          fill: makeColorLerp(dot.fill, markFill),
          fillEnd: String(markFill),
          delay: 0,
          meta: { markState }
        });
      }
    }
    if (!pieces.length) return this._cancelPieces();
    this._runPieces(pieces, (piece) => {
      const state = piece.meta.markState;
      state.remaining--;
      if (state.remaining === 0) {
        state.els.forEach((el) => {
          el.removeAttribute("opacity");
          el.removeAttribute("data-piece-hidden");
        });
        state.tiles.forEach((t2) => {
          if (t2.parentNode) t2.parentNode.removeChild(t2);
        });
      }
    });
  }
  /**
   * Stagger and start a piece run. Delays sweep the (already spatially
   * sorted) list front to back, and the last piece still lands within the
   * configured morph speed.
   *
   * @param {import('./MorphPieces').Piece[]} pieces
   * @param {(piece: import('./MorphPieces').Piece) => void} onPieceDone
   */
  _runPieces(pieces, onPieceDone) {
    const speed = this.getSpeed();
    const stagger = Math.min(PIECE_STAGGER_MAX, speed * 0.35);
    const flight = Math.max(180, speed - stagger);
    for (let k = 0; k < pieces.length; k++) {
      pieces[k].delay = pieces.length > 1 ? k / (pieces.length - 1) * stagger : 0;
    }
    this._pieceCancel = runPieceTween({
      pieces,
      duration: flight,
      onPieceDone,
      onAllDone: () => {
        this._pieceCancel = null;
        this._cancelPieces();
      }
    });
  }
  /**
   * The incoming chart's marks, read live: every `path[pathTo]` grouped into
   * one mark per (realIndex, j), with the union bbox of its final geometry.
   * Bar marks are one path each; summary marks (boxPlot, violin) may be
   * several, walked exactly like the capture branch walks the outgoing ones.
   *
   * @param {string} toType
   * @returns {Map<string, { realIndex: number, j: number, bbox: {x:number,y:number,width:number,height:number}, fill: string|null, els: any[], d?: string }>}
   */
  _collectTargetMarks(toType) {
    var _a;
    const baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
    const out = /* @__PURE__ */ new Map();
    if (!baseEl) return out;
    const fam = familyOf(toType);
    if (fam === "radial") {
      baseEl.querySelectorAll(".apexcharts-pie-series .apexcharts-pie-area").forEach((p2, i2) => {
        const d = p2.getAttribute("data:pathFinal") || p2.getAttribute("d");
        if (!d || !d.trim()) return;
        const box = this._pathBBox(d);
        if (!box) return;
        out.set(`${i2}:0`, {
          realIndex: i2,
          j: 0,
          d,
          bbox: {
            x: box.minX,
            y: box.minY,
            width: box.maxX - box.minX,
            height: box.maxY - box.minY
          },
          fill: p2.getAttribute("fill"),
          els: [p2]
        });
      });
      return out;
    }
    const wrapClass = fam === "summary" ? `.apexcharts-${toType}-series` : ".apexcharts-bar-series";
    baseEl.querySelectorAll(`${wrapClass} .apexcharts-series`).forEach((group) => {
      var _a2;
      const realIndex = parseInt((_a2 = group.getAttribute("data:realIndex")) != null ? _a2 : "0", 10) || 0;
      let order = 0;
      group.querySelectorAll("path[pathTo]").forEach((p2) => {
        var _a3;
        const d = p2.getAttribute("pathTo") || p2.getAttribute("d");
        if (!d || !d.trim()) return;
        const jAttr = parseInt((_a3 = p2.getAttribute("j")) != null ? _a3 : "", 10);
        const j = isNaN(jAttr) ? order++ : jAttr;
        const box = this._pathBBox(d);
        if (!box) return;
        const key = `${realIndex}:${j}`;
        const prev = out.get(key);
        if (prev) {
          prev.els.push(p2);
          prev.bbox = {
            x: Math.min(prev.bbox.x, box.minX),
            y: Math.min(prev.bbox.y, box.minY),
            width: Math.max(prev.bbox.x + prev.bbox.width, box.maxX) - Math.min(prev.bbox.x, box.minX),
            height: Math.max(prev.bbox.y + prev.bbox.height, box.maxY) - Math.min(prev.bbox.y, box.minY)
          };
        } else {
          out.set(key, {
            realIndex,
            j,
            bbox: {
              x: box.minX,
              y: box.minY,
              width: box.maxX - box.minX,
              height: box.maxY - box.minY
            },
            fill: p2.getAttribute("fill"),
            els: [p2]
          });
        }
      });
    });
    return out;
  }
  /**
   * Walk the outgoing chart's DOM and collect path `d` strings keyed by
   * (realIndex, j). The selectors are scoped to the chart family — bar
   * elements have `pathTo` set; pie/radial elements use their final `d`.
   *
   * @param {string} fromType
   * @returns {{ marks: Array<{ realIndex: number, j: number, d: string, fill: string|null, key?: string|null }>, branches: Array<{ key: string, d: string, fill: string|null }>, unitDots: Map<number, Array<{x:number,y:number,r:number,fill:string|null}>> }}
   */
  _captureFromDOM(fromType) {
    var _a;
    const baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
    if (!baseEl) return { marks: [], branches: [], unitDots: /* @__PURE__ */ new Map() };
    const captured = [];
    const branches = [];
    const unitDots = /* @__PURE__ */ new Map();
    const fam = familyOf(fromType);
    if (fam === "bar") {
      const seriesNodes = baseEl.querySelectorAll(
        ".apexcharts-bar-series .apexcharts-series"
      );
      seriesNodes.forEach((seriesNode) => {
        var _a2;
        const realIndex = parseInt(
          (_a2 = seriesNode.getAttribute("data:realIndex")) != null ? _a2 : "0",
          10
        );
        const paths = seriesNode.querySelectorAll("path[pathTo]");
        paths.forEach((p2, j) => {
          const d = p2.getAttribute("pathTo") || p2.getAttribute("d");
          if (!d) return;
          captured.push({
            realIndex,
            j,
            d,
            fill: p2.getAttribute("fill")
          });
        });
      });
    } else if (fam === "summary") {
      const byMark = /* @__PURE__ */ new Map();
      baseEl.querySelectorAll(`.apexcharts-${fromType}-area`).forEach((p2) => {
        var _a2, _b;
        const j = parseInt((_a2 = p2.getAttribute("j")) != null ? _a2 : "", 10);
        if (isNaN(j)) return;
        const d = p2.getAttribute("pathTo") || p2.getAttribute("d");
        if (!d || !d.trim()) return;
        const group = typeof p2.closest === "function" ? p2.closest(".apexcharts-series") : null;
        const realIndex = parseInt((_b = group == null ? void 0 : group.getAttribute("data:realIndex")) != null ? _b : "0", 10) || 0;
        const key = `${realIndex}:${j}`;
        const prev = byMark.get(key);
        if (prev) prev.d += ` ${d}`;
        else byMark.set(key, { realIndex, j, d, fill: p2.getAttribute("fill") });
      });
      Array.from(byMark.values()).sort((a2, b2) => a2.realIndex - b2.realIndex || a2.j - b2.j).forEach((m2) => captured.push(m2));
    } else if (fam === "partition") {
      if (fromType === "treemap") {
        const rectPath = (el) => {
          var _a2, _b, _c, _d;
          const x = parseFloat((_a2 = el.getAttribute("x")) != null ? _a2 : "");
          const y = parseFloat((_b = el.getAttribute("y")) != null ? _b : "");
          const width = parseFloat((_c = el.getAttribute("width")) != null ? _c : "");
          const height = parseFloat((_d = el.getAttribute("height")) != null ? _d : "");
          if (![x, y, width, height].every((v2) => isFinite(v2))) return null;
          return `M ${x} ${y} L ${x + width} ${y} L ${x + width} ${y + height} L ${x} ${y + height} Z`;
        };
        const tiles = baseEl.querySelectorAll(".apexcharts-treemap-rect");
        tiles.forEach((t2) => {
          var _a2, _b;
          const d = rectPath(t2);
          if (!d) return;
          captured.push({
            realIndex: parseInt((_a2 = t2.getAttribute("i")) != null ? _a2 : "0", 10) || 0,
            j: parseInt((_b = t2.getAttribute("j")) != null ? _b : "0", 10) || 0,
            d,
            fill: t2.getAttribute("fill"),
            key: t2.getAttribute("data:key") || null
          });
        });
        const containers = baseEl.querySelectorAll(
          ".apexcharts-treemap-parent-rect"
        );
        containers.forEach((c2) => {
          const d = rectPath(c2);
          const key = c2.getAttribute("data:key");
          if (!d || !key) return;
          branches.push({ key, d, fill: c2.getAttribute("fill") });
        });
      } else {
        const arcs = baseEl.querySelectorAll(".apexcharts-sunburst-arc");
        const leaves = [];
        arcs.forEach((a2) => {
          if (a2.getAttribute("data:leaf") === "true") leaves.push(a2);
        });
        const source = leaves.length ? leaves : Array.from(arcs);
        source.forEach((a2, i2) => {
          const d = a2.getAttribute("d");
          if (!d || !d.trim()) return;
          captured.push({
            realIndex: i2,
            j: 0,
            d,
            fill: a2.getAttribute("fill"),
            key: a2.getAttribute("data:key") || null
          });
        });
        arcs.forEach((a2) => {
          if (a2.getAttribute("data:leaf") === "true") return;
          const d = a2.getAttribute("d");
          const key = a2.getAttribute("data:key");
          if (!d || !d.trim() || !key) return;
          branches.push({ key, d, fill: a2.getAttribute("fill") });
        });
      }
    } else if (fam === "unit") {
      const dots = baseEl.querySelectorAll(".apexcharts-unit-area");
      const boxes = /* @__PURE__ */ new Map();
      dots.forEach((dot) => {
        var _a2, _b, _c, _d, _e, _f, _g;
        const i2 = parseInt((_a2 = dot.getAttribute("i")) != null ? _a2 : "", 10);
        if (isNaN(i2)) return;
        const cxAttr = dot.getAttribute("cx");
        let x;
        let y;
        let r2 = 3;
        if (cxAttr != null) {
          x = parseFloat(cxAttr);
          y = parseFloat((_b = dot.getAttribute("cy")) != null ? _b : "");
          r2 = parseFloat((_c = dot.getAttribute("r")) != null ? _c : "3") || 3;
        } else {
          const wAttr = parseFloat((_d = dot.getAttribute("width")) != null ? _d : "0") || 0;
          const hAttr = parseFloat((_e = dot.getAttribute("height")) != null ? _e : "0") || 0;
          x = parseFloat((_f = dot.getAttribute("x")) != null ? _f : "") + wAttr / 2;
          y = parseFloat((_g = dot.getAttribute("y")) != null ? _g : "") + hAttr / 2;
          r2 = Math.max(wAttr, hAttr) / 2 || 3;
        }
        if (!isFinite(x) || !isFinite(y)) return;
        let list = unitDots.get(i2);
        if (!list) {
          list = [];
          unitDots.set(i2, list);
        }
        list.push({ x, y, r: r2, fill: dot.getAttribute("fill") });
        const box = boxes.get(i2);
        if (!box) {
          boxes.set(i2, {
            minX: x,
            minY: y,
            maxX: x,
            maxY: y,
            fill: dot.getAttribute("fill")
          });
          return;
        }
        if (x < box.minX) box.minX = x;
        if (x > box.maxX) box.maxX = x;
        if (y < box.minY) box.minY = y;
        if (y > box.maxY) box.maxY = y;
      });
      Array.from(boxes.keys()).sort((a2, b2) => a2 - b2).forEach((i2) => {
        const b2 = (
          /** @type {any} */
          boxes.get(i2)
        );
        const pad = 2;
        const x1 = b2.minX - pad;
        const y1 = b2.minY - pad;
        const x2 = b2.maxX + pad;
        const y2 = b2.maxY + pad;
        captured.push({
          realIndex: i2,
          j: 0,
          d: `M ${x1} ${y1} L ${x2} ${y1} L ${x2} ${y2} L ${x1} ${y2} Z`,
          fill: b2.fill
        });
      });
    } else if (fam === "radial") {
      if (fromType === "radialBar" || fromType === "gauge") {
        const centerX = this.w.layout.gridWidth / 2;
        const centerY = Math.min(this.w.layout.gridWidth, this.w.layout.gridHeight) / 2;
        const rings = baseEl.querySelectorAll(
          ".apexcharts-radial-series .apexcharts-radialbar-area"
        );
        rings.forEach((p2) => {
          var _a2;
          const parent = (
            /** @type {Element|null} */
            p2.parentElement
          );
          const realIndex = parseInt(
            (_a2 = parent == null ? void 0 : parent.getAttribute("data:realIndex")) != null ? _a2 : "0",
            10
          );
          const rawD = p2.getAttribute("d");
          if (!rawD) return;
          const strokeWidth = parseFloat(p2.getAttribute("stroke-width") || "0");
          const d = strokeWidth > 1 ? this._radialArcToFilledSegment(
            rawD,
            strokeWidth,
            centerX,
            centerY
          ) || rawD : rawD;
          captured.push({
            realIndex,
            j: 0,
            d,
            fill: p2.getAttribute("stroke")
          });
        });
      } else {
        const slices = baseEl.querySelectorAll(
          ".apexcharts-pie-series .apexcharts-pie-area"
        );
        slices.forEach(
          (p2, i2) => {
            const d = p2.getAttribute("d");
            if (!d) return;
            captured.push({
              realIndex: i2,
              j: 0,
              d,
              fill: p2.getAttribute("fill")
            });
          }
        );
      }
    }
    return { marks: captured, branches, unitDots };
  }
  /**
   * Convert a radialBar's stroked open-arc `d` ("M x1 y1 A r r 0 large sweep
   * x2 y2") into a closed donut-segment polygon whose FILLED rendering
   * visually matches the original stroked arc — needed because the morph
   * target (pie/donut/polarArea) renders by fill, not stroke. Returns null
   * if the input doesn't match the expected M-then-A shape.
   *
   * @param {string} rawD
   * @param {number} strokeWidth
   * @param {number} centerX
   * @param {number} centerY
   * @returns {string | null}
   */
  _radialArcToFilledSegment(rawD, strokeWidth, centerX, centerY) {
    const m2 = rawD.match(
      /M\s*(-?[\d.]+)\s+(-?[\d.]+)\s+A\s*(-?[\d.]+)\s+(?:-?[\d.]+)\s+(?:-?[\d.]+)\s+(\d)\s+(\d)\s+(-?[\d.]+)\s+(-?[\d.]+)/
    );
    if (!m2) return null;
    const x1 = parseFloat(m2[1]);
    const y1 = parseFloat(m2[2]);
    const r2 = parseFloat(m2[3]);
    const large = parseInt(m2[4], 10);
    const sweep = parseInt(m2[5], 10);
    const x2 = parseFloat(m2[6]);
    const y2 = parseFloat(m2[7]);
    if (!isFinite(r2) || r2 <= 0) return null;
    const half = strokeWidth / 2;
    const rOuter = r2 + half;
    const rInner = Math.max(0, r2 - half);
    const proj = (px, py, newR) => {
      const dx = px - centerX;
      const dy = py - centerY;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist === 0) return { x: centerX, y: centerY };
      const k = newR / dist;
      return { x: centerX + dx * k, y: centerY + dy * k };
    };
    const o1 = proj(x1, y1, rOuter);
    const o2 = proj(x2, y2, rOuter);
    const i1 = proj(x1, y1, rInner);
    const i2 = proj(x2, y2, rInner);
    const sweepBack = sweep ? 0 : 1;
    return `M ${o1.x} ${o1.y} A ${rOuter} ${rOuter} 0 ${large} ${sweep} ${o2.x} ${o2.y} L ${i2.x} ${i2.y} A ${rInner} ${rInner} 0 ${large} ${sweepBack} ${i1.x} ${i1.y} Z`;
  }
  /**
   * Build a closed donut-segment path for the given polar arc geometry. Used
   * by Radial.drawArcs when morphing FROM a filled wedge (pie/donut/polarArea)
   * TO a radialBar arc: the final radialBar is rendered as a stroked open arc,
   * but during the morph we tween d toward this closed-segment form (which
   * looks identical to the stroked arc when filled with the same color) so
   * the in-between frames remain visually consistent filled shapes rather
   * than a thick-outlined wedge.
   *
   * @param {number} centerX
   * @param {number} centerY
   * @param {number} ringRadius - centerline radius of the radialBar ring
   * @param {number} strokeWidth - the ring's stroke thickness
   * @param {number} startAngleDeg - in degrees, 0° = top (12 o'clock)
   * @param {number} endAngleDeg
   * @returns {string}
   */
  buildRingSegmentPath(centerX, centerY, ringRadius, strokeWidth, startAngleDeg, endAngleDeg) {
    const halfStroke = strokeWidth / 2;
    const rOuter = ringRadius + halfStroke;
    const rInner = Math.max(0, ringRadius - halfStroke);
    const sRad = (startAngleDeg - 90) * Math.PI / 180;
    const eRad = (endAngleDeg - 90) * Math.PI / 180;
    const oStart = {
      x: centerX + rOuter * Math.cos(sRad),
      y: centerY + rOuter * Math.sin(sRad)
    };
    const oEnd = {
      x: centerX + rOuter * Math.cos(eRad),
      y: centerY + rOuter * Math.sin(eRad)
    };
    const iStart = {
      x: centerX + rInner * Math.cos(sRad),
      y: centerY + rInner * Math.sin(sRad)
    };
    const iEnd = {
      x: centerX + rInner * Math.cos(eRad),
      y: centerY + rInner * Math.sin(eRad)
    };
    const sweep = endAngleDeg > startAngleDeg ? 1 : 0;
    const large = Math.abs(endAngleDeg - startAngleDeg) > 180 ? 1 : 0;
    return `M ${oStart.x} ${oStart.y} A ${rOuter} ${rOuter} 0 ${large} ${sweep} ${oEnd.x} ${oEnd.y} L ${iEnd.x} ${iEnd.y} A ${rInner} ${rInner} 0 ${large} ${1 - sweep} ${iStart.x} ${iStart.y} Z`;
  }
  /**
   * @returns {string | null} the chart-type the active snapshot was captured
   *   from, or null when no morph is in flight.
   */
  getFromType() {
    return this._snapshot ? this._snapshot.fromType : null;
  }
  /**
   * Build a (targetKey → captured) map. The targetKey matches the lookup
   * pattern each chart-type renderer uses when it asks
   * `getInitialPathFor(realIndex, j)`.
   *
   * Strategy: flatten the captured items into a linear sequence (matching the
   * source chart's natural DOM iteration order: series-then-point for bar,
   * ring-by-ring for radial), then walk the target's iteration positions in
   * the same order and pair them up 1:1. This handles every supported shape
   * without per-pair branching:
   *
   *   - bar (1 series, N pts) ↔ radial-family (N items)  → linear[k] ↔ k
   *   - bar (M series, 1 pt)  ↔ radial-family (M items)  → linear[k] ↔ k
   *   - radial-family (N items) ↔ bar (any matching shape) → linear[k] ↔ flat target
   *   - radial-family ↔ radial-family                    → linear[k] ↔ k
   *
   * @param {Array<{ realIndex: number, j: number, d: string, fill: string|null, key?: string|null }>} captured
   * @param {string} _fromType
   * @param {string} toType
   * @param {any} newSeries - the series array being passed to the new chart;
   *   used only to derive the bar target's (realIndex, j) iteration positions.
   * @param {Array<{ key: string, d: string, fill: string|null }>} [branches]
   *   non-leaf marks, for the key-based partition pairing.
   */
  _buildMapping(captured, _fromType, toType, newSeries, branches) {
    const map = /* @__PURE__ */ new Map();
    const tf = familyOf(toType);
    const flat = captured.slice().sort((a2, b2) => a2.realIndex - b2.realIndex || a2.j - b2.j);
    if (tf === "partition" && branches && branches.length) {
      const keyedMarks = flat.filter((c2) => c2.key);
      if (keyedMarks.length === flat.length) {
        keyedMarks.forEach((c2) => {
          map.set(`key:${c2.key}`, { d: c2.d, fill: c2.fill });
        });
        branches.forEach((br) => {
          map.set(`key:${br.key}`, { d: br.d, fill: br.fill });
        });
      }
    }
    if (tf === "radial" || tf === "unit" || tf === "partition") {
      flat.forEach((c2, i2) => {
        map.set(`${i2}:0`, { d: c2.d, fill: c2.fill });
      });
      return map;
    }
    if (tf === "bar" || tf === "summary") {
      const positions = [];
      const series = Array.isArray(newSeries) ? newSeries : [];
      series.forEach((s2, seriesIdx) => {
        const data = s2 && Array.isArray(s2.data) ? s2.data : [];
        for (let j = 0; j < data.length; j++) {
          positions.push({ realIndex: seriesIdx, j });
        }
      });
      flat.forEach((c2, i2) => {
        const pos = positions[i2];
        if (pos) {
          map.set(`${pos.realIndex}:${pos.j}`, { d: c2.d, fill: c2.fill });
        }
      });
      return map;
    }
    return map;
  }
  isActive() {
    return this._snapshot !== null;
  }
  /**
   * @param {number|string} realIndex
   * @param {number|string} j
   * @returns {string | null}
   */
  getInitialPathFor(realIndex, j) {
    if (!this._snapshot) return null;
    const entry = this._snapshot.mapping.get(`${realIndex}:${j}`);
    if (!entry) return null;
    const dx = this._snapshot.oldLayout.translateX - (this.w.layout.translateX || 0);
    const dy = this._snapshot.oldLayout.translateY - (this.w.layout.translateY || 0);
    return dx === 0 && dy === 0 ? entry.d : this._translatePathD(entry.d, dx, dy);
  }
  /**
   * Offset every absolute coordinate in an SVG path `d` by (dx, dy).
   *
   * Assumes the path uses only uppercase (absolute) commands — every path
   * ApexCharts generates does. Relative-command paths would pass through
   * unchanged at the lowercase, which is also semantically correct (deltas
   * don't shift under a parent translate).
   *
   * @param {string} d
   * @param {number} dx
   * @param {number} dy
   * @returns {string}
   */
  _translatePathD(d, dx, dy) {
    if (dx === 0 && dy === 0) return d;
    const commands = parsePath(d);
    return commands.map(
      /** @param {any[]} c */
      (c2) => {
        const cmd = c2[0];
        if (cmd === "Z") return "Z";
        if (cmd === "M" || cmd === "L" || cmd === "T") {
          return `${cmd} ${c2[1] + dx} ${c2[2] + dy}`;
        }
        if (cmd === "H") return `${cmd} ${c2[1] + dx}`;
        if (cmd === "V") return `${cmd} ${c2[1] + dy}`;
        if (cmd === "C") {
          return `${cmd} ${c2[1] + dx} ${c2[2] + dy} ${c2[3] + dx} ${c2[4] + dy} ${c2[5] + dx} ${c2[6] + dy}`;
        }
        if (cmd === "S" || cmd === "Q") {
          return `${cmd} ${c2[1] + dx} ${c2[2] + dy} ${c2[3] + dx} ${c2[4] + dy}`;
        }
        if (cmd === "A") {
          return `${cmd} ${c2[1]} ${c2[2]} ${c2[3]} ${c2[4]} ${c2[5]} ${c2[6] + dx} ${c2[7] + dy}`;
        }
        return c2.join(" ");
      }
    ).join(" ");
  }
  /**
   * The centre point (in the NEW chart's screen space) of the captured shape
   * for cluster `i`. Kept for callers that only need a point; the unit renderer
   * uses getInitialBBoxFor so its dots fill the shape rather than stack on a
   * single point.
   * @param {number} i
   * @returns {{ x: number, y: number } | null}
   */
  getInitialCenterFor(i2) {
    const box = this.getInitialBBoxFor(i2);
    if (!box) return null;
    return { x: box.x + box.width / 2, y: box.y + box.height / 2 };
  }
  /**
   * The `k`-th captured path in draw order, already shifted into the NEW
   * chart's coordinate space.
   *
   * For marks that pair up by position rather than by a (series, point) grid:
   * a treemap's tiles and a sunburst's leaves are each one mark per row, laid
   * out in the same reading order, so the k-th of one becomes the k-th of the
   * other.
   *
   * @param {number} k
   * @returns {string | null}
   */
  getInitialPathAt(k) {
    return this.getInitialPathFor(k, 0);
  }
  /**
   * The captured shape for a branch identity (charts/common/Hierarchy.morphKey),
   * or null when the outgoing chart had no mark for that branch.
   *
   * This is what lets a partition morph pair at every level: a sector, an
   * industry and a company each find the arc or tile that stood for the same
   * branch, instead of leaves pairing by draw order while the containers pop.
   *
   * @param {string} key
   * @returns {string | null}
   */
  getInitialPathForKey(key) {
    if (!this._snapshot || !key) return null;
    return this.getInitialPathFor("key", key);
  }
  /** True when the active snapshot can pair by branch key. */
  hasKeyedMarks() {
    if (!this._snapshot) return false;
    for (const k of this._snapshot.mapping.keys()) {
      if (typeof k === "string" && k.startsWith("key:")) return true;
    }
    return false;
  }
  /**
   * Where the `j`-th of `n` objects in cluster `i` starts, INSIDE the shape it
   * came out of.
   *
   * An aggregate mark stands for a quantity, and its extent is that quantity: a
   * bar of height h representing n units gives its k-th unit the height
   * fraction (k + 0.5)/n. So a bar does not spray its dots from a single point,
   * it comes apart along its own length, bottom-up, and each dot leaves from
   * the part of the bar that was standing for it. The reverse direction reads
   * the same geometry, so explode and collapse are inverses.
   *
   * The distribution follows the captured shape's LONGER axis, which is what
   * makes one function serve both marks: a bar's box is tall and thin, so the
   * dots leave in a column; a wedge's box is squat, so they leave in a row
   * across it.
   *
   * @param {number} i - cluster index
   * @param {number} j - the object's rank within its cluster
   * @param {number} n - objects in the cluster
   * @returns {{ x: number, y: number } | null}
   */
  getInitialSlotFor(i2, j, n2) {
    const box = this.getInitialBBoxFor(i2);
    if (!box) return null;
    const cx = box.x + box.width / 2;
    const cy = box.y + box.height / 2;
    if (!(n2 > 1) || !(j >= 0)) return { x: cx, y: cy };
    const t2 = (Math.min(j, n2 - 1) + 0.5) / n2;
    if (box.height >= box.width) {
      return { x: cx, y: box.y + box.height * (1 - t2) };
    }
    return { x: box.x + box.width * t2, y: cy };
  }
  /**
   * The bounding box (in the NEW chart's screen space) of the captured shape
   * for cluster `i`. `getInitialSlotFor` distributes a cluster's objects across
   * this box as their start positions, so a tall bar visibly breaks apart into
   * a tall column of dots that then swarm into the cluster.
   * @param {number} i
   * @returns {{ x: number, y: number, width: number, height: number } | null}
   */
  getInitialBBoxFor(i2) {
    if (!this._snapshot) return null;
    const entry = this._snapshot.mapping.get(`${i2}:0`);
    if (!entry) return null;
    const box = this._pathBBox(entry.d);
    if (!box) return null;
    const dx = this._snapshot.oldLayout.translateX - (this.w.layout.translateX || 0);
    const dy = this._snapshot.oldLayout.translateY - (this.w.layout.translateY || 0);
    return {
      x: box.minX + dx,
      y: box.minY + dy,
      width: box.maxX - box.minX,
      height: box.maxY - box.minY
    };
  }
  /**
   * Bounding box of an absolute-command SVG path `d`. Good enough as the burst
   * footprint (we only need where the shape sat, not exact geometry).
   * @param {string} d
   * @returns {{ minX:number, minY:number, maxX:number, maxY:number } | null}
   */
  _pathBBox(d) {
    const commands = parsePath(d);
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    let seen = false;
    commands.forEach(
      /** @param {any[]} c */
      (c2) => {
        const cmd = c2[0];
        if (cmd === "Z") return;
        let pairs = [];
        if (cmd === "H") pairs = [[c2[1], (minY + maxY) / 2 || c2[1]]];
        else if (cmd === "V") pairs = [[(minX + maxX) / 2 || c2[1], c2[1]]];
        else if (cmd === "A") pairs = [[c2[6], c2[7]]];
        else {
          for (let k = 1; k + 1 < c2.length; k += 2) pairs.push([c2[k], c2[k + 1]]);
        }
        pairs.forEach(([x, y]) => {
          if (!isFinite(x) || !isFinite(y)) return;
          seen = true;
          if (x < minX) minX = x;
          if (x > maxX) maxX = x;
          if (y < minY) minY = y;
          if (y > maxY) maxY = y;
        });
      }
    );
    if (!seen) return null;
    return { minX, minY, maxX, maxY };
  }
  /**
   * @param {number} realIndex
   * @param {number} j
   * @returns {string | null}
   */
  getInitialFillFor(realIndex, j) {
    if (!this._snapshot) return null;
    const entry = this._snapshot.mapping.get(`${realIndex}:${j}`);
    return entry ? entry.fill : null;
  }
  /** @returns {number} */
  getSpeed() {
    const animCfg = this.w.config.chart.animations;
    return animCfg.chartTypeMorph && animCfg.chartTypeMorph.speed || animCfg.speed || 600;
  }
  /**
   * Fade newly-mounted axes / grid / legend / titles from opacity 0 → 1 in
   * parallel with the morph. Without this the chart's chrome would pop in
   * abruptly while the series elements are still mid-tween, which reads as a
   * jarring layout shift.
   */
  applyChromeFade() {
    var _a;
    if (!this._snapshot || !Environment.isBrowser()) return;
    const baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
    if (!baseEl) return;
    if (this._snapshot.pieceOut) this._separatePieces();
    else if (this._snapshot.pieceIn) this._combinePieces();
    else this._mountGhost();
    const speed = this.getSpeed();
    const chromeSelectors = [
      ".apexcharts-xaxis",
      ".apexcharts-yaxis",
      ".apexcharts-grid",
      ".apexcharts-gridlines-horizontal",
      ".apexcharts-gridlines-vertical",
      ".apexcharts-legend",
      ".apexcharts-title-text",
      ".apexcharts-subtitle-text"
    ];
    chromeSelectors.forEach((sel) => {
      baseEl.querySelectorAll(sel).forEach((el) => {
        if (!el.style) return;
        el.style.opacity = "0";
        el.style.transition = `opacity ${speed}ms ease-out`;
        BrowserAPIs.requestAnimationFrame(() => {
          el.style.opacity = "1";
        });
        setTimeout(() => {
          el.style.transition = "";
          el.style.opacity = "";
        }, speed + 80);
      });
    });
    setTimeout(() => this.cleanup(), speed + 100);
  }
  cleanup() {
    this._snapshot = null;
    this._removeGhost();
    this._cancelPieces();
  }
}
ApexCharts.registerFeatures({ morphTypeChange: MorphTypeChange });
const XHTML$2 = "http://www.w3.org/1999/xhtml";
class Breadcrumb {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   * @param {import('./Drilldown').default} drilldown
   */
  constructor(w2, ctx, drilldown) {
    this.w = w2;
    this.ctx = ctx;
    this.drilldown = drilldown;
  }
  /**
   * @param {Array<string|number>} path - ['root', id, id, ...]
   */
  render(path) {
    if (!Environment.isBrowser()) return;
    const w2 = this.w;
    const elWrap = w2.dom.elWrap;
    if (!elWrap) return;
    const existing = elWrap.querySelector(".apexcharts-breadcrumb");
    if (existing && existing.parentNode) {
      existing.parentNode.removeChild(existing);
    }
    const cfg = w2.config.drilldown && w2.config.drilldown.breadcrumb;
    if (!cfg || cfg.show === false) return;
    if (this.drilldown.depth === 0) return;
    const nav = BrowserAPIs.createElementNS(XHTML$2, "nav");
    nav.setAttribute("class", "apexcharts-breadcrumb");
    nav.setAttribute("aria-label", "Drilldown breadcrumb");
    this._position(nav, cfg);
    const separator = cfg.separator != null ? cfg.separator : " / ";
    path.forEach((id, i2) => {
      if (i2 > 0) {
        const sep = BrowserAPIs.createElementNS(XHTML$2, "span");
        sep.setAttribute("class", "apexcharts-breadcrumb-separator");
        sep.setAttribute("aria-hidden", "true");
        sep.textContent = separator;
        nav.appendChild(sep);
      }
      const label = this._label(id, i2);
      const isCurrent = i2 === path.length - 1;
      if (isCurrent) {
        const cur = BrowserAPIs.createElementNS(XHTML$2, "span");
        cur.setAttribute(
          "class",
          "apexcharts-breadcrumb-item apexcharts-breadcrumb-current"
        );
        cur.setAttribute("aria-current", "page");
        cur.textContent = label;
        nav.appendChild(cur);
      } else {
        const btn = (
          /** @type {HTMLButtonElement} */
          BrowserAPIs.createElementNS(XHTML$2, "button")
        );
        btn.setAttribute("type", "button");
        btn.setAttribute("class", "apexcharts-breadcrumb-item");
        if (i2 === 0) {
          const arrow = BrowserAPIs.createElementNS(XHTML$2, "span");
          arrow.setAttribute("class", "apexcharts-breadcrumb-arrow");
          arrow.setAttribute("aria-hidden", "true");
          arrow.textContent = "←";
          btn.appendChild(arrow);
        }
        const text = BrowserAPIs.createElementNS(XHTML$2, "span");
        text.setAttribute("class", "apexcharts-breadcrumb-label");
        text.textContent = label;
        btn.appendChild(text);
        btn.addEventListener("click", () => this.drilldown.drillToLevel(i2));
        nav.appendChild(btn);
      }
    });
    elWrap.appendChild(nav);
    if (this.w.globals.axisCharts) {
      placeInReservedBand(this.w, this.ctx, nav, cfg);
    }
    this._avoidChromeOverlap(nav);
  }
  /**
   * The breadcrumb is an absolute overlay, so at its default top-left it can
   * sit on top of a left-aligned title (or subtitle). After mounting, push it
   * below any chart chrome it intersects. (Sunburst's self-contained
   * breadcrumb applies the same rule.)
   * @param {HTMLElement} nav
   */
  _avoidChromeOverlap(nav) {
    const w2 = this.w;
    const chrome = (
      /** @type {Element[]} */
      [".apexcharts-title-text", ".apexcharts-subtitle-text"].map((s2) => w2.dom.baseEl.querySelector(s2)).filter((el) => el !== null)
    );
    if (!chrome.length) return;
    const wrapTop = w2.dom.elWrap.getBoundingClientRect().top;
    for (let pass = 0; pass < chrome.length + 1; pass++) {
      const nr = nav.getBoundingClientRect();
      const hit = chrome.find((el) => {
        const r2 = el.getBoundingClientRect();
        return nr.left < r2.right && nr.right > r2.left && nr.top < r2.bottom && nr.bottom > r2.top;
      });
      if (!hit) break;
      nav.style.top = `${hit.getBoundingClientRect().bottom - wrapTop + 4}px`;
    }
  }
  /**
   * @param {string|number} id
   * @param {number} index
   * @returns {string}
   */
  _label(id, index) {
    const cfg = this.w.config.drilldown.breadcrumb;
    let label;
    if (index === 0) {
      label = cfg.rootLabel != null ? cfg.rootLabel : "All";
    } else {
      const list = (this.w.config.drilldown.series || []).find(
        (s2) => s2 && s2.id === id
      );
      label = list && list.name || String(id);
    }
    if (typeof cfg.formatter === "function") {
      return cfg.formatter(label, { index, depth: this.drilldown.depth });
    }
    return label;
  }
  /**
   * @param {HTMLElement} nav
   * @param {Record<string, any>} cfg
   */
  _position(nav, cfg) {
    const ox = cfg.offsetX || 0;
    const oy = cfg.offsetY || 0;
    nav.style.position = "absolute";
    nav.style.top = oy + "px";
    if (cfg.position === "top-right") {
      nav.style.right = -ox + 3 + "px";
    } else {
      nav.style.left = ox + "px";
    }
  }
}
const XHTML$1 = "http://www.w3.org/1999/xhtml";
const CLASS = "apexcharts-drilldown-loading";
class DrilldownLoading {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
    this.el = null;
  }
  /** @returns {any} the drilldown.loading config, normalised. */
  _cfg() {
    const d = this.w.config.drilldown;
    const l2 = d && d.loading;
    if (l2 === false) return { show: false };
    return l2 || {};
  }
  /**
   * Mount the overlay. No-op when disabled, outside a browser, or already up.
   */
  show() {
    if (!Environment.isBrowser()) return;
    const cfg = this._cfg();
    if (cfg.show === false) return;
    const elWrap = this.w.dom.elWrap;
    if (!elWrap) return;
    this.hide();
    const box = BrowserAPIs.createElementNS(XHTML$1, "div");
    box.setAttribute("class", CLASS);
    box.setAttribute("role", "status");
    box.setAttribute("aria-live", "polite");
    box.setAttribute("aria-label", cfg.text || "Loading");
    const spinner = BrowserAPIs.createElementNS(XHTML$1, "div");
    spinner.setAttribute("class", `${CLASS}-spinner`);
    spinner.setAttribute("aria-hidden", "true");
    box.appendChild(spinner);
    if (cfg.text) {
      const label = BrowserAPIs.createElementNS(XHTML$1, "span");
      label.setAttribute("class", `${CLASS}-text`);
      label.textContent = cfg.text;
      box.appendChild(label);
    }
    elWrap.appendChild(box);
    this.el = box;
  }
  /** Remove the overlay. Safe to call when it is not mounted. */
  hide() {
    const elWrap = this.w.dom.elWrap;
    if (elWrap) {
      const nodes = elWrap.querySelectorAll(`.${CLASS}`);
      for (let i2 = 0; i2 < nodes.length; i2++) {
        const n2 = nodes[i2];
        if (n2.parentNode) n2.parentNode.removeChild(n2);
      }
    } else if (this.el && this.el.parentNode) {
      this.el.parentNode.removeChild(this.el);
    }
    this.el = null;
  }
}
const MAX_DEPTH = 32;
const DRILL_MARKER = "__apexDrilldownMarker";
class Drilldown {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.stack = [];
    this.rootSnapshot = null;
    this._wired = false;
    this._asyncCache = /* @__PURE__ */ new Map();
    this._pending = null;
    this._warnedUnreachable = false;
    this._warnedNoSliceOffset = false;
    this.breadcrumb = new Breadcrumb(w2, ctx, this);
    this.loading = new DrilldownLoading(w2);
    this._onPointSelect = this._onPointSelect.bind(this);
    this._afterRender = this._afterRender.bind(this);
    this._onPlotDown = this._onPlotDown.bind(this);
    this._onPlotClick = this._onPlotClick.bind(this);
    this._downAt = null;
    this._plotClickWired = null;
    this.init();
  }
  init() {
    const w2 = this.w;
    if (!w2.config.drilldown || !w2.config.drilldown.enabled) return;
    if (this._wired) return;
    this._wired = true;
    this.ctx.addEventListener("dataPointSelection", this._onPointSelect);
    this.ctx.addEventListener("mounted", this._afterRender);
    this.ctx.addEventListener("updated", this._afterRender);
    if (w2.config.markers) {
      w2.config.markers.discrete = this._drillMarkers(w2.config.series);
    }
  }
  // ─── Observable state ──────────────────────────────────────────────────────
  /** @returns {Array<string|number>} e.g. ['root', '2024-quarters'] */
  get path() {
    return ["root", ...this.stack.map((f) => f.id)];
  }
  /** @returns {number} 0 at root */
  get depth() {
    return this.stack.length;
  }
  // ─── Navigation API ────────────────────────────────────────────────────────
  /**
   * Drill into the child level with the given id.
   * @param {string|number} id
   * @param {any} [triggerPoint] - the clicked data point (for events / async ctx)
   * @param {{ seriesIndex?: number, dataPointIndex?: number }} [meta]
   * @returns {Promise<any>}
   */
  drillDown(id, triggerPoint, meta) {
    const child = this._resolveChild(id);
    if (child) return this._drillInto(child, triggerPoint, meta);
    if (typeof this.w.config.drilldown.onDrillDown === "function") {
      return this._drillDownAsync(id, triggerPoint, meta);
    }
    console.warn(
      `ApexCharts: drilldown id "${id}" not found in chart.drilldown.series, and no onDrillDown resolver is set.`
    );
    return Promise.resolve(this.ctx);
  }
  /**
   * Navigate back one level.
   * @returns {Promise<any>}
   */
  drillUp() {
    return this.drillToLevel(this.stack.length - 1);
  }
  /**
   * Navigate back to the root view.
   * @returns {Promise<any>}
   */
  drillToRoot() {
    return this.drillToLevel(0);
  }
  /**
   * Navigate to an arbitrary depth (0 = root). Used by breadcrumb clicks.
   * @param {number} targetDepth
   * @returns {Promise<any>}
   */
  drillToLevel(targetDepth) {
    const cur = this.stack.length;
    if (targetDepth < 0 || targetDepth >= cur) return Promise.resolve(this.ctx);
    const from = this.path[this.path.length - 1];
    const restore = targetDepth === 0 ? this.rootSnapshot : this.stack[targetDepth].restore;
    this.stack = this.stack.slice(0, targetDepth);
    const to = this.path[this.path.length - 1];
    return this._apply(this._viewFromSnapshot(restore), "up", { from, to });
  }
  // ─── Internals ─────────────────────────────────────────────────────────────
  /**
   * @param {string|number} id
   * @returns {any|null}
   */
  _resolveChild(id) {
    const list = this.w.config.drilldown && this.w.config.drilldown.series;
    if (!Array.isArray(list)) return null;
    return list.find((s2) => s2 && s2.id === id) || null;
  }
  /**
   * @param {any} child
   * @param {any} [triggerPoint]
   * @param {{ seriesIndex?: number, dataPointIndex?: number }} [meta]
   * @returns {Promise<any>}
   */
  _drillInto(child, triggerPoint, meta) {
    if (this.stack.length >= MAX_DEPTH) {
      console.warn(`ApexCharts: drilldown max depth (${MAX_DEPTH}) reached.`);
      return Promise.resolve(this.ctx);
    }
    if (!this.rootSnapshot) this.rootSnapshot = this._snapshot();
    const from = this.path[this.path.length - 1];
    this.stack.push({ id: child.id, name: child.name, restore: this._snapshot() });
    return this._apply(this._viewFromChild(child), "down", {
      from,
      to: child.id,
      point: triggerPoint,
      seriesIndex: meta && meta.seriesIndex,
      dataPointIndex: meta && meta.dataPointIndex
    });
  }
  /**
   * Resolve a level through `onDrillDown` and drill into it.
   *
   * Failure never changes state: on a throw, a rejection, or a resolver that
   * hands back something undrillable, the chart stays exactly where it was and
   * `drillDownError` fires. That is what makes this usable against a real
   * backend, where a fetch failing is ordinary rather than exceptional.
   *
   * @param {string|number|null} id
   * @param {any} point
   * @param {{ seriesIndex?: number, dataPointIndex?: number }} [meta]
   * @returns {Promise<any>}
   */
  _drillDownAsync(id, point, meta) {
    const cfg = this.w.config.drilldown;
    const fn = cfg.onDrillDown;
    const cached = this._cacheGet(id);
    if (cached) return this._drillInto(cached, point, meta);
    if (this._pending) return this._pending;
    let result;
    this.loading.show();
    try {
      result = fn({
        // `id` was missing here, so a resolver could not tell WHICH level was
        // asked for without re-deriving it from the point. It is the first
        // thing a real implementation needs (`fetch('/levels/' + id)`).
        id,
        point,
        seriesIndex: meta && meta.seriesIndex,
        dataPointIndex: meta && meta.dataPointIndex
      });
    } catch (error) {
      this.loading.hide();
      this._fire("drillDownError", { id, error });
      return Promise.resolve(this.ctx);
    }
    const settle = () => {
      this._pending = null;
      this.loading.hide();
    };
    const p2 = Promise.resolve(result).then(
      (child) => {
        settle();
        if (this._isDead()) return this.ctx;
        if (!child || !child.data) {
          this._fire("drillDownError", {
            id,
            error: new Error(
              `drilldown: onDrillDown resolved without a drillable level for id "${id}" (expected an object with a \`data\` array).`
            )
          });
          return this.ctx;
        }
        const level = child.id != null ? child : __spreadProps(__spreadValues({}, child), { id });
        this._cacheSet(id, level);
        return this._drillInto(level, point, meta);
      },
      (error) => {
        settle();
        if (this._isDead()) return this.ctx;
        this._fire("drillDownError", { id, error });
        return this.ctx;
      }
    );
    this._pending = p2;
    return p2;
  }
  /**
   * Whether the chart was torn down while a resolver was in flight.
   *
   * Clicking to drill and then navigating away is ordinary, not exceptional: a
   * component unmounts, `destroy()` runs, and the fetch settles afterwards.
   * Without this the resolved level would be applied to a destroyed chart,
   * which throws out of `updateOptions` and surfaces in the host app as an
   * unhandled rejection from a click the user has already forgotten about.
   *
   * @returns {boolean}
   */
  _isDead() {
    const w2 = this.w;
    return !w2 || !w2.globals || w2.globals.isDestroyed === true;
  }
  /** @returns {boolean} whether resolved async levels are cached. */
  _cacheEnabled() {
    const cfg = this.w.config.drilldown;
    return !!(cfg && cfg.cache !== false);
  }
  /**
   * @param {string|number|null} id
   * @returns {any|null}
   */
  _cacheGet(id) {
    if (!this._cacheEnabled() || id == null) return null;
    return this._asyncCache.get(id) || null;
  }
  /**
   * @param {string|number|null} id
   * @param {any} level
   */
  _cacheSet(id, level) {
    if (!this._cacheEnabled() || id == null) return;
    this._asyncCache.set(id, level);
  }
  /**
   * Drop cached async levels, so the next drill re-runs `onDrillDown`. Call it
   * when the underlying data changes behind a chart that has already drilled.
   * @param {string|number} [id] a single level, or every level when omitted
   * @returns {any} the chart, for chaining
   */
  clearCache(id) {
    if (id == null) this._asyncCache.clear();
    else this._asyncCache.delete(id);
    return this.ctx;
  }
  /**
   * Capture the overridable surface of the current view so it can be restored.
   * Only fields that some drilldown.series entry can change are cloned; series
   * and chart.type/stacked are always captured.
   * @returns {object}
   */
  _snapshot() {
    const c2 = this.w.config;
    const fields = this._overrideFields();
    const snap = { series: this._uncollapseSeries(Utils$1.clone(c2.series)) };
    if (Array.isArray(c2.labels) && c2.labels.length) {
      snap.labels = Utils$1.clone(c2.labels);
    }
    snap.chart = { type: c2.chart.type, stacked: c2.chart.stacked };
    if (fields.has("xaxis")) snap.xaxis = Utils$1.clone(c2.xaxis);
    if (fields.has("yaxis")) snap.yaxis = Utils$1.clone(c2.yaxis);
    if (fields.has("colors")) snap.colors = c2.colors ? Utils$1.clone(c2.colors) : void 0;
    if (fields.has("plotOptions")) snap.plotOptions = Utils$1.clone(c2.plotOptions);
    if (fields.has("fill")) snap.fill = Utils$1.clone(c2.fill);
    if (fields.has("legend")) snap.legend = Utils$1.clone(c2.legend);
    return snap;
  }
  /**
   * Restore any legend-collapsed slices/series to their original values in a
   * cloned series array, so a drill snapshot captures the pre-collapse data.
   * Mirrors legend Helpers' collapse addressing: object-form pie/donut packs
   * every slice as a data point inside `series[0].data`; numeric pie stores a
   * slice per top-level element; axis series carry a `data` array. No-op when
   * nothing is collapsed.
   * @param {any[]} series
   * @returns {any[]}
   */
  _uncollapseSeries(series) {
    const w2 = this.w;
    const gl = w2.globals;
    const entries = [
      ...gl.collapsedSeries || [],
      ...gl.ancillaryCollapsedSeries || []
    ];
    if (!entries.length) return series;
    const type = w2.config.chart.type;
    const objectFormPie = (type === "pie" || type === "donut" || type === "polarArea") && series.length === 1 && series[0] && typeof series[0] === "object" && Array.isArray(series[0].data);
    const container = objectFormPie ? series[0].data : series;
    for (const entry of entries) {
      const i2 = entry.index;
      if (gl.axisCharts) {
        if (series[i2]) {
          series[i2].data = Array.isArray(entry.data) ? entry.data.slice() : entry.data;
        }
      } else if (container[i2] && typeof container[i2] === "object") {
        container[i2].y = entry.data;
      } else if (container[i2] !== void 0) {
        container[i2] = entry.data;
      }
    }
    return series;
  }
  /**
   * Union of overridable fields across all declared drilldown levels. Ensures a
   * deep drillToRoot restores everything any intermediate level may have changed.
   * @returns {Set<string>}
   */
  _overrideFields() {
    const fields = /* @__PURE__ */ new Set();
    const list = this.w.config.drilldown && this.w.config.drilldown.series || [];
    for (const s2 of list) {
      if (!s2) continue;
      if (s2.xaxis) fields.add("xaxis");
      if (s2.yaxis) fields.add("yaxis");
      if (s2.colors) fields.add("colors");
      if (s2.plotOptions) fields.add("plotOptions");
      if (s2.fill) fields.add("fill");
      if (s2.legend) fields.add("legend");
    }
    return fields;
  }
  /**
   * Copy the optional view fields shared by a drilldown child level and a
   * restore snapshot (`xaxis`, `yaxis`, `colors`, `plotOptions`, `fill`,
   * `legend`) from `src` onto `view`, only when present.
   * @param {Record<string, any>} view @param {Record<string, any>} src
   */
  _copyOptionalViewFields(view, src) {
    if (src.xaxis) view.xaxis = src.xaxis;
    if (src.yaxis) view.yaxis = src.yaxis;
    if (src.colors) view.colors = src.colors;
    if (src.plotOptions) view.plotOptions = src.plotOptions;
    if (src.fill) view.fill = src.fill;
    if (src.legend) view.legend = src.legend;
  }
  /**
   * Build an updateOptions/updateSeries payload for drilling INTO a child level.
   * Works for axis charts and pie/donut alike: both accept series objects with a
   * `data` array of `{ x, y }` points (pie derives slice labels from `x`).
   * @param {any} child
   * @returns {Record<string, any>}
   */
  _viewFromChild(child) {
    const view = {};
    if (Array.isArray(child.series)) {
      view.series = child.series;
    } else {
      view.series = [{ name: child.name || "", data: child.data }];
    }
    const chart = {};
    if (child.chart && child.chart.type) chart.type = child.chart.type;
    if (child.chart && child.chart.stacked != null) chart.stacked = child.chart.stacked;
    if (Object.keys(chart).length) view.chart = chart;
    this._copyOptionalViewFields(view, child);
    return view;
  }
  /**
   * Build an updateOptions payload from a restore-snapshot.
   * @param {Record<string, any>} snap
   * @returns {Record<string, any>}
   */
  _viewFromSnapshot(snap) {
    const view = { series: snap.series, chart: snap.chart };
    if (snap.labels && snap.labels.length) view.labels = snap.labels;
    this._copyOptionalViewFields(view, snap);
    return view;
  }
  /**
   * Apply a view by delegating to the right update path, firing drill events
   * around it.
   * @param {Record<string, any>} view
   * @param {'down'|'up'} direction
   * @param {object} meta
   * @returns {Promise<any>}
   */
  _apply(view, direction, meta) {
    const w2 = this.w;
    w2.interact.selectedDataPoints = [];
    w2.globals.collapsedSeries = [];
    w2.globals.collapsedSeriesIndices = [];
    w2.globals.ancillaryCollapsedSeries = [];
    w2.globals.ancillaryCollapsedSeriesIndices = [];
    w2.globals.allSeriesCollapsed = false;
    w2.globals.risingSeries = [];
    view.markers = __spreadProps(__spreadValues({}, view.markers || {}), {
      discrete: this._drillMarkers(view.series)
    });
    const animate = (!w2.config.drilldown.animation || w2.config.drilldown.animation.enabled !== false) && w2.config.chart.animations.enabled !== false;
    if (direction === "down") this._fire("drillDownStart", meta);
    const runUpdate = (anim) => this.ctx.updateOptions(view, false, anim, false, false);
    const done = () => {
      this._fire(direction === "down" ? "drillDownEnd" : "drillUp", meta);
      return this.ctx;
    };
    if (animate && this._zoomEnabled()) {
      const origin = this._triggerOrigin(meta);
      if (origin) {
        return this._zoomDrill(origin, direction, () => runUpdate(false)).then(done);
      }
    }
    return runUpdate(animate).then(done);
  }
  /** @returns {boolean} whether trigger-point zoom is configured on. */
  _zoomEnabled() {
    const a2 = this.w.config.drilldown && this.w.config.drilldown.animation;
    return !!(a2 && a2.zoomFromPoint);
  }
  /** @returns {SVGSVGElement|null} the chart's root <svg> node, if present. */
  _svgNode() {
    const paper = this.w.dom && this.w.dom.Paper;
    return paper && paper.node ? paper.node : null;
  }
  /**
   * The group wrapping ONLY the data marks (bars/cells/tiles) — not the axes,
   * grid, or titles. Animating this keeps the chart frame still while the marks
   * move. Covers bar/line/area (`.apexcharts-plot-series`), heatmap, and treemap.
   * @returns {SVGElement|null}
   */
  _markGroup() {
    const svg = this._svgNode();
    if (!svg || typeof svg.querySelector !== "function") return null;
    return svg.querySelector(
      ".apexcharts-plot-series, .apexcharts-heatmap, .apexcharts-treemap"
    );
  }
  /**
   * Centre of the clicked point in the SVG's view-box pixel space, used as the
   * transform-origin for the mark-group scale (which uses `transform-box:
   * view-box`, so the origin is resolved in SVG coordinates and stays stable
   * across the parent and child renders). Falls back to the mark group's centre
   * when there is no trigger point (e.g. drillUp / imperative drill). Returns
   * null when the marks / SVG / WAAPI are unavailable (SSR / old browsers).
   * @param {object} meta
   * @returns {{ x: number, y: number }|null}
   */
  _triggerOrigin(meta) {
    if (!Environment.isBrowser()) return null;
    const svg = this._svgNode();
    const group = this._markGroup();
    if (!svg || !group || typeof group.animate !== "function" || typeof svg.getBoundingClientRect !== "function") {
      return null;
    }
    const svgRect = svg.getBoundingClientRect();
    let el = null;
    if (meta && meta.seriesIndex != null && meta.dataPointIndex != null && this.w.dom.baseEl) {
      el = this.w.dom.baseEl.querySelector(
        `[index="${meta.seriesIndex}"][j="${meta.dataPointIndex}"]`
      );
    }
    if (el && typeof el.getBoundingClientRect === "function") {
      const r2 = el.getBoundingClientRect();
      return {
        x: r2.left + r2.width / 2 - svgRect.left,
        y: r2.top + r2.height / 2 - svgRect.top
      };
    }
    const gRect = group.getBoundingClientRect();
    return {
      x: gRect.left + gRect.width / 2 - svgRect.left,
      y: gRect.top + gRect.height / 2 - svgRect.top
    };
  }
  /**
   * Run the "expand from the clicked point" choreography around an instant
   * (un-animated) update. Only the data-mark group is animated — the axes, grid,
   * and titles stay fixed, so the effect doesn't drag the whole chart frame. The
   * current marks fade out near-in-place (a quick fade, not a balloon), the child
   * renders invisibly underneath, then the child marks unfold outward from the
   * clicked point: a horizontal-biased scale anchored there, so the bars read as
   * emerging from the column you clicked. Drilling up has no trigger column, so
   * it settles gently from the marks' centre.
   *
   * `transform-box: view-box` resolves the origin in SVG coordinates, so the same
   * origin applies cleanly to the parent and the freshly-rendered child group.
   * @param {{ x: number, y: number }} origin
   * @param {'down'|'up'} direction
   * @param {() => Promise<any>} runUpdate
   * @returns {Promise<void>}
   */
  _zoomDrill(origin, direction, runUpdate) {
    return __async(this, null, function* () {
      const dur = this._zoomDuration();
      const down = direction === "down";
      const outDur = Math.round(dur * 0.55);
      const outTo = down ? "scale(1.03)" : "scale(0.97)";
      const inFrom = down ? "scaleX(0.55) scaleY(0.85)" : "scale(1.04)";
      const anchor = (el) => {
        el.style.transformBox = "view-box";
        el.style.transformOrigin = `${origin.x}px ${origin.y}px`;
      };
      const clear = (el) => {
        el.style.transform = "";
        el.style.opacity = "";
        el.style.transformOrigin = "";
        el.style.transformBox = "";
      };
      const outGroup = this._markGroup();
      let outAnim = null;
      if (outGroup) {
        anchor(outGroup);
        outAnim = outGroup.animate(
          [
            { transform: "scale(1)", opacity: 1 },
            { transform: outTo, opacity: 0 }
          ],
          { duration: outDur, easing: "ease-in", fill: "forwards" }
        );
        try {
          yield outAnim.finished;
        } catch (e2) {
        }
      }
      yield runUpdate();
      const inGroup = this._markGroup();
      if (inGroup) {
        anchor(inGroup);
        inGroup.style.opacity = "0";
        inGroup.style.transform = inFrom;
        if (outAnim && outGroup === inGroup) outAnim.cancel();
        const inAnim = inGroup.animate(
          [
            { transform: inFrom, opacity: 0 },
            { transform: "scale(1)", opacity: 1 }
          ],
          // Decelerating ease so the unfold settles softly into place.
          { duration: dur, easing: "cubic-bezier(0.16, 1, 0.3, 1)", fill: "forwards" }
        );
        try {
          yield inAnim.finished;
        } catch (e2) {
        }
        clear(inGroup);
        inAnim.cancel();
      }
    });
  }
  /** @returns {number} per-phase zoom duration in ms. */
  _zoomDuration() {
    const a2 = this.w.config.drilldown && this.w.config.drilldown.animation;
    const speed = a2 && typeof a2.speed === "number" ? a2.speed : 260;
    return Math.max(80, speed);
  }
  /**
   * Fire a drill event through both the config callback and the listener registry.
   * @param {string} name
   * @param {object} payload
   */
  _fire(name2, payload) {
    const cb = this.w.config.chart.events && this.w.config.chart.events[name2];
    if (typeof cb === "function") cb(payload, this.ctx, this.w);
    this.ctx.events.fireEvent(name2, [payload, this.ctx, this.w]);
  }
  // ─── Click + post-render hooks ───────────────────────────────────────────────
  /**
   * @param {Event} _event
   * @param {any} _ctx
   * @param {{ seriesIndex?: number, dataPointIndex?: number }} opts
   */
  _onPointSelect(_event, _ctx, opts) {
    if (!opts) return void 0;
    const point = this._pointAt(opts.seriesIndex, opts.dataPointIndex);
    if (point && typeof point === "object" && point.drilldown != null) {
      return this.drillDown(point.drilldown, point, opts);
    }
    if (typeof this.w.config.drilldown.onDrillDown === "function") {
      return this._drillDownAsync(null, point, opts);
    }
    return void 0;
  }
  /**
   * @param {number|undefined} seriesIndex
   * @param {number|undefined} dataPointIndex
   * @returns {any|null}
   */
  _pointAt(seriesIndex, dataPointIndex) {
    const series = this.w.config.series;
    if (!Array.isArray(series) || seriesIndex == null || dataPointIndex == null) {
      return null;
    }
    const s2 = series[seriesIndex];
    if (!s2 || !Array.isArray(s2.data)) return null;
    return s2.data[dataPointIndex] != null ? s2.data[dataPointIndex] : null;
  }
  _afterRender() {
    const w2 = this.w;
    if (!w2.config.drilldown || !w2.config.drilldown.enabled) return;
    this._markDrillableTargets();
    this._wirePlotClick();
    this.breadcrumb.render(this.path);
    if (w2.config.markers) {
      w2.config.markers.discrete = this._drillMarkers(w2.config.series);
    }
  }
  /**
   * Mark every point that carries a `drilldown` field as an openable target.
   *
   * Two things have to be true for a point to be drillable, and on line/area
   * neither holds by default. It needs a mark to click (with `markers.size: 0`
   * there is no element at all), and that mark has to accept the click: core
   * gives line/area markers `no-pointer-events` so the shared tooltip can track
   * the whole plot, which silently swallows it. `_drillMarkers()` supplies the
   * missing dots; this re-enables pointer events on them.
   *
   * The cursor class only goes on marks that can actually take the click, so we
   * never promise an interaction that cannot happen.
   */
  _markDrillableTargets() {
    if (!Environment.isBrowser()) return;
    const w2 = this.w;
    const baseEl = w2.dom.baseEl;
    const series = w2.config.series;
    if (!baseEl || !Array.isArray(series)) return;
    let unreachable = 0;
    series.forEach((s2, i2) => {
      const data = s2 && Array.isArray(s2.data) ? s2.data : null;
      if (!data) return;
      data.forEach((point, j) => {
        if (!point || typeof point !== "object" || point.drilldown == null) return;
        const nodes = baseEl.querySelectorAll(`[index="${i2}"][j="${j}"]`);
        if (!nodes.length) unreachable++;
        nodes.forEach((node) => {
          if (this._isClickThroughMark(node)) {
            node.classList.remove("no-pointer-events");
          }
          node.classList.add("apexcharts-drilldown-target");
        });
      });
    });
    if (unreachable && !this._warnedUnreachable) {
      this._warnedUnreachable = true;
      console.warn(
        `ApexCharts: ${unreachable} drillable point(s) have no clickable mark, so clicking them cannot do anything. Leave \`drilldown.marker\` on, or give the series markers of its own (\`markers.size > 0\`).`
      );
    }
  }
  /**
   * Called by Pie when it declines to wire the slice pull-out because this
   * chart drills. Warned once per chart (a drill re-renders, and the same
   * notice on every navigation is just noise), and from here rather than from
   * Pie because this module is the reason it is unavailable.
   */
  warnSliceOffsetDisabled() {
    if (this._warnedNoSliceOffset) return;
    this._warnedNoSliceOffset = true;
    console.warn(
      "ApexCharts: `plotOptions.pie.expandOnClick` is not available in a drilldown pie/donut, so it was ignored. A slice click navigates, and a slice that slid out would be discarded by the drill it just triggered."
    );
  }
  /**
   * Make the whole band a drillable point owns clickable, not just its dot.
   *
   * A dot is ~6px across, so hitting it takes pixel-precise aim, it is far under
   * the ~44px a finger needs, and the tooltip's arrow points AT the point by
   * design, which puts a triangle over the very thing you are aiming at. Rather
   * than move the tooltip, widen the target: a click anywhere in the plot drills
   * whichever point the tooltip is currently reading. The hit area then matches
   * the feedback already on screen, so "the tooltip says 2024, I click, I get
   * 2024" holds, and the dot goes back to being an affordance rather than a
   * target you have to chase.
   *
   * Only for the point-based types, since a bar, slice or tile is already a
   * comfortably large mark and drilling one by clicking the background near it
   * would be surprising.
   */
  _wirePlotClick() {
    if (!Environment.isBrowser()) return;
    const baseEl = this.w.dom.baseEl;
    if (!baseEl || this._plotClickWired === baseEl) return;
    if (this._plotClickWired) {
      this._plotClickWired.removeEventListener("mousedown", this._onPlotDown);
      this._plotClickWired.removeEventListener("click", this._onPlotClick);
    }
    baseEl.addEventListener("mousedown", this._onPlotDown);
    baseEl.addEventListener("click", this._onPlotClick);
    this._plotClickWired = baseEl;
  }
  /** @param {any} e */
  _onPlotDown(e2) {
    this._downAt = { x: e2.clientX, y: e2.clientY };
  }
  /**
   * @param {any} e
   * @returns {any}
   */
  _onPlotClick(e2) {
    const w2 = this.w;
    if (!w2.config.drilldown || !w2.config.drilldown.enabled) return void 0;
    const down = this._downAt;
    this._downAt = null;
    if (down && Math.hypot(e2.clientX - down.x, e2.clientY - down.y) > 4) {
      return void 0;
    }
    const target = (
      /** @type {Element} */
      e2.target
    );
    if (!target || typeof target.closest !== "function") return void 0;
    if (target.closest(".apexcharts-drilldown-target")) return void 0;
    if (target.closest(
      ".apexcharts-legend, .apexcharts-toolbar, .apexcharts-breadcrumb, .apexcharts-menu, .apexcharts-tooltip"
    )) {
      return void 0;
    }
    const i2 = w2.interact.capturedSeriesIndex;
    const j = w2.interact.capturedDataPointIndex;
    if (i2 == null || j == null || i2 < 0 || j < 0) return void 0;
    if (!this._isPointBasedSeries(w2.config.series[i2])) return void 0;
    const point = this._pointAt(i2, j);
    if (!point || typeof point !== "object" || point.drilldown == null) {
      return void 0;
    }
    return this.drillDown(point.drilldown, point, {
      seriesIndex: i2,
      dataPointIndex: j
    });
  }
  /**
   * A series mark that is deliberately click-through. Restricted to markers
   * inside the plot: the tooltip draws its own `no-pointer-events` marker, and
   * that one must stay click-through or it would sit under the cursor and eat
   * the hover it exists to follow.
   * @param {Element} node
   * @returns {boolean}
   */
  _isClickThroughMark(node) {
    if (!node.classList || !node.classList.contains("no-pointer-events")) {
      return false;
    }
    if (!node.classList.contains("apexcharts-marker")) return false;
    return !(typeof node.closest === "function" && node.closest(".apexcharts-tooltip"));
  }
  /**
   * Discrete-marker entries that give each drillable point a visible dot.
   *
   * Only series drawn WITHOUT markers get them, so an author who already shows
   * markers keeps their styling untouched, and only drillable points get one, so
   * the dots read as "these are the ones you can open" rather than turning every
   * point into a dot. Core renders discrete markers even when `markers.size` is
   * 0, which is what makes the affordance possible without a core change.
   *
   * Entries are tagged so a resync replaces ours and leaves the author's alone.
   * @param {any[]} series - the series being rendered (a drill applies its
   *   level's series, which are not yet on `w.config` when this runs)
   * @returns {any[]}
   */
  _drillMarkers(series) {
    const w2 = this.w;
    const cfg = w2.config.drilldown;
    const authored = Array.isArray(w2.config.markers && w2.config.markers.discrete) ? w2.config.markers.discrete.filter(
      (d) => !d || !d[DRILL_MARKER]
    ) : [];
    const mk = cfg && cfg.marker || {};
    if (mk.show === false || !Array.isArray(series)) return authored;
    const own = [];
    series.forEach((s2, i2) => {
      if (!this._seriesNeedsDrillMarker(i2, s2)) return;
      const data = s2 && Array.isArray(s2.data) ? s2.data : null;
      if (!data) return;
      data.forEach((point, j) => {
        if (!point || typeof point !== "object" || point.drilldown == null) return;
        const entry = { seriesIndex: i2, dataPointIndex: j, [DRILL_MARKER]: true };
        if (mk.size !== void 0) entry.size = mk.size;
        if (mk.shape !== void 0) entry.shape = mk.shape;
        if (mk.fillColor !== void 0) entry.fillColor = mk.fillColor;
        if (mk.strokeColor !== void 0) entry.strokeColor = mk.strokeColor;
        own.push(entry);
      });
    });
    return authored.concat(own);
  }
  /**
   * Whether a series needs drill dots supplied for it: a point-based type whose
   * marks are the markers, drawn with markers off. Bar, pie, treemap and heatmap
   * marks are already real clickable elements, and a series that already shows
   * markers already has its affordance.
   * @param {number} i @param {any} s
   * @returns {boolean}
   */
  _seriesNeedsDrillMarker(i2, s2) {
    if (!this._isPointBasedSeries(s2)) return false;
    const size = this.w.config.markers && this.w.config.markers.size;
    const effective = Array.isArray(size) ? size[i2] : size;
    return !(Number(effective) > 0);
  }
  /**
   * A series whose marks are markers (a point), rather than a shape big enough
   * to aim at on its own.
   * @param {any} s
   * @returns {boolean}
   */
  _isPointBasedSeries(s2) {
    const type = s2 && s2.type || this.w.config.chart.type;
    return type === "line" || type === "area";
  }
}
ApexCharts.registerFeatures({ drilldown: Drilldown });
const VIEWSTATE_VERSION = 1;
function axisWindow(min, max) {
  const hasMin = min !== void 0 && min !== null;
  const hasMax = max !== void 0 && max !== null;
  if (!hasMin && !hasMax) return null;
  return { min: hasMin ? min : null, max: hasMax ? max : null };
}
function cloneSelection(sel) {
  if (!Array.isArray(sel)) return [];
  return sel.map((a2) => Array.isArray(a2) ? a2.slice() : a2);
}
function annotationKind(method, ctx) {
  if (typeof method !== "function" || !ctx) return null;
  if (method === ctx.addXaxisAnnotation) return "xaxis";
  if (method === ctx.addYaxisAnnotation) return "yaxis";
  if (method === ctx.addPointAnnotation) return "point";
  return null;
}
function addMethodName(kind) {
  switch (kind) {
    case "xaxis":
      return "addXaxisAnnotation";
    case "yaxis":
      return "addYaxisAnnotation";
    case "point":
      return "addPointAnnotation";
    default:
      return null;
  }
}
function captureAnnotations(w2, ctx) {
  const staticAnno = w2.config.annotations ? Utils$1.clone(w2.config.annotations) : null;
  const dynamic = [];
  const mem = w2.globals.memory && w2.globals.memory.methodsToExec || [];
  for (const entry of mem) {
    if (!entry || entry.label !== "addAnnotation") continue;
    const kind = annotationKind(entry.method, ctx);
    if (!kind) continue;
    dynamic.push({ kind, params: Utils$1.clone(entry.params) });
  }
  return { static: staticAnno, dynamic };
}
function captureMeasure(ctx) {
  const m2 = ctx && ctx.measure;
  if (!m2 || typeof m2.getPins !== "function") return null;
  const pins = m2.getPins();
  return Array.isArray(pins) && pins.length ? { pins } : null;
}
function captureViewState(w2, ctx) {
  var _a, _b;
  const cfgX = w2.config.xaxis || {};
  const cfgYArr = Array.isArray(w2.config.yaxis) ? w2.config.yaxis : w2.config.yaxis ? [w2.config.yaxis] : [];
  const yWindows = cfgYArr.map(
    (y) => axisWindow(y && y.min, y && y.max)
  );
  const anyY = yWindows.some((yw) => yw !== null);
  const theme = w2.config.theme;
  const drilldown = ctx && ctx.drilldown;
  return {
    v: VIEWSTATE_VERSION,
    window: {
      xaxis: axisWindow(cfgX.min, cfgX.max),
      yaxis: anyY ? yWindows : null
    },
    zoomed: !!w2.interact.zoomed,
    collapsed: (w2.globals.collapsedSeriesIndices || []).slice(),
    ancillaryCollapsed: (w2.globals.ancillaryCollapsedSeriesIndices || []).slice(),
    selectedDataPoints: cloneSelection(w2.interact.selectedDataPoints),
    theme: theme ? { mode: (_a = theme.mode) != null ? _a : null, palette: (_b = theme.palette) != null ? _b : null } : null,
    locale: w2.config.chart && w2.config.chart.defaultLocale || null,
    annotations: captureAnnotations(w2, ctx),
    drill: drilldown && drilldown.depth > 0 ? { path: drilldown.path.slice() } : null,
    measure: captureMeasure(ctx)
  };
}
function applyCollapsedSet(ctx, targetCollapsed, targetAncillary) {
  const w2 = ctx.w;
  if (targetCollapsed == null && targetAncillary == null) return;
  const names = w2.globals.seriesNames || [];
  const target = /* @__PURE__ */ new Set([
    ...targetCollapsed || [],
    ...targetAncillary || []
  ]);
  const current = /* @__PURE__ */ new Set([
    ...w2.globals.collapsedSeriesIndices || [],
    ...w2.globals.ancillaryCollapsedSeriesIndices || []
  ]);
  for (let realIndex = 0; realIndex < names.length; realIndex++) {
    const name2 = names[realIndex];
    if (name2 == null) continue;
    const shouldCollapse = target.has(realIndex);
    const isCollapsed = current.has(realIndex);
    if (shouldCollapse && !isCollapsed) {
      ctx.hideSeries(name2);
    } else if (!shouldCollapse && isCollapsed) {
      ctx.showSeries(name2);
    }
  }
}
function restoreSelection(ctx, selectedDataPoints) {
  if (!Array.isArray(selectedDataPoints)) return;
  ctx.w.interact.selectedDataPoints = cloneSelection(selectedDataPoints);
}
function applyViewState(ctx, view, { animate = true, mergeOptions } = {}) {
  var _a, _b;
  if (!ctx || !view) return;
  if (typeof view.v === "number" && view.v > VIEWSTATE_VERSION) {
    console.warn(
      `[apexcharts] ViewState v${view.v} is newer than this build understands (v${VIEWSTATE_VERSION}); applying best-effort.`
    );
  }
  ctx.clearAnnotations();
  const options2 = mergeOptions ? Utils$1.clone(mergeOptions) : {};
  const xw = view.window && view.window.xaxis;
  options2.xaxis = Object.assign(
    {},
    options2.xaxis,
    xw ? { min: (_a = xw.min) != null ? _a : void 0, max: (_b = xw.max) != null ? _b : void 0 } : { min: void 0, max: void 0 }
  );
  const yw = view.window && view.window.yaxis;
  if (Array.isArray(yw)) {
    options2.yaxis = yw.map(
      (y) => {
        var _a2, _b2;
        return y ? { min: (_a2 = y.min) != null ? _a2 : void 0, max: (_b2 = y.max) != null ? _b2 : void 0 } : {};
      }
    );
  }
  if (view.theme) {
    const theme = {};
    if (view.theme.mode != null) theme.mode = view.theme.mode;
    if (view.theme.palette != null) theme.palette = view.theme.palette;
    if (Object.keys(theme).length) {
      options2.theme = Object.assign({}, options2.theme, theme);
    }
  }
  if (view.annotations && view.annotations.static) {
    options2.annotations = Utils$1.clone(view.annotations.static);
  }
  ctx.updateOptions(
    options2,
    false,
    animate,
    false,
    false
  );
  applyViewInteraction(ctx, view);
}
function applyViewInteraction(ctx, view) {
  if (!ctx || !view) return;
  const w2 = ctx.w;
  w2.interact.zoomed = !!view.zoomed;
  applyCollapsedSet(ctx, view.collapsed, view.ancillaryCollapsed);
  if (view.annotations && Array.isArray(view.annotations.dynamic)) {
    view.annotations.dynamic.forEach((a2) => {
      const method = addMethodName(a2.kind);
      if (method && typeof ctx[method] === "function") {
        ctx[method](a2.params, true);
      }
    });
  }
  restoreSelection(ctx, view.selectedDataPoints);
  if (view.locale && view.locale !== w2.config.chart.defaultLocale) {
    ctx.setLocale(view.locale);
  }
  if (ctx.measure && typeof ctx.measure.setPins === "function") {
    ctx.measure.setPins(view.measure && view.measure.pins || []);
  }
}
const PERSPECTIVE_VERSION = 1;
const HASH_KEY = "apex";
function toBase64(str) {
  if (typeof Buffer !== "undefined") {
    return Buffer.from(str, "utf-8").toString("base64");
  }
  const bytes = new TextEncoder().encode(str);
  let bin = "";
  for (let i2 = 0; i2 < bytes.length; i2++) bin += String.fromCharCode(bytes[i2]);
  return btoa(bin);
}
function fromBase64(b64) {
  if (typeof Buffer !== "undefined") {
    return Buffer.from(b64, "base64").toString("utf-8");
  }
  const bin = atob(b64);
  const bytes = new Uint8Array(bin.length);
  for (let i2 = 0; i2 < bin.length; i2++) bytes[i2] = bin.charCodeAt(i2);
  return new TextDecoder().decode(bytes);
}
function base64urlEncode(str) {
  return toBase64(str).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function base64urlDecode(b64url) {
  let b64 = b64url.replace(/-/g, "+").replace(/_/g, "/");
  while (b64.length % 4) b64 += "=";
  return fromBase64(b64);
}
function stripFunctions(obj) {
  try {
    return JSON.parse(JSON.stringify(obj));
  } catch (e2) {
    return void 0;
  }
}
class Perspectives {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this._saved = [];
    this._counter = 0;
    this._used = false;
  }
  /**
   * Capture the current chart view as a Perspective token.
   * @returns {{ v: number, view: object, options?: Record<string, any> }}
   */
  capture() {
    const view = captureViewState(this.w, this.ctx);
    const token = (
      /** @type {any} */
      { v: PERSPECTIVE_VERSION, view }
    );
    const options2 = this._serializableDelta();
    if (options2 && Object.keys(options2).length) token.options = options2;
    return token;
  }
  /**
   * Build the whitelisted, function-free option override recorded in the token.
   * @returns {Record<string, any>}
   * @private
   */
  _serializableDelta() {
    const cfg = this.w.config;
    const whitelist = cfg.chart && cfg.chart.perspectives && cfg.chart.perspectives.serializeOptions || ["theme", "xaxis", "yaxis", "title", "subtitle"];
    const delta = {};
    whitelist.forEach((path) => {
      if (cfg[path] !== void 0) {
        const stripped = stripFunctions(cfg[path]);
        if (stripped !== void 0) delta[path] = stripped;
      }
    });
    return delta;
  }
  /**
   * Encode a token (or the current capture) to a compact base64url string.
   * JSON.stringify drops any functions embedded in annotation params / option
   * overrides by construction: a shared link carries data; the opening page
   * supplies its own functions from config.
   * @param {any} [token]
   * @returns {string}
   */
  encode(token) {
    const t2 = token || this.capture();
    return base64urlEncode(JSON.stringify(t2));
  }
  /**
   * Decode a base64url token string. Never throws: returns null on any error
   * or version mismatch (with a console warning).
   * @param {string} str
   * @returns {any | null}
   */
  decode(str) {
    return Perspectives.decode(str);
  }
  /**
   * Encode the current capture into a `#apex=<token>` URL hash fragment on the
   * current location. Browser-only; returns '' under SSR.
   * @returns {string}
   */
  toURL() {
    if (!Environment.isBrowser()) return "";
    const encoded = this.encode(this.capture());
    const url = new URL(window.location.href);
    url.hash = `${HASH_KEY}=${encoded}`;
    return url.toString();
  }
  /**
   * Restore a perspective. Accepts a token object or an encoded string. When
   * the chart is grouped, applies to every synced chart.
   *
   * The token's option overrides and `opts.mergeOptions` are folded into the
   * view restore's ONE updateOptions call (mergeOptions wins over
   * token.options; the view's own fields win over both). A single render is
   * deliberate: a second immediate updateOptions would kill the first one's
   * animation mid-flight, and a chart.type change applied this way morphs
   * (morph feature) inside the same re-render instead of being re-rendered
   * over. Consumed by Storyboard for per-beat option payloads.
   *
   * @param {any} tokenOrString
   * @param {{ animate?: boolean, mergeOptions?: Record<string, any> }} [opts]
   */
  apply(tokenOrString, opts = {}) {
    const token = typeof tokenOrString === "string" ? Perspectives.decode(tokenOrString) : tokenOrString;
    if (!token || !token.view) return;
    this._used = true;
    enforceLicense(this.w, this.ctx);
    const animate = opts.animate !== void 0 ? opts.animate : true;
    const combined = Utils$1.extend(
      token.options ? Utils$1.clone(token.options) : {},
      opts.mergeOptions || {}
    );
    const mergeOptions = Object.keys(combined).length ? combined : void 0;
    const targets = this.w.config.chart.group ? this.ctx.getSyncedCharts() : [this.ctx];
    targets.forEach((chart) => {
      applyViewState(chart, token.view, { animate, mergeOptions });
    });
  }
  /**
   * Save the current view under a name in the in-memory registry.
   * @param {string} name
   * @returns {string} generated id
   */
  save(name2) {
    const id = `perspective-${++this._counter}`;
    this._saved.push({ id, name: name2 || id, token: this.capture() });
    this._used = true;
    enforceLicense(this.w, this.ctx);
    return id;
  }
  /**
   * List saved perspectives.
   * @returns {{ id: string, name: string, token: any }[]}
   */
  list() {
    return this._saved.map((s2) => ({ id: s2.id, name: s2.name, token: s2.token }));
  }
  /**
   * Delete a saved perspective by id.
   * @param {string} id
   */
  delete(id) {
    const i2 = this._saved.findIndex((s2) => s2.id === id);
    if (i2 > -1) this._saved.splice(i2, 1);
  }
  /** Drop the saved-views registry (called on full destroy). */
  teardown() {
    this._saved = [];
    this._counter = 0;
  }
  // ── static, pure helpers (available once the feature is imported) ─────────
  /**
   * @param {string} str base64url token
   * @returns {any | null}
   */
  static decode(str) {
    if (typeof str !== "string" || !str) return null;
    try {
      const token = JSON.parse(base64urlDecode(str));
      if (!token || typeof token !== "object") return null;
      if (token.v !== PERSPECTIVE_VERSION) {
        console.warn(
          `apexcharts: unsupported perspective version ${token.v} (expected ${PERSPECTIVE_VERSION}).`
        );
        return null;
      }
      return token;
    } catch (e2) {
      console.warn("apexcharts: failed to decode perspective token.", e2);
      return null;
    }
  }
  /**
   * Parse a `#apex=<token>` fragment out of an href (or the current location in
   * a browser). Pure and Node-safe when given an explicit href.
   * @param {string} [href]
   * @returns {any | null}
   */
  static fromURL(href) {
    try {
      const target = href || (Environment.isBrowser() ? window.location.href : "");
      if (!target) return null;
      const url = new URL(target);
      const hash = url.hash.replace(/^#/, "");
      if (!hash) return null;
      const pair = hash.split("&").map((p2) => p2.split("=")).find((p2) => p2[0] === HASH_KEY);
      if (!pair || pair[1] == null) return null;
      return Perspectives.decode(decodeURIComponent(pair[1]));
    } catch (e2) {
      return null;
    }
  }
}
ApexCharts.registerFeatures({ perspectives: Perspectives });
ApexCharts.perspectives = {
  /** @param {string} str */
  decode: (str) => {
    markPerspectivesTokenDecoded();
    return Perspectives.decode(str);
  },
  /** @param {string} [href] */
  fromURL: (href) => {
    markPerspectivesTokenDecoded();
    return Perspectives.fromURL(href);
  }
};
class Storyboard {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this._beats = [];
    this._observer = null;
    this._activeIndex = -1;
    this._animate = true;
    this._warnedNoPerspectives = false;
    this._used = false;
  }
  /**
   * Bind beats to scroll position. Rebinding replaces the previous binding.
   * @param {{
   *   beats?: Array<{ el?: Element, selector?: string, key?: string, view?: any, announce?: string, onEnter?: (chart: any, info: StoryboardBeatInfo) => void }>,
   *   scroller?: Element | string,
   *   offset?: number,
   *   animate?: boolean,
   * }} [opts]
   * @returns {number} the number of beats bound
   */
  bind(opts = {}) {
    var _a;
    this.unbind();
    if (!Environment.isBrowser()) return 0;
    const doc = this.ctx.el && this.ctx.el.ownerDocument;
    if (!doc || typeof IntersectionObserver === "undefined") return 0;
    let root = null;
    if (opts.scroller) {
      root = typeof opts.scroller === "string" ? doc.querySelector(opts.scroller) : opts.scroller;
    }
    this._beats = this._resolveBeats(doc, root, opts.beats);
    if (!this._beats.length) return 0;
    this._animate = opts.animate !== false;
    const offset = Math.min(Math.max((_a = opts.offset) != null ? _a : 0.5, 0), 1);
    const top = +(offset * 100).toFixed(3);
    const bottom = +(100 - offset * 100).toFixed(3);
    this._observer = new IntersectionObserver(
      (entries) => this._onIntersect(entries),
      { root, rootMargin: `-${top}% 0px -${bottom}%`, threshold: 0 }
    );
    this._beats.forEach((b2) => {
      var _a2;
      return (_a2 = this._observer) == null ? void 0 : _a2.observe(b2.el);
    });
    this._used = true;
    enforceLicense(this.w, this.ctx);
    return this._beats.length;
  }
  /**
   * Normalize the beats option, or auto-discover [data-apex-beat] elements in
   * document order when no explicit list is given.
   * @param {Document} doc
   * @param {Element | null} root
   * @param {Array<any>} [beatsOpt]
   * @returns {StoryboardBeat[]}
   * @private
   */
  _resolveBeats(doc, root, beatsOpt) {
    const beats = [];
    if (Array.isArray(beatsOpt)) {
      beatsOpt.forEach((b2, i2) => {
        var _a, _b;
        if (!b2) return;
        const el = b2.el && typeof b2.el === "object" ? b2.el : b2.selector ? doc.querySelector(b2.selector) : null;
        if (!el) {
          console.warn(
            `apexcharts: storyboard beat ${i2} has no resolvable element; skipped.`
          );
          return;
        }
        beats.push({
          el,
          key: (_b = (_a = b2.key) != null ? _a : el.getAttribute("data-apex-beat")) != null ? _b : String(i2),
          view: b2.view,
          options: b2.options,
          announce: b2.announce,
          onEnter: typeof b2.onEnter === "function" ? b2.onEnter : void 0
        });
      });
      return beats;
    }
    const scope = root || doc;
    scope.querySelectorAll("[data-apex-beat]").forEach((el, i2) => {
      beats.push({
        el,
        key: el.getAttribute("data-apex-beat") || String(i2),
        view: el.getAttribute("data-apex-view") || void 0,
        options: void 0,
        announce: el.getAttribute("data-apex-announce") || void 0,
        onEnter: void 0
      });
    });
    return beats;
  }
  /**
   * @param {IntersectionObserverEntry[]} entries
   * @private
   */
  _onIntersect(entries) {
    entries.forEach((entry) => {
      const idx = this._beats.findIndex((b2) => b2.el === entry.target);
      if (idx < 0) return;
      if (entry.isIntersecting) {
        this._activate(idx);
      } else if (idx === this._activeIndex && entry.rootBounds) {
        if (entry.boundingClientRect.top >= entry.rootBounds.bottom && idx > 0) {
          this._activate(idx - 1);
        }
      }
    });
  }
  /**
   * Activate a beat: apply its view token, run its callback, announce it and
   * fire `beatChange`. Idempotent per beat (re-activating the current beat is
   * a no-op), so IO chatter never re-applies a view.
   * @param {number} idx
   * @param {{ animate?: boolean }} [opts]
   * @private
   */
  _activate(idx, opts = {}) {
    var _a, _b;
    if (idx === this._activeIndex) return;
    const beat = this._beats[idx];
    if (!beat) return;
    const direction = idx > this._activeIndex ? "down" : "up";
    this._activeIndex = idx;
    const animate = (opts.animate !== void 0 ? opts.animate : this._animate) && !prefersReducedMotion();
    if (beat.view != null || beat.options) {
      if (this.ctx.perspectives) {
        const v2 = (_a = beat.view) != null ? _a : {};
        const token = typeof v2 === "string" || v2.view ? v2 : { view: this._normalizeView(v2) };
        this.ctx.perspectives.apply(token, {
          animate,
          mergeOptions: beat.options
        });
      } else if (!this._warnedNoPerspectives) {
        this._warnedNoPerspectives = true;
        console.warn(
          'apexcharts: storyboard beats carry views but the perspectives feature is not bundled. import "apexcharts/features/storyboard" (which includes it) or drive beats via onEnter.'
        );
      }
    }
    const info = { index: idx, key: beat.key, el: beat.el, direction };
    if (beat.onEnter) beat.onEnter(this.ctx, info);
    if (beat.announce) this._announce(beat.announce);
    if (typeof this.w.config.chart.events.beatChange === "function") {
      this.w.config.chart.events.beatChange(this.ctx, info);
    }
    (_b = this.ctx.events) == null ? void 0 : _b.fireEvent("beatChange", [this.ctx, info]);
  }
  /**
   * Fill in the parts of a hand-authored (bare) ViewState that would
   * otherwise LEAK between beats. updateOptions merges objects, so a beat
   * listing only xaxis annotations would keep a previous beat's point
   * annotations; padding every annotation kind with an empty array makes
   * each beat fully describe its own state, which is what allows scrubbing
   * in both directions. Full tokens (from capture()/encode()) already carry
   * the complete set and never pass through here.
   * @param {any} view
   * @returns {any}
   * @private
   */
  _normalizeView(view) {
    const provided = view.annotations && view.annotations.static || {};
    return __spreadProps(__spreadValues({}, view), {
      annotations: {
        static: __spreadValues({
          points: [],
          xaxis: [],
          yaxis: [],
          texts: [],
          images: []
        }, provided),
        dynamic: view.annotations && view.annotations.dynamic || []
      }
    });
  }
  /**
   * Programmatically jump to a beat by index or author key (also usable
   * without scrolling, e.g. from next/prev buttons).
   * @param {number | string} indexOrKey
   * @param {{ animate?: boolean }} [opts]
   */
  goTo(indexOrKey, opts = {}) {
    const idx = typeof indexOrKey === "number" ? indexOrKey : this._beats.findIndex((b2) => b2.key === indexOrKey);
    if (idx >= 0 && idx < this._beats.length) this._activate(idx, opts);
  }
  /**
   * @returns {{ index: number, key: string | null } | null} the active beat
   */
  current() {
    if (this._activeIndex < 0) return null;
    const beat = this._beats[this._activeIndex];
    return { index: this._activeIndex, key: beat ? beat.key : null };
  }
  /** Disconnect the observer and drop the beat list. */
  unbind() {
    if (this._observer) {
      this._observer.disconnect();
      this._observer = null;
    }
    this._beats = [];
    this._activeIndex = -1;
    this._used = false;
    enforceLicense(this.w, this.ctx);
  }
  /** Full-destroy cleanup (called from Destroy). */
  teardown() {
    this.unbind();
  }
  /**
   * Push a beat's announcement to the chart's visually-hidden aria-live
   * status region so screen-reader users follow the story too. No-op when
   * announcements are disabled or the region is absent.
   * @param {string} message
   * @private
   */
  _announce(message) {
    const w2 = this.w;
    if (!w2.config.chart.accessibility.announcements.enabled) return;
    const baseEl = w2.dom.baseEl;
    if (!baseEl) return;
    const region = baseEl.querySelector(".apexcharts-sr-status");
    if (!region) return;
    region.textContent = "";
    setTimeout(() => {
      region.textContent = message;
    }, 0);
  }
}
ApexCharts.registerFeatures({ storyboard: Storyboard });
function isEditableTarget(node) {
  if (!node) return false;
  const tag = node.tagName;
  return tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT" || node.isContentEditable === true;
}
class History {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.stack = [];
    this.pointer = -1;
    this.applying = false;
    this._batching = false;
    this._counter = 0;
    this._coalesceTimer = null;
    this._settleTimer = null;
    this._pendingLabel = void 0;
    this._keydownTarget = null;
    this._pointerTarget = null;
    this._engaged = false;
    this._wired = false;
    this._readConfig();
    this._onMounted = this._onMounted.bind(this);
    this._onUpdated = this._onUpdated.bind(this);
    this._onSelection = this._onSelection.bind(this);
    this._onKeyDown = this._onKeyDown.bind(this);
    this._onPointerDown = this._onPointerDown.bind(this);
    this.init();
  }
  /**
   * (Re)read chart.history config. Called at construction and again on every
   * mounted/updated event so `updateOptions({ chart: { history: {...} } })`
   * takes effect at runtime: enabling wires the keyboard + starts capturing,
   * disabling stops capturing (the stack is kept for a later re-enable).
   */
  _readConfig() {
    const w2 = this.w;
    const cfg = w2.config.chart && w2.config.chart.history || {};
    this.enabled = !!cfg.enabled;
    this.maxDepth = cfg.maxDepth > 0 ? cfg.maxDepth : 100;
    this.coalesceMs = cfg.coalesceMs != null ? cfg.coalesceMs : 250;
    this.keyboard = cfg.keyboard !== false;
  }
  /** Re-sync config, then wire/unwire the keyboard to match. */
  _syncConfig() {
    this._readConfig();
    if (this.enabled && this.keyboard) this._wireKeyboard();
    else this._unwireKeyboard();
  }
  init() {
    if (this._wired) return;
    this._wired = true;
    this.ctx.addEventListener("mounted", this._onMounted);
    this.ctx.addEventListener("updated", this._onUpdated);
    this.ctx.addEventListener("scrolled", this._onUpdated);
    this.ctx.addEventListener("dataPointSelection", this._onSelection);
    if (this.enabled && this.keyboard) this._wireKeyboard();
  }
  /**
   * Keyboard: Cmd/Ctrl+Z = undo, Shift+Cmd/Ctrl+Z or Ctrl+Y = redo. Bound on
   * the document (not the chart element) because pointer gestures that create
   * an undo step (annotation drag, zoom, pan) call preventDefault and so never
   * move focus into the chart, leaving an el-scoped listener unreachable. To
   * stay non-intrusive it acts only when this chart is "engaged" (see
   * _onKeyDown): a capture-phase pointerdown marks engagement so the shortcut
   * follows the chart the user last touched, and defers to text editing.
   */
  _wireKeyboard() {
    if (this._keydownTarget) return;
    const el = (
      /** @type {any} */
      this.ctx.el
    );
    const doc = el && el.ownerDocument;
    if (!Environment.isBrowser() || !doc) return;
    doc.addEventListener("keydown", this._onKeyDown);
    doc.addEventListener("pointerdown", this._onPointerDown, true);
    doc.addEventListener("mousedown", this._onPointerDown, true);
    this._keydownTarget = doc;
    this._pointerTarget = doc;
  }
  _unwireKeyboard() {
    if (this._keydownTarget) {
      this._keydownTarget.removeEventListener("keydown", this._onKeyDown);
      this._keydownTarget = null;
    }
    if (this._pointerTarget) {
      this._pointerTarget.removeEventListener("pointerdown", this._onPointerDown, true);
      this._pointerTarget.removeEventListener("mousedown", this._onPointerDown, true);
      this._pointerTarget = null;
    }
    this._engaged = false;
  }
  // ─── Event handlers ─────────────────────────────────────────────────────
  _onMounted() {
    this._syncConfig();
    if (!this.enabled) return;
    if (this.stack.length === 0) this._commit("initial", true);
  }
  _onUpdated() {
    this._syncConfig();
    if (!this.enabled) return;
    if (this.applying) {
      this._refreshSettle();
      return;
    }
    this._schedule("update");
  }
  _onSelection() {
    if (!this.enabled || this.applying) return;
    this._schedule("selection");
  }
  /**
   * Mark whether the chart is engaged: a capture-phase pointerdown inside `el`
   * engages it (runs before feature handlers stopPropagation), one elsewhere
   * releases it. Capture phase so an annotation/zoom gesture that stops
   * propagation still registers.
   * @param {any} e
   */
  _onPointerDown(e2) {
    const el = (
      /** @type {any} */
      this.ctx.el
    );
    this._engaged = !!(el && e2.target && el.contains(e2.target));
  }
  /**
   * @param {KeyboardEvent} e
   */
  _onKeyDown(e2) {
    if (!(e2.metaKey || e2.ctrlKey)) return;
    const key = (e2.key || "").toLowerCase();
    if (key !== "z" && key !== "y") return;
    const el = (
      /** @type {any} */
      this.ctx.el
    );
    if (!el) return;
    const doc = el.ownerDocument;
    const active = doc && doc.activeElement;
    if (isEditableTarget(active)) return;
    if (!(el.contains(active) || this._engaged)) return;
    const redo = key === "y" || e2.shiftKey;
    e2.preventDefault();
    if (redo) this.redo();
    else this.undo();
  }
  // ─── Capture (coalesced) ────────────────────────────────────────────────
  /**
   * @param {string} label
   */
  _schedule(label) {
    if (this.applying || this._batching || !this.enabled) return;
    this._pendingLabel = label;
    if (this.coalesceMs > 0 && Environment.isBrowser()) {
      clearTimeout(this._coalesceTimer);
      this._coalesceTimer = setTimeout(
        () => this._commit(this._pendingLabel),
        this.coalesceMs
      );
    } else {
      this._commit(label);
    }
  }
  /**
   * @param {string} [label]
   * @param {boolean} [force] bypass the applying/batching guard (baseline / transaction)
   */
  _commit(label, force) {
    clearTimeout(this._coalesceTimer);
    this._coalesceTimer = null;
    if (!force && (this.applying || this._batching)) return;
    const cp = this._capture(label);
    const current = this.stack[this.pointer];
    if (current && current.sig === cp.sig) return;
    if (this.pointer < this.stack.length - 1) {
      this.stack.splice(this.pointer + 1);
    }
    this.stack.push(cp);
    this.pointer = this.stack.length - 1;
    while (this.stack.length > this.maxDepth) {
      this.stack.shift();
      this.pointer--;
    }
    this._emitChange();
  }
  /**
   * @param {string} [label]
   */
  _capture(label) {
    const view = captureViewState(this.w, this.ctx);
    const { config, seriesSig } = this._cloneConfigCOW();
    return {
      id: `hist-${++this._counter}`,
      view,
      config,
      seriesSig,
      label: label || "change",
      at: Environment.isBrowser() ? Date.now() : 0,
      origin: "local",
      // reserved for per-user scoping (Live Rooms)
      sig: this._signature(view, config)
    };
  }
  /**
   * Clone w.config, sharing the previous checkpoint's cloned series when the
   * live series CONTENT is unchanged (copy-on-write). Sharing is decided by a
   * value signature, not reference identity: callers commonly mutate a kept
   * series array in place and pass the same reference back to updateSeries, and
   * an identity check would share a stale clone for exactly that case. The
   * stringify is not extra cost: _signature already serialises the series as
   * part of dedup.
   * @returns {{ config: any, seriesSig: string|null }}
   */
  _cloneConfigCOW() {
    const w2 = this.w;
    const prev = this.stack[this.pointer];
    let seriesSig = null;
    try {
      seriesSig = JSON.stringify(w2.config.series);
    } catch (e2) {
      seriesSig = null;
    }
    let cloned;
    if (prev && seriesSig !== null && prev.seriesSig === seriesSig) {
      const _a = w2.config, { series: _series } = _a, rest = __objRest(_a, ["series"]);
      cloned = Utils$1.clone(rest);
      cloned.series = prev.config.series;
    } else {
      cloned = Utils$1.clone(w2.config);
    }
    return { config: cloned, seriesSig };
  }
  /**
   * Data-level signature for dedup. Functions are dropped by JSON (fine: a
   * checkpoint whose only change is a function reference is not a meaningful
   * undo step). Runs once per committed checkpoint, not per raw event.
   * @param {any} view
   * @param {any} config
   * @returns {string}
   */
  _signature(view, config) {
    try {
      return JSON.stringify(view) + "|" + JSON.stringify(config);
    } catch (e2) {
      return `nosig-${this._counter}`;
    }
  }
  // ─── Restore ────────────────────────────────────────────────────────────
  /**
   * @param {any} cp
   * @param {boolean} animate
   */
  _restore(cp, animate) {
    if (!cp) return;
    this.applying = true;
    let p2;
    try {
      this.ctx.clearAnnotations();
      p2 = this.ctx.updateOptions(Utils$1.clone(cp.config), false, animate, false, false);
    } catch (e2) {
      this.applying = false;
      throw e2;
    }
    Promise.resolve(p2).then(() => {
      if (this.w.globals.isDestroyed) {
        this.applying = false;
        return;
      }
      applyViewInteraction(this.ctx, cp.view);
      this._refreshSettle();
      this._emitChange();
    }).catch(() => {
      this.applying = false;
    });
  }
  /**
   * Hold `applying` true until the restore's burst of async 'updated' events
   * has drained (one macrotask after the last one). Refreshed by _onUpdated.
   */
  _refreshSettle() {
    if (!Environment.isBrowser()) {
      this.applying = false;
      return;
    }
    clearTimeout(this._settleTimer);
    this._settleTimer = setTimeout(() => {
      this.applying = false;
    }, 0);
  }
  // ─── Public API ─────────────────────────────────────────────────────────
  /**
   * Commit a checkpoint of the current state now (a discrete undo step). Used by
   * callers that mutate `w.config` without going through a full re-render (e.g.
   * Ink Layer's targeted annotation redraws, which fire no 'updated' event).
   * No-op when disabled or while a restore is applying.
   * @param {string} [label]
   */
  snapshot(label) {
    if (!this.enabled || this.applying) return;
    this._commit(label || "change");
  }
  /**
   * @param {boolean} [animate]
   */
  undo(animate = true) {
    if (!this.canUndo()) return;
    this.pointer--;
    this._restore(this.stack[this.pointer], animate);
  }
  /**
   * @param {boolean} [animate]
   */
  redo(animate = true) {
    if (!this.canRedo()) return;
    this.pointer++;
    this._restore(this.stack[this.pointer], animate);
  }
  canUndo() {
    return this.pointer > 0;
  }
  canRedo() {
    return this.pointer > -1 && this.pointer < this.stack.length - 1;
  }
  /**
   * @param {string} id
   * @param {boolean} [animate]
   */
  jump(id, animate = true) {
    const idx = this.stack.findIndex((c2) => c2.id === id);
    if (idx === -1 || idx === this.pointer) return;
    this.pointer = idx;
    this._restore(this.stack[idx], animate);
  }
  /** Clear the history, keeping the current state as the new baseline. */
  clear() {
    clearTimeout(this._coalesceTimer);
    this._coalesceTimer = null;
    const current = this.stack[this.pointer];
    this.stack = current ? [current] : [];
    this.pointer = this.stack.length - 1;
    this._emitChange();
  }
  /**
   * Group multiple edits into a single undo step. `fn` may be async; await your
   * updateOptions/updateSeries calls inside it so the intermediate 'updated'
   * events are suppressed and only one checkpoint is committed afterwards.
   * @param {() => (void | Promise<any>)} fn
   * @param {{ label?: string }} [opts]
   * @returns {Promise<void>}
   */
  transaction(fn, opts = {}) {
    if (typeof fn !== "function") return Promise.resolve();
    const wasBatching = this._batching;
    this._batching = true;
    return Promise.resolve().then(() => fn()).finally(() => {
      this._batching = wasBatching;
      if (!wasBatching) this._commit(opts.label || "transaction", true);
    });
  }
  /**
   * @returns {{ id: string, label: string, at: number }[]}
   */
  entries() {
    return this.stack.map((c2) => ({ id: c2.id, label: c2.label, at: c2.at }));
  }
  /** Lightweight state for the historyChange event / a history-rail UI. */
  state() {
    return {
      canUndo: this.canUndo(),
      canRedo: this.canRedo(),
      index: this.pointer,
      length: this.stack.length
    };
  }
  _emitChange() {
    this.ctx.events.fireEvent("historyChange", [this.ctx, this.state()]);
  }
  /** Drop the stack + detach listeners (called on full destroy). */
  teardown() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    clearTimeout(this._coalesceTimer);
    clearTimeout(this._settleTimer);
    this._coalesceTimer = null;
    this._settleTimer = null;
    this._unwireKeyboard();
    if (this._wired) {
      (_b = (_a = this.ctx).removeEventListener) == null ? void 0 : _b.call(_a, "mounted", this._onMounted);
      (_d = (_c = this.ctx).removeEventListener) == null ? void 0 : _d.call(_c, "updated", this._onUpdated);
      (_f = (_e = this.ctx).removeEventListener) == null ? void 0 : _f.call(_e, "scrolled", this._onUpdated);
      (_h = (_g = this.ctx).removeEventListener) == null ? void 0 : _h.call(_g, "dataPointSelection", this._onSelection);
    }
    this.stack = [];
    this.pointer = -1;
    this._wired = false;
  }
}
ApexCharts.registerFeatures({ history: History });
const WEAVE_API_VERSION = 1;
const PLUGIN_CHART_METHODS = [
  "updateOptions",
  "updateSeries",
  "appendData",
  "appendSeries",
  "toggleSeries",
  "showSeries",
  "hideSeries",
  "highlightSeries",
  "isSeriesHidden",
  "zoomX",
  "addXaxisAnnotation",
  "addYaxisAnnotation",
  "addPointAnnotation",
  "clearAnnotations",
  "removeAnnotation",
  "dataURI",
  "exportToCSV"
];
function buildBoundPublicMethods(ctx) {
  const out = {};
  PLUGIN_CHART_METHODS.forEach((m2) => {
    if (typeof ctx[m2] === "function") out[m2] = ctx[m2].bind(ctx);
  });
  return Object.freeze(out);
}
function makeLayerHandle(g2, graphics) {
  const add = (el) => {
    if (el) g2.add(el);
    return el;
  };
  const handle = {
    get node() {
      return g2.node;
    },
    /** @param {any} opts */
    path(opts = {}) {
      const {
        d = "",
        stroke = "#000",
        width = 1,
        fill = "none",
        opacity = 1,
        dash = 0,
        className = ""
      } = opts;
      return add(
        graphics.drawPath({
          d,
          stroke,
          strokeWidth: width,
          fill,
          fillOpacity: fill === "none" ? 0 : opacity,
          strokeOpacity: opacity,
          strokeDashArray: dash,
          classes: className
        })
      );
    },
    /** @param {any} opts */
    line(opts = {}) {
      const { x1, y1, x2, y2, stroke = "#000", width = 1, dash = 0 } = opts;
      return add(graphics.drawLine(x1, y1, x2, y2, stroke, dash, width));
    },
    /** @param {any} opts */
    rect(opts = {}) {
      const {
        x = 0,
        y = 0,
        w: w2 = 0,
        h: h2 = 0,
        r: r2 = 0,
        fill = "#000",
        stroke = null,
        opacity = 1
      } = opts;
      return add(
        graphics.drawRect(
          x,
          y,
          w2,
          h2,
          r2,
          fill,
          opacity,
          stroke != null ? 1 : null,
          stroke
        )
      );
    },
    /** @param {any} opts */
    circle(opts = {}) {
      const { cx = 0, cy = 0, r: r2 = 0, fill = "#000", stroke = null } = opts;
      return add(
        graphics.drawCircle(r2, { cx, cy, fill, stroke: stroke || "none" })
      );
    },
    /** @param {any} opts */
    text(opts = {}) {
      const {
        x = 0,
        y = 0,
        text = "",
        color,
        size,
        anchor = "start",
        weight
      } = opts;
      return add(
        graphics.drawText({
          x,
          y,
          text,
          textAnchor: anchor,
          fontSize: size,
          foreColor: color,
          fontWeight: weight
        })
      );
    },
    clear() {
      const node = g2.node;
      while (node.firstChild) node.removeChild(node.firstChild);
      return handle;
    }
  };
  return handle;
}
function buildPluginAPI(host, record) {
  const ctx = host.ctx;
  const w2 = host.w;
  const api = {
    name: record.def.name,
    version: WEAVE_API_VERSION,
    // Live: reconcile refreshes record.options when the chart's plugins config
    // changes, so updateOptions({ plugins: [{ name, options }] }) reconfigures
    // an active plugin in place. The returned object is frozen.
    get options() {
      return record.options;
    },
    // ── lifecycle subscription ──
    /**
     * @param {string} hook
     * @param {Function} fn
     */
    on(hook, fn) {
      const m2 = record.handlers;
      if (!m2.has(hook)) m2.set(hook, []);
      m2.get(hook).push(fn);
      return api;
    },
    /**
     * @param {string} hook
     * @param {Function} fn
     */
    off(hook, fn) {
      const a2 = record.handlers.get(hook);
      if (a2) {
        const i2 = a2.indexOf(fn);
        if (i2 > -1) a2.splice(i2, 1);
      }
      return api;
    },
    // ── per-plugin, per-chart scratch state (survives updates, dropped on
    //    destroy). The api object is frozen, but this object is mutable. ──
    store: {},
    // ── drawing (renderer-agnostic) ──
    // Call this INSIDE each draw handler: the host wipes plugin layers at the
    // start of every draw pass, so a handle cached across draws points at a
    // detached node and its writes vanish silently.
    /** @param {any} [opts] */
    layer(opts) {
      return host._layer(record.def.name, opts || {});
    },
    // ── reads ──
    get scales() {
      return host._currentScales;
    },
    // Served from the per-dispatch snapshot when one exists (invalidated at
    // every dispatch), so reading api.data in a loop does not rebuild the
    // point arrays on each property access.
    get data() {
      return host._lastData || (host._lastData = host._dataSnapshot());
    },
    theme: Object.freeze({
      get mode() {
        return w2.config.theme.mode;
      },
      get foreColor() {
        return w2.config.chart.foreColor;
      },
      /** @param {number} i */
      seriesColor(i2) {
        return w2.globals.colors[i2];
      },
      /** @param {string} name */
      token(name2) {
        return host._token(name2);
      }
    }),
    // ── curated actions (bound public methods only; NEVER raw w) ──
    chart: buildBoundPublicMethods(ctx),
    // ── custom events out to the host app ──
    /**
     * Fires as `plugin:<pluginName>:<name>` on the chart's event bus. The
     * namespace is not optional: the bus also carries the internal lifecycle
     * events ('updated', 'mounted', ...), and an un-namespaced emit could
     * trigger every internal subscriber (history capture, re-render hooks).
     * Listen with chart.addEventListener('plugin:myplugin:myevent', fn).
     * @param {string} name
     * @param {any} [detail]
     */
    emit(name2, detail) {
      ctx.events.fireEvent(`plugin:${record.def.name}:${name2}`, [ctx, detail]);
    },
    // ── host element (read; lazy: baseEl is not set until render) ──
    get el() {
      return w2.dom.baseEl;
    }
  };
  return Object.freeze(api);
}
class WeaveHost {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.active = [];
    this._layers = /* @__PURE__ */ new Map();
    this._currentScales = null;
    this._lastPluginsRef = null;
    this._lastData = null;
    this._updatedWired = false;
    this._onUpdated = this._onUpdated.bind(this);
    this._init();
  }
  _init() {
    const list = this.w.config.plugins || [];
    list.map((entry, i2) => ({
      entry,
      order: entry.order != null ? entry.order : i2
    })).sort((a2, b2) => a2.order - b2.order).forEach((o2) => this._activate(o2.entry));
    this._lastPluginsRef = this.w.config.plugins;
    this._wireUpdated();
  }
  _wireUpdated() {
    if (this._updatedWired) return;
    this.ctx.addEventListener("updated", this._onUpdated);
    this._updatedWired = true;
  }
  _onUpdated() {
    this.dispatch("afterUpdate", { pass: "update" });
  }
  /**
   * @param {any} entry { name, options?, order? }
   */
  _activate(entry) {
    const def = getPlugin(entry.name);
    if (!def) {
      console.error(`[apexcharts] plugin "${entry.name}" is not registered.`);
      return;
    }
    const v2 = def.apiVersion != null ? def.apiVersion : 1;
    if (Math.trunc(v2) !== WEAVE_API_VERSION) {
      console.error(
        `[apexcharts] plugin "${def.name}" targets Weave API v${v2}, host is v${WEAVE_API_VERSION}; skipped.`
      );
      return;
    }
    const record = {
      def,
      options: Object.freeze(__spreadValues({}, entry.options || {})),
      handlers: /* @__PURE__ */ new Map(),
      disabled: false,
      failures: 0,
      api: null
    };
    record.api = buildPluginAPI(this, record);
    this.active.push(record);
    this._guard(record, "setup", () => def.setup(record.api));
  }
  /**
   * @param {string} hook
   * @param {{ pass?: string, xyRatios?: any }} [extra]
   */
  dispatch(hook, extra) {
    if (hook === "draw") {
      this._reconcile();
      this._resetLayers();
    }
    this._lastData = null;
    if (!this.active.length) return;
    if (hook === "afterParse") {
      this._currentScales = null;
    } else if (extra && "xyRatios" in extra) {
      this._setScales(extra.xyRatios);
    }
    const pass = extra && extra.pass || "full";
    let data = null;
    for (const record of this.active) {
      if (record.disabled) continue;
      const fns = record.handlers.get(hook);
      if (!fns || !fns.length) continue;
      if (data === null) {
        data = this._dataSnapshot();
        this._lastData = data;
      }
      const payload = {
        api: record.api,
        scales: this._currentScales,
        data,
        pass,
        hook
      };
      for (const fn of fns.slice()) {
        this._guard(record, hook, () => fn(payload));
      }
    }
  }
  /**
   * @param {any} record
   * @param {string} where
   * @param {Function} fn
   */
  _guard(record, where, fn) {
    if (record.disabled) return;
    try {
      fn();
    } catch (e2) {
      console.error(
        `[apexcharts] plugin "${record.def.name}" threw in "${where}":`,
        e2
      );
      record.failures = (record.failures || 0) + 1;
      if (record.failures >= 3) {
        record.disabled = true;
        console.error(
          `[apexcharts] plugin "${record.def.name}" disabled after repeated errors.`
        );
      }
    }
  }
  // ─── Scales facade ──────────────────────────────────────────────────────
  /**
   * Build api.scales from the SAME xyRatios the series were drawn with, so
   * plugin pixels align with series pixels by construction.
   * @param {any} xyRatios
   */
  _setScales(xyRatios) {
    const w2 = this.w;
    const gl = w2.globals;
    const L2 = w2.layout;
    if (!xyRatios || !gl.axisCharts) {
      this._currentScales = null;
      return;
    }
    const xRatio = xyRatios.xRatio;
    const yRatio = xyRatios.yRatio || [];
    const yr = (axis) => yRatio[axis] != null ? yRatio[axis] : yRatio[0];
    const maxY = (axis) => gl.maxYArr[axis] != null ? gl.maxYArr[axis] : gl.maxY;
    const minY = (axis) => gl.minYArr[axis] != null ? gl.minYArr[axis] : gl.minY;
    this._currentScales = {
      /** @param {number} v */
      x: (v2) => L2.translateX + (v2 - gl.minX) / xRatio,
      /**
       * @param {number} v
       * @param {number} [axis]
       */
      y: (v2, axis = 0) => L2.translateY + (maxY(axis) - v2) / yr(axis),
      domainX: [gl.minX, gl.maxX],
      /** @param {number} [axis] */
      domainY: (axis = 0) => [minY(axis), maxY(axis)],
      gridWidth: L2.gridWidth,
      gridHeight: L2.gridHeight,
      ratios: xyRatios
    };
  }
  // ─── Read-only data snapshot ────────────────────────────────────────────
  /**
   * @returns {any[]} defensive per-series snapshot (never the live slice)
   */
  _dataSnapshot() {
    const w2 = this.w;
    const gl = w2.globals;
    const series = w2.seriesData.series || [];
    const seriesX = w2.seriesData.seriesX || [];
    return series.map((sData, i2) => {
      const xs = seriesX[i2] || [];
      const points = (sData || []).map((y, j) => ({
        x: xs[j] != null ? xs[j] : j,
        y
      }));
      return {
        name: gl.seriesNames ? gl.seriesNames[i2] : void 0,
        hidden: (gl.collapsedSeriesIndices || []).includes(i2),
        color: gl.colors ? gl.colors[i2] : void 0,
        points
      };
    });
  }
  // ─── Theme tokens ───────────────────────────────────────────────────────
  /**
   * @param {string} name
   * @returns {any}
   */
  _token(name2) {
    const w2 = this.w;
    const gl = w2.globals;
    switch (name2) {
      case "foreColor":
        return w2.config.chart.foreColor;
      case "background":
        return w2.config.chart.background;
      case "accent":
      case "primary":
        return gl.colors ? gl.colors[0] : void 0;
      default:
        if (/^series-\d+$/.test(name2)) {
          return gl.colors ? gl.colors[Number(name2.split("-")[1])] : void 0;
        }
        return void 0;
    }
  }
  // ─── Layers ─────────────────────────────────────────────────────────────
  /**
   * @param {string} name
   * @param {{ z?: 'front'|'behind', className?: string }} opts
   */
  _layer(name2, { z = "front", className = "" } = {}) {
    let g2 = this._layers.get(name2);
    if (!g2) {
      g2 = this.ctx.graphics.group({
        class: `apexcharts-plugin-${name2} ${className}`.trim()
      });
      const parent = this.w.dom.elGraphical.node;
      if (z === "behind") parent.insertBefore(g2.node, parent.firstChild);
      else parent.appendChild(g2.node);
      g2.node.setAttribute("aria-hidden", "true");
      this._layers.set(name2, g2);
    }
    return makeLayerHandle(g2, this.ctx.graphics);
  }
  /**
   * Remove all plugin layers. Run at the start of every `draw` because
   * fastUpdate only removes series/data-label groups (not arbitrary plugin
   * groups), so without this, fast-path redraws would duplicate plugin output.
   */
  _resetLayers() {
    const el = this.w.dom.elGraphical;
    const parent = el && el.node;
    if (parent) {
      const groups = parent.querySelectorAll('g[class*="apexcharts-plugin-"]');
      Array.prototype.forEach.call(groups, (n2) => n2.remove());
    }
    this._layers.clear();
  }
  // ─── Config-change reconciliation ───────────────────────────────────────
  /**
   * Diff w.config.plugins by name: teardown removed, activate added; unchanged
   * plugins keep their instance + store, but their `options` are refreshed from
   * the new entry (api.options is a live getter), so
   * updateOptions({ plugins: [{ name, options }] }) reconfigures in place.
   * Skipped when the plugins array reference is unchanged (fast redraws), so it
   * costs nothing on hover.
   */
  _reconcile() {
    const plugins = this.w.config.plugins || [];
    if (plugins === this._lastPluginsRef) return;
    this._lastPluginsRef = plugins;
    const desired = new Map(
      plugins.map((e2, i2) => [
        e2.name,
        { entry: e2, order: e2.order != null ? e2.order : i2 }
      ])
    );
    for (let i2 = this.active.length - 1; i2 >= 0; i2--) {
      const r2 = this.active[i2];
      const want = desired.get(r2.def.name);
      if (!want) {
        this._guard(r2, "destroy", () => r2.def.destroy && r2.def.destroy(r2.api));
        this.active.splice(i2, 1);
      } else {
        r2.options = Object.freeze(__spreadValues({}, want.entry.options || {}));
      }
    }
    const activeNames = new Set(this.active.map((r2) => r2.def.name));
    const toAdd = [];
    desired.forEach((v2, name2) => {
      if (!activeNames.has(name2)) toAdd.push(v2);
    });
    toAdd.sort((a2, b2) => a2.order - b2.order).forEach((v2) => this._activate(v2.entry));
  }
  /**
   * @param {boolean} [isUpdating]
   */
  teardown(isUpdating) {
    if (!isUpdating) {
      this.dispatch("destroy");
      for (const record of this.active) {
        this._guard(record, "destroy", () => record.def.destroy && record.def.destroy(record.api));
      }
      this.active = [];
      if (this._updatedWired) {
        this.ctx.removeEventListener && this.ctx.removeEventListener("updated", this._onUpdated);
        this._updatedWired = false;
      }
    }
    this._layers.clear();
  }
}
ApexCharts.registerFeatures({ weave: WeaveHost });
const STYLE_KEYS = {
  fill: "fill",
  stroke: "stroke",
  "stroke-width": "strokeWidth",
  "stroke-dasharray": "strokeDash",
  "stroke-linecap": "lineCap",
  "fill-opacity": "fillOpacity",
  "stroke-opacity": "strokeOpacity",
  "fill-rule": "fillRule"
};
const NEVER = Symbol("never");
const SHAPE_ID = {
  circle: 0,
  square: 1,
  rect: 1,
  triangle: 2,
  diamond: 3,
  star: 4,
  sparkle: 5,
  cross: 6,
  plus: 7,
  line: 8
};
const SHAPE_NAME = [
  "circle",
  "square",
  "triangle",
  "diamond",
  "star",
  "sparkle",
  "cross",
  "plus",
  "line"
];
const NOOP_RUNNER = {
  /** @returns {any} */
  attr() {
    return NOOP_RUNNER;
  },
  plot() {
    return NOOP_RUNNER;
  },
  during() {
    return NOOP_RUNNER;
  },
  after(fn) {
    if (typeof fn === "function") fn();
    return NOOP_RUNNER;
  },
  animate() {
    return NOOP_RUNNER;
  },
  delay() {
    return NOOP_RUNNER;
  },
  loop() {
    return NOOP_RUNNER;
  },
  finish() {
    return NOOP_RUNNER;
  },
  stop() {
    return NOOP_RUNNER;
  }
};
const SHARED_MARKER_NODE = {
  nodeName: "path",
  style: {},
  classList: { add() {
  }, remove() {
  }, toggle() {
  }, contains: () => false },
  setAttribute() {
  },
  getAttribute: () => null,
  removeAttribute() {
  },
  hasAttribute: () => false,
  addEventListener() {
  },
  removeEventListener() {
  },
  appendChild() {
  },
  getBBox: () => ({ x: 0, y: 0, width: 0, height: 0 })
};
const SHARED_GROUP = {
  __isCanvasMark: true,
  node: {
    nodeName: "g",
    instance: null,
    style: {},
    classList: { add() {
    }, remove() {
    }, toggle() {
    }, contains: () => false },
    setAttribute() {
    },
    getAttribute: () => null,
    removeAttribute() {
    },
    addEventListener() {
    },
    removeEventListener() {
    },
    appendChild() {
    },
    getBBox: () => ({ x: 0, y: 0, width: 0, height: 0 })
  },
  /** @returns {any} */
  attr() {
    return SHARED_GROUP;
  },
  add() {
    return SHARED_GROUP;
  },
  addTo() {
    return SHARED_GROUP;
  },
  remove() {
    return SHARED_GROUP;
  },
  clear() {
    return SHARED_GROUP;
  },
  css() {
    return SHARED_GROUP;
  },
  hide() {
    return SHARED_GROUP;
  },
  show() {
    return SHARED_GROUP;
  },
  removeClass() {
    return SHARED_GROUP;
  },
  animate() {
    return NOOP_RUNNER;
  }
};
class CanvasMarkerRef {
  /**
   * @param {CanvasGraphics} g
   * @param {number} i
   */
  constructor(g2, i2) {
    this.__isCanvasMark = true;
    this._g = g2;
    this._i = i2;
  }
  get node() {
    return SHARED_MARKER_NODE;
  }
  /**
   * @param {any} a
   * @param {any} [v]
   * @returns {any}
   */
  attr(a2, v2) {
    if (typeof a2 === "string") {
      if (a2 === "fill" && v2 !== void 0) this._g._setMarkerFill(this._i, v2);
      return v2 === void 0 ? null : this;
    }
    if (a2 && a2.fill !== void 0) this._g._setMarkerFill(this._i, a2.fill);
    return this;
  }
  /** @param {any} _c */
  add(_c) {
    return this;
  }
  /** @param {any} _p */
  addTo(_p) {
    return this;
  }
  remove() {
    return this;
  }
  /** @param {any} _s */
  css(_s) {
    return this;
  }
  /** @param {any} _v */
  fill(_v) {
    if (_v !== void 0) this._g._setMarkerFill(this._i, _v);
    return this;
  }
  /** @param {any} _v */
  stroke(_v) {
    return this;
  }
  hide() {
    return this;
  }
  show() {
    return this;
  }
  /** @param {string} _c */
  removeClass(_c) {
    return this;
  }
  animate() {
    return NOOP_RUNNER;
  }
}
const SHARED_RECT_REF = {
  __isCanvasMark: true,
  node: SHARED_MARKER_NODE,
  /** @returns {any} */
  attr() {
    return SHARED_RECT_REF;
  },
  add() {
    return SHARED_RECT_REF;
  },
  addTo() {
    return SHARED_RECT_REF;
  },
  remove() {
    return SHARED_RECT_REF;
  },
  /** @returns {any} */
  css() {
    return SHARED_RECT_REF;
  },
  animate() {
    return NOOP_RUNNER;
  }
};
class CanvasMark {
  /** @param {any} cmd */
  constructor(cmd) {
    this.__isCanvasMark = true;
    this._cmd = cmd;
    const self = this;
    this.node = {
      nodeName: cmd ? cmd.tag : "g",
      instance: this,
      style: {},
      classList: { add() {
      }, remove() {
      }, toggle() {
      }, contains: () => false },
      /** @param {string} k @param {any} v */
      setAttribute(k, v2) {
        self._applyAttr(k, v2);
      },
      getAttribute: () => null,
      removeAttribute() {
      },
      hasAttribute: () => false,
      addEventListener() {
      },
      removeEventListener() {
      },
      appendChild() {
      },
      getBBox: () => ({ x: 0, y: 0, width: 0, height: 0 })
    };
  }
  /**
   * @param {string} k
   * @param {any} v
   */
  _applyAttr(k, v2) {
    const cmd = this._cmd;
    if (!cmd) return;
    const sk = STYLE_KEYS[k];
    if (sk !== void 0) cmd[sk] = v2;
  }
  /**
   * @param {any} a
   * @param {any} [v]
   * @returns {any}
   */
  attr(a2, v2) {
    if (typeof a2 === "string") {
      if (v2 === void 0) return null;
      this._applyAttr(a2, v2);
      return this;
    }
    for (const k in a2) {
      if (a2[k] !== void 0) this._applyAttr(k, a2[k]);
    }
    return this;
  }
  /** @param {any} _c */
  add(_c) {
    return this;
  }
  /** @param {any} _p */
  addTo(_p) {
    return this;
  }
  remove() {
    return this;
  }
  clear() {
    return this;
  }
  /** @param {any} _s */
  css(_s) {
    return this;
  }
  /** @param {any} v */
  fill(v2) {
    if (typeof v2 === "object") return this.attr(v2);
    return this.attr("fill", v2);
  }
  /** @param {any} v */
  stroke(v2) {
    if (typeof v2 === "object") {
      if (v2.color !== void 0) this.attr("stroke", v2.color);
      if (v2.width !== void 0) this.attr("stroke-width", v2.width);
      return this;
    }
    return this.attr("stroke", v2);
  }
  /** @param {string} d */
  plot(d) {
    if (typeof d === "string" && this._cmd && this._cmd.tag === "path") {
      this._cmd.d = d;
    }
    return this;
  }
  hide() {
    return this;
  }
  show() {
    return this;
  }
  /** @param {string} _c */
  removeClass(_c) {
    return this;
  }
  bbox() {
    return { x: 0, y: 0, width: 0, height: 0 };
  }
  animate() {
    return NOOP_RUNNER;
  }
}
class CanvasGraphics {
  /** @param {any} w */
  constructor(w2) {
    this.w = w2;
    this._g = new Graphics(w2);
    this._list = [];
    this._mx = new Float64Array(16);
    this._my = new Float64Array(16);
    this._msize = new Float64Array(16);
    this._mshape = new Int16Array(16);
    this._mstyle = new Int32Array(16);
    this._msi = new Int32Array(16);
    this._mn = 0;
    this._mcap = 16;
    this._crx = new Float64Array(16);
    this._cry = new Float64Array(16);
    this._crw = new Float64Array(16);
    this._crh = new Float64Array(16);
    this._crstyle = new Int32Array(16);
    this._crsi = new Int32Array(16);
    this._crdi = new Int32Array(16);
    this._crn = 0;
    this._crcap = 16;
    this._cellRadius = 0;
    this._styles = [];
    this._styleMap = /* @__PURE__ */ new Map();
    this._lf = NEVER;
    this._ls = NEVER;
    this._lsw = NEVER;
    this._ld = NEVER;
    this._lfo = NEVER;
    this._lso = NEVER;
    this._lid = -1;
    this._lofFill = NEVER;
    this._lofBase = -1;
    this._lofId = -1;
    this._rlf = NEVER;
    this._rls = NEVER;
    this._rlsw = NEVER;
    this._rlfo = NEVER;
    this._rlso = NEVER;
    this._rlid = -1;
  }
  _resetStyleCache() {
    this._lf = NEVER;
    this._ls = NEVER;
    this._lsw = NEVER;
    this._ld = NEVER;
    this._lfo = NEVER;
    this._lso = NEVER;
    this._lid = -1;
    this._lofFill = NEVER;
    this._lofBase = -1;
    this._lofId = -1;
    this._rlf = NEVER;
    this._rls = NEVER;
    this._rlsw = NEVER;
    this._rlfo = NEVER;
    this._rlso = NEVER;
    this._rlid = -1;
  }
  /** Start a fresh scene (columnar marker store + object-command list). */
  reset() {
    this._list = [];
    this._mn = 0;
    this._styles = [];
    this._styleMap = /* @__PURE__ */ new Map();
    this._resetStyleCache();
    const series = this.w.config.series || [];
    let cap = 16;
    for (let i2 = 0; i2 < series.length; i2++) {
      const d = series[i2] && series[i2].data;
      if (Array.isArray(d)) cap += d.length;
    }
    cap = Math.ceil(cap * 1.15) + 16;
    if (cap > this._mcap) this._allocMarkers(cap);
    this._crn = 0;
    this._cellRadius = 0;
    if (cap > this._crcap) this._allocRects(cap);
  }
  /** @param {number} cap */
  _allocRects(cap) {
    this._crcap = cap;
    this._crx = new Float64Array(cap);
    this._cry = new Float64Array(cap);
    this._crw = new Float64Array(cap);
    this._crh = new Float64Array(cap);
    this._crstyle = new Int32Array(cap);
    this._crsi = new Int32Array(cap);
    this._crdi = new Int32Array(cap);
  }
  /** Grow the rect columns (rare: capacity estimate was low). */
  _growRects() {
    const cap = this._crcap * 2;
    const nx = new Float64Array(cap);
    nx.set(this._crx);
    this._crx = nx;
    const ny = new Float64Array(cap);
    ny.set(this._cry);
    this._cry = ny;
    const nw = new Float64Array(cap);
    nw.set(this._crw);
    this._crw = nw;
    const nh = new Float64Array(cap);
    nh.set(this._crh);
    this._crh = nh;
    const nst = new Int32Array(cap);
    nst.set(this._crstyle);
    this._crstyle = nst;
    const nsi = new Int32Array(cap);
    nsi.set(this._crsi);
    this._crsi = nsi;
    const ndi = new Int32Array(cap);
    ndi.set(this._crdi);
    this._crdi = ndi;
    this._crcap = cap;
  }
  /** @param {number} cap */
  _allocMarkers(cap) {
    this._mcap = cap;
    this._mx = new Float64Array(cap);
    this._my = new Float64Array(cap);
    this._msize = new Float64Array(cap);
    this._mshape = new Int16Array(cap);
    this._mstyle = new Int32Array(cap);
    this._msi = new Int32Array(cap);
  }
  /** Grow the marker columns (rare: capacity estimate was low). */
  _growMarkers() {
    const cap = this._mcap * 2;
    const nx = new Float64Array(cap);
    nx.set(this._mx);
    this._mx = nx;
    const ny = new Float64Array(cap);
    ny.set(this._my);
    this._my = ny;
    const ns = new Float64Array(cap);
    ns.set(this._msize);
    this._msize = ns;
    const nsh = new Int16Array(cap);
    nsh.set(this._mshape);
    this._mshape = nsh;
    const nst = new Int32Array(cap);
    nst.set(this._mstyle);
    this._mstyle = nst;
    const nsi = new Int32Array(cap);
    nsi.set(this._msi);
    this._msi = nsi;
    this._mcap = cap;
  }
  displayList() {
    return this._list;
  }
  markerCount() {
    return this._mn;
  }
  /**
   * Intern a marker style; returns its palette id. Keeps the per-point columns
   * numeric (no retained per-point object).
   * @param {any} fill @param {any} stroke @param {any} sw @param {any} dash
   * @param {any} fo @param {any} so
   * @returns {number}
   */
  _internStyle(fill, stroke, sw, dash, fo, so) {
    const key = `${fill}|${stroke}|${sw}|${dash}|${fo}|${so}`;
    const cached = this._styleMap.get(key);
    if (cached !== void 0) return cached;
    const id = this._styles.length;
    this._styles.push({
      fill,
      stroke,
      strokeWidth: sw,
      strokeDash: dash,
      fillOpacity: fo,
      strokeOpacity: so
    });
    this._styleMap.set(key, id);
    return id;
  }
  /**
   * Override a recorded marker's fill (scatter sets a per-point fill via `attr`
   * right after drawMarker). Like the draw path, the string-key intern is cached
   * on (base style, fill) so the Map/string work stays OFF the per-point path
   * (it otherwise mixes with the `_mstyle[i]` typed-array write → the ~80× slow
   * path). For a scatter series the base + fill are uniform, so it interns once.
   * @param {number} i @param {any} fill
   */
  _setMarkerFill(i2, fill) {
    const base = this._mstyle[i2];
    if (fill === this._lofFill && base === this._lofBase) {
      this._mstyle[i2] = this._lofId;
      return;
    }
    const s2 = this._styles[base];
    if (!s2 || s2.fill === fill) {
      this._lofFill = fill;
      this._lofBase = base;
      this._lofId = base;
      return;
    }
    const id = this._internStyle(
      fill,
      s2.stroke,
      s2.strokeWidth,
      s2.strokeDash,
      s2.fillOpacity,
      s2.strokeOpacity
    );
    this._mstyle[i2] = id;
    this._lofFill = fill;
    this._lofBase = base;
    this._lofId = id;
  }
  /** @param {number} i @returns {any} the style object for a marker index */
  markerStyle(i2) {
    return this._styles[this._mstyle[i2]];
  }
  /** @param {number} i @returns {number} series (realIndex) of a marker, -1 if none */
  markerSeries(i2) {
    return this._msi[i2];
  }
  /** @param {number} id @returns {string} */
  shapeName(id) {
    return SHAPE_NAME[id] || "circle";
  }
  // ── columnar rect cell (heatmap): parallel unboxed arrays, no per-cell object ──
  /**
   * Record a heatmap-style cell (a filled, optionally stroked rect). Geometry
   * and style are captured up front into the columns; the returned handle is a
   * shared no-op (the emit site sets nothing back on it in canvas mode).
   * @param {number} x @param {number} y @param {number} w @param {number} h
   * @param {any} opts {fill, fillOpacity, stroke, strokeWidth, radius, seriesIndex, dataPointIndex}
   * @returns {any}
   */
  drawRectCell(x, y, w2, h2, opts = {}) {
    const styleId = this._rectStyleId(opts);
    if (this._crn >= this._crcap) this._growRects();
    const i2 = this._crn++;
    this._crx[i2] = x || 0;
    this._cry[i2] = y || 0;
    this._crw[i2] = w2 > 0 ? w2 : 0;
    this._crh[i2] = h2 > 0 ? h2 : 0;
    this._crstyle[i2] = styleId;
    this._crsi[i2] = opts.seriesIndex == null ? -1 : opts.seriesIndex;
    this._crdi[i2] = opts.dataPointIndex == null ? -1 : opts.dataPointIndex;
    if (opts.radius) this._cellRadius = opts.radius;
    return SHARED_RECT_REF;
  }
  /**
   * Resolve (and dedupe) a rect-cell style → shared-palette id. A last-style
   * cache keeps the Map/string work off the path for runs of same-style cells.
   * @param {any} opts
   * @returns {number}
   */
  _rectStyleId(opts) {
    const fill = opts.fill;
    const stroke = opts.stroke;
    const sw = opts.strokeWidth;
    const fo = opts.fillOpacity;
    const so = opts.strokeOpacity;
    if (fill === this._rlf && stroke === this._rls && sw === this._rlsw && fo === this._rlfo && so === this._rlso) {
      return this._rlid;
    }
    const id = this._internStyle(fill, stroke, sw, 0, fo, so);
    this._rlf = fill;
    this._rls = stroke;
    this._rlsw = sw;
    this._rlfo = fo;
    this._rlso = so;
    this._rlid = id;
    return id;
  }
  /** @returns {number} number of recorded rect cells */
  rectCount() {
    return this._crn;
  }
  /** @param {number} i @returns {any} the style object for a rect cell */
  rectStyle(i2) {
    return this._styles[this._crstyle[i2]];
  }
  /** @param {number} i @returns {number} series (realIndex) of a cell, -1 if none */
  rectSeries(i2) {
    return this._crsi[i2];
  }
  /**
   * @param {string} tag
   * @param {number} z
   * @returns {any}
   */
  _cmd(tag, z) {
    const cmd = {
      tag,
      z: z || 0,
      fill: void 0,
      stroke: void 0,
      strokeWidth: void 0,
      strokeDash: void 0,
      lineCap: void 0,
      fillOpacity: void 0,
      strokeOpacity: void 0,
      fillRule: void 0
    };
    this._list.push(cmd);
    return cmd;
  }
  // ── organizational (groups don't paint or record) ──
  // Series draw() creates a wrap group PER POINT (scatter.draw / plotChartMarkers
  // run per point), so allocating a handle per group is ~50k heavy allocations
  // at scale: enough transient churn to tip V8 into a GC blow-up. Groups carry
  // no paint state in canvas mode (attr/add are no-ops), so every group shares
  // one singleton: zero per-point allocation.
  /** @param {any} _attrs */
  group(_attrs) {
    return SHARED_GROUP;
  }
  // ── per-point marker (line/area markers, scatter, bubble): COLUMNAR ──
  /**
   * @param {number} x
   * @param {number} y
   * @param {any} opts
   */
  drawMarker(x, y, opts = {}) {
    var _a;
    const styleId = this._markerStyleId(opts);
    if (this._mn >= this._mcap) this._growMarkers();
    const i2 = this._mn++;
    this._mx[i2] = x || 0;
    this._my[i2] = typeof y === "number" ? y : NaN;
    this._msize[i2] = opts.pSize || 0;
    this._mshape[i2] = (_a = SHAPE_ID[opts.shape || "circle"]) != null ? _a : 0;
    this._mstyle[i2] = styleId;
    this._msi[i2] = opts.seriesIndex == null ? -1 : opts.seriesIndex;
    return new CanvasMarkerRef(this, i2);
  }
  /**
   * Resolve (and dedupe) a marker style → palette id. A last-style cache keeps
   * the Map/string work off the path when consecutive markers share a style
   * (the common case), so intern runs once per style run.
   * @param {any} opts
   * @returns {number}
   */
  _markerStyleId(opts) {
    const shape = opts.shape || "circle";
    const strokeTinted = shape === "line" || shape === "plus" || shape === "cross";
    const fill = strokeTinted ? "none" : opts.pointFillColor;
    const stroke = strokeTinted ? opts.pointFillColor : opts.pointStrokeColor;
    const sw = opts.pointStrokeWidth;
    const dash = opts.pointStrokeDashArray;
    const fo = opts.pointFillOpacity;
    const so = strokeTinted ? opts.pointFillOpacity : opts.pointStrokeOpacity;
    if (fill === this._lf && stroke === this._ls && sw === this._lsw && dash === this._ld && fo === this._lfo && so === this._lso) {
      return this._lid;
    }
    const id = this._internStyle(fill, stroke, sw, dash, fo, so);
    this._lf = fill;
    this._ls = stroke;
    this._lsw = sw;
    this._ld = dash;
    this._lfo = fo;
    this._lso = so;
    this._lid = id;
    return id;
  }
  // ── series body path (line/area/bar): object command ──
  /** @param {any} opts */
  renderPaths(opts) {
    const cmd = this._cmd("path", opts.realIndex);
    cmd.d = opts.pathTo;
    if (opts.pathToNumeric) {
      cmd.nxs = opts.pathToNumeric.xs;
      cmd.nys = opts.pathToNumeric.ys;
      cmd.ncloseY = opts.pathToNumeric.closeY;
    }
    cmd.stroke = opts.stroke;
    cmd.strokeWidth = opts.strokeWidth;
    cmd.fill = opts.fill;
    cmd.lineCap = opts.strokeLinecap;
    cmd.si = opts.realIndex;
    this.w.globals.animationEnded = true;
    return new CanvasMark(cmd);
  }
  /** @param {any} opts */
  drawPath(opts) {
    const cmd = this._cmd("path", 0);
    cmd.d = opts.d;
    cmd.stroke = opts.stroke;
    cmd.strokeWidth = opts.strokeWidth;
    cmd.fill = opts.fill;
    cmd.fillOpacity = opts.fillOpacity;
    cmd.strokeOpacity = opts.strokeOpacity;
    cmd.lineCap = opts.strokeLinecap;
    cmd.strokeDash = opts.strokeDashArray;
    return new CanvasMark(cmd);
  }
  // ── remaining primitives (contract completeness / Marks #11 forward-compat) ──
  /**
   * @param {number} x1 @param {number} y1 @param {number} x2 @param {number} y2
   * @param {string} lineColor @param {any} dashArray @param {number} strokeWidth
   */
  drawLine(x1, y1, x2, y2, lineColor = "#a8a8a8", dashArray = 0, strokeWidth = 1) {
    const cmd = this._cmd("line", 0);
    cmd.lx1 = x1;
    cmd.ly1 = y1;
    cmd.lx2 = x2;
    cmd.ly2 = y2;
    cmd.stroke = lineColor;
    cmd.strokeDash = dashArray;
    cmd.strokeWidth = strokeWidth;
    return new CanvasMark(cmd);
  }
  /**
   * Mirrors Graphics.drawRect's full signature (including stroke), so callers
   * that stroke rects (Marks api.rect, chart code) paint the same on canvas.
   * @param {number} x1 @param {number} y1 @param {number} x2 @param {number} y2
   * @param {number} radius @param {string} color @param {number} opacity
   * @param {number|null} [strokeWidth] @param {string|null} [strokeColor]
   * @param {any} [strokeDashArray]
   */
  drawRect(x1 = 0, y1 = 0, x2 = 0, y2 = 0, radius = 0, color = "#fefefe", opacity = 1, strokeWidth = null, strokeColor = null, strokeDashArray = 0) {
    const cmd = this._cmd("rect", 0);
    cmd.x1 = x1;
    cmd.y1 = y1;
    cmd.rw = x2 > 0 ? x2 : 0;
    cmd.rh = y2 > 0 ? y2 : 0;
    cmd.radius = radius;
    cmd.fill = color;
    cmd.fillOpacity = opacity;
    if (strokeColor != null) {
      cmd.stroke = strokeColor;
      cmd.strokeWidth = strokeWidth == null ? 1 : strokeWidth;
      cmd.strokeDash = strokeDashArray;
    }
    return new CanvasMark(cmd);
  }
  /**
   * @param {number} radius
   * @param {any} attrs
   */
  drawCircle(radius, attrs = null) {
    const cmd = this._cmd("circle", 0);
    cmd.r = radius < 0 ? 0 : radius;
    if (attrs) {
      cmd.cx = attrs.cx;
      cmd.cy = attrs.cy;
      if (attrs.fill !== void 0) cmd.fill = attrs.fill;
      if (attrs.stroke !== void 0) cmd.stroke = attrs.stroke;
    }
    return new CanvasMark(cmd);
  }
  /** @param {any} opts */
  drawText(opts) {
    const cmd = this._cmd("text", 0);
    cmd.text = Array.isArray(opts.text) ? opts.text.join(" ") : opts.text;
    cmd.tx = opts.x;
    cmd.ty = opts.y;
    cmd.textAnchor = opts.textAnchor || "start";
    cmd.fontSize = opts.fontSize;
    cmd.fontFamily = opts.fontFamily;
    cmd.fill = opts.foreColor;
    return new CanvasMark(cmd);
  }
  /**
   * Resolve a marker's SVG path `d` (non-circle shapes) lazily at paint time.
   * @param {number} x @param {number} y @param {number} shapeId @param {number} size
   * @returns {string}
   */
  markerPath(x, y, shapeId, size) {
    return this._g.getMarkerPath(x, y, SHAPE_NAME[shapeId] || "circle", size);
  }
}
const TWO_PI = Math.PI * 2;
const DPR_CAP = 2;
class CanvasCompositor {
  /** @param {any} w */
  constructor(w2) {
    this.w = w2;
    this._host = null;
    this._canvas = null;
    this._c2d = null;
    this._margin = 0;
    this._dpr = 1;
    this._dim = null;
    this._alpha = 1;
    this._unitPaths = /* @__PURE__ */ new Map();
    this._markerBatches = 0;
  }
  /** Marker style-batches applied during the last paint() (dev/test hook). */
  markerBatchCount() {
    return this._markerBatches;
  }
  /**
   * Opacity multiplier for a series index under the active dim spec: 1 for the
   * highlighted series (or when not dimming, or for unidentified marks), else
   * the inactive opacity.
   * @param {number} si
   * @returns {number}
   */
  _seriesAlpha(si) {
    const d = this._dim;
    if (!d || d.active == null || d.active < 0 || si == null || si < 0) return 1;
    return si === d.active ? 1 : d.opacity == null ? 0.2 : d.opacity;
  }
  _plotDims() {
    var _a;
    const gw = Math.max(0, Math.ceil(this.w.layout.gridWidth || 0));
    const gh = Math.max(0, Math.ceil(this.w.layout.gridHeight || 0));
    const largest = ((_a = this.w.globals.markers) == null ? void 0 : _a.largestSize) || 0;
    const margin = Math.ceil(largest + 8);
    return { gw, gh, margin };
  }
  /**
   * Create (or recreate) the foreignObject + canvas sized to the plot rect and
   * return the SVGElement host that `plotChartType` inserts into the tree.
   * @returns {any}
   */
  createHost() {
    const win = BrowserAPIs.getWindow();
    this._dpr = Math.min(DPR_CAP, win && win.devicePixelRatio || 1);
    const { gw, gh, margin } = this._plotDims();
    this._margin = margin;
    const w2 = gw + margin * 2;
    const h2 = gh + margin * 2;
    const fo = BrowserAPIs.createElementNS(SVGNS$2, "foreignObject");
    fo.setAttribute("x", String(-margin));
    fo.setAttribute("y", String(-margin));
    fo.setAttribute("width", String(w2));
    fo.setAttribute("height", String(h2));
    fo.setAttribute("class", "apexcharts-canvas-series");
    fo.style.overflow = "visible";
    const canvas = (
      /** @type {any} */
      BrowserAPIs.createElement("canvas")
    );
    canvas.setAttribute("class", "apexcharts-series-canvas");
    canvas.width = Math.max(1, Math.round(w2 * this._dpr));
    canvas.height = Math.max(1, Math.round(h2 * this._dpr));
    canvas.style.width = w2 + "px";
    canvas.style.height = h2 + "px";
    canvas.style.pointerEvents = "none";
    fo.appendChild(canvas);
    this._canvas = canvas;
    this._c2d = canvas.getContext("2d");
    this._host = new SVGElement(fo);
    return this._host;
  }
  getHost() {
    return this._host;
  }
  clear() {
    if (!this._c2d || !this._canvas) return;
    this._c2d.setTransform(1, 0, 0, 1, 0, 0);
    this._c2d.clearRect(0, 0, this._canvas.width, this._canvas.height);
  }
  /**
   * Re-check devicePixelRatio before painting: a restyle() repaint after the
   * window moved between monitors would otherwise keep the stale backing-store
   * scale (blurry or over-sized) until the next full render rebuilds the host.
   * The canvas CSS size is unchanged; only the backing store is resized.
   */
  _syncDpr() {
    if (!this._canvas) return;
    const win = BrowserAPIs.getWindow();
    const dpr = Math.min(DPR_CAP, win && win.devicePixelRatio || 1);
    if (dpr === this._dpr) return;
    this._dpr = dpr;
    const wCss = parseFloat(this._canvas.style.width) || 0;
    const hCss = parseFloat(this._canvas.style.height) || 0;
    this._canvas.width = Math.max(1, Math.round(wCss * dpr));
    this._canvas.height = Math.max(1, Math.round(hCss * dpr));
  }
  /**
   * Paint the recorded scene: object commands (series bodies / rects / lines /
   * text) first, then the columnar markers on top (matching SVG z-order where
   * markers sit above the series path). `shim` supplies the columnar marker
   * arrays + lazy non-circle marker geometry.
   * @param {any[]} list
   * @param {any} shim
   * @param {{active:number, opacity:number}|null} [dim] per-series dim spec
   *   (hover / legend restyle); null repaints at full opacity.
   */
  paint(list, shim2, dim = null) {
    const ctx = this._c2d;
    if (!ctx) return;
    this._dim = dim || null;
    this._syncDpr();
    this.clear();
    const dpr = this._dpr;
    const m2 = this._margin;
    ctx.setTransform(dpr, 0, 0, dpr, m2 * dpr, m2 * dpr);
    if (list.length) {
      const ordered = list.length > 1 ? list.map((c2, i2) => [c2, i2]).sort(
        (a2, b2) => a2[0].z === b2[0].z ? a2[1] - b2[1] : a2[0].z - b2[0].z
      ).map((pair) => pair[0]) : list;
      for (let i2 = 0; i2 < ordered.length; i2++) {
        const c2 = ordered[i2];
        this._alpha = this._dim ? this._seriesAlpha(c2.si) : 1;
        this._paintOne(ctx, c2);
      }
    }
    this._paintRects(ctx, shim2);
    this._paintMarkers(ctx, shim2);
    this._alpha = 1;
  }
  /**
   * Paint the columnar rect cells (heatmap) as STYLE BATCHES: one fill/stroke
   * state application per run of consecutive same-style cells, then a fast
   * fillRect (or a roundRect path when the shared corner radius is non-zero)
   * per cell. Clipped to the plot rect so cells never bleed into the canvas
   * margin (mirrors the SVG gridRectMask). Per-cell globalAlpha carries the
   * hover/legend dim multiplier when a dim spec is active.
   * @param {any} ctx
   * @param {any} shim
   */
  _paintRects(ctx, shim2) {
    const n2 = shim2.rectCount ? shim2.rectCount() : 0;
    if (!n2) return;
    const rx = shim2._crx;
    const ry = shim2._cry;
    const rw = shim2._crw;
    const rh = shim2._crh;
    const rstyle = shim2._crstyle;
    const radius = shim2._cellRadius || 0;
    const cx = (
      /** @type {any} */
      ctx
    );
    const useRound = radius > 0 && typeof cx.roundRect === "function";
    const dimming = !!this._dim;
    const gw = Math.max(0, this.w.layout.gridWidth || 0);
    const gh = Math.max(0, this.w.layout.gridHeight || 0);
    ctx.save();
    ctx.beginPath();
    ctx.rect(0, 0, gw, gh);
    ctx.clip();
    let i2 = 0;
    while (i2 < n2) {
      const styleId = rstyle[i2];
      const style = shim2.rectStyle(i2);
      if (!style) {
        i2++;
        continue;
      }
      const fill = style.fill;
      const doFill = fill && fill !== "none" && !(typeof fill === "string" && fill.indexOf("url(") === 0);
      const stroke = style.stroke;
      const sw = style.strokeWidth == null ? 0 : Number(style.strokeWidth);
      const doStroke = stroke && stroke !== "none" && sw > 0 && !(typeof stroke === "string" && stroke.indexOf("url(") === 0);
      if (doFill) ctx.fillStyle = fill;
      if (doStroke) {
        ctx.strokeStyle = stroke;
        ctx.lineWidth = sw;
        ctx.setLineDash([]);
      }
      const baseFillA = style.fillOpacity == null ? 1 : Number(style.fillOpacity);
      const baseStrokeA = style.strokeOpacity == null ? 1 : Number(style.strokeOpacity);
      let j = i2;
      while (j < n2 && rstyle[j] === styleId) {
        const w2 = rw[j];
        const h2 = rh[j];
        if (w2 > 0 && h2 > 0) {
          const f = dimming ? this._seriesAlpha(shim2.rectSeries(j)) : 1;
          if (useRound) {
            ctx.beginPath();
            cx.roundRect(rx[j], ry[j], w2, h2, radius);
            if (doFill) {
              ctx.globalAlpha = baseFillA * f;
              ctx.fill();
            }
            if (doStroke) {
              ctx.globalAlpha = baseStrokeA * f;
              ctx.stroke();
            }
          } else {
            if (doFill) {
              ctx.globalAlpha = baseFillA * f;
              ctx.fillRect(rx[j], ry[j], w2, h2);
            }
            if (doStroke) {
              ctx.globalAlpha = baseStrokeA * f;
              ctx.strokeRect(rx[j], ry[j], w2, h2);
            }
          }
        }
        j++;
      }
      i2 = j;
    }
    ctx.globalAlpha = 1;
    ctx.restore();
  }
  /**
   * Reusable unit Path2D for a (shape, size): the shape's geometry built at the
   * origin once, then translated per marker via setTransform. Returns null when
   * the geometry string cannot be parsed.
   * @param {any} shim
   * @param {number} shapeId
   * @param {number} size
   * @returns {any}
   */
  _unitPath(shim2, shapeId, size) {
    const key = shapeId + "|" + size;
    let p2 = this._unitPaths.get(key);
    if (p2 === void 0) {
      try {
        p2 = new Path2D(shim2.markerPath(0, 0, shapeId, size));
      } catch (e2) {
        p2 = null;
      }
      this._unitPaths.set(key, p2);
    }
    return p2;
  }
  /**
   * Markers paint as STYLE BATCHES: one fill/stroke state application per run
   * of consecutive same-style markers (a uniform single-series scatter is
   * exactly one batch), then per-marker geometry inside the run. Per-marker
   * geometry stays painter's-ordered (fill+stroke per marker) so overlapping
   * semi-transparent markers composite exactly as SVG does.
   * @param {any} ctx
   * @param {any} shim
   */
  _paintMarkers(ctx, shim2) {
    this._markerBatches = 0;
    const n2 = shim2.markerCount();
    if (!n2) return;
    const mx = shim2._mx;
    const my = shim2._my;
    const msize = shim2._msize;
    const mshape = shim2._mshape;
    const mstyle = shim2._mstyle;
    const dimming = !!this._dim;
    if (!dimming) this._alpha = 1;
    let i2 = 0;
    while (i2 < n2) {
      const styleId = mstyle[i2];
      const shapeId = mshape[i2];
      const style = shim2.markerStyle(i2);
      if (!style) {
        i2++;
        continue;
      }
      const doFill = this._applyFill(ctx, style);
      const doStroke = this._applyStroke(ctx, style);
      this._markerBatches++;
      const baseFillA = style.fillOpacity == null ? 1 : Number(style.fillOpacity);
      const baseStrokeA = style.strokeOpacity == null ? 1 : Number(style.strokeOpacity);
      if (shapeId === 0) {
        let j = i2;
        while (j < n2 && mshape[j] === 0 && mstyle[j] === styleId) {
          const r2 = msize[j] || 0;
          const y = my[j];
          if (r2 > 0 && y === y) {
            ctx.beginPath();
            ctx.arc(mx[j], y, r2, 0, TWO_PI);
            if (dimming) {
              const f = this._seriesAlpha(shim2.markerSeries(j));
              if (doFill) {
                ctx.globalAlpha = baseFillA * f;
                ctx.fill();
              }
              if (doStroke) {
                ctx.globalAlpha = baseStrokeA * f;
                ctx.stroke();
              }
            } else {
              if (doFill) ctx.fill();
              if (doStroke) ctx.stroke();
            }
          }
          j++;
        }
        ctx.globalAlpha = 1;
        i2 = j;
      } else {
        const dpr = this._dpr;
        const m2 = this._margin;
        let j = i2;
        while (j < n2 && mshape[j] === shapeId && mstyle[j] === styleId) {
          const y = my[j];
          const size = msize[j];
          if (y === y && size > 0) {
            const p2 = this._unitPath(shim2, shapeId, size);
            if (p2) {
              ctx.setTransform(dpr, 0, 0, dpr, (m2 + mx[j]) * dpr, (m2 + y) * dpr);
              const f = dimming ? this._seriesAlpha(shim2.markerSeries(j)) : 1;
              if (doFill) {
                ctx.globalAlpha = baseFillA * f;
                ctx.fill(p2);
              }
              if (doStroke) {
                ctx.globalAlpha = baseStrokeA * f;
                ctx.stroke(p2);
              }
            }
          }
          j++;
        }
        ctx.setTransform(dpr, 0, 0, dpr, m2 * dpr, m2 * dpr);
        ctx.globalAlpha = 1;
        i2 = j;
      }
    }
  }
  /**
   * Paint a series path from its numeric fast-path coords: a direct
   * moveTo/lineTo loop over the typed arrays, no Path2D and no d-string
   * parse. `ncloseY` (areas) closes the polygon down to the baseline exactly
   * like the string form's `L xLast bottom L x0 bottom z` tail.
   * @param {any} ctx
   * @param {any} cmd
   */
  _paintNumericPath(ctx, cmd) {
    const xs = cmd.nxs;
    const ys = cmd.nys;
    const n2 = xs.length;
    if (!n2) return;
    ctx.beginPath();
    ctx.moveTo(xs[0], ys[0]);
    for (let k = 1; k < n2; k++) {
      ctx.lineTo(xs[k], ys[k]);
    }
    if (cmd.ncloseY != null) {
      ctx.lineTo(xs[n2 - 1], cmd.ncloseY);
      ctx.lineTo(xs[0], cmd.ncloseY);
      ctx.closePath();
    }
    if (this._applyFill(ctx, cmd)) {
      ctx.fill(cmd.fillRule === "evenodd" ? "evenodd" : "nonzero");
    }
    if (this._applyStroke(ctx, cmd)) {
      ctx.stroke();
    }
    ctx.globalAlpha = 1;
  }
  /**
   * @param {any} ctx
   * @param {any} cmd style-bearing flat command
   */
  _paintOne(ctx, cmd) {
    switch (cmd.tag) {
      case "path": {
        if (cmd.nxs) {
          this._paintNumericPath(ctx, cmd);
          break;
        }
        if (!cmd.d) return;
        if (!cmd.path2d) {
          try {
            cmd.path2d = new Path2D(cmd.d);
          } catch (e2) {
            return;
          }
        }
        this._fillStrokePath(ctx, cmd, cmd.path2d);
        break;
      }
      case "rect": {
        const p2 = new Path2D();
        if (cmd.radius && typeof /** @type {any} */
        p2.roundRect === "function") {
          p2.roundRect(cmd.x1, cmd.y1, cmd.rw, cmd.rh, cmd.radius);
        } else {
          p2.rect(cmd.x1, cmd.y1, cmd.rw, cmd.rh);
        }
        this._fillStrokePath(ctx, cmd, p2);
        break;
      }
      case "circle": {
        if (!(cmd.r > 0)) return;
        ctx.beginPath();
        ctx.arc(cmd.cx, cmd.cy, cmd.r, 0, TWO_PI);
        this._fillStroke(ctx, cmd);
        break;
      }
      case "line": {
        ctx.beginPath();
        ctx.moveTo(cmd.lx1, cmd.ly1);
        ctx.lineTo(cmd.lx2, cmd.ly2);
        this._strokeOnly(ctx, cmd);
        break;
      }
      case "text": {
        if (cmd.text == null) return;
        ctx.save();
        ctx.globalAlpha = this._alpha;
        ctx.fillStyle = cmd.fill || "#000";
        const size = cmd.fontSize || "11px";
        ctx.font = `${typeof size === "number" ? size + "px" : size} ${cmd.fontFamily || "Helvetica, Arial, sans-serif"}`;
        ctx.textAlign = cmd.textAnchor === "middle" ? "center" : cmd.textAnchor === "end" ? "right" : "left";
        ctx.fillText(String(cmd.text), cmd.tx, cmd.ty);
        ctx.restore();
        break;
      }
    }
  }
  /**
   * @param {any} ctx
   * @param {any} style
   * @param {any} path2d
   */
  _fillStrokePath(ctx, style, path2d) {
    if (this._applyFill(ctx, style)) {
      ctx.fill(path2d, style.fillRule === "evenodd" ? "evenodd" : "nonzero");
    }
    if (this._applyStroke(ctx, style)) {
      ctx.stroke(path2d);
    }
    ctx.globalAlpha = 1;
  }
  /**
   * @param {any} ctx
   * @param {any} style
   */
  _fillStroke(ctx, style) {
    if (this._applyFill(ctx, style)) ctx.fill();
    if (this._applyStroke(ctx, style)) ctx.stroke();
    ctx.globalAlpha = 1;
  }
  /**
   * @param {any} ctx
   * @param {any} style
   */
  _strokeOnly(ctx, style) {
    if (this._applyStroke(ctx, style)) ctx.stroke();
    ctx.globalAlpha = 1;
  }
  /**
   * Set fill state. Returns false when there's nothing to fill.
   * @param {any} ctx
   * @param {any} style
   */
  _applyFill(ctx, style) {
    const fill = style.fill;
    if (!fill || fill === "none") return false;
    if (typeof fill === "string" && fill.indexOf("url(") === 0) return false;
    ctx.globalAlpha = (style.fillOpacity == null ? 1 : Number(style.fillOpacity)) * this._alpha;
    ctx.fillStyle = fill;
    return true;
  }
  /**
   * Set stroke state. Returns false when there's nothing to stroke.
   * @param {any} ctx
   * @param {any} style
   */
  _applyStroke(ctx, style) {
    const stroke = style.stroke;
    const sw = style.strokeWidth == null ? 1 : Number(style.strokeWidth);
    if (!stroke || stroke === "none" || !(sw > 0)) return false;
    if (typeof stroke === "string" && stroke.indexOf("url(") === 0) return false;
    ctx.globalAlpha = (style.strokeOpacity == null ? 1 : Number(style.strokeOpacity)) * this._alpha;
    ctx.strokeStyle = stroke;
    ctx.lineWidth = sw;
    ctx.lineCap = style.lineCap || "butt";
    const dash = style.strokeDash;
    if (dash && dash !== 0) {
      ctx.setLineDash(Array.isArray(dash) ? dash : [Number(dash)]);
    } else {
      ctx.setLineDash([]);
    }
    return true;
  }
  /** Series bitmap for the export composite bridge (P4). @returns {string|null} */
  toDataURL() {
    return this._canvas ? this._canvas.toDataURL() : null;
  }
  destroy() {
    this._host = null;
    this._canvas = null;
    this._c2d = null;
    this._unitPaths.clear();
  }
}
class CanvasRenderer {
  /**
   * @param {any} w
   * @param {any} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.kind = "canvas";
    this._g = new CanvasGraphics(w2);
    this._compositor = new CanvasCompositor(w2);
  }
  // ── lifecycle ──
  /** Start a fresh series display list for this render pass. */
  beginSeries() {
    this._g.reset();
  }
  /**
   * Finalize the pass: paint the recorded display list and return the SVG host
   * (a `<foreignObject><canvas>`) for `plotChartType` to composite into the tree.
   * @returns {any}
   */
  present() {
    const host = this._compositor.createHost();
    this._compositor.paint(this._g.displayList(), this._g);
    return host;
  }
  /** Fast-path wipe of the series layer. */
  clear() {
    this._compositor.clear();
  }
  /**
   * Whether the existing <canvas> host can be repainted in place (it exists
   * and is still mounted). Used by the data-only fast update path to skip
   * recreating the foreignObject + backing store on every tick.
   * @returns {boolean}
   */
  canRepaintInPlace() {
    const host = this._compositor.getHost();
    return !!(host && host.node && host.node.isConnected);
  }
  /** Repaint the freshly recorded display list into the EXISTING canvas. */
  repaintInPlace() {
    this._compositor.paint(this._g.displayList(), this._g);
  }
  // ── emit primitives (delegate to the display-list shim) ──
  /** @param {any} attrs */
  group(attrs) {
    return this._g.group(attrs);
  }
  /** @param {any} opts */
  drawPath(opts) {
    return this._g.drawPath(opts);
  }
  /** @param {any[]} args */
  drawLine(...args) {
    return (
      /** @type {any} */
      this._g.drawLine(...args)
    );
  }
  /** @param {any[]} args */
  drawRect(...args) {
    return (
      /** @type {any} */
      this._g.drawRect(...args)
    );
  }
  /**
   * Columnar heatmap-cell rect (dense same-shape rects): recorded into typed
   * arrays, painted as style batches. Distinct from drawRect (object command)
   * so 100k cells don't allocate 100k retained commands.
   * @param {number} x @param {number} y @param {number} w @param {number} h
   * @param {any} opts
   */
  drawRectCell(x, y, w2, h2, opts) {
    return this._g.drawRectCell(x, y, w2, h2, opts);
  }
  /**
   * @param {number} r
   * @param {any} attrs
   */
  drawCircle(r2, attrs) {
    return this._g.drawCircle(r2, attrs);
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {any} opts
   */
  drawMarker(x, y, opts) {
    return this._g.drawMarker(x, y, opts);
  }
  /** @param {any} opts */
  renderPaths(opts) {
    return this._g.renderPaths(opts);
  }
  /** @param {any} opts */
  drawText(opts) {
    return this._g.drawText(opts);
  }
  // ── capabilities ──
  /** @param {string} feature */
  supports(feature) {
    return feature === "solidFill" || feature === "dashArray";
  }
  // ── interaction ──
  // Line/area/bar/scatter tooltips resolve via coordinate lookup (pointsArray),
  // so those need no per-mark query. Heatmap cells, however, are hovered by
  // point (the SVG path hit-tests the <rect> under the cursor); with cells on
  // canvas there is no node, so hitTest resolves the columnar rect store.
  /**
   * Find the cell under a plot-local point (0,0 = plot origin, the same space
   * as the recorded cell geometry). Reverse scan so a later-painted cell wins
   * when cells overlap (continuous-x edges). A linear scan stays well under a
   * frame even at 100k cells (~100k integer compares). Returns the cell's
   * series/dataPoint index plus its geometry for tooltip positioning, or null
   * when the point is off every cell.
   * @param {number} px
   * @param {number} py
   * @returns {({seriesIndex:number,dataPointIndex:number,x:number,y:number,width:number,height:number})|null}
   */
  hitTest(px, py) {
    const g2 = this._g;
    const n2 = g2.rectCount ? g2.rectCount() : 0;
    if (!n2) return null;
    const rx = g2._crx;
    const ry = g2._cry;
    const rw = g2._crw;
    const rh = g2._crh;
    for (let k = n2 - 1; k >= 0; k--) {
      const w2 = rw[k];
      const h2 = rh[k];
      if (w2 <= 0 || h2 <= 0) continue;
      if (px >= rx[k] && px < rx[k] + w2 && py >= ry[k] && py < ry[k] + h2) {
        return {
          seriesIndex: g2._crsi[k],
          dataPointIndex: g2._crdi[k],
          x: rx[k],
          y: ry[k],
          width: w2,
          height: h2
        };
      }
    }
    return null;
  }
  /**
   * Repaint the retained series scene with a per-series dim spec (hover /
   * legend restyle). No geometry recompute: reuses the display list + marker
   * columns recorded at render time. Pass null to repaint at full opacity.
   * @param {{active:number, opacity:number}|null} [dim]
   */
  restyle(dim) {
    this._compositor.paint(this._g.displayList(), this._g, dim || null);
  }
  // ── export ── toBitmap() and the compositor's toDataURL() back
  //    Exports.inlineCanvasLayers, which inlines the series bitmap as an SVG
  //    <image> so PNG/SVG export includes the canvas layer in correct z-order.
  /** @returns {{dataURL:string,x:number,y:number,w:number,h:number}|null} */
  toBitmap() {
    const url = this._compositor.toDataURL();
    if (!url) return null;
    const gl = this.w.globals;
    const cfg = this.w.config.chart;
    const margin = this._compositor._margin;
    return {
      dataURL: url,
      x: (gl.translateX || 0) + (cfg.offsetX || 0) - margin,
      y: (gl.translateY || 0) + (cfg.offsetY || 0) - margin,
      w: (this.w.layout.gridWidth || 0) + margin * 2,
      h: (this.w.layout.gridHeight || 0) + margin * 2
    };
  }
  destroy() {
    this._compositor.destroy();
  }
}
ApexCharts.registerRenderer(
  "canvas",
  /**
   * @param {any} w
   * @param {any} ctx
   */
  (w2, ctx) => new CanvasRenderer(w2, ctx)
);
function makeCustomSeriesClass(name2, def) {
  const cls = class CustomSeries {
    /**
     * @param {any} w @param {any} ctx @param {any} xyRatios
     */
    constructor(w2, ctx, xyRatios) {
      this.w = w2;
      this.ctx = ctx;
      this.xyRatios = xyRatios;
      this._warned = false;
    }
    /**
     * @param {any[]} series parsed y-arrays (one per drawn series)
     * @param {string} [_ctype]
     * @param {number[]} [seriesIndices] realIndex per entry (combo dispatch)
     * @returns {any} the wrap group
     */
    draw(series, _ctype, seriesIndices) {
      var _a, _b;
      const w2 = this.w;
      const graphics = new Graphics(w2, this.ctx);
      const emit = seriesEmitter(this.ctx, graphics);
      const ret = graphics.group({ class: "apexcharts-marks-series" });
      series.forEach((_s, idx) => {
        var _a2;
        const realIndex = Array.isArray(seriesIndices) ? seriesIndices[idx] : idx;
        const elSeries = graphics.group({
          class: "apexcharts-series",
          rel: realIndex + 1,
          seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex]),
          "data:realIndex": realIndex
        });
        const scales = this._scales(
          realIndex,
          (w2.seriesData.series[realIndex] || []).length
        );
        const color = w2.globals.colors[realIndex];
        const rawData = (
          /** @type {any} */
          ((_a2 = w2.config.series[realIndex]) == null ? void 0 : _a2.data) || []
        );
        const xvals = w2.seriesData.seriesX[realIndex] || [];
        const yvals = w2.seriesData.series[realIndex] || [];
        w2.globals.seriesXvalues[realIndex] = [];
        w2.globals.seriesYvalues[realIndex] = [];
        if (typeof w2.globals.pointsArray[realIndex] === "undefined") {
          w2.globals.pointsArray[realIndex] = [];
        }
        for (let j = 0; j < yvals.length; j++) {
          const yVal = yvals[j];
          if (yVal === null || typeof yVal === "undefined") continue;
          const xVal = xvals[j];
          const xPx = scales.xAt(j, xVal);
          const yPx = scales.y(yVal);
          const api = this._api(emit, elSeries, realIndex, j);
          try {
            def.renderItem({
              datum: rawData[j],
              x: xPx,
              y: yPx,
              scales,
              api,
              seriesIndex: realIndex,
              dataPointIndex: j,
              color
            });
          } catch (e2) {
            if (!this._warned) {
              console.warn(
                `[apexcharts] renderItem for series type "${name2}" threw; skipping datum:`,
                e2
              );
              this._warned = true;
            }
          }
          w2.globals.seriesXvalues[realIndex][j] = xPx;
          w2.globals.seriesYvalues[realIndex][j] = yPx;
          w2.globals.pointsArray[realIndex][j] = [xPx, yPx];
        }
        graphics.setupEventDelegation(elSeries, ".apexcharts-marks-mark");
        ret.add(elSeries);
      });
      (_b = (_a = this.ctx.animations) == null ? void 0 : _a.animationCompleted) == null ? void 0 : _b.call(_a, ret);
      return ret;
    }
    /**
     * Series-space (elGraphical-local, translate-free) scales, matching how the
     * built-ins compute pixels, so custom marks align with axes and gridlines
     * and paint correctly on the elGraphical-local canvas.
     * @param {number} realIndex
     * @param {number} [nPts] number of data points (for categorical band sizing)
     */
    _scales(realIndex, nPts) {
      var _a, _b, _c, _d;
      const gl = this.w.globals;
      const cnf = this.w.config;
      const xRatio = this.xyRatios.xRatio;
      const yRatioArr = this.xyRatios.yRatio;
      const axis = (_b = (_a = gl.seriesYAxisReverseMap) == null ? void 0 : _a[realIndex]) != null ? _b : 0;
      const yr = Array.isArray(yRatioArr) ? (_c = yRatioArr[axis]) != null ? _c : yRatioArr[0] : yRatioArr;
      const maxYArr = (
        /** @type {any} */
        gl.maxYArr
      );
      const maxY = Array.isArray(maxYArr) && maxYArr.length ? (_d = maxYArr[axis]) != null ? _d : gl.maxY : gl.maxY;
      const gridWidth = gl.gridWidth;
      const gridHeight = gl.gridHeight;
      const catMode = !gl.isXNumeric;
      const n2 = nPts || gl.dataPoints || 1;
      const bandW = n2 > 0 ? gridWidth / n2 : gridWidth;
      const tickOn = cnf.xaxis.tickPlacement === "on";
      const x = (v2) => xRatio ? (v2 - gl.minX) / xRatio : gridWidth / 2;
      const y = (v2) => (maxY - v2) / yr;
      const xAt = (index, v2) => {
        if (!catMode) return x(v2);
        if (tickOn && n2 > 1) return index / (n2 - 1) * gridWidth;
        return (index + 0.5) * bandW;
      };
      const step = gl.minXDiff || 1;
      const band = catMode ? bandW : xRatio ? step / xRatio : gridWidth;
      return {
        x,
        xAt,
        y,
        gridWidth,
        gridHeight,
        band
      };
    }
    /**
     * Per-datum primitive API. Each call emits immediately (canvas-aware via
     * `emit`), tags the node with the datum's identity, and adds it to the
     * series group; on canvas the tag/add are inert (marks live on the canvas,
     * events are coordinate-based).
     * @param {any} emit @param {any} elSeries @param {number} realIndex @param {number} j
     */
    _api(emit, elSeries, realIndex, j) {
      const tag = (el) => {
        if (el) {
          try {
            el.node.setAttribute("index", String(realIndex));
            el.node.setAttribute("j", String(j));
            el.node.classList.add("apexcharts-marks-mark");
          } catch (e2) {
          }
          elSeries.add(el);
        }
        return el;
      };
      return {
        /** @param {any} o */
        path: (o2 = {}) => {
          var _a, _b, _c, _d, _e, _f, _g, _h, _i;
          return tag(
            emit.drawPath({
              d: o2.d || "",
              stroke: (_a = o2.stroke) != null ? _a : "#000",
              strokeWidth: (_c = (_b = o2.width) != null ? _b : o2.strokeWidth) != null ? _c : 1,
              fill: (_d = o2.fill) != null ? _d : "none",
              fillOpacity: (_f = o2.fillOpacity) != null ? _f : o2.fill && o2.fill !== "none" ? (_e = o2.opacity) != null ? _e : 1 : 0,
              strokeOpacity: (_h = (_g = o2.strokeOpacity) != null ? _g : o2.opacity) != null ? _h : 1,
              strokeDashArray: (_i = o2.dash) != null ? _i : 0,
              strokeLinecap: o2.lineCap
            })
          );
        },
        /** @param {any} o */
        line: (o2 = {}) => {
          var _a, _b, _c, _d;
          return tag(
            emit.drawLine(
              o2.x1,
              o2.y1,
              o2.x2,
              o2.y2,
              (_a = o2.stroke) != null ? _a : "#000",
              (_b = o2.dash) != null ? _b : 0,
              (_d = (_c = o2.width) != null ? _c : o2.strokeWidth) != null ? _d : 1
            )
          );
        },
        /** @param {any} o */
        rect: (o2 = {}) => {
          var _a, _b, _c, _d, _e, _f, _g, _h;
          return tag(
            emit.drawRect(
              (_a = o2.x) != null ? _a : 0,
              (_b = o2.y) != null ? _b : 0,
              (_c = o2.w) != null ? _c : 0,
              (_d = o2.h) != null ? _d : 0,
              (_e = o2.r) != null ? _e : 0,
              (_f = o2.fill) != null ? _f : "#000",
              (_g = o2.opacity) != null ? _g : 1,
              o2.stroke != null ? (_h = o2.strokeWidth) != null ? _h : 1 : null,
              o2.stroke
            )
          );
        },
        /** @param {any} o */
        circle: (o2 = {}) => {
          var _a, _b, _c, _d, _e;
          return tag(
            emit.drawCircle((_a = o2.r) != null ? _a : 0, {
              cx: (_b = o2.cx) != null ? _b : 0,
              cy: (_c = o2.cy) != null ? _c : 0,
              fill: (_d = o2.fill) != null ? _d : "#000",
              stroke: o2.stroke || "none",
              "stroke-width": (_e = o2.strokeWidth) != null ? _e : o2.stroke ? 1 : 0
            })
          );
        },
        /** @param {any} o */
        text: (o2 = {}) => {
          var _a, _b, _c, _d;
          return tag(
            emit.drawText({
              x: (_a = o2.x) != null ? _a : 0,
              y: (_b = o2.y) != null ? _b : 0,
              text: (_c = o2.text) != null ? _c : "",
              textAnchor: (_d = o2.anchor) != null ? _d : "start",
              fontSize: o2.size,
              foreColor: o2.color,
              fontWeight: o2.weight
            })
          );
        }
      };
    }
  };
  cls.dataType = def.dataType || "xy";
  cls.yExtent = typeof def.yExtent === "function" ? def.yExtent : null;
  return cls;
}
ApexCharts._customSeriesFactory = makeCustomSeriesClass;
const DARK_QUERY = "(prefers-color-scheme: dark)";
const CONTRAST_QUERY = "(prefers-contrast: more)";
class OSThemeWatcher {
  /**
   * @param {any} w @param {any} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    if (w2.config.theme.follow !== "os" || !Environment.isBrowser()) return;
    const media = this._ensureMedia();
    if (!media) return;
    this._applyToConfig(media);
    this._ensureListeners(media);
  }
  /**
   * Create (once per instance) the MediaQueryLists and stash them on `ctx` so
   * they persist across the re-render that `updateOptions` triggers.
   * @returns {{dark: MediaQueryList|null, contrast: MediaQueryList|null, handler: null|(()=>void)}|null}
   */
  _ensureMedia() {
    if (!this.ctx._osThemeMedia) {
      const dark = BrowserAPIs.matchMedia(DARK_QUERY);
      const contrast = BrowserAPIs.matchMedia(CONTRAST_QUERY);
      if (!dark && !contrast) return null;
      this.ctx._osThemeMedia = { dark, contrast, handler: null };
    }
    return this.ctx._osThemeMedia;
  }
  /**
   * Write the OS-resolved mode / high-contrast onto the live `w.config.theme`.
   * @param {{dark: MediaQueryList|null, contrast: MediaQueryList|null}} media
   */
  _applyToConfig(media) {
    const theme = this.w.config.theme;
    if (media.dark) {
      theme.mode = media.dark.matches ? "dark" : "light";
    }
    if (media.contrast && media.contrast.matches) {
      theme.accessibility = theme.accessibility || {};
      theme.accessibility.colorBlindMode = "highContrast";
    }
  }
  /**
   * Attach the `change` listener once. The handler closes over `ctx` + `media`
   * (both stable across re-renders), NOT over `this` (a fresh watcher is built
   * each create), so it never goes stale.
   * @param {{dark: MediaQueryList|null, contrast: MediaQueryList|null, handler: null|(()=>void)}} media
   */
  _ensureListeners(media) {
    if (media.handler) return;
    const ctx = this.ctx;
    const handler = () => {
      const m2 = ctx._osThemeMedia;
      if (!m2) return;
      const themeOpt = { mode: m2.dark && m2.dark.matches ? "dark" : "light" };
      if (m2.contrast && m2.contrast.matches) {
        themeOpt.accessibility = { colorBlindMode: "highContrast" };
      } else {
        themeOpt.accessibility = { colorBlindMode: "" };
      }
      ctx.updateOptions({ theme: themeOpt }, false, true, false);
    };
    OSThemeWatcher._add(media.dark, handler);
    OSThemeWatcher._add(media.contrast, handler);
    media.handler = handler;
  }
  /** Remove the listeners and drop the stashed media. Called on full destroy. */
  teardown() {
    const media = this.ctx._osThemeMedia;
    if (!media) return;
    if (media.handler) {
      OSThemeWatcher._remove(media.dark, media.handler);
      OSThemeWatcher._remove(media.contrast, media.handler);
    }
    this.ctx._osThemeMedia = null;
  }
  /**
   * @param {MediaQueryList|null} mql @param {()=>void} handler
   */
  static _add(mql, handler) {
    if (!mql) return;
    if (typeof mql.addEventListener === "function") {
      mql.addEventListener("change", handler);
    } else if (typeof /** @type {any} */
    mql.addListener === "function") {
      mql.addListener(handler);
    }
  }
  /**
   * @param {MediaQueryList|null} mql @param {()=>void} handler
   */
  static _remove(mql, handler) {
    if (!mql) return;
    if (typeof mql.removeEventListener === "function") {
      mql.removeEventListener("change", handler);
    } else if (typeof /** @type {any} */
    mql.removeListener === "function") {
      mql.removeListener(handler);
    }
  }
}
ApexCharts.registerFeatures({ osThemeWatcher: OSThemeWatcher });
const MARK_SELECTOR = [
  ".apexcharts-bar-area",
  ".apexcharts-candlestick-area",
  ".apexcharts-boxPlot-area",
  ".apexcharts-rangebar-area",
  ".apexcharts-marker"
].join(", ");
const FILTER_MARK_SELECTOR = [
  ".apexcharts-pie-area",
  ".apexcharts-bar-area"
].join(", ");
const DIMMED_CLASS = "apexcharts-crossfilter-dimmed";
const PIE_TYPES = ["pie", "donut", "polarArea", "radialBar"];
class LinkedViews {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this._dimmed = false;
    this._wired = false;
    this._pending = false;
    this._lastValues = null;
    this._onPointSelect = this._onPointSelect.bind(this);
    this._afterRender = this._afterRender.bind(this);
    this._onChange = this._onChange.bind(this);
    if (this._mode() === "filter") this._initEngine();
  }
  /** @returns {'highlight'|'filter'|'off'} */
  _mode() {
    const link = this.w.config.chart.link;
    if (link && typeof link.dimension === "function") return "filter";
    if (link && link.enabled) return "highlight";
    return "off";
  }
  _enabled() {
    const link = this.w.config.chart.link;
    return !!(link && link.enabled);
  }
  /**
   * The source chart's rectangle brush produced a data-x range. In FILTER mode
   * this becomes a `[min,max]` range filter on the chart's dimension (the other
   * charts re-aggregate). In HIGHLIGHT mode (P1) it dims out-of-range marks
   * across the group. Called (null-safe) from ZoomPanSelection selectionDrawn /
   * selectionDragging.
   * @param {{min:number, max:number}} xaxis
   */
  onSourceSelection(xaxis) {
    var _a;
    const mode = this._mode();
    if (mode === "off") return;
    if (!xaxis || xaxis.min == null || xaxis.max == null) return;
    let min = Math.min(xaxis.min, xaxis.max);
    let max = Math.max(xaxis.min, xaxis.max);
    const gMinX = this.w.globals.minX;
    const gMaxX = this.w.globals.maxX;
    if (isFinite(gMinX) && isFinite(gMaxX) && gMaxX > gMinX) {
      const tol = (gMaxX - gMinX) * 1e-6;
      if (min - gMinX <= tol) min = gMinX;
      if (gMaxX - max <= tol) max = gMaxX;
    }
    if (mode === "filter") {
      const cf = this._cf();
      if (!cf) return;
      cf.filter(this._chartId(), [min, max]);
      this._fireFilterChange(cf, [min, max]);
      return;
    }
    this._group().forEach((ch) => {
      var _a2;
      (_a2 = ch == null ? void 0 : ch.linkedViews) == null ? void 0 : _a2.applyDim(min, max);
    });
    const args = { xaxis: { min, max }, sourceChartID: this.w.globals.chartID };
    if (typeof this.w.config.chart.events.crossFilter === "function") {
      this.w.config.chart.events.crossFilter(this.ctx, args);
    }
    (_a = this.ctx.events) == null ? void 0 : _a.fireEvent("crossFilter", [this.ctx, args]);
  }
  /** self + grouped siblings (dedup-safe; getGroupedCharts excludes self). */
  _group() {
    const siblings = typeof this.ctx.getGroupedCharts === "function" ? this.ctx.getGroupedCharts() : [];
    return [this.ctx, ...siblings];
  }
  /**
   * Dim this chart's marks whose x is outside [min,max]; un-dim those inside.
   * No re-render, so mark identities are preserved.
   * @param {number} min @param {number} max
   */
  applyDim(min, max) {
    if (!this._enabled()) return;
    const w2 = this.w;
    const baseEl = w2.dom.baseEl;
    if (!baseEl) return;
    const dimOpacity = w2.config.chart.link.dimOpacity;
    if (w2.dom.elWrap && typeof dimOpacity === "number") {
      w2.dom.elWrap.style.setProperty("--apx-cf-dim", String(dimOpacity));
    }
    const seriesX = w2.globals.seriesX || [];
    const marks = baseEl.querySelectorAll(MARK_SELECTOR);
    marks.forEach((node) => {
      const jAttr = node.getAttribute("j");
      if (jAttr === null) return;
      const j = parseInt(jAttr, 10);
      const iAttr = node.getAttribute("index");
      const i2 = iAttr === null ? 0 : parseInt(iAttr, 10);
      const row = seriesX[i2] || seriesX[0];
      if (!row) return;
      const x = row[j];
      if (x == null) return;
      node.classList.toggle(DIMMED_CLASS, x < min || x > max);
    });
    this._dimmed = true;
  }
  /** Remove dimming from this chart only. */
  clear() {
    const baseEl = this.w.dom.baseEl;
    if (!baseEl) return;
    baseEl.querySelectorAll("." + DIMMED_CLASS).forEach((n2) => n2.classList.remove(DIMMED_CLASS));
    this._dimmed = false;
  }
  /** Clear dimming across the whole group (backs chart.clearCrossfilter). */
  clearGroup() {
    if (this._mode() === "filter") {
      const cf = this._cf();
      if (cf) cf.reset();
      return;
    }
    this._group().forEach((ch) => {
      var _a;
      return (_a = ch == null ? void 0 : ch.linkedViews) == null ? void 0 : _a.clear();
    });
  }
  // ─── FILTER mode (crossfilter engine glue) ───────────────────────────────
  /**
   * The chart's stable internal id (keys its dimension in the coordinator).
   * Always set by the ApexCharts constructor (falls back to a cuid).
   * @returns {string}
   */
  _chartId() {
    return (
      /** @type {string} */
      this.w.globals.chartID
    );
  }
  /** @returns {import('./Crossfilter').default|null} the coordinator, or null */
  _cf() {
    const link = this.w.config.chart.link;
    const id = link && (link.id || this.w.config.chart.group);
    return id ? se.get(id) : null;
  }
  _isPie() {
    return PIE_TYPES.indexOf(this.w.config.chart.type) !== -1;
  }
  _isHeatmap() {
    return this.w.config.chart.type === "heatmap";
  }
  /**
   * Before the first render: resolve the coordinator, register this chart's
   * dimension, inject the initial aggregated series into w.config (so the first
   * paint is already aggregated, no empty flash), and wire the listeners.
   */
  _initEngine() {
    const cf = this._cf();
    const link = this.w.config.chart.link;
    if (!cf) {
      const id = link && link.id || this.w.config.chart.group;
      console.warn(
        `[apexcharts] chart.link.dimension is set but no crossfilter coordinator "${id}" exists. Call ApexCharts.crossfilter({ id, records }) before creating the chart.`
      );
      return;
    }
    const chartId = this._chartId();
    if (!cf.hasDimension(chartId)) {
      cf.registerDimension(chartId, {
        dimension: link.dimension,
        reduce: link.reduce,
        // heatmap => 2D matrix dimension (accessor returns [xKey, yKey]).
        type: link.type || (this._isHeatmap() ? "matrix" : void 0),
        bins: link.bins,
        order: link.order
      });
    }
    this._injectSeries(cf.aggregateFor(chartId));
    this._wire(cf);
  }
  /**
   * Build the chart's series value from an aggregation, shaped by chart type:
   *   matrix (heatmap) -> [{ name:yKey, data:[{x:xKey, y:value}] }]
   *   pie/donut  -> number[]
   *   axis + category -> [{ name, data:number[] }] (categories set separately)
   *   axis + range    -> [{ name, data:[x,value][] }] on a numeric/time x-axis
   * @param {any} agg
   */
  _seriesFromAgg(agg) {
    if (agg.type === "matrix") {
      return agg.yLabels.map((yl, yi) => ({
        name: String(yl),
        data: agg.xLabels.map((xl, xi) => ({
          x: String(xl),
          y: agg.matrix[yi][xi]
        }))
      }));
    }
    if (this._isPie()) return agg.values.slice();
    const name2 = this.w.config.chart.link.seriesName || "Count";
    if (agg.type === "range") {
      return [{ name: name2, data: agg.labels.map((x, i2) => [x, agg.values[i2]]) }];
    }
    return [{ name: name2, data: agg.values.slice() }];
  }
  /**
   * Value signature used to skip a reflow when only dimming changed.
   * @param {any} agg
   */
  _sigOf(agg) {
    return JSON.stringify(agg.matrix || agg.values);
  }
  /**
   * Write the aggregation into w.config as the chart's series/labels. Runs once
   * before the first paint; later updates go through updateSeries.
   * @param {any} agg
   */
  _injectSeries(agg) {
    const w2 = this.w;
    this._lastValues = this._sigOf(agg);
    w2.config.series = this._seriesFromAgg(agg);
    if (agg.type === "matrix") return;
    if (this._isPie()) {
      w2.config.labels = agg.labels.map(String);
    } else if (agg.type === "category") {
      if (!w2.config.xaxis) w2.config.xaxis = {};
      w2.config.xaxis.categories = agg.labels.map(String);
    } else if (agg.type === "range") {
      this._pinRangeDomain(agg.edges);
    }
  }
  /**
   * Pin the numeric/datetime x-axis to the outer bin edges of a range-binned
   * dimension (unless the user set xaxis.min/max explicitly). See _injectSeries.
   * @param {number[]|null|undefined} edges
   */
  _pinRangeDomain(edges) {
    if (!Array.isArray(edges) || edges.length < 2) return;
    const w2 = this.w;
    if (!w2.config.xaxis) w2.config.xaxis = /** @type {any} */
    {};
    if (w2.config.xaxis.min == null) w2.config.xaxis.min = edges[0];
    if (w2.config.xaxis.max == null) w2.config.xaxis.max = edges[edges.length - 1];
  }
  /** @param {import('./Crossfilter').default} cf */
  _wire(cf) {
    if (this._wired) return;
    this._wired = true;
    this.ctx.addEventListener("dataPointSelection", this._onPointSelect);
    this.ctx.addEventListener("mounted", this._afterRender);
    this.ctx.addEventListener("updated", this._afterRender);
    cf.on("change", this._onChange);
  }
  /**
   * A pie slice / bar was clicked: toggle its bucket key on the coordinator.
   * @param {any} _e @param {any} _ctx @param {{dataPointIndex?:number}} opts
   */
  _onPointSelect(_e, _ctx, opts) {
    if (this._mode() !== "filter" || !opts || opts.dataPointIndex == null) return;
    const cf = this._cf();
    if (!cf) return;
    const chartId = this._chartId();
    const agg = cf.aggregateFor(chartId);
    if (agg.type === "matrix") return;
    const key = agg.keys[opts.dataPointIndex];
    if (key == null) return;
    cf.toggleKey(chartId, key);
    this._fireFilterChange(cf, key);
  }
  /** Coordinator filter changed: re-aggregate this chart on a microtask so the
   *  triggering click handler unwinds before we destroy/redraw the DOM. */
  _onChange() {
    if (this._mode() !== "filter" || this._pending) return;
    this._pending = true;
    Promise.resolve().then(() => {
      this._pending = false;
      if (this.w.globals.isDestroyed) return;
      this._applyAggregation();
    });
  }
  /**
   * Pull this chart's crossfilter aggregation and push it through updateSeries
   * (animated). When the values are unchanged (e.g. only this chart's own
   * filter moved, which it ignores for itself), skip the reflow and just
   * refresh the self-dim.
   */
  _applyAggregation() {
    if (this._mode() !== "filter") return;
    const cf = this._cf();
    if (!cf) return;
    const agg = cf.aggregateFor(this._chartId());
    const sig = this._sigOf(agg);
    if (sig === this._lastValues) {
      this._applySelfDim();
      return;
    }
    this._lastValues = sig;
    this.ctx.updateSeries(this._seriesFromAgg(agg), true);
  }
  _afterRender() {
    if (this._mode() !== "filter") return;
    const series = this.w.config.series;
    if (!series || series.length === 0) {
      this._reassertSeries();
      return;
    }
    this._applySelfDim();
  }
  /** Restore the aggregated series after an external updateSeries emptied it.
   *  Deferred a microtask so the triggering update fully unwinds first. */
  _reassertSeries() {
    if (this._pending) return;
    this._pending = true;
    Promise.resolve().then(() => {
      this._pending = false;
      if (this.w.globals.isDestroyed) return;
      const cf = this._cf();
      if (!cf) return;
      const agg = cf.aggregateFor(this._chartId());
      const series = this._seriesFromAgg(agg);
      if (!series.length) return;
      this._lastValues = this._sigOf(agg);
      this.ctx.updateSeries(series, true);
    });
  }
  /**
   * Dim this chart's own buckets that are not in its own filter (no filter ->
   * none dimmed). Categorical: dim buckets whose key is not in the selected Set.
   * Range: dim bins lying fully outside the selected `[min,max]`. Keyed by each
   * mark's `j` (dataPointIndex) -> the aggregation key.
   */
  _applySelfDim() {
    const cf = this._cf();
    if (!cf) return;
    const w2 = this.w;
    const baseEl = w2.dom.baseEl;
    if (!baseEl) return;
    const chartId = this._chartId();
    const filter = cf.filterOf(chartId);
    const dimOpacity = w2.config.chart.link.dimOpacity;
    if (w2.dom.elWrap && typeof dimOpacity === "number") {
      w2.dom.elWrap.style.setProperty("--apx-cf-dim", String(dimOpacity));
    }
    const isCategory = filter instanceof Set;
    const isRange = Array.isArray(filter);
    const agg = cf.aggregateFor(chartId);
    if (agg.type === "matrix") return;
    const keys = agg.keys;
    baseEl.querySelectorAll(FILTER_MARK_SELECTOR).forEach((node) => {
      const jAttr = node.getAttribute("j");
      if (jAttr === null) return;
      const key = keys[parseInt(jAttr, 10)];
      let dim = false;
      if (isCategory) {
        dim = !/** @type {Set<any>} */
        filter.has(key);
      } else if (isRange && Array.isArray(key)) {
        dim = key[1] <= filter[0] || key[0] >= filter[1];
      }
      node.classList.toggle(DIMMED_CLASS, dim);
    });
    this._dimmed = !!filter;
  }
  /**
   * Fire the `filterChange` event on this (source) chart.
   * @param {import('./Crossfilter').default} cf @param {any} key
   */
  _fireFilterChange(cf, key) {
    var _a;
    const args = __spreadProps(__spreadValues({}, cf.state()), {
      sourceChartID: this._chartId(),
      key
    });
    const events = this.w.config.chart.events;
    if (typeof events.filterChange === "function") {
      events.filterChange(this.ctx, args);
    }
    (_a = this.ctx.events) == null ? void 0 : _a.fireEvent("filterChange", [this.ctx, args]);
  }
  teardown() {
    var _a, _b, _c, _d, _e, _f;
    this.clear();
    if (this._wired) {
      (_b = (_a = this.ctx).removeEventListener) == null ? void 0 : _b.call(_a, "dataPointSelection", this._onPointSelect);
      (_d = (_c = this.ctx).removeEventListener) == null ? void 0 : _d.call(_c, "mounted", this._afterRender);
      (_f = (_e = this.ctx).removeEventListener) == null ? void 0 : _f.call(_e, "updated", this._afterRender);
      const cf = this._cf();
      if (cf) {
        cf.off("change", this._onChange);
        cf.removeDimension(this._chartId());
      }
      this._wired = false;
    }
  }
}
ApexCharts.registerFeatures({ linkedViews: LinkedViews });
const AC = (
  /** @type {any} */
  ApexCharts
);
AC._crossfilterFactory = (opts) => se.getOrCreate(opts);
AC._crossfilterGet = (id) => se.get(id);
const DRAG_CLASS = "apexcharts-ink-draggable";
const TYPES = ["point", "xaxis", "yaxis"];
const EDGE_PX = 8;
const CLICK_SLOP_PX = 2;
const FONT_STEPS = [10, 11, 12, 14, 17, 20];
const MARKER_SHAPES = ["circle", "square", "diamond", "triangle"];
const SHAPE_GLYPHS = {
  circle: "●",
  square: "■",
  diamond: "◆",
  triangle: "▲"
};
const NOTE_COLORS = [
  "#ffffff",
  "#334155",
  "#2563eb",
  "#16a34a",
  "#d97706",
  "#dc2626"
];
const TRASH_ICON = '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 6h18M8 6V4a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1v2m3 0v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>';
class InkLayer {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this._wired = false;
    this._drag = null;
    this._editor = null;
    this._creating = false;
    this._createSeq = 0;
    this._attach = this._attach.bind(this);
    this._onRerender = this._onRerender.bind(this);
    this._onMove = this._onMove.bind(this);
    this._onUp = this._onUp.bind(this);
    this._onCreateClick = this._onCreateClick.bind(this);
    this._onDocDownEditor = this._onDocDownEditor.bind(this);
    if (this._enabledGlobally() || this._hasDraggable() || this._paletteEnabled()) {
      this._wire();
    }
  }
  _enabledGlobally() {
    const ink = this.w.config.chart.ink;
    return !!(ink && ink.enabled);
  }
  _paletteEnabled() {
    const ink = this.w.config.chart.ink;
    return !!(ink && ink.palette);
  }
  _snapEnabled() {
    const ink = this.w.config.chart.ink;
    return !!(ink && ink.snap);
  }
  // ─── P5: snap to gridlines ────────────────────────────────────────────────
  /** @param {number} v @param {number[]} ticks @returns {number} nearest tick */
  _nearest(v2, ticks) {
    let best = v2;
    let bd = Infinity;
    for (let i2 = 0; i2 < ticks.length; i2++) {
      const d = Math.abs(ticks[i2] - v2);
      if (d < bd) {
        bd = d;
        best = ticks[i2];
      }
    }
    return best;
  }
  /**
   * Snap an x value to the nearest x gridline (numeric axes only).
   * @param {number} x
   */
  _snapX(x) {
    if (!this._snapEnabled() || typeof x !== "number") return x;
    const s2 = this.w.globals.xAxisScale;
    return s2 && Array.isArray(s2.result) && s2.result.length ? this._nearest(x, s2.result) : x;
  }
  /**
   * Snap a y value to the nearest y gridline.
   * @param {number} y @param {number} si
   */
  _snapY(y, si) {
    if (!this._snapEnabled() || typeof y !== "number") return y;
    const scales = this.w.globals.yAxisScale;
    const s2 = scales && scales[si];
    return s2 && Array.isArray(s2.result) && s2.result.length ? this._nearest(y, s2.result) : y;
  }
  /** @param {string} type @returns {any[]} the config annotations of a type */
  _annoList(type) {
    const a2 = this.w.config.annotations;
    if (!a2) return [];
    const key = type === "point" ? "points" : type;
    return Array.isArray(a2[key]) ? a2[key] : [];
  }
  /** @param {any} anno */
  _isDraggable(anno) {
    if (!anno) return false;
    if (anno.draggable === true) return true;
    if (anno.draggable === false) return false;
    return this._enabledGlobally();
  }
  _hasDraggable() {
    return TYPES.some((t2) => this._annoList(t2).some((p2) => this._isDraggable(p2)));
  }
  _wire() {
    if (this._wired) return;
    this._wired = true;
    this.ctx.addEventListener("mounted", this._onRerender);
    this.ctx.addEventListener("updated", this._onRerender);
  }
  /**
   * A full (re)render rebuilds the SVG and may swap the annotations config
   * (updateOptions, undo restore), so an open editor card points at stale
   * state: drop it (without committing) before rebinding handlers. Targeted
   * redraws call _attach() directly and keep the card open.
   */
  _onRerender() {
    this._closeEditor(false);
    this._attach();
    if (this._creating) {
      const svg = this.w.dom.Paper && this.w.dom.Paper.node;
      if (svg) {
        svg.style.cursor = "crosshair";
        svg.addEventListener("click", this._onCreateClick, true);
      }
    }
  }
  /**
   * After each (re)render, bind drag + edit handlers to every draggable
   * annotation's elements. Idempotent via a per-node flag so a targeted redraw
   * re-runs this without double-binding the untouched annotations.
   */
  _attach() {
    const w2 = this.w;
    const baseEl = w2.dom.baseEl;
    if (!baseEl) return;
    TYPES.forEach((type) => {
      this._annoList(type).forEach((anno, index) => {
        if (!this._isDraggable(anno)) return;
        if (!anno.id) {
          anno.id = "apexcharts-ink-" + type + "-" + index + "-" + w2.globals.chartID;
        }
        baseEl.querySelectorAll("." + anno.id).forEach((el) => {
          if (el.__inkBound) return;
          el.__inkBound = true;
          el.style.cursor = "move";
          el.classList.add(DRAG_CLASS);
          el.addEventListener(
            "mousedown",
            (e2) => this._onDown(e2, type, index)
          );
          el.addEventListener(
            "touchstart",
            (e2) => this._onDown(e2, type, index)
          );
          el.addEventListener("dblclick", (e2) => {
            e2.preventDefault();
            e2.stopPropagation();
            this._startEdit(type, index, { select: true });
          });
        });
      });
    });
    if (this._paletteEnabled()) this._renderPalette();
  }
  // ─── drag / resize ────────────────────────────────────────────────────────
  /**
   * @param {any} e @param {string} type @param {number} index
   */
  _onDown(e2, type, index) {
    if (e2.button && e2.button !== 0) return;
    const w2 = this.w;
    const doc = w2.dom.baseEl && w2.dom.baseEl.ownerDocument;
    if (!doc) return;
    e2.stopPropagation();
    if (e2.cancelable) e2.preventDefault();
    const isTouch = e2.type === "touchstart";
    const ev = isTouch ? e2.touches[0] : e2;
    const svgRoot = w2.dom.Paper && w2.dom.Paper.node;
    const ctm = svgRoot && svgRoot.getScreenCTM ? svgRoot.getScreenCTM() : null;
    const anno = this._annoList(type)[index];
    let mode = "move";
    let rect = null;
    let origX = 0;
    let origW = 0;
    if (type === "xaxis" && anno.x2 != null) {
      rect = w2.dom.baseEl.querySelector(".apexcharts-annotation-rect." + anno.id);
      if (rect) {
        const r2 = rect.getBoundingClientRect();
        if (Math.abs(ev.clientX - r2.left) <= EDGE_PX) mode = "resize-x1";
        else if (Math.abs(ev.clientX - r2.right) <= EDGE_PX) mode = "resize-x2";
        origX = parseFloat(rect.getAttribute("x")) || 0;
        origW = parseFloat(rect.getAttribute("width")) || 0;
      }
    }
    this._drag = {
      type,
      index,
      anno,
      els: Array.from(w2.dom.baseEl.querySelectorAll("." + anno.id)),
      mode,
      rect,
      origX,
      origW,
      startX: ev.clientX,
      startY: ev.clientY,
      scaleX: ctm && ctm.a ? ctm.a : 1,
      scaleY: ctm && ctm.d ? ctm.d : 1,
      dxPixel: 0,
      dyPixel: 0,
      moved: false
    };
    doc.addEventListener("mousemove", this._onMove);
    doc.addEventListener("touchmove", this._onMove, { passive: false });
    doc.addEventListener("mouseup", this._onUp);
    doc.addEventListener("touchend", this._onUp);
  }
  /** @param {any} me */
  _onMove(me) {
    const d = this._drag;
    if (!d) return;
    if (me.cancelable) me.preventDefault();
    const mev = me.type === "touchmove" ? me.touches[0] : me;
    d.dxPixel = (mev.clientX - d.startX) / d.scaleX;
    d.dyPixel = (mev.clientY - d.startY) / d.scaleY;
    if (Math.abs(d.dxPixel) > CLICK_SLOP_PX || Math.abs(d.dyPixel) > CLICK_SLOP_PX) {
      d.moved = true;
    }
    if (d.mode === "move") {
      const t2 = `translate(${d.dxPixel} ${d.dyPixel})`;
      d.els.forEach((el) => el.setAttribute("transform", t2));
    } else if (d.rect) {
      if (d.mode === "resize-x1") {
        d.rect.setAttribute("x", d.origX + d.dxPixel);
        d.rect.setAttribute("width", Math.max(1, d.origW - d.dxPixel));
      } else if (d.mode === "resize-x2") {
        d.rect.setAttribute("width", Math.max(1, d.origW + d.dxPixel));
      }
    }
  }
  _onUp() {
    const d = this._drag;
    this._drag = null;
    this._teardownDocListeners();
    if (!d || !d.moved) {
      if (d) {
        d.els.forEach((el) => el.removeAttribute("transform"));
        this._startEdit(d.type, d.index);
      }
      return;
    }
    const anno = this._annoList(d.type)[d.index];
    if (!anno) return;
    this._applyDelta(d, anno);
    d.els.forEach((el) => el.removeAttribute("transform"));
    this._redrawAnno(d.type, anno, d.index);
    this._checkpoint("ink:drag");
    this._fireDragged(d.type, anno, d.index);
  }
  /**
   * Record a Rewind (undo) checkpoint for an ink edit. Targeted redraws fire no
   * 'updated' event, so History would otherwise miss them. No-op when the
   * history feature is absent or disabled.
   * @param {string} label
   */
  _checkpoint(label) {
    var _a, _b;
    (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, label);
  }
  /**
   * Mutate the annotation's config from the pixel drag delta (type + mode aware).
   * @param {any} d @param {any} anno
   */
  _applyDelta(d, anno) {
    const w2 = this.w;
    const dxData = w2.layout.gridWidth ? d.dxPixel * (w2.globals.xRange / w2.layout.gridWidth) : 0;
    if (d.type === "point") {
      const { newX, newY } = this._invertPoint(anno, d.dxPixel, d.dyPixel);
      anno.x = this._snapX(newX);
      if (newY != null) {
        const yi = anno.yAxisIndex || 0;
        const map = w2.globals.seriesYAxisMap;
        anno.y = this._snapY(newY, map && map[yi] ? map[yi][0] : 0);
      }
      return;
    }
    if (d.type === "xaxis") {
      if (typeof anno.x !== "number") return;
      if (d.mode === "move") {
        if (typeof anno.x2 === "number") {
          anno.x += dxData;
          anno.x2 += dxData;
        } else {
          anno.x = this._snapX(anno.x + dxData);
        }
      } else if (d.mode === "resize-x1" || d.mode === "resize-x2") {
        const xIsLeft = anno.x2 == null || anno.x <= anno.x2;
        const grow = d.mode === "resize-x2" ? !xIsLeft : xIsLeft;
        if (grow) anno.x = this._snapX(anno.x + dxData);
        else if (typeof anno.x2 === "number") anno.x2 = this._snapX(anno.x2 + dxData);
      }
      return;
    }
    if (d.type === "yaxis") {
      const yi = anno.yAxisIndex || 0;
      const map = w2.globals.seriesYAxisMap;
      const si = map && map[yi] ? map[yi][0] : 0;
      const yRange = w2.globals.yRange ? w2.globals.yRange[si] : null;
      if (yRange == null || !w2.layout.gridHeight) return;
      const dyData = -d.dyPixel * (yRange / w2.layout.gridHeight);
      if (typeof anno.y2 === "number") {
        if (typeof anno.y === "number") anno.y += dyData;
        anno.y2 += dyData;
      } else if (typeof anno.y === "number") {
        anno.y = this._snapY(anno.y + dyData, si);
      }
    }
  }
  /**
   * Invert a pixel drag delta to a point annotation's data x/y.
   * @param {any} anno @param {number} dxPixel @param {number} dyPixel
   * @returns {{newX:any, newY:any}}
   */
  _invertPoint(anno, dxPixel, dyPixel) {
    const w2 = this.w;
    const categoryX = (w2.config.xaxis.type === "category" || w2.config.xaxis.convertedCatToNumeric) && !w2.axisFlags.dataFormatXNumeric;
    let newX = anno.x;
    if (!categoryX && typeof anno.x === "number" && w2.layout.gridWidth) {
      newX = anno.x + dxPixel * (w2.globals.xRange / w2.layout.gridWidth);
    }
    let newY = anno.y;
    const yi = anno.yAxisIndex || 0;
    const map = w2.globals.seriesYAxisMap;
    const si = map && map[yi] ? map[yi][0] : 0;
    const yRange = w2.globals.yRange ? w2.globals.yRange[si] : null;
    const logY = w2.config.yaxis[yi] && w2.config.yaxis[yi].logarithmic;
    if (typeof anno.y === "number" && yRange != null && !logY && w2.layout.gridHeight) {
      newY = anno.y - dyPixel * (yRange / w2.layout.gridHeight);
    }
    return { newX, newY };
  }
  /**
   * Targeted redraw of one annotation: drop its elements and re-add the shape +
   * label + label background at the current config coordinates (no full chart
   * re-render, and repeat-safe unlike updateOptions({})).
   * @param {string} type @param {any} anno @param {number} index
   */
  _redrawAnno(type, anno, index) {
    const w2 = this.w;
    const baseEl = w2.dom.baseEl;
    const annotations = this.ctx.annotations;
    if (!baseEl || !annotations) return;
    baseEl.querySelectorAll("." + anno.id).forEach((el) => el.remove());
    const group = baseEl.querySelector(".apexcharts-" + type + "-annotations");
    if (!group) return;
    if (type === "point" && annotations.pointsAnnotations) {
      annotations.pointsAnnotations.addPointAnnotation(anno, group, index);
    } else if (type === "xaxis" && annotations.xAxisAnnotations) {
      annotations.xAxisAnnotations.addXaxisAnnotation(anno, group, index);
    } else if (type === "yaxis" && annotations.yAxisAnnotations) {
      annotations.yAxisAnnotations.addYaxisAnnotation(anno, group, index);
    }
    const labelEl = baseEl.querySelector(
      ".apexcharts-" + type + "-annotation-label." + anno.id
    );
    if (labelEl && annotations.helpers && anno.label && anno.label.text) {
      const elRect = annotations.helpers.addBackgroundToAnno(labelEl, anno);
      if (elRect && labelEl.parentNode) {
        labelEl.parentNode.insertBefore(elRect.node, labelEl);
      }
    }
    this._attach();
  }
  /**
   * Dispatch an ink annotation lifecycle event both to the user callback
   * (`chart.events[name]`) and the internal event bus, in that order.
   * @param {string} name @param {any} args
   */
  _fireAnnotationEvent(name2, args) {
    var _a;
    const events = this.w.config.chart.events;
    if (typeof events[name2] === "function") {
      events[name2](this.ctx, args);
    }
    (_a = this.ctx.events) == null ? void 0 : _a.fireEvent(name2, [this.ctx, args]);
  }
  /** @param {string} type @param {any} anno @param {number} index */
  _fireDragged(type, anno, index) {
    const args = { type, id: anno.id, index, x: anno.x, y: anno.y };
    if (anno.x2 != null) args.x2 = anno.x2;
    if (anno.y2 != null) args.y2 = anno.y2;
    this._fireAnnotationEvent("annotationDragged", args);
  }
  // ─── P3: click-to-create ─────────────────────────────────────────────────
  /**
   * Enter create mode: the next click on the plot area drops a new draggable
   * point annotation there and opens its label editor.
   */
  startCreate() {
    if (this._creating) return;
    const svg = this.w.dom.Paper && this.w.dom.Paper.node;
    if (!svg) return;
    this._creating = true;
    svg.style.cursor = "crosshair";
    svg.addEventListener("click", this._onCreateClick, true);
    this._syncPalette();
  }
  /** Leave create mode. */
  stopCreate() {
    if (!this._creating) return;
    this._creating = false;
    const svg = this.w.dom.Paper && this.w.dom.Paper.node;
    if (svg) {
      svg.style.cursor = "";
      svg.removeEventListener("click", this._onCreateClick, true);
    }
    this._syncPalette();
  }
  /** @param {any} e */
  _onCreateClick(e2) {
    if (!this._creating) return;
    e2.preventDefault();
    e2.stopPropagation();
    const pos = this._pixelToData(e2.clientX, e2.clientY);
    this.stopCreate();
    if (!pos) return;
    this.createAt(pos.x, pos.y);
  }
  /**
   * Create a draggable note at data coordinates and open its editor card.
   * Public: the context menu's "Add note here" routes here so its notes are
   * config-backed too, and thus draggable, editable, persistable and undoable.
   * @param {any} x @param {any} y @param {{text?: string}} [opts]
   * @returns {any} the created annotation config
   */
  createAt(x, y, opts = {}) {
    const w2 = this.w;
    this._wire();
    this._createSeq += 1;
    const id = "apexcharts-ink-new-" + this._createSeq + "-" + w2.globals.chartID;
    const anno = Utils$1.extend(new Options().pointAnnotation, {
      x,
      y,
      id,
      draggable: true,
      label: { text: opts.text || "Note" }
    });
    if (!w2.config.annotations) w2.config.annotations = {};
    if (!Array.isArray(w2.config.annotations.points)) w2.config.annotations.points = [];
    w2.config.annotations.points.push(anno);
    const index = w2.config.annotations.points.length - 1;
    this._redrawAnno("point", anno, index);
    this._checkpoint("ink:create");
    this._fireCreated("point", anno, index);
    this._startEdit("point", index, { select: true });
    return anno;
  }
  /**
   * Create a draggable dashed LINE annotation at a data value and open its
   * editor card: axis 'x' drops a vertical line at a data x, axis 'y' a
   * horizontal line at a data y. Public: the context menu's "Annotate here"
   * routes here so its lines are config-backed too, and thus draggable,
   * editable, persistable and undoable. Lines only: x2/y2 are never set, so
   * this can never produce a range rectangle.
   * @param {'x'|'y'} axis @param {any} val
   * @param {{text?: string, strokeDashArray?: number, color?: string, select?: boolean}} [opts]
   * @returns {any} the created annotation config
   */
  createLineAt(axis, val, opts = {}) {
    const w2 = this.w;
    this._wire();
    this._createSeq += 1;
    const id = "apexcharts-ink-new-" + this._createSeq + "-" + w2.globals.chartID;
    const type = axis === "y" ? "yaxis" : "xaxis";
    const defaults = type === "yaxis" ? new Options().yAxisAnnotation : new Options().xAxisAnnotation;
    const over = {
      id,
      draggable: true,
      strokeDashArray: opts.strokeDashArray != null ? opts.strokeDashArray : 4,
      label: { text: opts.text || "" }
    };
    if (opts.color) {
      over.borderColor = opts.color;
      over.label.borderColor = opts.color;
    }
    if (type === "yaxis") over.y = val;
    else over.x = val;
    const anno = Utils$1.extend(defaults, over);
    if (!w2.config.annotations) w2.config.annotations = {};
    if (!Array.isArray(w2.config.annotations[type])) w2.config.annotations[type] = [];
    w2.config.annotations[type].push(anno);
    const index = w2.config.annotations[type].length - 1;
    this._redrawAnno(type, anno, index);
    this._checkpoint("ink:create");
    this._fireCreated(type, anno, index);
    if (opts.select !== false) this._startEdit(type, index, { select: true });
    return anno;
  }
  /**
   * Convert a client-space point to data coordinates (absolute, for create).
   * @param {number} clientX @param {number} clientY
   * @returns {{x:any, y:any}|null}
   */
  _pixelToData(clientX, clientY) {
    const w2 = this.w;
    const gridEl = w2.dom.baseEl && w2.dom.baseEl.querySelector(".apexcharts-grid");
    if (!gridEl) return null;
    const g2 = gridEl.getBoundingClientRect();
    if (!g2.width || !g2.height) return null;
    const fx = (clientX - g2.left) / g2.width;
    const fy = (clientY - g2.top) / g2.height;
    const minX = w2.globals.minX;
    const xRange = w2.globals.xRange;
    const minY = w2.globals.minYArr && w2.globals.minYArr[0] != null ? w2.globals.minYArr[0] : w2.globals.minY;
    const yRange = w2.globals.yRange && w2.globals.yRange[0] != null ? w2.globals.yRange[0] : w2.globals.maxY - w2.globals.minY;
    let x = minX + fx * xRange;
    const y = minY + (1 - fy) * yRange;
    const categoryX = (w2.config.xaxis.type === "category" || w2.config.xaxis.convertedCatToNumeric) && !w2.axisFlags.dataFormatXNumeric;
    if (categoryX) x = Math.round(x);
    return { x, y };
  }
  /** @param {string} type @param {any} anno @param {number} index */
  _fireCreated(type, anno, index) {
    const args = { type, id: anno.id, index };
    if (typeof anno.x !== "undefined") args.x = anno.x;
    if (typeof anno.y !== "undefined") args.y = anno.y;
    this._fireAnnotationEvent("annotationCreated", args);
  }
  // ─── P3: tool palette ────────────────────────────────────────────────────
  /** Render a minimal "add note" toggle into the chart wrap (once per render). */
  _renderPalette() {
    const w2 = this.w;
    const elWrap = w2.dom.elWrap;
    if (!elWrap || elWrap.querySelector(".apexcharts-ink-palette")) return;
    const doc = elWrap.ownerDocument;
    const bar = doc.createElement("div");
    bar.className = "apexcharts-ink-palette";
    const s2 = bar.style;
    s2.position = "absolute";
    s2.top = "6px";
    s2.left = "6px";
    s2.zIndex = "15";
    const btn = doc.createElement("button");
    btn.type = "button";
    btn.className = "apexcharts-ink-add";
    btn.textContent = "+ Note";
    const bs = btn.style;
    bs.cursor = "pointer";
    bs.font = "12px sans-serif";
    bs.padding = "4px 9px";
    bs.borderRadius = "5px";
    bs.border = "1px solid #6366f1";
    bs.color = "#4338ca";
    bs.background = "#fff";
    btn.addEventListener("click", (e2) => {
      e2.stopPropagation();
      if (this._creating) this.stopCreate();
      else this.startCreate();
    });
    bar.appendChild(btn);
    elWrap.appendChild(bar);
    this._syncPalette();
  }
  /** Reflect create-mode state on the palette button. */
  _syncPalette() {
    const elWrap = this.w.dom.elWrap;
    const btn = (
      /** @type {any} */
      elWrap && elWrap.querySelector(".apexcharts-ink-add")
    );
    if (!btn) return;
    if (this._creating) {
      btn.style.background = "#6366f1";
      btn.style.color = "#fff";
      btn.textContent = "Click chart...";
    } else {
      btn.style.background = "#fff";
      btn.style.color = "#4338ca";
      btn.textContent = "+ Note";
    }
  }
  // ─── P2 + P6: the floating note editor card ──────────────────────────────
  // Click (or double-click) an ink-managed annotation to open a small card
  // anchored to it: rename inline, recolor via accent swatches, toggle bold,
  // step the font size, size/reshape the marker (points), or delete the note.
  /** @returns {string[]} the accent swatches offered by the editor */
  _noteColors() {
    const ink = this.w.config.chart.ink;
    return ink && Array.isArray(ink.noteColors) && ink.noteColors.length ? ink.noteColors : NOTE_COLORS;
  }
  /**
   * Perceived-luminance check so text/border contrast follows the accent.
   * @param {string} hex
   */
  static _isLight(hex) {
    const h2 = String(hex || "").replace("#", "");
    const full = h2.length === 3 ? h2.split("").map((c2) => c2 + c2).join("") : h2;
    const n2 = parseInt(full, 16);
    if (isNaN(n2) || full.length !== 6) return false;
    const r2 = n2 >> 16 & 255;
    const g2 = n2 >> 8 & 255;
    const b2 = n2 & 255;
    return (0.299 * r2 + 0.587 * g2 + 0.114 * b2) / 255 > 0.72;
  }
  /** @param {any} style */
  static _isBold(style) {
    const fw = style && style.fontWeight;
    return fw === "bold" || parseInt(String(fw), 10) >= 600;
  }
  /**
   * Small icon/text button for the editor card. mousedown is prevented so the
   * text input keeps focus while formatting.
   * @param {any} doc @param {string} content @param {string} title
   * @param {Function} onClick @param {string} [extraClass] @param {boolean} [isSvg]
   */
  _cardBtn(doc, content, title, onClick, extraClass, isSvg) {
    const b2 = doc.createElement("button");
    b2.type = "button";
    b2.className = "apexcharts-ink-btn" + (extraClass ? " " + extraClass : "");
    b2.title = title;
    b2.setAttribute("aria-label", title);
    if (isSvg) b2.innerHTML = content;
    else b2.textContent = content;
    b2.addEventListener("mousedown", (e2) => e2.preventDefault());
    b2.addEventListener("click", (e2) => {
      e2.stopPropagation();
      onClick();
    });
    return b2;
  }
  /**
   * Uppercase row caption ("Label" / "Line" / "Marker") for the editor card.
   * @param {any} doc @param {string} text
   */
  _cardLabel(doc, text) {
    const lab = doc.createElement("span");
    lab.className = "apexcharts-ink-cardlabel";
    lab.textContent = text;
    return lab;
  }
  /**
   * Color swatch button for the editor card. mousedown is prevented so the
   * text input keeps focus while restyling.
   * @param {any} doc @param {string} c
   * @param {(c: string) => void} pick @param {string} [extraClass]
   */
  _mkSwatch(doc, c2, pick, extraClass) {
    const sw = doc.createElement("button");
    sw.type = "button";
    sw.className = "apexcharts-ink-swatch" + (extraClass ? " " + extraClass : "");
    sw.title = c2;
    sw.setAttribute("aria-label", "Color " + c2);
    sw.dataset.color = c2;
    sw.style.background = c2;
    sw.addEventListener("mousedown", (e2) => e2.preventDefault());
    sw.addEventListener("click", (e2) => {
      e2.stopPropagation();
      pick(c2);
    });
    return sw;
  }
  /**
   * Open the floating editor card for an annotation.
   * @param {string} type @param {number} index
   * @param {{select?: boolean}} [opts] select: preselect the text (create / dblclick)
   */
  _startEdit(type, index, opts = {}) {
    const w2 = this.w;
    const anno = this._annoList(type)[index];
    const baseEl = w2.dom.baseEl;
    const elWrap = w2.dom.elWrap;
    if (!anno || !anno.id || !baseEl || !elWrap) return;
    this._closeEditor(false);
    const doc = baseEl.ownerDocument;
    const card = doc.createElement("div");
    card.className = "apexcharts-ink-card";
    card.setAttribute("role", "dialog");
    card.setAttribute("aria-label", "Edit note");
    card.style.visibility = "hidden";
    const rowText = doc.createElement("div");
    rowText.className = "apexcharts-ink-card-row";
    const input = doc.createElement("input");
    input.type = "text";
    input.className = "apexcharts-ink-editor";
    input.placeholder = "Note text";
    input.value = anno.label && anno.label.text || "";
    rowText.appendChild(input);
    rowText.appendChild(
      this._cardBtn(
        doc,
        TRASH_ICON,
        "Delete note",
        () => this._deleteAnno(),
        "apexcharts-ink-btn--delete",
        true
      )
    );
    card.appendChild(rowText);
    const rowStyle = doc.createElement("div");
    rowStyle.className = "apexcharts-ink-card-row";
    if (type !== "point") {
      rowStyle.appendChild(this._cardLabel(doc, "Label"));
    }
    this._noteColors().forEach((c2) => {
      rowStyle.appendChild(this._mkSwatch(doc, c2, (col) => this._applyColor(col)));
    });
    const sep = doc.createElement("span");
    sep.className = "apexcharts-ink-sep";
    rowStyle.appendChild(sep);
    rowStyle.appendChild(
      this._cardBtn(doc, "B", "Bold", () => this._toggleBold(), "apexcharts-ink-btn--bold")
    );
    rowStyle.appendChild(this._cardBtn(doc, "A-", "Smaller text", () => this._stepFont(-1)));
    rowStyle.appendChild(this._cardBtn(doc, "A+", "Larger text", () => this._stepFont(1)));
    card.appendChild(rowStyle);
    if (type !== "point") {
      const rowLine = doc.createElement("div");
      rowLine.className = "apexcharts-ink-card-row";
      rowLine.appendChild(this._cardLabel(doc, "Line"));
      this._noteColors().forEach((c2) => {
        rowLine.appendChild(
          this._mkSwatch(
            doc,
            c2,
            (col) => this._applyLineColor(col),
            "apexcharts-ink-swatch--line"
          )
        );
      });
      card.appendChild(rowLine);
    }
    if (type === "point") {
      const rowMarker = doc.createElement("div");
      rowMarker.className = "apexcharts-ink-card-row";
      rowMarker.appendChild(this._cardLabel(doc, "Marker"));
      rowMarker.appendChild(
        this._cardBtn(doc, "-", "Smaller marker", () => this._stepMarker(-1))
      );
      const sizeOut = doc.createElement("span");
      sizeOut.className = "apexcharts-ink-marker-size";
      rowMarker.appendChild(sizeOut);
      rowMarker.appendChild(
        this._cardBtn(doc, "+", "Larger marker", () => this._stepMarker(1))
      );
      rowMarker.appendChild(
        this._cardBtn(
          doc,
          SHAPE_GLYPHS.circle,
          "Marker shape",
          () => this._cycleShape(),
          "apexcharts-ink-btn--shape"
        )
      );
      card.appendChild(rowMarker);
    }
    elWrap.appendChild(card);
    this._editor = { card, input, type, index };
    this._positionCard();
    this._syncCard();
    card.style.visibility = "";
    input.focus();
    if (opts.select) input.select();
    card.addEventListener("keydown", (e2) => {
      if (e2.key === "Escape") {
        e2.preventDefault();
        e2.stopPropagation();
        this._closeEditor(false);
      } else if (e2.key === "Enter" && e2.target === input) {
        e2.preventDefault();
        this._closeEditor(true);
      }
    });
    doc.addEventListener("mousedown", this._onDocDownEditor, true);
    doc.addEventListener("touchstart", this._onDocDownEditor, true);
  }
  /**
   * Commit + close on any press outside the card.
   * @param {any} e
   */
  _onDocDownEditor(e2) {
    const ed = this._editor;
    if (!ed || ed.card.contains(e2.target)) return;
    this._closeEditor(true);
  }
  /**
   * Anchor the card to the annotation's label (below it, or above when there
   * is no room), clamped inside the chart wrap. Re-run after each restyle
   * since the label rect changes.
   */
  _positionCard() {
    const ed = this._editor;
    if (!ed) return;
    const w2 = this.w;
    const baseEl = w2.dom.baseEl;
    const elWrap = w2.dom.elWrap;
    const anno = this._annoList(ed.type)[ed.index];
    if (!baseEl || !elWrap || !anno) return;
    const anchor = baseEl.querySelector(
      ".apexcharts-" + ed.type + "-annotation-label." + anno.id
    ) || baseEl.querySelector("." + anno.id);
    if (!anchor) return;
    const wrapRect = elWrap.getBoundingClientRect();
    const aRect = anchor.getBoundingClientRect();
    const cw = ed.card.offsetWidth;
    const ch = ed.card.offsetHeight;
    let left = Math.round(aRect.left - wrapRect.left);
    let top = Math.round(aRect.bottom - wrapRect.top) + 8;
    if (top + ch > elWrap.clientHeight - 4) {
      top = Math.round(aRect.top - wrapRect.top) - ch - 8;
    }
    if (left + cw > elWrap.clientWidth - 4) left = elWrap.clientWidth - cw - 4;
    ed.card.style.left = Math.max(4, left) + "px";
    ed.card.style.top = Math.max(4, top) + "px";
  }
  /** Reflect the annotation's current style on the card controls. */
  _syncCard() {
    const ed = this._editor;
    if (!ed) return;
    const anno = this._annoList(ed.type)[ed.index];
    if (!anno) return;
    const style = anno.label && anno.label.style || {};
    const bg = String(style.background || "").toLowerCase();
    ed.card.querySelectorAll(".apexcharts-ink-swatch:not(.apexcharts-ink-swatch--line)").forEach((sw) => {
      sw.classList.toggle(
        "apexcharts-ink-swatch--active",
        (sw.dataset.color || "").toLowerCase() === bg
      );
    });
    const stroke = String(anno.borderColor || "").toLowerCase();
    ed.card.querySelectorAll(".apexcharts-ink-swatch--line").forEach((sw) => {
      sw.classList.toggle(
        "apexcharts-ink-swatch--active",
        (sw.dataset.color || "").toLowerCase() === stroke
      );
    });
    const boldBtn = ed.card.querySelector(".apexcharts-ink-btn--bold");
    if (boldBtn) {
      boldBtn.classList.toggle("apexcharts-ink-btn--active", InkLayer._isBold(style));
    }
    const m2 = anno.marker || {};
    const sizeOut = ed.card.querySelector(".apexcharts-ink-marker-size");
    if (sizeOut) {
      sizeOut.textContent = String(typeof m2.size === "number" ? m2.size : 4);
    }
    const shapeBtn = ed.card.querySelector(".apexcharts-ink-btn--shape");
    if (shapeBtn) {
      shapeBtn.textContent = SHAPE_GLYPHS[m2.shape] || SHAPE_GLYPHS.circle;
    }
  }
  /**
   * Commit the input's text into the annotation (the card stays open). Also
   * runs before any style apply so typed-but-unconfirmed text survives the
   * redraw.
   * @param {any} ed
   */
  _commitTextOf(ed) {
    const anno = this._annoList(ed.type)[ed.index];
    if (!anno) return;
    const text = ed.input.value;
    if (!anno.label) anno.label = {};
    if (anno.label.text === text) return;
    anno.label.text = text;
    this._redrawAnno(ed.type, anno, ed.index);
    this._checkpoint("ink:edit");
    this._fireEdited(ed.type, anno, ed.index);
  }
  /**
   * Close the editor card. commit=true also commits the pending text. Style
   * edits apply immediately and are not rolled back by Escape; use undo.
   * @param {boolean} commit
   */
  _closeEditor(commit) {
    const ed = this._editor;
    if (!ed) return;
    this._editor = null;
    const doc = this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
    if (doc) {
      doc.removeEventListener("mousedown", this._onDocDownEditor, true);
      doc.removeEventListener("touchstart", this._onDocDownEditor, true);
    }
    if (ed.card.parentNode) ed.card.parentNode.removeChild(ed.card);
    if (commit) this._commitTextOf(ed);
  }
  /**
   * Apply a config mutation from a card control: commit pending text, mutate,
   * redraw, checkpoint for undo, then refresh + re-anchor the card.
   * @param {string} label @param {(anno: any) => void} mutate
   */
  _applyStyle(label, mutate) {
    const ed = this._editor;
    if (!ed) return;
    const anno = this._annoList(ed.type)[ed.index];
    if (!anno) return;
    this._commitTextOf(ed);
    if (!anno.label) anno.label = {};
    if (!anno.label.style) anno.label.style = {};
    mutate(anno);
    this._redrawAnno(ed.type, anno, ed.index);
    this._checkpoint(label);
    this._fireStyled(ed.type, anno, ed.index);
    this._syncCard();
    this._positionCard();
  }
  /**
   * Apply an accent color: label chip + marker (points) or line/range fill
   * (axis annotations), with text/border contrast following the luminance.
   * @param {string} c
   */
  _applyColor(c2) {
    const ed = this._editor;
    if (!ed) return;
    const light = InkLayer._isLight(c2);
    this._applyStyle("ink:style", (anno) => {
      anno.label.style.background = c2;
      anno.label.style.color = light ? "#334155" : "#ffffff";
      anno.label.borderColor = light ? "#cbd5e1" : c2;
      if (ed.type === "point") {
        if (!anno.marker) anno.marker = {};
        anno.marker.strokeColor = light ? "#334155" : c2;
        anno.marker.fillColor = light ? "#ffffff" : c2;
      }
    });
  }
  /**
   * Line-stroke color for axis annotations, separate from the label chip.
   * @param {string} c
   */
  _applyLineColor(c2) {
    const ed = this._editor;
    if (!ed || ed.type === "point") return;
    this._applyStyle("ink:style", (anno) => {
      anno.borderColor = c2;
      if (anno.x2 != null || anno.y2 != null) anno.fillColor = c2;
    });
  }
  /**
   * Step the label font size through the preset scale.
   * @param {number} dir
   */
  _stepFont(dir) {
    this._applyStyle("ink:style", (anno) => {
      const cur = parseFloat(anno.label.style.fontSize) || 11;
      let i2 = 0;
      for (let k = 1; k < FONT_STEPS.length; k++) {
        if (Math.abs(FONT_STEPS[k] - cur) < Math.abs(FONT_STEPS[i2] - cur)) i2 = k;
      }
      i2 = Math.min(FONT_STEPS.length - 1, Math.max(0, i2 + dir));
      anno.label.style.fontSize = FONT_STEPS[i2] + "px";
    });
  }
  _toggleBold() {
    this._applyStyle("ink:style", (anno) => {
      anno.label.style.fontWeight = InkLayer._isBold(anno.label.style) ? 400 : 700;
    });
  }
  /**
   * Grow/shrink the point marker.
   * @param {number} dir
   */
  _stepMarker(dir) {
    this._applyStyle("ink:style", (anno) => {
      if (!anno.marker) anno.marker = {};
      const cur = typeof anno.marker.size === "number" ? anno.marker.size : 4;
      anno.marker.size = Math.min(14, Math.max(2, cur + dir));
    });
  }
  /** Cycle the point marker shape (circle, square, diamond, triangle). */
  _cycleShape() {
    this._applyStyle("ink:style", (anno) => {
      if (!anno.marker) anno.marker = {};
      const i2 = MARKER_SHAPES.indexOf(anno.marker.shape);
      anno.marker.shape = MARKER_SHAPES[(i2 + 1) % MARKER_SHAPES.length];
    });
  }
  /** Delete the annotation the editor is open on (undoable via Rewind). */
  _deleteAnno() {
    const ed = this._editor;
    if (!ed) return;
    const list = this._annoList(ed.type);
    const anno = list[ed.index];
    this._closeEditor(false);
    if (!anno) return;
    const baseEl = this.w.dom.baseEl;
    if (baseEl && anno.id) {
      baseEl.querySelectorAll("." + anno.id).forEach((el) => el.remove());
    }
    list.splice(ed.index, 1);
    for (let i2 = ed.index; i2 < list.length; i2++) {
      this._redrawAnno(ed.type, list[i2], i2);
    }
    this._checkpoint("ink:delete");
    this._fireDeleted(ed.type, anno, ed.index);
  }
  /** @param {string} type @param {any} anno @param {number} index */
  _fireEdited(type, anno, index) {
    const args = { type, id: anno.id, index, text: anno.label ? anno.label.text : "" };
    this._fireAnnotationEvent("annotationEdited", args);
  }
  /** @param {string} type @param {any} anno @param {number} index */
  _fireStyled(type, anno, index) {
    const args = { type, id: anno.id, index, label: anno.label, marker: anno.marker };
    this._fireAnnotationEvent("annotationStyled", args);
  }
  /** @param {string} type @param {any} anno @param {number} index */
  _fireDeleted(type, anno, index) {
    const args = { type, id: anno.id, index };
    this._fireAnnotationEvent("annotationDeleted", args);
  }
  // ─── lifecycle ────────────────────────────────────────────────────────────
  _teardownDocListeners() {
    const doc = this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
    if (!doc) return;
    doc.removeEventListener("mousemove", this._onMove);
    doc.removeEventListener("touchmove", this._onMove);
    doc.removeEventListener("mouseup", this._onUp);
    doc.removeEventListener("touchend", this._onUp);
  }
  teardown() {
    var _a, _b, _c, _d;
    this._teardownDocListeners();
    this._closeEditor(false);
    this.stopCreate();
    this._drag = null;
    if (this._wired) {
      (_b = (_a = this.ctx).removeEventListener) == null ? void 0 : _b.call(_a, "mounted", this._onRerender);
      (_d = (_c = this.ctx).removeEventListener) == null ? void 0 : _d.call(_c, "updated", this._onRerender);
      this._wired = false;
    }
  }
}
ApexCharts.registerFeatures({ ink: InkLayer });
class Measure {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.graphics = new Graphics(w2);
    this.pins = [];
    this.drag = null;
    this.armed = false;
    this.persistent = false;
    this.pane = null;
    this._seedActive = false;
    this._onKeyDown = this._onKeyDown.bind(this);
    this._onKeyUp = this._onKeyUp.bind(this);
    this._onDown = this._onDown.bind(this);
    this._onMove = this._onMove.bind(this);
    this._onUp = this._onUp.bind(this);
    this._onSeedDown = this._onSeedDown.bind(this);
    this._onSeedKey = this._onSeedKey.bind(this);
    this._afterRender = this._afterRender.bind(this);
    ctx.addEventListener("mounted", this._afterRender);
    ctx.addEventListener("updated", this._afterRender);
    this._bindKeys();
  }
  _cfg() {
    return this.w.config.chart.measure || {};
  }
  _enabled() {
    return this.w.globals.axisCharts && this._cfg().enabled === true;
  }
  _mode() {
    return this._cfg().mode === "free" ? "free" : "span";
  }
  /**
   * Nearest first-series data point to a data x (used to snap span endpoints
   * onto the series line).
   * @param {number} dataX
   * @returns {{x:number,y:number}|null}
   */
  _snapToSeries(dataX) {
    const s0 = (
      /** @type {any} */
      (this.w.config.series || [])[0]
    );
    if (!s0) return null;
    const pts = this._points(s0.data || []);
    if (!pts.length) return null;
    let best = pts[0];
    let bd = Math.abs(pts[0].x - dataX);
    for (let i2 = 1; i2 < pts.length; i2++) {
      const d = Math.abs(pts[i2].x - dataX);
      if (d < bd) {
        bd = d;
        best = pts[i2];
      }
    }
    return best;
  }
  /**
   * Resolve a raw projected point to the endpoint that is actually drawn: span
   * mode snaps x to the nearest first-series data point (y follows the line);
   * free mode keeps the raw point.
   * @param {{x:number,y:number,gx:number,gy:number}} raw
   */
  _resolve(raw) {
    if (this._mode() === "free") return raw;
    const snapped = this._snapToSeries(raw.x);
    if (!snapped) return raw;
    const g2 = this._dataToGrid(snapped.x, snapped.y);
    return { x: snapped.x, y: snapped.y, gx: g2.gx, gy: g2.gy };
  }
  _doc() {
    return this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
  }
  /** Bind the measure-key listeners once on the owner document. */
  _bindKeys() {
    if (this._keysBound) return;
    const doc = this._doc();
    if (!doc) return;
    doc.addEventListener("keydown", this._onKeyDown);
    doc.addEventListener("keyup", this._onKeyUp);
    this._keysBound = true;
  }
  /** @param {any} e */
  _onKeyDown(e2) {
    if (!this._enabled() || this.persistent) return;
    const key = this._cfg().key || "m";
    if (e2.key && e2.key.toLowerCase() === String(key).toLowerCase()) {
      this._arm();
    } else if (e2.key === "Escape") {
      this._cancelDrag();
    }
  }
  /** @param {any} e */
  _onKeyUp(e2) {
    if (this.persistent) return;
    const key = this._cfg().key || "m";
    if (e2.key && e2.key.toLowerCase() === String(key).toLowerCase()) {
      if (!this.drag) this._disarm();
    }
  }
  /** Public: arm a sticky measure mode (survives key release) until stopped. */
  startMeasure() {
    if (!this._enabled()) return;
    this.persistent = true;
    this._arm();
  }
  /** Public: leave measure mode. */
  stopMeasure() {
    this.persistent = false;
    this._cancelDrag();
    this._disarm();
  }
  /**
   * Public: begin a measurement anchored at a client-space point (used by the
   * context menu's "Measure from here"). Endpoint A is fixed at (cx,cy), the
   * ruler follows the cursor, and the next pointer press sets B and pins it.
   * Escape cancels. No-op unless the measure tool is enabled.
   * @param {number} cx @param {number} cy
   */
  seedFromClient(cx, cy) {
    if (!this._enabled()) return;
    this._cancelDrag();
    this._endSeed(false);
    const a2 = this._project(cx, cy);
    this.drag = { a: a2, b: a2 };
    this._seedActive = true;
    const doc = this._doc();
    if (doc) {
      doc.addEventListener("mousemove", this._onMove);
      doc.addEventListener("touchmove", this._onMove, { passive: false });
      doc.addEventListener("mousedown", this._onSeedDown, true);
      doc.addEventListener("touchstart", this._onSeedDown, true);
      doc.addEventListener("keydown", this._onSeedKey, true);
    }
    this._renderLive();
  }
  /** @param {any} e */
  _onSeedDown(e2) {
    if (!this._seedActive || !this.drag) return;
    e2.preventDefault();
    e2.stopPropagation();
    const { cx, cy } = this._clientXY(e2);
    this.drag.b = this._project(cx, cy);
    this._endSeed(true);
  }
  /** @param {any} e */
  _onSeedKey(e2) {
    if (e2.key === "Escape") this._endSeed(false);
  }
  /**
   * Finish (commit) or cancel a "measure from here" seed and detach listeners.
   * @param {boolean} commit
   */
  _endSeed(commit) {
    var _a, _b;
    if (!this._seedActive) return;
    this._seedActive = false;
    const doc = this._doc();
    if (doc) {
      doc.removeEventListener("mousemove", this._onMove);
      doc.removeEventListener("touchmove", this._onMove);
      doc.removeEventListener("mousedown", this._onSeedDown, true);
      doc.removeEventListener("touchstart", this._onSeedDown, true);
      doc.removeEventListener("keydown", this._onSeedKey, true);
    }
    const d = this.drag;
    this.drag = null;
    this._clearLive();
    if (!commit || !d) return;
    const a2 = this._resolve(d.a);
    const b2 = this._resolve(d.b);
    if (Math.abs(a2.gx - b2.gx) < 2 && Math.abs(a2.gy - b2.gy) < 2) return;
    const mode = this._mode();
    if (this._cfg().pinOnRelease !== false) {
      this.pins.push({ xa: a2.x, ya: a2.y, xb: b2.x, yb: b2.y, mode });
      this._renderPins();
      (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, "measure");
    }
    this._fireMeasured(a2, b2);
  }
  /** Public: remove all pinned rulers. */
  clearMeasures() {
    var _a, _b;
    this.pins = [];
    this._renderPins();
    (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, "clear measures");
  }
  /**
   * Snapshot of the pinned rulers as JSON-safe plain data. This is the piece of
   * state ViewState / Perspectives (shareable URL) / Rewind (undo) persist:
   * pins already live in data space, so they round-trip and re-project.
   * Returns a deep copy so callers cannot mutate ours.
   * @returns {Array<{xa:number,ya:number,xb:number,yb:number,mode:string}>}
   */
  getPins() {
    return this.pins.map((p2) => ({
      xa: p2.xa,
      ya: p2.ya,
      xb: p2.xb,
      yb: p2.yb,
      mode: p2.mode === "free" ? "free" : "span"
    }));
  }
  /**
   * Replace the pinned rulers with a restored set and redraw. Accepts the shape
   * getPins() returns; a nullish / non-array value (or an old token without
   * measure state) clears all pins. Non-finite entries are dropped. Does NOT
   * record a Rewind step (the restore itself is the caller's undo boundary).
   * @param {any} pins
   */
  setPins(pins) {
    this.pins = Array.isArray(pins) ? pins.filter(
      (p2) => p2 && isFinite(p2.xa) && isFinite(p2.ya) && isFinite(p2.xb) && isFinite(p2.yb)
    ).map((p2) => ({
      xa: +p2.xa,
      ya: +p2.ya,
      xb: +p2.xb,
      yb: +p2.yb,
      mode: p2.mode === "free" ? "free" : "span"
    })) : [];
    this._renderPins();
  }
  /**
   * Normalize a series `data` array into {x,y} points (numeric/datetime x or
   * category index). Non-finite / non-scalar (range/candle) points drop out.
   * @param {any[]} data
   * @returns {Array<{x:number,y:number}>}
   */
  _points(data) {
    const out = [];
    for (let i2 = 0; i2 < data.length; i2++) {
      const p2 = data[i2];
      let x;
      let y;
      if (Array.isArray(p2)) {
        x = +p2[0];
        y = +p2[1];
      } else if (p2 && typeof p2 === "object") {
        x = +p2.x;
        y = +p2.y;
      } else {
        x = i2;
        y = +p2;
      }
      if (isFinite(x) && isFinite(y)) out.push({ x, y });
    }
    return out;
  }
  /** Lay the transparent capture pane over the plot so our pointer handlers own
   *  the drag (ZoomPanSelection never sees it). */
  _arm() {
    if (this.armed || !this._enabled()) return;
    const w2 = this.w;
    const parent = w2.dom.elGraphical;
    if (!parent) return;
    this.armed = true;
    const pane = this.graphics.drawRect(0, 0, w2.layout.gridWidth, w2.layout.gridHeight);
    pane.node.setAttribute("class", "apexcharts-measure-capture");
    pane.node.setAttribute("fill", "transparent");
    pane.node.style.cursor = "crosshair";
    pane.node.style.pointerEvents = "all";
    pane.node.addEventListener("mousedown", this._onDown);
    pane.node.addEventListener("touchstart", this._onDown, { passive: false });
    parent.add(pane);
    this.pane = pane;
  }
  _disarm() {
    this.armed = false;
    if (this.pane) {
      this.pane.node.removeEventListener("mousedown", this._onDown);
      this.pane.node.removeEventListener("touchstart", this._onDown);
      const p2 = this.pane.node;
      if (p2.parentNode) p2.parentNode.removeChild(p2);
      this.pane = null;
    }
  }
  /** @param {any} e @returns {{cx:number,cy:number}} */
  _clientXY(e2) {
    const t2 = e2.touches && e2.touches[0] ? e2.touches[0] : e2;
    return { cx: t2.clientX, cy: t2.clientY };
  }
  _gridRect() {
    const g2 = this.w.dom.baseEl.querySelector(".apexcharts-grid");
    return g2 ? g2.getBoundingClientRect() : null;
  }
  /** [min,max] for the primary y-axis, preferring the rendered nice scale. */
  _yRange() {
    const g2 = this.w.globals;
    const s2 = g2.yAxisScale && g2.yAxisScale[0];
    if (s2 && isFinite(s2.niceMin) && isFinite(s2.niceMax) && s2.niceMax !== s2.niceMin) {
      return [s2.niceMin, s2.niceMax];
    }
    return [g2.minY, g2.maxY];
  }
  /**
   * Client pixel -> { x, y (data), gx, gy (grid-local SVG units) }.
   * @param {number} cx @param {number} cy
   */
  _project(cx, cy) {
    const w2 = this.w;
    const rect = this._gridRect();
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const clamp = (v2) => v2 < 0 ? 0 : v2 > 1 ? 1 : v2;
    const fx = rect ? clamp((cx - rect.left) / rect.width) : 0;
    const fy = rect ? clamp((cy - rect.top) / rect.height) : 0;
    const [ymin, ymax] = this._yRange();
    const x = w2.globals.minX + fx * (w2.globals.maxX - w2.globals.minX);
    const y = ymax - fy * (ymax - ymin);
    return { x, y, gx: fx * gw, gy: fy * gh };
  }
  /**
   * Data (x,y) -> grid-local SVG coords, for redrawing pinned rulers.
   * @param {number} x @param {number} y
   */
  _dataToGrid(x, y) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const xr = w2.globals.maxX - w2.globals.minX || 1;
    const [ymin, ymax] = this._yRange();
    const yr = ymax - ymin || 1;
    return {
      gx: (x - w2.globals.minX) / xr * gw,
      gy: gh - (y - ymin) / yr * gh
    };
  }
  /** @param {any} e */
  _onDown(e2) {
    if (!this.armed) return;
    e2.preventDefault();
    e2.stopPropagation();
    const { cx, cy } = this._clientXY(e2);
    const a2 = this._project(cx, cy);
    this.drag = { a: a2, b: a2 };
    const doc = this._doc();
    if (doc) {
      doc.addEventListener("mousemove", this._onMove);
      doc.addEventListener("mouseup", this._onUp);
      doc.addEventListener("touchmove", this._onMove, { passive: false });
      doc.addEventListener("touchend", this._onUp);
    }
    this._renderLive();
  }
  /** @param {any} e */
  _onMove(e2) {
    if (!this.drag) return;
    if (e2.cancelable) e2.preventDefault();
    const { cx, cy } = this._clientXY(e2);
    this.drag.b = this._project(cx, cy);
    this._renderLive();
  }
  /** @param {any} _e */
  _onUp(_e) {
    var _a, _b;
    const doc = this._doc();
    if (doc) {
      doc.removeEventListener("mousemove", this._onMove);
      doc.removeEventListener("mouseup", this._onUp);
      doc.removeEventListener("touchmove", this._onMove);
      doc.removeEventListener("touchend", this._onUp);
    }
    if (!this.drag) return;
    const rawA = this.drag.a;
    const rawB = this.drag.b;
    this.drag = null;
    this._clearLive();
    if (Math.abs(rawA.gx - rawB.gx) < 2 && Math.abs(rawA.gy - rawB.gy) < 2) {
      if (!this.persistent) this._disarm();
      return;
    }
    const mode = this._mode();
    const a2 = this._resolve(rawA);
    const b2 = this._resolve(rawB);
    if (this._cfg().pinOnRelease !== false) {
      this.pins.push({ xa: a2.x, ya: a2.y, xb: b2.x, yb: b2.y, mode });
      this._renderPins();
      (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, "measure");
    }
    this._fireMeasured(a2, b2);
    if (!this.persistent) this._disarm();
  }
  _cancelDrag() {
    const doc = this._doc();
    if (doc) {
      doc.removeEventListener("mousemove", this._onMove);
      doc.removeEventListener("mouseup", this._onUp);
      doc.removeEventListener("touchmove", this._onMove);
      doc.removeEventListener("touchend", this._onUp);
    }
    this.drag = null;
    this._clearLive();
  }
  /**
   * @param {{x:number,y:number}} a @param {{x:number,y:number}} b
   * @returns {{dx:number,dy:number,pct:number,slope:number}}
   */
  _stats(a2, b2) {
    const dx = b2.x - a2.x;
    const dy = b2.y - a2.y;
    return {
      dx,
      dy,
      pct: a2.y !== 0 ? dy / Math.abs(a2.y) * 100 : NaN,
      slope: dx !== 0 ? dy / dx : NaN
    };
  }
  /** @param {number} v */
  _fmt(v2) {
    if (!isFinite(v2)) return "n/a";
    const a2 = Math.abs(v2);
    if (a2 !== 0 && (a2 < 0.01 || a2 >= 1e6)) return v2.toExponential(2);
    return String(Math.round(v2 * 100) / 100);
  }
  /** getComputedStyle of the graphical layer (browser only), for CSS vars. */
  _computedStyle() {
    if (!Environment.isBrowser()) return null;
    const node = this.w.dom.elGraphical && this.w.dom.elGraphical.node;
    if (!node || typeof getComputedStyle !== "function") return null;
    try {
      return getComputedStyle(node);
    } catch (e2) {
      return null;
    }
  }
  /**
   * Resolve a color: explicit config value, else a `--apx-measure-*` CSS custom
   * property, else the built-in default.
   * @param {any} cfgVal @param {string} varName @param {string} fallback
   * @param {CSSStyleDeclaration|null} [cs]
   */
  _resolveColor(cfgVal, varName, fallback, cs) {
    if (cfgVal) return cfgVal;
    const style = cs !== void 0 ? cs : this._computedStyle();
    const v2 = style ? style.getPropertyValue(varName).trim() : "";
    return v2 || fallback;
  }
  /** Resolved semantic colors (config -> CSS var -> built-in default). */
  _colors() {
    const c2 = this._cfg().colors || {};
    const cs = this._computedStyle();
    return {
      up: this._resolveColor(c2.up, "--apx-measure-up", "#16a34a", cs),
      down: this._resolveColor(c2.down, "--apx-measure-down", "#dc2626", cs),
      neutral: this._resolveColor(c2.neutral, "--apx-measure-neutral", "#64748b", cs),
      guide: this._resolveColor(c2.guide, "--apx-measure-guide", "#94a3b8", cs)
    };
  }
  /** @param {number} dy */
  _dirColor(dy) {
    const c2 = this._colors();
    return dy === 0 ? c2.neutral : dy > 0 ? c2.up : c2.down;
  }
  /** @param {number} dy */
  _dirClass(dy) {
    return dy === 0 ? "apexcharts-measure-flat" : dy > 0 ? "apexcharts-measure-up" : "apexcharts-measure-down";
  }
  /**
   * Format a percentage, via `measure.format.percent` when set.
   * @param {number} p
   */
  _fmtPct(p2) {
    if (!isFinite(p2)) return "n/a";
    const f = this._cfg().format && this._cfg().format.percent;
    if (typeof f === "function") {
      try {
        const s2 = f(p2);
        if (s2 != null) return String(s2);
      } catch (e2) {
      }
    }
    return (p2 >= 0 ? "+" : "") + this._fmt(p2) + "%";
  }
  /**
   * The readout lines for a ruler: `measure.label` override, else the default
   * per-mode text.
   * @param {{x:number,y:number}} a @param {{x:number,y:number}} b
   * @param {{dx:number,dy:number,pct:number,slope:number}} st @param {string} mode
   * @returns {string[]}
   */
  _label(a2, b2, st, mode) {
    const fn = this._cfg().label;
    if (typeof fn === "function") {
      const out = fn({
        from: { x: a2.x, y: a2.y },
        to: { x: b2.x, y: b2.y },
        dx: st.dx,
        dy: st.dy,
        percentChange: st.pct,
        slope: st.slope,
        mode
      });
      return Array.isArray(out) ? out.map(String) : [String(out)];
    }
    if (mode === "span") {
      const arrow = st.dy === 0 ? "" : st.dy > 0 ? " ↑" : " ↓";
      const pct = isFinite(st.pct) ? "(" + this._fmtPct(st.pct) + ")" : "";
      return [this._fmtY(st.dy) + "  " + pct + arrow, this._fmtX(a2.x) + "  to  " + this._fmtX(b2.x)];
    }
    return ["Δx " + this._fmtDx(st.dx), "Δy " + this._fmtY(st.dy), this._fmtPct(st.pct)];
  }
  /**
   * Format an x delta, treating datetime x as a day count.
   * @param {number} dx
   */
  _fmtDx(dx) {
    if (this.w.config.xaxis.type === "datetime") {
      const days = dx / 864e5;
      return Math.round(days * 10) / 10 + "d";
    }
    return this._fmt(dx);
  }
  /**
   * Format a y value (a delta) via the y-axis label formatter when present.
   * @param {number} v
   */
  _fmtY(v2) {
    const cf = this._cfg().format && this._cfg().format.y;
    if (typeof cf === "function") {
      try {
        const s2 = cf(v2);
        if (s2 != null) return String(s2);
      } catch (e2) {
      }
    }
    const f = this.w.globals.yLabelFormatters && this.w.globals.yLabelFormatters[0];
    if (typeof f === "function") {
      try {
        const s2 = f(v2, { seriesIndex: 0, dataPointIndex: -1, w: this.w });
        if (s2 != null && s2 !== "") return String(s2);
      } catch (e2) {
      }
    }
    return this._fmt(v2);
  }
  /**
   * Format an x value: a short date for datetime x, else the x-label formatter
   * or a plain number.
   * @param {number} x
   */
  _fmtX(x) {
    const w2 = this.w;
    const cf = this._cfg().format && this._cfg().format.x;
    if (typeof cf === "function") {
      try {
        const s2 = cf(x);
        if (s2 != null) return String(s2);
      } catch (e2) {
      }
    }
    if (w2.config.xaxis.type === "datetime") {
      const d = new Date(x);
      return d.toLocaleDateString(void 0, {
        day: "numeric",
        month: "short",
        year: "numeric"
      });
    }
    const f = w2.globals.xLabelFormatter;
    if (typeof f === "function") {
      try {
        const s2 = f(x, { w: w2 });
        if (s2 != null && s2 !== "") return String(s2);
      } catch (e2) {
      }
    }
    return this._fmt(x);
  }
  /** The live overlay group, created lazily inside elGraphical. */
  _liveGroup() {
    const w2 = this.w;
    if (this._live && this._live.node && this._live.node.parentNode) {
      return this._live;
    }
    const g2 = this.graphics.group({ class: "apexcharts-measure-live" });
    g2.node.style.pointerEvents = "none";
    if (w2.dom.elGraphical) w2.dom.elGraphical.add(g2);
    this._live = g2;
    return g2;
  }
  _clearLive() {
    if (this._live && this._live.node) {
      const n2 = this._live.node;
      if (n2.parentNode) n2.parentNode.removeChild(n2);
    }
    this._live = null;
  }
  _renderLive() {
    if (!this.drag) return;
    const g2 = this._liveGroup();
    while (g2.node.firstChild) g2.node.removeChild(g2.node.firstChild);
    const a2 = this._resolve(this.drag.a);
    const b2 = this._resolve(this.drag.b);
    this._drawRuler(g2, a2, b2, false, this._mode());
  }
  /** Redraw all pinned rulers into a fresh group (called after each render). */
  _renderPins() {
    const w2 = this.w;
    const old = w2.dom.baseEl && w2.dom.baseEl.querySelector(".apexcharts-measure-pins");
    if (old && old.parentNode) old.parentNode.removeChild(old);
    if (!this.pins.length || !w2.dom.elGraphical) return;
    const g2 = this.graphics.group({ class: "apexcharts-measure-pins" });
    g2.node.style.pointerEvents = "none";
    w2.dom.elGraphical.add(g2);
    this.pins.forEach((p2) => {
      const a2 = __spreadProps(__spreadValues({}, this._dataToGrid(p2.xa, p2.ya)), { x: p2.xa, y: p2.ya });
      const b2 = __spreadProps(__spreadValues({}, this._dataToGrid(p2.xb, p2.yb)), { x: p2.xb, y: p2.yb });
      this._drawRuler(g2, a2, b2, true, p2.mode || "span");
    });
  }
  /**
   * Draw one ruler into `g`, dispatching on style.
   * @param {any} g
   * @param {{gx:number,gy:number,x:number,y:number}} a
   * @param {{gx:number,gy:number,x:number,y:number}} b
   * @param {boolean} [pinned]
   * @param {string} [mode] 'span' (finance-style band, default) | 'free'
   */
  _drawRuler(g2, a2, b2, pinned, mode) {
    const st = this._stats(a2, b2);
    const rg = this.graphics.group({
      class: "apexcharts-measure-ruler " + this._dirClass(st.dy) + (pinned ? " apexcharts-measure-pinned" : "")
    });
    if ((mode || this._mode()) === "free") this._drawFree(rg, a2, b2, st);
    else this._drawSpan(rg, a2, b2, st);
    g2.add(rg);
  }
  /** Endpoint dots on the series line (skipped when markers:false).
   * @param {any} g @param {{gx:number,gy:number}} p @param {string} color */
  _dot(g2, p2, color) {
    if (this._cfg().markers === false) return;
    const dot = this.graphics.drawMarker(p2.gx, p2.gy, {
      pSize: 4,
      shape: "circle",
      pointFillColor: color,
      pointFillOpacity: 1,
      pointStrokeColor: "#fff",
      pointStrokeWidth: 2,
      pointStrokeOpacity: 1
    });
    g2.add(dot);
  }
  /** Draw the readout label box + text into `g` at (bx,by).
   * @param {any} g @param {string[]} lines @param {number} bx @param {number} by
   * @param {number} boxW @param {number} boxH @param {string} color */
  _readout(g2, lines, bx, by, boxW, boxH, color) {
    const box = this.graphics.drawRect(bx, by, boxW, boxH, 4);
    box.node.setAttribute("class", "apexcharts-measure-label-bg");
    box.attr({ fill: "#ffffff", "fill-opacity": 0.95, stroke: color, "stroke-width": 1 });
    g2.add(box);
    const label = this.graphics.drawText({
      x: bx + 9,
      y: by + 15,
      text: lines,
      textAnchor: "start",
      fontSize: "11px",
      foreColor: "#1e293b",
      cssClass: "apexcharts-measure-label"
    });
    g2.add(label);
  }
  /**
   * Finance-style ruler: vertical guides + shaded band + endpoints on the
   * series line + a top readout. Guides/band/markers are individually
   * toggleable via config.
   * @param {any} g
   * @param {{gx:number,gy:number,x:number,y:number}} a
   * @param {{gx:number,gy:number,x:number,y:number}} b
   * @param {{dx:number,dy:number,pct:number,slope:number}} st
   */
  _drawSpan(g2, a2, b2, st) {
    const w2 = this.w;
    const cfg = this._cfg();
    const gh = w2.layout.gridHeight;
    const color = this._dirColor(st.dy);
    const lx = Math.min(a2.gx, b2.gx);
    const rx = Math.max(a2.gx, b2.gx);
    if (cfg.band !== false) {
      const band = this.graphics.drawRect(lx, 0, Math.max(0, rx - lx), gh, 0);
      band.node.setAttribute("class", "apexcharts-measure-band");
      band.attr({ fill: color, "fill-opacity": 0.09, stroke: "none" });
      g2.add(band);
    }
    if (cfg.guides !== false) {
      [a2, b2].forEach((p2) => {
        const vline = this.graphics.drawLine(p2.gx, 0, p2.gx, gh, this._colors().guide, 4, 1);
        vline.node.setAttribute("class", "apexcharts-measure-vline");
        g2.add(vline);
      });
    }
    [a2, b2].forEach((p2) => this._dot(g2, p2, color));
    const lines = this._label(a2, b2, st, "span");
    const longest = lines.reduce((m2, s2) => Math.max(m2, s2.length), 0);
    const boxW = Math.max(148, longest * 6.4);
    const boxH = 20 + lines.length * 15;
    let bx = (lx + rx) / 2 - boxW / 2;
    bx = Math.max(2, Math.min(bx, w2.layout.gridWidth - boxW - 2));
    this._readout(g2, lines, bx, 4, boxW, boxH, color);
  }
  /**
   * Free 2D ruler: a diagonal line between two arbitrary points + a readout.
   * @param {any} g
   * @param {{gx:number,gy:number,x:number,y:number}} a
   * @param {{gx:number,gy:number,x:number,y:number}} b
   * @param {{dx:number,dy:number,pct:number,slope:number}} st
   */
  _drawFree(g2, a2, b2, st) {
    const color = this._dirColor(st.dy);
    const line = this.graphics.drawLine(a2.gx, a2.gy, b2.gx, b2.gy, color, 0, 2);
    line.node.setAttribute("class", "apexcharts-measure-line");
    g2.add(line);
    [a2, b2].forEach((p2) => this._dot(g2, p2, color));
    const lines = this._label(a2, b2, st, "free");
    const longest = lines.reduce((m2, s2) => Math.max(m2, s2.length), 0);
    const boxW = Math.max(72, longest * 6.2);
    const boxH = 16 + lines.length * 15;
    let bx = (a2.gx + b2.gx) / 2 + 8;
    let by = (a2.gy + b2.gy) / 2 - boxH / 2;
    bx = Math.max(2, Math.min(bx, this.w.layout.gridWidth - boxW - 2));
    by = Math.max(2, Math.min(by, this.w.layout.gridHeight - boxH - 2));
    this._readout(g2, lines, bx, by, boxW, boxH, color);
  }
  /**
   * @param {{x:number,y:number}} a @param {{x:number,y:number}} b
   */
  _fireMeasured(a2, b2) {
    const st = this._stats(a2, b2);
    const payload = {
      from: { x: a2.x, y: a2.y },
      to: { x: b2.x, y: b2.y },
      dx: st.dx,
      dy: st.dy,
      percentChange: st.pct,
      slope: st.slope
    };
    const fn = this.w.config.chart.events.measured;
    if (typeof fn === "function") fn(this.ctx, payload);
    this.ctx.events.fireEvent("measured", [this.ctx, payload]);
  }
  /** Re-project the measure pins after each render. */
  _afterRender() {
    if (!this._enabled()) return;
    if (this.w.interact.measureEnabled && !this.persistent) {
      this.persistent = true;
    }
    if (this.pins.length) this._renderPins();
    if (this.persistent && !this.drag) {
      this._disarm();
      this._arm();
    }
  }
  teardown() {
    this._cancelDrag();
    this._endSeed(false);
    this._disarm();
    const doc = this._doc();
    if (doc && this._keysBound) {
      doc.removeEventListener("keydown", this._onKeyDown);
      doc.removeEventListener("keyup", this._onKeyUp);
    }
    this._keysBound = false;
    this.pins = [];
  }
}
ApexCharts.registerFeatures({ measure: Measure });
class ContextMenu {
  /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {import('../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.w = w2;
    this.ctx = ctx;
    this.menu = null;
    this._items = [];
    this._focusIndex = -1;
    this._trigger = null;
    this._onContext = this._onContext.bind(this);
    this._onDocDown = this._onDocDown.bind(this);
    this._onKey = this._onKey.bind(this);
    this._afterRender = this._afterRender.bind(this);
    ctx.addEventListener("mounted", this._afterRender);
    ctx.addEventListener("updated", this._afterRender);
  }
  _cfg() {
    return this.w.config.chart.contextMenu || {};
  }
  _enabled() {
    return this._cfg().enabled === true;
  }
  _doc() {
    return this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
  }
  /** (Re)attach the contextmenu trigger to the freshly (re)built SVG. */
  _afterRender() {
    this._detachTrigger();
    this.close();
    if (!this._enabled() || !Environment.isBrowser()) return;
    const svg = this.w.dom.Paper && this.w.dom.Paper.node;
    if (!svg) return;
    svg.addEventListener("contextmenu", this._onContext);
    this._trigger = svg;
  }
  _detachTrigger() {
    if (this._trigger) {
      this._trigger.removeEventListener("contextmenu", this._onContext);
      this._trigger = null;
    }
  }
  /** @param {any} e */
  _onContext(e2) {
    if (!this._enabled()) return;
    e2.preventDefault();
    this.open(e2.clientX, e2.clientY);
  }
  /**
   * Client pixel -> data {x,y} via the grid client-rect fraction (scale
   * independent). Null when the grid is not measurable.
   * @param {number} cx @param {number} cy
   * @returns {{x:number,y:number}|null}
   */
  _clientToData(cx, cy) {
    const w2 = this.w;
    const grid = w2.dom.baseEl && w2.dom.baseEl.querySelector(".apexcharts-grid");
    if (!grid) return null;
    const r2 = grid.getBoundingClientRect();
    if (!r2.width || !r2.height) return null;
    const clamp = (v2) => v2 < 0 ? 0 : v2 > 1 ? 1 : v2;
    const fx = clamp((cx - r2.left) / r2.width);
    const fy = clamp((cy - r2.top) / r2.height);
    const x = w2.globals.minX + fx * (w2.globals.maxX - w2.globals.minX);
    const s2 = w2.globals.yAxisScale && w2.globals.yAxisScale[0];
    const ymin = s2 && isFinite(s2.niceMin) ? s2.niceMin : w2.globals.minY;
    const ymax = s2 && isFinite(s2.niceMax) ? s2.niceMax : w2.globals.maxY;
    const y = ymax - fy * (ymax - ymin);
    return { x, y };
  }
  /**
   * Resolve the configured items into runnable entries, dropping built-ins
   * whose dependency is absent (e.g. measure not enabled).
   * @param {any} context
   * @returns {Array<{id:string,label:string,run:Function}>}
   */
  _resolveItems(context) {
    const cfg = this._cfg();
    const raw = Array.isArray(cfg.items) && cfg.items.length ? cfg.items : ["annotate", "xline", "yline", "measure"];
    const labels = cfg.labels || {};
    const out = [];
    raw.forEach((it) => {
      if (typeof it === "string") {
        if (it === "annotate") {
          out.push({
            id: "annotate",
            label: labels.annotate || "Add note here",
            run: () => this._annotate(context)
          });
        } else if (it === "xline") {
          out.push({
            id: "xline",
            label: labels.xline || "Annotate here",
            run: () => this._line(context, "x")
          });
        } else if (it === "yline") {
          out.push({
            id: "yline",
            label: labels.yline || "Mark this level",
            run: () => this._line(context, "y")
          });
        } else if (it === "measure") {
          const m2 = this.ctx.measure;
          const on = m2 && this.w.config.chart.measure && this.w.config.chart.measure.enabled;
          if (on) {
            out.push({
              id: "measure",
              label: labels.measure || "Measure from here",
              run: () => m2.seedFromClient(context.clientX, context.clientY)
            });
          }
        }
      } else if (it && typeof it === "object" && typeof it.onClick === "function") {
        out.push({
          id: it.id || "custom",
          label: it.label || "Action",
          run: () => it.onClick(this.ctx, context)
        });
      }
    });
    return out;
  }
  /** @param {any} context */
  _annotate(context) {
    if (context.x == null || context.y == null) return;
    const cfg = this._cfg();
    const ink = this.ctx.ink;
    if (ink && typeof ink.createAt === "function") {
      ink.createAt(context.x, context.y, { text: cfg.noteText || "Note" });
      return;
    }
    this.ctx.addPointAnnotation(
      {
        x: context.x,
        y: context.y,
        marker: { size: 5 },
        label: {
          text: cfg.noteText || "Note",
          style: { background: "#fff", color: "#334155" }
        }
      },
      true
    );
  }
  /**
   * The 'xline' / 'yline' items: drop a dashed line annotation at the clicked
   * data point ('x' = vertical line at the clicked x, 'y' = horizontal line
   * at the clicked y). Lines only: no x2/y2 is ever set, so this never
   * creates a range rectangle. Both items share chart.contextMenu.line
   * ({ text, strokeDashArray, color }) for styling.
   * @param {any} context @param {'x'|'y'} axis
   */
  _line(context, axis) {
    const lc = this._cfg().line || {};
    const val = axis === "x" ? context.x : context.y;
    if (val == null) return;
    const ink = this.ctx.ink;
    if (ink && typeof ink.createLineAt === "function") {
      ink.createLineAt(axis, val, {
        text: lc.text,
        strokeDashArray: lc.strokeDashArray,
        color: lc.color
      });
      return;
    }
    const anno = {
      strokeDashArray: lc.strokeDashArray != null ? lc.strokeDashArray : 4
    };
    if (lc.text) anno.label = { text: lc.text };
    if (lc.color) {
      anno.borderColor = lc.color;
      anno.label = Utils$1.extend(anno.label || {}, { borderColor: lc.color });
    }
    if (axis === "x") {
      this.ctx.addXaxisAnnotation(Utils$1.extend(anno, { x: val }), true);
    } else {
      this.ctx.addYaxisAnnotation(Utils$1.extend(anno, { y: val }), true);
    }
  }
  /**
   * Open the menu at a client-space point.
   * @param {number} clientX @param {number} clientY
   */
  open(clientX, clientY) {
    this.close();
    const w2 = this.w;
    const elWrap = w2.dom.elWrap;
    const doc = this._doc();
    if (!elWrap || !doc) return;
    const data = this._clientToData(clientX, clientY);
    const g2 = w2.globals;
    const context = {
      x: data ? data.x : null,
      y: data ? data.y : null,
      seriesIndex: g2.capturedSeriesIndex >= 0 ? g2.capturedSeriesIndex : null,
      dataPointIndex: g2.capturedDataPointIndex >= 0 ? g2.capturedDataPointIndex : null,
      clientX,
      clientY
    };
    const items = this._resolveItems(context);
    if (!items.length) return;
    this._items = items;
    const menu = doc.createElement("div");
    menu.className = "apexcharts-context-menu";
    menu.setAttribute("role", "menu");
    menu.style.position = "absolute";
    menu.style.visibility = "hidden";
    items.forEach((it, i2) => {
      const btn = doc.createElement("button");
      btn.type = "button";
      btn.className = "apexcharts-context-menu-item";
      btn.setAttribute("role", "menuitem");
      btn.tabIndex = -1;
      btn.textContent = it.label;
      btn.addEventListener("click", (ev) => {
        ev.stopPropagation();
        this._activate(i2);
      });
      btn.addEventListener("mouseenter", () => this._focus(i2));
      menu.appendChild(btn);
    });
    elWrap.appendChild(menu);
    this.menu = menu;
    const wrapRect = elWrap.getBoundingClientRect();
    let left = clientX - wrapRect.left;
    let top = clientY - wrapRect.top;
    const mw = menu.offsetWidth;
    const mh = menu.offsetHeight;
    const maxLeft = Math.max(0, elWrap.clientWidth - mw);
    const maxTop = Math.max(0, elWrap.clientHeight - mh);
    if (left > maxLeft) left = maxLeft;
    if (top > maxTop) top = maxTop;
    menu.style.left = Math.max(0, left) + "px";
    menu.style.top = Math.max(0, top) + "px";
    menu.style.visibility = "visible";
    doc.addEventListener("mousedown", this._onDocDown, true);
    doc.addEventListener("keydown", this._onKey, true);
    this._focus(0);
  }
  /** @param {number} i */
  _focus(i2) {
    if (!this.menu) return;
    const btns = this.menu.querySelectorAll(".apexcharts-context-menu-item");
    if (this._focusIndex >= 0 && btns[this._focusIndex]) {
      btns[this._focusIndex].classList.remove("apexcharts-context-menu-item--active");
    }
    this._focusIndex = i2;
    if (btns[i2]) {
      btns[i2].classList.add("apexcharts-context-menu-item--active");
      if (typeof btns[i2].focus === "function") btns[i2].focus();
    }
  }
  /** @param {number} i */
  _activate(i2) {
    const it = this._items[i2];
    this.close();
    if (it) it.run();
  }
  /** @param {any} e */
  _onDocDown(e2) {
    if (this.menu && this.menu.contains(e2.target)) return;
    this.close();
  }
  /** @param {any} e */
  _onKey(e2) {
    if (!this.menu || !this._items.length) return;
    if (e2.key === "Escape") {
      e2.preventDefault();
      this.close();
    } else if (e2.key === "ArrowDown") {
      e2.preventDefault();
      this._focus((this._focusIndex + 1) % this._items.length);
    } else if (e2.key === "ArrowUp") {
      e2.preventDefault();
      this._focus((this._focusIndex - 1 + this._items.length) % this._items.length);
    } else if (e2.key === "Enter" || e2.key === " ") {
      e2.preventDefault();
      this._activate(this._focusIndex < 0 ? 0 : this._focusIndex);
    }
  }
  close() {
    const doc = this._doc();
    if (doc) {
      doc.removeEventListener("mousedown", this._onDocDown, true);
      doc.removeEventListener("keydown", this._onKey, true);
    }
    if (this.menu && this.menu.parentNode) {
      this.menu.parentNode.removeChild(this.menu);
    }
    this.menu = null;
    this._items = [];
    this._focusIndex = -1;
  }
  teardown() {
    this.close();
    this._detachTrigger();
  }
}
ApexCharts.registerFeatures({ contextMenu: ContextMenu });
const MAX_BINS = 1e3;
function quantileSorted(sorted, q) {
  const n2 = sorted.length;
  if (n2 === 0) return NaN;
  if (n2 === 1) return sorted[0];
  const pos = (n2 - 1) * q;
  const lo = Math.floor(pos);
  const hi = Math.ceil(pos);
  if (lo === hi) return sorted[lo];
  return sorted[lo] + (sorted[hi] - sorted[lo]) * (pos - lo);
}
function stdDev(values) {
  const n2 = values.length;
  if (n2 < 2) return 0;
  let sum = 0;
  for (let i2 = 0; i2 < n2; i2++) sum += values[i2];
  const mean = sum / n2;
  let acc = 0;
  for (let i2 = 0; i2 < n2; i2++) {
    const d = values[i2] - mean;
    acc += d * d;
  }
  return Math.sqrt(acc / n2);
}
function widthForRule(sorted, span, rule) {
  const n2 = sorted.length;
  const byCount = (count) => span / Math.max(1, Math.ceil(count));
  switch (rule) {
    case "sqrt":
      return { width: byCount(Math.sqrt(n2)), rule: "sqrt" };
    case "rice":
      return { width: byCount(2 * Math.cbrt(n2)), rule: "rice" };
    case "scott": {
      const sd = stdDev(sorted);
      if (sd > 0) return { width: 3.49 * sd * Math.pow(n2, -1 / 3), rule: "scott" };
      return { width: byCount(Math.log2(n2) + 1), rule: "sturges" };
    }
    case "fd": {
      const iqr = quantileSorted(sorted, 0.75) - quantileSorted(sorted, 0.25);
      if (iqr > 0) return { width: 2 * iqr * Math.pow(n2, -1 / 3), rule: "fd" };
      return { width: byCount(Math.log2(n2) + 1), rule: "sturges" };
    }
    case "auto": {
      const sturges = byCount(Math.log2(n2) + 1);
      const iqr = quantileSorted(sorted, 0.75) - quantileSorted(sorted, 0.25);
      if (iqr <= 0) return { width: sturges, rule: "sturges" };
      const fd = 2 * iqr * Math.pow(n2, -1 / 3);
      return fd < sturges ? { width: fd, rule: "fd" } : { width: sturges, rule: "sturges" };
    }
    case "sturges":
    default:
      return { width: byCount(Math.log2(n2) + 1), rule: "sturges" };
  }
}
function computeBinning(values, opts = {}) {
  if (!Array.isArray(values) || values.length === 0) return null;
  const sorted = values.slice().sort((a2, b2) => a2 - b2);
  let lo = sorted[0];
  let hi = sorted[sorted.length - 1];
  const range = opts.range;
  if (Array.isArray(range) && range.length === 2) {
    const rLo = Number(range[0]);
    const rHi = Number(range[1]);
    if (isFinite(rLo) && isFinite(rHi) && rHi > rLo) {
      lo = rLo;
      hi = rHi;
    }
  }
  if (!(hi > lo)) {
    const pad = Math.abs(lo) > 0 ? Math.abs(lo) * 0.05 : 0.5;
    return {
      edges: [lo - pad, lo + pad],
      binWidth: pad * 2,
      rule: "single",
      capped: false
    };
  }
  const span = hi - lo;
  let width;
  let rule;
  if (typeof opts.binWidth === "number" && opts.binWidth > 0) {
    width = opts.binWidth;
    rule = "binWidth";
  } else if (typeof opts.bins === "number" && opts.bins >= 1) {
    width = span / Math.floor(opts.bins);
    rule = "count";
  } else {
    const chosen = widthForRule(
      sorted,
      span,
      typeof opts.bins === "string" ? opts.bins : "auto"
    );
    width = chosen.width;
    rule = chosen.rule;
  }
  if (!isFinite(width) || width <= 0) width = span;
  let count = Math.ceil(span / width);
  if (!isFinite(count) || count < 1) count = 1;
  let capped = false;
  if (count > MAX_BINS) {
    count = MAX_BINS;
    width = span / count;
    capped = true;
  }
  width = span / count;
  const edges = new Array(count + 1);
  for (let k = 0; k <= count; k++) edges[k] = lo + k * width;
  edges[count] = Math.max(edges[count], hi);
  return { edges, binWidth: width, rule, capped };
}
function binIndexOf(v2, edges) {
  const last = edges.length - 1;
  if (!(v2 >= edges[0]) || v2 > edges[last]) return -1;
  if (v2 === edges[last]) return last - 1;
  const width = (edges[last] - edges[0]) / last;
  if (width > 0) {
    let k = Math.floor((v2 - edges[0]) / width);
    if (k < 0) k = 0;
    if (k > last - 1) k = last - 1;
    if (v2 < edges[k]) k--;
    else if (v2 >= edges[k + 1]) k++;
    if (k < 0 || k > last - 1) return -1;
    return k;
  }
  let lo = 0;
  let hi = last - 1;
  while (lo <= hi) {
    const mid = lo + hi >> 1;
    if (v2 < edges[mid]) hi = mid - 1;
    else if (v2 >= edges[mid + 1]) lo = mid + 1;
    else return mid;
  }
  return -1;
}
function binCounts(values, edges) {
  const counts = new Array(Math.max(0, edges.length - 1)).fill(0);
  for (let i2 = 0; i2 < values.length; i2++) {
    const k = binIndexOf(values[i2], edges);
    if (k >= 0) counts[k]++;
  }
  return counts;
}
function rowsByBin(values, edges) {
  const n2 = Math.max(0, edges.length - 1);
  const buckets = new Array(n2);
  for (let k = 0; k < n2; k++) buckets[k] = [];
  for (let i2 = 0; i2 < values.length; i2++) {
    const k = binIndexOf(values[i2], edges);
    if (k >= 0) buckets[k].push(values[i2]);
  }
  return buckets;
}
function fiveNumberSummary(values, opts = {}) {
  if (!Array.isArray(values) || values.length === 0) return null;
  const sorted = values.slice().sort((a2, b2) => a2 - b2);
  const q1 = quantileSorted(sorted, 0.25);
  const median = quantileSorted(sorted, 0.5);
  const q3 = quantileSorted(sorted, 0.75);
  const iqr = q3 - q1;
  let lo = sorted[0];
  let hi = sorted[sorted.length - 1];
  let outliers = [];
  if (opts.whiskers === "tukey" && iqr > 0) {
    const loFence = q1 - 1.5 * iqr;
    const hiFence = q3 + 1.5 * iqr;
    let i2 = 0;
    while (i2 < sorted.length && sorted[i2] < loFence) i2++;
    let j = sorted.length - 1;
    while (j >= 0 && sorted[j] > hiFence) j--;
    if (i2 <= j) {
      lo = sorted[i2];
      hi = sorted[j];
      outliers = sorted.slice(0, i2).concat(sorted.slice(j + 1));
    }
  }
  return { summary: [lo, q1, median, q3, hi], outliers, iqr };
}
function kernelDensity(values, opts = {}) {
  if (!Array.isArray(values) || values.length === 0) return null;
  const sorted = values.slice().sort((a2, b2) => a2 - b2);
  const n2 = sorted.length;
  let h2 = opts.bandwidth;
  if (!(typeof h2 === "number" && h2 > 0)) {
    const sd = stdDev(sorted);
    const iqr = quantileSorted(sorted, 0.75) - quantileSorted(sorted, 0.25);
    const spread = iqr > 0 ? Math.min(sd, iqr / 1.349) : sd;
    h2 = 0.9 * spread * Math.pow(n2, -1 / 5);
  }
  if (!isFinite(h2) || h2 <= 0) {
    const v2 = sorted[0];
    const eps = Math.abs(v2) > 0 ? Math.abs(v2) * 1e-3 : 1e-3;
    return {
      density: [
        [v2 - eps, 0],
        [v2, 1],
        [v2 + eps, 0]
      ],
      bandwidth: eps
    };
  }
  const steps = Math.max(8, Math.floor(opts.resolution || 64));
  const lo = sorted[0] - 2 * h2;
  const hi = sorted[n2 - 1] + 2 * h2;
  const step = (hi - lo) / (steps - 1);
  const norm = 1 / (n2 * h2 * Math.sqrt(2 * Math.PI));
  const density = [];
  for (let g2 = 0; g2 < steps; g2++) {
    const x = lo + g2 * step;
    let sum = 0;
    for (let i2 = 0; i2 < n2; i2++) {
      const z = (x - sorted[i2]) / h2;
      sum += Math.exp(-0.5 * z * z);
    }
    density.push([x, sum * norm]);
  }
  return { density, bandwidth: h2 };
}
function normalizeCounts(counts, opts = {}) {
  let out = counts.slice();
  if (opts.cumulative) {
    let acc = 0;
    out = out.map((c2) => acc += c2);
  }
  const total = counts.reduce((a2, b2) => a2 + b2, 0);
  if (total <= 0) return out;
  if (opts.normalize === "relative") {
    return out.map((c2) => c2 / total * 100);
  }
  if (opts.normalize === "density") {
    const w2 = opts.binWidth;
    if (typeof w2 === "number" && w2 > 0) return out.map((c2) => c2 / (total * w2));
  }
  return out;
}
function histogramValues(data) {
  const out = [];
  if (!Array.isArray(data)) return out;
  for (let i2 = 0; i2 < data.length; i2++) {
    const d = data[i2];
    let raw = d;
    if (Array.isArray(d)) raw = d.length === 1 ? d[0] : d[1];
    else if (d && typeof d === "object") raw = d.y !== void 0 ? d.y : d.x;
    const v2 = Utils$1.parseNumber(raw);
    if (v2 !== null && isFinite(v2)) out.push(v2);
  }
  return out;
}
function histogramTransform(ser, w2) {
  var _a;
  const cnf = w2.config;
  const gl = w2.globals;
  if (!Array.isArray(ser)) return ser;
  if (!gl.histogramRawSeries) {
    gl.histogramRawSeries = ser.map((s2) => __spreadProps(__spreadValues({}, s2), {
      data: Array.isArray(s2 == null ? void 0 : s2.data) ? s2.data.slice() : s2 == null ? void 0 : s2.data
    }));
  }
  const raw = gl.histogramRawSeries;
  const hcfg = ((_a = cnf.plotOptions) == null ? void 0 : _a.histogram) || {};
  const perSeries = raw.map((s2) => histogramValues(s2 == null ? void 0 : s2.data));
  let all = [];
  if (perSeries.length === 1) {
    all = perSeries[0];
  } else {
    for (const vals of perSeries) all = all.concat(vals);
  }
  const binning = computeBinning(all, {
    bins: hcfg.bins,
    binWidth: hcfg.binWidth,
    range: hcfg.range
  });
  if (!binning) {
    w2.histogramData = {
      edges: [],
      binWidth: 0,
      counts: [],
      rule: "",
      capped: false
    };
    return raw;
  }
  const { edges, binWidth } = binning;
  const counts = perSeries.map(
    (vals) => binCounts(vals, edges)
  );
  w2.histogramData = {
    edges,
    binWidth,
    counts,
    rule: binning.rule,
    capped: binning.capped
  };
  const collapsed = gl.collapsedSeriesIndices || [];
  return raw.map((s2, i2) => {
    if (collapsed.indexOf(i2) !== -1) return __spreadProps(__spreadValues({}, s2), { data: [] });
    const ys = normalizeCounts(counts[i2], {
      normalize: hcfg.normalize,
      cumulative: hcfg.cumulative,
      binWidth
    });
    const data = [];
    for (let k = 0; k < ys.length; k++) {
      data.push({ x: (edges[k] + edges[k + 1]) / 2, y: ys[k] });
    }
    return __spreadProps(__spreadValues({}, s2), { data });
  });
}
const derivedData = /* @__PURE__ */ new WeakSet();
function observationsOf(d, allowFlatY) {
  var _a;
  if (!d || typeof d !== "object" || Array.isArray(d)) return null;
  let raw = null;
  if (Array.isArray(d.points)) raw = d.points;
  else if (Array.isArray((_a = d.y) == null ? void 0 : _a.points)) raw = d.y.points;
  else if (allowFlatY && Array.isArray(d.y) && typeof d.y[0] === "number") {
    raw = d.y;
  }
  if (!raw) return null;
  const out = [];
  for (let i2 = 0; i2 < raw.length; i2++) {
    const v2 = Utils$1.parseNumber(raw[i2]);
    if (v2 !== null && isFinite(v2)) out.push(v2);
  }
  return out.length ? out : null;
}
function boxPlotTransform(ser, w2) {
  var _a, _b;
  if (!Array.isArray(ser)) return ser;
  const whiskers = ((_b = (_a = w2.config.plotOptions) == null ? void 0 : _a.boxPlot) == null ? void 0 : _b.whiskers) || "minmax";
  return ser.map((s2) => {
    if (!Array.isArray(s2 == null ? void 0 : s2.data)) return s2;
    let touched = false;
    const data = s2.data.map((d) => {
      if (Array.isArray(d == null ? void 0 : d.y) && d.y.length === 5 && !derivedData.has(d)) {
        return d;
      }
      const values = observationsOf(d, false);
      if (!values) return d;
      const summary = fiveNumberSummary(values, { whiskers });
      if (!summary) return d;
      touched = true;
      const next = __spreadProps(__spreadValues({}, d), { y: summary.summary, points: values });
      derivedData.add(next);
      return next;
    });
    return touched ? __spreadProps(__spreadValues({}, s2), { data }) : s2;
  });
}
function violinTransform(ser, w2) {
  var _a, _b;
  if (!Array.isArray(ser)) return ser;
  const kde = ((_b = (_a = w2.config.plotOptions) == null ? void 0 : _a.violin) == null ? void 0 : _b.kde) || {};
  return ser.map((s2) => {
    if (!Array.isArray(s2 == null ? void 0 : s2.data)) return s2;
    let touched = false;
    const data = s2.data.map((d) => {
      var _a2;
      if (Array.isArray((_a2 = d == null ? void 0 : d.y) == null ? void 0 : _a2.density) && d.y.density.length && !derivedData.has(d)) {
        return d;
      }
      const values = observationsOf(d, true);
      if (!values) return d;
      const est = kernelDensity(values, {
        bandwidth: kde.bandwidth,
        resolution: kde.resolution
      });
      if (!est) return d;
      touched = true;
      const next = __spreadProps(__spreadValues({}, d), { y: { density: est.density, points: values } });
      derivedData.add(next);
      return next;
    });
    return touched ? __spreadProps(__spreadValues({}, s2), { data }) : s2;
  });
}
const DEFAULT_MAX_ROWS = 3e3;
function thinClusters(clusters, maxRows) {
  let total = 0;
  let widest = 0;
  for (const c2 of clusters) {
    total += c2.length;
    if (c2.length > widest) widest = c2.length;
  }
  if (total <= maxRows) return { clusters, stride: 1, total, kept: total };
  const keptAt = (s2) => {
    let n2 = 0;
    for (const c2 of clusters) n2 += Math.ceil(c2.length / s2);
    return n2;
  };
  let stride = Math.max(2, Math.ceil(total / maxRows));
  while (stride < widest && keptAt(stride) > maxRows) stride++;
  let kept = 0;
  const out = clusters.map((rows) => {
    const keepList = [];
    for (let i2 = 0; i2 < rows.length; i2 += stride) keepList.push(rows[i2]);
    kept += keepList.length;
    return keepList;
  });
  return { clusters: out, stride, total, kept };
}
function toUnitSeries(w2, clusters, opts) {
  const maxRows = opts && opts.maxRows != null ? opts.maxRows : DEFAULT_MAX_ROWS;
  const thinned = thinClusters(
    clusters.map((c2) => c2.rows),
    maxRows
  );
  if (thinned.stride > 1) {
    console.warn(
      `ApexCharts: rowSeries() thinned ${thinned.total} rows to ${thinned.kept} (every ${thinned.stride}${thinned.stride === 2 ? "nd" : thinned.stride === 3 ? "rd" : "th"} row) to stay under maxRows=${maxRows}. Raise maxRows to draw more.`
    );
  }
  const colors = w2.globals && w2.globals.colors || [];
  return clusters.map((c2, i2) => {
    const fillColor = colors[c2.realIndex] || colors[0];
    return {
      name: c2.name,
      data: thinned.clusters[i2].map((v2, q) => __spreadValues({
        id: `${c2.realIndex}:${i2}:${q}`,
        x: c2.name,
        y: v2
      }, fillColor ? { fillColor } : {}))
    };
  });
}
function histogramRows(w2, opts) {
  const gl = w2.globals;
  const hd = w2.histogramData;
  const raw = gl && gl.histogramRawSeries;
  if (!hd || !Array.isArray(hd.edges) || hd.edges.length < 2) return null;
  if (!Array.isArray(raw) || !raw.length) return null;
  const collapsed = gl && gl.collapsedSeriesIndices || [];
  const edges = hd.edges;
  const clusters = [];
  raw.forEach((s2, i2) => {
    var _a;
    if (collapsed.indexOf(i2) !== -1) return;
    const buckets = rowsByBin(histogramValues(s2 && s2.data), edges);
    const seriesName = w2.seriesData && ((_a = w2.seriesData.seriesNames) == null ? void 0 : _a[i2]) || (s2 == null ? void 0 : s2.name);
    buckets.forEach((rows, k) => {
      const range = `${formatEdge(edges[k])}-${formatEdge(edges[k + 1])}`;
      clusters.push({
        // Only qualify by series when there is more than one to tell apart.
        name: raw.length > 1 && seriesName ? `${seriesName} ${range}` : range,
        realIndex: i2,
        rows
      });
    });
  });
  return clusters.length ? toUnitSeries(w2, clusters, opts) : null;
}
function formatEdge(v2) {
  if (!isFinite(v2)) return String(v2);
  const r2 = Math.round(v2);
  return Math.abs(v2 - r2) < 1e-6 ? String(r2) : String(Number(v2.toFixed(2)));
}
function pointsRowSource(pick) {
  return (w2, opts) => {
    var _a;
    const perSeries = pick(w2);
    if (!Array.isArray(perSeries) || !perSeries.length) return null;
    const collapsed = w2.globals && w2.globals.collapsedSeriesIndices || [];
    const labels = w2.globals && (((_a = w2.globals.categoryLabels) == null ? void 0 : _a.length) ? w2.globals.categoryLabels : w2.globals.labels) || [];
    const clusters = [];
    perSeries.forEach((byCat, i2) => {
      var _a2;
      if (collapsed.indexOf(i2) !== -1) return;
      if (!Array.isArray(byCat)) return;
      const seriesName = w2.seriesData && ((_a2 = w2.seriesData.seriesNames) == null ? void 0 : _a2[i2]);
      byCat.forEach((pts, j) => {
        const label = labels[j] != null ? String(labels[j]) : `#${j + 1}`;
        clusters.push({
          name: perSeries.length > 1 && seriesName ? `${seriesName} ${label}` : label,
          realIndex: i2,
          rows: Array.isArray(pts) ? pts.slice() : []
        });
      });
    });
    return clusters.length ? toUnitSeries(w2, clusters, opts) : null;
  };
}
const boxPlotRows = pointsRowSource((w2) => {
  var _a;
  return (_a = w2.candleData) == null ? void 0 : _a.seriesBoxPoints;
});
const violinRows = pointsRowSource((w2) => {
  var _a;
  return (_a = w2.violinData) == null ? void 0 : _a.seriesViolinPoints;
});
registerSeriesTransform("histogram", histogramTransform);
registerSeriesTransform("boxPlot", boxPlotTransform);
registerSeriesTransform("violin", violinTransform);
registerRowSource("histogram", histogramRows);
registerRowSource("boxPlot", boxPlotRows);
registerRowSource("violin", violinRows);
class BarDataLabels {
  /**
   * @param {import('../../../charts/Bar').default} barCtx
   */
  constructor(barCtx) {
    this.w = barCtx.w;
    this.barCtx = barCtx;
    this.totalFormatter = this.w.config.plotOptions.bar.dataLabels.total.formatter;
    if (!this.totalFormatter) {
      this.totalFormatter = this.w.config.dataLabels.formatter;
    }
  }
  /** handleBarDataLabels is used to calculate the positions for the data-labels
   * It also sets the element's data attr for bars and calls drawCalculatedBarDataLabels()
   * After calculating, it also calls the function to draw data labels
   * @memberof Bar
   * @param {Record<string, any>} opts - bar properties used throughout the bar drawing function
   * @return {object} dataLabels node-element which you can append later
   **/
  handleBarDataLabels(opts) {
    const {
      x,
      y,
      y1,
      y2,
      i: i2,
      j,
      realIndex,
      columnGroupIndex,
      series,
      barHeight,
      barWidth,
      barXPosition,
      barYPosition,
      visibleSeries
    } = opts;
    const w2 = this.w;
    const graphics = new Graphics(this.barCtx.w);
    const strokeWidth = Array.isArray(this.barCtx.strokeWidth) ? this.barCtx.strokeWidth[realIndex] : this.barCtx.strokeWidth;
    let bcx;
    let bcy;
    if (w2.axisFlags.isXNumeric && !w2.globals.isBarHorizontal) {
      bcx = x + barWidth * (visibleSeries + 1);
      bcy = y + barHeight * (visibleSeries + 1) - strokeWidth;
    } else {
      bcx = x + barWidth * visibleSeries;
      bcy = y + barHeight * visibleSeries;
    }
    let dataLabels = null;
    let totalDataLabels = null;
    let dataLabelsX = x;
    let dataLabelsY = y;
    let dataLabelsPos = (
      /** @type {any} */
      {}
    );
    const dataLabelsConfig = w2.config.dataLabels;
    const barDataLabelsConfig = this.barCtx.barOptions.dataLabels;
    const barTotalDataLabelsConfig = this.barCtx.barOptions.dataLabels.total;
    if (typeof barYPosition !== "undefined" && (this.barCtx.isRangeBar || this.barCtx.isPyramid)) {
      bcy = barYPosition;
      dataLabelsY = barYPosition;
    }
    if (typeof barXPosition !== "undefined" && this.barCtx.isVerticalGroupedRangeBar) {
      bcx = barXPosition;
      dataLabelsX = barXPosition;
    }
    const offX = resolveDataLabelOffset(
      dataLabelsConfig.offsetX,
      w2,
      realIndex,
      j
    );
    const offY = resolveDataLabelOffset(
      dataLabelsConfig.offsetY,
      w2,
      realIndex,
      j
    );
    let textRects = {
      width: 0,
      height: 0
    };
    if (w2.config.dataLabels.enabled) {
      const yLabel = w2.seriesData.series[realIndex][j];
      textRects = graphics.getTextRects(
        w2.config.dataLabels.formatter ? w2.config.dataLabels.formatter(yLabel, __spreadProps(__spreadValues({}, w2), {
          seriesIndex: realIndex,
          dataPointIndex: j,
          w: w2
        })) : w2.formatters.yLabelFormatters[0](yLabel),
        parseFloat(dataLabelsConfig.style.fontSize).toString()
      );
    }
    const params = {
      x,
      y,
      i: i2,
      j,
      realIndex,
      columnGroupIndex,
      bcx,
      bcy,
      barHeight,
      barWidth,
      textRects,
      strokeWidth,
      dataLabelsX,
      dataLabelsY,
      dataLabelsConfig,
      barDataLabelsConfig,
      barTotalDataLabelsConfig,
      offX,
      offY
    };
    if (this.barCtx.isHorizontal) {
      dataLabelsPos = this.calculateBarsDataLabelsPosition(params);
    } else {
      dataLabelsPos = this.calculateColumnsDataLabelsPosition(params);
    }
    dataLabels = this.drawCalculatedDataLabels({
      x: dataLabelsPos.dataLabelsX,
      y: dataLabelsPos.dataLabelsY,
      val: this.barCtx.isRangeBar ? [y1, y2] : w2.config.chart.stackType === "100%" ? series[realIndex][j] : w2.seriesData.series[realIndex][j],
      i: realIndex,
      j,
      barWidth,
      barHeight,
      textRects,
      dataLabelsConfig
    });
    if (w2.config.chart.stacked && barTotalDataLabelsConfig.enabled) {
      totalDataLabels = this.drawTotalDataLabels({
        x: dataLabelsPos.totalDataLabelsX,
        y: dataLabelsPos.totalDataLabelsY,
        barWidth,
        barHeight,
        realIndex,
        textAnchor: dataLabelsPos.totalDataLabelsAnchor,
        val: this.getStackedTotalDataLabel({ realIndex, j }),
        dataLabelsConfig,
        barTotalDataLabelsConfig
      });
    }
    return {
      dataLabelsPos,
      dataLabels,
      totalDataLabels
    };
  }
  /**
   * True when this chart stacks in more than one group, so totals have to be
   * resolved per group rather than across the whole data point. A single-group
   * chart keeps every old code path exactly as it was. See #4173.
   */
  hasMultipleSeriesGroups() {
    return this.w.labelData.seriesGroups.length > 1;
  }
  /**
   * The series group `realIndex` belongs to, and that group's own stacking
   * state, or null when totals are chart-wide (the single-group case).
   * @param {number} realIndex
   * @returns {{groupIndex: number, group: string[]} | null}
   */
  getTotalGroupContext(realIndex) {
    if (!this.hasMultipleSeriesGroups()) return null;
    const groupIndex = this.barCtx.barHelpers.getSeriesGroupIndex(realIndex);
    if (groupIndex < 0) return null;
    return { groupIndex, group: this.w.labelData.seriesGroups[groupIndex] };
  }
  /**
   * Whether `realIndex` is the series that should draw the stacked total.
   *
   * That is the series capping the stack. With grouped stacks each group has
   * its own cap, so gating on the chart-wide `lastActiveBarSerieIndex` drew a
   * single total for the last group only. See #4173.
   * @param {number} realIndex
   */
  drawsStackedTotal(realIndex) {
    const byGroup = this.barCtx.lastActiveBarSerieIndexByGroup;
    const ctx = this.getTotalGroupContext(realIndex);
    if (ctx && byGroup && byGroup.length > ctx.groupIndex) {
      return byGroup[ctx.groupIndex] === realIndex;
    }
    return this.barCtx.lastActiveBarSerieIndex === realIndex;
  }
  /** @param {{realIndex: any, j: any}} opts */
  getStackedTotalDataLabel({ realIndex, j }) {
    const w2 = this.w;
    const ctx = this.getTotalGroupContext(realIndex);
    const byGroups = w2.seriesData.stackedSeriesTotalsByGroups;
    let val = ctx && byGroups && byGroups[ctx.groupIndex] ? byGroups[ctx.groupIndex][j] : this.barCtx.stackedSeriesTotals[j];
    if (this.totalFormatter) {
      val = this.totalFormatter(val, __spreadProps(__spreadValues({}, w2), {
        seriesIndex: realIndex,
        dataPointIndex: j,
        w: w2
      }));
    }
    return val;
  }
  /**
   * @param {Record<string, any>} opts
   */
  calculateColumnsDataLabelsPosition(opts) {
    const w2 = this.w;
    let {
      i: i2,
      j,
      realIndex,
      y,
      bcx,
      barWidth,
      barHeight,
      textRects,
      dataLabelsX,
      dataLabelsY,
      dataLabelsConfig,
      barDataLabelsConfig,
      barTotalDataLabelsConfig,
      strokeWidth,
      offX,
      offY
    } = opts;
    let totalDataLabelsY;
    let totalDataLabelsX;
    const totalDataLabelsAnchor = "middle";
    const totalDataLabelsBcx = bcx;
    barHeight = Math.abs(barHeight);
    const vertical = w2.config.plotOptions.bar.dataLabels.orientation === "vertical";
    const { zeroEncounters } = this.barCtx.barHelpers.getZeroValueEncounters({
      i: i2,
      j
    });
    bcx = bcx - strokeWidth / 2;
    const dataPointsDividedWidth = w2.layout.gridWidth / w2.globals.dataPoints;
    if (this.barCtx.isVerticalGroupedRangeBar) {
      dataLabelsX += barWidth / 2;
    } else {
      if (w2.axisFlags.isXNumeric) {
        dataLabelsX = bcx - barWidth / 2 + offX;
      } else {
        dataLabelsX = bcx - dataPointsDividedWidth + barWidth / 2 + offX;
      }
      if (!w2.config.chart.stacked && zeroEncounters > 0 && w2.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
        dataLabelsX -= barWidth * zeroEncounters;
      }
    }
    if (vertical) {
      const offsetDLX = 2;
      dataLabelsX = dataLabelsX + textRects.height / 2 - strokeWidth / 2 - offsetDLX;
    }
    const valIsNegative = w2.seriesData.series[i2][j] < 0;
    let newY = y;
    if (this.barCtx.isReversed) {
      newY = y + (valIsNegative ? barHeight : -barHeight);
    }
    switch (barDataLabelsConfig.position) {
      case "center":
        if (vertical) {
          if (valIsNegative) {
            dataLabelsY = newY - barHeight / 2 + offY;
          } else {
            dataLabelsY = newY + barHeight / 2 - offY;
          }
        } else {
          if (valIsNegative) {
            dataLabelsY = newY - barHeight / 2 + textRects.height / 2 + offY;
          } else {
            dataLabelsY = newY + barHeight / 2 + textRects.height / 2 - offY;
          }
        }
        break;
      case "bottom":
        if (vertical) {
          if (valIsNegative) {
            dataLabelsY = newY - barHeight + offY;
          } else {
            dataLabelsY = newY + barHeight - offY;
          }
        } else {
          if (valIsNegative) {
            dataLabelsY = newY - barHeight + textRects.height + strokeWidth + offY;
          } else {
            dataLabelsY = newY + barHeight - textRects.height / 2 + strokeWidth - offY;
          }
        }
        break;
      case "top":
        if (vertical) {
          if (valIsNegative) {
            dataLabelsY = newY + offY;
          } else {
            dataLabelsY = newY - offY;
          }
        } else {
          if (valIsNegative) {
            dataLabelsY = newY - textRects.height / 2 - offY;
          } else {
            dataLabelsY = newY + textRects.height + offY;
          }
        }
        break;
    }
    let lowestPrevY = newY;
    const totalGroupCtx = this.getTotalGroupContext(realIndex);
    const prevYGroups = totalGroupCtx ? [totalGroupCtx.group] : w2.labelData.seriesGroups;
    prevYGroups.forEach((sg) => {
      var _a;
      (_a = this.barCtx[sg.join(",")]) == null ? void 0 : _a.prevY.forEach(
        (arr) => {
          if (valIsNegative) {
            lowestPrevY = Math.max(arr[j], lowestPrevY);
          } else {
            lowestPrevY = Math.min(arr[j], lowestPrevY);
          }
        }
      );
    });
    if (this.drawsStackedTotal(realIndex) && barTotalDataLabelsConfig.enabled) {
      const ADDITIONAL_OFFY = 18;
      const graphics = new Graphics(this.barCtx.w);
      const totalLabeltextRects = graphics.getTextRects(
        this.getStackedTotalDataLabel({ realIndex, j }),
        dataLabelsConfig.fontSize
      );
      if (valIsNegative) {
        totalDataLabelsY = lowestPrevY - totalLabeltextRects.height / 2 - offY - barTotalDataLabelsConfig.offsetY + ADDITIONAL_OFFY;
      } else {
        totalDataLabelsY = lowestPrevY + totalLabeltextRects.height + offY + barTotalDataLabelsConfig.offsetY - ADDITIONAL_OFFY;
      }
      const xDivision = dataPointsDividedWidth;
      totalDataLabelsX = totalDataLabelsBcx + (w2.axisFlags.isXNumeric ? -barWidth / 2 : barWidth / 2 - xDivision) + barTotalDataLabelsConfig.offsetX;
    }
    if (!w2.config.chart.stacked) {
      if (dataLabelsY < 0) {
        dataLabelsY = 0 + strokeWidth;
      } else if (dataLabelsY + textRects.height / 3 > w2.layout.gridHeight) {
        dataLabelsY = w2.layout.gridHeight - strokeWidth;
      }
    }
    return {
      bcx,
      bcy: y,
      dataLabelsX,
      dataLabelsY,
      totalDataLabelsX,
      totalDataLabelsY,
      totalDataLabelsAnchor
    };
  }
  /**
   * @param {Record<string, any>} opts
   */
  calculateBarsDataLabelsPosition(opts) {
    var _a;
    const w2 = this.w;
    let {
      x,
      i: i2,
      j,
      realIndex,
      bcy,
      barHeight,
      barWidth,
      textRects,
      dataLabelsX,
      strokeWidth,
      dataLabelsConfig,
      barDataLabelsConfig,
      barTotalDataLabelsConfig,
      offX,
      offY
    } = opts;
    const dataPointsDividedHeight = w2.layout.gridHeight / w2.globals.dataPoints;
    const { zeroEncounters } = this.barCtx.barHelpers.getZeroValueEncounters({
      i: i2,
      j
    });
    barWidth = Math.abs(barWidth);
    let dataLabelsY;
    if (this.barCtx.isPyramid) {
      const centerOffset = (_a = textRects.centerOffset) != null ? _a : 0;
      dataLabelsY = bcy + barHeight / 2 + offY - centerOffset;
    } else {
      dataLabelsY = bcy - (this.barCtx.isRangeBar ? 0 : dataPointsDividedHeight) + barHeight / 2 + textRects.height / 2 + offY - 3;
    }
    if (!w2.config.chart.stacked && zeroEncounters > 0 && w2.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
      dataLabelsY -= barHeight * zeroEncounters;
    }
    let totalDataLabelsX;
    let totalDataLabelsY;
    let totalDataLabelsAnchor = "start";
    const valIsNegative = w2.seriesData.series[i2][j] < 0;
    let newX = x;
    if (this.barCtx.isReversed) {
      newX = x + (valIsNegative ? -barWidth : barWidth);
      totalDataLabelsAnchor = valIsNegative ? "start" : "end";
    }
    if (this.barCtx.isPyramid) {
      dataLabelsX = w2.layout.gridWidth / 2 + offX;
    } else {
      switch (barDataLabelsConfig.position) {
        case "center":
          if (valIsNegative) {
            dataLabelsX = newX + barWidth / 2 - offX;
          } else {
            dataLabelsX = Math.max(textRects.width / 2, newX - barWidth / 2) + offX;
          }
          break;
        case "bottom":
          if (valIsNegative) {
            dataLabelsX = newX + barWidth - strokeWidth - offX;
          } else {
            dataLabelsX = newX - barWidth + strokeWidth + offX;
          }
          break;
        case "top":
          if (valIsNegative) {
            dataLabelsX = newX - strokeWidth - offX;
          } else {
            dataLabelsX = newX - strokeWidth + offX;
          }
          break;
      }
    }
    let lowestPrevX = newX;
    const totalGroupCtx = this.getTotalGroupContext(realIndex);
    const prevXGroups = totalGroupCtx ? [totalGroupCtx.group] : w2.labelData.seriesGroups;
    prevXGroups.forEach((sg) => {
      var _a2;
      (_a2 = this.barCtx[sg.join(",")]) == null ? void 0 : _a2.prevX.forEach(
        (arr) => {
          if (valIsNegative) {
            lowestPrevX = Math.min(arr[j], lowestPrevX);
          } else {
            lowestPrevX = Math.max(arr[j], lowestPrevX);
          }
        }
      );
    });
    if (this.drawsStackedTotal(realIndex) && barTotalDataLabelsConfig.enabled) {
      const graphics = new Graphics(this.barCtx.w);
      const totalLabeltextRects = graphics.getTextRects(
        this.getStackedTotalDataLabel({ realIndex, j }),
        dataLabelsConfig.fontSize
      );
      if (valIsNegative) {
        totalDataLabelsX = lowestPrevX - strokeWidth - offX - barTotalDataLabelsConfig.offsetX;
        totalDataLabelsAnchor = "end";
      } else {
        totalDataLabelsX = lowestPrevX + offX + barTotalDataLabelsConfig.offsetX + (this.barCtx.isReversed ? -(barWidth + strokeWidth) : strokeWidth);
      }
      totalDataLabelsY = dataLabelsY - textRects.height / 2 + totalLabeltextRects.height / 2 + barTotalDataLabelsConfig.offsetY + strokeWidth;
      if (w2.globals.barGroups.length > 1 && !totalGroupCtx) {
        totalDataLabelsY = totalDataLabelsY - w2.globals.barGroups.length / 2 * (barHeight / 2);
      }
    }
    if (!w2.config.chart.stacked) {
      if (dataLabelsConfig.textAnchor === "start") {
        if (dataLabelsX - textRects.width < 0) {
          dataLabelsX = valIsNegative ? textRects.width + strokeWidth - offX : strokeWidth + offX;
        } else if (dataLabelsX + textRects.width > w2.layout.gridWidth) {
          dataLabelsX = valIsNegative ? w2.layout.gridWidth - strokeWidth : w2.layout.gridWidth - textRects.width - strokeWidth;
        }
      } else if (dataLabelsConfig.textAnchor === "middle") {
        if (dataLabelsX - textRects.width / 2 < 0) {
          dataLabelsX = textRects.width / 2 + strokeWidth;
        } else if (dataLabelsX + textRects.width / 2 > w2.layout.gridWidth) {
          dataLabelsX = w2.layout.gridWidth - textRects.width / 2 - strokeWidth;
        }
      } else if (dataLabelsConfig.textAnchor === "end") {
        if (dataLabelsX < 1) {
          dataLabelsX = textRects.width + strokeWidth;
        } else if (dataLabelsX + 1 > w2.layout.gridWidth) {
          dataLabelsX = w2.layout.gridWidth - textRects.width - strokeWidth;
        }
      }
    }
    return {
      bcx: x,
      bcy,
      dataLabelsX,
      dataLabelsY,
      totalDataLabelsX,
      totalDataLabelsY,
      totalDataLabelsAnchor
    };
  }
  /** @param {{x: any, y: any, val: any, i: any, j: any, textRects: any, barHeight: any, barWidth: any, dataLabelsConfig: any}} opts */
  drawCalculatedDataLabels({
    x,
    y,
    val,
    i: i2,
    // = realIndex
    j,
    textRects,
    barHeight,
    barWidth,
    dataLabelsConfig
  }) {
    var _a, _b;
    const w2 = this.w;
    let rotate = "rotate(0)";
    if (w2.config.plotOptions.bar.dataLabels.orientation === "vertical")
      rotate = `rotate(-90, ${x}, ${y})`;
    const dataLabels = new DataLabels(this.barCtx.w, this.barCtx.ctx);
    const graphics = new Graphics(this.barCtx.w);
    const formatter = dataLabelsConfig.formatter;
    let elDataLabelsWrap = null;
    const isSeriesNotCollapsed = w2.globals.collapsedSeriesIndices.indexOf(i2) > -1;
    if (dataLabelsConfig.enabled && !isSeriesNotCollapsed) {
      elDataLabelsWrap = graphics.group({
        class: "apexcharts-data-labels",
        transform: rotate
      });
      const dlCfg = w2.config.dataLabels;
      if (((_a = dlCfg.animate) == null ? void 0 : _a.enabled) || ((_b = dlCfg.countUp) == null ? void 0 : _b.enabled)) {
        elDataLabelsWrap.node.setAttribute(
          "data:dlKey",
          `${i2}::${datumKey(w2, i2, j)}`
        );
        elDataLabelsWrap.node.setAttribute("data:dlJ", String(j));
        if (typeof val === "number" && isFinite(val)) {
          elDataLabelsWrap.node.setAttribute("data:dlVal", String(val));
        }
      }
      let text = "";
      if (typeof val !== "undefined") {
        text = formatter(val, __spreadProps(__spreadValues({}, w2), {
          seriesIndex: i2,
          dataPointIndex: j,
          w: w2
        }));
      }
      if (!val && w2.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
        text = "";
      }
      const valIsNegative = w2.seriesData.series[i2][j] < 0;
      const position = w2.config.plotOptions.bar.dataLabels.position;
      if (w2.config.plotOptions.bar.dataLabels.orientation === "vertical") {
        if (position === "top") {
          if (valIsNegative) dataLabelsConfig.textAnchor = "end";
          else dataLabelsConfig.textAnchor = "start";
        }
        if (position === "center") {
          dataLabelsConfig.textAnchor = "middle";
        }
        if (position === "bottom") {
          if (valIsNegative) dataLabelsConfig.textAnchor = "end";
          else dataLabelsConfig.textAnchor = "start";
        }
      }
      if (this.barCtx.isRangeBar && this.barCtx.barOptions.dataLabels.hideOverflowingLabels) {
        const txRect = graphics.getTextRects(
          text,
          parseFloat(dataLabelsConfig.style.fontSize).toString()
        );
        if (barWidth < txRect.width) {
          text = "";
        }
      }
      if (w2.config.chart.stacked && this.barCtx.barOptions.dataLabels.hideOverflowingLabels) {
        if (this.barCtx.isHorizontal) {
          if (textRects.width / 1.6 > Math.abs(barWidth)) {
            text = "";
          }
        } else {
          if (textRects.height / 1.6 > Math.abs(barHeight)) {
            text = "";
          }
        }
      }
      const modifiedDataLabelsConfig = __spreadValues({}, dataLabelsConfig);
      if (this.barCtx.isHorizontal) {
        if (val < 0) {
          if (dataLabelsConfig.textAnchor === "start") {
            modifiedDataLabelsConfig.textAnchor = "end";
          } else if (dataLabelsConfig.textAnchor === "end") {
            modifiedDataLabelsConfig.textAnchor = "start";
          }
        }
      }
      dataLabels.plotDataLabelsText({
        x,
        y,
        text,
        i: i2,
        j,
        parent: elDataLabelsWrap,
        dataLabelsConfig: modifiedDataLabelsConfig,
        alwaysDrawDataLabel: true,
        offsetCorrection: true
      });
    }
    return elDataLabelsWrap;
  }
  /** @param {{ x?: any, y?: any, val?: any, realIndex?: any, textAnchor?: any, barWidth?: any, barHeight?: any, dataLabelsConfig?: any, barTotalDataLabelsConfig?: any }} opts */
  drawTotalDataLabels({
    x,
    y,
    val,
    realIndex,
    textAnchor,
    barTotalDataLabelsConfig
  }) {
    const graphics = new Graphics(this.barCtx.w);
    let totalDataLabelText;
    if (barTotalDataLabelsConfig.enabled && typeof x !== "undefined" && typeof y !== "undefined" && this.drawsStackedTotal(realIndex)) {
      totalDataLabelText = graphics.drawText({
        x,
        y,
        foreColor: barTotalDataLabelsConfig.style.color,
        text: val,
        textAnchor,
        fontFamily: barTotalDataLabelsConfig.style.fontFamily,
        fontSize: barTotalDataLabelsConfig.style.fontSize,
        fontWeight: barTotalDataLabelsConfig.style.fontWeight
      });
    }
    return totalDataLabelText;
  }
}
function isHistogramOverlay(w2) {
  var _a, _b, _c, _d, _e, _f, _g;
  if (((_b = (_a = w2 == null ? void 0 : w2.config) == null ? void 0 : _a.chart) == null ? void 0 : _b.requestedType) !== "histogram") return false;
  if (((_d = (_c = w2.config.plotOptions) == null ? void 0 : _c.histogram) == null ? void 0 : _d.overlap) === false) return false;
  return ((_g = (_f = (_e = w2.seriesData) == null ? void 0 : _e.series) == null ? void 0 : _f.length) != null ? _g : 0) > 1;
}
let Helpers$1 = class Helpers4 {
  /**
   * @param {Record<string, any>} barCtx
   */
  constructor(barCtx) {
    this.w = barCtx.w;
    this.barCtx = barCtx;
  }
  /**
   * @param {any[]} series
   */
  initVariables(series) {
    const w2 = this.w;
    this.barCtx.series = series;
    this.barCtx.totalItems = 0;
    this.barCtx.seriesLen = 0;
    this.barCtx.visibleI = -1;
    this.barCtx.visibleItems = 1;
    for (let sl = 0; sl < series.length; sl++) {
      if (series[sl].length > 0) {
        this.barCtx.seriesLen = this.barCtx.seriesLen + 1;
        this.barCtx.totalItems += series[sl].length;
      }
      if (w2.axisFlags.isXNumeric) {
        for (let j = 0; j < series[sl].length; j++) {
          if (w2.seriesData.seriesX[sl][j] > w2.globals.minX && w2.seriesData.seriesX[sl][j] < w2.globals.maxX) {
            this.barCtx.visibleItems++;
          }
        }
      } else {
        this.barCtx.visibleItems = w2.globals.dataPoints;
      }
    }
    this.arrBorderRadius = this.createBorderRadiusArr(w2.seriesData.series);
    if (Utils$1.isSafari()) {
      this.arrBorderRadius = this.arrBorderRadius.map(
        (brArr) => (
          /**
           * @param {any} _
           */
          brArr.map((_) => "none")
        )
      );
    }
    if (this.barCtx.seriesLen === 0) {
      this.barCtx.seriesLen = 1;
    }
  }
  /**
   * @param {number} realIndex
   */
  initialPositions(realIndex) {
    const w2 = this.w;
    let x, y, yDivision, xDivision, barHeight, barWidth, zeroH, zeroW;
    let dataPoints = w2.globals.dataPoints;
    if (this.barCtx.isRangeBar) {
      dataPoints = w2.labelData.labels.length;
    }
    let seriesLen = this.barCtx.seriesLen;
    if (w2.config.plotOptions.bar.rangeBarGroupRows || isHistogramOverlay(w2)) {
      seriesLen = 1;
    }
    if (this.barCtx.isHorizontal) {
      yDivision = w2.layout.gridHeight / dataPoints;
      barHeight = yDivision / seriesLen;
      if (w2.axisFlags.isXNumeric) {
        yDivision = w2.layout.gridHeight / this.barCtx.totalItems;
        barHeight = yDivision / this.barCtx.seriesLen;
      }
      barHeight = barHeight * parseInt(this.barCtx.barOptions.barHeight, 10) / 100;
      if (String(this.barCtx.barOptions.barHeight).indexOf("%") === -1) {
        barHeight = parseInt(this.barCtx.barOptions.barHeight, 10);
      }
      zeroW = this.barCtx.baseLineInvertedY + w2.globals.padHorizontal + (this.barCtx.isReversed ? w2.layout.gridWidth : 0) - (this.barCtx.isReversed ? this.barCtx.baseLineInvertedY * 2 : 0);
      if (this.barCtx.isFunnel) {
        zeroW = w2.layout.gridWidth / 2;
      }
      y = (yDivision - barHeight * this.barCtx.seriesLen) / 2;
    } else {
      xDivision = w2.layout.gridWidth / this.barCtx.visibleItems;
      if (w2.config.xaxis.convertedCatToNumeric) {
        xDivision = w2.layout.gridWidth / w2.globals.dataPoints;
      }
      barWidth = xDivision / seriesLen * parseInt(this.barCtx.barOptions.columnWidth, 10) / 100;
      if (w2.axisFlags.isXNumeric) {
        const xRatio = this.barCtx.xRatio;
        if (w2.globals.minXDiff && w2.globals.minXDiff !== 0.5 && w2.globals.minXDiff / xRatio > 0) {
          xDivision = w2.globals.minXDiff / xRatio;
        }
        barWidth = xDivision / seriesLen * parseInt(this.barCtx.barOptions.columnWidth, 10) / 100;
        if (barWidth < 1) {
          barWidth = 1;
        }
      }
      if (String(this.barCtx.barOptions.columnWidth).indexOf("%") === -1) {
        barWidth = parseInt(this.barCtx.barOptions.columnWidth, 10);
      }
      zeroH = w2.layout.gridHeight - this.barCtx.baseLineY[this.barCtx.translationsIndex] - (this.barCtx.isReversed ? w2.layout.gridHeight : 0) + (this.barCtx.isReversed ? this.barCtx.baseLineY[this.barCtx.translationsIndex] * 2 : 0);
      if (w2.axisFlags.isXNumeric) {
        const xForNumericX = this.barCtx.getBarXForNumericXAxis({
          x,
          j: 0,
          realIndex,
          barWidth
        });
        x = xForNumericX.x;
      } else {
        x = w2.globals.padHorizontal + Utils$1.noExponents(xDivision - barWidth * this.barCtx.seriesLen) / 2;
      }
    }
    w2.globals.barHeight = barHeight;
    w2.globals.barWidth = barWidth;
    return {
      x,
      y,
      yDivision,
      xDivision,
      barHeight,
      barWidth,
      zeroH,
      zeroW
    };
  }
  /**
   * @param {Record<string, any>} ctx
   */
  initializeStackedPrevVars(ctx) {
    const w2 = ctx.w;
    w2.labelData.seriesGroups.forEach((group) => {
      if (!ctx[group]) ctx[group] = {};
      ctx[group].prevY = [];
      ctx[group].prevX = [];
      ctx[group].prevYF = [];
      ctx[group].prevXF = [];
      ctx[group].prevYVal = [];
      ctx[group].prevXVal = [];
    });
  }
  /**
   * @param {Record<string, any>} ctx
   */
  initializeStackedXYVars(ctx) {
    const w2 = ctx.w;
    w2.labelData.seriesGroups.forEach((group) => {
      if (!ctx[group]) ctx[group] = {};
      ctx[group].xArrj = [];
      ctx[group].xArrjF = [];
      ctx[group].xArrjVal = [];
      ctx[group].yArrj = [];
      ctx[group].yArrjF = [];
      ctx[group].yArrjVal = [];
    });
  }
  /**
   * @param {any[]} series
   * @param {number} i
   * @param {number} j
   * @param {number} realIndex
   */
  getPathFillColor(series, i2, j, realIndex) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const fill = new Fill(this.barCtx.w);
    let fillColor = null;
    const seriesNumber = this.barCtx.barOptions.distributed ? j : i2;
    let useRangeColor = false;
    if (this.barCtx.barOptions.colors.ranges.length > 0) {
      const colorRange = this.barCtx.barOptions.colors.ranges;
      colorRange.map((range) => {
        if (series[i2][j] >= range.from && series[i2][j] <= range.to) {
          fillColor = range.color;
          useRangeColor = true;
        }
      });
    }
    const pathFill = fill.fillPath({
      seriesNumber: this.barCtx.barOptions.distributed ? seriesNumber : realIndex,
      dataPointIndex: j,
      color: fillColor,
      value: series[i2][j],
      fillConfig: (_a = w2.config.series[i2].data[j]) == null ? void 0 : _a.fill,
      fillType: ((_c = (_b = w2.config.series[i2].data[j]) == null ? void 0 : _b.fill) == null ? void 0 : _c.type) ? (_d = w2.config.series[i2].data[j]) == null ? void 0 : _d.fill.type : Array.isArray(w2.config.fill.type) ? w2.config.fill.type[realIndex] : w2.config.fill.type
    });
    return {
      color: pathFill,
      useRangeColor
    };
  }
  /**
   * @param {number} i
   * @param {number} j
   * @param {number} realIndex
   */
  getStrokeWidth(i2, j, realIndex) {
    let strokeWidth = 0;
    const w2 = this.w;
    if (typeof this.barCtx.series[i2][j] === "undefined" || this.barCtx.series[i2][j] === null || w2.config.chart.type === "bar" && !this.barCtx.series[i2][j]) {
      this.barCtx.isNullValue = true;
    } else {
      this.barCtx.isNullValue = false;
    }
    if (w2.config.stroke.show) {
      if (!this.barCtx.isNullValue) {
        strokeWidth = Array.isArray(this.barCtx.strokeWidth) ? this.barCtx.strokeWidth[realIndex] : this.barCtx.strokeWidth;
      }
    }
    return strokeWidth;
  }
  /**
   * @param {any[]} series
   */
  createBorderRadiusArr(series) {
    var _a;
    const w2 = this.w;
    const alwaysApplyRadius = !this.w.config.chart.stacked || w2.config.plotOptions.bar.borderRadius <= 0;
    const numSeries = series.length;
    const numColumns = ((_a = series[0]) == null ? void 0 : _a.length) | 0;
    const output = Array.from(
      { length: numSeries },
      () => Array(numColumns).fill(alwaysApplyRadius ? "top" : "none")
    );
    if (alwaysApplyRadius) return output;
    const chartType = this.w.config.chart.type;
    for (let j = 0; j < numColumns; j++) {
      const positiveIndices = [];
      const negativeIndices = [];
      let nonZeroCount = 0;
      for (let i2 = 0; i2 < numSeries; i2++) {
        const value = series[i2][j];
        if (value > 0) {
          positiveIndices.push(i2);
          nonZeroCount++;
        } else if (value < 0) {
          negativeIndices.push(i2);
          nonZeroCount++;
        }
      }
      if (positiveIndices.length > 0 && negativeIndices.length === 0) {
        if (positiveIndices.length === 1) {
          output[positiveIndices[0]][j] = chartType === "bar" && numColumns === 1 ? "top" : "both";
        } else {
          const firstPositiveIndex = positiveIndices[0];
          const lastPositiveIndex = positiveIndices[positiveIndices.length - 1];
          for (const i2 of positiveIndices) {
            if (i2 === firstPositiveIndex) {
              output[i2][j] = chartType === "bar" && numColumns === 1 ? "top" : "bottom";
            } else if (i2 === lastPositiveIndex) {
              output[i2][j] = "top";
            } else {
              output[i2][j] = "none";
            }
          }
        }
      } else if (negativeIndices.length > 0 && positiveIndices.length === 0) {
        if (negativeIndices.length === 1) {
          output[negativeIndices[0]][j] = "both";
        } else {
          const highestNegativeIndex = Math.max(...negativeIndices);
          const lowestNegativeIndex = Math.min(...negativeIndices);
          for (const i2 of negativeIndices) {
            if (i2 === highestNegativeIndex) {
              output[i2][j] = "bottom";
            } else if (i2 === lowestNegativeIndex) {
              output[i2][j] = "top";
            } else {
              output[i2][j] = "none";
            }
          }
        }
      } else if (positiveIndices.length > 0 && negativeIndices.length > 0) {
        const lastPositiveIndex = positiveIndices[positiveIndices.length - 1];
        for (const i2 of positiveIndices) {
          if (i2 === lastPositiveIndex) {
            output[i2][j] = "top";
          } else {
            output[i2][j] = "none";
          }
        }
        const highestNegativeIndex = Math.max(...negativeIndices);
        for (const i2 of negativeIndices) {
          if (i2 === highestNegativeIndex) {
            output[i2][j] = "bottom";
          } else {
            output[i2][j] = "none";
          }
        }
      } else if (nonZeroCount === 1) {
        const index = positiveIndices[0] || negativeIndices[0];
        output[index][j] = "both";
      }
    }
    return output;
  }
  /** @param {{ j?: any, i?: any, x1?: any, x2?: any, y1?: any, y2?: any, bc?: any, elSeries?: any }} opts */
  barBackground({ j, i: i2, x1, x2, y1, y2, elSeries }) {
    const w2 = this.w;
    const graphics = new Graphics(this.barCtx.w);
    const sr = new Series(this.barCtx.w);
    const activeSeriesIndex = sr.getActiveConfigSeriesIndex();
    if (this.barCtx.barOptions.colors.backgroundBarColors.length > 0 && activeSeriesIndex === i2) {
      if (j >= this.barCtx.barOptions.colors.backgroundBarColors.length) {
        j %= this.barCtx.barOptions.colors.backgroundBarColors.length;
      }
      const bcolor = this.barCtx.barOptions.colors.backgroundBarColors[j];
      const rect = graphics.drawRect(
        typeof x1 !== "undefined" ? x1 : 0,
        typeof y1 !== "undefined" ? y1 : 0,
        typeof x2 !== "undefined" ? x2 : w2.layout.gridWidth,
        typeof y2 !== "undefined" ? y2 : w2.layout.gridHeight,
        this.barCtx.barOptions.colors.backgroundBarRadius,
        bcolor,
        this.barCtx.barOptions.colors.backgroundBarOpacity
      );
      elSeries.add(rect);
      rect.node.classList.add("apexcharts-backgroundBar");
    }
  }
  /** @param {{ barWidth?: any, barXPosition?: any, y1?: any, y2?: any, yRatio?: any, strokeWidth?: any, isReversed?: any, series?: any, seriesGroup?: any, realIndex?: any, i?: any, j?: any, w?: any }} opts */
  getColumnPaths({
    barWidth,
    barXPosition,
    y1,
    y2,
    strokeWidth,
    isReversed,
    series,
    seriesGroup,
    realIndex,
    i: i2,
    j,
    w: w2
  }) {
    var _a, _b, _c;
    const graphics = new Graphics(this.barCtx.w);
    strokeWidth = Array.isArray(strokeWidth) ? strokeWidth[realIndex] : strokeWidth;
    if (!strokeWidth) strokeWidth = 0;
    let bW = barWidth;
    let bXP = barXPosition;
    if ((_a = w2.config.series[realIndex].data[j]) == null ? void 0 : _a.columnWidthOffset) {
      bXP = barXPosition - w2.config.series[realIndex].data[j].columnWidthOffset / 2;
      bW = barWidth + w2.config.series[realIndex].data[j].columnWidthOffset;
    }
    const strokeCenter = strokeWidth / 2;
    const x1 = bXP + strokeCenter;
    const x2 = bXP + bW - strokeCenter;
    const direction = (series[i2][j] >= 0 ? 1 : -1) * (isReversed ? -1 : 1);
    y1 += 1e-3 - strokeCenter * direction;
    y2 += 1e-3 + strokeCenter * direction;
    const sl = graphics.line(x2, y1);
    const closing = w2.config.plotOptions.bar.borderRadiusApplication === "around" || this.arrBorderRadius[realIndex][j] === "both" ? " Z" : " z";
    let pathTo = graphics.move(x1, y1) + graphics.line(x1, y2) + graphics.line(x2, y2) + sl + closing;
    if (this.arrBorderRadius[realIndex][j] !== "none") {
      pathTo = graphics.roundPathCorners(
        pathTo,
        w2.config.plotOptions.bar.borderRadius
      );
    }
    let pathFrom = null;
    const morphFrom = (_c = (_b = this.barCtx.ctx) == null ? void 0 : _b.morphTypeChange) == null ? void 0 : _c.getInitialPathFor(
      realIndex,
      j
    );
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
    }
    if (pathFrom == null) {
      pathFrom = graphics.move(x1, y1) + graphics.line(x1, y1) + sl + sl + sl + sl + sl + graphics.line(x1, y1) + closing;
    }
    if (w2.config.chart.stacked) {
      let _ctx = this.barCtx;
      _ctx = this.barCtx[seriesGroup];
      _ctx.yArrj.push(y2 - strokeCenter * direction);
      _ctx.yArrjF.push(Math.abs(y1 - y2 + strokeWidth * direction));
      _ctx.yArrjVal.push(this.barCtx.series[i2][j]);
    }
    return {
      pathTo,
      pathFrom
    };
  }
  /**
   * Build a trapezoidal funnel-stage path. Used when
   * `plotOptions.funnel.shape === 'trapezoid'` is active alongside `isFunnel`.
   *
   * Each stage is a 4-corner polygon whose top width matches the current
   * stage's value and bottom width matches the next stage's value, producing
   * continuous sloped sides between consecutive stages.
   *
   * For the last stage, the bottom width is configurable:
   * - `lastShape: 'flat'`  → bottom width = top width (parallel sides)
   * - `lastShape: 'taper'` → bottom width = 0 (taper to a point)
   *
   * @param {{ barYPosition: number, barHeight: number, series: any[][], i: number, j: number, realIndex: number, strokeWidth: number, w: any }} opts
   */
  getFunnelTrapezoidPaths({
    barYPosition,
    barHeight,
    series,
    i: i2,
    j,
    realIndex,
    strokeWidth,
    w: w2
  }) {
    var _a, _b;
    const graphics = new Graphics(this.barCtx.w);
    const center = w2.layout.gridWidth / 2;
    const halfWidthFor = (v2) => Math.abs(v2 / this.barCtx.invertedYRatio) / 2;
    const topHalf = halfWidthFor(series[i2][j]);
    const lastIdx = series[i2].length - 1;
    const isLast = j === lastIdx;
    const lastShape = w2.config.plotOptions.funnel.lastShape === "taper" ? "taper" : "flat";
    let bottomHalf;
    if (isLast) {
      bottomHalf = lastShape === "taper" ? 0 : topHalf;
    } else {
      bottomHalf = halfWidthFor(series[i2][j + 1]);
    }
    const strokeCenter = strokeWidth / 2;
    const y1 = barYPosition + strokeCenter;
    const y2 = barYPosition + barHeight - strokeCenter;
    const topLeftX = center - topHalf;
    const topRightX = center + topHalf;
    const bottomLeftX = center - bottomHalf;
    const bottomRightX = center + bottomHalf;
    const pathTo = graphics.move(topLeftX, y1) + graphics.line(topRightX, y1) + graphics.line(bottomRightX, y2) + graphics.line(bottomLeftX, y2) + " Z";
    let pathFrom = null;
    const morphFrom = (_b = (_a = this.barCtx.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.getInitialPathFor(
      realIndex,
      j
    );
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
    }
    if (pathFrom == null) {
      pathFrom = graphics.move(center, y1) + graphics.line(center, y1) + graphics.line(center, y2) + graphics.line(center, y2) + " Z";
    }
    return {
      pathTo,
      pathFrom,
      // x is the right edge of the wider (top) side — used by dataLabel
      // positioning helpers that expect a "right" reference.
      x: topRightX,
      x1: topLeftX,
      barXPosition: center
    };
  }
  /**
   * Pre-compute the per-segment layout for a value-proportional pyramid.
   *
   * Each segment is a horizontal slice of a triangle whose apex sits at the
   * top of the plot area (width = 0) and whose base spans `gridWidth` at the
   * bottom. The vertical extent of each slice is its share of the total
   * series value, so areas track value contribution and segments share
   * edges (no gaps). The first data point is the apex, the last is the base.
   *
   * @param {any[]} seriesData - 1D array of values for a single series row
   * @returns {{ y: number, height: number, topHalf: number, bottomHalf: number }[]}
   */
  computePyramidLayout(seriesData) {
    const w2 = this.w;
    const gridHeight = w2.layout.gridHeight;
    const gridWidth = w2.layout.gridWidth;
    const values = seriesData.map(
      /** @param {any} v */
      (v2) => Math.abs(Number(v2) || 0)
    );
    const total = values.reduce(
      /** @param {number} a @param {number} b */
      (a2, b2) => a2 + b2,
      0
    );
    if (total === 0 || gridHeight <= 0) {
      return values.map(() => ({ y: 0, height: 0, topHalf: 0, bottomHalf: 0 }));
    }
    const halfWidth = gridWidth / 2;
    let cumulative = 0;
    const layout = [];
    for (let j = 0; j < values.length; j++) {
      const topRatio = cumulative / total;
      cumulative += values[j];
      const bottomRatio = cumulative / total;
      const topY = topRatio * gridHeight;
      const bottomY = bottomRatio * gridHeight;
      layout.push({
        y: topY,
        height: bottomY - topY,
        topHalf: topRatio * halfWidth,
        bottomHalf: bottomRatio * halfWidth
      });
    }
    return layout;
  }
  /**
   * Build a single pyramid stage path. Geometry is precomputed by
   * `computePyramidLayout`; this method only renders that geometry into an
   * SVG path string plus a `pathFrom` for entry/morph animations.
   *
   * @param {{ barYPosition: number, barHeight: number, topHalf: number, bottomHalf: number, realIndex: number, j: number, strokeWidth: number, w: any }} opts
   */
  getPyramidPaths({
    barYPosition,
    barHeight,
    topHalf,
    bottomHalf,
    realIndex,
    j,
    strokeWidth,
    w: w2
  }) {
    var _a, _b;
    const graphics = new Graphics(this.barCtx.w);
    const center = w2.layout.gridWidth / 2;
    const strokeCenter = strokeWidth / 2;
    const y1 = barYPosition + strokeCenter;
    const y2 = barYPosition + barHeight - strokeCenter;
    const topLeftX = center - topHalf;
    const topRightX = center + topHalf;
    const bottomLeftX = center - bottomHalf;
    const bottomRightX = center + bottomHalf;
    const pathTo = graphics.move(topLeftX, y1) + graphics.line(topRightX, y1) + graphics.line(bottomRightX, y2) + graphics.line(bottomLeftX, y2) + " Z";
    let pathFrom = null;
    const morphFrom = (_b = (_a = this.barCtx.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.getInitialPathFor(
      realIndex,
      j
    );
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
    }
    if (pathFrom == null) {
      pathFrom = graphics.move(center, y1) + graphics.line(center, y1) + graphics.line(center, y2) + graphics.line(center, y2) + " Z";
    }
    return {
      pathTo,
      pathFrom,
      x: topRightX,
      x1: topLeftX,
      barXPosition: center
    };
  }
  /** @param {{ barYPosition?: any, barHeight?: any, x1?: any, x2?: any, strokeWidth?: any, isReversed?: any, series?: any, seriesGroup?: any, realIndex?: any, i?: any, j?: any, w?: any }} opts */
  getBarpaths({
    barYPosition,
    barHeight,
    x1,
    x2,
    strokeWidth,
    isReversed,
    series,
    seriesGroup,
    realIndex,
    i: i2,
    j,
    w: w2
  }) {
    var _a, _b, _c;
    const graphics = new Graphics(this.barCtx.w);
    strokeWidth = Array.isArray(strokeWidth) ? strokeWidth[realIndex] : strokeWidth;
    if (!strokeWidth) strokeWidth = 0;
    let bYP = barYPosition;
    let bH = barHeight;
    if ((_a = w2.config.series[realIndex].data[j]) == null ? void 0 : _a.barHeightOffset) {
      bYP = barYPosition - w2.config.series[realIndex].data[j].barHeightOffset / 2;
      bH = barHeight + w2.config.series[realIndex].data[j].barHeightOffset;
    }
    const strokeCenter = strokeWidth / 2;
    const y1 = bYP + strokeCenter;
    const y2 = bYP + bH - strokeCenter;
    const direction = (series[i2][j] >= 0 ? 1 : -1) * (isReversed ? -1 : 1);
    x1 += 1e-3 + strokeCenter * direction;
    x2 += 1e-3 - strokeCenter * direction;
    const isFunnel = this.barCtx.isFunnel;
    const fromX = isFunnel ? (x1 + x2) / 2 : x1;
    const sl = graphics.line(x1, y2);
    const closing = w2.config.plotOptions.bar.borderRadiusApplication === "around" || this.arrBorderRadius[realIndex][j] === "both" ? " Z" : " z";
    let pathTo = graphics.move(x1, y1) + graphics.line(x2, y1) + graphics.line(x2, y2) + sl + closing;
    if (this.arrBorderRadius[realIndex][j] !== "none") {
      pathTo = graphics.roundPathCorners(
        pathTo,
        w2.config.plotOptions.bar.borderRadius
      );
    }
    let pathFrom = null;
    const morphFrom = (_c = (_b = this.barCtx.ctx) == null ? void 0 : _b.morphTypeChange) == null ? void 0 : _c.getInitialPathFor(
      realIndex,
      j
    );
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
    }
    if (pathFrom == null) {
      const slFrom = isFunnel ? graphics.line(fromX, y2) : sl;
      pathFrom = graphics.move(fromX, y1) + graphics.line(fromX, y1) + slFrom + slFrom + slFrom + slFrom + slFrom + graphics.line(fromX, y1) + closing;
    }
    if (w2.config.chart.stacked) {
      let _ctx = this.barCtx;
      _ctx = this.barCtx[seriesGroup];
      _ctx.xArrj.push(x2 + strokeCenter * direction);
      _ctx.xArrjF.push(Math.abs(x1 - x2 - strokeWidth * direction));
      _ctx.xArrjVal.push(this.barCtx.series[i2][j]);
    }
    return {
      pathTo,
      pathFrom
    };
  }
  /**
   * @param {number} value
   * @param {number} zeroW
   */
  getXForValue(value, zeroW, zeroPositionForNull = true) {
    let xForVal = zeroPositionForNull ? zeroW : null;
    if (typeof value !== "undefined" && value !== null) {
      xForVal = zeroW + value / this.barCtx.invertedYRatio - (this.barCtx.isReversed ? value / this.barCtx.invertedYRatio : 0) * 2;
    }
    return xForVal;
  }
  /**
   * @param {number} value
   * @param {number} zeroH
   * @param {number} translationsIndex
   */
  getYForValue(value, zeroH, translationsIndex, zeroPositionForNull = true) {
    let yForVal = zeroPositionForNull ? zeroH : null;
    if (typeof value !== "undefined" && value !== null) {
      yForVal = zeroH - value / this.barCtx.yRatio[translationsIndex] + (this.barCtx.isReversed ? value / this.barCtx.yRatio[translationsIndex] : 0) * 2;
    }
    return yForVal;
  }
  /**
   * @param {string} type
   * @param {number} zeroW
   * @param {number} zeroH
   * @param {number} i
   * @param {number} j
   * @param {number} translationsIndex
   */
  getGoalValues(type, zeroW, zeroH, i2, j, translationsIndex) {
    const w2 = this.w;
    const goals = [];
    const pushGoal = (value, attrs) => {
      goals.push({
        [type]: type === "x" ? this.getXForValue(value, zeroW, false) : this.getYForValue(value, zeroH, translationsIndex, false),
        attrs
      });
    };
    if (w2.seriesData.seriesGoals[i2] && w2.seriesData.seriesGoals[i2][j] && Array.isArray(w2.seriesData.seriesGoals[i2][j])) {
      w2.seriesData.seriesGoals[i2][j].forEach((goal) => {
        pushGoal(goal.value, goal);
      });
    }
    if (this.barCtx.barOptions.isDumbbell && w2.rangeData.seriesRange.length) {
      const colors = this.barCtx.barOptions.dumbbellColors ? this.barCtx.barOptions.dumbbellColors : w2.globals.colors;
      const commonAttrs = {
        strokeHeight: type === "x" ? 0 : w2.globals.markers.size[i2],
        strokeWidth: type === "x" ? w2.globals.markers.size[i2] : 0,
        strokeDashArray: 0,
        strokeLineCap: "round",
        strokeColor: Array.isArray(colors[i2]) ? colors[i2][0] : colors[i2]
      };
      pushGoal(w2.rangeData.seriesRangeStart[i2][j], commonAttrs);
      pushGoal(w2.rangeData.seriesRangeEnd[i2][j], __spreadProps(__spreadValues({}, commonAttrs), {
        strokeColor: Array.isArray(colors[i2]) ? colors[i2][1] : colors[i2]
      }));
    }
    return goals;
  }
  /** @param {{barXPosition: any, barYPosition: any, goalX: any, goalY: any, barWidth: any, barHeight: any}} opts */
  drawGoalLine({
    barXPosition,
    barYPosition,
    goalX,
    goalY,
    barWidth,
    barHeight
  }) {
    const hasGoals = Array.isArray(goalX) && goalX.length > 0 || Array.isArray(goalY) && goalY.length > 0;
    if (!hasGoals) {
      return null;
    }
    const graphics = new Graphics(this.barCtx.w);
    const lineGroup = graphics.group({
      className: "apexcharts-bar-goals-groups"
    });
    lineGroup.node.classList.add("apexcharts-element-hidden");
    this.barCtx.w.globals.delayedElements.push({
      el: lineGroup.node
    });
    lineGroup.attr(
      "clip-path",
      `url(#gridRectMarkerMask${this.barCtx.w.globals.cuid})`
    );
    let line = null;
    if (this.barCtx.isHorizontal) {
      if (Array.isArray(goalX)) {
        goalX.forEach((goal) => {
          if (goal.x >= -1 && goal.x <= graphics.w.layout.gridWidth + 1) {
            const sHeight = typeof goal.attrs.strokeHeight !== "undefined" ? goal.attrs.strokeHeight : barHeight / 2;
            const y = barYPosition + sHeight + barHeight / 2;
            line = graphics.drawLine(
              goal.x,
              y - sHeight * 2,
              goal.x,
              y,
              goal.attrs.strokeColor ? goal.attrs.strokeColor : void 0,
              goal.attrs.strokeDashArray,
              goal.attrs.strokeWidth ? goal.attrs.strokeWidth : 2,
              goal.attrs.strokeLineCap
            );
            lineGroup.add(line);
          }
        });
      }
    } else {
      if (Array.isArray(goalY)) {
        goalY.forEach((goal) => {
          if (goal.y >= -1 && goal.y <= graphics.w.layout.gridHeight + 1) {
            const sWidth = typeof goal.attrs.strokeWidth !== "undefined" ? goal.attrs.strokeWidth : barWidth / 2;
            const x = barXPosition + sWidth + barWidth / 2;
            line = graphics.drawLine(
              x - sWidth * 2,
              goal.y,
              x,
              goal.y,
              goal.attrs.strokeColor ? goal.attrs.strokeColor : void 0,
              goal.attrs.strokeDashArray,
              goal.attrs.strokeHeight ? goal.attrs.strokeHeight : 2,
              goal.attrs.strokeLineCap
            );
            lineGroup.add(line);
          }
        });
      }
    }
    return lineGroup;
  }
  /** @param {{prevPaths: any, currPaths: any, color: any, realIndex: any, j: any}} opts */
  drawBarShadow({ prevPaths, currPaths, color, realIndex, j }) {
    const w2 = this.w;
    const { x: prevX2, x1: prevX1, barYPosition: prevY1 } = prevPaths;
    const { x: currX2, x1: currX1, barYPosition: currY1 } = currPaths;
    const prevY2 = prevY1 + currPaths.barHeight;
    const graphics = new Graphics(this.barCtx.w);
    const utils = new Utils$1();
    const shadowPath = graphics.move(prevX1, prevY2) + graphics.line(prevX2, prevY2) + graphics.line(currX2, currY1) + graphics.line(currX1, currY1) + graphics.line(prevX1, prevY2) + (w2.config.plotOptions.bar.borderRadiusApplication === "around" || this.arrBorderRadius[realIndex][j] === "both" ? " Z" : " z");
    return graphics.drawPath({
      d: shadowPath,
      fill: utils.shadeColor(0.5, Utils$1.rgb2hex(color)),
      stroke: "none",
      strokeWidth: 0,
      fillOpacity: 1,
      classes: "apexcharts-bar-shadow apexcharts-decoration-element"
    });
  }
  /** @param {{i: any, j: any}} opts */
  getZeroValueEncounters({ i: i2, j }) {
    var _a;
    const w2 = this.w;
    let nonZeroColumns = 0;
    let zeroEncounters = 0;
    const seriesIndices = w2.config.plotOptions.bar.horizontal ? w2.seriesData.series.map((_, _i) => _i) : ((_a = w2.globals.columnSeries) == null ? void 0 : _a.i.map((_i) => _i)) || [];
    seriesIndices.forEach((_si) => {
      const val = w2.globals.seriesPercent[_si][j];
      if (val) {
        nonZeroColumns++;
      }
      if (_si < i2 && val === 0) {
        zeroEncounters++;
      }
    });
    return {
      nonZeroColumns,
      zeroEncounters
    };
  }
  /**
   * Index of the series group `seriesIndex` belongs to within
   * `w.labelData.seriesGroups`, or -1 when the chart has no groups.
   *
   * Unlike `getGroupIndex` this is a pure lookup: it never appends to
   * `columnGroupIndices`, so it is safe to call from positioning/label code
   * that must not perturb the draw order bookkeeping.
   * @param {number} seriesIndex
   * @returns {number}
   */
  getSeriesGroupIndex(seriesIndex) {
    const w2 = this.w;
    return w2.labelData.seriesGroups.findIndex(
      (group) => group.indexOf(w2.seriesData.seriesNames[seriesIndex]) > -1
    );
  }
  /**
   * @param {number} seriesIndex
   */
  getGroupIndex(seriesIndex) {
    const w2 = this.w;
    const groupIndex = w2.labelData.seriesGroups.findIndex(
      (group) => (
        // w.config.series[i].name may be undefined, so use
        // w.seriesData.seriesNames[i], which has default names for those
        // series. w.labelData.seriesGroups[] uses the same default naming.
        group.indexOf(w2.seriesData.seriesNames[seriesIndex]) > -1
      )
    );
    const cGI = this.barCtx.columnGroupIndices;
    let columnGroupIndex = cGI.indexOf(groupIndex);
    if (columnGroupIndex < 0) {
      cGI.push(groupIndex);
      columnGroupIndex = cGI.length - 1;
    }
    return { groupIndex, columnGroupIndex };
  }
};
class Bar {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   * @param {import('../types/internal').XYRatios} xyRatios
   */
  constructor(w2, ctx, xyRatios) {
    this.ctx = ctx;
    this.w = w2;
    this.barOptions = w2.config.plotOptions.bar;
    this.isHorizontal = this.barOptions.horizontal;
    this.strokeWidth = w2.config.stroke.width;
    this.isNullValue = false;
    this.isRangeBar = w2.rangeData.seriesRange.length && this.isHorizontal;
    this.isVerticalGroupedRangeBar = !w2.globals.isBarHorizontal && w2.rangeData.seriesRange.length && w2.config.plotOptions.bar.rangeBarGroupRows;
    this.isFunnel = this.barOptions.isFunnel;
    this.isPyramid = this.barOptions.isPyramid;
    this.pyramidLayout = null;
    this.xyRatios = xyRatios;
    this.xRatio = 0;
    this.yRatio = [];
    this.invertedXRatio = 0;
    this.invertedYRatio = 0;
    this.baseLineY = [];
    this.baseLineInvertedY = 0;
    if (this.xyRatios !== null) {
      this.xRatio = xyRatios.xRatio;
      this.yRatio = xyRatios.yRatio;
      this.invertedXRatio = xyRatios.invertedXRatio;
      this.invertedYRatio = xyRatios.invertedYRatio;
      this.baseLineY = xyRatios.baseLineY;
      this.baseLineInvertedY = xyRatios.baseLineInvertedY;
    }
    this.yaxisIndex = 0;
    this.translationsIndex = 0;
    this.seriesLen = 0;
    this.pathArr = [];
    this._prevKeyed = null;
    this._ltCache = null;
    this._layoutShiftCache = null;
    this.series = [];
    this.elSeries = null;
    this.visibleI = 0;
    this.isReversed = false;
    const ser = new Series(this.w);
    this.lastActiveBarSerieIndex = ser.getActiveConfigSeriesIndex("desc", [
      "bar",
      "column"
    ]);
    this.lastActiveBarSerieIndexByGroup = ser.getActiveConfigSeriesIndexByGroup(["bar", "column"]);
    this.columnGroupIndices = [];
    const barSeriesIndices = ser.getBarSeriesIndices();
    const coreUtils = new CoreUtils(this.w);
    this.stackedSeriesTotals = coreUtils.getStackedSeriesTotals(
      this.w.config.series.map((s2, i2) => {
        return barSeriesIndices.indexOf(i2) === -1 ? i2 : -1;
      }).filter((s2) => {
        return s2 !== -1;
      })
    );
    this.barHelpers = new Helpers$1(this);
  }
  /** primary draw method which is called on bar object
   * @memberof Bar
   * @param {any[]} series - user supplied series values
   * @param {number} seriesIndex - the index by which series will be drawn on the svg
   * @return {Element} element which is supplied to parent chart draw method for appending
   **/
  draw(series, seriesIndex) {
    var _a, _b, _c;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const coreUtils = new CoreUtils(this.w);
    series = coreUtils.getLogSeries(series);
    this.series = series;
    this.yRatio = coreUtils.getLogYRatios(this.yRatio);
    this.barHelpers.initVariables(series);
    const ret = graphics.group({
      class: "apexcharts-bar-series apexcharts-plot-series"
    });
    if (w2.config.dataLabels.enabled) {
      if (this.totalItems > this.barOptions.dataLabels.maxItems) {
        console.warn(
          "WARNING: DataLabels are enabled but there are too many to display. This may cause performance issue when rendering - ApexCharts"
        );
      }
    }
    for (let i2 = 0, bc = 0; i2 < series.length; i2++, bc++) {
      let x, y;
      const yArrj = [];
      const xArrj = [];
      const realIndex = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex[i2]
      ) : i2;
      const { columnGroupIndex } = this.barHelpers.getGroupIndex(realIndex);
      const elSeries = graphics.group({
        class: `apexcharts-series`,
        rel: i2 + 1,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex]),
        "data:realIndex": realIndex
      });
      Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
      if (series[i2].length > 0) {
        this.visibleI = this.visibleI + 1;
      }
      if (this.yRatio.length > 1) {
        this.yaxisIndex = w2.globals.seriesYAxisReverseMap[realIndex];
        this.translationsIndex = realIndex;
      }
      const translationsIndex = this.translationsIndex;
      this.isReversed = w2.config.yaxis[this.yaxisIndex] && w2.config.yaxis[this.yaxisIndex].reversed;
      if (this.isPyramid) {
        this.pyramidLayout = this.barHelpers.computePyramidLayout(series[i2]);
      }
      const initPositions = this.barHelpers.initialPositions(realIndex);
      const {
        y: initY,
        yDivision,
        // yDivision is the GRIDHEIGHT divided by number of datapoints (bars)
        zeroW,
        // zeroW is the baseline where 0 meets x axis
        x: initX,
        xDivision,
        // xDivision is the GRIDWIDTH divided by number of datapoints (columns)
        zeroH
        // zeroH is the baseline where 0 meets y axis
      } = initPositions;
      let barHeight = initPositions.barHeight;
      let barWidth = initPositions.barWidth;
      y = initY;
      x = initX;
      if (!this.isHorizontal) {
        xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
      }
      const elDataLabelsWrap = graphics.group({
        class: "apexcharts-datalabels",
        "data:realIndex": realIndex
      });
      w2.globals.delayedElements.push({
        el: elDataLabelsWrap.node,
        // On a layout-changing update the labels must stay hidden through the
        // reflow morph (the updateOptions flow otherwise reveals them at
        // frame 0, where they float over sliding bars). When dataLabels.animate
        // is on the labels instead RIDE the morph (see DataLabelTransition), so
        // keep them visible: holding would hide the very motion we want to show.
        holdUntilComplete: !((_a = w2.config.dataLabels.animate) == null ? void 0 : _a.enabled) && this.isLengthTransition(realIndex)
      });
      elDataLabelsWrap.node.classList.add("apexcharts-element-hidden");
      const elGoalsMarkers = graphics.group({
        class: "apexcharts-bar-goals-markers"
      });
      const elBarShadows = graphics.group({
        class: "apexcharts-bar-shadows"
      });
      w2.globals.delayedElements.push({
        el: elBarShadows.node
      });
      elBarShadows.node.classList.add("apexcharts-element-hidden");
      for (let j = 0; j < series[i2].length; j++) {
        const strokeWidth = this.barHelpers.getStrokeWidth(i2, j, realIndex);
        let paths = (
          /** @type {any} */
          null
        );
        const pathsParams = {
          indexes: {
            i: i2,
            j,
            realIndex,
            translationsIndex,
            bc
          },
          x,
          y,
          strokeWidth,
          elSeries
        };
        if (this.isHorizontal) {
          paths = this.drawBarPaths(__spreadProps(__spreadValues({}, pathsParams), {
            barHeight,
            zeroW,
            yDivision
          }));
          barWidth = this.series[i2][j] / this.invertedYRatio;
        } else {
          paths = this.drawColumnPaths(__spreadProps(__spreadValues({}, pathsParams), {
            xDivision,
            barWidth,
            zeroH
          }));
          barHeight = this.series[i2][j] / this.yRatio[translationsIndex];
        }
        const pathFill = this.barHelpers.getPathFillColor(
          series,
          i2,
          j,
          realIndex
        );
        if (this.isFunnel && !this.isPyramid && this.barOptions.isFunnel3d && ((_b = w2.config.plotOptions.funnel) == null ? void 0 : _b.shape) !== "trapezoid" && this.pathArr.length && j > 0) {
          const barShadow = this.barHelpers.drawBarShadow({
            color: typeof pathFill.color === "string" && ((_c = pathFill.color) == null ? void 0 : _c.indexOf("url")) === -1 ? pathFill.color : Utils$1.hexToRgba(w2.globals.colors[i2]),
            prevPaths: this.pathArr[this.pathArr.length - 1],
            currPaths: paths,
            realIndex,
            j
          });
          elBarShadows.add(barShadow);
          if (w2.config.chart.dropShadow.enabled) {
            const filters = new Filters(this.w);
            filters.dropShadow(barShadow, w2.config.chart.dropShadow, realIndex);
          }
        }
        this.pathArr.push(paths);
        const barGoalLine = this.barHelpers.drawGoalLine({
          barXPosition: paths.barXPosition,
          barYPosition: paths.barYPosition,
          goalX: paths.goalX,
          goalY: paths.goalY,
          barHeight,
          barWidth
        });
        if (barGoalLine) {
          elGoalsMarkers.add(barGoalLine);
        }
        y = paths.y;
        x = paths.x;
        if (j > 0) {
          xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
        }
        yArrj.push(y);
        this.renderSeries(__spreadProps(__spreadValues({
          realIndex,
          pathFill: pathFill.color
        }, pathFill.useRangeColor ? { lineFill: pathFill.color } : {}), {
          j,
          i: i2,
          columnGroupIndex,
          pathFrom: paths.pathFrom,
          pathTo: paths.pathTo,
          strokeWidth,
          elSeries,
          x,
          y,
          series,
          barHeight: Math.abs(paths.barHeight ? paths.barHeight : barHeight),
          barWidth: Math.abs(paths.barWidth ? paths.barWidth : barWidth),
          elDataLabelsWrap,
          elGoalsMarkers,
          elBarShadows,
          visibleSeries: this.visibleI,
          type: "bar"
        }));
      }
      w2.globals.seriesXvalues[realIndex] = xArrj;
      w2.globals.seriesYvalues[realIndex] = yArrj;
      if (w2.globals.previousPaths.length > 0) {
        const newKeys = [];
        for (let j = 0; j < series[i2].length; j++) {
          newKeys.push(datumKey(w2, realIndex, j));
        }
        renderBarExitGhosts({
          w: w2,
          elSeries,
          record: this._prevRecord(realIndex),
          newKeys,
          isHorizontal: this.isHorizontal,
          speed: w2.config.chart.animations.dynamicAnimation.speed
        });
      }
      ret.add(elSeries);
    }
    return ret;
  }
  /** @param {{ realIndex?: any, pathFill?: any, lineFill?: any, j?: any, i?: any, columnGroupIndex?: any, pathFrom?: any, pathTo?: any, strokeWidth?: any, elSeries?: any, x?: any, y?: any, y1?: any, y2?: any, series?: any, barHeight?: any, barWidth?: any, barXPosition?: any, barYPosition?: any, elDataLabelsWrap?: any, elGoalsMarkers?: any, elBarShadows?: any, visibleSeries?: any, type?: any, classes?: any }} opts */
  renderSeries({
    realIndex,
    pathFill,
    lineFill,
    j,
    i: i2,
    columnGroupIndex,
    pathFrom,
    pathTo,
    strokeWidth,
    elSeries,
    x,
    // x pos
    y,
    // y pos
    y1,
    // absolute value
    y2,
    // absolute value
    series,
    barHeight,
    barWidth,
    barXPosition,
    barYPosition,
    elDataLabelsWrap,
    elGoalsMarkers,
    elBarShadows,
    visibleSeries,
    type,
    classes
  }) {
    var _a;
    const w2 = this.w;
    const graphics = new Graphics(this.w, this.ctx);
    const emit = seriesEmitter(this.ctx, graphics);
    let skipDrawing = false;
    if (!elSeries._bindingsDelegated) {
      elSeries._bindingsDelegated = true;
      graphics.setupEventDelegation(elSeries, `.apexcharts-${type}-area`);
    }
    if (!lineFill) {
      let fetchColor = function(i3) {
        const exp = w2.config.stroke.colors;
        let c2;
        if (Array.isArray(exp) && exp.length > 0) {
          c2 = exp[i3];
          if (!c2) c2 = "";
          if (typeof c2 === "function") {
            return c2({
              value: w2.seriesData.series[i3][j],
              dataPointIndex: j,
              w: w2
            });
          }
        }
        return c2;
      };
      const checkAvailableColor = typeof w2.globals.stroke.colors[realIndex] === "function" ? fetchColor(realIndex) : w2.globals.stroke.colors[realIndex];
      lineFill = this.barOptions.distributed ? w2.globals.stroke.colors[j] : checkAvailableColor;
    }
    const barDataLabels = new BarDataLabels(this);
    const dataLabelsObj = (
      /** @type {any} */
      barDataLabels.handleBarDataLabels({
        x,
        y,
        y1,
        y2,
        i: i2,
        j,
        series,
        realIndex,
        columnGroupIndex,
        barHeight,
        barWidth,
        barXPosition,
        barYPosition,
        visibleSeries
      })
    );
    if (!w2.globals.isBarHorizontal) {
      if (dataLabelsObj.dataLabelsPos.dataLabelsX + Math.max(barWidth, w2.globals.barPadForNumericAxis) < 0 || dataLabelsObj.dataLabelsPos.dataLabelsX - Math.max(barWidth, w2.globals.barPadForNumericAxis) > w2.layout.gridWidth) {
        skipDrawing = true;
      }
    }
    if (
      /** @type {Record<string,any>} */
      w2.config.series[i2].data[j] && /** @type {Record<string,any>} */
      w2.config.series[i2].data[j].strokeColor
    ) {
      lineFill = /** @type {Record<string,any>} */
      w2.config.series[i2].data[j].strokeColor;
    }
    if (this.isNullValue) {
      pathFill = "none";
    }
    const animCfg = w2.config.chart.animations;
    const gradCfg = animCfg.animateGradually;
    const staggerEnabled = gradCfg && gradCfg.enabled !== false && !(w2.globals.dataChanged && this.isLayoutShift(realIndex));
    let delay = 0;
    if (staggerEnabled) {
      const totalBars = w2.globals.dataPoints || 1;
      const configStep = gradCfg.delay || 0;
      const baseDelayMs = Math.min(
        configStep,
        animCfg.speed * 0.5 / Math.max(1, totalBars)
      );
      let delayMs = computeStagger({
        style: "sequential",
        index: j,
        baseDelay: baseDelayMs
      });
      if (w2.config.chart.stacked) {
        delayMs += i2 * baseDelayMs * 0.5;
      }
      const delayFactor = configStep || 1;
      delay = delayMs / delayFactor;
    }
    if (!skipDrawing) {
      const morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
      const dataChangeSpeed = morphActive ? this.ctx.morphTypeChange.getSpeed() : w2.config.chart.animations.dynamicAnimation.speed;
      const pieceClaimed = morphActive && this.ctx.morphTypeChange.claimsTargetMark(realIndex, j);
      if (pieceClaimed) {
        pathFrom = pathTo;
        delay = 0;
      }
      const renderedPath = (
        /** @type {any} */
        emit.renderPaths({
          i: i2,
          j,
          realIndex,
          pathFrom,
          pathTo,
          stroke: lineFill,
          strokeWidth,
          strokeLineCap: w2.config.stroke.lineCap,
          fill: pathFill,
          animationDelay: delay,
          initialSpeed: w2.config.chart.animations.speed,
          dataChangeSpeed,
          // `classes` is optional: boxPlot, violin and candlestick call
          // renderSeries without it, and interpolating it unguarded stamped a
          // literal "undefined" into every one of their marks' class lists.
          className: `apexcharts-${type}-area${classes ? ` ${classes}` : ""}`,
          chartType: type,
          bindEventsOnPaths: false
        })
      );
      renderedPath.attr("clip-path", `url(#gridRectBarMask${w2.globals.cuid})`);
      if (pieceClaimed) {
        renderedPath.node.setAttribute("opacity", "0");
        renderedPath.node.setAttribute("data-piece-hidden", "1");
      }
      const forecast = w2.config.forecastDataPoints;
      if (forecast.count > 0) {
        if (j >= w2.globals.dataPoints - forecast.count) {
          renderedPath.node.setAttribute("stroke-dasharray", forecast.dashArray);
          renderedPath.node.setAttribute("stroke-width", forecast.strokeWidth);
          renderedPath.node.setAttribute("fill-opacity", forecast.fillOpacity);
        }
      }
      if (typeof y1 !== "undefined" && typeof y2 !== "undefined") {
        renderedPath.attr("data-range-y1", y1);
        renderedPath.attr("data-range-y2", y2);
      }
      const filters = new Filters(this.w);
      filters.setSelectionFilter(renderedPath, realIndex, j);
      elSeries.add(renderedPath);
      renderedPath.attr({
        cy: dataLabelsObj.dataLabelsPos.bcy,
        cx: dataLabelsObj.dataLabelsPos.bcx,
        j,
        val: w2.seriesData.series[i2][j],
        barHeight,
        barWidth,
        // Datum identity for the next update's keyed join (see
        // LengthTransition): survivors match by key, not array position.
        "data:pathKey": datumKey(w2, realIndex, j)
      });
      if (emit.kind === "canvas") {
        if (!w2.globals.barCanvasCoords) w2.globals.barCanvasCoords = {};
        if (!w2.globals.barCanvasCoords[realIndex]) {
          w2.globals.barCanvasCoords[realIndex] = {};
        }
        w2.globals.barCanvasCoords[realIndex][j] = {
          cx: dataLabelsObj.dataLabelsPos.bcx,
          cy: dataLabelsObj.dataLabelsPos.bcy,
          barWidth
        };
      }
      if (dataLabelsObj.dataLabels !== null) {
        elDataLabelsWrap.add(dataLabelsObj.dataLabels);
      }
      if (dataLabelsObj.totalDataLabels) {
        elDataLabelsWrap.add(dataLabelsObj.totalDataLabels);
      }
      elSeries.add(elDataLabelsWrap);
      if (elGoalsMarkers) {
        elSeries.add(elGoalsMarkers);
      }
      if (elBarShadows) {
        elSeries.add(elBarShadows);
      }
    }
    return elSeries;
  }
  /** @param {{indexes: any, barHeight: any, strokeWidth: any, zeroW: any, x: any, y: any, yDivision: any, elSeries: any}} opts */
  drawBarPaths({
    indexes,
    barHeight,
    strokeWidth,
    zeroW,
    x,
    y,
    yDivision,
    elSeries
  }) {
    const w2 = this.w;
    const i2 = indexes.i;
    const j = indexes.j;
    let barYPosition;
    if (w2.axisFlags.isXNumeric) {
      y = (w2.seriesData.seriesX[i2][j] - w2.globals.minX) / this.invertedXRatio - barHeight;
      barYPosition = y + barHeight * this.visibleI;
    } else {
      if (w2.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
        const { nonZeroColumns, zeroEncounters } = this.barHelpers.getZeroValueEncounters({ i: i2, j });
        if (nonZeroColumns > 0) {
          barHeight = this.seriesLen * barHeight / nonZeroColumns;
        }
        barYPosition = y + barHeight * this.visibleI;
        barYPosition -= barHeight * zeroEncounters;
      } else {
        barYPosition = y + barHeight * this.visibleI;
      }
    }
    const useTrapezoid = this.isFunnel && w2.config.plotOptions.funnel.shape === "trapezoid";
    const pyramidSeg = this.isPyramid && this.pyramidLayout ? this.pyramidLayout[j] : null;
    const usePyramid = !!pyramidSeg;
    if (pyramidSeg) {
      barYPosition = pyramidSeg.y;
      barHeight = pyramidSeg.height;
    } else if (this.isFunnel && !useTrapezoid) {
      const _zeroW = zeroW != null ? zeroW : 0;
      zeroW = _zeroW - /** @type {number} */
      /** @type {any} */
      (this.barHelpers.getXForValue(
        /** @type {any} */
        this.series[i2][j],
        _zeroW
      ) - _zeroW) / 2;
    }
    x = this.barHelpers.getXForValue(
      /** @type {any} */
      this.series[i2][j],
      zeroW != null ? zeroW : 0
    );
    let paths;
    if (pyramidSeg) {
      paths = /** @type {any} */
      this.barHelpers.getPyramidPaths({
        barYPosition,
        barHeight,
        topHalf: pyramidSeg.topHalf,
        bottomHalf: pyramidSeg.bottomHalf,
        realIndex: indexes.realIndex,
        j,
        strokeWidth,
        w: w2
      });
    } else if (useTrapezoid) {
      paths = /** @type {any} */
      this.barHelpers.getFunnelTrapezoidPaths({
        barYPosition,
        barHeight,
        series: (
          /** @type {any} */
          this.series
        ),
        i: i2,
        j,
        realIndex: indexes.realIndex,
        strokeWidth,
        w: w2
      });
    } else {
      paths = /** @type {any} */
      this.barHelpers.getBarpaths({
        barYPosition,
        barHeight,
        x1: zeroW,
        x2: x,
        strokeWidth,
        isReversed: this.isReversed,
        series: this.series,
        realIndex: indexes.realIndex,
        i: i2,
        j,
        w: w2
      });
    }
    if (useTrapezoid || usePyramid) {
      zeroW = paths.x1;
      x = paths.x;
    }
    if (!w2.axisFlags.isXNumeric && !usePyramid) {
      y = y + yDivision;
    }
    if (usePyramid) {
      y = barYPosition;
    }
    this.barHelpers.barBackground({
      j,
      i: i2,
      y1: barYPosition - barHeight * this.visibleI,
      y2: barHeight * this.seriesLen,
      elSeries
    });
    return {
      pathTo: paths.pathTo,
      pathFrom: paths.pathFrom,
      x1: zeroW,
      x,
      y,
      goalX: this.barHelpers.getGoalValues(
        "x",
        zeroW,
        /** @type {any} */
        null,
        indexes.realIndex,
        j,
        0
      ),
      barYPosition,
      barHeight
    };
  }
  /** @param {{indexes: any, x: any, y: any, xDivision: any, barWidth: any, zeroH: any, strokeWidth: any, elSeries: any}} opts */
  drawColumnPaths({
    indexes,
    x,
    y,
    xDivision,
    barWidth,
    zeroH,
    strokeWidth,
    elSeries
  }) {
    const w2 = this.w;
    const realIndex = indexes.realIndex;
    const translationsIndex = indexes.translationsIndex;
    const i2 = indexes.i;
    const j = indexes.j;
    const bc = indexes.bc;
    let barXPosition;
    if (w2.axisFlags.isXNumeric) {
      const xForNumericX = this.getBarXForNumericXAxis({
        x,
        j,
        realIndex,
        barWidth
      });
      x = xForNumericX.x;
      barXPosition = xForNumericX.barXPosition;
    } else {
      if (w2.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
        const { nonZeroColumns, zeroEncounters } = this.barHelpers.getZeroValueEncounters({ i: i2, j });
        if (nonZeroColumns > 0) {
          barWidth = this.seriesLen * barWidth / nonZeroColumns;
        }
        barXPosition = x + barWidth * this.visibleI;
        barXPosition -= barWidth * zeroEncounters;
      } else {
        barXPosition = x + barWidth * this.visibleI;
      }
    }
    y = this.barHelpers.getYForValue(
      /** @type {any} */
      this.series[i2][j],
      zeroH,
      translationsIndex
    );
    const paths = (
      /** @type {any} */
      this.barHelpers.getColumnPaths({
        barXPosition,
        barWidth,
        y1: zeroH,
        y2: y,
        strokeWidth,
        isReversed: this.isReversed,
        series: this.series,
        realIndex,
        i: i2,
        j,
        w: w2
      })
    );
    if (!w2.axisFlags.isXNumeric) {
      x = x + xDivision;
    }
    this.barHelpers.barBackground({
      bc,
      j,
      i: i2,
      x1: barXPosition - strokeWidth / 2 - barWidth * this.visibleI,
      x2: barWidth * this.seriesLen + strokeWidth / 2,
      elSeries
    });
    return {
      pathTo: paths.pathTo,
      pathFrom: paths.pathFrom,
      x,
      y,
      goalY: this.barHelpers.getGoalValues(
        "y",
        /** @type {any} */
        null,
        zeroH,
        realIndex,
        j,
        translationsIndex
      ),
      barXPosition,
      barWidth
    };
  }
  /** @param {{x: any, barWidth: any, realIndex: any, j: any}} opts */
  getBarXForNumericXAxis({ x, barWidth, realIndex, j }) {
    const w2 = this.w;
    let sxI = realIndex;
    if (!w2.seriesData.seriesX[realIndex].length) {
      sxI = w2.globals.maxValsInArrayIndex;
    }
    if (Utils$1.isNumber(w2.seriesData.seriesX[sxI][j])) {
      x = AxisMapping.dataXToPx(w2, w2.seriesData.seriesX[sxI][j]) - barWidth * this.seriesLen / 2;
    }
    return {
      barXPosition: x + (isHistogramOverlay(w2) ? 0 : barWidth * this.visibleI),
      x
    };
  }
  /**
   * The captured previous-render record for a series (last match wins, same
   * as the historical scan order).
   *
   * @param {number} realIndex
   * @returns {any | null}
   */
  _prevRecord(realIndex) {
    const w2 = this.w;
    let record = null;
    for (let pp = 0; pp < w2.globals.previousPaths.length; pp++) {
      const gpp = w2.globals.previousPaths[pp];
      if (gpp.paths && gpp.paths.length > 0 && parseInt(gpp.realIndex, 10) === parseInt(String(realIndex), 10)) {
        record = gpp;
      }
    }
    return record;
  }
  /**
   * Previous paths of a series re-keyed by datum key (stamped as
   * `data:pathKey` on each bar path and captured by Series.getPreviousPaths).
   * Returns null when the previous render carries no keys (so the caller
   * falls back to positional matching).
   *
   * @param {number} realIndex
   * @returns {Map<string, {d: string}> | null}
   */
  _prevKeyedPaths(realIndex) {
    if (!this._prevKeyed) this._prevKeyed = {};
    if (this._prevKeyed[realIndex] !== void 0) {
      return this._prevKeyed[realIndex];
    }
    const record = this._prevRecord(realIndex);
    let map = null;
    if (record && record.paths.every((p2) => p2.key != null)) {
      const keyed = /* @__PURE__ */ new Map();
      record.paths.forEach((p2) => {
        keyed.set(p2.key, p2);
      });
      map = keyed;
    }
    this._prevKeyed[realIndex] = map;
    return map;
  }
  /**
   * Whether this series' update changes its datum layout (points entered,
   * exited, or changed identity). Layout-changing updates run all survivors
   * on one shared clock (no per-bar stagger) so the reflow reads as a single
   * coordinated motion; pure value updates keep the stagger.
   *
   * @param {number} realIndex
   * @returns {boolean}
   */
  isLengthTransition(realIndex) {
    var _a, _b;
    if (!this._ltCache) this._ltCache = {};
    if (this._ltCache[realIndex] !== void 0) return this._ltCache[realIndex];
    const w2 = this.w;
    let result = false;
    if (lengthTransitionEnabled(w2) && w2.globals.previousPaths.length > 0) {
      const record = this._prevRecord(realIndex);
      const dataLen = (_b = (_a = w2.seriesData.series[realIndex]) == null ? void 0 : _a.length) != null ? _b : 0;
      if (!record) {
        result = dataLen > 0;
      } else if (record.paths.length !== dataLen) {
        result = true;
      } else {
        const keyed = this._prevKeyedPaths(realIndex);
        if (keyed) {
          for (let j = 0; j < dataLen; j++) {
            if (!keyed.has(datumKey(w2, realIndex, j))) {
              result = true;
              break;
            }
          }
        }
      }
    }
    this._ltCache[realIndex] = result;
    return result;
  }
  /**
   * Whether this update moves survivors to new slots: a length change
   * (enter/exit, via isLengthTransition) OR a pure reorder (a "bar chart race"
   * swap, same datum set in a new order). Used to drop the per-bar stagger so
   * all bars slide on one shared clock, staying locked to the axis/data labels.
   * Broader than isLengthTransition, which is deliberately enter/exit-only
   * (exit ghosts / baseline enters must not fire on a plain reorder).
   * @param {number} realIndex
   */
  isLayoutShift(realIndex) {
    if (this.isLengthTransition(realIndex)) return true;
    if (!this._layoutShiftCache) this._layoutShiftCache = {};
    if (this._layoutShiftCache[realIndex] !== void 0) {
      return this._layoutShiftCache[realIndex];
    }
    const sj = seriesJoin(this.w, realIndex, true, true);
    const result = !!(sj && sj.join.changed);
    this._layoutShiftCache[realIndex] = result;
    return result;
  }
  /**
   * Resolve `pathFrom` for a bar on data update, joining old and new datums
   * by KEY (category label / x value) so survivors keep their identity across
   * inserts, prepends, and removals. Three outcomes:
   *
   *  - survivor with stable shape → the previous `d` (smooth reflow morph);
   *  - survivor whose command count changed (corner state flipped, e.g. bar
   *    became new top-of-stack) → `pathTo` (pathFrom === pathTo, a snap);
   *  - genuinely new datum (key absent from the previous render, or the whole
   *    series is new) → null, telling the path builder to use its
   *    grow-from-baseline enter path.
   *
   * Falls back to positional (index j) matching when the previous render
   * carries no datum keys.
   *
   * @param {number} realIndex - stable series index from `data:realIndex`
   * @param {number} j - data-point index within the series
   * @param {string} pathTo - the freshly-built path for this bar (post-roundPathCorners)
   * @returns {string | null}
   **/
  getPreviousPath(realIndex, j, pathTo) {
    const w2 = this.w;
    const record = this._prevRecord(realIndex);
    if (!record) {
      return lengthTransitionEnabled(w2) ? null : pathTo;
    }
    let oldD = null;
    let isNewDatum = false;
    const keyed = this._prevKeyedPaths(realIndex);
    if (keyed) {
      const prev = keyed.get(datumKey(w2, realIndex, j));
      if (prev) {
        oldD = prev.d;
      } else {
        isNewDatum = true;
      }
    } else if (typeof record.paths[j] !== "undefined") {
      oldD = record.paths[j].d;
    } else {
      isNewDatum = true;
    }
    if (oldD && Bar.pathCommandCount(oldD) === Bar.pathCommandCount(pathTo)) {
      return oldD;
    }
    if (isNewDatum && lengthTransitionEnabled(w2)) {
      return null;
    }
    return pathTo;
  }
  /**
   * Count SVG path commands (M, L, C, Q, Z, etc.). Used to detect whether
   * two paths can be morphed safely — SVG.js requires matching command counts.
   *
   * @param {string} d
   * @returns {number}
   */
  static pathCommandCount(d) {
    if (!d) return 0;
    const matches = d.match(/[A-Za-z]/g);
    return matches ? matches.length : 0;
  }
}
class BarStacked extends Bar {
  /**
   * @param {any[]} series
   * @param {number} seriesIndex
   */
  draw(series, seriesIndex) {
    const w2 = this.w;
    this.graphics = new Graphics(this.w);
    this.bar = new Bar(this.w, this.ctx, this.xyRatios);
    const coreUtils = new CoreUtils(this.w);
    series = coreUtils.getLogSeries(series);
    this.yRatio = coreUtils.getLogYRatios(this.yRatio);
    this.barHelpers.initVariables(series);
    if (w2.config.chart.stackType === "100%") {
      series = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex.map(
          (_) => w2.globals.seriesPercent[_]
        )
      ) : w2.globals.seriesPercent.slice();
    }
    this.series = series;
    this.barHelpers.initializeStackedPrevVars(this);
    const ret = this.graphics.group({
      class: "apexcharts-bar-series apexcharts-plot-series"
    });
    let x = 0;
    let y = 0;
    for (let i2 = 0, bc = 0; i2 < series.length; i2++, bc++) {
      const realIndex = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex[i2]
      ) : i2;
      const { groupIndex, columnGroupIndex } = this.barHelpers.getGroupIndex(realIndex);
      this.groupCtx = /** @type {any} */
      this[
        /** @type {any} */
        w2.labelData.seriesGroups[groupIndex]
      ];
      const xArrValues = [];
      const yArrValues = [];
      let translationsIndex = 0;
      if (this.yRatio.length > 1) {
        this.yaxisIndex = /** @type {any} */
        w2.globals.seriesYAxisReverseMap[realIndex][0];
        translationsIndex = realIndex;
      }
      this.isReversed = w2.config.yaxis[this.yaxisIndex] && w2.config.yaxis[this.yaxisIndex].reversed;
      let elSeries = this.graphics.group({
        class: `apexcharts-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex]),
        rel: i2 + 1,
        "data:realIndex": realIndex
      });
      Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
      const elDataLabelsWrap = this.graphics.group({
        class: "apexcharts-datalabels",
        "data:realIndex": realIndex
      });
      const elGoalsMarkers = this.graphics.group({
        class: "apexcharts-bar-goals-markers"
      });
      const initPositions = this.initialPositions(
        x,
        y,
        void 0,
        void 0,
        void 0,
        void 0,
        translationsIndex
      );
      const {
        xDivision,
        // xDivision is the GRIDWIDTH divided by number of datapoints (columns)
        yDivision,
        // yDivision is the GRIDHEIGHT divided by number of datapoints (bars)
        zeroH,
        // zeroH is the baseline where 0 meets y axis
        zeroW
        // zeroW is the baseline where 0 meets x axis
      } = initPositions;
      let barHeight = initPositions.barHeight;
      let barWidth = initPositions.barWidth;
      y = initPositions.y;
      x = initPositions.x;
      w2.globals.barHeight = barHeight;
      w2.globals.barWidth = barWidth;
      this.barHelpers.initializeStackedXYVars(this);
      if (this.groupCtx.prevY.length === 1 && /**
       * @param {number} val
       */
      this.groupCtx.prevY[0].every((val) => isNaN(val))) {
        this.groupCtx.prevY[0] = this.groupCtx.prevY[0].map(() => zeroH);
        this.groupCtx.prevYF[0] = this.groupCtx.prevYF[0].map(() => 0);
      }
      for (let j = 0; j < w2.globals.dataPoints; j++) {
        const strokeWidth = this.barHelpers.getStrokeWidth(i2, j, realIndex);
        const commonPathOpts = {
          indexes: { i: i2, j, realIndex, translationsIndex, bc },
          strokeWidth,
          x,
          y,
          elSeries,
          columnGroupIndex,
          seriesGroup: w2.labelData.seriesGroups[groupIndex]
        };
        let paths = (
          /** @type {any} */
          null
        );
        if (this.isHorizontal) {
          paths = this.drawStackedBarPaths(__spreadProps(__spreadValues({}, commonPathOpts), {
            zeroW,
            barHeight,
            yDivision
          }));
          barWidth = this.series[i2][j] / this.invertedYRatio;
        } else {
          paths = this.drawStackedColumnPaths(__spreadProps(__spreadValues({}, commonPathOpts), {
            xDivision,
            barWidth,
            zeroH
          }));
          barHeight = this.series[i2][j] / this.yRatio[translationsIndex];
        }
        const barGoalLine = this.barHelpers.drawGoalLine({
          barXPosition: paths.barXPosition,
          barYPosition: paths.barYPosition,
          goalX: paths.goalX,
          goalY: paths.goalY,
          barHeight,
          barWidth
        });
        if (barGoalLine) {
          elGoalsMarkers.add(barGoalLine);
        }
        y = paths.y;
        x = paths.x;
        xArrValues.push(x);
        yArrValues.push(y);
        const pathFill = this.barHelpers.getPathFillColor(
          series,
          i2,
          j,
          realIndex
        );
        let classes = "";
        const flipClass = w2.globals.isBarHorizontal ? "apexcharts-flip-x" : "apexcharts-flip-y";
        if (this.barHelpers.arrBorderRadius[realIndex][j] === "bottom" && w2.seriesData.series[realIndex][j] > 0 || this.barHelpers.arrBorderRadius[realIndex][j] === "top" && w2.seriesData.series[realIndex][j] < 0) {
          classes = flipClass;
        }
        elSeries = this.renderSeries(__spreadProps(__spreadValues({
          realIndex,
          pathFill: pathFill.color
        }, pathFill.useRangeColor ? { lineFill: pathFill.color } : {}), {
          j,
          i: i2,
          columnGroupIndex,
          pathFrom: paths.pathFrom,
          pathTo: paths.pathTo,
          strokeWidth,
          elSeries,
          x,
          y,
          series,
          barHeight,
          barWidth,
          elDataLabelsWrap,
          elGoalsMarkers,
          type: "bar",
          visibleSeries: columnGroupIndex,
          classes
        }));
      }
      w2.globals.seriesXvalues[realIndex] = xArrValues;
      w2.globals.seriesYvalues[realIndex] = yArrValues;
      this.groupCtx.prevY.push(this.groupCtx.yArrj);
      this.groupCtx.prevYF.push(this.groupCtx.yArrjF);
      this.groupCtx.prevYVal.push(this.groupCtx.yArrjVal);
      this.groupCtx.prevX.push(this.groupCtx.xArrj);
      this.groupCtx.prevXF.push(this.groupCtx.xArrjF);
      this.groupCtx.prevXVal.push(this.groupCtx.xArrjVal);
      ret.add(elSeries);
    }
    return ret;
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {number | undefined} xDivision
   * @param {number | undefined} yDivision
   * @param {number | undefined} zeroH
   * @param {number | undefined} zeroW
   * @param {number} translationsIndex
   */
  initialPositions(x, y, xDivision, yDivision, zeroH, zeroW, translationsIndex) {
    const w2 = this.w;
    let barHeight, barWidth;
    if (this.isHorizontal) {
      yDivision = w2.layout.gridHeight / w2.globals.dataPoints;
      const userBarHeight = w2.config.plotOptions.bar.barHeight;
      if (String(userBarHeight).indexOf("%") === -1) {
        barHeight = parseInt(userBarHeight, 10);
      } else {
        barHeight = yDivision * parseInt(userBarHeight, 10) / 100;
      }
      zeroW = w2.globals.padHorizontal + (this.isReversed ? w2.layout.gridWidth - this.baseLineInvertedY : this.baseLineInvertedY);
      y = (yDivision - barHeight) / 2;
    } else {
      xDivision = w2.layout.gridWidth / w2.globals.dataPoints;
      barWidth = xDivision;
      const userColumnWidth = w2.config.plotOptions.bar.columnWidth;
      if (w2.axisFlags.isXNumeric && w2.globals.dataPoints > 1) {
        xDivision = w2.globals.minXDiff / this.xRatio;
        barWidth = xDivision * parseInt(this.barOptions.columnWidth, 10) / 100;
      } else if (String(userColumnWidth).indexOf("%") === -1) {
        barWidth = parseInt(userColumnWidth, 10);
      } else {
        barWidth *= parseInt(userColumnWidth, 10) / 100;
      }
      if (this.isReversed) {
        zeroH = this.baseLineY[translationsIndex];
      } else {
        zeroH = w2.layout.gridHeight - this.baseLineY[translationsIndex];
      }
      x = w2.globals.padHorizontal + (xDivision - barWidth) / 2;
    }
    const subDivisions = w2.globals.barGroups.length || 1;
    return {
      x,
      y,
      yDivision,
      xDivision,
      barHeight: (barHeight != null ? barHeight : 0) / subDivisions,
      barWidth: (barWidth != null ? barWidth : 0) / subDivisions,
      zeroH,
      zeroW
    };
  }
  /** @param {{indexes: any, barHeight: any, strokeWidth: any, zeroW: any, x: any, y: any, columnGroupIndex: any, seriesGroup: any, yDivision: any, elSeries: any}} opts */
  drawStackedBarPaths({
    indexes,
    barHeight,
    strokeWidth,
    zeroW,
    x,
    y,
    columnGroupIndex,
    seriesGroup,
    yDivision,
    elSeries
  }) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const barYPosition = y + columnGroupIndex * barHeight;
    let barXPosition;
    const i2 = indexes.i;
    const j = indexes.j;
    const realIndex = indexes.realIndex;
    const translationsIndex = indexes.translationsIndex;
    let prevBarW = 0;
    for (let k = 0; k < this.groupCtx.prevXF.length; k++) {
      prevBarW = prevBarW + this.groupCtx.prevXF[k][j];
    }
    let gsi = i2;
    if (
      /** @type {Record<string,any>} */
      w2.config.series[realIndex].name
    ) {
      gsi = seriesGroup.indexOf(
        /** @type {Record<string,any>} */
        w2.config.series[realIndex].name
      );
    }
    if (gsi > 0) {
      let bXP = zeroW;
      if (this.groupCtx.prevXVal[gsi - 1][j] < 0) {
        bXP = /** @type {any} */
        ((_a = this.series[i2]) == null ? void 0 : _a[j]) >= 0 ? this.groupCtx.prevX[gsi - 1][j] + prevBarW - (this.isReversed ? prevBarW : 0) * 2 : this.groupCtx.prevX[gsi - 1][j];
      } else if (this.groupCtx.prevXVal[gsi - 1][j] >= 0) {
        bXP = /** @type {any} */
        ((_b = this.series[i2]) == null ? void 0 : _b[j]) >= 0 ? this.groupCtx.prevX[gsi - 1][j] : this.groupCtx.prevX[gsi - 1][j] - prevBarW + (this.isReversed ? prevBarW : 0) * 2;
      }
      barXPosition = bXP;
    } else {
      barXPosition = zeroW;
    }
    if (
      /** @type {any} */
      ((_c = this.series[i2]) == null ? void 0 : _c[j]) === null
    ) {
      x = barXPosition;
    } else {
      x = barXPosition + /** @type {any} */
      ((_d = this.series[i2]) == null ? void 0 : _d[j]) / this.invertedYRatio - (this.isReversed ? (
        /** @type {any} */
        ((_e = this.series[i2]) == null ? void 0 : _e[j]) / this.invertedYRatio
      ) : 0) * 2;
    }
    const paths = this.barHelpers.getBarpaths({
      barYPosition,
      barHeight,
      x1: barXPosition,
      x2: x,
      strokeWidth,
      isReversed: this.isReversed,
      series: this.series,
      realIndex: indexes.realIndex,
      seriesGroup,
      i: i2,
      j,
      w: w2
    });
    this.barHelpers.barBackground({
      j,
      i: i2,
      y1: barYPosition,
      y2: barHeight,
      elSeries
    });
    y = y + yDivision;
    return {
      pathTo: paths.pathTo,
      pathFrom: paths.pathFrom,
      goalX: this.barHelpers.getGoalValues(
        "x",
        zeroW,
        /** @type {any} */
        null,
        realIndex,
        j,
        translationsIndex
      ),
      barXPosition,
      barYPosition,
      x,
      y
    };
  }
  /** @param {{indexes: any, x: any, y: any, xDivision: any, barWidth: any, zeroH: any, columnGroupIndex: any, seriesGroup: any, elSeries: any}} opts */
  drawStackedColumnPaths({
    indexes,
    x,
    y,
    xDivision,
    barWidth,
    zeroH,
    columnGroupIndex,
    seriesGroup,
    elSeries
  }) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const w2 = this.w;
    const i2 = indexes.i;
    const j = indexes.j;
    const bc = indexes.bc;
    const realIndex = indexes.realIndex;
    const translationsIndex = indexes.translationsIndex;
    if (w2.axisFlags.isXNumeric) {
      let seriesVal = w2.seriesData.seriesX[realIndex][j];
      if (!seriesVal) seriesVal = 0;
      x = (seriesVal - w2.globals.minX) / this.xRatio - barWidth / 2 * w2.globals.barGroups.length;
    }
    const barXPosition = x + columnGroupIndex * barWidth;
    let barYPosition;
    let prevBarH = 0;
    for (let k = 0; k < this.groupCtx.prevYF.length; k++) {
      prevBarH = prevBarH + (!isNaN(this.groupCtx.prevYF[k][j]) ? this.groupCtx.prevYF[k][j] : 0);
    }
    let gsi = i2;
    if (seriesGroup) {
      gsi = seriesGroup.indexOf(w2.seriesData.seriesNames[realIndex]);
    }
    if (gsi > 0 && !w2.axisFlags.isXNumeric || gsi > 0 && w2.axisFlags.isXNumeric && w2.seriesData.seriesX[realIndex - 1][j] === w2.seriesData.seriesX[realIndex][j]) {
      let bYP;
      let prevYValue;
      const p2 = Math.min(this.yRatio.length + 1, realIndex + 1);
      if (this.groupCtx.prevY[gsi - 1] !== void 0 && this.groupCtx.prevY[gsi - 1].length) {
        for (let ii = 1; ii < p2; ii++) {
          if (!isNaN((_a = this.groupCtx.prevY[gsi - ii]) == null ? void 0 : _a[j])) {
            prevYValue = this.groupCtx.prevY[gsi - ii][j];
            break;
          }
        }
      }
      for (let ii = 1; ii < p2; ii++) {
        if (((_b = this.groupCtx.prevYVal[gsi - ii]) == null ? void 0 : _b[j]) < 0) {
          bYP = /** @type {any} */
          ((_c = this.series[i2]) == null ? void 0 : _c[j]) >= 0 ? prevYValue - prevBarH + (this.isReversed ? prevBarH : 0) * 2 : prevYValue;
          break;
        } else if (((_d = this.groupCtx.prevYVal[gsi - ii]) == null ? void 0 : _d[j]) >= 0) {
          bYP = /** @type {any} */
          ((_e = this.series[i2]) == null ? void 0 : _e[j]) >= 0 ? prevYValue : prevYValue + prevBarH - (this.isReversed ? prevBarH : 0) * 2;
          break;
        }
      }
      if (typeof bYP === "undefined") bYP = w2.layout.gridHeight;
      if (
        /**
         * @param {number} val
         */
        ((_f = this.groupCtx.prevYF[0]) == null ? void 0 : _f.every((val) => val === 0)) && this.groupCtx.prevYF.slice(1, gsi).every(
          (arr) => arr.every((val) => isNaN(val))
        )
      ) {
        barYPosition = zeroH;
      } else {
        barYPosition = bYP;
      }
    } else {
      barYPosition = zeroH;
    }
    if (
      /** @type {any} */
      (_g = this.series[i2]) == null ? void 0 : _g[j]
    ) {
      y = barYPosition - /** @type {any} */
      ((_h = this.series[i2]) == null ? void 0 : _h[j]) / this.yRatio[translationsIndex] + (this.isReversed ? (
        /** @type {any} */
        ((_i = this.series[i2]) == null ? void 0 : _i[j]) / this.yRatio[translationsIndex]
      ) : 0) * 2;
    } else {
      y = barYPosition;
    }
    const paths = this.barHelpers.getColumnPaths({
      barXPosition,
      barWidth,
      y1: barYPosition,
      y2: y,
      yRatio: this.yRatio[translationsIndex],
      strokeWidth: this.strokeWidth,
      isReversed: this.isReversed,
      series: this.series,
      seriesGroup,
      realIndex: indexes.realIndex,
      i: i2,
      j,
      w: w2
    });
    this.barHelpers.barBackground({
      bc,
      j,
      i: i2,
      x1: barXPosition,
      x2: barWidth,
      elSeries
    });
    return {
      pathTo: paths.pathTo,
      pathFrom: paths.pathFrom,
      goalY: this.barHelpers.getGoalValues(
        "y",
        /** @type {any} */
        null,
        zeroH,
        realIndex,
        j,
        0
      ),
      barXPosition,
      x: w2.axisFlags.isXNumeric ? x : x + xDivision,
      y
    };
  }
}
function buildJitterGroups({
  w: w2,
  points,
  seedA,
  seedB,
  center,
  halfExtent,
  alongFn,
  isHorizontal,
  options: options2,
  clampAt
}) {
  const opts = options2;
  if (!opts || opts.show === false) return [];
  if (!points || !points.length) return [];
  const maxPoints = opts.maxPoints || 3e3;
  const stride = points.length > maxPoints ? Math.ceil(points.length / maxPoints) : 1;
  const r2 = opts.size != null ? opts.size : 2.5;
  const jitterFrac = opts.jitter != null ? opts.jitter : 0.5;
  const jitterPx = halfExtent * jitterFrac;
  const constrain = opts.constrainToViolin !== false && typeof clampAt === "function";
  const isSquare = opts.shape === "square";
  const scale = opts.colorScale;
  const useScale = scale && Array.isArray(scale.colors) && scale.colors.length > 0;
  const steps = useScale ? Math.max(2, scale.steps || 24) : 1;
  const sMin = useScale && scale.min != null ? scale.min : w2.globals.minY;
  const sMax = useScale && scale.max != null ? scale.max : w2.globals.maxY;
  const span = sMax - sMin || 1;
  const buckets = useScale ? new Array(steps).fill("") : [""];
  for (let k = 0; k < points.length; k += stride) {
    const v2 = points[k];
    const a2 = alongFn(v2);
    let off = (hash01(seedA * 7919 + seedB * 100003 + k) - 0.5) * 2 * jitterPx;
    if (constrain) {
      const cap = (
        /** @type {(v:number)=>number} */
        clampAt(v2)
      );
      if (off > cap) off = cap;
      if (off < -cap) off = -cap;
    }
    const px = isHorizontal ? a2 : center + off;
    const py = isHorizontal ? center + off : a2;
    const sub = isSquare ? squareSubPath(px, py, r2) : circleSubPath(px, py, r2);
    if (useScale) {
      let t2 = (v2 - sMin) / span;
      if (t2 < 0) t2 = 0;
      if (t2 > 1) t2 = 1;
      buckets[Math.round(t2 * (steps - 1))] += sub;
    } else {
      buckets[0] += sub;
    }
  }
  if (!useScale) {
    return buckets[0] ? [{ fill: null, d: buckets[0] }] : [];
  }
  const groups = [];
  for (let b2 = 0; b2 < steps; b2++) {
    if (!buckets[b2]) continue;
    groups.push({
      fill: rampColorAt(scale.colors, b2 / (steps - 1)),
      d: buckets[b2]
    });
  }
  return groups;
}
function renderJitter({
  graphics,
  w: w2,
  elSeries,
  pointsByCat,
  options: options2,
  distributed,
  realIndex,
  wrapClass,
  pointClass
}) {
  if (!options2 || options2.show === false || !pointsByCat.length) return;
  const pOpacity = options2.opacity != null ? options2.opacity : 0.9;
  const strokeColor = options2.strokeColor != null ? options2.strokeColor : "#fff";
  const strokeW = options2.strokeWidth != null ? options2.strokeWidth : 1;
  const willAnimateIn = w2.config.chart.animations.enabled && !w2.globals.resized && !w2.globals.dataChanged;
  const elPointsWrap = graphics.group({
    class: willAnimateIn ? `${wrapClass} apexcharts-element-hidden` : wrapClass
  });
  if (willAnimateIn) {
    w2.globals.delayedElements.push({ el: elPointsWrap.node });
  }
  pointsByCat.forEach(({ groups, j }) => {
    const catColor = distributed ? w2.globals.colors[j] : w2.globals.colors[realIndex];
    const fc = options2.fillColor;
    const defaultFill = fc === "series" ? catColor : fc === "series-dark" ? darkenColor(catColor, 0.45) : fc || darkenColor(catColor, 0.45);
    groups.forEach((g2) => {
      const elPoints = graphics.drawPath({
        d: g2.d,
        fill: g2.fill != null ? g2.fill : defaultFill,
        stroke: strokeW > 0 ? strokeColor : "none",
        strokeWidth: strokeW,
        fillOpacity: pOpacity,
        classes: pointClass
      });
      elPoints.attr("data:realIndex", realIndex);
      elPoints.attr("j", j);
      elPoints.attr("clip-path", `url(#gridRectBarMask${w2.globals.cuid})`);
      elPoints.node.style.pointerEvents = "none";
      elPointsWrap.add(elPoints);
    });
  });
  elSeries.add(elPointsWrap);
}
function darkenColor(color, amount) {
  const rgb = Utils$1.parseHex(color);
  if (!rgb) return color;
  const f = Math.max(0, 1 - amount);
  return `rgb(${Math.round(rgb[0] * f)},${Math.round(rgb[1] * f)},${Math.round(rgb[2] * f)})`;
}
function rampColorAt(colors, t2) {
  if (!colors.length) return "#000";
  if (colors.length === 1) return colors[0];
  const x = Math.max(0, Math.min(1, t2)) * (colors.length - 1);
  const i2 = Math.floor(x);
  const frac = x - i2;
  const c0 = Utils$1.parseHex(colors[i2]) || [0, 0, 0];
  const c1 = Utils$1.parseHex(colors[Math.min(i2 + 1, colors.length - 1)]) || c0;
  const mix = (a2, b2) => Math.round(a2 + (b2 - a2) * frac);
  return `rgb(${mix(c0[0], c1[0])},${mix(c0[1], c1[1])},${mix(c0[2], c1[2])})`;
}
function hash01(n2) {
  let h2 = (n2 ^ 2654435769) >>> 0;
  h2 = Math.imul(h2 ^ h2 >>> 16, 73244475);
  h2 = Math.imul(h2 ^ h2 >>> 16, 73244475);
  return ((h2 ^ h2 >>> 16) >>> 0) / 4294967296;
}
function circleSubPath(px, py, r2) {
  return `M ${px - r2} ${py} a ${r2} ${r2} 0 1 0 ${2 * r2} 0 a ${r2} ${r2} 0 1 0 ${-2 * r2} 0 `;
}
function squareSubPath(px, py, r2) {
  return `M ${px - r2} ${py - r2} h ${2 * r2} v ${2 * r2} h ${-2 * r2} z `;
}
class BoxCandleStick extends Bar {
  /**
   * @param {any[]} series
   * @param {string} ctype
   * @param {number} seriesIndex
   */
  // @ts-ignore -- BoxCandleStick.draw has an extra ctype param compared to Bar.draw
  draw(series, ctype, seriesIndex) {
    var _a;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const type = w2.globals.comboCharts ? ctype : w2.config.chart.type;
    const fill = new Fill(this.w);
    this.candlestickOptions = this.w.config.plotOptions.candlestick;
    this.boxOptions = this.w.config.plotOptions.boxPlot;
    this.isHorizontal = w2.config.plotOptions.bar.horizontal;
    this.isOHLC = this.candlestickOptions && this.candlestickOptions.type === "ohlc";
    this.coreUtils = new CoreUtils(this.w);
    series = this.coreUtils.getLogSeries(series);
    this.series = series;
    this.yRatio = this.coreUtils.getLogYRatios(this.yRatio);
    this.barHelpers.initVariables(series);
    const ret = graphics.group({
      class: `apexcharts-${type}-series apexcharts-plot-series`
    });
    for (let i2 = 0; i2 < series.length; i2++) {
      this.isBoxPlot = w2.config.chart.type === "boxPlot" || /** @type {Record<string,any>} */
      w2.config.series[i2].type === "boxPlot";
      let x;
      let y;
      const yArrj = [];
      const xArrj = [];
      const realIndex = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex[i2]
      ) : i2;
      const { columnGroupIndex } = this.barHelpers.getGroupIndex(realIndex);
      const elSeries = graphics.group({
        class: `apexcharts-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex]),
        rel: i2 + 1,
        "data:realIndex": realIndex
      });
      Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
      if (series[i2].length > 0) {
        this.visibleI = this.visibleI + 1;
      }
      let translationsIndex = 0;
      if (this.yRatio.length > 1) {
        this.yaxisIndex = /** @type {any} */
        w2.globals.seriesYAxisReverseMap[realIndex][0];
        translationsIndex = realIndex;
      }
      const initPositions = this.barHelpers.initialPositions(realIndex);
      const {
        y: initY,
        barHeight,
        yDivision,
        // yDivision is the GRIDHEIGHT divided by number of datapoints (bars)
        zeroW,
        // zeroW is the baseline where 0 meets x axis
        x: initX,
        barWidth,
        xDivision,
        // xDivision is the GRIDWIDTH divided by number of datapoints (columns)
        zeroH
        // zeroH is the baseline where 0 meets y axis
      } = initPositions;
      y = initY;
      x = initX;
      xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
      const elDataLabelsWrap = graphics.group({
        class: "apexcharts-datalabels",
        "data:realIndex": realIndex
      });
      const elGoalsMarkers = graphics.group({
        class: "apexcharts-bar-goals-markers"
      });
      const boxPointsOpts = this.isBoxPlot ? this.boxOptions.points : null;
      const pointsByCat = [];
      const gridW = w2.layout.gridWidth;
      const cullBuffer = barWidth != null ? barWidth : 0;
      for (let j = 0; j < w2.globals.dataPoints; j++) {
        const strokeWidth = this.barHelpers.getStrokeWidth(i2, j, realIndex);
        let paths = (
          /** @type {any} */
          null
        );
        const pathsParams = {
          indexes: {
            i: i2,
            j,
            realIndex,
            translationsIndex
          },
          x,
          y,
          strokeWidth,
          elSeries
        };
        if (this.isHorizontal) {
          paths = this.drawHorizontalBoxPaths(__spreadProps(__spreadValues({}, pathsParams), {
            yDivision,
            barHeight,
            zeroW
          }));
        } else {
          paths = this.drawVerticalBoxPaths(__spreadProps(__spreadValues({}, pathsParams), {
            xDivision,
            barWidth,
            zeroH,
            cullBounds: { lo: -cullBuffer, hi: gridW + cullBuffer }
          }));
        }
        y = paths.y;
        x = paths.x;
        if (j > 0) {
          xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
        }
        yArrj.push(y);
        if (paths.culled) {
          continue;
        }
        const barGoalLine = this.barHelpers.drawGoalLine({
          barXPosition: paths.barXPosition,
          barYPosition: paths.barYPosition,
          goalX: paths.goalX,
          goalY: paths.goalY,
          barHeight,
          barWidth
        });
        if (barGoalLine) {
          elGoalsMarkers.add(barGoalLine);
        }
        paths.pathTo.forEach(
          (pathTo, pi) => {
            const lineFill = !this.isBoxPlot && this.candlestickOptions.wick.useFillColor ? paths.color[pi] : w2.globals.stroke.colors[i2];
            const pathFill = fill.fillPath({
              seriesNumber: realIndex,
              dataPointIndex: j,
              color: paths.color[pi],
              value: series[i2][j]
            });
            this.renderSeries({
              realIndex,
              pathFill,
              lineFill,
              j,
              i: i2,
              pathFrom: paths.pathFrom,
              pathTo,
              strokeWidth,
              elSeries,
              x,
              y,
              series,
              columnGroupIndex,
              barHeight,
              barWidth,
              elDataLabelsWrap,
              elGoalsMarkers,
              visibleSeries: this.visibleI,
              type: w2.config.chart.type
            });
          }
        );
        if (boxPointsOpts && boxPointsOpts.show !== false) {
          const pts = (_a = w2.candleData.seriesBoxPoints[realIndex]) == null ? void 0 : _a[j];
          if (pts && pts.length) {
            const logVal = (v2) => (
              /** @type {any} */
              this.coreUtils.getLogValAtSeriesIndex(
                v2,
                realIndex
              )
            );
            let center, halfExtent, alongFn;
            if (this.isHorizontal) {
              const yRatio = this.invertedYRatio;
              const bh = barHeight != null ? barHeight : 0;
              const z = zeroW != null ? zeroW : 0;
              center = paths.barYPosition + bh / 2;
              halfExtent = bh / 2;
              alongFn = (v2) => z + logVal(v2) / yRatio;
            } else {
              const yRatio = this.yRatio[translationsIndex];
              const bw = barWidth != null ? barWidth : 0;
              const z = zeroH != null ? zeroH : 0;
              center = paths.barXPosition + bw / 2;
              halfExtent = bw / 2;
              alongFn = (v2) => z - logVal(v2) / yRatio;
            }
            const groups = buildJitterGroups({
              w: w2,
              points: pts,
              seedA: realIndex,
              seedB: j,
              center,
              halfExtent,
              alongFn,
              isHorizontal: this.isHorizontal,
              options: boxPointsOpts
            });
            if (groups.length) pointsByCat.push({ groups, j });
          }
        }
      }
      if (boxPointsOpts) {
        renderJitter({
          graphics,
          w: w2,
          elSeries,
          pointsByCat,
          options: boxPointsOpts,
          distributed: w2.config.plotOptions.bar.distributed,
          realIndex,
          wrapClass: "apexcharts-boxPlot-points-wrap",
          pointClass: "apexcharts-boxPlot-points"
        });
      }
      w2.globals.seriesXvalues[realIndex] = xArrj;
      w2.globals.seriesYvalues[realIndex] = yArrj;
      ret.add(elSeries);
    }
    return ret;
  }
  /** @param {{indexes: any, x: any, xDivision: any, barWidth: any, zeroH: any, strokeWidth: any, cullBounds?: {lo: number, hi: number}|null}} opts */
  drawVerticalBoxPaths({
    indexes,
    x,
    xDivision,
    barWidth,
    zeroH,
    strokeWidth,
    cullBounds = null
  }) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const i2 = indexes.i;
    const j = indexes.j;
    const { colors: candleColors } = w2.config.plotOptions.candlestick;
    const { colors: boxColors } = this.boxOptions;
    const realIndex = indexes.realIndex;
    const getColor = (color2) => Array.isArray(color2) ? color2[realIndex] : color2;
    const colorPos = getColor(candleColors.upward);
    const colorNeg = getColor(candleColors.downward);
    const yRatio = this.yRatio[indexes.translationsIndex];
    const ohlc = this.getOHLCValue(realIndex, j);
    let l1 = zeroH;
    let l2 = zeroH;
    let color = ohlc.o < ohlc.c ? [colorPos] : [colorNeg];
    if (this.isBoxPlot) {
      color = [getColor(boxColors.lower), getColor(boxColors.upper)];
    }
    let y1 = Math.min(ohlc.o, ohlc.c);
    let y2 = Math.max(ohlc.o, ohlc.c);
    let m2 = ohlc.m;
    if (w2.axisFlags.isXNumeric) {
      x = (w2.seriesData.seriesX[realIndex][j] - w2.globals.minX) / this.xRatio - barWidth / 2;
    }
    const barXPosition = x + barWidth * this.visibleI;
    if (typeof /** @type {any} */
    ((_a = this.series[i2]) == null ? void 0 : _a[j]) === "undefined" || /** @type {any} */
    ((_b = this.series[i2]) == null ? void 0 : _b[j]) === null) {
      y1 = zeroH;
      y2 = zeroH;
    } else {
      y1 = zeroH - y1 / yRatio;
      y2 = zeroH - y2 / yRatio;
      l1 = zeroH - ohlc.h / yRatio;
      l2 = zeroH - ohlc.l / yRatio;
      m2 = zeroH - ohlc.m / yRatio;
    }
    if (cullBounds && (barXPosition + barWidth < cullBounds.lo || barXPosition > cullBounds.hi)) {
      return {
        pathTo: null,
        pathFrom: null,
        x: w2.axisFlags.isXNumeric ? x : x + xDivision,
        y: y2,
        barXPosition,
        color,
        culled: true
      };
    }
    let pathTo;
    if (this.isOHLC) {
      const centerX = barXPosition + barWidth / 2;
      const openY = zeroH - ohlc.o / yRatio;
      const closeY = zeroH - ohlc.c / yRatio;
      pathTo = [
        graphics.move(centerX, l1) + graphics.line(centerX, l2) + graphics.move(centerX, openY) + graphics.line(barXPosition, openY) + graphics.move(centerX, closeY) + graphics.line(barXPosition + barWidth, closeY)
      ];
    } else if (this.isBoxPlot) {
      pathTo = [
        graphics.move(barXPosition, y1) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition + barWidth / 2, l1) + graphics.line(barXPosition + barWidth / 4, l1) + graphics.line(barXPosition + barWidth - barWidth / 4, l1) + graphics.line(barXPosition + barWidth / 2, l1) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition + barWidth, y1) + graphics.line(barXPosition + barWidth, m2) + graphics.line(barXPosition, m2) + graphics.line(barXPosition, y1 + strokeWidth / 2),
        graphics.move(barXPosition, m2) + graphics.line(barXPosition + barWidth, m2) + graphics.line(barXPosition + barWidth, y2) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition + barWidth / 2, l2) + graphics.line(barXPosition + barWidth - barWidth / 4, l2) + graphics.line(barXPosition + barWidth / 4, l2) + graphics.line(barXPosition + barWidth / 2, l2) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition, y2) + graphics.line(barXPosition, m2) + "z"
      ];
    } else {
      pathTo = [
        graphics.move(barXPosition, y2) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition + barWidth / 2, l1) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition + barWidth, y2) + graphics.line(barXPosition + barWidth, y1) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition + barWidth / 2, l2) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition, y1) + graphics.line(barXPosition, y2 - strokeWidth / 2)
      ];
    }
    let pathFrom = null;
    const morphFrom = (_d = (_c = this.ctx) == null ? void 0 : _c.morphTypeChange) == null ? void 0 : _d.getInitialPathFor(realIndex, j);
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.getPreviousPath(realIndex, j, pathTo[0]);
    }
    if (pathFrom == null) {
      pathFrom = graphics.move(barXPosition + barWidth / 2, y1) + graphics.move(barXPosition, y1);
    }
    if (!w2.axisFlags.isXNumeric) {
      x = x + xDivision;
    }
    return {
      pathTo,
      pathFrom,
      x,
      y: y2,
      goalY: this.barHelpers.getGoalValues(
        "y",
        /** @type {any} */
        null,
        zeroH,
        realIndex,
        j,
        indexes.translationsIndex
      ),
      barXPosition,
      color
    };
  }
  /** @param {{indexes: any, y: any, yDivision: any, barHeight: any, zeroW: any, strokeWidth: any}} opts */
  drawHorizontalBoxPaths({
    indexes,
    y,
    yDivision,
    barHeight,
    zeroW,
    strokeWidth
  }) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const i2 = indexes.i;
    const j = indexes.j;
    const realIndex = indexes.realIndex;
    const { colors: candleColors } = w2.config.plotOptions.candlestick;
    const { colors: boxColors } = this.boxOptions;
    const getColor = (color2) => Array.isArray(color2) ? color2[realIndex] : color2;
    const yRatio = this.invertedYRatio;
    const ohlc = this.getOHLCValue(realIndex, j);
    let color = ohlc.o < ohlc.c ? [getColor(candleColors.upward)] : [getColor(candleColors.downward)];
    if (this.isBoxPlot) {
      color = [getColor(boxColors.lower), getColor(boxColors.upper)];
    }
    let l1 = zeroW;
    let l2 = zeroW;
    let x1 = Math.min(ohlc.o, ohlc.c);
    let x2 = Math.max(ohlc.o, ohlc.c);
    let m2 = ohlc.m;
    if (w2.axisFlags.isXNumeric) {
      y = (w2.seriesData.seriesX[realIndex][j] - w2.globals.minX) / this.invertedXRatio - barHeight / 2;
    }
    const barYPosition = y + barHeight * this.visibleI;
    if (typeof /** @type {any} */
    ((_a = this.series[i2]) == null ? void 0 : _a[j]) === "undefined" || /** @type {any} */
    ((_b = this.series[i2]) == null ? void 0 : _b[j]) === null) {
      x1 = zeroW;
      x2 = zeroW;
    } else {
      x1 = zeroW + x1 / yRatio;
      x2 = zeroW + x2 / yRatio;
      l1 = zeroW + ohlc.h / yRatio;
      l2 = zeroW + ohlc.l / yRatio;
      m2 = zeroW + ohlc.m / yRatio;
    }
    const pathTo = [
      graphics.move(x1, barYPosition) + graphics.line(x1, barYPosition + barHeight / 2) + graphics.line(l1, barYPosition + barHeight / 2) + graphics.line(l1, barYPosition + barHeight / 2 - barHeight / 4) + graphics.line(l1, barYPosition + barHeight / 2 + barHeight / 4) + graphics.line(l1, barYPosition + barHeight / 2) + graphics.line(x1, barYPosition + barHeight / 2) + graphics.line(x1, barYPosition + barHeight) + graphics.line(m2, barYPosition + barHeight) + graphics.line(m2, barYPosition) + graphics.line(x1 + strokeWidth / 2, barYPosition),
      graphics.move(m2, barYPosition) + graphics.line(m2, barYPosition + barHeight) + graphics.line(x2, barYPosition + barHeight) + graphics.line(x2, barYPosition + barHeight / 2) + graphics.line(l2, barYPosition + barHeight / 2) + graphics.line(l2, barYPosition + barHeight - barHeight / 4) + graphics.line(l2, barYPosition + barHeight / 4) + graphics.line(l2, barYPosition + barHeight / 2) + graphics.line(x2, barYPosition + barHeight / 2) + graphics.line(x2, barYPosition) + graphics.line(m2, barYPosition) + "z"
    ];
    let pathFrom = null;
    const morphFrom = (_d = (_c = this.ctx) == null ? void 0 : _c.morphTypeChange) == null ? void 0 : _d.getInitialPathFor(realIndex, j);
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.getPreviousPath(realIndex, j, pathTo[0]);
    }
    if (pathFrom == null) {
      pathFrom = graphics.move(x1, barYPosition + barHeight / 2) + graphics.move(x1, barYPosition);
    }
    if (!w2.axisFlags.isXNumeric) {
      y = y + yDivision;
    }
    return {
      pathTo,
      pathFrom,
      x: x2,
      y,
      goalX: this.barHelpers.getGoalValues(
        "x",
        zeroW,
        /** @type {any} */
        null,
        realIndex,
        j,
        0
      ),
      barYPosition,
      color
    };
  }
  /**
   * @param {number} i
   * @param {number} j
   */
  getOHLCValue(i2, j) {
    const w2 = this.w;
    const coreUtils = this.coreUtils;
    const getCandleVal = (arr) => arr[i2] && arr[i2][j] != null ? (
      /** @type {any} */
      coreUtils.getLogValAtSeriesIndex(arr[i2][j], i2)
    ) : 0;
    const h2 = getCandleVal(w2.candleData.seriesCandleH);
    const o2 = getCandleVal(w2.candleData.seriesCandleO);
    const m2 = getCandleVal(w2.candleData.seriesCandleM);
    const c2 = getCandleVal(w2.candleData.seriesCandleC);
    const l2 = getCandleVal(w2.candleData.seriesCandleL);
    return {
      o: this.isBoxPlot ? h2 : o2,
      h: this.isBoxPlot ? o2 : h2,
      m: m2,
      l: this.isBoxPlot ? c2 : l2,
      c: this.isBoxPlot ? l2 : c2
    };
  }
}
const tangents = (points) => {
  const m2 = finiteDifferences(points);
  const n2 = points.length - 1;
  const ε = 1e-6;
  const tgts = [];
  let a2, b2, d, s2;
  for (let i2 = 0; i2 < n2; i2++) {
    d = slope(points[i2], points[i2 + 1]);
    if (Math.abs(d) < ε) {
      m2[i2] = m2[i2 + 1] = 0;
    } else {
      a2 = m2[i2] / d;
      b2 = m2[i2 + 1] / d;
      s2 = a2 * a2 + b2 * b2;
      if (s2 > 9) {
        s2 = d * 3 / Math.sqrt(s2);
        m2[i2] = s2 * a2;
        m2[i2 + 1] = s2 * b2;
      }
    }
  }
  for (let i2 = 0; i2 <= n2; i2++) {
    s2 = (points[Math.min(n2, i2 + 1)][0] - points[Math.max(0, i2 - 1)][0]) / (6 * (1 + m2[i2] * m2[i2]));
    tgts.push([s2 || 0, m2[i2] * s2 || 0]);
  }
  return tgts;
};
const svgPath = (points) => {
  let p2 = "";
  for (let i2 = 0; i2 < points.length; i2++) {
    const point = points[i2];
    const n2 = point.length;
    if (n2 > 4) {
      p2 += `C${point[0]}, ${point[1]}`;
      p2 += `, ${point[2]}, ${point[3]}`;
      p2 += `, ${point[4]}, ${point[5]}`;
    } else if (n2 > 2) {
      p2 += `S${point[0]}, ${point[1]}`;
      p2 += `, ${point[2]}, ${point[3]}`;
    }
  }
  return p2;
};
const spline = {
  /**
   * Convert 'points' to bezier
   * @param {any[]} points
   * @returns {any[]}
   */
  points(points) {
    const tgts = tangents(points);
    const p2 = points[1];
    const p0 = points[0];
    const pts = [];
    const t2 = tgts[1];
    const t0 = tgts[0];
    pts.push(p0, [
      p0[0] + t0[0],
      p0[1] + t0[1],
      p2[0] - t2[0],
      p2[1] - t2[1],
      p2[0],
      p2[1]
    ]);
    for (let i2 = 2, n2 = tgts.length; i2 < n2; i2++) {
      const p3 = points[i2];
      const t3 = tgts[i2];
      pts.push([p3[0] - t3[0], p3[1] - t3[1], p3[0], p3[1]]);
    }
    return pts;
  },
  /**
   * Slice out a segment of 'points'
   * @param {any[]} points
   * @param {Number} start
   * @param {Number} end
   * @returns {any[]}
   */
  slice(points, start, end) {
    const pts = points.slice(start, end);
    if (start) {
      if (end - start > 1 && pts[1].length < 6) {
        const n2 = pts[0].length;
        pts[1] = [
          pts[0][n2 - 2] * 2 - pts[0][n2 - 4],
          pts[0][n2 - 1] * 2 - pts[0][n2 - 3]
        ].concat(pts[1]);
      }
      pts[0] = pts[0].slice(-2);
    }
    return pts;
  }
};
function slope(p0, p1) {
  return (p1[1] - p0[1]) / (p1[0] - p0[0]);
}
function finiteDifferences(points) {
  const m2 = [];
  let p0 = points[0];
  let p1 = points[1];
  let d = m2[0] = slope(p0, p1);
  let i2 = 1;
  for (let n2 = points.length - 1; i2 < n2; i2++) {
    p0 = p1;
    p1 = points[i2 + 1];
    m2[i2] = (d + (d = slope(p0, p1))) * 0.5;
  }
  m2[i2] = d;
  return m2;
}
class Violin extends Bar {
  /**
   * @param {any[]} series
   * @param {string} ctype
   * @param {number} [seriesIndex]
   */
  // @ts-ignore -- Violin.draw has extra ctype param compared to Bar.draw
  draw(series, ctype, seriesIndex) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const fill = new Fill(this.w);
    this.violinOptions = w2.config.plotOptions.violin;
    this.pointsOptions = this.violinOptions.points;
    this.bandwidthScale = this.violinOptions.bandwidthScale || 1;
    this.normalize = this.violinOptions.normalize || "individual";
    this.distributed = w2.config.plotOptions.bar.distributed;
    this.isHorizontal = w2.config.plotOptions.bar.horizontal;
    this.coreUtils = new CoreUtils(this.w);
    series = this.coreUtils.getLogSeries(series);
    this.series = series;
    this.yRatio = this.coreUtils.getLogYRatios(this.yRatio);
    this.barHelpers.initVariables(series);
    const ret = graphics.group({
      class: "apexcharts-violin-series apexcharts-plot-series"
    });
    for (let i2 = 0; i2 < series.length; i2++) {
      let x;
      let y;
      const yArrj = [];
      const xArrj = [];
      const realIndex = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex[i2]
      ) : i2;
      const { columnGroupIndex } = this.barHelpers.getGroupIndex(realIndex);
      const elSeries = graphics.group({
        class: "apexcharts-series",
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex]),
        rel: i2 + 1,
        "data:realIndex": realIndex
      });
      Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
      if (series[i2].length > 0) {
        this.visibleI = this.visibleI + 1;
      }
      let translationsIndex = 0;
      if (this.yRatio.length > 1) {
        this.yaxisIndex = /** @type {any} */
        w2.globals.seriesYAxisReverseMap[realIndex][0];
        translationsIndex = realIndex;
      }
      const initPositions = this.barHelpers.initialPositions(realIndex);
      const {
        y: initY,
        barHeight,
        yDivision,
        zeroW,
        x: initX,
        barWidth,
        xDivision,
        zeroH
      } = initPositions;
      y = initY;
      x = initX;
      xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
      const elDataLabelsWrap = graphics.group({
        class: "apexcharts-datalabels",
        "data:realIndex": realIndex
      });
      this.seriesMaxWeight = 0;
      if (this.normalize === "group") {
        const dens = w2.violinData.seriesViolinDensity[realIndex] || [];
        dens.forEach((d) => {
          if (d && d.maxWeight > this.seriesMaxWeight) {
            this.seriesMaxWeight = d.maxWeight;
          }
        });
      }
      const pointsByViolin = [];
      for (let j = 0; j < w2.globals.dataPoints; j++) {
        const strokeWidth = this.barHelpers.getStrokeWidth(i2, j, realIndex);
        const paths = this.isHorizontal ? this.drawHorizontalViolin({
          indexes: { i: i2, j, realIndex, translationsIndex },
          y,
          yDivision,
          barHeight,
          zeroW
        }) : this.drawVerticalViolin({
          indexes: { i: i2, j, realIndex, translationsIndex },
          x,
          xDivision,
          barWidth,
          zeroH
        });
        x = paths.x;
        y = paths.y;
        if (j > 0) {
          xArrj.push(paths.center);
        }
        yArrj.push(paths.alongRepresentative);
        const pointGroups = this.buildPointsSubPath({
          realIndex,
          j,
          center: paths.center,
          halfExtent: paths.halfExtent,
          alongFn: paths.alongFn,
          density: paths.density,
          maxWeight: paths.maxWeight
        });
        if (pointGroups.length) pointsByViolin.push({ groups: pointGroups, j });
        const pathFill = fill.fillPath({
          // distributed → color per category (data point) instead of per series
          seriesNumber: this.distributed ? j : realIndex,
          dataPointIndex: j,
          color: this.distributed ? w2.globals.colors[j] : void 0,
          value: series[i2][j]
        });
        this.renderSeries({
          realIndex,
          pathFill,
          lineFill: w2.globals.stroke.colors[realIndex],
          j,
          i: i2,
          pathFrom: paths.pathFrom,
          pathTo: paths.pathTo,
          strokeWidth,
          elSeries,
          x,
          y,
          series,
          columnGroupIndex,
          barHeight,
          barWidth,
          elDataLabelsWrap,
          visibleSeries: this.visibleI,
          type: "violin"
        });
        const bodyEl = elSeries.node.querySelector(
          `path.apexcharts-violin-area[j='${j}']`
        );
        if (bodyEl && isFinite(paths.alongRepresentative)) {
          bodyEl.setAttribute(
            this.isHorizontal ? "cx" : "cy",
            `${paths.alongRepresentative}`
          );
        }
      }
      renderJitter({
        graphics,
        w: w2,
        elSeries,
        pointsByCat: pointsByViolin,
        options: this.pointsOptions,
        distributed: this.distributed,
        realIndex,
        wrapClass: "apexcharts-violin-points-wrap",
        pointClass: "apexcharts-violin-points"
      });
      w2.globals.seriesXvalues[realIndex] = xArrj;
      w2.globals.seriesYvalues[realIndex] = yArrj;
      ret.add(elSeries);
    }
    return ret;
  }
  /** @param {{indexes: any, x: any, xDivision: any, barWidth: any, zeroH: any}} opts */
  drawVerticalViolin({ indexes, x, xDivision, barWidth, zeroH }) {
    var _a, _b, _c;
    const w2 = this.w;
    const { realIndex, j, translationsIndex } = indexes;
    const yRatio = this.yRatio[translationsIndex];
    if (w2.axisFlags.isXNumeric) {
      x = (w2.seriesData.seriesX[realIndex][j] - w2.globals.minX) / this.xRatio - barWidth / 2;
    }
    const barXPosition = x + barWidth * this.visibleI;
    const center = barXPosition + barWidth / 2;
    const halfExtent = barWidth / 2;
    const density = this.getDensity(realIndex, j);
    const maxWeight = this.effectiveMaxWeight(density);
    const alongFn = (v2) => zeroH - this.logVal(v2, realIndex) / yRatio;
    const pathTo = this.buildBodyPath({
      nodes: density.nodes,
      center,
      halfExtent,
      maxWeight,
      vertical: true,
      alongFn,
      collapsed: false
    });
    let pathFrom = null;
    const morphFrom = (_b = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.getInitialPathFor(realIndex, j);
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.getPreviousPath(realIndex, j, pathTo);
    }
    if (pathFrom == null) {
      pathFrom = this.buildBodyPath({
        nodes: density.nodes,
        center,
        halfExtent,
        maxWeight,
        vertical: true,
        alongFn,
        collapsed: true
      });
    }
    if (!w2.axisFlags.isXNumeric) {
      x = x + xDivision;
    }
    return {
      pathTo,
      pathFrom,
      x,
      y: zeroH,
      center,
      halfExtent,
      alongFn,
      density,
      maxWeight,
      alongRepresentative: alongFn((_c = this.series[indexes.i][j]) != null ? _c : 0)
    };
  }
  /** @param {{indexes: any, y: any, yDivision: any, barHeight: any, zeroW: any}} opts */
  drawHorizontalViolin({ indexes, y, yDivision, barHeight, zeroW }) {
    var _a, _b, _c;
    const w2 = this.w;
    const { realIndex, j } = indexes;
    const yRatio = this.invertedYRatio;
    if (w2.axisFlags.isXNumeric) {
      y = (w2.seriesData.seriesX[realIndex][j] - w2.globals.minX) / this.invertedXRatio - barHeight / 2;
    }
    const barYPosition = y + barHeight * this.visibleI;
    const center = barYPosition + barHeight / 2;
    const halfExtent = barHeight / 2;
    const density = this.getDensity(realIndex, j);
    const maxWeight = this.effectiveMaxWeight(density);
    const alongFn = (v2) => zeroW + this.logVal(v2, realIndex) / yRatio;
    const pathTo = this.buildBodyPath({
      nodes: density.nodes,
      center,
      halfExtent,
      maxWeight,
      vertical: false,
      alongFn,
      collapsed: false
    });
    let pathFrom = null;
    const morphFrom = (_b = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.getInitialPathFor(realIndex, j);
    if (morphFrom) {
      pathFrom = morphFrom;
    } else if (w2.globals.previousPaths.length > 0) {
      pathFrom = this.getPreviousPath(realIndex, j, pathTo);
    }
    if (pathFrom == null) {
      pathFrom = this.buildBodyPath({
        nodes: density.nodes,
        center,
        halfExtent,
        maxWeight,
        vertical: false,
        alongFn,
        collapsed: true
      });
    }
    if (!w2.axisFlags.isXNumeric) {
      y = y + yDivision;
    }
    return {
      pathTo,
      pathFrom,
      x: zeroW,
      y,
      center,
      halfExtent,
      alongFn,
      maxWeight,
      density,
      alongRepresentative: alongFn((_c = this.series[indexes.i][j]) != null ? _c : 0)
    };
  }
  /**
   * Read the parsed density for one violin and return sorted, de-duplicated
   * nodes (strictly increasing value — a hard requirement for the spline).
   * @param {number} realIndex
   * @param {number} j
   */
  getDensity(realIndex, j) {
    var _a;
    const w2 = this.w;
    const d = (_a = w2.violinData.seriesViolinDensity[realIndex]) == null ? void 0 : _a[j];
    if (!d || !d.values.length) {
      return { nodes: [], maxWeight: 0 };
    }
    const order = d.values.map(
      (_, k) => k
    );
    order.sort(
      (a2, b2) => d.values[a2] - d.values[b2]
    );
    const nodes = [];
    let prevV = null;
    for (const k of order) {
      const v2 = d.values[k];
      if (prevV !== null && v2 === prevV) continue;
      nodes.push({ v: v2, w: d.weights[k] });
      prevV = v2;
    }
    return { nodes, maxWeight: d.maxWeight };
  }
  /**
   * The peak weight used to scale a violin's width: its own ('individual') or
   * the densest violin in the series ('group', preserving relative widths).
   * @param {{maxWeight:number}} density
   */
  effectiveMaxWeight(density) {
    return this.normalize === "group" && this.seriesMaxWeight > 0 ? this.seriesMaxWeight : density.maxWeight;
  }
  /**
   * Build the closed, smooth violin outline. The value axis is the monotonic
   * parameter for the spline (vertical → Y, horizontal → X); the spline is fed
   * with that axis first and the control points swapped back to screen space.
   *
   * @param {{nodes:{v:number,w:number}[], center:number, halfExtent:number, maxWeight:number, vertical:boolean, alongFn:(v:number)=>number, collapsed:boolean}} opts
   */
  buildBodyPath({
    nodes,
    center,
    halfExtent,
    maxWeight,
    vertical,
    alongFn,
    collapsed
  }) {
    const graphics = new Graphics(this.w);
    if (nodes.length === 0) {
      const a2 = alongFn(0);
      return vertical ? graphics.move(center, a2) + graphics.line(center, a2) : graphics.move(a2, center) + graphics.line(a2, center);
    }
    const wpxOf = (weight) => {
      if (collapsed || maxWeight <= 0) return 0;
      const wp = weight / maxWeight * halfExtent * this.bandwidthScale;
      return Math.min(halfExtent, Math.max(0, wp));
    };
    const rightPts = [];
    const leftPts = [];
    for (let k = 0; k < nodes.length; k++) {
      const a2 = alongFn(nodes[k].v);
      const wp = wpxOf(nodes[k].w);
      if (vertical) {
        rightPts.push([center + wp, a2]);
        leftPts.push([center - wp, a2]);
      } else {
        rightPts.push([a2, center + wp]);
        leftPts.push([a2, center - wp]);
      }
    }
    leftPts.reverse();
    return this.smoothSegment(rightPts, vertical, false) + this.smoothSegment(leftPts, vertical, true) + "z";
  }
  /**
   * Emit one edge as a smooth (monotone-cubic) path segment, or a polyline
   * when there are too few nodes for a spline.
   *
   * @param {[number,number][]} screenPts ordered screen points for this edge
   * @param {boolean} monotonicIsY true when the value axis is vertical
   * @param {boolean} continued false → start with M; true → start with L (joins the previous edge)
   */
  smoothSegment(screenPts, monotonicIsY, continued) {
    const graphics = new Graphics(this.w);
    const first = screenPts[0];
    let d = continued ? graphics.line(first[0], first[1]) : graphics.move(first[0], first[1]);
    const usePolyline = screenPts.length < 3 || !this.strictlyMonotonic(screenPts, monotonicIsY);
    if (usePolyline) {
      for (let k = 1; k < screenPts.length; k++) {
        d += graphics.line(screenPts[k][0], screenPts[k][1]);
      }
      return d;
    }
    const input = screenPts.map(
      ([px, py]) => monotonicIsY ? [py, px] : [px, py]
    );
    const bez = spline.points(input);
    const out = monotonicIsY ? bez.map(swapPairs) : bez;
    d += svgPath(out);
    return d;
  }
  /**
   * @param {[number,number][]} screenPts
   * @param {boolean} monotonicIsY
   */
  strictlyMonotonic(screenPts, monotonicIsY) {
    const axis = monotonicIsY ? 1 : 0;
    for (let k = 1; k < screenPts.length; k++) {
      if (screenPts[k][axis] === screenPts[k - 1][axis]) return false;
    }
    return true;
  }
  /**
   * Build the jitter sub-paths for one violin, grouped for rendering. Returns
   * `[]` when points are hidden or absent. Normally one group (single dot
   * colour); with `points.colorScale` the dots are bucketed by value into shade
   * groups, each carrying its ramp colour. Offsets are a deterministic index
   * hash (SSR-safe); points beyond maxPoints are stride-thinned.
   *
   * @param {{realIndex:number, j:number, center:number, halfExtent:number, alongFn:(v:number)=>number, density:{nodes:{v:number,w:number}[], maxWeight:number}, maxWeight:number}} opts
   * @returns {{fill:string|null, d:string}[]}
   */
  buildPointsSubPath({
    realIndex,
    j,
    center,
    halfExtent,
    alongFn,
    density,
    maxWeight
  }) {
    var _a;
    return buildJitterGroups({
      w: this.w,
      points: (_a = this.w.violinData.seriesViolinPoints[realIndex]) == null ? void 0 : _a[j],
      seedA: realIndex,
      seedB: j,
      center,
      halfExtent,
      alongFn,
      isHorizontal: this.isHorizontal,
      options: this.pointsOptions,
      // Violin clamps jitter to the density half-width at each value so dots
      // stay inside the shape.
      clampAt: (v2) => this.halfWidthAtValue(v2, density, halfExtent, maxWeight)
    });
  }
  /**
   * Density half-width (pixels) at a given value — used to keep jitter inside
   * the violin. Linear interpolation between the two nearest density nodes.
   * @param {number} value
   * @param {{nodes:{v:number,w:number}[], maxWeight:number}} density
   * @param {number} halfExtent
   * @param {number} [maxWeightOverride] use the group max for 'group' normalize
   */
  halfWidthAtValue(value, density, halfExtent, maxWeightOverride) {
    const { nodes } = density;
    const maxWeight = maxWeightOverride != null ? maxWeightOverride : density.maxWeight;
    if (!nodes.length || maxWeight <= 0) return 0;
    const toPx = (weight) => Math.min(
      halfExtent,
      weight / maxWeight * halfExtent * this.bandwidthScale
    );
    if (value <= nodes[0].v) return toPx(nodes[0].w);
    if (value >= nodes[nodes.length - 1].v)
      return toPx(nodes[nodes.length - 1].w);
    for (let k = 1; k < nodes.length; k++) {
      if (value <= nodes[k].v) {
        const a2 = nodes[k - 1];
        const b2 = nodes[k];
        const t2 = b2.v === a2.v ? 0 : (value - a2.v) / (b2.v - a2.v);
        return toPx(a2.w + (b2.w - a2.w) * t2);
      }
    }
    return 0;
  }
  /**
   * Apply the y-axis log transform to a value when that axis is logarithmic,
   * mirroring BoxCandleStick. Linear axes return the value unchanged.
   * @param {number} value
   * @param {number} realIndex
   */
  logVal(value, realIndex) {
    return (
      /** @type {any} */
      this.coreUtils.getLogValAtSeriesIndex(
        value,
        realIndex
      )
    );
  }
}
function swapPairs(arr) {
  const out = [];
  for (let k = 0; k < arr.length; k += 2) {
    out.push(arr[k + 1], arr[k]);
  }
  return out;
}
class TreemapHelpers {
  /**
   * @param {import('../../../types/internal').ChartStateW} w
   * @param {import('../../../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
  }
  checkColorRange() {
    const w2 = this.w;
    let negRange = false;
    const chartOpts = w2.config.plotOptions[w2.config.chart.type];
    if (chartOpts.colorScale.ranges.length > 0) {
      chartOpts.colorScale.ranges.map((range) => {
        if (range.from <= 0) {
          negRange = true;
        }
      });
    }
    return negRange;
  }
  /**
   * @param {string} chartType
   * @param {number} i
   * @param {number} j
   * @param {any} negRange
   */
  getShadeColor(chartType, i2, j, negRange) {
    const w2 = this.w;
    let colorShadePercent = 1;
    const shadeIntensity = w2.config.plotOptions[chartType].shadeIntensity;
    const colorProps = this.determineColor(chartType, i2, j);
    if (
      /** @type {any} */
      w2.globals.hasNegs || negRange
    ) {
      if (w2.config.plotOptions[chartType].reverseNegativeShade) {
        if (colorProps.percent < 0) {
          colorShadePercent = colorProps.percent / 100 * (shadeIntensity * 1.25);
        } else {
          colorShadePercent = (1 - colorProps.percent / 100) * (shadeIntensity * 1.25);
        }
      } else {
        if (colorProps.percent <= 0) {
          colorShadePercent = 1 - (1 + colorProps.percent / 100) * shadeIntensity;
        } else {
          colorShadePercent = (1 - colorProps.percent / 100) * shadeIntensity;
        }
      }
    } else {
      colorShadePercent = 1 - colorProps.percent / 100;
      if (chartType === "treemap") {
        colorShadePercent = (1 - colorProps.percent / 100) * (shadeIntensity * 1.25);
      }
    }
    let color = colorProps.color;
    const utils = new Utils$1();
    if (w2.config.plotOptions[chartType].enableShades) {
      if (this.w.config.theme.mode === "dark") {
        const shadeColor = utils.shadeColor(
          colorShadePercent * -1,
          colorProps.color
        );
        color = Utils$1.hexToRgba(
          Utils$1.isColorHex(shadeColor) ? shadeColor : Utils$1.rgb2hex(shadeColor),
          w2.config.fill.opacity
        );
      } else {
        const shadeColor = utils.shadeColor(colorShadePercent, colorProps.color);
        color = Utils$1.hexToRgba(
          Utils$1.isColorHex(shadeColor) ? shadeColor : Utils$1.rgb2hex(shadeColor),
          w2.config.fill.opacity
        );
      }
    }
    return { color, colorProps };
  }
  /**
   * @param {string} chartType
   * @param {number} i
   * @param {number} j
   */
  determineColor(chartType, i2, j) {
    const w2 = this.w;
    const val = w2.seriesData.series[i2][j];
    const chartOpts = w2.config.plotOptions[chartType];
    let seriesNumber = chartOpts.colorScale.inverse ? j : i2;
    if (chartOpts.distributed && w2.config.chart.type === "treemap") {
      seriesNumber = j;
    }
    let color = w2.globals.colors[seriesNumber];
    let foreColor = null;
    let min;
    let max;
    if (!chartOpts.distributed && chartType === "heatmap") {
      min = w2.globals.minY;
      max = w2.globals.maxY;
    } else {
      const row = w2.seriesData.series[i2];
      min = row.length ? row[0] : 0;
      max = min;
      for (let k = 1; k < row.length; k++) {
        if (row[k] < min) min = row[k];
        if (row[k] > max) max = row[k];
      }
    }
    if (typeof chartOpts.colorScale.min !== "undefined") {
      min = chartOpts.colorScale.min < w2.globals.minY ? chartOpts.colorScale.min : w2.globals.minY;
      max = chartOpts.colorScale.max > w2.globals.maxY ? chartOpts.colorScale.max : w2.globals.maxY;
    }
    const total = Math.abs(max) + Math.abs(min);
    let percent = total === 0 ? 0 : 100 * val / total;
    if (chartOpts.colorScale.ranges.length > 0) {
      const colorRange = chartOpts.colorScale.ranges;
      colorRange.map((range) => {
        if (val >= range.from && val <= range.to) {
          color = range.color;
          foreColor = range.foreColor ? range.foreColor : null;
          min = range.from;
          max = range.to;
          const rTotal = Math.abs(max) + Math.abs(min);
          percent = rTotal === 0 ? 0 : 100 * val / rTotal;
        }
      });
    }
    return {
      color,
      foreColor,
      percent
    };
  }
  /** @param {{ text?: any, x?: any, y?: any, i?: any, j?: any, colorProps?: any, fontSize?: any, series?: any }} opts */
  calculateDataLabels({ text, x, y, i: i2, j, colorProps, fontSize }) {
    const w2 = this.w;
    const dataLabelsConfig = w2.config.dataLabels;
    const graphics = new Graphics(this.w);
    const dataLabels = new DataLabels(this.w, this.ctx);
    let elDataLabelsWrap = null;
    if (dataLabelsConfig.enabled) {
      elDataLabelsWrap = graphics.group({
        class: "apexcharts-data-labels"
      });
      const offX = resolveDataLabelOffset(dataLabelsConfig.offsetX, w2, i2, j);
      const offY = resolveDataLabelOffset(dataLabelsConfig.offsetY, w2, i2, j);
      const dataLabelsX = x + offX;
      const dataLabelsY = y + parseFloat(dataLabelsConfig.style.fontSize) / 3 + offY;
      dataLabels.plotDataLabelsText({
        x: dataLabelsX,
        y: dataLabelsY,
        text,
        i: i2,
        j,
        color: colorProps.foreColor,
        parent: elDataLabelsWrap,
        fontSize,
        dataLabelsConfig
      });
    }
    return elDataLabelsWrap;
  }
}
class HeatMap {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   * @param {import('../types/internal').XYRatios} xyRatios
   */
  constructor(w2, ctx, xyRatios) {
    this.ctx = ctx;
    this.w = w2;
    this.xRatio = xyRatios.xRatio;
    this.yRatio = xyRatios.yRatio;
    this.dynamicAnim = this.w.config.chart.animations.dynamicAnimation;
    this.helpers = new TreemapHelpers(w2, ctx);
    this.rectRadius = this.w.config.plotOptions.heatmap.radius;
    this.strokeWidth = this.w.config.stroke.show ? this.w.config.stroke.width : 0;
  }
  /**
   * @param {any[]} series
   */
  draw(series) {
    var _a, _b;
    const w2 = this.w;
    const graphics = new Graphics(this.w, this.ctx);
    const emit = seriesEmitter(this.ctx, graphics);
    const useCanvas = emit !== graphics && typeof emit.drawRectCell === "function";
    const ret = graphics.group({
      class: "apexcharts-heatmap"
    });
    ret.attr("clip-path", `url(#gridRectMask${w2.globals.cuid})`);
    const xDivision = w2.layout.gridWidth / w2.globals.dataPoints;
    const yDivision = w2.layout.gridHeight / w2.seriesData.series.length;
    const isContinuousX = (w2.config.xaxis.type === "numeric" || w2.config.xaxis.type === "datetime") && w2.axisFlags.isXNumeric && this.xRatio > 0;
    let binPx = xDivision;
    if (isContinuousX) {
      const diff = w2.globals.minXDiff;
      binPx = Number.isFinite(diff) && diff > 0 ? diff / this.xRatio : xDivision;
    }
    const cellFillOpacity = Array.isArray(w2.config.fill.opacity) ? (_a = w2.config.fill.opacity[0]) != null ? _a : 1 : (_b = w2.config.fill.opacity) != null ? _b : 1;
    let y1 = 0;
    let rev = false;
    this.negRange = this.helpers.checkColorRange();
    const heatSeries = series.slice();
    if (w2.config.yaxis[0].reversed) {
      rev = true;
      heatSeries.reverse();
    }
    for (let i2 = rev ? 0 : heatSeries.length - 1; rev ? i2 < heatSeries.length : i2 >= 0; rev ? i2++ : i2--) {
      const elSeries = graphics.group({
        class: `apexcharts-series apexcharts-heatmap-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[i2]),
        rel: i2 + 1,
        "data:realIndex": i2
      });
      Series.addCollapsedClassToSeries(this.w, elSeries, i2);
      if (!useCanvas) {
        graphics.setupEventDelegation(elSeries, ".apexcharts-heatmap-rect");
      }
      if (w2.config.chart.dropShadow.enabled) {
        const shadow = w2.config.chart.dropShadow;
        const filters = new Filters(this.w);
        filters.dropShadow(elSeries, shadow, i2);
      }
      let x1 = 0;
      const shadeIntensity = w2.config.plotOptions.heatmap.shadeIntensity;
      let j = 0;
      for (let dIndex = 0; dIndex < w2.globals.dataPoints; dIndex++) {
        if (!isContinuousX && w2.seriesData.seriesX.length && !w2.globals.allSeriesHasEqualX) {
          if (w2.globals.minX + w2.globals.minXDiff * dIndex < w2.seriesData.seriesX[i2][j]) {
            x1 = x1 + xDivision;
            continue;
          }
        }
        if (j >= heatSeries[i2].length) break;
        const cellW = isContinuousX ? binPx : xDivision;
        if (isContinuousX) {
          const xVal = w2.seriesData.seriesX[i2] ? w2.seriesData.seriesX[i2][j] : null;
          if (xVal == null || xVal !== xVal) {
            j++;
            continue;
          }
          x1 = (xVal - w2.globals.minX) / this.xRatio - binPx / 2;
        }
        const heatColor = this.helpers.getShadeColor(
          w2.config.chart.type,
          i2,
          j,
          this.negRange
        );
        let color = heatColor.color;
        const heatColorProps = heatColor.colorProps;
        if (w2.config.fill.type === "image") {
          const fill = new Fill(this.w);
          color = fill.fillPath({
            seriesNumber: i2,
            dataPointIndex: j,
            opacity: (
              /** @type {any} */
              w2.globals.hasNegs ? heatColorProps.percent < 0 ? 1 - (1 + heatColorProps.percent / 100) : shadeIntensity + heatColorProps.percent / 100 : heatColorProps.percent / 100
            ),
            patternID: Utils$1.randomId(),
            width: w2.config.fill.image.width ? w2.config.fill.image.width : cellW,
            height: w2.config.fill.image.height ? w2.config.fill.image.height : yDivision
          });
        }
        const radius = this.rectRadius;
        const stroke = w2.config.plotOptions.heatmap.useFillColorAsStroke ? color : w2.globals.stroke.colors[0];
        if (useCanvas) {
          emit.drawRectCell(x1, y1, cellW, yDivision, {
            fill: color,
            fillOpacity: cellFillOpacity,
            stroke,
            strokeWidth: this.strokeWidth,
            radius,
            seriesIndex: i2,
            dataPointIndex: j
          });
        } else {
          const rect = graphics.drawRect(x1, y1, cellW, yDivision, radius);
          rect.attr({
            cx: x1,
            cy: y1
          });
          rect.node.classList.add("apexcharts-heatmap-rect");
          elSeries.add(rect);
          rect.attr({
            fill: color,
            i: i2,
            index: i2,
            j,
            val: series[i2][j],
            "stroke-width": this.strokeWidth,
            stroke,
            color
          });
          if (w2.config.chart.animations.enabled && !w2.globals.dataChanged) {
            let speed = 1;
            if (!w2.globals.resized) {
              speed = w2.config.chart.animations.speed;
            }
            this.animateHeatMap(rect, x1, y1, cellW, yDivision, speed, i2, j);
          }
          if (w2.globals.dataChanged) {
            let speed = 1;
            if (this.dynamicAnim.enabled && w2.globals.shouldAnimate) {
              speed = this.dynamicAnim.speed;
              let colorFrom = w2.globals.previousPaths[i2] && w2.globals.previousPaths[i2][j] && w2.globals.previousPaths[i2][j].color;
              if (!colorFrom) colorFrom = "rgba(255, 255, 255, 0)";
              this.animateHeatColor(
                rect,
                Utils$1.isColorHex(colorFrom) ? colorFrom : Utils$1.rgb2hex(colorFrom),
                Utils$1.isColorHex(color) ? color : Utils$1.rgb2hex(color),
                speed
              );
            }
          }
        }
        const formatter = w2.config.dataLabels.formatter;
        const formattedText = formatter(w2.seriesData.series[i2][j], {
          value: w2.seriesData.series[i2][j],
          seriesIndex: i2,
          dataPointIndex: j,
          w: w2
        });
        const dataLabels = this.helpers.calculateDataLabels({
          text: formattedText,
          x: x1 + cellW / 2,
          y: y1 + yDivision / 2,
          i: i2,
          j,
          colorProps: heatColorProps,
          series: heatSeries
        });
        if (dataLabels !== null) {
          elSeries.add(dataLabels);
        }
        if (!isContinuousX) x1 = x1 + xDivision;
        j++;
      }
      y1 = y1 + yDivision;
      ret.add(elSeries);
    }
    const yAxisScale = (
      /** @type {any[]} */
      w2.globals.yAxisScale[0].result.slice()
    );
    if (w2.config.yaxis[0].reversed) {
      yAxisScale.unshift("");
    } else {
      yAxisScale.push("");
    }
    w2.globals.yAxisScale[0].result = yAxisScale;
    return ret;
  }
  /**
   * @param {any} el
   * @param {number} x
   * @param {number} y
   * @param {number} width
   * @param {number} height
   * @param {number} speed
   * @param {number} [row] - series index (heatmap row)
   * @param {number} [col] - data point index (heatmap column)
   */
  animateHeatMap(el, x, y, width, height, speed, row = 0, col = 0) {
    const w2 = this.w;
    const animations = new Animations(this.w);
    const animCfg = w2.config.chart.animations;
    const gradCfg = animCfg.animateGradually;
    const staggerEnabled = gradCfg && gradCfg.enabled !== false;
    let delay = 0;
    if (staggerEnabled) {
      const seriesCount = (w2.seriesData.series || []).length || 1;
      const pointsCount = w2.globals.dataPoints || 1;
      const maxDiag = seriesCount + pointsCount - 2;
      const baseDelay = Math.min(
        gradCfg.delay || 0,
        speed * 0.5 / Math.max(1, maxDiag)
      );
      delay = computeStagger({
        style: "diagonal",
        index: col,
        row,
        col,
        baseDelay
      });
    }
    animations.animateRect(
      el,
      {
        x: x + width / 2,
        y: y + height / 2,
        width: 0,
        height: 0
      },
      {
        x,
        y,
        width,
        height
      },
      speed,
      () => {
        animations.animationCompleted(el);
      },
      delay
    );
  }
  /**
   * @param {any} el
   * @param {string} colorFrom
   * @param {string} colorTo
   * @param {number} speed
   */
  animateHeatColor(el, colorFrom, colorTo, speed) {
    el.attr({
      fill: colorFrom
    }).animate(speed).attr({
      fill: colorTo
    });
  }
}
class Helpers5 {
  /**
   * @param {import('../../../charts/Line').default} lineCtx
   */
  constructor(lineCtx) {
    this.w = lineCtx.w;
    this.lineCtx = lineCtx;
  }
  /**
   * @param {number} i
   * @param {any[]} series
   */
  sameValueSeriesFix(i2, series) {
    const w2 = this.w;
    if (w2.config.fill.type === "gradient" || w2.config.fill.type[i2] === "gradient") {
      const coreUtils = new CoreUtils(this.lineCtx.w);
      if (coreUtils.seriesHaveSameValues(i2)) {
        const gSeries = series[i2].slice();
        gSeries[gSeries.length - 1] = gSeries[gSeries.length - 1] + 1e-6;
        series[i2] = gSeries;
      }
    }
    return series;
  }
  /** @param {{series: any, realIndex: any, x: any, y: any, i: any, j: any, prevY: any}} opts */
  calculatePoints({ series, realIndex, x, y, i: i2, j, prevY }) {
    const w2 = this.w;
    const ptX = [];
    const ptY = [];
    let xPT1st = this.lineCtx.categoryAxisCorrection + w2.config.markers.offsetX;
    if (w2.axisFlags.isXNumeric) {
      xPT1st = (w2.seriesData.seriesX[realIndex][0] - w2.globals.minX) / this.lineCtx.xRatio + w2.config.markers.offsetX;
    }
    if (j === 0) {
      ptX.push(xPT1st);
      ptY.push(
        Utils$1.isNumber(series[i2][0]) ? prevY + w2.config.markers.offsetY : null
      );
    }
    ptX.push(x + w2.config.markers.offsetX);
    ptY.push(
      Utils$1.isNumber(series[i2][j + 1]) ? y + w2.config.markers.offsetY : null
    );
    return {
      x: ptX,
      y: ptY
    };
  }
  /** @param {{pathFromLine: any, pathFromArea: any, realIndex: any}} opts */
  checkPreviousPaths({ pathFromLine, pathFromArea, realIndex }) {
    const w2 = this.w;
    for (let pp = 0; pp < w2.globals.previousPaths.length; pp++) {
      const gpp = w2.globals.previousPaths[pp];
      if ((gpp.type === "line" || gpp.type === "area") && gpp.paths.length > 0 && parseInt(gpp.realIndex, 10) === parseInt(realIndex, 10)) {
        if (gpp.type === "line") {
          this.lineCtx.appendPathFrom = false;
          pathFromLine = w2.globals.previousPaths[pp].paths[0].d;
        } else if (gpp.type === "area") {
          this.lineCtx.appendPathFrom = false;
          pathFromArea = w2.globals.previousPaths[pp].paths[0].d;
          if (w2.config.stroke.show && w2.globals.previousPaths[pp].paths[1]) {
            pathFromLine = w2.globals.previousPaths[pp].paths[1].d;
          }
        }
      }
    }
    return {
      pathFromLine,
      pathFromArea
    };
  }
  /** @param {{i: any, realIndex: any, series: any, prevY: any, lineYPosition: any, translationsIndex: any}} opts */
  determineFirstPrevY({
    i: i2,
    realIndex,
    series,
    prevY,
    lineYPosition,
    translationsIndex
  }) {
    var _a, _b, _c;
    const w2 = this.w;
    const stackSeries = w2.config.chart.stacked && !w2.globals.comboCharts || w2.config.chart.stacked && w2.globals.comboCharts && (!this.w.config.chart.stackOnlyBar || /** @type {any} */
    ((_a = this.w.config.series[realIndex]) == null ? void 0 : _a.type) === "bar" || /** @type {any} */
    ((_b = this.w.config.series[realIndex]) == null ? void 0 : _b.type) === "column");
    if (typeof ((_c = series[i2]) == null ? void 0 : _c[0]) !== "undefined") {
      if (stackSeries) {
        if (i2 > 0) {
          lineYPosition = this.lineCtx.prevSeriesY[i2 - 1][0];
        } else {
          lineYPosition = this.lineCtx.zeroY;
        }
      } else {
        lineYPosition = this.lineCtx.zeroY;
      }
      prevY = lineYPosition - series[i2][0] / this.lineCtx.yRatio[translationsIndex] + (this.lineCtx.isReversed ? series[i2][0] / this.lineCtx.yRatio[translationsIndex] : 0) * 2;
    } else {
      if (stackSeries && i2 > 0 && typeof series[i2][0] === "undefined") {
        for (let s2 = i2 - 1; s2 >= 0; s2--) {
          if (series[s2][0] !== null && typeof series[s2][0] !== "undefined") {
            lineYPosition = this.lineCtx.prevSeriesY[s2][0];
            prevY = lineYPosition;
            break;
          }
        }
      }
    }
    return {
      prevY,
      lineYPosition
    };
  }
}
class Line {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   * @param {import('../types/internal').XYRatios} xyRatios
   * @param {boolean} isPointsChart
   */
  constructor(w2, ctx, xyRatios, isPointsChart) {
    this.ctx = ctx;
    this.w = w2;
    this.xyRatios = xyRatios;
    this.xRatio = 0;
    this.yRatio = [];
    this.zRatio = 0;
    this.baseLineY = [];
    this.pointsChart = !(this.w.config.chart.type !== "bubble" && this.w.config.chart.type !== "scatter") || isPointsChart;
    this.scatter = new Scatter(this.w, this.ctx);
    this.noNegatives = this.w.globals.minX === Number.MAX_VALUE;
    this.lineHelpers = new Helpers5(this);
    this.markers = new Markers(this.w, this.ctx);
    this.prevSeriesY = [];
    this.categoryAxisCorrection = 0;
    this.yaxisIndex = 0;
    this.xDivision = 0;
    this.zeroY = 0;
    this.areaBottomY = 0;
    this.strokeWidth = 0;
    this.isReversed = false;
    this.appendPathFrom = false;
    this.elSeries = null;
    this.elPointsMain = null;
    this.elDataLabelsWrap = null;
    this._elLastPointsWrap = null;
  }
  /**
   * @param {any[]} series
   * @param {string} ctype
   * @param {number} seriesIndex
   * @param {any} seriesRangeEnd
   */
  draw(series, ctype, seriesIndex, seriesRangeEnd) {
    var _a;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const type = w2.globals.comboCharts ? ctype : w2.config.chart.type;
    const ret = graphics.group({
      class: `apexcharts-${type}-series apexcharts-plot-series`
    });
    const coreUtils = new CoreUtils(this.w);
    this.yRatio = this.xyRatios.yRatio;
    this.zRatio = this.xyRatios.zRatio;
    this.xRatio = this.xyRatios.xRatio;
    this.baseLineY = this.xyRatios.baseLineY;
    series = coreUtils.getLogSeries(series);
    this.yRatio = coreUtils.getLogYRatios(this.yRatio);
    this.prevSeriesY = [];
    const allSeries = [];
    for (let i2 = 0; i2 < series.length; i2++) {
      series = this.lineHelpers.sameValueSeriesFix(i2, series);
      const realIndex = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex[i2]
      ) : i2;
      const translationsIndex = this.yRatio.length > 1 ? realIndex : 0;
      this._initSerieVariables(series, i2, realIndex);
      const yArrj = [];
      const y2Arrj = [];
      const xArrj = [];
      let x = w2.globals.padHorizontal + this.categoryAxisCorrection;
      const y = 1;
      const linePaths = [];
      const areaPaths = [];
      Series.addCollapsedClassToSeries(this.w, this.elSeries, realIndex);
      if (w2.axisFlags.isXNumeric && w2.seriesData.seriesX.length > 0) {
        x = (w2.seriesData.seriesX[realIndex][0] - w2.globals.minX) / this.xRatio;
      }
      xArrj.push(x);
      const pX = x;
      let pY2;
      const prevX = pX;
      let prevY = this.zeroY;
      let prevY2 = this.zeroY;
      const lineYPosition = 0;
      const firstPrevY = this.lineHelpers.determineFirstPrevY({
        i: i2,
        realIndex,
        series,
        prevY,
        lineYPosition,
        translationsIndex
      });
      prevY = firstPrevY.prevY;
      if (w2.config.stroke.curve === "monotoneCubic" && series[i2][0] === null) {
        yArrj.push(null);
      } else {
        yArrj.push(prevY);
      }
      const pY = prevY;
      let firstPrevY2;
      if (type === "rangeArea") {
        firstPrevY2 = this.lineHelpers.determineFirstPrevY({
          i: i2,
          realIndex,
          series: seriesRangeEnd,
          prevY: prevY2,
          lineYPosition,
          translationsIndex
        });
        prevY2 = firstPrevY2.prevY;
        pY2 = prevY2;
        y2Arrj.push(yArrj[0] !== null ? prevY2 : null);
      }
      const pathsFrom = this._calculatePathsFrom({
        type,
        series,
        i: i2,
        realIndex,
        translationsIndex,
        prevX,
        prevY,
        prevY2
      });
      const rYArrj = [yArrj[0]];
      const rY2Arrj = [y2Arrj[0]];
      const iteratingOpts = {
        type,
        series,
        realIndex,
        translationsIndex,
        i: i2,
        x,
        y,
        pX,
        pY,
        pathsFrom,
        linePaths,
        areaPaths,
        seriesIndex,
        lineYPosition,
        xArrj,
        yArrj,
        y2Arrj,
        seriesRangeEnd
      };
      const paths = this._iterateOverDataPoints(__spreadProps(__spreadValues({}, iteratingOpts), {
        iterations: type === "rangeArea" ? series[i2].length - 1 : void 0,
        isRangeStart: true
      }));
      if (type === "rangeArea") {
        const pathsFrom2 = this._calculatePathsFrom({
          series: seriesRangeEnd,
          i: i2,
          realIndex,
          prevX,
          prevY: prevY2
        });
        const rangePaths = this._iterateOverDataPoints(__spreadProps(__spreadValues({}, iteratingOpts), {
          series: seriesRangeEnd,
          xArrj: [x],
          yArrj: rYArrj,
          y2Arrj: rY2Arrj,
          pY: pY2,
          areaPaths: paths.areaPaths,
          pathsFrom: pathsFrom2,
          iterations: seriesRangeEnd[i2].length - 1,
          isRangeStart: false
        }));
        const segments = paths.linePaths.length / 2;
        for (let s2 = 0; s2 < segments; s2++) {
          paths.linePaths[s2] = rangePaths.linePaths[s2 + segments] + paths.linePaths[s2];
        }
        paths.linePaths.splice(segments);
        paths.pathFromLine = rangePaths.pathFromLine + paths.pathFromLine;
      } else if (!/z\s*$/i.test(paths.pathFromArea)) {
        paths.pathFromArea += "z";
      }
      this._handlePaths({ type, realIndex, i: i2, paths });
      this.elSeries.add(this.elPointsMain);
      this.elSeries.add(this.elDataLabelsWrap);
      allSeries.push(this.elSeries);
    }
    if (typeof /** @type {Record<string,any>} */
    ((_a = w2.config.series[0]) == null ? void 0 : _a.zIndex) !== "undefined") {
      allSeries.sort(
        (a2, b2) => Number(a2.node.getAttribute("zIndex")) - Number(b2.node.getAttribute("zIndex"))
      );
    }
    if (w2.config.chart.stacked) {
      for (let s2 = allSeries.length - 1; s2 >= 0; s2--) {
        ret.add(allSeries[s2]);
      }
    } else {
      for (let s2 = 0; s2 < allSeries.length; s2++) {
        ret.add(allSeries[s2]);
      }
    }
    return ret;
  }
  /**
   * @param {any[]} series
   * @param {number} i
   * @param {number} realIndex
   */
  _initSerieVariables(series, i2, realIndex) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    this.xDivision = w2.layout.gridWidth / (w2.globals.dataPoints - (w2.config.xaxis.tickPlacement === "on" ? 1 : 0));
    this.strokeWidth = Array.isArray(w2.config.stroke.width) ? w2.config.stroke.width[realIndex] : w2.config.stroke.width;
    let translationsIndex = 0;
    if (this.yRatio.length > 1) {
      this.yaxisIndex = w2.globals.seriesYAxisReverseMap[realIndex];
      translationsIndex = realIndex;
    }
    this.isReversed = w2.config.yaxis[this.yaxisIndex] && w2.config.yaxis[this.yaxisIndex].reversed;
    this.zeroY = w2.layout.gridHeight - this.baseLineY[translationsIndex] - (this.isReversed ? w2.layout.gridHeight : 0) + (this.isReversed ? this.baseLineY[translationsIndex] * 2 : 0);
    this.areaBottomY = this.zeroY;
    if (this.zeroY > w2.layout.gridHeight || w2.config.plotOptions.area.fillTo === "end") {
      this.areaBottomY = w2.layout.gridHeight;
    }
    this.categoryAxisCorrection = this.xDivision / 2;
    const seriesItem = (
      /** @type {Record<string,any>} */
      w2.config.series[realIndex]
    );
    this.elSeries = graphics.group({
      class: `apexcharts-series`,
      zIndex: typeof seriesItem.zIndex !== "undefined" ? seriesItem.zIndex : realIndex,
      seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex])
    });
    this.elPointsMain = graphics.group({
      class: "apexcharts-series-markers-wrap",
      "data:realIndex": realIndex
    });
    this.markers.resetSeriesWrapCache();
    this._elLastPointsWrap = null;
    if (w2.globals.hasNullValues) {
      const firstPoint = this.markers.plotChartMarkers({
        pointsPos: {
          x: [0],
          y: [w2.layout.gridHeight + w2.globals.markers.largestSize]
        },
        seriesIndex: i2,
        j: 0,
        pSize: 0.1,
        alwaysDrawMarker: true,
        isVirtualPoint: true
      });
      if (firstPoint !== null) {
        this.elPointsMain.add(firstPoint);
      }
    }
    this.elDataLabelsWrap = graphics.group({
      class: "apexcharts-datalabels",
      "data:realIndex": realIndex
    });
    const longestSeries = series[i2].length === w2.globals.dataPoints;
    this.elSeries.attr({
      "data:longestSeries": longestSeries,
      rel: i2 + 1,
      "data:realIndex": realIndex
    });
    this.appendPathFrom = true;
  }
  /** @param {{ type?: any, series?: any, i?: any, realIndex?: any, translationsIndex?: any, prevX?: any, prevY?: any, prevY2?: any }} opts */
  _calculatePathsFrom({
    type,
    series,
    i: i2,
    realIndex,
    translationsIndex,
    prevX,
    prevY,
    prevY2
  }) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    let linePath, areaPath, pathFromLine, pathFromArea;
    if (series[i2][0] === null) {
      for (let s2 = 0; s2 < series[i2].length; s2++) {
        if (series[i2][s2] !== null) {
          prevX = this.xDivision * s2;
          prevY = this.zeroY - series[i2][s2] / this.yRatio[translationsIndex];
          linePath = graphics.move(prevX, prevY);
          areaPath = graphics.move(prevX, this.areaBottomY);
          break;
        }
      }
    } else {
      linePath = graphics.move(prevX, prevY);
      if (type === "rangeArea") {
        linePath = graphics.move(prevX, prevY2) + graphics.line(prevX, prevY);
      }
      areaPath = graphics.move(prevX, this.areaBottomY) + graphics.line(prevX, prevY);
    }
    pathFromLine = graphics.move(0, this.areaBottomY) + graphics.line(0, this.areaBottomY);
    pathFromArea = graphics.move(0, this.areaBottomY) + graphics.line(0, this.areaBottomY);
    if (w2.globals.previousPaths.length > 0) {
      const pathFrom = this.lineHelpers.checkPreviousPaths({
        pathFromLine,
        pathFromArea,
        realIndex
      });
      pathFromLine = pathFrom.pathFromLine;
      pathFromArea = pathFrom.pathFromArea;
    }
    return {
      prevX,
      prevY,
      linePath,
      areaPath,
      pathFromLine,
      pathFromArea
    };
  }
  /** @param {{type: any, realIndex: any, i: any, paths: any}} opts */
  _handlePaths({ type, realIndex, i: i2, paths }) {
    var _a, _b, _c, _d, _e, _f, _g;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const emit = seriesEmitter(this.ctx, graphics);
    const fill = new Fill(this.w);
    this.prevSeriesY.push(paths.yArrj);
    let streamScroll = null;
    if ((type === "line" || type === "area") && w2.globals.dataChanged) {
      streamScroll = detectStreamScroll(w2, realIndex, paths.xArrj, paths.yArrj);
    }
    let reconcile = null;
    if (!streamScroll && (type === "line" || type === "area")) {
      reconcile = reconcileSeriesPaths(w2, {
        type,
        realIndex,
        pathFromLine: paths.pathFromLine,
        pathFromArea: paths.pathFromArea,
        linePaths: paths.linePaths,
        areaPaths: paths.areaPaths
      });
    }
    w2.globals.seriesXvalues[realIndex] = paths.xArrj;
    w2.globals.seriesYvalues[realIndex] = paths.yArrj;
    const forecast = w2.config.forecastDataPoints;
    if (forecast.count > 0 && type !== "rangeArea") {
      const forecastCutoff = w2.globals.seriesXvalues[realIndex][w2.globals.seriesXvalues[realIndex].length - forecast.count - 1];
      const elForecastMask = graphics.drawRect(
        forecastCutoff,
        0,
        w2.layout.gridWidth,
        w2.layout.gridHeight,
        0
      );
      w2.dom.elForecastMask.appendChild(elForecastMask.node);
      const elNonForecastMask = graphics.drawRect(
        0,
        0,
        forecastCutoff,
        w2.layout.gridHeight,
        0
      );
      w2.dom.elNonForecastMask.appendChild(elNonForecastMask.node);
    }
    if (!this.pointsChart) {
      w2.globals.delayedElements.push({
        el: this.elPointsMain.node,
        index: realIndex
      });
      tweenSeriesMarkers(w2, {
        elPointsMain: this.elPointsMain,
        realIndex,
        speed: w2.config.chart.animations.dynamicAnimation.speed
      });
      if (seriesJoin(w2, realIndex) && ((_a = this.elDataLabelsWrap) == null ? void 0 : _a.node)) {
        this.elDataLabelsWrap.node.classList.add("apexcharts-element-hidden");
        w2.globals.delayedElements.push({
          el: this.elDataLabelsWrap.node,
          holdUntilComplete: true
        });
      }
    } else {
      tweenSeriesMarkers(w2, {
        elPointsMain: this.elPointsMain,
        realIndex,
        speed: w2.config.chart.animations.dynamicAnimation.speed
      });
    }
    const defaultRenderedPathOptions = {
      i: i2,
      realIndex,
      animationDelay: i2,
      initialSpeed: w2.config.chart.animations.speed,
      dataChangeSpeed: w2.config.chart.animations.dynamicAnimation.speed,
      className: `apexcharts-${type}`
    };
    const numericXY = paths.numericXY;
    if (type === "area") {
      const pathFill = fill.fillPath({
        seriesNumber: realIndex
      });
      for (let p2 = 0; p2 < paths.areaPaths.length; p2++) {
        const renderedPath = emit.renderPaths(__spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
          pathFrom: streamScroll ? projectPathToPrevFrame(paths.areaPaths[p2], streamScroll) : (_c = (_b = reconcile == null ? void 0 : reconcile.area) == null ? void 0 : _b.from) != null ? _c : paths.pathFromArea,
          pathTo: paths.areaPaths[p2],
          pathToNumeric: numericXY ? {
            xs: numericXY.xs,
            ys: numericXY.ys,
            closeY: numericXY.areaCloseY
          } : void 0,
          pathToInterp: (_d = reconcile == null ? void 0 : reconcile.area) == null ? void 0 : _d.toInterp,
          scrollMorph: !!streamScroll,
          stroke: "none",
          strokeWidth: 0,
          strokeLineCap: null,
          fill: pathFill
        }));
        this.elSeries.add(renderedPath);
      }
    }
    if (w2.config.stroke.show && !this.pointsChart) {
      let lineFill = null;
      if (type === "line") {
        lineFill = fill.fillPath({
          seriesNumber: realIndex,
          i: i2
        });
      } else {
        if (w2.config.stroke.fill.type === "solid") {
          lineFill = w2.globals.stroke.colors[realIndex];
        } else {
          const prevFill = w2.config.fill;
          w2.config.fill = w2.config.stroke.fill;
          lineFill = fill.fillPath({
            seriesNumber: realIndex,
            i: i2
          });
          w2.config.fill = prevFill;
        }
      }
      for (let p2 = 0; p2 < paths.linePaths.length; p2++) {
        let pathFill = lineFill;
        if (type === "rangeArea") {
          pathFill = fill.fillPath({
            seriesNumber: realIndex
          });
        }
        const linePathCommonOpts = __spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
          pathFrom: streamScroll ? projectPathToPrevFrame(paths.linePaths[p2], streamScroll) : (_f = (_e = reconcile == null ? void 0 : reconcile.line) == null ? void 0 : _e.from) != null ? _f : paths.pathFromLine,
          pathTo: paths.linePaths[p2],
          pathToNumeric: numericXY ? { xs: numericXY.xs, ys: numericXY.ys } : void 0,
          pathToInterp: (_g = reconcile == null ? void 0 : reconcile.line) == null ? void 0 : _g.toInterp,
          scrollMorph: !!streamScroll,
          stroke: lineFill,
          strokeWidth: this.strokeWidth,
          strokeLineCap: w2.config.stroke.lineCap,
          fill: type === "rangeArea" ? pathFill : "none"
        });
        const renderedPath = emit.renderPaths(linePathCommonOpts);
        this.elSeries.add(renderedPath);
        renderedPath.attr("fill-rule", `evenodd`);
        if (forecast.count > 0 && type !== "rangeArea") {
          const renderedForecastPath = emit.renderPaths(linePathCommonOpts);
          renderedForecastPath.node.setAttribute(
            "stroke-dasharray",
            forecast.dashArray
          );
          if (forecast.strokeWidth) {
            renderedForecastPath.node.setAttribute(
              "stroke-width",
              forecast.strokeWidth
            );
          }
          this.elSeries.add(renderedForecastPath);
          renderedForecastPath.attr(
            "clip-path",
            `url(#forecastMask${w2.globals.cuid})`
          );
          renderedPath.attr(
            "clip-path",
            `url(#nonForecastMask${w2.globals.cuid})`
          );
        }
      }
    }
  }
  _iterateOverDataPoints({
    type,
    series,
    iterations,
    realIndex,
    translationsIndex,
    i: i2,
    x,
    y,
    pX,
    pY,
    pathsFrom,
    linePaths,
    areaPaths,
    seriesIndex,
    lineYPosition,
    xArrj,
    yArrj,
    y2Arrj,
    isRangeStart,
    seriesRangeEnd
  }) {
    var _a, _b;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const yRatio = this.yRatio;
    let { prevY, linePath, areaPath, pathFromLine, pathFromArea } = pathsFrom;
    const minY = Utils$1.isNumber(w2.globals.minYArr[realIndex]) ? w2.globals.minYArr[realIndex] : w2.globals.minY;
    if (!iterations) {
      iterations = w2.globals.dataPoints > 1 ? w2.globals.dataPoints - 1 : w2.globals.dataPoints;
    }
    const getY = (_y, lineYPos) => {
      return lineYPos - _y / yRatio[translationsIndex] + (this.isReversed ? _y / yRatio[translationsIndex] : 0) * 2;
    };
    let y2 = y;
    const stackSeries = w2.config.chart.stacked && !w2.globals.comboCharts || w2.config.chart.stacked && w2.globals.comboCharts && (!this.w.config.chart.stackOnlyBar || /** @type {Record<string,any>} */
    ((_a = this.w.config.series[realIndex]) == null ? void 0 : _a.type) === "bar" || /** @type {Record<string,any>} */
    ((_b = this.w.config.series[realIndex]) == null ? void 0 : _b.type) === "column");
    let curve = w2.config.stroke.curve;
    if (Array.isArray(curve)) {
      if (Array.isArray(seriesIndex)) {
        curve = curve[seriesIndex[i2]];
      } else {
        curve = curve[i2];
      }
    }
    let pathState = 0;
    let segmentStartX;
    const jitterPx = this.pointsChart ? this._scatterJitterPx(realIndex) : null;
    if (curve === "straight" && !this.pointsChart) {
      const fast = this._fastStraightPath({
        type,
        series,
        i: i2,
        realIndex,
        translationsIndex,
        iterations,
        x,
        y,
        pX,
        pY,
        pathsFrom,
        linePaths,
        areaPaths,
        xArrj,
        yArrj,
        y2Arrj,
        stackSeries
      });
      if (fast) return fast;
    }
    for (let j = 0; j < iterations; j++) {
      if (series[i2].length === 0) break;
      const isNull = typeof series[i2][j + 1] === "undefined" || series[i2][j + 1] === null;
      if (w2.axisFlags.isXNumeric) {
        let sX = w2.seriesData.seriesX[realIndex][j + 1];
        if (typeof w2.seriesData.seriesX[realIndex][j + 1] === "undefined") {
          sX = w2.seriesData.seriesX[realIndex][iterations - 1];
        }
        x = (sX - w2.globals.minX) / this.xRatio;
      } else {
        x = x + this.xDivision;
      }
      if (stackSeries) {
        if (i2 > 0 && w2.globals.collapsedSeries.length < w2.config.series.length - 1) {
          const prevIndex = (pi) => {
            var _a2;
            for (let pii = pi; pii >= 0; pii--) {
              const ri = (_a2 = seriesIndex == null ? void 0 : seriesIndex[pii]) != null ? _a2 : pii;
              if (w2.globals.collapsedSeriesIndices.indexOf(ri) === -1 && w2.globals.ancillaryCollapsedSeriesIndices.indexOf(ri) === -1) {
                return pii;
              }
            }
            return -1;
          };
          const pIdx = prevIndex(i2 - 1);
          lineYPosition = pIdx < 0 ? this.zeroY : this.prevSeriesY[pIdx][j + 1];
        } else {
          lineYPosition = this.zeroY;
        }
      } else {
        lineYPosition = this.zeroY;
      }
      if (isNull) {
        y = getY(minY, lineYPosition);
      } else {
        y = getY(series[i2][j + 1], lineYPosition);
        if (type === "rangeArea") {
          y2 = getY(seriesRangeEnd[i2][j + 1], lineYPosition);
        }
      }
      let xj = x;
      let yj = y;
      if (jitterPx) {
        const seed = realIndex * 100003 + (j + 1);
        if (jitterPx.x) xj = x + (hash01(seed * 7919 + 13) - 0.5) * 2 * jitterPx.x;
        if (jitterPx.y) yj = y + (hash01(seed * 6271 + 97) - 0.5) * 2 * jitterPx.y;
      }
      xArrj.push(series[i2][j + 1] === null ? null : xj);
      if (isNull && (w2.config.stroke.curve === "smooth" || w2.config.stroke.curve === "monotoneCubic")) {
        yArrj.push(null);
        y2Arrj.push(null);
      } else {
        yArrj.push(yj);
        y2Arrj.push(y2);
      }
      const pointsPos = this.lineHelpers.calculatePoints({
        series,
        x: xj,
        y: yj,
        realIndex,
        i: i2,
        j,
        prevY
      });
      const calculatedPaths = this._createPaths({
        type,
        series,
        i: i2,
        j,
        x,
        y,
        y2,
        xArrj,
        yArrj,
        y2Arrj,
        pX,
        pY,
        pathState,
        segmentStartX,
        linePath,
        areaPath,
        linePaths,
        areaPaths,
        curve,
        isRangeStart
      });
      areaPaths = calculatedPaths.areaPaths;
      linePaths = calculatedPaths.linePaths;
      pX = calculatedPaths.pX;
      pY = calculatedPaths.pY;
      pathState = calculatedPaths.pathState;
      segmentStartX = calculatedPaths.segmentStartX;
      areaPath = calculatedPaths.areaPath;
      linePath = calculatedPaths.linePath;
      if (this.appendPathFrom && !w2.globals.hasNullValues && !(curve === "monotoneCubic" && type === "rangeArea")) {
        pathFromLine += graphics.line(x, this.areaBottomY);
        pathFromArea += graphics.line(x, this.areaBottomY);
      }
      this.handleNullDataPoints(series, pointsPos, i2, j, realIndex);
      this._handleMarkersAndLabels({
        type,
        pointsPos,
        i: i2,
        j,
        realIndex,
        isRangeStart
      });
    }
    return {
      yArrj,
      xArrj,
      pathFromArea,
      areaPaths,
      pathFromLine,
      linePaths,
      linePath,
      areaPath
    };
  }
  /** @param {{type: any, pointsPos: any, isRangeStart: any, i: any, j: any, realIndex: any}} opts */
  _handleMarkersAndLabels({ type, pointsPos, isRangeStart, i: i2, j, realIndex }) {
    const w2 = this.w;
    const dataLabels = new DataLabels(this.w, this.ctx);
    if (!this.pointsChart) {
      const useProgressive = !w2.globals.dataChanged && !w2.globals.resized;
      if (!useProgressive && w2.seriesData.series[i2].length > 1) {
        this.elPointsMain.node.classList.add("apexcharts-element-hidden");
      }
      const elPointsWrap = this.markers.plotChartMarkers({
        pointsPos,
        seriesIndex: realIndex,
        j: j + 1
      });
      if (elPointsWrap !== null && elPointsWrap !== this._elLastPointsWrap) {
        this.elPointsMain.add(elPointsWrap);
        this._elLastPointsWrap = elPointsWrap;
      }
    } else {
      this.scatter.draw(this.elSeries, j, {
        realIndex,
        pointsPos,
        zRatio: this.zRatio,
        elParent: this.elPointsMain
      });
    }
    const drawnLabels = dataLabels.drawDataLabel({
      type,
      isRangeStart,
      pos: pointsPos,
      i: realIndex,
      j: j + 1
    });
    if (drawnLabels !== null) {
      this.elDataLabelsWrap.add(drawnLabels);
    }
  }
  /**
   * Max scatter-jitter offsets in pixels for this series, or null when jitter is
   * off. The config offsets are in axis units (x: 1 = one category step / x-data
   * unit, y: 1 = one y-data unit); convert each to pixels using the chart's
   * ratios. The actual per-point offset is a deterministic fraction of these
   * (see Scatter.drawPoint).
   * @param {number} realIndex
   * @returns {{ x: number, y: number } | null}
   */
  _scatterJitterPx(realIndex) {
    var _a;
    const w2 = this.w;
    const jt = (_a = w2.config.plotOptions.scatter) == null ? void 0 : _a.jitter;
    if (!jt || !jt.enabled || !jt.x && !jt.y) return null;
    const xUnitPx = w2.axisFlags.isXNumeric && this.xRatio ? 1 / this.xRatio : this.xDivision;
    const ti = this.yRatio.length > 1 ? realIndex : 0;
    const yUnitPx = this.yRatio[ti] ? 1 / this.yRatio[ti] : 0;
    return {
      x: (jt.x || 0) * xUnitPx,
      y: (jt.y || 0) * yUnitPx
    };
  }
  /**
   * Numeric geometry fast path for plain straight line/area series (the
   * render-2026 perf work). Eligibility is strict: everything the per-point
   * slow loop can do beyond plain geometry (null gaps, markers, data labels,
   * discrete markers, stacking, combos, range areas) bails to the state
   * machine. When eligible it produces the SAME outputs as the slow loop
   * (byte-identical d strings via join, the same xArrj/yArrj/y2Arrj
   * pushes, and the same pointsArray tooltip cache) in one tight loop.
   * In canvas mode it additionally emits typed-array coordinates so the
   * renderer can paint via moveTo/lineTo without a Path2D d-string parse.
   *
   * @param {{type: any, series: any, i: number, realIndex: number,
   *   translationsIndex: number, iterations: number, x: number, y: number,
   *   pX: number, pY: number, pathsFrom: any, linePaths: any[],
   *   areaPaths: any[], xArrj: any[], yArrj: any[], y2Arrj: any[],
   *   stackSeries: boolean}} opts
   * @returns {any} the _iterateOverDataPoints result, or null when ineligible
   */
  _fastStraightPath({
    type,
    series,
    i: i2,
    realIndex,
    translationsIndex,
    iterations,
    x,
    y,
    pX,
    pY,
    pathsFrom,
    linePaths,
    areaPaths,
    xArrj,
    yArrj,
    y2Arrj,
    stackSeries
  }) {
    const w2 = this.w;
    if (type !== "line" && type !== "area") return null;
    if (w2.globals.comboCharts || stackSeries) return null;
    if (w2.config.dataLabels.enabled) return null;
    if (w2.config.markers.discrete.length) return null;
    if (w2.globals.markers.size[realIndex] > 0) return null;
    const s2 = series[i2];
    const n2 = s2.length;
    if (!iterations || n2 < 2 || n2 - 1 !== iterations) return null;
    for (let k = 0; k <= iterations; k++) {
      const v2 = s2[k];
      if (v2 === null || typeof v2 === "undefined") return null;
    }
    const isXNumeric = w2.axisFlags.isXNumeric;
    const sx = isXNumeric ? w2.seriesData.seriesX[realIndex] : null;
    if (isXNumeric && (!sx || sx.length < n2)) return null;
    const yR = this.yRatio[translationsIndex];
    const isReversed = this.isReversed;
    const zeroY = this.zeroY;
    const bottomY = this.areaBottomY;
    const xRatio = this.xRatio;
    const minX = w2.globals.minX;
    const xDivision = this.xDivision;
    const offX = w2.config.markers.offsetX;
    const offY = w2.config.markers.offsetY;
    const appendFrom = this.appendPathFrom && !w2.globals.hasNullValues;
    let { pathFromLine, pathFromArea } = pathsFrom;
    const r2 = this.ctx && this.ctx.renderer;
    const canvasMode = !!(r2 && r2.kind && r2.kind !== "svg");
    const buildStrings = !canvasMode || w2.globals.dataChanged && !!w2.globals.prevStreamFrame;
    const nxs = canvasMode ? new Float64Array(n2) : null;
    const nys = canvasMode ? new Float64Array(n2) : null;
    if (nxs && nys) {
      nxs[0] = pX;
      nys[0] = pY;
    }
    if (typeof w2.globals.pointsArray[realIndex] === "undefined") {
      w2.globals.pointsArray[realIndex] = [];
    }
    const pts = w2.globals.pointsArray[realIndex];
    const xPT1st = sx ? (sx[0] - minX) / xRatio + offX : this.categoryAxisCorrection + offX;
    pts.push([xPT1st, pathsFrom.prevY + offY]);
    const parts = buildStrings ? new Array(iterations + 1) : [];
    if (buildStrings) parts[0] = "M " + pX + " " + pY;
    const fromParts = buildStrings && appendFrom ? new Array(iterations) : null;
    let xv = x;
    let xj = pX;
    let yj = pY;
    for (let j = 0; j < iterations; j++) {
      if (sx) {
        xj = (sx[j + 1] - minX) / xRatio;
      } else {
        xv = xv + xDivision;
        xj = xv;
      }
      const v2 = s2[j + 1];
      yj = zeroY - v2 / yR + (isReversed ? v2 / yR : 0) * 2;
      xArrj.push(xj);
      yArrj.push(yj);
      y2Arrj.push(y);
      pts.push([xj + offX, yj + offY]);
      if (buildStrings) {
        parts[j + 1] = " L " + xj + " " + yj;
        if (fromParts) fromParts[j] = " L " + xj + " " + bottomY;
      }
      if (nxs && nys) {
        nxs[j + 1] = xj;
        nys[j + 1] = yj;
      }
    }
    let linePath = "";
    let areaPath = "";
    if (buildStrings) {
      linePath = parts.join("");
      areaPath = linePath + " L " + xj + " " + bottomY + " L " + pX + " " + bottomY + "z";
    }
    if (fromParts) {
      const fromAppend = fromParts.join("");
      pathFromLine += fromAppend;
      pathFromArea += fromAppend;
    } else if (!buildStrings && appendFrom) {
      pathFromLine += " L " + xj + " " + bottomY;
      pathFromArea += " L " + xj + " " + bottomY;
    }
    linePaths.push(linePath);
    areaPaths.push(areaPath);
    return {
      yArrj,
      xArrj,
      pathFromArea,
      areaPaths,
      pathFromLine,
      linePaths,
      linePath,
      areaPath,
      numericXY: nxs ? { xs: nxs, ys: nys, areaCloseY: bottomY } : void 0
    };
  }
  /** @param {{type: any, series: any, i: any, j: any, x: any, y: any, xArrj: any, yArrj: any, y2: any, y2Arrj: any, pX: any, pY: any, pathState: any, segmentStartX: any, linePath: any, areaPath: any, linePaths: any, areaPaths: any, curve: any, isRangeStart: any}} opts */
  _createPaths({
    type,
    series,
    i: i2,
    j,
    x,
    y,
    xArrj,
    yArrj,
    y2,
    y2Arrj,
    pX,
    pY,
    pathState,
    segmentStartX,
    linePath,
    areaPath,
    linePaths,
    areaPaths,
    curve,
    isRangeStart
  }) {
    const graphics = new Graphics(this.w);
    const areaBottomY = this.areaBottomY;
    const rangeArea = type === "rangeArea";
    const isLowerRangeAreaPath = type === "rangeArea" && isRangeStart;
    switch (curve) {
      case "monotoneCubic": {
        const yAj = isRangeStart ? yArrj : y2Arrj;
        const getSmoothInputs = (xArr, yArr) => {
          return xArr.map((_, i3) => {
            return [_, yArr[i3]];
          }).filter((_) => _[1] !== null);
        };
        const getSegmentLengths = (yArr) => {
          const segLens = [];
          let count = 0;
          yArr.forEach((_) => {
            if (_ !== null) {
              count++;
            } else if (count > 0) {
              segLens.push(count);
              count = 0;
            }
          });
          if (count > 0) {
            segLens.push(count);
          }
          return segLens;
        };
        const getSegments = (yArr, points) => {
          const segLens = getSegmentLengths(yArr);
          const segments = [];
          for (let i3 = 0, len = 0; i3 < segLens.length; len += segLens[i3++]) {
            segments[i3] = spline.slice(points, len, len + segLens[i3]);
          }
          return segments;
        };
        switch (pathState) {
          case 0:
            if (yAj[j + 1] === null) {
              break;
            }
            pathState = 1;
          // falls through
          case 1:
            if (!(rangeArea ? xArrj.length === series[i2].length : j === series[i2].length - 2)) {
              break;
            }
          // falls through
          case 2: {
            const _xAj = isRangeStart ? xArrj : xArrj.slice().reverse();
            const _yAj = isRangeStart ? yAj : yAj.slice().reverse();
            const smoothInputs = getSmoothInputs(_xAj, _yAj);
            const points = smoothInputs.length > 1 ? spline.points(smoothInputs) : smoothInputs;
            let smoothInputsLower = [];
            if (rangeArea) {
              if (isLowerRangeAreaPath) {
                areaPaths = smoothInputs;
              } else {
                smoothInputsLower = areaPaths.reverse();
              }
            }
            let segmentCount = 0;
            let smoothInputsIndex = 0;
            getSegments(_yAj, points).forEach((_) => {
              segmentCount++;
              const svgPoints = svgPath(_);
              const _start = smoothInputsIndex;
              smoothInputsIndex += _.length;
              const _end = smoothInputsIndex - 1;
              if (isLowerRangeAreaPath) {
                linePath = graphics.move(
                  smoothInputs[_start][0],
                  smoothInputs[_start][1]
                ) + svgPoints;
              } else if (rangeArea) {
                linePath = graphics.move(
                  smoothInputsLower[_start][0],
                  smoothInputsLower[_start][1]
                ) + graphics.line(
                  smoothInputs[_start][0],
                  smoothInputs[_start][1]
                ) + svgPoints + graphics.line(
                  smoothInputsLower[_end][0],
                  smoothInputsLower[_end][1]
                );
              } else {
                linePath = graphics.move(
                  smoothInputs[_start][0],
                  smoothInputs[_start][1]
                ) + svgPoints;
                areaPath = linePath + graphics.line(smoothInputs[_end][0], areaBottomY) + graphics.line(smoothInputs[_start][0], areaBottomY) + "z";
                areaPaths.push(areaPath);
              }
              linePaths.push(linePath);
            });
            if (rangeArea && segmentCount > 1 && !isLowerRangeAreaPath) {
              const upperLinePaths = linePaths.slice(segmentCount).reverse();
              linePaths.splice(segmentCount);
              upperLinePaths.forEach(
                (u) => linePaths.push(u)
              );
            }
            pathState = 0;
            break;
          }
        }
        break;
      }
      case "smooth": {
        const length = (x - pX) * 0.35;
        if (series[i2][j] === null) {
          pathState = 0;
        } else {
          switch (pathState) {
            case 0:
              segmentStartX = pX;
              if (isLowerRangeAreaPath) {
                linePath = graphics.move(pX, y2Arrj[j]) + graphics.line(pX, pY);
              } else {
                linePath = graphics.move(pX, pY);
              }
              areaPath = graphics.move(pX, pY);
              if (series[i2][j + 1] === null || typeof series[i2][j + 1] === "undefined") {
                linePaths.push(linePath);
                areaPaths.push(areaPath);
                break;
              }
              pathState = 1;
              if (j < series[i2].length - 2) {
                const p2 = graphics.curve(pX + length, pY, x - length, y, x, y);
                linePath += p2;
                areaPath += p2;
                break;
              }
            // falls through
            case 1:
              if (series[i2][j + 1] === null) {
                if (isLowerRangeAreaPath) {
                  linePath += graphics.line(pX, y2);
                } else {
                  linePath += graphics.move(pX, pY);
                }
                areaPath += graphics.line(pX, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                linePaths.push(linePath);
                areaPaths.push(areaPath);
                pathState = -1;
              } else {
                const p2 = graphics.curve(pX + length, pY, x - length, y, x, y);
                linePath += p2;
                areaPath += p2;
                if (j >= series[i2].length - 2) {
                  if (isLowerRangeAreaPath) {
                    linePath += graphics.curve(x, y, x, y, x, y2) + graphics.move(x, y2);
                  }
                  areaPath += graphics.curve(x, y, x, y, x, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                  linePaths.push(linePath);
                  areaPaths.push(areaPath);
                  pathState = -1;
                }
              }
              break;
          }
        }
        pX = x;
        pY = y;
        break;
      }
      default: {
        const pathToPoint = (curve2, x2, y3) => {
          let path = "";
          switch (curve2) {
            case "stepline":
              path = graphics.line(x2, null, "H") + graphics.line(null, y3, "V");
              break;
            case "linestep":
              path = graphics.line(null, y3, "V") + graphics.line(x2, null, "H");
              break;
            case "straight":
              path = graphics.line(x2, y3);
              break;
          }
          return path;
        };
        if (series[i2][j] === null) {
          pathState = 0;
        } else {
          switch (pathState) {
            case 0:
              segmentStartX = pX;
              if (isLowerRangeAreaPath) {
                linePath = graphics.move(pX, y2Arrj[j]) + graphics.line(pX, pY);
              } else {
                linePath = graphics.move(pX, pY);
              }
              areaPath = graphics.move(pX, pY);
              if (series[i2][j + 1] === null || typeof series[i2][j + 1] === "undefined") {
                linePaths.push(linePath);
                areaPaths.push(areaPath);
                break;
              }
              pathState = 1;
              if (j < series[i2].length - 2) {
                const p2 = pathToPoint(curve, x, y);
                linePath += p2;
                areaPath += p2;
                break;
              }
            // falls through
            case 1:
              if (series[i2][j + 1] === null) {
                if (isLowerRangeAreaPath) {
                  linePath += graphics.line(pX, y2);
                } else {
                  linePath += graphics.move(pX, pY);
                }
                areaPath += graphics.line(pX, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                linePaths.push(linePath);
                areaPaths.push(areaPath);
                pathState = -1;
              } else {
                const p2 = pathToPoint(curve, x, y);
                linePath += p2;
                areaPath += p2;
                if (j >= series[i2].length - 2) {
                  if (isLowerRangeAreaPath) {
                    linePath += graphics.line(x, y2);
                  }
                  areaPath += graphics.line(x, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                  linePaths.push(linePath);
                  areaPaths.push(areaPath);
                  pathState = -1;
                }
              }
              break;
          }
        }
        pX = x;
        pY = y;
        break;
      }
    }
    return {
      linePaths,
      areaPaths,
      pX,
      pY,
      pathState,
      segmentStartX,
      linePath,
      areaPath
    };
  }
  /**
   * @param {any[]} series
   * @param {any} pointsPos
   * @param {number} i
   * @param {number} j
   * @param {number} realIndex
   */
  handleNullDataPoints(series, pointsPos, i2, j, realIndex) {
    const w2 = this.w;
    if (series[i2][j] === null && w2.config.markers.showNullDataPoints || series[i2].length === 1) {
      let pSize = this.strokeWidth - w2.config.markers.strokeWidth / 2;
      if (!(pSize > 0)) {
        pSize = 0;
      }
      const elPointsWrap = this.markers.plotChartMarkers({
        pointsPos,
        seriesIndex: realIndex,
        j: j + 1,
        pSize,
        alwaysDrawMarker: true
      });
      if (elPointsWrap !== null) {
        this.elPointsMain.add(elPointsWrap);
      }
    }
  }
}
class CircularChartsHelpers {
  /**
   * @param {import('../../../types/internal').ChartStateW} w
   */
  constructor(w2) {
    this.w = w2;
  }
  /**
   * @param {number} x
   * @param {number} y
   * @param {number} i
   * @param {string | number} text
   */
  drawYAxisTexts(x, y, i2, text) {
    const w2 = this.w;
    const yaxisConfig = w2.config.yaxis[0];
    const formatter = w2.formatters.yLabelFormatters[0];
    const graphics = new Graphics(this.w);
    const yaxisLabel = graphics.drawText({
      x: x + yaxisConfig.labels.offsetX,
      y: y + yaxisConfig.labels.offsetY,
      text: formatter(text, i2),
      textAnchor: "middle",
      fontSize: yaxisConfig.labels.style.fontSize,
      fontFamily: yaxisConfig.labels.style.fontFamily,
      foreColor: Array.isArray(yaxisConfig.labels.style.colors) ? yaxisConfig.labels.style.colors[i2] : yaxisConfig.labels.style.colors
    });
    return yaxisLabel;
  }
  /**
   * Widest rendered width among the given label strings. Used to reserve
   * horizontal room for outer (name) labels so the pie can shrink to fit them.
   * @param {string[]} labels
   * @param {{ fontSize?: string, fontFamily?: string }} style
   * @returns {number}
   */
  getMaxLabelWidth(labels, { fontSize, fontFamily } = {}) {
    const graphics = new Graphics(this.w);
    let maxWidth = 0;
    labels.forEach((text) => {
      if (text === null || typeof text === "undefined" || text === "") return;
      const rect = graphics.getTextRects(
        `${text}`,
        fontSize || "12px",
        fontFamily,
        ""
      );
      maxWidth = Math.max(maxWidth, rect.width);
    });
    return maxWidth;
  }
  /**
   * Draw a single outer (name) label: an optional leader line from the slice
   * edge (anchor -> radial elbow -> label) plus the name text (one or more
   * lines, e.g. name + percent). Geometry is computed by the caller (Pie.js)
   * so it can run a de-overlap pass first. The text block is vertically
   * centered on `labelY`, which is where the connector terminates.
   * @param {{
   *   lines: string[],
   *   lineHeight: number,
   *   anchor: { x: number, y: number },
   *   elbow: { x: number, y: number },
   *   labelX: number,
   *   labelY: number,
   *   side: 'left' | 'right',
   *   connector: { show: boolean, width: number, color: string },
   *   style: { fontSize?: string, fontFamily?: string, fontWeight?: string | number },
   *   foreColor: string,
   * }} opts
   */
  drawExternalLabel({
    lines,
    lineHeight,
    anchor,
    elbow,
    labelX,
    labelY,
    side,
    connector,
    style,
    foreColor
  }) {
    const graphics = new Graphics(this.w);
    const group = graphics.group({
      class: "apexcharts-pie-name-label-group"
    });
    if (connector.show) {
      const d = `M ${anchor.x} ${anchor.y} L ${elbow.x} ${elbow.y} L ${labelX} ${labelY}`;
      const line = graphics.drawPath({
        d,
        stroke: connector.color,
        strokeWidth: connector.width,
        fill: "none",
        strokeLinecap: "round"
      });
      line.node.classList.add("apexcharts-pie-label-connector");
      group.add(line);
    }
    const textX = side === "right" ? labelX + 4 : labelX - 4;
    const n2 = lines.length;
    const startY = labelY - (n2 - 1) * lineHeight / 2;
    const elText = graphics.drawText({
      x: textX,
      y: startY,
      text: n2 === 1 ? lines[0] : lines,
      textAnchor: side === "right" ? "start" : "end",
      fontSize: style.fontSize,
      fontFamily: style.fontFamily,
      fontWeight: style.fontWeight,
      foreColor,
      dominantBaseline: "central",
      cssClass: "apexcharts-pie-name-label"
    });
    if (n2 > 1) {
      const tspans = elText.node.getElementsByTagName("tspan");
      for (let li = 0; li < tspans.length; li++) {
        tspans[li].setAttribute("x", `${textX}`);
        tspans[li].setAttribute("dy", li === 0 ? "0" : `${lineHeight}`);
      }
    }
    group.add(elText);
    return group;
  }
}
const D2R$1 = Math.PI / 180;
const R2D$1 = 180 / Math.PI;
function arcPoint(cx, cy, radius, deg) {
  return {
    x: cx + radius * Math.cos((deg - 90) * D2R$1),
    y: cy + radius * Math.sin((deg - 90) * D2R$1)
  };
}
const xy = (p2) => `${p2.x} ${p2.y}`;
function roundedDonutSegmentPath({ cx, cy, rIn, rOut, a0, a1, r: r2, spanDeg }) {
  const ptAt = (radius, deg) => arcPoint(cx, cy, radius, deg);
  const degOut = r2 / rOut * R2D$1;
  const degIn = r2 / rIn * R2D$1;
  const oStart = ptAt(rOut, a0 + degOut);
  const oEnd = ptAt(rOut, a1 - degOut);
  const largeOut = spanDeg - 2 * degOut > 180 ? 1 : 0;
  const ocEnd = ptAt(rOut, a1);
  const rEndOut = ptAt(rOut - r2, a1);
  const ocStart = ptAt(rOut, a0);
  const rStartOut = ptAt(rOut - r2, a0);
  const iEnd = ptAt(rIn, a1 - degIn);
  const iStart = ptAt(rIn, a0 + degIn);
  const largeIn = spanDeg - 2 * degIn > 180 ? 1 : 0;
  const icEnd = ptAt(rIn, a1);
  const rEndIn = ptAt(rIn + r2, a1);
  const icStart = ptAt(rIn, a0);
  const rStartIn = ptAt(rIn + r2, a0);
  return [
    "M",
    xy(oStart),
    "A",
    rOut,
    rOut,
    0,
    largeOut,
    1,
    xy(oEnd),
    "Q",
    xy(ocEnd),
    xy(rEndOut),
    "L",
    xy(rEndIn),
    "Q",
    xy(icEnd),
    xy(iEnd),
    "A",
    rIn,
    rIn,
    0,
    largeIn,
    0,
    xy(iStart),
    "Q",
    xy(icStart),
    xy(rStartIn),
    "L",
    xy(rStartOut),
    "Q",
    xy(ocStart),
    xy(oStart),
    "Z"
  ].join(" ");
}
function roundedPieSegmentPath({ cx, cy, rOut, a0, a1, r: r2, spanDeg }) {
  const ptAt = (radius, deg) => arcPoint(cx, cy, radius, deg);
  const degOut = r2 / rOut * R2D$1;
  const oStart = ptAt(rOut, a0 + degOut);
  const oEnd = ptAt(rOut, a1 - degOut);
  const largeOut = spanDeg - 2 * degOut > 180 ? 1 : 0;
  const ocEnd = ptAt(rOut, a1);
  const rEndOut = ptAt(rOut - r2, a1);
  const ocStart = ptAt(rOut, a0);
  const rStartOut = ptAt(rOut - r2, a0);
  return [
    "M",
    xy(oStart),
    "A",
    rOut,
    rOut,
    0,
    largeOut,
    1,
    xy(oEnd),
    "Q",
    xy(ocEnd),
    xy(rEndOut),
    "L",
    `${cx} ${cy}`,
    "L",
    xy(rStartOut),
    "Q",
    xy(ocStart),
    xy(oStart),
    "Z"
  ].join(" ");
}
function sharpDonutSegmentPath({ cx, cy, rIn, rOut, a0, a1, spanDeg }) {
  const ptAt = (radius, deg) => arcPoint(cx, cy, radius, deg);
  const largeArc = spanDeg > 180 ? 1 : 0;
  const A = ptAt(rOut, a0);
  const B = ptAt(rOut, a1);
  const C = ptAt(rIn, a1);
  const Din = ptAt(rIn, a0);
  return [
    "M",
    xy(A),
    "A",
    rOut,
    rOut,
    0,
    largeArc,
    1,
    xy(B),
    "L",
    xy(C),
    "A",
    rIn,
    rIn,
    0,
    largeArc,
    0,
    xy(Din),
    "Z"
  ].join(" ");
}
const SLICE_OFFSET_TRANSITION = "transform 320ms cubic-bezier(0.25, 0.8, 0.3, 1)";
const HOVER_OUTLINE_TRANSITION = "opacity 180ms ease-out";
class Pie {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
    this.chartType = this.w.config.chart.type;
    this.initialAnim = this.w.config.chart.animations.enabled;
    this.dynamicAnim = this.initialAnim && this.w.config.chart.animations.dynamicAnimation.enabled;
    this.animBeginArr = [0];
    this.animDur = 0;
    this.donutDataLabels = this.w.config.plotOptions.pie.donut.labels;
    this.lineColorArr = w2.globals.stroke.colors !== void 0 ? w2.globals.stroke.colors : w2.globals.colors;
    this.defaultSize = Math.min(w2.layout.gridWidth, w2.layout.gridHeight);
    this.centerY = this.defaultSize / 2;
    this.centerX = w2.layout.gridWidth / 2;
    if (w2.config.chart.type === "radialBar") {
      this.fullAngle = 360;
    } else {
      this.fullAngle = Math.abs(
        w2.config.plotOptions.pie.endAngle - w2.config.plotOptions.pie.startAngle
      );
    }
    this.initialAngle = w2.config.plotOptions.pie.startAngle % this.fullAngle;
    w2.globals.radialSize = this.defaultSize / 2.05 - w2.config.stroke.width - (!w2.config.chart.sparkline.enabled ? w2.config.chart.dropShadow.blur : 0);
    this.externalCfg = w2.config.plotOptions.pie.dataLabels.external;
    const dlStyle = w2.config.dataLabels.style;
    this.externalLabelStyle = {
      fontSize: this.externalCfg.fontSize || dlStyle.fontSize,
      fontFamily: this.externalCfg.fontFamily || dlStyle.fontFamily,
      fontWeight: this.externalCfg.fontWeight || dlStyle.fontWeight
    };
    this.externalLabels = [];
    this.externalLabelMaxLines = 1;
    this.externalLabelLineH = parseFloat(this.externalLabelStyle.fontSize) || 12;
    w2.globals.pieExternalLabelMarginY = 0;
    this.showExternalLabels = this.externalCfg.show && this.chartType !== "polarArea";
    if (this.showExternalLabels && !w2.globals.noData) {
      this.reserveExternalLabelSpace();
    }
    this.donutSize = w2.globals.radialSize * parseInt(w2.config.plotOptions.pie.donut.size, 10) / 100;
    const scaleSize = w2.config.plotOptions.pie.customScale;
    const halfW = w2.layout.gridWidth / 2;
    const halfH = w2.layout.gridHeight / 2;
    this.translateX = halfW - halfW * scaleSize;
    this.translateY = halfH - halfH * scaleSize;
    this.dataLabelsGroup = new Graphics(this.w).group({
      class: "apexcharts-datalabels-group",
      transform: `translate(${this.translateX}, ${this.translateY}) scale(${scaleSize})`
    });
    this.maxY = 0;
    this.sliceLabels = [];
    this.sliceSizes = [];
    this.prevSliceSizes = [];
    this.sliceLabelGroups = {};
    this.externalLabelGroups = {};
    this.elHoverOutline = null;
    this.elHoverOutlinePath = null;
    this.hoverOutlineIndex = -1;
    this.prevSectorAngleArr = [];
  }
  /**
   * The text shown in an outer (name) label for slice `i`. Applies the
   * user `name.formatter` if provided, otherwise the raw series name. The
   * formatter may return a string or an array of strings (one per line, e.g.
   * `[name, percent]`); normalize via `getExternalLabelLines`.
   * @param {number} i
   * @returns {string | string[]}
   */
  getExternalLabelText(i2) {
    var _a, _b, _c;
    const w2 = this.w;
    const name2 = w2.seriesData.seriesNames[i2];
    const fn = this.externalCfg.formatter;
    if (typeof fn === "function") {
      return fn(name2, {
        seriesIndex: i2,
        percent: (_b = (_a = w2.globals.seriesPercent) == null ? void 0 : _a[i2]) == null ? void 0 : _b[0],
        value: (_c = w2.globals.seriesTotals) == null ? void 0 : _c[i2],
        w: w2
      });
    }
    return name2 == null ? "" : `${name2}`;
  }
  /**
   * Outer label content for slice `i` normalized to an array of line strings.
   * Supports a formatter returning an array, or a string with `\n` separators.
   * @param {number} i
   * @returns {string[]}
   */
  getExternalLabelLines(i2) {
    const raw = this.getExternalLabelText(i2);
    const arr = Array.isArray(raw) ? raw : `${raw == null ? "" : raw}`.split("\n");
    return arr.map((l2) => l2 == null ? "" : `${l2}`);
  }
  /**
   * Shrink the pie radius (and reposition its center) so outer name labels and
   * their connector lines fit inside the chart area without clipping. Stores
   * the reserved vertical band on `w.globals.pieExternalLabelMarginY` so
   * Core.resizeNonAxisCharts can grow the SVG height to match.
   */
  reserveExternalLabelSpace() {
    const w2 = this.w;
    const helpers = new CircularChartsHelpers(w2);
    const lineSets = (w2.seriesData.seriesNames || []).map(
      (_, i2) => this.getExternalLabelLines(i2)
    );
    const maxLabelWidth = helpers.getMaxLabelWidth(lineSets.flat(), {
      fontSize: this.externalLabelStyle.fontSize,
      fontFamily: this.externalLabelStyle.fontFamily
    });
    this.externalLabelMaxLines = lineSets.reduce((m2, s2) => Math.max(m2, s2.length), 1);
    this.externalLabelLineH = Math.round(
      (parseFloat(this.externalLabelStyle.fontSize) || 12) * 1.35
    );
    const cn = this.externalCfg.connector;
    const blockHeight = this.externalLabelMaxLines * this.externalLabelLineH;
    const mh = maxLabelWidth + (cn.length || 0) + (cn.gap || 0) + 12;
    const mv = blockHeight / 2 + (cn.gap || 0) + 6;
    const fitted = Math.min(
      w2.globals.radialSize,
      w2.layout.gridWidth / 2 - mh,
      w2.layout.gridHeight / 2 - mv
    );
    w2.globals.radialSize = Math.max(fitted, this.defaultSize * 0.15);
    w2.globals.pieExternalLabelMarginY = mv;
    const heightStr = w2.config.chart.height ? String(w2.config.chart.height) : "";
    const userSetFixedHeight = heightStr !== "" && heightStr !== "auto";
    this.centerY = userSetFixedHeight ? w2.layout.gridHeight / 2 : w2.globals.radialSize + mv;
  }
  /**
   * @param {any[]} series
   */
  draw(series) {
    var _a;
    const self = this;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const elPie = graphics.group({
      class: "apexcharts-pie"
    });
    if (w2.globals.noData) return elPie;
    let total = 0;
    for (let k = 0; k < series.length; k++) {
      total += Utils$1.negToZero(series[k]);
    }
    const sectorAngleArr = [];
    const elSeries = graphics.group();
    if (total === 0) {
      total = 1e-5;
    }
    series.forEach((m2) => {
      this.maxY = Math.max(this.maxY, m2);
    });
    if (w2.config.yaxis[0].max) {
      this.maxY = w2.config.yaxis[0].max;
    }
    if (w2.config.grid.position === "back" && this.chartType === "polarArea") {
      this.drawPolarElements(elPie);
    }
    const collapsedIdx = w2.globals.collapsedSeriesIndices || [];
    let polarVisible = 1;
    if (this.chartType === "polarArea") {
      let visible = 0;
      for (let k = 0; k < series.length; k++) {
        if (collapsedIdx.indexOf(k) === -1) visible++;
      }
      polarVisible = Math.max(1, visible);
    }
    for (let i2 = 0; i2 < series.length; i2++) {
      const angle = this.fullAngle * Utils$1.negToZero(series[i2]) / total;
      sectorAngleArr.push(angle);
      if (this.chartType === "polarArea") {
        sectorAngleArr[i2] = collapsedIdx.indexOf(i2) > -1 ? 0 : this.fullAngle / polarVisible;
        this.sliceSizes.push(
          w2.globals.radialSize * series[i2] / (this.maxY || 1)
        );
      } else {
        this.sliceSizes.push(w2.globals.radialSize);
      }
    }
    const morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
    if (w2.globals.dataChanged && !morphActive) {
      if (this.chartType === "polarArea") {
        const prevValues = w2.globals.previousPaths;
        const stash = w2.globals.prevPolarAngles;
        if (Array.isArray(stash) && stash.length === prevValues.length) {
          this.prevSectorAngleArr = stash.slice();
        } else {
          for (let i2 = 0; i2 < prevValues.length; i2++) {
            this.prevSectorAngleArr.push(
              this.fullAngle / Math.max(1, prevValues.length)
            );
          }
        }
        let prevMaxY = 0;
        for (let k = 0; k < prevValues.length; k++) {
          prevMaxY = Math.max(prevMaxY, Utils$1.negToZero(prevValues[k]));
        }
        if (w2.config.yaxis[0].max) {
          prevMaxY = w2.config.yaxis[0].max;
        }
        this.prevSliceSizes = prevValues.map(
          (v2) => w2.globals.radialSize * Utils$1.negToZero(v2) / (prevMaxY || 1)
        );
      } else {
        let prevTotal = 0;
        for (let k = 0; k < w2.globals.previousPaths.length; k++) {
          prevTotal += Utils$1.negToZero(w2.globals.previousPaths[k]);
        }
        let previousAngle;
        for (let i2 = 0; i2 < w2.globals.previousPaths.length; i2++) {
          previousAngle = this.fullAngle * Utils$1.negToZero(w2.globals.previousPaths[i2]) / prevTotal;
          this.prevSectorAngleArr.push(previousAngle);
        }
      }
    }
    if (this.chartType === "polarArea") {
      w2.globals.prevPolarAngles = sectorAngleArr.slice();
    }
    if (this.donutSize < 0) {
      this.donutSize = 0;
    }
    if (this.chartType === "donut") {
      const circle = graphics.drawCircle(this.donutSize);
      circle.attr({
        cx: this.centerX,
        cy: this.centerY,
        fill: w2.config.plotOptions.pie.donut.background ? w2.config.plotOptions.pie.donut.background : "transparent"
      });
      elSeries.add(circle);
    }
    const elG = self.drawArcs(sectorAngleArr, series);
    this.sliceLabels.forEach((s2) => {
      elG.add(s2);
    });
    elSeries.attr({
      transform: `translate(${this.translateX}, ${this.translateY}) scale(${w2.config.plotOptions.pie.customScale})`
    });
    elSeries.add(elG);
    elPie.add(elSeries);
    if (this.donutDataLabels.show) {
      const shouldFadeInLabels = this.initialAnim && !w2.globals.resized && !w2.globals.dataChanged && this.animDur > 0;
      const dataLabels = this.renderInnerDataLabels(
        this.dataLabelsGroup,
        this.donutDataLabels,
        {
          hollowSize: this.donutSize,
          centerX: this.centerX,
          centerY: this.centerY,
          opacity: shouldFadeInLabels ? 0 : this.donutDataLabels.show
        }
      );
      if (shouldFadeInLabels) {
        const labelsNode = this.dataLabelsGroup.node;
        labelsNode.style.transition = "opacity 280ms ease-out";
        setTimeout(() => {
          labelsNode.style.opacity = "1";
        }, this.animDur);
      }
      elPie.add(dataLabels);
    }
    if (w2.config.grid.position === "front" && this.chartType === "polarArea") {
      this.drawPolarElements(elPie);
    }
    return elPie;
  }
  // core function for drawing pie arcs
  /**
   * @param {any[]} sectorAngleArr
   * @param {any[]} series
   */
  drawArcs(sectorAngleArr, series) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const filters = new Filters(this.w);
    const graphics = new Graphics(this.w);
    const fill = new Fill(this.w);
    const g2 = graphics.group({
      class: "apexcharts-slices"
    });
    this.elHoverOutline = graphics.group({
      class: "apexcharts-pie-hover-outline"
    });
    this.elHoverOutline.node.style.pointerEvents = "none";
    this.elHoverOutline.node.style.opacity = "0";
    if (w2.config.chart.animations.enabled) {
      this.elHoverOutline.node.style.transition = HOVER_OUTLINE_TRANSITION;
    }
    g2.add(this.elHoverOutline);
    let startAngle = this.initialAngle;
    let prevStartAngle = this.initialAngle;
    let endAngle = this.initialAngle;
    let prevEndAngle = this.initialAngle;
    this.strokeWidth = w2.config.stroke.show ? w2.config.stroke.width : 0;
    const morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
    for (let i2 = 0; i2 < sectorAngleArr.length; i2++) {
      const elPieArc = graphics.group({
        class: `apexcharts-series apexcharts-pie-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[i2]),
        rel: i2 + 1,
        "data:realIndex": i2
      });
      g2.add(elPieArc);
      startAngle = endAngle;
      prevStartAngle = prevEndAngle;
      endAngle = startAngle + sectorAngleArr[i2];
      prevEndAngle = prevStartAngle + this.prevSectorAngleArr[i2];
      const angle = endAngle < startAngle ? this.fullAngle + endAngle - startAngle : endAngle - startAngle;
      const pathFill = fill.fillPath({
        seriesNumber: i2,
        size: this.sliceSizes[i2],
        value: series[i2]
      });
      const morphFrom = morphActive ? this.ctx.morphTypeChange.getInitialPathFor(i2, 0) : null;
      const prevSize = this.chartType === "polarArea" && w2.globals.dataChanged && !morphActive ? this.prevSliceSizes[i2] || 0 : void 0;
      const path = morphFrom || this.getChangedPath(prevStartAngle, prevEndAngle, prevSize);
      const elPath = graphics.drawPath({
        d: path,
        // Pie/donut/polarArea data is a single series, so a user-supplied
        // `stroke.colors` shorter than the slice count is NOT padded by the
        // theme engine (unlike fill colors, which cycle). Without this, only
        // slice 0 gets the requested color and the rest fall back to a grey
        // default. Cycle the array — matching fill-color behaviour — so a
        // single `stroke.colors: ['#fff']` borders every slice as expected.
        stroke: Array.isArray(this.lineColorArr) ? (_b = this.lineColorArr[i2]) != null ? _b : this.lineColorArr[i2 % this.lineColorArr.length] : this.lineColorArr,
        strokeWidth: 0,
        fill: pathFill,
        fillOpacity: w2.config.fill.opacity,
        classes: `apexcharts-pie-area apexcharts-${this.chartType.toLowerCase()}-slice-${i2}`
      });
      elPath.attr({
        index: 0,
        j: i2
      });
      filters.setSelectionFilter(elPath, 0, i2);
      if (w2.config.chart.dropShadow.enabled) {
        const shadow = w2.config.chart.dropShadow;
        filters.dropShadow(elPath, shadow, i2);
      }
      this.addListeners(elPath, this.donutDataLabels, i2);
      let labelPosition = {
        x: 0,
        y: 0
      };
      const midAngle = (startAngle + angle / 2) % this.fullAngle;
      let arcCenter = { x: this.centerX, y: this.centerY };
      if (this.chartType === "pie" || this.chartType === "polarArea") {
        labelPosition = Utils$1.polarToCartesian(
          this.centerX,
          this.centerY,
          w2.globals.radialSize / 1.25 + w2.config.plotOptions.pie.dataLabels.offset,
          midAngle
        );
        arcCenter = Utils$1.polarToCartesian(
          this.centerX,
          this.centerY,
          w2.globals.radialSize / 2,
          midAngle
        );
      } else if (this.chartType === "donut") {
        labelPosition = Utils$1.polarToCartesian(
          this.centerX,
          this.centerY,
          (w2.globals.radialSize + this.donutSize) / 2 + w2.config.plotOptions.pie.dataLabels.offset,
          midAngle
        );
        arcCenter = Utils$1.polarToCartesian(
          this.centerX,
          this.centerY,
          (w2.globals.radialSize + this.donutSize) / 2,
          midAngle
        );
      }
      Graphics.setAttrs(elPath.node, {
        "data:angle": angle,
        "data:startAngle": startAngle,
        "data:strokeWidth": this.strokeWidth,
        "data:value": series[i2],
        "data:cx": arcCenter.x,
        "data:cy": arcCenter.y
      });
      elPieArc.add(elPath);
      let pieceClaimed = false;
      if (morphActive) {
        const finalD = this.getPiePath({
          me: this,
          startAngle,
          angle,
          size: this.sliceSizes[i2]
        });
        elPath.node.setAttribute("data:pathFinal", finalD);
        pieceClaimed = ((_d = (_c = this.ctx.morphTypeChange).claimsTargetMark) == null ? void 0 : _d.call(_c, i2, 0)) === true;
        if (pieceClaimed) {
          elPath.attr({ d: finalD });
          elPath.node.setAttribute("data:pathOrig", finalD);
          elPath.node.setAttribute("opacity", "0");
          elPath.node.setAttribute("data-piece-hidden", "1");
        }
      }
      let dur = 0;
      if (this.initialAnim && !w2.globals.resized && !w2.globals.dataChanged) {
        dur = angle / this.fullAngle * w2.config.chart.animations.speed;
        if (dur === 0) dur = 1;
        this.animDur = dur + this.animDur;
        this.animBeginArr.push(this.animDur);
      } else {
        this.animBeginArr.push(0);
      }
      if (pieceClaimed) ;
      else if (morphActive && morphFrom) {
        const targetD = this.getPiePath({
          me: this,
          startAngle,
          angle,
          size: this.sliceSizes[i2]
        });
        const morphSpeed = this.ctx.morphTypeChange.getSpeed();
        const animations = this.ctx.animations;
        elPath.node.setAttribute("data:pathOrig", targetD);
        const morphRunner = elPath.animate(morphSpeed).plot(targetD, "polygons").attr({ "stroke-width": this.strokeWidth });
        if (morphRunner && typeof morphRunner.after === "function") {
          morphRunner.after(() => animations.animationCompleted(elPath));
        } else {
          animations.animationCompleted(elPath);
        }
      } else if (this.dynamicAnim && w2.globals.dataChanged) {
        this.animatePaths(elPath, {
          size: this.sliceSizes[i2],
          prevSize,
          endAngle,
          startAngle,
          prevStartAngle,
          prevEndAngle,
          animateStartingPos: true,
          i: i2,
          animBeginArr: this.animBeginArr,
          shouldSetPrevPaths: true,
          dur: w2.config.chart.animations.dynamicAnimation.speed
        });
      } else {
        this.animatePaths(elPath, {
          size: this.sliceSizes[i2],
          endAngle,
          startAngle,
          i: i2,
          totalItems: sectorAngleArr.length - 1,
          animBeginArr: this.animBeginArr,
          dur
        });
      }
      if (this.getExpandOffset() > 0) {
        elPath.node.addEventListener("mouseup", this.pieClicked.bind(this, i2));
      } else if (i2 === 0 && Filters.drilldownBlocksSliceOffset(w2)) {
        (_e = this.ctx.drilldown) == null ? void 0 : _e.warnSliceOffsetDisabled();
      }
      if (typeof w2.interact.selectedDataPoints[0] !== "undefined" && w2.interact.selectedDataPoints[0].indexOf(i2) > -1) {
        if (this.initialAnim && !w2.globals.resized && !w2.globals.dataChanged && this.animDur > 0) {
          const _this = this;
          const _i = i2;
          setTimeout(() => _this.pieClicked(_i, { animate: false }), this.animDur);
        } else {
          this.pieClicked(i2, { animate: false });
        }
      }
      if (w2.config.dataLabels.enabled) {
        const xPos = labelPosition.x;
        const yPos = labelPosition.y;
        let text = 100 * angle / this.fullAngle + "%";
        if (angle !== 0 && w2.config.plotOptions.pie.dataLabels.minAngleToShowLabel < sectorAngleArr[i2]) {
          const formatter = w2.config.dataLabels.formatter;
          if (formatter !== void 0) {
            text = formatter(w2.globals.seriesPercent[i2][0], {
              seriesIndex: i2,
              w: w2
            });
          }
          const foreColor = w2.globals.dataLabels.style.colors[i2];
          const elPieLabelWrap = graphics.group({
            class: `apexcharts-datalabels`
          });
          const elPieLabel = graphics.drawText({
            x: xPos,
            y: yPos,
            text,
            textAnchor: "middle",
            fontSize: w2.config.dataLabels.style.fontSize,
            fontFamily: w2.config.dataLabels.style.fontFamily,
            fontWeight: w2.config.dataLabels.style.fontWeight,
            foreColor
          });
          elPieLabelWrap.add(elPieLabel);
          if (w2.config.dataLabels.dropShadow.enabled) {
            const textShadow = w2.config.dataLabels.dropShadow;
            filters.dropShadow(elPieLabel, textShadow);
          }
          elPieLabel.node.classList.add("apexcharts-pie-label");
          if (w2.config.chart.animations.animate && w2.globals.resized === false) {
            elPieLabel.node.classList.add("apexcharts-pie-label-delay");
            elPieLabel.node.style.animationDelay = w2.config.chart.animations.speed / 940 + "s";
          }
          this.sliceLabels.push(elPieLabelWrap);
          this.sliceLabelGroups[i2] = elPieLabelWrap.node;
        }
      }
      if (this.showExternalLabels && angle !== 0) {
        const lines = this.getExternalLabelLines(i2);
        if (lines.some((l2) => l2 !== "")) {
          const anchor = Utils$1.polarToCartesian(
            this.centerX,
            this.centerY,
            w2.globals.radialSize,
            midAngle
          );
          const elbow = Utils$1.polarToCartesian(
            this.centerX,
            this.centerY,
            w2.globals.radialSize + (this.externalCfg.connector.gap || 0),
            midAngle
          );
          const isRight = elbow.x >= this.centerX;
          const baseLabelX = isRight ? elbow.x + (this.externalCfg.connector.length || 0) : elbow.x - (this.externalCfg.connector.length || 0);
          this.externalLabels.push({
            i: i2,
            lines,
            anchor,
            elbow,
            side: isRight ? "right" : "left",
            labelX: baseLabelX + parseFloat(this.externalCfg.offsetX || 0),
            idealY: elbow.y + parseFloat(this.externalCfg.offsetY || 0),
            connectorColor: this.externalCfg.connector.color || w2.globals.colors[i2],
            foreColor: this.externalCfg.color || w2.config.chart.foreColor
          });
        }
      }
    }
    if (this.showExternalLabels && this.externalLabels.length) {
      this.placeExternalLabels();
      const revealOnAnimEnd = Environment.isBrowser() && (morphActive || this.dynamicAnim && w2.globals.dataChanged || this.initialAnim && !w2.globals.resized && !w2.globals.dataChanged);
      this.externalLabels.forEach((lbl) => {
        const group = new CircularChartsHelpers(w2).drawExternalLabel({
          lines: lbl.lines,
          lineHeight: this.externalLabelLineH,
          anchor: lbl.anchor,
          elbow: lbl.elbow,
          labelX: lbl.labelX,
          labelY: lbl.labelY,
          side: lbl.side,
          connector: {
            show: this.externalCfg.connector.show,
            width: this.externalCfg.connector.width,
            color: lbl.connectorColor
          },
          style: this.externalLabelStyle,
          foreColor: lbl.foreColor
        });
        if (revealOnAnimEnd) {
          group.node.classList.add("apexcharts-element-hidden");
          w2.globals.delayedElements.push({ el: group.node });
        }
        this.externalLabelGroups[lbl.i] = group.node;
        g2.add(group);
      });
    }
    return g2;
  }
  /**
   * Vertical de-overlap for outer (name) labels: per side, sort by ideal y and
   * push neighbours apart so they keep at least one line-height of spacing.
   * Mutates each entry's `labelY`. Connector lines re-route to the moved y.
   */
  placeExternalLabels() {
    const w2 = this.w;
    const lineHeight = this.externalLabelMaxLines * this.externalLabelLineH + 2;
    const maxY = this.centerY + w2.globals.radialSize + w2.globals.pieExternalLabelMarginY;
    ["left", "right"].forEach((side) => {
      const items = this.externalLabels.filter((l2) => l2.side === side).sort((a2, b2) => a2.idealY - b2.idealY);
      items.forEach((l2) => {
        l2.labelY = l2.idealY;
      });
      for (let k = 1; k < items.length; k++) {
        if (items[k].labelY - items[k - 1].labelY < lineHeight) {
          items[k].labelY = items[k - 1].labelY + lineHeight;
        }
      }
      const last = items[items.length - 1];
      const overflow = last ? last.labelY - maxY : 0;
      if (overflow > 0) {
        for (let k = items.length - 1; k >= 0; k--) {
          items[k].labelY -= overflow;
          if (k < items.length - 1 && items[k + 1].labelY - items[k].labelY < lineHeight) {
            items[k].labelY = items[k + 1].labelY - lineHeight;
          }
        }
      }
    });
  }
  /**
   * @param {any} elPath
   * @param {Record<string, any>} dataLabels
   * @param {number} [i] slice index, for the hover outline band
   */
  addListeners(elPath, dataLabels, i2) {
    const graphics = new Graphics(this.w, this.ctx);
    elPath.node.addEventListener(
      "mouseenter",
      graphics.pathMouseEnter.bind(graphics, elPath)
    );
    elPath.node.addEventListener(
      "mouseleave",
      graphics.pathMouseLeave.bind(graphics, elPath)
    );
    elPath.node.addEventListener(
      "mouseleave",
      this.revertDataLabelsInner.bind(this)
    );
    if (typeof i2 === "number") {
      elPath.node.addEventListener(
        "mouseenter",
        this.showHoverOutline.bind(this, i2)
      );
      elPath.node.addEventListener(
        "mouseleave",
        this.hideHoverOutline.bind(this)
      );
    }
    elPath.node.addEventListener(
      "mousedown",
      graphics.pathMouseDown.bind(graphics, elPath)
    );
    if (!this.donutDataLabels.total.showAlways) {
      elPath.node.addEventListener(
        "mouseenter",
        this.printDataLabelsInner.bind(this, elPath.node, dataLabels)
      );
      elPath.node.addEventListener(
        "mousedown",
        this.printDataLabelsInner.bind(this, elPath.node, dataLabels)
      );
    }
  }
  // This function can be used for other circle charts too
  /**
   * @param {any} el
   * @param {Record<string, any>} opts
   */
  animatePaths(el, opts) {
    const w2 = this.w;
    const me = this;
    let angle = opts.endAngle < opts.startAngle ? this.fullAngle + opts.endAngle - opts.startAngle : opts.endAngle - opts.startAngle;
    let prevAngle = angle;
    let fromStartAngle = opts.startAngle;
    const toStartAngle = opts.startAngle;
    if (opts.prevStartAngle !== void 0 && opts.prevEndAngle !== void 0) {
      fromStartAngle = opts.prevEndAngle;
      prevAngle = opts.prevEndAngle < opts.prevStartAngle ? this.fullAngle + opts.prevEndAngle - opts.prevStartAngle : opts.prevEndAngle - opts.prevStartAngle;
    }
    if (opts.i === w2.config.series.length - 1) {
      if (angle + toStartAngle > this.fullAngle) {
        opts.endAngle = opts.endAngle - (angle + toStartAngle);
      } else if (angle + toStartAngle < this.fullAngle) {
        opts.endAngle = opts.endAngle + (this.fullAngle - (angle + toStartAngle));
      }
    }
    if (angle === this.fullAngle) angle = this.fullAngle - 0.01;
    me.animateArc(el, fromStartAngle, toStartAngle, angle, prevAngle, opts);
  }
  /**
   * @param {any} el
   * @param {number} fromStartAngle
   * @param {number} toStartAngle
   * @param {number} angle
   * @param {number} prevAngle
   * @param {Record<string, any>} opts
   */
  animateArc(el, fromStartAngle, toStartAngle, angle, prevAngle, opts) {
    const me = this;
    const w2 = this.w;
    const animations = new Animations(this.w);
    const size = opts.size;
    let path;
    if (isNaN(fromStartAngle) || isNaN(prevAngle)) {
      fromStartAngle = toStartAngle;
      prevAngle = angle;
      opts.dur = 0;
    }
    let currAngle = angle;
    let startAngle = toStartAngle;
    const fromAngle = fromStartAngle < toStartAngle ? this.fullAngle + fromStartAngle - toStartAngle : fromStartAngle - toStartAngle;
    const hasPrevSize = typeof opts.prevSize === "number";
    if (w2.globals.dataChanged && opts.shouldSetPrevPaths) {
      if (opts.prevEndAngle) {
        path = me.getPiePath({
          me,
          startAngle: opts.prevStartAngle,
          angle: opts.prevEndAngle < opts.prevStartAngle ? this.fullAngle + opts.prevEndAngle - opts.prevStartAngle : opts.prevEndAngle - opts.prevStartAngle,
          size: hasPrevSize ? opts.prevSize : size
        });
        el.attr({ d: path });
      }
    }
    if (opts.dur !== 0) {
      el.animate(opts.dur, opts.animBeginArr[opts.i]).after(
        /** @this {any} */
        function() {
          if (me.chartType === "pie" || me.chartType === "donut" || me.chartType === "polarArea") {
            this.animate(
              w2.config.chart.animations.dynamicAnimation.speed
            ).attr({
              "stroke-width": me.strokeWidth
            });
          }
          if (opts.i === w2.config.series.length - 1) {
            animations.animationCompleted(el);
          }
        }
      ).during((pos) => {
        currAngle = fromAngle + (angle - fromAngle) * pos;
        if (opts.animateStartingPos) {
          currAngle = prevAngle + (angle - prevAngle) * pos;
          startAngle = fromStartAngle - prevAngle + (toStartAngle - (fromStartAngle - prevAngle)) * pos;
        }
        path = me.getPiePath({
          me,
          startAngle,
          angle: currAngle,
          size: hasPrevSize ? opts.prevSize + (size - opts.prevSize) * pos : size
        });
        el.node.setAttribute("data:pathOrig", path);
        el.attr({
          d: path
        });
      });
    } else {
      path = me.getPiePath({
        me,
        startAngle,
        angle,
        size
      });
      if (!opts.isTrack) {
        w2.globals.animationEnded = true;
      }
      el.node.setAttribute("data:pathOrig", path);
      el.attr({
        d: path,
        "stroke-width": me.strokeWidth
      });
    }
  }
  /**
   * Toggle slice `i` in or out of the pie. Only one slice sits outside at a
   * time. Bound to `mouseup` on each slice, and also reached from the
   * `toggleDataPointSelection` API and from the pre-selected-slice pass in
   * drawArcs (which passes `animate: false` so the slice is already parked by
   * the time the first frame is painted).
   * @param {number} i
   * @param {{animate?: boolean}} [opts] a MouseEvent when called as a listener,
   *   which carries no `animate`, so real clicks animate
   */
  pieClicked(i2, opts) {
    const w2 = this.w;
    const me = this;
    const animate = !(opts && opts.animate === false);
    const elPath = w2.dom.Paper.findOne(
      `.apexcharts-${me.chartType.toLowerCase()}-slice-${i2}`
    );
    if (!elPath) return;
    if (elPath.attr("data:pieClicked") === "true") {
      elPath.attr({
        "data:pieClicked": "false"
      });
      this.revertDataLabelsInner();
      this.offsetSlice(i2, 0, animate);
      return;
    }
    const allEls = w2.dom.baseEl.getElementsByClassName("apexcharts-pie-area");
    Array.prototype.forEach.call(allEls, (pieSlice) => {
      const wasOut = pieSlice.getAttribute("data:pieClicked") === "true";
      pieSlice.setAttribute("data:pieClicked", "false");
      if (wasOut) {
        this.offsetSlice(parseInt(pieSlice.getAttribute("j"), 10), 0, animate);
      }
    });
    w2.interact.capturedDataPointIndex = i2;
    elPath.attr("data:pieClicked", "true");
    this.offsetSlice(i2, this.getExpandOffset(), animate);
  }
  /**
   * How far a clicked slice slides out, in px. 0 when the pull-out is off, when
   * drilldown owns the click, and always for polarArea: there the radius
   * encodes the value, so moving a slice outward would read as a bigger number.
   * @returns {number}
   */
  getExpandOffset() {
    const pie = this.w.config.plotOptions.pie;
    if (!pie.expandOnClick || this.chartType === "polarArea") return 0;
    if (Filters.drilldownBlocksSliceOffset(this.w)) return 0;
    const offset = Number(pie.expandOffset);
    return Number.isFinite(offset) && offset > 0 ? offset : 0;
  }
  /**
   * Every node that has to travel with slice `i`: the slice path itself plus
   * its labels, which live in sibling groups so they paint above all slices.
   * @param {number} i
   * @returns {SVGElement[]}
   */
  getSliceMovers(i2) {
    const elPath = this.w.dom.Paper.findOne(
      `.apexcharts-${this.chartType.toLowerCase()}-slice-${i2}`
    );
    return [
      elPath ? elPath.node : null,
      this.sliceLabelGroups[i2],
      this.externalLabelGroups[i2],
      // The hover band, when it is this slice's: clicking a slice you are
      // hovering has to take its outline along, or the band is left behind
      // sitting in the gap the slice just opened.
      this.hoverOutlineIndex === i2 && this.elHoverOutlinePath ? this.elHoverOutlinePath.node : null
    ].filter(Boolean);
  }
  /**
   * Slide slice `i` `dist` px out of the pie along its own mid-angle, or back
   * to its resting place when `dist` is 0.
   *
   * The slice is *translated*, never re-drawn: the arc keeps the exact radius
   * and span it had, so the pulled-out slice still encodes the same quantity
   * (growing the radius, as this used to, quietly inflates it) and a clean gap
   * opens between it and the rest of the pie.
   * @param {number} i
   * @param {number} dist
   * @param {boolean} [animate] false to park it instantly, with no transition
   */
  offsetSlice(i2, dist, animate = true) {
    const w2 = this.w;
    const elPath = w2.dom.Paper.findOne(
      `.apexcharts-${this.chartType.toLowerCase()}-slice-${i2}`
    );
    if (!elPath) return;
    const { dx, dy } = this.getSliceOffsetVector(i2, dist);
    const transform = `translate(${dx} ${dy})`;
    const transition = animate && w2.config.chart.animations.enabled ? SLICE_OFFSET_TRANSITION : "";
    this.getSliceMovers(i2).forEach((node) => {
      node.style.transition = transition;
      node.setAttribute("transform", transform);
    });
  }
  /**
   * The px vector `dist` along slice `i`'s mid-angle. Zero for a slice that
   * fills the pie: there is no "outside" for it to move to, and sliding it
   * would just shift the whole chart sideways.
   * @param {number} i
   * @param {number} dist
   * @returns {{dx: number, dy: number}}
   */
  getSliceOffsetVector(i2, dist) {
    const elPath = this.w.dom.Paper.findOne(
      `.apexcharts-${this.chartType.toLowerCase()}-slice-${i2}`
    );
    const angle = elPath ? parseFloat(elPath.attr("data:angle")) : NaN;
    if (!dist || !Number.isFinite(angle) || angle >= this.fullAngle) {
      return { dx: 0, dy: 0 };
    }
    const startAngle = parseFloat(elPath.attr("data:startAngle"));
    const midRad = Math.PI * (startAngle + angle / 2 - 90) / 180;
    return { dx: dist * Math.cos(midRad), dy: dist * Math.sin(midRad) };
  }
  /**
   * Fade in the hover outline: a translucent band traced just outside the rim
   * of slice `i`, in the slice's own colour. It replaces lightening the slice
   * (see Filters.hoverOutlineOwnsHoverState) so a hovered slice keeps the
   * colour the legend and the data labels claim it has.
   * @param {number} i
   */
  showHoverOutline(i2) {
    const w2 = this.w;
    if (!Filters.hoverOutlineOwnsHoverState(w2)) return;
    if (!this.elHoverOutline) return;
    const path = this.getHoverOutlinePath(i2);
    if (!path) return;
    const cfg = w2.config.plotOptions.pie.hoverOutline;
    const fill = cfg.color || w2.globals.colors[i2];
    if (!this.elHoverOutlinePath) {
      const graphics = new Graphics(w2);
      this.elHoverOutlinePath = graphics.drawPath({
        d: path,
        fill,
        strokeWidth: 0,
        classes: "apexcharts-pie-hover-outline-band"
      });
      this.elHoverOutline.add(this.elHoverOutlinePath);
    }
    this.elHoverOutlinePath.attr({
      d: path,
      fill,
      "fill-opacity": cfg.opacity
    });
    const { dx, dy } = this.getSliceOffsetVector(
      i2,
      this.isSliceOut(i2) ? this.getExpandOffset() : 0
    );
    this.elHoverOutlinePath.node.style.transition = "";
    this.elHoverOutlinePath.node.setAttribute("transform", `translate(${dx} ${dy})`);
    this.hoverOutlineIndex = i2;
    this.elHoverOutline.node.style.opacity = "1";
  }
  /** Fade the hover outline back out, leaving the band node in place. */
  hideHoverOutline() {
    if (this.elHoverOutline) {
      this.elHoverOutline.node.style.opacity = "0";
    }
  }
  /** @param {number} i @returns {boolean} */
  isSliceOut(i2) {
    const elPath = this.w.dom.Paper.findOne(
      `.apexcharts-${this.chartType.toLowerCase()}-slice-${i2}`
    );
    return !!elPath && elPath.attr("data:pieClicked") === "true";
  }
  /**
   * Band geometry for the hover outline of slice `i`: an annulus from the
   * slice rim (plus the stroke and the configured clearance) outward, over the
   * same angular extent the slice is actually drawn over, so it lines up with
   * both slice edges even with `spacing` insetting them. Rounded into a pill
   * when the band is thick enough for the fillets to fit.
   * @param {number} i
   * @returns {string | null}
   */
  getHoverOutlinePath(i2) {
    const w2 = this.w;
    const cfg = w2.config.plotOptions.pie.hoverOutline;
    const elPath = w2.dom.Paper.findOne(
      `.apexcharts-${this.chartType.toLowerCase()}-slice-${i2}`
    );
    if (!elPath) return null;
    const angle = parseFloat(elPath.attr("data:angle"));
    if (!Number.isFinite(angle) || angle <= 0) return null;
    const size = this.sliceSizes[i2];
    const thickness = Number(cfg.size);
    if (!Number.isFinite(size) || !Number.isFinite(thickness) || thickness <= 0) {
      return null;
    }
    const { startDeg, spanDeg } = this.getSliceExtent({
      me: this,
      startAngle: parseFloat(elPath.attr("data:startAngle")),
      angle,
      size
    });
    if (!(spanDeg > 0)) return null;
    const rIn = size + (this.strokeWidth || 0) / 2 + (Number(cfg.gap) || 0);
    const rOut = rIn + thickness;
    const geo = {
      cx: this.centerX,
      cy: this.centerY,
      rIn,
      rOut,
      a0: startDeg,
      a1: startDeg + spanDeg,
      spanDeg
    };
    const r2 = Math.min(thickness / 2, spanDeg * Math.PI / 180 / 2 * rIn);
    return r2 > 0.5 ? roundedDonutSegmentPath(__spreadProps(__spreadValues({}, geo), { r: r2 })) : sharpDonutSegmentPath(geo);
  }
  /**
   * @param {number} prevStartAngle
   * @param {number} prevEndAngle
   */
  /**
   * @param {number} prevStartAngle
   * @param {number} prevEndAngle
   * @param {number} [prevSize] - polarArea passes its previous slice radius,
   *   so the pre-animation frame sits at the previous VALUE too, not just the
   *   previous angles.
   */
  getChangedPath(prevStartAngle, prevEndAngle, prevSize) {
    let path = "";
    if (this.dynamicAnim && this.w.globals.dataChanged) {
      path = this.getPiePath({
        me: this,
        startAngle: prevStartAngle,
        angle: prevEndAngle - prevStartAngle,
        // @ts-ignore — size is set dynamically during draw()
        size: typeof prevSize === "number" ? prevSize : this.size
      });
    }
    return path;
  }
  /**
   * The angular extent a slice is actually drawn over: the raw start / span
   * clamped so a full circle never overlaps itself, then inset by
   * `plotOptions.pie.spacing`. Shared by getPiePath and the hover outline, so
   * the band lines up with the slice edges instead of with the un-inset angles
   * cached on the path node.
   * @param {{me: any, startAngle: number, angle: number, size: number}} opts
   * @returns {{startDeg: number, spanDeg: number, endDeg: number}}
   */
  getSliceExtent({ me, startAngle, angle, size }) {
    const w2 = this.w;
    let startDeg = startAngle;
    let endDeg = angle + startAngle;
    if (Math.ceil(endDeg) >= this.fullAngle + this.w.config.plotOptions.pie.startAngle % this.fullAngle) {
      endDeg = this.fullAngle + this.w.config.plotOptions.pie.startAngle % this.fullAngle - 0.01;
    }
    let spanDeg = endDeg - startDeg;
    const isSliceType = me.chartType === "pie" || me.chartType === "donut" || me.chartType === "polarArea";
    const spacing = w2.config.plotOptions.pie.spacing;
    if (isSliceType && spacing > 0 && spanDeg > 0) {
      const rRef = me.chartType === "donut" ? (size + me.donutSize) / 2 : size;
      const gapDeg = rRef > 0 ? spacing / rRef * (180 / Math.PI) : 0;
      const inset = Math.min(gapDeg / 2, Math.max(0, spanDeg / 2 - 0.5));
      startDeg += inset;
      spanDeg -= 2 * inset;
    }
    endDeg = startDeg + spanDeg;
    if (Math.ceil(endDeg) > this.fullAngle) endDeg -= this.fullAngle;
    return { startDeg, spanDeg, endDeg };
  }
  /** @param {{me: any, startAngle: any, angle: any, size: any}} opts */
  getPiePath({ me, startAngle, angle, size }) {
    let path;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const { startDeg, spanDeg, endDeg } = this.getSliceExtent({
      me,
      startAngle,
      angle,
      size
    });
    const isSliceType = me.chartType === "pie" || me.chartType === "donut" || me.chartType === "polarArea";
    const startRadians = Math.PI * (startDeg - 90) / 180;
    const borderRadius = w2.config.plotOptions.pie.borderRadius;
    if (borderRadius > 0 && isSliceType) {
      const roundedPath = this.getRoundedSlicePath({
        me,
        startDeg,
        spanDeg,
        size,
        borderRadius
      });
      if (roundedPath) return roundedPath;
    }
    const endRadians = Math.PI * (endDeg - 90) / 180;
    const x1 = me.centerX + size * Math.cos(startRadians);
    const y1 = me.centerY + size * Math.sin(startRadians);
    const x2 = me.centerX + size * Math.cos(endRadians);
    const y2 = me.centerY + size * Math.sin(endRadians);
    const startInner = Utils$1.polarToCartesian(
      me.centerX,
      me.centerY,
      me.donutSize,
      endDeg
    );
    const endInner = Utils$1.polarToCartesian(
      me.centerX,
      me.centerY,
      me.donutSize,
      startDeg
    );
    const largeArc = spanDeg > 180 ? 1 : 0;
    const pathBeginning = ["M", x1, y1, "A", size, size, 0, largeArc, 1, x2, y2];
    if (me.chartType === "donut") {
      path = [
        ...pathBeginning,
        "L",
        startInner.x,
        startInner.y,
        "A",
        me.donutSize,
        me.donutSize,
        0,
        largeArc,
        0,
        endInner.x,
        endInner.y,
        "L",
        x1,
        y1,
        "z"
      ].join(" ");
    } else if (me.chartType === "pie" || me.chartType === "polarArea") {
      path = [...pathBeginning, "L", me.centerX, me.centerY, "L", x1, y1].join(
        " "
      );
    } else {
      path = [...pathBeginning].join(" ");
    }
    return graphics.roundPathCorners(path, this.strokeWidth * 2);
  }
  /**
   * Build a slice path with rounded corners (plotOptions.pie.borderRadius).
   *
   * The generic roundPathCorners() only rounds line->line joins, but a slice
   * corner is an arc<->line join, so we construct the fillets explicitly here:
   * every corner is inset by the (clamped) radius along both the arc and the
   * radial edge, and a quadratic Bezier with its control point at the original
   * sharp corner bridges the two inset points. Donut slices round all four
   * corners; pie / polarArea slices round the two outer corners and keep the
   * center apex sharp.
   *
   * Returns null when the slice is too small to round meaningfully, so the
   * caller can fall back to a sharp-corner path.
   *
   * @param {{me: any, startDeg: number, spanDeg: number, size: number, borderRadius: number}} opts
   * @returns {string | null}
   */
  getRoundedSlicePath({ me, startDeg, spanDeg, size, borderRadius }) {
    if (!(spanDeg > 0)) return null;
    const D2R2 = Math.PI / 180;
    const cx = me.centerX;
    const cy = me.centerY;
    const isDonut = me.chartType === "donut";
    const rOut = size;
    const rIn = isDonut ? me.donutSize : 0;
    const spanRad = spanDeg * D2R2;
    let r2 = borderRadius;
    r2 = Math.min(r2, spanRad * rOut / 2);
    if (isDonut) {
      r2 = Math.min(r2, spanRad * rIn / 2);
      r2 = Math.min(r2, (rOut - rIn) / 2);
    } else {
      r2 = Math.min(r2, rOut / 2);
    }
    if (!(r2 > 0.5)) return null;
    const a0 = startDeg;
    const a1 = startDeg + spanDeg;
    if (isDonut) {
      return roundedDonutSegmentPath({ cx, cy, rIn, rOut, a0, a1, r: r2, spanDeg });
    }
    return roundedPieSegmentPath({ cx, cy, rOut, a0, a1, r: r2, spanDeg });
  }
  /**
   * @param {any} parent
   */
  drawPolarElements(parent) {
    const w2 = this.w;
    const scale = new Scales(this.w);
    const graphics = new Graphics(this.w);
    const helpers = new CircularChartsHelpers(this.w);
    const gCircles = graphics.group();
    const gYAxis = graphics.group();
    const yScale = scale.niceScale(0, Math.ceil(this.maxY), 0);
    const yTexts = yScale.result.reverse();
    const len = yScale.result.length;
    this.maxY = yScale.niceMax;
    let circleSize = w2.globals.radialSize;
    const diff = circleSize / (len - 1);
    for (let i2 = 0; i2 < len - 1; i2++) {
      const circle = graphics.drawCircle(circleSize);
      circle.attr({
        cx: this.centerX,
        cy: this.centerY,
        fill: "none",
        "stroke-width": w2.config.plotOptions.polarArea.rings.strokeWidth,
        stroke: w2.config.plotOptions.polarArea.rings.strokeColor
      });
      if (w2.config.yaxis[0].show) {
        const yLabel = helpers.drawYAxisTexts(
          this.centerX,
          this.centerY - circleSize + parseInt(w2.config.yaxis[0].labels.style.fontSize, 10) / 2,
          i2,
          yTexts[i2]
        );
        gYAxis.add(yLabel);
      }
      gCircles.add(circle);
      circleSize = circleSize - diff;
    }
    this.drawSpokes(parent);
    parent.add(gCircles);
    parent.add(gYAxis);
  }
  /**
   * @param {any} dataLabelsGroup
   * @param {Record<string, any>} dataLabelsConfig
   * @param {Record<string, any>} opts
   */
  renderInnerDataLabels(dataLabelsGroup, dataLabelsConfig, opts) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const showTotal = dataLabelsConfig.total.show;
    dataLabelsGroup.node.innerHTML = "";
    dataLabelsGroup.node.style.opacity = opts.opacity;
    const x = opts.centerX;
    const y = !this.donutDataLabels.total.label ? opts.centerY - opts.centerY / 6 : opts.centerY;
    let labelColor, valueColor;
    if (dataLabelsConfig.name.color === void 0) {
      labelColor = w2.globals.colors[0];
    } else {
      labelColor = dataLabelsConfig.name.color;
    }
    let labelFontSize = dataLabelsConfig.name.fontSize;
    let labelFontFamily = dataLabelsConfig.name.fontFamily;
    let labelFontWeight = dataLabelsConfig.name.fontWeight;
    if (dataLabelsConfig.value.color === void 0) {
      valueColor = w2.config.chart.foreColor;
    } else {
      valueColor = dataLabelsConfig.value.color;
    }
    const lbFormatter = dataLabelsConfig.value.formatter;
    let val = "";
    let name2 = "";
    if (showTotal) {
      labelColor = dataLabelsConfig.total.color;
      labelFontSize = dataLabelsConfig.total.fontSize;
      labelFontFamily = dataLabelsConfig.total.fontFamily;
      labelFontWeight = dataLabelsConfig.total.fontWeight;
      name2 = !this.donutDataLabels.total.label ? "" : dataLabelsConfig.total.label;
      val = dataLabelsConfig.total.formatter(w2);
    } else {
      if (w2.seriesData.series.length === 1) {
        val = lbFormatter(w2.seriesData.series[0], w2);
        name2 = w2.seriesData.seriesNames[0];
      }
    }
    if (name2) {
      name2 = dataLabelsConfig.name.formatter(
        name2,
        dataLabelsConfig.total.show,
        w2
      );
    }
    if (dataLabelsConfig.name.show) {
      const elLabel = graphics.drawText({
        x,
        y: y + parseFloat(dataLabelsConfig.name.offsetY),
        text: name2,
        textAnchor: "middle",
        foreColor: labelColor,
        fontSize: labelFontSize,
        fontWeight: labelFontWeight,
        fontFamily: labelFontFamily
      });
      elLabel.node.classList.add("apexcharts-datalabel-label");
      dataLabelsGroup.add(elLabel);
    }
    if (dataLabelsConfig.value.show) {
      const valOffset = dataLabelsConfig.name.show ? parseFloat(dataLabelsConfig.value.offsetY) + 16 : dataLabelsConfig.value.offsetY;
      const elValue = graphics.drawText({
        x,
        y: y + valOffset,
        text: val,
        textAnchor: "middle",
        foreColor: valueColor,
        fontWeight: dataLabelsConfig.value.fontWeight,
        fontSize: dataLabelsConfig.value.fontSize,
        fontFamily: dataLabelsConfig.value.fontFamily
      });
      elValue.node.classList.add("apexcharts-datalabel-value");
      dataLabelsGroup.add(elValue);
    }
    return dataLabelsGroup;
  }
  /**
   *
   * @param {string} name - The name of the series
   * @param {string} val - The value of that series
   * @param {any} el - Optional el (indicates which series was hovered/clicked). If this param is not present, means we need to show total
   * @param {Record<string, any>} labelsConfig
   */
  printInnerLabels(labelsConfig, name2, val, el) {
    const w2 = this.w;
    let labelColor;
    if (el) {
      if (labelsConfig.name.color === void 0) {
        labelColor = w2.globals.colors[parseInt(el.parentNode.getAttribute("rel"), 10) - 1];
      } else {
        labelColor = labelsConfig.name.color;
      }
    } else {
      if (w2.seriesData.series.length > 1 && labelsConfig.total.show) {
        labelColor = labelsConfig.total.color;
      }
    }
    const elLabel = w2.dom.baseEl.querySelector(".apexcharts-datalabel-label");
    const elValue = w2.dom.baseEl.querySelector(".apexcharts-datalabel-value");
    const lbFormatter = labelsConfig.value.formatter;
    val = lbFormatter(val, w2);
    if (!el && typeof labelsConfig.total.formatter === "function") {
      val = labelsConfig.total.formatter(w2);
    }
    const isTotal = name2 === labelsConfig.total.label;
    name2 = !this.donutDataLabels.total.label ? "" : labelsConfig.name.formatter(name2, isTotal, w2);
    if (elLabel !== null) {
      elLabel.textContent = name2;
    }
    if (elValue !== null) {
      elValue.textContent = val;
    }
    if (elLabel !== null) {
      const elLabelEl = (
        /** @type {HTMLElement} */
        elLabel
      );
      elLabelEl.style.fill = labelColor;
    }
  }
  /**
   * @param {any} el
   * @param {Record<string, any>} dataLabelsConfig
   */
  printDataLabelsInner(el, dataLabelsConfig) {
    const w2 = this.w;
    const val = el.getAttribute("data:value");
    const name2 = w2.seriesData.seriesNames[parseInt(el.parentNode.getAttribute("rel"), 10) - 1];
    if (w2.seriesData.series.length > 1) {
      this.printInnerLabels(dataLabelsConfig, name2, val, el);
    }
    const dataLabelsGroup = w2.dom.baseEl.querySelector(
      ".apexcharts-datalabels-group"
    );
    if (dataLabelsGroup !== null) {
      const dataLabelsGroupEl = (
        /** @type {HTMLElement} */
        dataLabelsGroup
      );
      dataLabelsGroupEl.style.opacity = "1";
    }
  }
  /**
   * @param {any} parent
   */
  drawSpokes(parent) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const spokeConfig = w2.config.plotOptions.polarArea.spokes;
    if (spokeConfig.strokeWidth === 0) return;
    const spokes = [];
    const angleDivision = 360 / w2.seriesData.series.length;
    for (let i2 = 0; i2 < w2.seriesData.series.length; i2++) {
      spokes.push(
        Utils$1.polarToCartesian(
          this.centerX,
          this.centerY,
          w2.globals.radialSize,
          w2.config.plotOptions.pie.startAngle + angleDivision * i2
        )
      );
    }
    spokes.forEach((p2, i2) => {
      const line = graphics.drawLine(
        p2.x,
        p2.y,
        this.centerX,
        this.centerY,
        Array.isArray(spokeConfig.connectorColors) ? spokeConfig.connectorColors[i2] : spokeConfig.connectorColors
      );
      parent.add(line);
    });
  }
  revertDataLabelsInner() {
    const w2 = this.w;
    if (this.donutDataLabels.show) {
      const dataLabelsGroup = w2.dom.Paper.findOne(
        `.apexcharts-datalabels-group`
      );
      const dataLabels = this.renderInnerDataLabels(
        dataLabelsGroup,
        this.donutDataLabels,
        {
          hollowSize: this.donutSize,
          centerX: this.centerX,
          centerY: this.centerY,
          opacity: this.donutDataLabels.show
        }
      );
      const elPie = w2.dom.Paper.findOne(
        ".apexcharts-radialbar, .apexcharts-pie"
      );
      elPie.add(dataLabels);
    }
  }
}
const RADAR_HIT_AREA_SIZE = 5;
class Radar {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
    this.chartType = this.w.config.chart.type;
    this.initialAnim = this.w.config.chart.animations.enabled;
    this.dynamicAnim = this.initialAnim && this.w.config.chart.animations.dynamicAnimation.enabled;
    this.animDur = 0;
    this.graphics = new Graphics(this.w);
    this.lineColorArr = w2.globals.stroke.colors !== void 0 ? w2.globals.stroke.colors : w2.globals.colors;
    this.defaultSize = w2.globals.svgHeight < w2.globals.svgWidth ? w2.layout.gridHeight : w2.layout.gridWidth;
    this.isLog = w2.config.yaxis[0].logarithmic;
    this.logBase = w2.config.yaxis[0].logBase;
    this.coreUtils = new CoreUtils(this.w);
    this.maxValue = this.isLog ? this.coreUtils.getLogVal(this.logBase, w2.globals.maxY, 0) : w2.globals.maxY;
    this.minValue = this.isLog ? this.coreUtils.getLogVal(this.logBase, this.w.globals.minY, 0) : w2.globals.minY;
    this.polygons = w2.config.plotOptions.radar.polygons;
    this.strokeWidth = w2.config.stroke.show ? w2.config.stroke.width : 0;
    this.size = this.defaultSize / 2.1 - this.strokeWidth - w2.config.chart.dropShadow.blur;
    if (w2.config.xaxis.labels.show) {
      this.size = this.size - w2.layout.xAxisLabelsWidth / 1.75;
    }
    if (w2.config.plotOptions.radar.size !== void 0) {
      this.size = w2.config.plotOptions.radar.size;
    }
    this.dataRadiusOfPercent = /** @type {any} */
    [];
    this.dataRadius = /** @type {any} */
    [];
    this.angleArr = /** @type {any} */
    [];
    this.dataPointsLen = 0;
    this.disAngle = 0;
    this.yaxisLabelsTextsPos = [];
  }
  /**
   * @param {any[]} series
   */
  draw(series) {
    const w2 = this.w;
    const fill = new Fill(this.w);
    const allSeries = [];
    const dataLabels = new DataLabels(this.w, this.ctx);
    if (series.length) {
      this.dataPointsLen = series[w2.globals.maxValsInArrayIndex].length;
    }
    this.disAngle = Math.PI * 2 / this.dataPointsLen;
    const halfW = w2.layout.gridWidth / 2;
    const halfH = w2.layout.gridHeight / 2;
    const translateX = halfW + w2.config.plotOptions.radar.offsetX;
    const translateY = halfH + w2.config.plotOptions.radar.offsetY;
    const ret = this.graphics.group({
      class: "apexcharts-radar-series apexcharts-plot-series",
      transform: `translate(${translateX || 0}, ${translateY || 0})`
    });
    let dataPointsPos = [];
    let elPointsMain = null;
    let elDataPointsMain = null;
    this.yaxisLabels = this.graphics.group({
      class: "apexcharts-yaxis"
    });
    series.forEach((s2, i2) => {
      const longestSeries = s2.length === w2.globals.dataPoints;
      const elSeries = this.graphics.group().attr({
        class: `apexcharts-series`,
        "data:longestSeries": longestSeries,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[i2]),
        rel: i2 + 1,
        "data:realIndex": i2
      });
      this.dataRadiusOfPercent[i2] = [];
      this.dataRadius[i2] = [];
      this.angleArr[i2] = [];
      s2.forEach((dv, j) => {
        const range = Math.abs(this.maxValue - this.minValue);
        dv = dv - this.minValue;
        if (this.isLog) {
          dv = this.coreUtils.getLogVal(this.logBase, dv, 0);
        }
        this.dataRadiusOfPercent[i2][j] = dv / range;
        this.dataRadius[i2][j] = this.dataRadiusOfPercent[i2][j] * this.size;
        this.angleArr[i2][j] = j * this.disAngle;
      });
      dataPointsPos = this.getDataPointsPos(
        this.dataRadius[i2],
        this.angleArr[i2]
      );
      const paths = this.createPaths(dataPointsPos, {
        x: 0,
        y: 0
      });
      elPointsMain = this.graphics.group({
        class: "apexcharts-series-markers-wrap apexcharts-element-hidden"
      });
      elDataPointsMain = this.graphics.group({
        class: `apexcharts-datalabels`,
        "data:realIndex": i2
      });
      w2.globals.delayedElements.push({
        el: elPointsMain.node,
        index: i2
      });
      const defaultRenderedPathOptions = {
        i: i2,
        realIndex: i2,
        animationDelay: i2,
        initialSpeed: w2.config.chart.animations.speed,
        dataChangeSpeed: w2.config.chart.animations.dynamicAnimation.speed,
        className: `apexcharts-radar`,
        shouldClipToGrid: false,
        bindEventsOnPaths: false,
        stroke: w2.globals.stroke.colors[i2],
        strokeLineCap: w2.config.stroke.lineCap
      };
      let pathFrom = null;
      if (w2.globals.previousPaths.length > 0) {
        pathFrom = this.getPreviousPath(i2);
      }
      for (let p2 = 0; p2 < paths.linePathsTo.length; p2++) {
        const renderedLinePath = this.graphics.renderPaths(__spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
          pathFrom: pathFrom === null ? paths.linePathsFrom[p2] : pathFrom,
          pathTo: paths.linePathsTo[p2],
          strokeWidth: Array.isArray(this.strokeWidth) ? this.strokeWidth[i2] : this.strokeWidth,
          fill: "none",
          drawShadow: false
        }));
        elSeries.add(renderedLinePath);
        const pathFill = fill.fillPath({
          seriesNumber: i2
        });
        const renderedAreaPath = this.graphics.renderPaths(__spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
          pathFrom: pathFrom === null ? paths.areaPathsFrom[p2] : pathFrom,
          pathTo: paths.areaPathsTo[p2],
          strokeWidth: 0,
          fill: pathFill,
          drawShadow: false,
          // Radial mask: the area fill blooms outward from the radar's center
          // (in this group's local coords) instead of the default L→R rect wipe.
          drawMask: { type: "radial", cx: 0, cy: 0, r: this.size }
        }));
        if (w2.config.chart.dropShadow.enabled) {
          const filters = new Filters(this.w);
          const shadow = w2.config.chart.dropShadow;
          filters.dropShadow(
            renderedAreaPath,
            Object.assign({}, shadow, { noUserSpaceOnUse: true }),
            i2
          );
        }
        elSeries.add(renderedAreaPath);
      }
      s2.forEach((sj, j) => {
        const markers = new Markers(this.w, this.ctx);
        const opts = markers.getMarkerConfig({
          cssClass: "apexcharts-marker",
          seriesIndex: i2,
          dataPointIndex: j
        });
        if (!opts.pSize) {
          opts.pSize = RADAR_HIT_AREA_SIZE;
          opts.pointFillColor = "transparent";
          opts.pointStrokeColor = "transparent";
          opts.pointStrokeWidth = 0;
        }
        const point = this.graphics.drawMarker(
          dataPointsPos[j].x,
          dataPointsPos[j].y,
          opts
        );
        point.attr("rel", j);
        point.attr("j", j);
        point.attr("index", i2);
        point.node.setAttribute("default-marker-size", opts.pSize);
        const elPointsWrap = this.graphics.group({
          class: "apexcharts-series-markers"
        });
        if (elPointsWrap) {
          elPointsWrap.add(point);
        }
        elPointsMain.add(elPointsWrap);
        elSeries.add(elPointsMain);
        const dataLabelsConfig = w2.config.dataLabels;
        if (dataLabelsConfig.enabled) {
          const text = dataLabelsConfig.formatter(w2.seriesData.series[i2][j], {
            seriesIndex: i2,
            dataPointIndex: j,
            w: w2
          });
          dataLabels.plotDataLabelsText({
            x: dataPointsPos[j].x,
            y: dataPointsPos[j].y,
            text,
            textAnchor: "middle",
            i: i2,
            j: i2,
            // `j` above is the series index (kept for the color lookup), so
            // pass the real data point index for per-point offsets
            seriesIndex: i2,
            dataPointIndex: j,
            parent: elDataPointsMain,
            offsetCorrection: false,
            dataLabelsConfig: __spreadValues({}, dataLabelsConfig)
          });
        }
        elSeries.add(elDataPointsMain);
      });
      allSeries.push(elSeries);
    });
    this.drawPolygons({
      parent: ret
    });
    if (w2.config.xaxis.labels.show) {
      const xaxisTexts = this.drawXAxisTexts();
      ret.add(xaxisTexts);
    }
    allSeries.forEach((elS) => {
      ret.add(elS);
    });
    ret.add(this.yaxisLabels);
    return ret;
  }
  /**
   * @param {Record<string, any>} opts
   */
  drawPolygons(opts) {
    const w2 = this.w;
    const { parent } = opts;
    const helpers = new CircularChartsHelpers(this.w);
    const yaxisTexts = w2.globals.yAxisScale[0].result.reverse();
    const layers = yaxisTexts.length;
    const radiusSizes = [];
    const layerDis = this.size / (layers - 1);
    for (let i2 = 0; i2 < layers; i2++) {
      radiusSizes[i2] = layerDis * i2;
    }
    radiusSizes.reverse();
    const polygonStrings = [];
    const lines = [];
    radiusSizes.forEach((radiusSize, r2) => {
      const polygon = Utils$1.getPolygonPos(radiusSize, this.dataPointsLen);
      let string = "";
      polygon.forEach((p2, i2) => {
        if (r2 === 0) {
          const line = this.graphics.drawLine(
            p2.x,
            p2.y,
            0,
            0,
            Array.isArray(this.polygons.connectorColors) ? this.polygons.connectorColors[i2] : this.polygons.connectorColors
          );
          lines.push(line);
        }
        if (i2 === 0) {
          this.yaxisLabelsTextsPos.push({
            x: p2.x,
            y: p2.y
          });
        }
        string += p2.x + "," + p2.y + " ";
      });
      polygonStrings.push(string);
    });
    polygonStrings.forEach((p2, i2) => {
      const strokeColors = this.polygons.strokeColors;
      const strokeWidth = this.polygons.strokeWidth;
      const polygon = this.graphics.drawPolygon(
        p2,
        Array.isArray(strokeColors) ? strokeColors[i2] : strokeColors,
        Array.isArray(strokeWidth) ? strokeWidth[i2] : strokeWidth,
        w2.globals.radarPolygons.fill.colors[i2]
      );
      parent.add(polygon);
    });
    lines.forEach((l2) => {
      parent.add(l2);
    });
    if (w2.config.yaxis[0].show) {
      this.yaxisLabelsTextsPos.forEach(
        (p2, i2) => {
          const yText = helpers.drawYAxisTexts(p2.x, p2.y, i2, yaxisTexts[i2]);
          this.yaxisLabels.add(yText);
        }
      );
    }
  }
  drawXAxisTexts() {
    const w2 = this.w;
    const xaxisLabelsConfig = w2.config.xaxis.labels;
    const elXAxisWrap = this.graphics.group({
      class: "apexcharts-xaxis"
    });
    const polygonPos = Utils$1.getPolygonPos(this.size, this.dataPointsLen);
    w2.labelData.labels.forEach((label, i2) => {
      const formatter = w2.config.xaxis.labels.formatter;
      const dataLabels = new DataLabels(this.w, this.ctx);
      if (polygonPos[i2]) {
        const textPos = this.getTextPos(polygonPos[i2], this.size);
        const text = formatter(label, {
          seriesIndex: -1,
          dataPointIndex: i2,
          w: w2
        });
        const dataLabelText = dataLabels.plotDataLabelsText({
          x: textPos.newX,
          y: textPos.newY,
          text,
          textAnchor: textPos.textAnchor,
          i: i2,
          j: i2,
          parent: elXAxisWrap,
          className: "apexcharts-xaxis-label",
          color: Array.isArray(xaxisLabelsConfig.style.colors) && xaxisLabelsConfig.style.colors[i2] ? xaxisLabelsConfig.style.colors[i2] : "#a8a8a8",
          dataLabelsConfig: __spreadValues({
            textAnchor: textPos.textAnchor,
            dropShadow: { enabled: false }
          }, xaxisLabelsConfig),
          offsetCorrection: false
        });
        dataLabelText.on("click", (e2) => {
          if (typeof w2.config.chart.events.xAxisLabelClick === "function") {
            const opts = Object.assign({}, w2, {
              labelIndex: i2
            });
            w2.config.chart.events.xAxisLabelClick(e2, this.ctx, opts);
          }
        });
      }
    });
    return elXAxisWrap;
  }
  /**
   * @param {Array<Record<string, any>>} pos
   * @param {Record<string, any>} origin
   */
  createPaths(pos, origin) {
    const linePathsTo = [];
    let linePathsFrom = [];
    const areaPathsTo = [];
    let areaPathsFrom = [];
    if (pos.length) {
      linePathsFrom = [this.graphics.move(origin.x, origin.y)];
      areaPathsFrom = [this.graphics.move(origin.x, origin.y)];
      let linePathTo = this.graphics.move(pos[0].x, pos[0].y);
      let areaPathTo = this.graphics.move(pos[0].x, pos[0].y);
      pos.forEach((p2, i2) => {
        linePathTo += this.graphics.line(p2.x, p2.y);
        areaPathTo += this.graphics.line(p2.x, p2.y);
        if (i2 === pos.length - 1) {
          linePathTo += "Z";
          areaPathTo += "Z";
        }
      });
      linePathsTo.push(linePathTo);
      areaPathsTo.push(areaPathTo);
    }
    return {
      linePathsFrom,
      linePathsTo,
      areaPathsFrom,
      areaPathsTo
    };
  }
  /**
   * @param {Record<string, any>} pos
   * @param {number} polygonSize
   */
  getTextPos(pos, polygonSize) {
    const limit = 10;
    let textAnchor = "middle";
    let newX = pos.x;
    let newY = pos.y;
    if (Math.abs(pos.x) >= limit) {
      if (pos.x > 0) {
        textAnchor = "start";
        newX += 10;
      } else if (pos.x < 0) {
        textAnchor = "end";
        newX -= 10;
      }
    } else {
      textAnchor = "middle";
    }
    if (Math.abs(pos.y) >= polygonSize - limit) {
      if (pos.y < 0) {
        newY -= 10;
      } else if (pos.y > 0) {
        newY += 10;
      }
    }
    return {
      textAnchor,
      newX,
      newY
    };
  }
  /**
   * @param {number} realIndex
   */
  getPreviousPath(realIndex) {
    const w2 = this.w;
    let pathFrom = null;
    for (let pp = 0; pp < w2.globals.previousPaths.length; pp++) {
      const gpp = w2.globals.previousPaths[pp];
      if (gpp.paths.length > 0 && parseInt(gpp.realIndex, 10) === parseInt(String(realIndex), 10)) {
        if (typeof w2.globals.previousPaths[pp].paths[0] !== "undefined") {
          pathFrom = w2.globals.previousPaths[pp].paths[0].d;
        }
      }
    }
    return pathFrom;
  }
  /**
   * @param {any[]} dataRadiusArr
   * @param {any[]} angleArr
   */
  getDataPointsPos(dataRadiusArr, angleArr, dataPointsLen = this.dataPointsLen) {
    dataRadiusArr = dataRadiusArr || [];
    angleArr = angleArr || [];
    const dataPointsPosArray = [];
    for (let j = 0; j < dataPointsLen; j++) {
      const curPointPos = {};
      curPointPos.x = dataRadiusArr[j] * Math.sin(angleArr[j]);
      curPointPos.y = -dataRadiusArr[j] * Math.cos(angleArr[j]);
      dataPointsPosArray.push(curPointPos);
    }
    return dataPointsPosArray;
  }
}
class Radial extends Pie {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    super(w2, ctx);
    this.ctx = ctx;
    this.w = w2;
    this.animBeginArr = [0];
    this.animDur = 0;
    this.startAngle = w2.config.plotOptions.radialBar.startAngle;
    this.endAngle = w2.config.plotOptions.radialBar.endAngle;
    this.totalAngle = Math.abs(
      w2.config.plotOptions.radialBar.endAngle - w2.config.plotOptions.radialBar.startAngle
    );
    this.trackStartAngle = w2.config.plotOptions.radialBar.track.startAngle;
    this.trackEndAngle = w2.config.plotOptions.radialBar.track.endAngle;
    this.barLabels = this.w.config.plotOptions.radialBar.barLabels;
    this.donutDataLabels = this.w.config.plotOptions.radialBar.dataLabels;
    this.radialDataLabels = this.donutDataLabels;
    if (!this.trackStartAngle) this.trackStartAngle = this.startAngle;
    if (!this.trackEndAngle) this.trackEndAngle = this.endAngle;
    if (this.endAngle === 360) this.endAngle = 359.99;
    this.margin = parseInt(w2.config.plotOptions.radialBar.track.margin, 10);
    this.onBarLabelClick = this.onBarLabelClick.bind(this);
  }
  /**
   * @param {any[]} series
   */
  draw(series) {
    var _a;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const ret = graphics.group({
      class: "apexcharts-radialbar"
    });
    if (w2.globals.noData) return ret;
    const elSeries = graphics.group();
    const centerY = this.defaultSize / 2;
    const centerX = w2.layout.gridWidth / 2;
    let size = this.defaultSize / 2.05;
    if (!w2.config.chart.sparkline.enabled) {
      size = size - w2.config.stroke.width - w2.config.chart.dropShadow.blur;
    }
    const colorArr = w2.globals.fill.colors;
    const rb = w2.config.plotOptions.radialBar;
    const hasBands = Array.isArray(rb.bands) && rb.bands.length > 0;
    const hideTrack = hasBands && rb.bandsStyle && rb.bandsStyle.hideTrackWhenPresent;
    const isNeedleShape = rb.shape === "needle";
    if (rb.track.show && !hideTrack) {
      const elTracks = this.drawTracks({
        size,
        centerX,
        centerY,
        colorArr,
        series
      });
      elSeries.add(elTracks);
    }
    if (hasBands) {
      const elBands = this.drawBands({
        size,
        centerX,
        centerY,
        series
      });
      elSeries.add(elBands);
    }
    const elG = this.drawArcs({
      size,
      centerX,
      centerY,
      colorArr,
      series,
      // When `needle.showValueArc` is true, render both the filled value-arc
      // and the needle on top — required for gauges that want a progress
      // ring with a pointer indicator (default still hides the arc when in
      // needle shape, preserving prior behavior).
      skipValueArc: isNeedleShape && !((_a = rb.needle) == null ? void 0 : _a.showValueArc)
    });
    if (rb.ticks && rb.ticks.show) {
      const elTicks = this.drawTicks({
        size,
        centerX,
        centerY,
        series
      });
      const isInitialMount = this.initialAnim && !w2.globals.dataChanged && !w2.globals.resized;
      if (isInitialMount && Environment.isBrowser() && w2.globals.shouldAnimate) {
        const ticksNode = elTicks.node;
        ticksNode.style.opacity = "0";
        ticksNode.style.transition = "opacity 280ms ease-out";
        const sweepDur = w2.config.chart.animations.speed || 800;
        setTimeout(() => {
          ticksNode.style.opacity = "1";
        }, sweepDur);
      }
      elSeries.add(elTicks);
    }
    if (isNeedleShape) {
      const elNeedle = this.drawNeedle({
        size,
        centerX,
        centerY,
        series
      });
      elSeries.add(elNeedle);
    }
    let totalAngle = 360;
    if (w2.config.plotOptions.radialBar.startAngle < 0) {
      totalAngle = this.totalAngle;
    }
    const angleRatio = (360 - totalAngle) / 360;
    w2.globals.radialSize = size - size * angleRatio;
    if (this.radialDataLabels.value.show) {
      const offset = Math.max(
        this.radialDataLabels.value.offsetY,
        this.radialDataLabels.name.offsetY
      );
      w2.globals.radialSize += offset * angleRatio;
    }
    elSeries.add(elG.g);
    if (w2.config.plotOptions.radialBar.hollow.position === "front") {
      elG.g.add(elG.elHollow);
      if (elG.dataLabels) {
        elG.g.add(elG.dataLabels);
      }
    }
    ret.add(elSeries);
    return ret;
  }
  /**
   * @param {Record<string, any>} opts
   */
  drawTracks(opts) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const g2 = graphics.group({
      class: "apexcharts-tracks"
    });
    const filters = new Filters(this.w);
    const fill = new Fill(this.w);
    const strokeWidth = this.getStrokeWidth(opts);
    opts.size = opts.size - strokeWidth / 2;
    for (let i2 = 0; i2 < opts.series.length; i2++) {
      const elRadialBarTrack = graphics.group({
        class: "apexcharts-radialbar-track apexcharts-track"
      });
      g2.add(elRadialBarTrack);
      elRadialBarTrack.attr({
        rel: i2 + 1
      });
      opts.size = opts.size - strokeWidth - this.margin;
      const trackConfig = w2.config.plotOptions.radialBar.track;
      const pathFill = fill.fillPath({
        seriesNumber: 0,
        size: opts.size,
        fillColors: Array.isArray(trackConfig.background) ? trackConfig.background[i2] : trackConfig.background,
        solid: true
      });
      const startAngle = this.trackStartAngle;
      let endAngle = this.trackEndAngle;
      if (Math.abs(endAngle) + Math.abs(startAngle) >= 360)
        endAngle = 360 - Math.abs(this.startAngle) - 0.1;
      const elPath = graphics.drawPath({
        d: "",
        stroke: pathFill,
        strokeWidth: strokeWidth * parseInt(trackConfig.strokeWidth, 10) / 100,
        fill: "none",
        strokeOpacity: trackConfig.opacity,
        classes: "apexcharts-radialbar-area"
      });
      if (trackConfig.dropShadow.enabled) {
        const shadow = trackConfig.dropShadow;
        filters.dropShadow(elPath, shadow);
      }
      elRadialBarTrack.add(elPath);
      elPath.attr("id", "apexcharts-radialbarTrack-" + i2);
      this.animatePaths(elPath, {
        centerX: opts.centerX,
        centerY: opts.centerY,
        endAngle,
        startAngle,
        size: opts.size,
        i: i2,
        totalItems: 2,
        animBeginArr: 0,
        dur: 0,
        isTrack: true
      });
    }
    return g2;
  }
  /**
   * @param {Record<string, any>} opts
   */
  drawArcs(opts) {
    var _a;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const fill = new Fill(this.w);
    const filters = new Filters(this.w);
    const g2 = graphics.group();
    const strokeWidth = this.getStrokeWidth(opts);
    opts.size = opts.size - strokeWidth / 2;
    let hollowFillID = w2.config.plotOptions.radialBar.hollow.background;
    const hollowSize = opts.size - strokeWidth * opts.series.length - this.margin * opts.series.length - strokeWidth * parseInt(w2.config.plotOptions.radialBar.track.strokeWidth, 10) / 100 / 2;
    const hollowRadius = hollowSize - w2.config.plotOptions.radialBar.hollow.margin;
    if (w2.config.plotOptions.radialBar.hollow.image !== void 0) {
      hollowFillID = this.drawHollowImage(opts, g2, hollowSize, hollowFillID);
    }
    const elHollow = this.drawHollow({
      size: hollowRadius,
      centerX: opts.centerX,
      centerY: opts.centerY,
      fill: hollowFillID ? hollowFillID : "transparent"
    });
    if (w2.config.plotOptions.radialBar.hollow.dropShadow.enabled) {
      const shadow = w2.config.plotOptions.radialBar.hollow.dropShadow;
      filters.dropShadow(elHollow, shadow);
    }
    let shown = 1;
    if (!this.radialDataLabels.total.show && w2.seriesData.series.length > 1) {
      shown = 0;
    }
    let dataLabels = null;
    if (this.radialDataLabels.show) {
      const dataLabelsGroup = w2.dom.Paper.findOne(
        `.apexcharts-datalabels-group`
      );
      dataLabels = this.renderInnerDataLabels(
        dataLabelsGroup,
        this.radialDataLabels,
        {
          hollowSize,
          centerX: opts.centerX,
          centerY: opts.centerY,
          opacity: shown
        }
      );
    }
    if (w2.config.plotOptions.radialBar.hollow.position === "back") {
      g2.add(elHollow);
      if (dataLabels) {
        g2.add(dataLabels);
      }
    }
    let reverseLoop = false;
    if (w2.config.plotOptions.radialBar.inverseOrder) {
      reverseLoop = true;
    }
    const morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
    for (let i2 = reverseLoop ? opts.series.length - 1 : 0; reverseLoop ? i2 >= 0 : i2 < opts.series.length; reverseLoop ? i2-- : i2++) {
      const elRadialBarArc = graphics.group({
        class: `apexcharts-series apexcharts-radial-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[i2])
      });
      g2.add(elRadialBarArc);
      elRadialBarArc.attr({
        rel: i2 + 1,
        "data:realIndex": i2
      });
      Series.addCollapsedClassToSeries(this.w, elRadialBarArc, i2);
      opts.size = opts.size - strokeWidth - this.margin;
      const pathFill = fill.fillPath({
        seriesNumber: i2,
        size: opts.size,
        value: opts.series[i2]
      });
      const startAngle = this.startAngle;
      let prevStartAngle;
      const rb = w2.config.plotOptions.radialBar;
      const domainMin = typeof rb.min === "number" ? rb.min : 0;
      const domainMax = typeof rb.max === "number" ? rb.max : 100;
      const domainSpan = domainMax === domainMin ? 1 : domainMax - domainMin;
      const valueToFraction = (v2) => {
        const clamped = Math.min(Math.max(v2, domainMin), domainMax);
        return Math.max(0, (clamped - domainMin) / domainSpan);
      };
      const dataValue = valueToFraction(Utils$1.negToZero(opts.series[i2]));
      let endAngle = Math.round(this.totalAngle * dataValue) + this.startAngle;
      let prevEndAngle;
      if (w2.globals.dataChanged) {
        prevStartAngle = this.startAngle;
        prevEndAngle = Math.round(
          this.totalAngle * valueToFraction(Utils$1.negToZero(w2.globals.previousPaths[i2]))
        ) + prevStartAngle;
      }
      const currFullAngle = Math.abs(endAngle) + Math.abs(startAngle);
      if (currFullAngle > 360) {
        endAngle = endAngle - 0.01;
      }
      const prevFullAngle = Math.abs(prevEndAngle) + Math.abs(prevStartAngle);
      if (prevFullAngle > 360) {
        prevEndAngle = prevEndAngle - 0.01;
      }
      const angle = endAngle - startAngle;
      const dashArray = Array.isArray(w2.config.stroke.dashArray) ? w2.config.stroke.dashArray[i2] : w2.config.stroke.dashArray;
      const morphFrom = morphActive ? this.ctx.morphTypeChange.getInitialPathFor(i2, 0) : null;
      const morphFromType = morphActive ? this.ctx.morphTypeChange.getFromType() : null;
      const morphFromFilled = !!morphFrom && (morphFromType === "bar" || morphFromType === "funnel" || morphFromType === "pyramid" || morphFromType === "pie" || morphFromType === "donut" || morphFromType === "polarArea");
      const elPath = graphics.drawPath({
        d: morphFrom || "",
        stroke: morphFromFilled ? "transparent" : opts.skipValueArc ? "transparent" : pathFill,
        strokeWidth: morphFromFilled ? 0 : opts.skipValueArc ? 0 : strokeWidth,
        fill: morphFromFilled ? pathFill : "none",
        fillOpacity: w2.config.fill.opacity,
        classes: "apexcharts-radialbar-area apexcharts-radialbar-slice-" + i2,
        strokeDashArray: dashArray
      });
      const radialMidAngle = startAngle + angle / 2;
      const radialArcCenter = Utils$1.polarToCartesian(
        opts.centerX,
        opts.centerY,
        opts.size,
        radialMidAngle
      );
      Graphics.setAttrs(elPath.node, {
        "data:angle": angle,
        "data:value": opts.series[i2],
        "data:cx": radialArcCenter.x,
        "data:cy": radialArcCenter.y
      });
      if (w2.config.chart.dropShadow.enabled) {
        const shadow = w2.config.chart.dropShadow;
        filters.dropShadow(elPath, shadow, i2);
      }
      filters.setSelectionFilter(elPath, 0, i2);
      this.addListeners(elPath, this.radialDataLabels);
      elRadialBarArc.add(elPath);
      elPath.attr({
        index: 0,
        j: i2
      });
      if (this.barLabels.enabled) {
        const barStartCords = Utils$1.polarToCartesian(
          opts.centerX,
          opts.centerY,
          opts.size,
          startAngle
        );
        const text = this.barLabels.formatter(w2.seriesData.seriesNames[i2], {
          seriesIndex: i2,
          w: w2
        });
        const classes = ["apexcharts-radialbar-label"];
        if (!this.barLabels.onClick) {
          classes.push("apexcharts-no-click");
        }
        let textColor = this.barLabels.useSeriesColors ? w2.globals.colors[i2] : w2.config.chart.foreColor;
        if (!textColor) {
          textColor = w2.config.chart.foreColor;
        }
        const x = barStartCords.x + this.barLabels.offsetX;
        const y = barStartCords.y + this.barLabels.offsetY;
        const elText = graphics.drawText({
          x,
          y,
          text,
          textAnchor: "end",
          dominantBaseline: "middle",
          fontFamily: this.barLabels.fontFamily,
          fontWeight: this.barLabels.fontWeight,
          fontSize: this.barLabels.fontSize,
          foreColor: textColor,
          cssClass: classes.join(" ")
        });
        elText.on("click", this.onBarLabelClick);
        elText.attr({
          rel: i2 + 1
        });
        if (startAngle !== 0) {
          elText.attr({
            "transform-origin": `${x} ${y}`,
            transform: `rotate(${startAngle} 0 0)`
          });
        }
        elRadialBarArc.add(elText);
      }
      let dur = 0;
      if (this.initialAnim && !w2.globals.resized && !w2.globals.dataChanged) {
        dur = w2.config.chart.animations.speed;
      }
      if (w2.globals.dataChanged) {
        dur = w2.config.chart.animations.dynamicAnimation.speed;
      }
      this.animDur = dur / (opts.series.length * 1.2) + this.animDur;
      this.animBeginArr.push(this.animDur);
      if (morphActive && morphFrom) {
        const morphSpeed = this.ctx.morphTypeChange.getSpeed();
        const actualArcD = this.getPiePath({
          me: this,
          startAngle,
          angle,
          size: opts.size
        });
        if (morphFromFilled) {
          const targetD = this.ctx.morphTypeChange.buildRingSegmentPath(
            opts.centerX,
            opts.centerY,
            opts.size,
            strokeWidth,
            startAngle,
            startAngle + angle
          );
          elPath.animate(morphSpeed).plot(targetD, "polygons").after(
            /** @this {any} */
            function() {
              this.attr({
                d: actualArcD,
                fill: "none",
                stroke: opts.skipValueArc ? "transparent" : pathFill,
                "stroke-width": opts.skipValueArc ? 0 : strokeWidth
              });
            }
          );
        } else {
          elPath.animate(morphSpeed).plot(actualArcD, "polygons").attr({ "stroke-width": strokeWidth });
        }
      } else {
        this.animatePaths(elPath, {
          centerX: opts.centerX,
          centerY: opts.centerY,
          endAngle,
          startAngle,
          prevEndAngle,
          prevStartAngle,
          size: opts.size,
          i: i2,
          totalItems: 2,
          animBeginArr: this.animBeginArr,
          dur,
          shouldSetPrevPaths: true
        });
      }
    }
    return {
      g: g2,
      elHollow,
      dataLabels
    };
  }
  /**
   * Map a domain value (between `min` and `max`) to the corresponding angle
   * in the gauge's `startAngle`..`endAngle` range. Values outside the
   * domain are clamped.
   *
   * @param {number} value
   * @returns {number}
   */
  _angleAtValue(value) {
    const rb = this.w.config.plotOptions.radialBar;
    const min = typeof rb.min === "number" ? rb.min : 0;
    const max = typeof rb.max === "number" ? rb.max : 100;
    const safeMax = max === min ? min + 1 : max;
    const clamped = Math.max(min, Math.min(safeMax, Number(value)));
    const t2 = (clamped - min) / (safeMax - min);
    return this.startAngle + t2 * (this.endAngle - this.startAngle);
  }
  /**
   * Build an SVG arc path from `startAngle` to `endAngle` at radius `r`
   * around `(cx, cy)`. Angles are in degrees, with 0° at the top.
   * Used by drawBands; mirrors the `M ... A ... ` form used elsewhere.
   *
   * @param {number} cx
   * @param {number} cy
   * @param {number} r
   * @param {number} startAngle
   * @param {number} endAngle
   * @returns {string}
   */
  _describeArc(cx, cy, r2, startAngle, endAngle) {
    const start = Utils$1.polarToCartesian(cx, cy, r2, endAngle);
    const end = Utils$1.polarToCartesian(cx, cy, r2, startAngle);
    const sweep = endAngle - startAngle;
    const largeArc = Math.abs(sweep) > 180 ? 1 : 0;
    return `M ${start.x} ${start.y} A ${r2} ${r2} 0 ${largeArc} 0 ${end.x} ${end.y}`;
  }
  /**
   * Draw threshold bands as colored arc segments along the gauge arc.
   * Bands sit behind the value-arc and tick marks. Used for gauges that
   * indicate ranges like 0-30 critical / 30-70 warning / 70-100 healthy.
   *
   * @param {Record<string, any>} opts
   */
  drawBands(opts) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const rb = w2.config.plotOptions.radialBar;
    const bands = rb.bands || [];
    const g2 = graphics.group({
      class: "apexcharts-gauge-bands"
    });
    const strokeWidth = this.getStrokeWidth(opts);
    const radius = opts.size - strokeWidth / 2 - strokeWidth - this.margin;
    const bandStroke = strokeWidth * parseInt(rb.bandsStyle.strokeWidth, 10) / 100;
    const min = typeof rb.min === "number" ? rb.min : 0;
    const max = typeof rb.max === "number" ? rb.max : 100;
    const gapDeg = max === min ? 0 : (rb.bandsStyle.gap || 0) * ((this.endAngle - this.startAngle) / (max - min));
    for (let b2 = 0; b2 < bands.length; b2++) {
      const band = bands[b2];
      if (band.from === void 0 || band.to === void 0) continue;
      const a1 = this._angleAtValue(band.from);
      const a2 = this._angleAtValue(band.to);
      const startA = Math.min(a1, a2) + gapDeg / 2;
      const endA = Math.max(a1, a2) - gapDeg / 2;
      if (endA - startA <= 0) continue;
      const elBand = graphics.drawPath({
        d: this._describeArc(opts.centerX, opts.centerY, radius, startA, endA),
        stroke: band.color || "#ccc",
        strokeWidth: bandStroke,
        fill: "none",
        strokeLinecap: rb.bandsStyle.linecap || "butt",
        classes: "apexcharts-gauge-band"
      });
      elBand.node.setAttribute("data-band-index", String(b2));
      g2.add(elBand);
    }
    return g2;
  }
  /**
   * Draw tick marks (major + minor) along the gauge arc, with optional
   * value labels at each major tick.
   *
   * @param {Record<string, any>} opts
   */
  drawTicks(opts) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const rb = w2.config.plotOptions.radialBar;
    const ticks = rb.ticks;
    const g2 = graphics.group({ class: "apexcharts-gauge-ticks" });
    const strokeWidth = this.getStrokeWidth(opts);
    const radius = opts.size - strokeWidth / 2 - strokeWidth - this.margin;
    const min = typeof rb.min === "number" ? rb.min : 0;
    const max = typeof rb.max === "number" ? rb.max : 100;
    const majorCount = Math.max(2, (_b = (_a = ticks.major) == null ? void 0 : _a.count) != null ? _b : 11);
    const minorCount = Math.max(0, (_d = (_c = ticks.minor) == null ? void 0 : _c.count) != null ? _d : 0);
    const drawTickAt = (value, cfg, isMajor) => {
      var _a2, _b2, _c2;
      const angle = this._angleAtValue(value);
      const length = (_a2 = cfg.length) != null ? _a2 : 8;
      const inner = cfg.placement === "inside" ? radius - length : radius;
      const outer = cfg.placement === "inside" ? radius : radius + length;
      const p1 = Utils$1.polarToCartesian(
        opts.centerX,
        opts.centerY,
        inner,
        angle
      );
      const p2 = Utils$1.polarToCartesian(
        opts.centerX,
        opts.centerY,
        outer,
        angle
      );
      const line = graphics.drawLine(
        p1.x,
        p1.y,
        p2.x,
        p2.y,
        cfg.color || (isMajor ? "#666" : "#999"),
        0,
        cfg.width || (isMajor ? 2 : 1)
      );
      g2.add(line);
      if (isMajor && ((_b2 = ticks.labels) == null ? void 0 : _b2.show)) {
        const labelRadius = (cfg.placement === "inside" ? inner : outer) + (cfg.placement === "inside" ? -1 : 1) * ((_c2 = ticks.labels.offset) != null ? _c2 : 6);
        const labelPos = Utils$1.polarToCartesian(
          opts.centerX,
          opts.centerY,
          labelRadius,
          angle
        );
        const labelText = typeof ticks.labels.formatter === "function" ? ticks.labels.formatter(value) : String(value);
        const elText = graphics.drawText({
          x: labelPos.x,
          y: labelPos.y,
          text: labelText,
          textAnchor: "middle",
          dominantBaseline: "middle",
          fontFamily: ticks.labels.fontFamily,
          fontSize: ticks.labels.fontSize,
          fontWeight: ticks.labels.fontWeight,
          foreColor: ticks.labels.color,
          cssClass: "apexcharts-gauge-tick-label"
        });
        g2.add(elText);
      }
    };
    for (let m2 = 0; m2 < majorCount; m2++) {
      const t2 = m2 / (majorCount - 1);
      const value = min + t2 * (max - min);
      drawTickAt(value, ticks.major || {}, true);
      if (m2 < majorCount - 1 && minorCount > 0) {
        for (let n2 = 1; n2 <= minorCount; n2++) {
          const tMinor = (m2 + n2 / (minorCount + 1)) / (majorCount - 1);
          const minorValue = min + tMinor * (max - min);
          drawTickAt(minorValue, ticks.minor || {}, false);
        }
      }
    }
    return g2;
  }
  /**
   * Draw a rotating needle pointing at the current series value. Only
   * called when `plotOptions.radialBar.shape === 'needle'`. The needle is
   * a tapered polygon inside a `<g>` whose rotation transform is animated
   * from `startAngle` to the value's angle.
   *
   * Renders a single needle for the first series value (gauge use case).
   * Additional series are ignored — drilled-down multi-series gauges are
   * out of scope for this iteration.
   *
   * @param {Record<string, any>} opts
   */
  drawNeedle(opts) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    const rb = w2.config.plotOptions.radialBar;
    const cfg = rb.needle || {};
    const g2 = graphics.group({ class: "apexcharts-gauge-needle" });
    if (!opts.series || opts.series.length === 0) return g2;
    const strokeWidth = this.getStrokeWidth(opts);
    const arcRadius = opts.size - strokeWidth / 2 - strokeWidth - this.margin;
    const length = typeof cfg.length === "string" && cfg.length.endsWith("%") ? arcRadius * parseInt(cfg.length, 10) / 100 : Number(cfg.length || arcRadius * 0.85);
    const baseW = (_a = cfg.baseWidth) != null ? _a : 4;
    const tipW = (_b = cfg.tipWidth) != null ? _b : 1;
    const color = cfg.color || "#333";
    const cx = opts.centerX;
    const needleOffsetY = Number((_c = cfg.offsetY) != null ? _c : 0);
    const cy = opts.centerY + needleOffsetY;
    const path = `M ${cx + baseW / 2} ${cy} A ${baseW / 2} ${baseW / 2} 0 0 1 ${cx - baseW / 2} ${cy} L ${cx - tipW / 2} ${cy - length} L ${cx + tipW / 2} ${cy - length} Z`;
    const elNeedle = graphics.drawPath({
      d: path,
      stroke: color,
      strokeWidth: 0,
      fill: color,
      classes: "apexcharts-gauge-needle-shape"
    });
    g2.add(elNeedle);
    const value = Number(opts.series[0]);
    const targetAngle = this._angleAtValue(value);
    const isInitialMount = this.initialAnim && !w2.globals.dataChanged && !w2.globals.resized;
    const ctx = (
      /** @type {any} */
      this.ctx
    );
    const fromAngle = typeof ctx._lastNeedleAngle === "number" ? ctx._lastNeedleAngle : this.startAngle;
    ctx._lastNeedleAngle = targetAngle;
    const shouldAnimate = Environment.isBrowser() && w2.globals.shouldAnimate && (isInitialMount || w2.globals.dataChanged);
    if (shouldAnimate && fromAngle !== targetAngle) {
      const node = g2.node;
      node.setAttribute("transform-origin", `${cx} ${cy}`);
      node.setAttribute("transform", `rotate(${fromAngle})`);
      const speed = ((_d = cfg.animation) == null ? void 0 : _d.duration) && Number(cfg.animation.duration) || cfg.animationSpeed && Number(cfg.animationSpeed) || ((_e = w2.config.chart.animations.dynamicAnimation) == null ? void 0 : _e.speed) || w2.config.chart.animations.speed || 800;
      const c1 = 1.70158;
      const c3 = c1 + 1;
      const easeOutBack2 = (t2) => 1 + c3 * Math.pow(t2 - 1, 3) + c1 * Math.pow(t2 - 1, 2);
      const easeOutCubic2 = (t2) => 1 - Math.pow(1 - t2, 3);
      const ease = isInitialMount ? easeOutBack2 : easeOutCubic2;
      if (w2.globals.radialNeedleRAF != null) {
        BrowserAPIs.cancelAnimationFrame(w2.globals.radialNeedleRAF);
        w2.globals.radialNeedleRAF = null;
      }
      const startAt = performance.now();
      const step = (now) => {
        if (this.w.globals.isDestroyed) {
          w2.globals.radialNeedleRAF = null;
          return;
        }
        const t2 = Math.max(0, Math.min(1, (now - startAt) / speed));
        const angle = fromAngle + (targetAngle - fromAngle) * ease(t2);
        node.setAttribute("transform", `rotate(${angle})`);
        if (t2 < 1) {
          w2.globals.radialNeedleRAF = BrowserAPIs.requestAnimationFrame(step);
        } else {
          w2.globals.radialNeedleRAF = null;
        }
      };
      w2.globals.radialNeedleRAF = BrowserAPIs.requestAnimationFrame(step);
    } else {
      g2.attr({
        "transform-origin": `${cx} ${cy}`,
        transform: `rotate(${targetAngle})`
      });
    }
    return g2;
  }
  /**
   * @param {Record<string, any>} opts
   */
  drawHollow(opts) {
    var _a;
    const graphics = new Graphics(this.w);
    const hollow = this.w.config.plotOptions.radialBar.hollow;
    const circle = graphics.drawCircle(opts.size * 2);
    const attrs = {
      class: "apexcharts-radialbar-hollow",
      cx: opts.centerX,
      cy: opts.centerY,
      r: opts.size,
      fill: opts.fill
    };
    if (hollow.stroke || hollow.strokeDasharray) {
      attrs.stroke = hollow.stroke || "transparent";
      attrs["stroke-width"] = (_a = hollow.strokeWidth) != null ? _a : 1;
      if (hollow.strokeDasharray) {
        attrs["stroke-dasharray"] = hollow.strokeDasharray;
      }
    }
    circle.attr(attrs);
    return circle;
  }
  /**
   * @param {Record<string, any>} opts
   * @param {any} g
   * @param {number} hollowSize
   * @param {string} hollowFillID
   */
  drawHollowImage(opts, g2, hollowSize, hollowFillID) {
    const w2 = this.w;
    const fill = new Fill(this.w);
    const randID = Utils$1.randomId();
    const hollowFillImg = w2.config.plotOptions.radialBar.hollow.image;
    if (w2.config.plotOptions.radialBar.hollow.imageClipped) {
      fill.clippedImgArea({
        width: hollowSize,
        height: hollowSize,
        image: hollowFillImg,
        patternID: `pattern${w2.globals.cuid}${randID}`
      });
      hollowFillID = `url(#pattern${w2.globals.cuid}${randID})`;
    } else {
      const imgWidth = w2.config.plotOptions.radialBar.hollow.imageWidth;
      const imgHeight = w2.config.plotOptions.radialBar.hollow.imageHeight;
      if (imgWidth === void 0 && imgHeight === void 0) {
        const image = w2.dom.Paper.image(
          hollowFillImg,
          /** @this {any} */
          function(loader) {
            this.move(
              opts.centerX - loader.width / 2 + w2.config.plotOptions.radialBar.hollow.imageOffsetX,
              opts.centerY - loader.height / 2 + w2.config.plotOptions.radialBar.hollow.imageOffsetY
            );
          }
        );
        g2.add(image);
      } else {
        const image = w2.dom.Paper.image(
          hollowFillImg,
          /** @this {any} */
          function() {
            this.move(
              opts.centerX - imgWidth / 2 + w2.config.plotOptions.radialBar.hollow.imageOffsetX,
              opts.centerY - imgHeight / 2 + w2.config.plotOptions.radialBar.hollow.imageOffsetY
            );
            this.size(imgWidth, imgHeight);
          }
        );
        g2.add(image);
      }
    }
    return hollowFillID;
  }
  /**
   * @param {Record<string, any>} opts
   */
  getStrokeWidth(opts) {
    const w2 = this.w;
    return opts.size * (100 - parseInt(w2.config.plotOptions.radialBar.hollow.size, 10)) / 100 / (opts.series.length + 1) - this.margin;
  }
  /**
   * @param {Event} e
   */
  onBarLabelClick(e2) {
    var _a;
    const target = (
      /** @type {Element} */
      e2.target
    );
    const seriesIndex = parseInt((_a = target.getAttribute("rel")) != null ? _a : "", 10) - 1;
    const legendClick = this.barLabels.onClick;
    const w2 = this.w;
    if (legendClick) {
      legendClick(w2.seriesData.seriesNames[seriesIndex], { w: w2, seriesIndex });
    }
  }
}
class RangeBar extends Bar {
  /**
   * @param {any[]} series
   * @param {number} seriesIndex
   */
  draw(series, seriesIndex) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    this.rangeBarOptions = this.w.config.plotOptions.rangeBar;
    this.series = series;
    this.seriesRangeStart = w2.rangeData.seriesRangeStart;
    this.seriesRangeEnd = w2.rangeData.seriesRangeEnd;
    this.barHelpers.initVariables(series);
    const ret = graphics.group({
      class: "apexcharts-rangebar-series apexcharts-plot-series"
    });
    for (let i2 = 0; i2 < series.length; i2++) {
      let x, y;
      const realIndex = w2.globals.comboCharts ? (
        /** @type {any} */
        seriesIndex[i2]
      ) : i2;
      const { columnGroupIndex } = this.barHelpers.getGroupIndex(realIndex);
      const elSeries = graphics.group({
        class: `apexcharts-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[realIndex]),
        rel: i2 + 1,
        "data:realIndex": realIndex
      });
      Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
      if (series[i2].length > 0) {
        this.visibleI = this.visibleI + 1;
      }
      let translationsIndex = 0;
      if (this.yRatio.length > 1) {
        this.yaxisIndex = /** @type {any} */
        w2.globals.seriesYAxisReverseMap[realIndex][0];
        translationsIndex = realIndex;
      }
      const initPositions = this.barHelpers.initialPositions(realIndex);
      const {
        y: initY,
        zeroW,
        // zeroW is the baseline where 0 meets x axis
        x: initX,
        zeroH
        // zeroH is the baseline where 0 meets y axis
      } = initPositions;
      let barWidth = (_a = initPositions.barWidth) != null ? _a : 0;
      let barHeight = (_b = initPositions.barHeight) != null ? _b : 0;
      const yDivision = (_c = initPositions.yDivision) != null ? _c : 0;
      const xDivision = (_d = initPositions.xDivision) != null ? _d : 0;
      y = initY;
      x = initX;
      const elDataLabelsWrap = graphics.group({
        class: "apexcharts-datalabels",
        "data:realIndex": realIndex
      });
      const elGoalsMarkers = graphics.group({
        class: "apexcharts-rangebar-goals-markers"
      });
      for (let j = 0; j < w2.globals.dataPoints; j++) {
        const strokeWidth = this.barHelpers.getStrokeWidth(i2, j, realIndex);
        const y1 = this.seriesRangeStart[i2][j];
        const y2 = this.seriesRangeEnd[i2][j];
        let paths = (
          /** @type {any} */
          null
        );
        let barXPosition = null;
        let barYPosition = null;
        const params = { x, y, strokeWidth, elSeries };
        let seriesLen = this.seriesLen;
        if (w2.config.plotOptions.bar.rangeBarGroupRows) {
          seriesLen = 1;
        }
        if (typeof /** @type {Record<string,any>} */
        ((_e = w2.config.series[i2].data) == null ? void 0 : _e[j]) === "undefined") {
          break;
        }
        if (this.isHorizontal) {
          barYPosition = y + barHeight * /** @type {any} */
          this.visibleI;
          const srty = (yDivision - barHeight * seriesLen) / 2;
          if (
            /** @type {Record<string,any>} */
            (_g = (_f = w2.config.series[i2].data) == null ? void 0 : _f[j]) == null ? void 0 : _g.x
          ) {
            const positions = this.detectOverlappingBars({
              i: i2,
              j,
              barYPosition,
              srty,
              barHeight,
              yDivision,
              initPositions
            });
            barHeight = positions.barHeight;
            barYPosition = positions.barYPosition;
          }
          paths = this.drawRangeBarPaths(__spreadValues({
            indexes: { i: i2, j, realIndex },
            barHeight,
            barYPosition,
            zeroW,
            yDivision,
            y1,
            y2
          }, params));
          barWidth = paths.barWidth;
        } else {
          if (w2.axisFlags.isXNumeric) {
            x = (w2.seriesData.seriesX[i2][j] - w2.globals.minX) / this.xRatio - barWidth / 2;
          }
          barXPosition = x + barWidth * /** @type {any} */
          this.visibleI;
          const srtx = (xDivision - barWidth * seriesLen) / 2;
          if (
            /** @type {Record<string,any>} */
            (_i = (_h = w2.config.series[i2].data) == null ? void 0 : _h[j]) == null ? void 0 : _i.x
          ) {
            const positions = this.detectOverlappingBars({
              i: i2,
              j,
              barXPosition,
              srtx,
              barWidth,
              xDivision,
              initPositions
            });
            barWidth = positions.barWidth;
            barXPosition = positions.barXPosition;
          }
          paths = this.drawRangeColumnPaths(__spreadValues({
            indexes: { i: i2, j, realIndex, translationsIndex },
            barWidth,
            barXPosition,
            zeroH,
            xDivision
          }, params));
          barHeight = paths.barHeight;
        }
        const barGoalLine = this.barHelpers.drawGoalLine({
          barXPosition: paths.barXPosition,
          barYPosition,
          goalX: paths.goalX,
          goalY: paths.goalY,
          barHeight,
          barWidth
        });
        if (barGoalLine) {
          elGoalsMarkers.add(barGoalLine);
        }
        y = paths.y;
        x = paths.x;
        const pathFill = this.barHelpers.getPathFillColor(
          series,
          i2,
          j,
          realIndex
        );
        this.renderSeries({
          realIndex,
          pathFill: pathFill.color,
          lineFill: pathFill.useRangeColor ? pathFill.color : w2.globals.stroke.colors[realIndex],
          j,
          i: i2,
          x,
          y,
          y1,
          y2,
          pathFrom: paths.pathFrom,
          pathTo: paths.pathTo,
          strokeWidth,
          elSeries,
          series,
          barHeight,
          barWidth,
          barXPosition,
          barYPosition,
          columnGroupIndex,
          elDataLabelsWrap,
          elGoalsMarkers,
          visibleSeries: this.visibleI,
          type: "rangebar"
        });
      }
      ret.add(elSeries);
    }
    return ret;
  }
  /** @param {{ i?: any, j?: any, barYPosition?: any, barXPosition?: any, srty?: any, srtx?: any, barHeight?: any, barWidth?: any, yDivision?: any, xDivision?: any, initPositions?: any }} opts */
  detectOverlappingBars({
    i: i2,
    j,
    barYPosition,
    barXPosition,
    srty,
    srtx,
    barHeight,
    barWidth,
    yDivision,
    xDivision,
    initPositions
  }) {
    var _a, _b, _c, _d;
    const w2 = this.w;
    let overlaps = [];
    const rangeName = (_b = (_a = w2.globals.seriesRangeName) == null ? void 0 : _a[i2]) == null ? void 0 : _b[j];
    const x = (
      /** @type {Record<string,any>} */
      (_d = (_c = w2.config.series[i2].data) == null ? void 0 : _c[j]) == null ? void 0 : _d.x
    );
    const labelX = Array.isArray(x) ? x.join(" ") : x;
    const rowIndex = w2.labelData.labels.map((_) => Array.isArray(_) ? _.join(" ") : _).indexOf(labelX);
    const overlappedIndex = w2.rangeData.seriesRange[i2].findIndex(
      (tx) => {
        var _a2;
        return tx.x === labelX && ((_a2 = tx.overlaps) == null ? void 0 : _a2.size) > 0;
      }
    );
    if (this.isHorizontal) {
      if (w2.config.plotOptions.bar.rangeBarGroupRows) {
        barYPosition = srty + yDivision * rowIndex;
      } else {
        barYPosition = srty + barHeight * this.visibleI + yDivision * rowIndex;
      }
      if (overlappedIndex > -1 && !w2.config.plotOptions.bar.rangeBarOverlap) {
        overlaps = Array.from(
          /** @type {any} */
          w2.rangeData.seriesRange[i2][overlappedIndex].overlaps
        );
        if (overlaps.indexOf(rangeName) > -1) {
          barHeight = initPositions.barHeight / overlaps.length;
          barYPosition = barHeight * this.visibleI + yDivision * (100 - parseInt(this.barOptions.barHeight, 10)) / 100 / 2 + barHeight * (this.visibleI + overlaps.indexOf(rangeName)) + yDivision * rowIndex;
        }
      }
    } else {
      if (rowIndex > -1 && !w2.labelData.timescaleLabels.length) {
        if (w2.config.plotOptions.bar.rangeBarGroupRows) {
          barXPosition = srtx + xDivision * rowIndex;
        } else {
          barXPosition = srtx + barWidth * this.visibleI + xDivision * rowIndex;
        }
      }
      if (overlappedIndex > -1 && !w2.config.plotOptions.bar.rangeBarOverlap) {
        overlaps = Array.from(
          /** @type {any} */
          w2.rangeData.seriesRange[i2][overlappedIndex].overlaps
        );
        if (overlaps.indexOf(rangeName) > -1) {
          barWidth = initPositions.barWidth / overlaps.length;
          barXPosition = barWidth * this.visibleI + xDivision * (100 - parseInt(this.barOptions.barWidth, 10)) / 100 / 2 + barWidth * (this.visibleI + overlaps.indexOf(rangeName)) + xDivision * rowIndex;
        }
      }
    }
    return {
      barYPosition,
      barXPosition,
      barHeight,
      barWidth
    };
  }
  /** @param {{indexes: any, x: any, xDivision: any, barWidth: any, barXPosition: any, zeroH: any}} opts */
  drawRangeColumnPaths({
    indexes,
    x,
    xDivision,
    barWidth,
    barXPosition,
    zeroH
  }) {
    var _a, _b;
    const w2 = this.w;
    const { i: i2, j, realIndex, translationsIndex } = indexes;
    const yRatio = this.yRatio[translationsIndex];
    const range = this.getRangeValue(realIndex, j);
    let y1 = Math.min(range.start, range.end);
    let y2 = Math.max(range.start, range.end);
    if (typeof /** @type {any} */
    ((_a = this.series[i2]) == null ? void 0 : _a[j]) === "undefined" || /** @type {any} */
    ((_b = this.series[i2]) == null ? void 0 : _b[j]) === null) {
      y1 = zeroH;
    } else {
      y1 = zeroH - y1 / yRatio;
      y2 = zeroH - y2 / yRatio;
    }
    const barHeight = Math.abs(y2 - y1);
    const paths = this.barHelpers.getColumnPaths({
      barXPosition,
      barWidth,
      y1,
      y2,
      strokeWidth: this.strokeWidth,
      series: this.seriesRangeEnd,
      realIndex,
      i: realIndex,
      j,
      w: w2
    });
    if (!w2.axisFlags.isXNumeric) {
      x = x + xDivision;
    } else {
      const xForNumericXAxis = this.getBarXForNumericXAxis({
        x,
        j,
        realIndex,
        barWidth
      });
      x = xForNumericXAxis.x;
      barXPosition = xForNumericXAxis.barXPosition;
    }
    return {
      pathTo: paths.pathTo,
      pathFrom: paths.pathFrom,
      barHeight,
      x,
      y: range.start < 0 && range.end < 0 ? y1 : y2,
      goalY: this.barHelpers.getGoalValues(
        "y",
        /** @type {any} */
        null,
        zeroH,
        realIndex,
        j,
        translationsIndex
      ),
      barXPosition
    };
  }
  /**
   * @param {number} val
   */
  preventBarOverflow(val) {
    const w2 = this.w;
    if (val < 0) {
      val = 0;
    }
    if (val > w2.layout.gridWidth) {
      val = w2.layout.gridWidth;
    }
    return val;
  }
  /** @param {{indexes: any, y: any, y1: any, y2: any, yDivision: any, barHeight: any, barYPosition: any, zeroW: any}} opts */
  drawRangeBarPaths({
    indexes,
    y,
    y1,
    y2,
    yDivision,
    barHeight,
    barYPosition,
    zeroW
  }) {
    const w2 = this.w;
    const { realIndex, j } = indexes;
    const x1 = this.preventBarOverflow(zeroW + y1 / this.invertedYRatio);
    const x2 = this.preventBarOverflow(zeroW + y2 / this.invertedYRatio);
    const range = this.getRangeValue(realIndex, j);
    const barWidth = Math.abs(x2 - x1);
    const paths = this.barHelpers.getBarpaths({
      barYPosition,
      barHeight,
      x1,
      x2,
      strokeWidth: this.strokeWidth,
      series: this.seriesRangeEnd,
      i: realIndex,
      realIndex,
      j,
      w: w2
    });
    if (!w2.axisFlags.isXNumeric) {
      y = y + yDivision;
    }
    return {
      pathTo: paths.pathTo,
      pathFrom: paths.pathFrom,
      barWidth,
      x: range.start < 0 && range.end < 0 ? x1 : x2,
      goalX: this.barHelpers.getGoalValues(
        "x",
        zeroW,
        /** @type {any} */
        null,
        realIndex,
        j,
        0
      ),
      y
    };
  }
  /**
   * @param {number} i
   * @param {number} j
   */
  getRangeValue(i2, j) {
    const w2 = this.w;
    return {
      start: w2.rangeData.seriesRangeStart[i2][j],
      end: w2.rangeData.seriesRangeEnd[i2][j]
    };
  }
}
function normalize(data, area) {
  let sum = 0;
  for (let i2 = 0; i2 < data.length; i2++) {
    sum += data[i2];
  }
  const multiplier = area / sum;
  const result = new Array(data.length);
  for (let i2 = 0; i2 < data.length; i2++) {
    result[i2] = data[i2] * multiplier;
  }
  return result;
}
function calculateRatio(rowMin, rowMax, rowSum, length) {
  const lengthSq = length * length;
  const sumSq = rowSum * rowSum;
  return Math.max(
    lengthSq * rowMax / sumSq,
    sumSq / (lengthSq * rowMin)
  );
}
function improvesRatio(rowLen, rowMin, rowMax, rowSum, nextNode, length) {
  if (rowLen === 0) return true;
  const currentRatio = calculateRatio(rowMin, rowMax, rowSum, length);
  const newRatio = calculateRatio(
    Math.min(rowMin, nextNode),
    Math.max(rowMax, nextNode),
    rowSum + nextNode,
    length
  );
  return currentRatio >= newRatio;
}
function emitCoordinates(coords, row, rowLen, rowSum, xoffset, yoffset, width, height) {
  if (width >= height) {
    const areaWidth = rowSum / height;
    let subY = yoffset;
    for (let i2 = 0; i2 < rowLen; i2++) {
      const h2 = row[i2] / areaWidth;
      coords.push([xoffset, subY, xoffset + areaWidth, subY + h2]);
      subY += h2;
    }
  } else {
    const areaHeight = rowSum / width;
    let subX = xoffset;
    for (let i2 = 0; i2 < rowLen; i2++) {
      const w2 = row[i2] / areaHeight;
      coords.push([subX, yoffset, subX + w2, yoffset + areaHeight]);
      subX += w2;
    }
  }
}
function squarify(data, xoffset, yoffset, width, height) {
  const coords = [];
  const n2 = data.length;
  if (n2 === 0) return coords;
  const row = new Array(n2);
  let rowLen = 0;
  let rowSum = 0;
  let rowMin = Infinity;
  let rowMax = -Infinity;
  let i2 = 0;
  while (i2 < n2) {
    const length = Math.min(width, height);
    const val = data[i2];
    if (improvesRatio(rowLen, rowMin, rowMax, rowSum, val, length)) {
      row[rowLen] = val;
      rowLen++;
      rowSum += val;
      if (val < rowMin) rowMin = val;
      if (val > rowMax) rowMax = val;
      i2++;
    } else {
      emitCoordinates(coords, row, rowLen, rowSum, xoffset, yoffset, width, height);
      if (width >= height) {
        const areaWidth = rowSum / height;
        xoffset += areaWidth;
        width -= areaWidth;
      } else {
        const areaHeight = rowSum / width;
        yoffset += areaHeight;
        height -= areaHeight;
      }
      rowLen = 0;
      rowSum = 0;
      rowMin = Infinity;
      rowMax = -Infinity;
    }
  }
  if (rowLen > 0) {
    emitCoordinates(coords, row, rowLen, rowSum, xoffset, yoffset, width, height);
  }
  return coords;
}
function generate(data, width, height) {
  const n2 = data.length;
  const sums = new Array(n2);
  for (let i2 = 0; i2 < n2; i2++) {
    let s2 = 0;
    const series = data[i2];
    for (let j = 0; j < series.length; j++) {
      s2 += series[j];
    }
    sums[i2] = s2;
  }
  const seriesRects = squarify(
    normalize(sums, width * height),
    0,
    0,
    width,
    height
  );
  const results = new Array(n2);
  for (let i2 = 0; i2 < n2; i2++) {
    const rect = seriesRects[i2];
    const rx = rect[0];
    const ry = rect[1];
    const rw = rect[2] - rx;
    const rh = rect[3] - ry;
    results[i2] = squarify(
      normalize(data[i2], rw * rh),
      rx,
      ry,
      rw,
      rh
    );
  }
  return results;
}
function computeArea(node) {
  const kids = node.children;
  if (kids && kids.length) {
    let s2 = 0;
    for (let i2 = 0; i2 < kids.length; i2++) {
      s2 += computeArea(kids[i2]);
    }
    node._area = s2;
    return s2;
  }
  const v2 = Number(node.value);
  const a2 = isNaN(v2) ? 0 : Math.abs(v2);
  node._area = a2;
  return a2;
}
function layoutLevel(nodes, xoffset, yoffset, width, height, depth, padding, header) {
  const n2 = nodes.length;
  if (n2 === 0 || width <= 0 || height <= 0) return;
  const areas = new Array(n2);
  let total = 0;
  for (let i2 = 0; i2 < n2; i2++) {
    areas[i2] = nodes[i2]._area;
    total += areas[i2];
  }
  if (total <= 0) return;
  const rects = squarify(
    normalize(areas, width * height),
    xoffset,
    yoffset,
    width,
    height
  );
  for (let i2 = 0; i2 < n2; i2++) {
    const node = nodes[i2];
    const r2 = rects[i2];
    if (!r2) continue;
    node.rect = r2;
    node.depth = depth;
    const kids = node.children;
    if (!kids || !kids.length) continue;
    const rw = r2[2] - r2[0];
    const rh = r2[3] - r2[1];
    let pad = Math.max(0, padding(node, depth, rw, rh) || 0);
    if (pad * 2 >= rw || pad * 2 >= rh) pad = 0;
    let head = Math.max(0, header(node, depth, rw, rh) || 0);
    if (head > 0 && rh - pad * 2 - head < head) head = 0;
    node.headerHeight = head;
    layoutLevel(
      kids,
      r2[0] + pad,
      r2[1] + head + pad,
      rw - pad * 2,
      rh - head - pad * 2,
      depth + 1,
      padding,
      header
    );
  }
}
function generateNested(nodes, width, height, opts = {}) {
  if (!Array.isArray(nodes) || nodes.length === 0) return nodes || [];
  const padding = opts.padding || (() => 0);
  const header = opts.header || (() => 0);
  for (let i2 = 0; i2 < nodes.length; i2++) computeArea(nodes[i2]);
  layoutLevel(nodes, 0, 0, width, height, 0, padding, header);
  return nodes;
}
const TreemapSquared = { generate, generateNested };
const areaOf = (r2) => (r2[2] - r2[0]) * (r2[3] - r2[1]);
class TreemapChart {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
    this.strokeWidth = this.w.config.stroke.width;
    this.helpers = new TreemapHelpers(w2, ctx);
    this.dynamicAnim = this.w.config.chart.animations.dynamicAnimation;
    this.labels = [];
    this.roots = [];
    this.drawn = [];
    this.nested = false;
    this.showParents = false;
    this.scale = null;
    this._levelCache = null;
    this._total = null;
    this._avgLabelSize = null;
    this._tooltipEl = null;
    this._tipOwned = false;
    this._morphLeafIndex = 0;
  }
  /**
   * @param {any[]} series
   */
  draw(series) {
    var _a;
    const w2 = this.w;
    const graphics = new Graphics(this.w, this.ctx);
    const fill = new Fill(this.w);
    const ret = graphics.group({
      class: "apexcharts-treemap"
    });
    if (w2.globals.noData) return ret;
    series.forEach((s2) => {
      s2.map((v2) => {
        return Math.abs(v2);
      });
    });
    this.negRange = this.helpers.checkColorRange();
    w2.config.series.forEach((s2, i2) => {
      s2.data.forEach((l2) => {
        if (!Array.isArray(this.labels[i2])) this.labels[i2] = [];
        this.labels[i2].push(l2.x);
      });
    });
    const tree = getTreemapRoots(w2);
    this.nested = tree.nested;
    this.roots = tree.roots;
    this.scale = buildContinuousScale(w2);
    const drawn = tree.roots.length === 1 ? tree.roots[0].children || [] : tree.roots;
    this.drawn = drawn;
    const parentsCfg = w2.config.plotOptions.treemap.parents;
    this.showParents = parentsCfg.show === true || parentsCfg.show !== false && this.nested;
    const focus = this._resolveFocus(drawn);
    const layoutRoots = focus ? [focus] : drawn;
    TreemapSquared.generateNested(
      layoutRoots,
      w2.layout.gridWidth,
      w2.layout.gridHeight,
      {
        padding: (node, depth, rw, rh) => this.showParents ? this._levelPadding(depth, rw, rh) : 0,
        header: (node, depth, rw, rh) => this.showParents ? this._levelHeader(node, depth, rw, rh) : 0
      }
    );
    const morphSrc = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange;
    const morphActive = !!morphSrc && typeof morphSrc.isActive === "function" && morphSrc.isActive() && typeof morphSrc.getInitialPathAt === "function";
    this._morphLeafIndex = 0;
    const leavesBySeries = this._leavesBySeries(layoutRoots, w2.config.series.length);
    const parentsBySeries = this.showParents ? this._parentsBySeries(layoutRoots, w2.config.series.length) : [];
    leavesBySeries.forEach((node, i2) => {
      var _a2;
      const elSeries = graphics.group({
        class: `apexcharts-series apexcharts-treemap-series`,
        seriesName: Utils$1.escapeString(w2.seriesData.seriesNames[i2]),
        rel: i2 + 1,
        "data:realIndex": i2
      });
      graphics.setupEventDelegation(elSeries, ".apexcharts-treemap-rect");
      if (w2.config.chart.dropShadow.enabled) {
        const shadow = w2.config.chart.dropShadow;
        const filters = new Filters(this.w);
        filters.dropShadow(ret, shadow, i2);
      }
      const elDataLabelWrap = graphics.group({
        class: "apexcharts-data-labels"
      });
      const bounds = {
        xMin: Infinity,
        yMin: Infinity,
        xMax: -Infinity,
        yMax: -Infinity
      };
      if (this.showParents) {
        (parentsBySeries[i2] || []).forEach((p2) => {
          this._drawParent(elSeries, p2, i2);
        });
      }
      const animCfg = w2.config.chart.animations;
      const gradCfg = animCfg.animateGradually;
      const cascadeEnabled = gradCfg && gradCfg.enabled !== false;
      const cascadeDelays = new Array(node.length).fill(0);
      if (cascadeEnabled) {
        const tileCount = node.length || 1;
        const baseDelay = Math.min(
          gradCfg.delay || 0,
          animCfg.speed * 0.5 / tileCount
        );
        const ranked = node.map(
          /** @param {any} leaf @param {number} k */
          (leaf, k) => ({ j: k, area: leaf.rect ? areaOf(leaf.rect) : 0 })
        ).sort(
          /** @param {{j: number, area: number}} a @param {{j: number, area: number}} b */
          (a2, b2) => b2.area - a2.area
        );
        ranked.forEach(
          /** @param {{j: number, area: number}} item @param {number} rank */
          (item, rank) => {
            cascadeDelays[item.j] = rank * baseDelay;
          }
        );
      }
      node.forEach((leaf, k) => {
        const r2 = leaf.rect;
        if (!r2) return;
        const j = leaf._di;
        const x1 = r2[0];
        const y1 = r2[1];
        const x2 = r2[2];
        const y2 = r2[3];
        bounds.xMin = Math.min(bounds.xMin, x1);
        bounds.yMin = Math.min(bounds.yMin, y1);
        bounds.xMax = Math.max(bounds.xMax, x2);
        bounds.yMax = Math.max(bounds.yMax, y2);
        const colorProps = this._leafColor(i2, j);
        const color = colorProps.color;
        const pathFill = fill.fillPath({
          color,
          seriesNumber: i2,
          dataPointIndex: j
        });
        const morphFrom = morphActive ? this._morphSourceForLeaf(leaf) : null;
        const elRect = morphFrom ? graphics.drawPath({
          d: this._tilePath(x1, y1, x2, y2),
          fill: "#fff",
          stroke: w2.config.plotOptions.treemap.useFillColorAsStroke ? color : w2.globals.stroke.colors[i2],
          strokeWidth: this.strokeWidth,
          fillOpacity: 1
        }) : graphics.drawRect(
          x1,
          y1,
          x2 - x1,
          y2 - y1,
          w2.config.plotOptions.treemap.borderRadius,
          "#fff",
          1,
          this.strokeWidth,
          w2.config.plotOptions.treemap.useFillColorAsStroke ? color : w2.globals.stroke.colors[i2]
        );
        elRect.attr({
          cx: x1,
          cy: y1,
          index: i2,
          i: i2,
          j,
          width: x2 - x1,
          height: y2 - y1,
          fill: pathFill
        });
        elRect.node.classList.add("apexcharts-treemap-rect");
        elRect.node.setAttribute("data:key", morphKey(leaf._key));
        let fromRect = {
          x: x1 + (x2 - x1) / 2,
          y: y1 + (y2 - y1) / 2,
          width: 0,
          height: 0
        };
        const toRect = {
          x: x1,
          y: y1,
          width: x2 - x1,
          height: y2 - y1
        };
        if (morphFrom) {
          this._morphTile(
            elRect,
            morphFrom,
            this._tilePath(x1, y1, x2, y2),
            this.ctx.morphTypeChange.getSpeed(),
            i2,
            j
          );
        } else if (w2.config.chart.animations.enabled && !w2.globals.dataChanged) {
          let speed = 1;
          if (!w2.globals.resized) {
            speed = w2.config.chart.animations.speed;
          }
          this.animateTreemap(
            elRect,
            fromRect,
            toRect,
            speed,
            // Ranked by draw order, not by data index — the cascade is about
            // what is on screen.
            cascadeDelays[k] || 0
          );
        }
        if (w2.globals.dataChanged) {
          let speed = 1;
          if (this.dynamicAnim.enabled && w2.globals.shouldAnimate) {
            speed = this.dynamicAnim.speed;
            if (w2.globals.previousPaths[i2] && /** @type {Record<string,any>} */
            w2.globals.previousPaths[i2][j] && /** @type {Record<string,any>} */
            w2.globals.previousPaths[i2][j].rect) {
              fromRect = /** @type {Record<string,any>} */
              w2.globals.previousPaths[i2][j].rect;
            }
            this.animateTreemap(elRect, fromRect, toRect, speed);
          }
        }
        let fontSize = this.getFontSize(r2);
        if (w2.config.plotOptions.treemap.dataLabels.format === "truncate") {
          fontSize = parseInt(String(w2.config.dataLabels.style.fontSize), 10);
        }
        let dataLabels = null;
        if (w2.config.dataLabels.enabled && this._labelCanShow(fontSize, x2 - x1, y2 - y1)) {
          let formattedText = w2.config.dataLabels.formatter(this.labels[i2][j], {
            value: w2.seriesData.series[i2][j],
            seriesIndex: i2,
            dataPointIndex: j,
            w: w2
          });
          if (w2.config.plotOptions.treemap.dataLabels.format === "truncate") {
            formattedText = this.truncateLabels(
              String(formattedText),
              fontSize,
              x1,
              y1,
              x2,
              y2
            );
          }
          if (w2.seriesData.series[i2][j]) {
            dataLabels = this.helpers.calculateDataLabels({
              text: formattedText,
              x: (x1 + x2) / 2,
              y: (y1 + y2) / 2 + this.strokeWidth / 2 + fontSize / 3,
              i: i2,
              j,
              colorProps,
              fontSize,
              series
            });
          }
          if (w2.config.dataLabels.enabled && dataLabels) {
            this.rotateToFitLabel(
              dataLabels,
              fontSize,
              formattedText,
              x1,
              y1,
              x2,
              y2
            );
          }
        }
        elSeries.add(elRect);
        if (dataLabels !== null) {
          elSeries.add(dataLabels);
        }
      });
      const seriesTitle = w2.config.plotOptions.treemap.seriesTitle;
      if (!this.showParents && w2.config.series.length > 1 && seriesTitle && seriesTitle.show) {
        const sName = (
          /** @type {Record<string,any>} */
          w2.config.series[i2].name || ""
        );
        if (sName && bounds.xMin < Infinity && bounds.yMin < Infinity) {
          const {
            offsetX,
            offsetY,
            borderColor,
            borderWidth,
            borderRadius,
            style
          } = seriesTitle;
          const textColor = style.color || w2.config.chart.foreColor;
          const padding = {
            left: style.padding.left,
            right: style.padding.right,
            top: style.padding.top,
            bottom: style.padding.bottom
          };
          const textSize = graphics.getTextRects(
            sName,
            style.fontSize,
            style.fontFamily
          );
          const labelRectWidth = textSize.width + padding.left + padding.right;
          const labelRectHeight = textSize.height + padding.top + padding.bottom;
          const labelX = bounds.xMin + (offsetX || 0);
          const labelY = bounds.yMin + (offsetY || 0);
          const elLabelRect = graphics.drawRect(
            labelX,
            labelY,
            labelRectWidth,
            labelRectHeight,
            borderRadius,
            style.background,
            1,
            borderWidth,
            borderColor
          );
          const elLabelText = graphics.drawText({
            x: labelX + padding.left,
            y: labelY + padding.top + ((_a2 = textSize == null ? void 0 : textSize.height) != null ? _a2 : 0) * 0.75,
            text: sName,
            fontSize: style.fontSize,
            fontFamily: style.fontFamily,
            fontWeight: style.fontWeight,
            foreColor: textColor,
            cssClass: style.cssClass || ""
          });
          elSeries.add(elLabelRect);
          elSeries.add(elLabelText);
        }
      }
      elSeries.add(elDataLabelWrap);
      ret.add(elSeries);
    });
    this._renderBreadcrumb();
    return ret;
  }
  // ----------------------------------------------------------------- levels
  /**
   * Per-level config: `plotOptions.treemap.parents` is the base, and
   * `plotOptions.treemap.levels[depth]` overrides it. Depth 0 is the outermost
   * group actually drawn (the series, unless a single series was unwrapped).
   * @param {number} depth
   * @returns {any}
   */
  _levelCfg(depth) {
    if (!this._levelCache) this._levelCache = [];
    if (this._levelCache[depth]) return this._levelCache[depth];
    const tm = this.w.config.plotOptions.treemap;
    const base = tm.parents || {};
    const lvl = (tm.levels || [])[depth] || {};
    const merged = __spreadProps(__spreadValues(__spreadValues({}, base), lvl), {
      header: __spreadProps(__spreadValues(__spreadValues({}, base.header || {}), lvl.header || {}), {
        style: __spreadValues(__spreadValues({}, (base.header || {}).style || {}), (lvl.header || {}).style || {})
      }),
      hover: __spreadValues(__spreadValues({}, base.hover || {}), lvl.hover || {})
    });
    this._levelCache[depth] = merged;
    return merged;
  }
  /**
   * The inset between a parent's edge and its children, at this depth.
   * @param {number} depth
   * @param {number} _rw
   * @param {number} _rh
   * @returns {number}
   */
  _levelPadding(depth, _rw, _rh) {
    const cfg = this._levelCfg(depth);
    return Number(cfg.padding) || 0;
  }
  /**
   * The header strip height at this depth, or 0 when the tile is too small to
   * carry one. The geometry library clamps against its own box; this is the
   * legibility rule, which needs the font size and so lives here.
   * @param {any} node
   * @param {number} depth
   * @param {number} rw
   * @param {number} rh
   * @returns {number}
   */
  _levelHeader(node, depth, rw, rh) {
    const cfg = this._levelCfg(depth);
    const header = cfg.header || {};
    if (header.show === false) return 0;
    const h2 = Number(header.height);
    if (!Number.isFinite(h2) || h2 <= 0) return 0;
    const minWidth = Number(header.minWidth) || 40;
    if (rw < minWidth) return 0;
    if (rh < h2 * 2) return 0;
    return h2;
  }
  // ------------------------------------------------------------------ nodes
  /**
   * Leaves of the drawn tree, bucketed by the series they came from and kept in
   * depth-first order.
   * @param {any[]} roots
   * @param {number} seriesCount
   * @returns {any[][]}
   */
  _leavesBySeries(roots, seriesCount) {
    const out = new Array(Math.max(1, seriesCount));
    for (let i2 = 0; i2 < out.length; i2++) out[i2] = [];
    const walk = (node) => {
      if (node.children && node.children.length) {
        node.children.forEach(walk);
      } else {
        const si = node._si || 0;
        if (out[si]) out[si].push(node);
      }
    };
    roots.forEach(walk);
    return out;
  }
  /**
   * Non-leaf nodes of the drawn tree, bucketed by series, shallowest first so a
   * container is painted before anything nested inside it.
   * @param {any[]} roots
   * @param {number} seriesCount
   * @returns {any[][]}
   */
  _parentsBySeries(roots, seriesCount) {
    const out = new Array(Math.max(1, seriesCount));
    for (let i2 = 0; i2 < out.length; i2++) out[i2] = [];
    const walk = (node) => {
      if (!node.children || !node.children.length) return;
      const si = node._si || 0;
      if (out[si]) out[si].push(node);
      node.children.forEach(walk);
    };
    roots.forEach(walk);
    out.forEach(
      (list) => list.sort(
        (a2, b2) => (a2.depth || 0) - (b2.depth || 0)
      )
    );
    return out;
  }
  // ---------------------------------------------------------------- colours
  /**
   * A leaf's fill.
   *
   * Continuous colour (a datum carrying a second metric) takes over when it is
   * configured, because shading a tile by its own area value is precisely what
   * that mode replaces. Everything else - `colorScale.ranges`, `enableShades`,
   * `distributed`, negative handling - is the path it always was.
   *
   * The shape of the return value matches `Helpers.getShadeColor` so the
   * caller cannot tell the two apart.
   *
   * @param {number} i
   * @param {number} j
   * @returns {any}
   */
  _leafColor(i2, j) {
    const w2 = this.w;
    if (this.scale) {
      const cv = colorValueOf(w2, i2, j);
      if (cv != null) {
        const color = this.scale.at(cv);
        return {
          color,
          // The discrete path has never supplied a label colour, so only the
          // continuous path sets one: a diverging ramp runs right through the
          // middle of the luminance range and a fixed light-or-dark label
          // would be unreadable at one end or the other.
          foreColor: readableOn(color),
          colorProps: { color, foreColor: readableOn(color), percent: 0 }
        };
      }
    }
    return this.helpers.getShadeColor(
      w2.config.chart.type,
      i2,
      j,
      this.negRange
    );
  }
  // ---------------------------------------------------------------- parents
  /**
   * Resolved container/header colours for a level. Parent marks are chrome, not
   * data, so they default to a neutral tint of the background rather than to a
   * series colour: with continuous colour on the leaves, a coloured container
   * would read as another data value.
   * @param {number} depth
   * @returns {any}
   */
  _parentChrome(depth) {
    const w2 = this.w;
    const cfg = this._levelCfg(depth);
    const dark = w2.config.theme.mode === "dark";
    const header = cfg.header || {};
    const hstyle = header.style || {};
    const step = Math.min(depth, 3);
    const base = dark ? 255 : 0;
    const rgb = `${base},${base},${base}`;
    return {
      fill: cfg.fill || `rgba(${rgb},${(dark ? 0.04 : 0.03) + step * 0.02})`,
      fillOpacity: cfg.fillOpacity == null ? 1 : cfg.fillOpacity,
      borderColor: cfg.borderColor || `rgba(${rgb},${dark ? 0.18 : 0.14})`,
      borderWidth: cfg.borderWidth == null ? 1 : cfg.borderWidth,
      borderRadius: cfg.borderRadius == null ? w2.config.plotOptions.treemap.borderRadius : cfg.borderRadius,
      headerBg: hstyle.background || `rgba(${rgb},${dark ? 0.1 : 0.07})`,
      headerColor: hstyle.color || (dark ? "#e8e8e8" : w2.config.chart.foreColor),
      headerFontSize: hstyle.fontSize || "12px",
      headerFontFamily: hstyle.fontFamily || w2.config.chart.fontFamily,
      headerFontWeight: hstyle.fontWeight == null ? 600 : hstyle.fontWeight,
      hoverColor: cfg.hover && cfg.hover.color || (dark ? "rgba(255,255,255,0.85)" : "rgba(0,0,0,0.7)"),
      hoverWidth: cfg.hover && cfg.hover.width || 2,
      hoverShow: !(cfg.hover && cfg.hover.show === false)
    };
  }
  /**
   * Draw one parent as a real mark: a container rect, and the header strip the
   * layout already reserved room for.
   *
   * The container is `pointer-events: none` over its interior so the tiles
   * inside it keep receiving hover; only the padding gutter and the header
   * strip belong to the parent.
   *
   * @param {any} elSeries
   * @param {any} node
   * @param {number} i seriesIndex
   */
  _drawParent(elSeries, node, i2) {
    var _a;
    const r2 = node.rect;
    if (!r2) return;
    const w2 = this.w;
    const graphics = new Graphics(this.w, this.ctx);
    const depth = node.depth || 0;
    const cfg = this._levelCfg(depth);
    const chrome = this._parentChrome(depth);
    const x1 = r2[0];
    const y1 = r2[1];
    const width = r2[2] - r2[0];
    const height = r2[3] - r2[1];
    if (width <= 0 || height <= 0) return;
    const elGroup = graphics.group({
      class: "apexcharts-treemap-parent",
      "data:depth": depth,
      "data:name": Utils$1.escapeString(node.name)
    });
    const key = morphKey(node._key);
    const morphFrom = this._morphKeyed() ? this.ctx.morphTypeChange.getInitialPathForKey(key) : null;
    const elRect = morphFrom ? graphics.drawPath({
      d: this._tilePath(x1, y1, x1 + width, y1 + height),
      fill: chrome.fill,
      stroke: chrome.borderColor,
      strokeWidth: chrome.borderWidth,
      fillOpacity: chrome.fillOpacity
    }) : graphics.drawRect(
      x1,
      y1,
      width,
      height,
      chrome.borderRadius,
      chrome.fill,
      chrome.fillOpacity,
      chrome.borderWidth,
      chrome.borderColor
    );
    elRect.node.classList.add("apexcharts-treemap-parent-rect");
    elRect.node.setAttribute("data:key", key);
    elRect.node.setAttribute("data:depth", String(depth));
    if (morphFrom) {
      this._morphTile(
        elRect,
        morphFrom,
        this._tilePath(x1, y1, x1 + width, y1 + height),
        this.ctx.morphTypeChange.getSpeed(),
        i2,
        depth
      );
    }
    elGroup.add(elRect);
    const headerHeight = node.headerHeight || 0;
    let headerText = "";
    if (headerHeight > 0) {
      const header = cfg.header || {};
      const elHeaderRect = graphics.drawRect(
        x1,
        y1,
        width,
        headerHeight,
        0,
        chrome.headerBg,
        1,
        0,
        "transparent"
      );
      elHeaderRect.node.classList.add("apexcharts-treemap-parent-header");
      elGroup.add(elHeaderRect);
      let text = String((_a = node.name) != null ? _a : "");
      if (typeof header.formatter === "function") {
        text = String(
          header.formatter(node.name, {
            value: node.value,
            depth,
            seriesIndex: i2,
            node,
            w: w2
          })
        );
      } else if (header.showValue) {
        text = `${text}  ${this._formatValue(node.value)}`;
      }
      headerText = text;
      const offsetX = Number(header.offsetX) || 0;
      const align = header.align || "left";
      const pad = 6;
      const maxWidth = Math.max(0, width - pad * 2 - Math.abs(offsetX));
      const fontSize = parseFloat(String(chrome.headerFontSize)) || 12;
      const clipped = graphics.getTextBasedOnMaxWidth({
        text,
        maxWidth,
        fontSize
      });
      if (clipped) {
        let tx = x1 + pad + offsetX;
        let anchor = "start";
        if (align === "center") {
          tx = x1 + width / 2 + offsetX;
          anchor = "middle";
        } else if (align === "right") {
          tx = x1 + width - pad + offsetX;
          anchor = "end";
        }
        const elText = graphics.drawText({
          x: tx,
          y: y1 + headerHeight / 2 + fontSize / 3 + (Number(header.offsetY) || 0),
          text: clipped,
          textAnchor: anchor,
          fontSize: chrome.headerFontSize,
          fontFamily: chrome.headerFontFamily,
          fontWeight: chrome.headerFontWeight,
          foreColor: chrome.headerColor,
          cssClass: `apexcharts-treemap-parent-label ${header.style && header.style.cssClass || ""}`
        });
        elText.node.setAttribute("pointer-events", "none");
        elGroup.add(elText);
      }
      this._attachParentEvents(elHeaderRect.node, node, chrome, elRect);
      this._makeParentAccessible(
        elHeaderRect.node,
        node,
        chrome,
        elRect,
        headerText
      );
    }
    this._attachParentEvents(elRect.node, node, chrome, elRect);
    elGroup.node.setAttribute("role", "group");
    elGroup.node.setAttribute("aria-label", this._parentLabel(node, headerText));
    elSeries.add(elGroup);
  }
  /**
   * The spoken description of a branch: what it is, how big, and how much of
   * the chart it accounts for.
   *
   * Starts from the header's own rendered text when there is one (before
   * clipping), so the accessible name contains the visible label rather than a
   * differently-formatted number - a formatter that renders "$3.25T" must not
   * be announced as "3246".
   *
   * @param {any} node
   * @param {string} [visibleText] the header text, formatter applied, unclipped
   * @returns {string}
   */
  _parentLabel(node, visibleText) {
    const total = this._drawnTotal();
    const pct = total > 0 ? (node._area / total * 100).toFixed(1) : "0";
    const n2 = this._countLeaves(node);
    const lead = visibleText && String(visibleText).trim() ? String(visibleText).replace(/\s+/g, " ").trim() : `${node.name}, ${this._formatValue(node._area)}`;
    return `${lead}, ${n2} ${n2 === 1 ? "item" : "items"}, ${pct}% of total`;
  }
  /**
   * Make a branch reachable and operable from the keyboard.
   *
   * Only the header strip takes focus, and only while click-to-zoom is live: a
   * treemap can hold hundreds of tiles, and making every mark a tab stop would
   * bury the chart's own controls behind a few hundred presses. Tabbing the
   * groups and pressing Enter is the same path the mouse takes, and the
   * breadcrumb is already real buttons, so the way back is reachable too.
   *
   * @param {any} el
   * @param {any} node
   * @param {any} chrome
   * @param {any} elRect
   * @param {string} [visibleText] the header's rendered text, unclipped
   */
  _makeParentAccessible(el, node, chrome, elRect, visibleText) {
    if (!el || !el.setAttribute) return;
    if (!this._zoomEnabled()) return;
    el.setAttribute("role", "button");
    el.setAttribute("tabindex", "0");
    el.setAttribute(
      "aria-label",
      `${this._parentLabel(node, visibleText)}. Zoom in`
    );
    el.setAttribute("aria-expanded", "false");
    if (!Environment.isBrowser() || !el.addEventListener) return;
    el.addEventListener("focus", () => {
      elRect.node.setAttribute("stroke", chrome.hoverColor);
      elRect.node.setAttribute("stroke-width", String(chrome.hoverWidth + 1));
    });
    el.addEventListener("blur", () => {
      elRect.node.setAttribute("stroke", chrome.borderColor);
      elRect.node.setAttribute("stroke-width", String(chrome.borderWidth));
    });
    el.addEventListener("keydown", (e2) => {
      if (e2.key !== "Enter" && e2.key !== " " && e2.key !== "Spacebar") return;
      e2.preventDefault();
      this._zoomTo(node, true);
    });
  }
  /**
   * Hover outline, aggregate tooltip and click-to-zoom for a parent mark.
   * @param {any} el the element receiving the pointer
   * @param {any} node
   * @param {any} chrome
   * @param {any} elRect the container rect to outline
   */
  _attachParentEvents(el, node, chrome, elRect) {
    if (!Environment.isBrowser() || !el || !el.addEventListener) return;
    const w2 = this.w;
    if (chrome.hoverShow) {
      el.addEventListener("mouseenter", () => {
        elRect.node.setAttribute("stroke", chrome.hoverColor);
        elRect.node.setAttribute("stroke-width", String(chrome.hoverWidth));
      });
      el.addEventListener("mouseleave", () => {
        elRect.node.setAttribute("stroke", chrome.borderColor);
        elRect.node.setAttribute("stroke-width", String(chrome.borderWidth));
      });
    }
    if (w2.config.tooltip.enabled) {
      el.addEventListener(
        "mouseenter",
        (e2) => this._showParentTooltip(e2, node)
      );
      el.addEventListener(
        "mousemove",
        (e2) => this._positionTooltip(e2)
      );
      el.addEventListener("mouseleave", () => this._hideParentTooltip());
    }
    if (this._zoomEnabled()) {
      el.style.cursor = "pointer";
      el.addEventListener("click", () => this._zoomTo(node));
    }
  }
  // ------------------------------------------------------------------- zoom
  /**
   * Click-to-zoom is live only when nothing else already owns the click.
   *
   * The drilldown feature is the other way into a hierarchy on a treemap, and
   * the two are different models of the same gesture: drilldown replaces the
   * view one level at a time, zoom reframes a tree that stays whole. Running
   * both would fight over the same click and over the one breadcrumb slot in
   * the wrap, so drilldown wins where it is active and this stands down.
   * @returns {boolean}
   */
  _zoomEnabled() {
    const w2 = this.w;
    const z = w2.config.plotOptions.treemap.zoom;
    if (!z || !z.enabled || !this.showParents) return false;
    const dd = w2.config.drilldown;
    if (dd && dd.enabled && Array.isArray(dd.series) && dd.series.length) {
      if (!this._warnedZoomConflict) {
        this._warnedZoomConflict = true;
        console.warn(
          "ApexCharts treemap: `plotOptions.treemap.zoom` and the drilldown feature both navigate the hierarchy, so zoom is ignored here. Drop `drilldown.series` to zoom a nested treemap instead."
        );
      }
      return false;
    }
    return true;
  }
  /**
   * The focused branch for this render, looked up by the key stashed on the
   * last click. Keys are rebuilt identically from the same data, so the focus
   * survives a re-render; a key that no longer resolves (the data changed under
   * it) simply falls back to the whole tree.
   * @param {any[]} drawn
   * @returns {any}
   */
  _resolveFocus(drawn) {
    const key = this.w.globals.treemapFocusKey;
    if (!key || !this._zoomEnabled()) return null;
    let found = null;
    const walk = (node) => {
      if (found) return;
      if (node._key === key) {
        found = node;
        return;
      }
      if (node.children) node.children.forEach(walk);
    };
    drawn.forEach(walk);
    return found && found.children && found.children.length ? found : null;
  }
  /**
   * @param {any} node
   * @param {boolean} [restoreFocus] move focus into the new view once it is
   *   drawn. A zoom re-renders the chart, which destroys the element the
   *   keyboard user was standing on; without this they would be returned to the
   *   top of the document.
   */
  _zoomTo(node, restoreFocus = false) {
    const w2 = this.w;
    if (!node || !node.children || !node.children.length) return;
    const next = w2.globals.treemapFocusKey === node._key ? null : node._key;
    w2.globals.treemapFocusKey = next;
    this._hideParentTooltip();
    const done = this.ctx.update();
    if (!restoreFocus || !done || typeof done.then !== "function") return;
    done.then(() => {
      if (!Environment.isBrowser()) return;
      const crumb = (
        /** @type {any} */
        w2.dom.baseEl.querySelector(
          ".apexcharts-breadcrumb .apexcharts-breadcrumb-item"
        )
      );
      if (crumb && crumb.focus) {
        crumb.focus();
        return;
      }
      const header = (
        /** @type {any} */
        w2.dom.baseEl.querySelector(
          ".apexcharts-treemap-parent-header[tabindex]"
        )
      );
      if (header && header.focus) header.focus();
    });
  }
  /**
   * Outermost drawn group -> focus chain, for the breadcrumb.
   *
   * Stops at a drawn root rather than walking all the way to `_parent === null`:
   * when a single series was unwrapped, the series node is still every level-0
   * node's parent, and it is not a level the reader ever sees.
   */
  _focusChain() {
    const chain = [];
    const drawnRoots = new Set(this.drawn || []);
    let n2 = this._resolveFocus(this.drawn || []);
    while (n2) {
      chain.unshift(n2);
      if (drawnRoots.has(n2)) break;
      n2 = n2._parent;
    }
    return chain;
  }
  /** The breadcrumb config: a treemap-local override on the shared block. */
  _breadcrumbCfg() {
    const z = this.w.config.plotOptions.treemap.zoom;
    return breadcrumbConfig(this.w, z && z.breadcrumb);
  }
  /**
   * Breadcrumb back out of a zoom. Markup, config and accessible semantics are
   * the shared ones, so a zoomed treemap and a drilled-in chart present the
   * same affordance.
   */
  _renderBreadcrumb() {
    if (!Environment.isBrowser()) return;
    const w2 = this.w;
    if (!w2.dom.elWrap) return;
    clearBreadcrumb(w2);
    if (!this._zoomEnabled()) return;
    const chain = this._focusChain();
    if (!chain.length) return;
    const nav = renderBreadcrumb(w2, {
      ariaLabel: "Treemap breadcrumb",
      config: this._breadcrumbCfg(),
      compact: true,
      crumbs: [{ label: "All", data: null }].concat(
        chain.map((n2) => ({ label: n2.name, data: n2 }))
      ),
      onNavigate: (_i, crumb) => {
        w2.globals.treemapFocusKey = crumb.data ? crumb.data._key : null;
        this._hideParentTooltip();
        this.ctx.update();
      }
    });
    if (!nav) return;
    this._placeBreadcrumb(nav);
    avoidChromeOverlap(w2, nav);
  }
  /**
   * Sit the breadcrumb in the band the layout reserved for it
   * (Dimensions.gridPadForBreadcrumb), just above the grid.
   *
   * The fallback below should never fire: the reserve is unconditional once
   * zoom is enabled. It stays for the cases the reserve cannot cover - a
   * responsive override that turns zoom on after layout, or a host stylesheet
   * that grows the font - where a readable chip over the tiles beats a
   * breadcrumb clipped by the plot.
   *
   * @param {any} nav
   */
  _placeBreadcrumb(nav) {
    var _a, _b;
    const w2 = this.w;
    const gridTop = w2.layout.translateY || 0;
    const dimHelpers = (_b = (_a = this.ctx) == null ? void 0 : _a.dimensions) == null ? void 0 : _b.dimHelpers;
    const titleArea = dimHelpers ? dimHelpers.getTitleSubtitleCoords("title").height + dimHelpers.getTitleSubtitleCoords("subtitle").height : 0;
    const navH = nav.getBoundingClientRect().height || BREADCRUMB_HEIGHT;
    if (gridTop - titleArea >= navH + 1) {
      nav.style.top = `${gridTop - navH - 1}px`;
      return;
    }
    nav.style.top = `${titleArea}px`;
    const dark = w2.config.theme.mode === "dark";
    nav.style.background = dark ? "rgba(20,24,30,0.82)" : "rgba(255,255,255,0.86)";
    nav.style.borderRadius = "4px";
  }
  // ---------------------------------------------------------------- tooltip
  /**
   * A parent is not a row in the series matrix, so the shared tooltip - which
   * addresses everything by (seriesIndex, dataPointIndex) - has nothing to look
   * up. It writes into the same tooltip element instead, so the aggregate looks
   * like every other tooltip in the chart.
   * @param {MouseEvent} e
   * @param {any} node
   */
  _showParentTooltip(e2, node) {
    const w2 = this.w;
    const t2 = this._tip();
    if (!t2) return;
    const total = this._drawnTotal();
    const parentVal = node._parent ? node._parent._area : total;
    const pctTotal = total > 0 ? (node._area / total * 100).toFixed(1) : "0.0";
    const pctParent = parentVal > 0 ? (node._area / parentVal * 100).toFixed(1) : pctTotal;
    const cfg = this.w.config.plotOptions.treemap.parents;
    const custom = cfg && cfg.tooltip && cfg.tooltip.formatter;
    const leafCount = this._countLeaves(node);
    let html;
    if (typeof custom === "function") {
      html = custom({
        name: node.name,
        value: node._area,
        depth: node.depth || 0,
        leafCount,
        percentOfParent: Number(pctParent),
        percentOfTotal: Number(pctTotal),
        node,
        w: w2
      });
    } else {
      const marker = this._parentChrome(node.depth || 0).headerBg;
      const groupBg = w2.config.tooltip.fillSeriesColor ? `background-color:${marker};` : "";
      html = `<div class="apexcharts-tooltip-series-group apexcharts-active" style="display:flex;${groupBg}"><div class="apexcharts-tooltip-text"><div class="apexcharts-tooltip-y-group"><span class="apexcharts-tooltip-text-y-label">${Utils$1.escapeString(
        node.name
      )}: </span><span class="apexcharts-tooltip-text-y-value">${this._formatValue(
        node._area
      )}</span></div><div class="apexcharts-tooltip-y-group"><span class="apexcharts-tooltip-text-y-label">${leafCount} items, </span><span class="apexcharts-tooltip-text-y-value">${pctParent}% of parent, ${pctTotal}% of total</span></div></div></div>`;
    }
    t2.innerHTML = html;
    t2.classList.add("apexcharts-active");
    t2.style.opacity = "1";
    this._tipOwned = true;
    this._positionTooltip(e2);
  }
  /** @returns {any} */
  _tip() {
    if (!this._tooltipEl) {
      this._tooltipEl = this.w.dom.baseEl.querySelector(".apexcharts-tooltip");
    }
    return this._tooltipEl;
  }
  /**
   * Position beside the cursor, flipping to the opposite side when the box
   * would overflow the chart wrap, and clamping inside it either way.
   * @param {MouseEvent} e
   */
  _positionTooltip(e2) {
    const t2 = this._tip();
    if (!t2 || !this._tipOwned) return;
    const rect = this.w.dom.elWrap.getBoundingClientRect();
    const tw = t2.offsetWidth;
    const th = t2.offsetHeight;
    const pad = 12;
    let x = e2.clientX - rect.left + pad;
    if (x + tw > rect.width) x = e2.clientX - rect.left - tw - pad;
    x = Math.max(0, Math.min(x, rect.width - tw));
    let y = e2.clientY - rect.top + pad;
    if (y + th > rect.height) y = e2.clientY - rect.top - th - pad;
    y = Math.max(0, Math.min(y, rect.height - th));
    t2.style.left = x + "px";
    t2.style.top = y + "px";
  }
  _hideParentTooltip() {
    const t2 = this._tip();
    if (!t2 || !this._tipOwned) return;
    this._tipOwned = false;
    t2.classList.remove("apexcharts-active");
    t2.style.opacity = "0";
  }
  /**
   * The whole tree's area, zoomed in or not: "% of total" has to mean the same
   * thing at every zoom level, otherwise the branch you just zoomed into
   * reports 100%.
   * @returns {number}
   */
  _drawnTotal() {
    if (this._total == null) {
      this._total = (this.drawn || []).reduce(
        (s2, r2) => s2 + this._subtreeArea(r2),
        0
      );
    }
    return this._total || 0;
  }
  /**
   * A node's area, computed the same way the layout does it. Zooming lays out
   * only the focused branch, so every other branch reaches here without the
   * `_area` the layout would otherwise have left on it.
   * @param {any} node
   * @returns {number}
   */
  _subtreeArea(node) {
    if (node._area != null) return node._area;
    const kids = node.children;
    if (kids && kids.length) {
      let s2 = 0;
      for (let i2 = 0; i2 < kids.length; i2++) s2 += this._subtreeArea(kids[i2]);
      return s2;
    }
    const v2 = Number(node.value);
    return isNaN(v2) ? 0 : Math.abs(v2);
  }
  /**
   * @param {any} node
   * @returns {number}
   */
  _countLeaves(node) {
    if (!node.children || !node.children.length) return 1;
    return node.children.reduce(
      (s2, c2) => s2 + this._countLeaves(c2),
      0
    );
  }
  /**
   * Format an aggregate the way the chart formats its own y values, so a
   * parent's total reads like the tiles it contains.
   * @param {number} v
   * @returns {string}
   */
  _formatValue(v2) {
    var _a, _b, _c, _d, _e;
    const w2 = this.w;
    const fmt = ((_b = (_a = w2.config.tooltip) == null ? void 0 : _a.y) == null ? void 0 : _b.formatter) || ((_e = (_d = (_c = w2.config.yaxis) == null ? void 0 : _c[0]) == null ? void 0 : _d.labels) == null ? void 0 : _e.formatter);
    if (typeof fmt === "function") {
      try {
        return String(fmt(v2, { seriesIndex: 0, dataPointIndex: 0, w: w2 }));
      } catch (_) {
      }
    }
    return String(v2);
  }
  /**
   * Whether a tile could show a label at all, decided from geometry alone so
   * nothing has to be measured to find out.
   *
   * Two rules, both about legibility rather than about saving work:
   *   - a tile that cannot hold one character in EITHER orientation has no
   *     label to draw, rotated or not
   *   - below `dataLabels.minFontSize` the text is decoration, not information
   *
   * Together these keep a dense treemap from spending its whole render
   * measuring text nobody can read.
   *
   * @param {number} fontSize
   * @param {number} tileWidth
   * @param {number} tileHeight
   * @returns {boolean}
   */
  _labelCanShow(fontSize, tileWidth, tileHeight) {
    if (!Number.isFinite(fontSize) || fontSize <= 0) return false;
    const cfg = this.w.config.plotOptions.treemap.dataLabels;
    const min = cfg && cfg.minFontSize != null ? Number(cfg.minFontSize) : 0;
    if (fontSize < min) return false;
    return Math.max(tileWidth, tileHeight) >= fontSize;
  }
  /**
   * Mean label length across the whole chart.
   *
   * It scales every tile's font size, and it is the same number for all of
   * them, so it is computed once per draw. It used to be recomputed inside
   * `getFontSize`, which runs per tile: two full walks of every label, per
   * label, which is quadratic and was the entire cost of a large treemap (a
   * 10k-tile chart spent ~9s here).
   *
   * @returns {number}
   */
  _averageLabelSize() {
    if (this._avgLabelSize != null) return this._avgLabelSize;
    function totalLabelLength(arr) {
      let i2, total = 0;
      if (Array.isArray(arr[0])) {
        for (i2 = 0; i2 < arr.length; i2++) {
          total += totalLabelLength(arr[i2]);
        }
      } else {
        for (i2 = 0; i2 < arr.length; i2++) {
          total += arr[i2].length;
        }
      }
      return total;
    }
    function countLabels(arr) {
      let i2, total = 0;
      if (Array.isArray(arr[0])) {
        for (i2 = 0; i2 < arr.length; i2++) {
          total += countLabels(arr[i2]);
        }
      } else {
        for (i2 = 0; i2 < arr.length; i2++) {
          total += 1;
        }
      }
      return total;
    }
    this._avgLabelSize = totalLabelLength(this.labels) / countLabels(this.labels);
    return this._avgLabelSize;
  }
  // This calculates a font-size based upon
  // average label length and the size of the box
  /**
   * @param {number[]} coordinates
   */
  getFontSize(coordinates) {
    const w2 = this.w;
    const averagelabelsize = this._averageLabelSize();
    function fontSize(width, height) {
      const area = width * height;
      const arearoot = Math.pow(area, 0.5);
      return Math.min(
        arearoot / averagelabelsize,
        parseInt(w2.config.dataLabels.style.fontSize, 10)
      );
    }
    return fontSize(
      coordinates[2] - coordinates[0],
      coordinates[3] - coordinates[1]
    );
  }
  /**
   * @param {any} elText
   * @param {string | number} fontSize
   * @param {string} text
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   */
  rotateToFitLabel(elText, fontSize, text, x1, y1, x2, y2) {
    const graphics = new Graphics(this.w);
    const textRect = graphics.getTextRects(text, String(fontSize));
    if (textRect.width + this.w.config.stroke.width + 5 > x2 - x1 && textRect.width <= y2 - y1) {
      const labelRotatingCenter = graphics.rotateAroundCenter(elText.node);
      elText.node.setAttribute(
        "transform",
        `rotate(-90 ${labelRotatingCenter.x} ${labelRotatingCenter.y}) translate(${textRect.height / 3})`
      );
    }
  }
  // This is an alternative label formatting method that uses a
  // consistent font size, and trims the edge of long labels
  /**
   * @param {string} text
   * @param {number} fontSize
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   */
  truncateLabels(text, fontSize, x1, y1, x2, y2) {
    const graphics = new Graphics(this.w);
    const textRect = graphics.getTextRects(text, String(fontSize));
    const labelMaxWidth = textRect.width + this.w.config.stroke.width + 5 > x2 - x1 && y2 - y1 > x2 - x1 ? y2 - y1 : x2 - x1;
    const truncatedText = graphics.getTextBasedOnMaxWidth({
      text,
      maxWidth: labelMaxWidth,
      fontSize
    });
    if (text.length !== truncatedText.length && labelMaxWidth / fontSize < 5) {
      return "";
    } else {
      return truncatedText;
    }
  }
  /**
   * True when the active cross-type morph can pair marks by branch identity
   * rather than by draw order.
   * @returns {boolean}
   */
  _morphKeyed() {
    var _a;
    const m2 = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange;
    return !!(m2 && typeof m2.hasKeyedMarks === "function" && m2.hasKeyedMarks() && typeof m2.getInitialPathForKey === "function");
  }
  /**
   * The captured shape a LEAF tile unrolls from, or null when the outgoing
   * chart had nothing to give it.
   *
   * Branch identity first, so a tile unrolls from the arc that stood for the
   * same row rather than the k-th one. That key can find nothing even when
   * both sides carry keys, and a FLAT treemap taking from a nested sunburst is
   * exactly that case: its tiles are keyed at depth one ('/0:Tops') while the
   * arcs are keyed at the depth they actually sit ('/0:Apparel/0:Tops'), so no
   * key ever matches. Draw order is the fallback, the same one the mirror
   * direction has always had (Sunburst._morphSourceFor), and without it
   * treemap -> sunburst read as a morph while the return trip grew from
   * nothing.
   *
   * Leaves only. A container taking a leaf's path by position would unroll
   * from a different branch entirely, so `_morphParent` stays key-or-nothing.
   *
   * @param {any} leaf
   * @returns {string | null}
   */
  _morphSourceForLeaf(leaf) {
    var _a;
    const morph = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange;
    if (!morph || typeof morph.getInitialPathAt !== "function") return null;
    if (this._morphKeyed()) {
      const keyed = morph.getInitialPathForKey(morphKey(leaf._key));
      if (keyed) return keyed;
    }
    return morph.getInitialPathAt(this._morphLeafIndex++);
  }
  /**
   * A tile as closed path data, for the cross-type morph (a <rect> cannot hold
   * an arc, so a morphing tile is drawn as a <path>).
   * @param {number} x1 @param {number} y1 @param {number} x2 @param {number} y2
   * @returns {string}
   */
  _tilePath(x1, y1, x2, y2) {
    return `M ${x1} ${y1} L ${x2} ${y1} L ${x2} ${y2} L ${x1} ${y2} Z`;
  }
  /**
   * Tween a tile's path data from a captured shape (a sunburst arc) into its
   * rectangle.
   *
   * Routed through Animations.morphSVG, the same call every other morphing
   * renderer makes: it already selects the polygon-resample algorithm while a
   * cross-type morph is active, which is what tweens between shapes as
   * different as an arc and a rectangle. Reaching for the interpolator directly
   * would also add a name to a module the split bundles share, which breaks
   * them.
   *
   * @param {any} el
   * @param {string} fromD
   * @param {string} toD
   * @param {number} speed
   * @param {number} i
   * @param {number} j
   */
  _morphTile(el, fromD, toD, speed, i2, j) {
    const animations = new Animations(this.w, this.ctx);
    animations.morphSVG(el, i2, j, "none", fromD, toD, speed, 0);
  }
  /**
   * @param {any} el
   * @param {Record<string, any>} fromRect
   * @param {Record<string, any>} toRect
   * @param {number} speed
   * @param {number} [delay] - per-tile cascade delay in ms
   */
  animateTreemap(el, fromRect, toRect, speed, delay = 0) {
    const animations = new Animations(this.w);
    animations.animateRect(
      el,
      fromRect,
      toRect,
      speed,
      () => {
        animations.animationCompleted(el);
      },
      delay
    );
  }
}
const GOLDEN_ANGLE = Math.PI * (3 - Math.sqrt(5));
function easeOutCubic(t2) {
  return 1 - Math.pow(1 - t2, 3);
}
function easeOutBack(s2) {
  return (t2) => 1 + (s2 + 1) * Math.pow(t2 - 1, 3) + s2 * Math.pow(t2 - 1, 2);
}
function easeInOutCubic(t2) {
  return t2 < 0.5 ? 4 * t2 * t2 * t2 : 1 - Math.pow(-2 * t2 + 2, 3) / 2;
}
const SPRING_REFERENCE_SPEED = 800;
const MAX_FRAME_STEP = 0.25;
function springParams(preset, speed) {
  const [stiffness, damping] = L(
    /** @type {import('apex-commons').SpringPreset|undefined} */
    preset
  );
  const scale = SPRING_REFERENCE_SPEED / Math.max(1, speed);
  return [stiffness * scale * scale, damping * scale];
}
class Unit {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
    this._lastDotR = 1;
    this._gridTrack = null;
    this._gridDenom = 1;
    this._scatterAxis = null;
  }
  /**
   * @param {any[]} series - flat count array (non-axis / pie-shaped data)
   * @returns {any} the chart's root group element
   */
  draw(series) {
    const w2 = this.w;
    const graphics = new Graphics(w2, this.ctx);
    const ret = graphics.group({ class: "apexcharts-unit" });
    if (w2.globals.noData || !Array.isArray(series) || series.length === 0) {
      return ret;
    }
    const opts = w2.config.plotOptions.unit;
    const layout = opts.layout === "packed" ? "packed" : opts.layout === "columns" ? "columns" : opts.layout === "grid" ? "grid" : opts.layout === "scatter" ? "scatter" : opts.layout === "arc" ? "arc" : opts.layout === "custom" ? "custom" : "grouped";
    const transition = opts.transition;
    const flow = transition === "flow";
    const identity = transition === "identity";
    const unitValue = opts.unitValue > 0 ? opts.unitValue : 1;
    let counts = series.map((v2) => {
      const n2 = Math.abs(Utils$1.parseNumber(v2)) / unitValue;
      return n2 > 0 ? Math.max(1, Math.round(n2)) : 0;
    });
    counts = this._applyMaxUnits(counts, opts.maxUnits);
    const total = counts.reduce((a2, b2) => a2 + b2, 0);
    const clusters = layout === "packed" ? this._layoutPacked(counts, opts) : layout === "columns" ? this._layoutColumns(counts, opts) : layout === "grid" ? this._layoutGrid(counts, opts) : layout === "scatter" ? this._layoutScatter(opts) : layout === "arc" ? this._layoutArc(counts, opts) : layout === "custom" ? this._layoutCustom(counts, opts) : this._layoutGrouped(counts, opts);
    const gridSplit = layout === "grid" && !!(opts.grid && opts.grid.split);
    if (gridSplit) this._drawGridTrack(ret, graphics, opts);
    if (layout === "scatter") this._drawScatterAxes(ret, graphics);
    const dotR = this._lastDotR;
    const animate = this._shouldAnimate();
    const morph = this.ctx && this.ctx.morphTypeChange;
    const morphActive = animate && !!morph && typeof morph.isActive === "function" && morph.isActive() && typeof morph.getInitialCenterFor === "function";
    const perRowBurst = morphActive && typeof morph.getInitialSlotFor === "function";
    const pieceTakeover = morphActive && typeof morph.usesPieceTakeover === "function" && morph.usesPieceTakeover();
    const prev = animate && !morphActive && this.ctx ? this.ctx._unitPrevDots : null;
    const nextPrev = /* @__PURE__ */ new Map();
    const animDots = [];
    const unitData = w2.seriesData.unitData || [];
    const sizeStats = this._bubbleStats(unitData, opts, dotR);
    let gIndex = 0;
    clusters.forEach((cluster) => {
      const color = w2.globals.colors[cluster.i] || w2.globals.colors[0] || "#008FFB";
      const elSeries = graphics.group({
        class: "apexcharts-series",
        seriesName: Utils$1.escapeString(
          w2.seriesData.seriesNames[cluster.i] || `series-${cluster.i + 1}`
        ),
        rel: cluster.i + 1,
        "data:realIndex": cluster.i
      });
      const burst = morphActive && !perRowBurst ? morph.getInitialCenterFor(cluster.i) : null;
      const burstCount = cluster.dots.length;
      const catData = unitData[cluster.i];
      cluster.dots.forEach((d, j) => {
        const datum = catData ? catData[j] : void 0;
        const dotFill = datum && typeof datum === "object" && datum.fillColor ? datum.fillColor : color;
        const rj = d.r != null ? d.r : sizeStats ? this._radiusForValue(this._unitValueOf(datum), sizeStats) : dotR;
        const el = this._drawDot(graphics, opts, rj, dotFill, cluster.i, j);
        elSeries.add(el);
        let key;
        if (identity) {
          const id = datum && typeof datum === "object" ? datum.id != null ? datum.id : datum.name : void 0;
          key = id != null ? `id:${id}` : `g:${gIndex}`;
        } else if (flow) {
          key = String(gIndex);
        } else if (d.slot != null) {
          key = `slot:${d.slot}`;
        } else {
          key = `${cluster.i}:${j}`;
        }
        gIndex++;
        nextPrev.set(key, { x: d.x, y: d.y, fill: dotFill, r: rj });
        if (pieceTakeover) {
          this._placeDot(el.node, opts, d.x, d.y);
          el.node.setAttribute("opacity", "0");
          el.node.setAttribute("data-piece-hidden", "1");
        } else if (animate) {
          const from = prev && prev.get(key);
          const anchor = from || (perRowBurst ? morph.getInitialSlotFor(cluster.i, j, burstCount) : burst);
          const enter = opts.gather && opts.gather.enter || "burst";
          const inPlace = gridSplit || enter === "fade" || enter === "rise";
          const cx0 = anchor ? anchor.x : inPlace ? d.x : cluster.cx;
          const cy0 = anchor ? anchor.y : enter === "rise" && !gridSplit ? d.y + 14 : inPlace ? d.y : cluster.cy;
          el.node.style.opacity = anchor ? "1" : "0";
          this._placeDot(el.node, opts, cx0, cy0);
          animDots.push({
            node: el.node,
            x: d.x,
            y: d.y,
            cx0,
            cy0,
            // Carries live spring state (position AND velocity) forward when
            // this render interrupted one still in flight.
            key,
            // Radius tween: an identity-kept dot grows/shrinks from its previous
            // size to its new one (e.g. bubble sizing turning on) instead of
            // snapping. Enters/uniform updates keep r0 === r1 (no-op).
            r0: from && from.r != null ? from.r : rj,
            r1: rj,
            // Colour tween: a dot that flows into a differently coloured group
            // recolours as it travels rather than snapping at the first frame.
            fill0: from ? from.fill : dotFill,
            fill1: dotFill,
            delay: 0,
            // assigned below (staggered by global order)
            isEnter: !anchor
          });
        } else {
          this._placeDot(el.node, opts, d.x, d.y);
        }
      });
      if ((layout === "grouped" || layout === "columns" || gridSplit) && opts.clusterLabels && opts.clusterLabels.show && counts[cluster.i] > 0) {
        const labelTotal = gridSplit ? this._gridDenom : total;
        this._drawClusterLabel(elSeries, cluster, counts[cluster.i], labelTotal, opts, color);
      }
      ret.add(elSeries);
    });
    if (prev) {
      const exits = this._collectExits(prev, nextPrev, opts);
      if (exits.length) {
        const exitGroup = graphics.group({ class: "apexcharts-unit-exits" });
        ret.add(exitGroup);
        this._runExits(exitGroup, exits, opts);
      }
    }
    if (this.ctx) this.ctx._unitPrevDots = nextPrev;
    if (this.ctx && (!animate || morphActive)) this.ctx._unitSprings = null;
    if (animate && animDots.length) {
      this._runGather(animDots);
    }
    return ret;
  }
  /**
   * Cap total dots to `maxUnits`, scaling every category down proportionally
   * (a non-zero category keeps at least one dot). Warns once when it clips.
   * @param {number[]} counts
   * @param {number} maxUnits
   * @returns {number[]}
   */
  _applyMaxUnits(counts, maxUnits) {
    const total = counts.reduce((a2, b2) => a2 + b2, 0);
    if (!maxUnits || maxUnits <= 0 || total <= maxUnits) return counts;
    const scale = maxUnits / total;
    console.warn(
      `[ApexCharts] unit chart: ${total} dots exceeds maxUnits (${maxUnits}); counts were scaled down proportionally. Raise plotOptions.unit.maxUnits or use plotOptions.unit.unitValue to represent more units per dot.`
    );
    return counts.map((c2) => c2 > 0 ? Math.max(1, Math.round(c2 * scale)) : 0);
  }
  /**
   * `layout: 'custom'`. Positions come from a caller-supplied provider rather
   * than from a generator in this file.
   *
   * The provider is the whole extension point: `(objects, rect) => [{id, x, y,
   * r?}]`. Everything downstream is unchanged, which is the point - the engine
   * already tweens position, radius and colour, and already keeps a mark's
   * identity across a relayout, so an arbitrary new arrangement needs no new
   * transition code. A silhouette, a hex grid, a timeline, or a projection
   * handed over by ApexMaps are all just this function.
   *
   * Marks the provider omits are dropped, so they animate out through the
   * existing exit path. Ids matching no mark are ignored.
   *
   * @param {number[]} counts
   * @param {any} opts
   */
  _layoutCustom(counts, opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const total = counts.reduce((a2, b2) => a2 + b2, 0);
    const provider = this._resolveLayoutProvider(opts);
    if (!provider) {
      console.warn(
        `[ApexCharts] unit chart: layout 'custom' needs plotOptions.unit.positions (a function, or the name of a layout registered with ApexCharts.registerUnitLayout). Falling back to 'grouped'.`
      );
      return this._layoutGrouped(counts, opts);
    }
    const availR = Math.sqrt(gw * gh / Math.PI);
    const step = this._resolveStep(opts, availR, total);
    this._lastDotR = this._dotRadiusFromStep(step, opts);
    const dotR = this._lastDotR;
    const objects = this._layoutObjects(counts, dotR);
    const rect = { x: 0, y: 0, width: gw, height: gh };
    let placed;
    try {
      placed = provider(objects, rect);
    } catch (e2) {
      console.warn(
        "[ApexCharts] unit chart: the layout provider threw; falling back to 'grouped'.",
        e2
      );
      return this._layoutGrouped(counts, opts);
    }
    if (!Array.isArray(placed)) {
      console.warn(
        "[ApexCharts] unit chart: the layout provider must return an array of {id, x, y}; falling back to 'grouped'."
      );
      return this._layoutGrouped(counts, opts);
    }
    const byId = /* @__PURE__ */ new Map();
    placed.forEach((p2) => {
      if (!p2 || !isFinite(p2.x) || !isFinite(p2.y)) return;
      byId.set(String(p2.id), {
        x: p2.x,
        y: p2.y,
        r: typeof p2.r === "number" && p2.r > 0 ? p2.r : void 0
      });
    });
    const clusters = counts.map((_, i2) => ({
      i: i2,
      cx: gw / 2,
      cy: gh / 2,
      outerR: dotR,
      dots: []
    }));
    objects.forEach((o2) => {
      const hit = byId.get(o2.id);
      if (!hit) return;
      clusters[o2.seriesIndex].dots.push({ x: hit.x, y: hit.y, r: hit.r });
    });
    clusters.forEach((c2) => {
      if (!c2.dots.length) return;
      let sx = 0;
      let sy = 0;
      c2.dots.forEach((d) => {
        sx += d.x;
        sy += d.y;
      });
      c2.cx = sx / c2.dots.length;
      c2.cy = sy / c2.dots.length;
      let far = 0;
      c2.dots.forEach((d) => {
        far = Math.max(far, Math.hypot(d.x - c2.cx, d.y - c2.cy));
      });
      c2.outerR = far + dotR;
    });
    return clusters;
  }
  /**
   * One entry per mark, in global draw order, for a layout provider.
   *
   * `id` is the datum's own id/name where the per-unit object form supplies
   * one, so a provider can address a specific unit ("Texas", "employee 41")
   * rather than a positional slot. It falls back to `"<category>:<index>"`.
   *
   * @param {number[]} counts
   * @param {number} dotR the radius the engine would use, so a provider that
   *   packs by size does not have to rediscover it
   * @returns {{id:string,index:number,seriesIndex:number,dataPointIndex:number,label:string,value:number|undefined,datum:any,r:number}[]}
   */
  _layoutObjects(counts, dotR) {
    const w2 = this.w;
    const unitData = w2.seriesData.unitData || [];
    const names = w2.seriesData.seriesNames || [];
    const objects = [];
    let index = 0;
    counts.forEach((n2, i2) => {
      var _a;
      const catData = unitData[i2];
      for (let j = 0; j < n2; j++) {
        const datum = catData ? catData[j] : void 0;
        const id = datum && typeof datum === "object" && (datum.id != null || datum.name != null) ? String(datum.id != null ? datum.id : datum.name) : `${i2}:${j}`;
        objects.push({
          id,
          index,
          seriesIndex: i2,
          dataPointIndex: j,
          label: names[i2],
          // Normalised at the boundary: internally "no value" is null, but the
          // public object shape uses an absent property.
          value: (_a = this._unitValueOf(datum)) != null ? _a : void 0,
          datum,
          r: dotR
        });
        index++;
      }
    });
    return objects;
  }
  /**
   * Resolve `plotOptions.unit.positions` to a provider function: either the
   * function itself, or the name of one registered through
   * `ApexCharts.registerUnitLayout`.
   * @param {any} opts
   * @returns {Function|null}
   */
  _resolveLayoutProvider(opts) {
    const positions = opts.positions;
    if (typeof positions === "function") return positions;
    if (typeof positions === "string" && positions) {
      const found = getUnitLayout(positions);
      if (found) return found;
      console.warn(
        `[ApexCharts] unit chart: no layout named "${positions}" is registered. Register one with ApexCharts.registerUnitLayout("${positions}", fn).`
      );
    }
    return null;
  }
  /**
   * Lay out each category as its own cluster in a horizontal row. All clusters
   * share one dot radius (so dot size is comparable across clusters); the blob
   * radius encodes the count.
   * @param {number[]} counts
   * @param {any} opts
   */
  _layoutGrouped(counts, opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const labelSpace = opts.clusterLabels && opts.clusterLabels.show ? 30 : 6;
    const visible = counts.map((_, i2) => i2).filter((i2) => counts[i2] > 0);
    const Kv = Math.max(1, visible.length);
    const slotOf = new Array(counts.length).fill(-1);
    visible.forEach((i2, s2) => slotOf[i2] = s2);
    const cellW = gw / Kv;
    const availH = gh - labelSpace;
    const maxCount = Math.max(1, ...counts);
    const pad = Math.min(cellW, availH) * 0.08;
    const availR = Math.max(4, Math.min(cellW, availH) / 2 - pad);
    const step = this._resolveStep(opts, availR, maxCount);
    this._lastDotR = this._dotRadiusFromStep(step, opts);
    const dotR = this._lastDotR;
    const cy = labelSpace + availH / 2;
    const outerRs = counts.map((n2) => step * Math.sqrt(Math.max(1, n2)) + dotR);
    const cellCentre = (i2) => slotOf[i2] >= 0 ? cellW * (slotOf[i2] + 0.5) : gw / 2;
    let centers = counts.map((_, i2) => cellCentre(i2));
    const visOuter = visible.map((i2) => outerRs[i2]);
    let overlap = false;
    for (let s2 = 1; s2 < Kv; s2++) {
      if (centers[visible[s2]] - centers[visible[s2 - 1]] < visOuter[s2] + visOuter[s2 - 1]) {
        overlap = true;
        break;
      }
    }
    if (overlap) {
      const gap = Math.max(2 * dotR, 8);
      const totalW = visOuter.reduce((a2, r2) => a2 + 2 * r2, 0) + gap * (Kv - 1);
      let visCenters;
      if (totalW <= gw) {
        let x = (gw - totalW) / 2;
        visCenters = visOuter.map((r2) => {
          const c2 = x + r2;
          x += 2 * r2 + gap;
          return c2;
        });
      } else if (Kv === 1) {
        visCenters = [gw / 2];
      } else {
        const lo = visOuter[0];
        const hi = gw - visOuter[Kv - 1];
        visCenters = visOuter.map((_, s2) => lo + (hi - lo) * s2 / (Kv - 1));
      }
      centers = counts.map(
        (_, i2) => slotOf[i2] >= 0 ? visCenters[slotOf[i2]] : gw / 2
      );
    }
    return counts.map((n2, i2) => ({
      i: i2,
      cx: centers[i2],
      cy,
      outerR: outerRs[i2],
      dots: this._spiral(centers[i2], cy, n2, step, 0)
    }));
  }
  /**
   * Lay out all categories into ONE packed blob. Dots are assigned spiral
   * indices in category order (smallest-first when sortByGroup), so the
   * minority group nests in the centre.
   * @param {number[]} counts
   * @param {any} opts
   */
  _layoutPacked(counts, opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const labelSpace = 6;
    const total = Math.max(1, counts.reduce((a2, b2) => a2 + b2, 0));
    const availR = Math.max(
      4,
      Math.min(gw, gh - labelSpace) / 2 - Math.min(gw, gh) * 0.06
    );
    const step = this._resolveStep(opts, availR, total);
    this._lastDotR = this._dotRadiusFromStep(step, opts);
    const cx = gw / 2;
    const cy = labelSpace + (gh - labelSpace) / 2;
    const order = counts.map((_, i2) => i2);
    if (opts.sortByGroup !== false) {
      order.sort((a2, b2) => counts[a2] - counts[b2]);
    }
    const clusters = counts.map((_, i2) => ({
      i: i2,
      cx,
      cy,
      outerR: step * Math.sqrt(total) + this._lastDotR,
      /** @type {{x:number,y:number,slot?:number}[]} */
      dots: []
    }));
    let gi = 0;
    order.forEach((catI) => {
      for (let j = 0; j < counts[catI]; j++) {
        const r2 = step * Math.sqrt(gi + 0.5);
        const theta = gi * GOLDEN_ANGLE;
        clusters[catI].dots.push({
          x: cx + r2 * Math.cos(theta),
          y: cy + r2 * Math.sin(theta),
          slot: gi
        });
        gi++;
      }
    });
    return clusters;
  }
  /**
   * Lay out all marks as a PARLIAMENT / hemicycle: seats in concentric arced
   * rows across an annulus, filled in category (party) order so each category
   * forms a contiguous angular wedge (the classic seating chart). `arc` controls
   * the sweep (`startAngle`/`endAngle`, radialBar convention: 0 = top, clockwise;
   * default a top semicircle), the donut hole (`innerRadiusRatio`) and the row
   * count (`rows`, or 'auto'). Like `packed` this is ONE shared shape coloured by
   * category, so seats key by physical slot: a seat-count change recolours the
   * party boundary in place and only the rim adds / removes seats.
   * @param {number[]} counts
   * @param {any} opts
   */
  _layoutArc(counts, opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const total = Math.max(1, counts.reduce((a2, b3) => a2 + b3, 0));
    const acfg = opts.arc || {};
    const startDeg = typeof acfg.startAngle === "number" ? acfg.startAngle : -90;
    const endDeg = typeof acfg.endAngle === "number" ? acfg.endAngle : 90;
    const a0 = startDeg * Math.PI / 180;
    const a1 = endDeg * Math.PI / 180;
    const span = a1 - a0 || Math.PI;
    const innerRatio = Math.max(
      0,
      Math.min(0.95, typeof acfg.innerRadiusRatio === "number" ? acfg.innerRadiusRatio : 0.4)
    );
    const ux = (a2) => Math.sin(a2);
    const uy = (a2) => -Math.cos(a2);
    const b2 = this._arcBounds(a0, a1);
    const pad = Math.min(gw, gh) * 0.04;
    const boxW = Math.max(1e-6, b2.maxX - b2.minX);
    const boxH = Math.max(1e-6, b2.maxY - b2.minY);
    const r1 = Math.max(4, Math.min((gw - 2 * pad) / boxW, (gh - 2 * pad) / boxH));
    const r0 = r1 * innerRatio;
    const cx = gw / 2 - (b2.minX + b2.maxX) / 2 * r1;
    const cy = gh / 2 - (b2.minY + b2.maxY) / 2 * r1;
    const alloc = this._arcAllocate(total, r0, r1, span, opts);
    this._lastDotR = alloc.dotR;
    const seats = [];
    for (let r2 = 0; r2 < alloc.R; r2++) {
      const rho = alloc.radii[r2];
      const n2 = alloc.seatsPerRow[r2];
      for (let k = 0; k < n2; k++) {
        const a2 = n2 === 1 ? (a0 + a1) / 2 : a0 + span * (k + 0.5) / n2;
        seats.push({ a: a2, x: cx + rho * ux(a2), y: cy + rho * uy(a2) });
      }
    }
    seats.sort((s1, s2) => s1.a - s2.a);
    const clusters = counts.map((_, i2) => ({
      i: i2,
      cx,
      cy,
      outerR: r1,
      /** @type {{x:number,y:number,slot?:number}[]} */
      dots: []
    }));
    let ci = 0;
    let used = 0;
    seats.forEach((s2, slot) => {
      while (ci < counts.length && used >= counts[ci]) {
        ci++;
        used = 0;
      }
      if (ci >= counts.length) return;
      clusters[ci].dots.push({ x: s2.x, y: s2.y, slot });
      used++;
    });
    return clusters;
  }
  /**
   * Bounding box of the outer arc (radius 1) over [a0, a1], including the centre
   * and every cardinal angle (multiple of 90deg) inside the range, so a
   * semicircle / full circle / arbitrary sweep is all bounded correctly.
   * @param {number} a0 @param {number} a1
   * @returns {{minX:number,maxX:number,minY:number,maxY:number}}
   */
  _arcBounds(a0, a1) {
    const ux = (a2) => Math.sin(a2);
    const uy = (a2) => -Math.cos(a2);
    const lo = Math.min(a0, a1);
    const hi = Math.max(a0, a1);
    const xs = [0, ux(a0), ux(a1)];
    const ys = [0, uy(a0), uy(a1)];
    const q = Math.PI / 2;
    for (let k = Math.ceil(lo / q); k * q <= hi; k++) {
      xs.push(ux(k * q));
      ys.push(uy(k * q));
    }
    return {
      minX: Math.min(...xs),
      maxX: Math.max(...xs),
      minY: Math.min(...ys),
      maxY: Math.max(...ys)
    };
  }
  /**
   * Allocate `total` seats across concentric rows of the annulus [r0, r1] sweeping
   * `span` radians: seats per row are proportional to the row radius (a longer arc
   * holds more), summed EXACTLY to total by largest remainder. Row count is
   * `arc.rows` if given, else derived from a fixed dot size, else auto-searched to
   * maximise the dot radius (the largest dots that still pack without overlap,
   * mirroring `size:'auto'` elsewhere).
   * @param {number} total @param {number} r0 @param {number} r1 @param {number} span @param {any} opts
   * @returns {{R:number, radii:number[], seatsPerRow:number[], dotR:number}}
   */
  _arcAllocate(total, r0, r1, span, opts) {
    const spacing = opts.spacing > 0 ? opts.spacing : 1.05;
    const absSpan = Math.abs(span) || Math.PI;
    const fixed = this._fixedRadius(opts);
    const evalR = (R) => {
      R = Math.max(1, Math.round(R));
      const radii = [];
      for (let r2 = 0; r2 < R; r2++) {
        radii.push(R === 1 ? (r0 + r1) / 2 : r0 + (r1 - r0) * (r2 / (R - 1)));
      }
      const weightSum = radii.reduce((a2, x) => a2 + x, 0) || 1;
      const raw = radii.map((rho) => total * rho / weightSum);
      const seatsPerRow = raw.map((x) => Math.floor(x));
      let left = total - seatsPerRow.reduce((a2, x) => a2 + x, 0);
      raw.map((x, idx) => ({ idx, frac: x - Math.floor(x) })).sort((p2, qq) => qq.frac - p2.frac).forEach((o2) => {
        if (left > 0) {
          seatsPerRow[o2.idx]++;
          left--;
        }
      });
      while (left > 0) {
        seatsPerRow[R - 1]++;
        left--;
      }
      const radialPitch = R === 1 ? r1 - r0 || r1 : (r1 - r0) / (R - 1);
      let minArcPitch = Infinity;
      for (let r2 = 0; r2 < R; r2++) {
        const n2 = seatsPerRow[r2];
        if (n2 <= 0) continue;
        const arcPitch = radii[r2] * absSpan / n2;
        if (arcPitch < minArcPitch) minArcPitch = arcPitch;
      }
      const pitch = Math.min(radialPitch, minArcPitch);
      return { R, radii, seatsPerRow, dotR: Math.max(1, pitch / (2 * spacing)) };
    };
    const arcRows = opts.arc && opts.arc.rows;
    let res;
    if (typeof arcRows === "number" && arcRows >= 1) {
      res = evalR(arcRows);
    } else if (fixed) {
      const pitch = 2 * fixed * spacing;
      res = evalR((r1 - r0) / pitch + 1);
    } else {
      const maxR = Math.max(1, Math.min(40, Math.ceil(Math.sqrt(total)) + 6));
      res = evalR(1);
      for (let R = 2; R <= maxR; R++) {
        const cand = evalR(R);
        if (cand.dotR > res.dotR) res = cand;
      }
    }
    if (fixed) res.dotR = fixed;
    return res;
  }
  /**
   * Lay out each category as a vertical BAR built from stacked dots (a unit /
   * waffle column). Every bar shares one dot size and one width (the same
   * number of dot columns); the bar's HEIGHT encodes its count. Dots fill each
   * bar bottom-up, row by row. This is the "dot bar" state the circle layouts
   * morph into: with `transition:'flow'` the dots glide straight from their
   * circle slots into these bar slots (see the storyboard sample).
   * @param {number[]} counts
   * @param {any} opts
   */
  _layoutColumns(counts, opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const labelsOn = !!(opts.clusterLabels && opts.clusterLabels.show);
    const labelsBelow = labelsOn && opts.clusterLabels.position === "bottom";
    const topPad = labelsOn && !labelsBelow ? 30 : 6;
    const visible = counts.map((_, i2) => i2).filter((i2) => counts[i2] > 0);
    const Kv = Math.max(1, visible.length);
    const slotOf = new Array(counts.length).fill(-1);
    visible.forEach((i2, s2) => slotOf[i2] = s2);
    const cellW = gw / Kv;
    const barW = cellW * 0.62;
    const bottomPad = Math.max(8, gh * 0.04) + (labelsBelow ? 30 : 0);
    const availH = Math.max(4, gh - topPad - bottomPad);
    const maxCount = Math.max(1, ...counts);
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const colSize = opts.columns ? opts.columns.size : void 0;
    let fixed;
    if (opts.shape !== "image" && colSize === "auto") {
      fixed = null;
    } else if (opts.shape !== "image" && typeof colSize === "number" && colSize > 0) {
      fixed = colSize;
    } else {
      fixed = this._fixedRadius(opts);
    }
    let cols = 1;
    let pitch = 0;
    if (fixed) {
      pitch = 2 * fixed * spacing;
      this._lastDotR = fixed;
      const rowsCap = Math.max(1, Math.floor(availH / pitch));
      const maxColsByWidth = Math.max(1, Math.floor(barW / pitch));
      cols = Math.max(1, Math.min(maxColsByWidth, Math.ceil(maxCount / rowsCap)));
    } else {
      let best = 0;
      const maxCols = Math.max(1, Math.min(40, Math.round(barW / 4)));
      for (let c2 = 1; c2 <= maxCols; c2++) {
        const rows = Math.ceil(maxCount / c2);
        const d = Math.min(barW / c2, availH / rows);
        if (d > best) {
          best = d;
          cols = c2;
        }
      }
      pitch = best;
      this._lastDotR = Math.max(1, pitch / (2 * spacing));
    }
    const r2 = this._lastDotR;
    const maxRows = Math.ceil(maxCount / cols);
    const tallestBarH = Math.min(availH, maxRows * pitch);
    const bottom = topPad + (availH + tallestBarH) / 2;
    return counts.map((n2, i2) => {
      const cx = slotOf[i2] >= 0 ? cellW * (slotOf[i2] + 0.5) : gw / 2;
      const rows = Math.ceil(Math.max(1, n2) / cols);
      const barH = rows * pitch;
      const left = cx - cols * pitch / 2 + pitch / 2;
      const dots = [];
      for (let j = 0; j < n2; j++) {
        const rowIdx = Math.floor(j / cols);
        const colIdx = j % cols;
        dots.push({
          x: left + colIdx * pitch,
          y: bottom - r2 - rowIdx * pitch
        });
      }
      return {
        i: i2,
        cx,
        cy: bottom - barH / 2,
        outerR: barH / 2,
        // Flag read by _drawClusterLabel: a bar takes a straight label (above
        // or below per clusterLabels.position), never a curved arc.
        flat: true,
        dots
      };
    });
  }
  /**
   * Lay out ALL categories into ONE regular lattice - a waffle / grid. Dots take
   * sequential slots in DECLARED category order and fill row-major, `columns`
   * wide, so each category owns a contiguous band of cells: a part-to-whole
   * square "pie". `grid.total` (optional) re-allocates the cells to a fixed
   * budget (e.g. 100) by largest remainder, so the grid reads as exact
   * percentages regardless of the raw totals; without it there is one cell per
   * unit (respecting unitValue / maxUnits). `grid.fillFrom` picks the first row.
   * The category bands follow the legend order (no smallest-first sort), and
   * each physical slot is keyed so a proportion change recolours boundary cells
   * in place rather than reshuffling the whole grid.
   * @param {number[]} counts
   * @param {any} opts
   */
  _layoutGrid(counts, opts) {
    if (opts.grid && opts.grid.split) return this._layoutGridSplit(counts, opts);
    this._gridTrack = null;
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const gcfg = opts.grid || {};
    const cols = Math.max(1, Math.round(gcfg.columns > 0 ? gcfg.columns : 10));
    const fillFrom = gcfg.fillFrom === "top" ? "top" : "bottom";
    const cells = gcfg.total > 0 ? this._largestRemainder(counts, Math.round(gcfg.total)) : counts.slice();
    const totalCells = cells.reduce((a2, b2) => a2 + b2, 0);
    const rows = Math.max(1, Math.ceil(Math.max(1, totalCells) / cols));
    const labelSpace = 6;
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const availW = Math.max(4, gw);
    const availH = Math.max(4, gh - labelSpace);
    const fixed = this._fixedRadius(opts);
    let pitch = 0;
    if (fixed) {
      pitch = 2 * fixed * spacing;
      this._lastDotR = fixed;
    } else {
      pitch = Math.min(availW / cols, availH / rows);
      this._lastDotR = Math.max(1, pitch / (2 * spacing));
    }
    const blockW = cols * pitch;
    const blockH = rows * pitch;
    const originX = (gw - blockW) / 2 + pitch / 2;
    const topY = labelSpace + (availH - blockH) / 2;
    const rowY = (rowIdx) => fillFrom === "bottom" ? topY + blockH - pitch / 2 - rowIdx * pitch : topY + pitch / 2 + rowIdx * pitch;
    const clusters = counts.map((_, i2) => ({
      i: i2,
      cx: gw / 2,
      cy: labelSpace + availH / 2,
      outerR: Math.max(blockW, blockH) / 2,
      /** @type {{x:number,y:number,slot?:number}[]} */
      dots: []
    }));
    let k = 0;
    for (let ci = 0; ci < cells.length; ci++) {
      for (let j = 0; j < cells[ci]; j++) {
        const col = k % cols;
        const rowIdx = Math.floor(k / cols);
        clusters[ci].dots.push({
          x: originX + col * pitch,
          y: rowY(rowIdx),
          slot: k
        });
        k++;
      }
    }
    return clusters;
  }
  /**
   * Small-multiple ("trellis") waffles: ONE mini-waffle per category, laid out
   * in a near-square grid of tiles. Each tile has `grid.total` cells (default
   * 100 -> a 10x10 tile) and fills a fraction of them equal to the category's
   * value over a denominator (`grid.max`, else the largest count so the leader
   * fills its tile and every other tile stays proportionally full - no empty
   * tiles for arbitrary data). The unfilled cells are drawn as a faint TRACK
   * backdrop (see _drawGridTrack) so each tile reads as a part-to-whole "of N".
   * Only VISIBLE (non-zero) categories claim a tile, so a legend hide drops the
   * tile and the rest re-flow. Each filled cell is keyed by a physical
   * `tile*cells + localCell` slot, so a value change grows/shrinks a tile's fill
   * in place instead of reshuffling.
   * @param {number[]} counts @param {any} opts
   */
  _layoutGridSplit(counts, opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const gcfg = opts.grid || {};
    const cols = Math.max(1, Math.round(gcfg.columns > 0 ? gcfg.columns : 10));
    const fillFrom = gcfg.fillFrom === "top" ? "top" : "bottom";
    const cellsPerTile = Math.max(1, Math.round(gcfg.total > 0 ? gcfg.total : 100));
    const rowsPerTile = Math.max(1, Math.ceil(cellsPerTile / cols));
    const visible = counts.map((_, i2) => i2).filter((i2) => counts[i2] > 0);
    const K = Math.max(1, visible.length);
    const denom = gcfg.max > 0 ? gcfg.max : Math.max(1, ...counts);
    const tileCols = Math.max(
      1,
      Math.round(gcfg.tileColumns > 0 ? gcfg.tileColumns : Math.ceil(Math.sqrt(K)))
    );
    const tileRows = Math.max(1, Math.ceil(K / tileCols));
    const labelsOn = !(opts.clusterLabels && opts.clusterLabels.show === false);
    const labelsBelow = labelsOn && opts.clusterLabels && opts.clusterLabels.position === "bottom";
    const topBand = labelsOn && !labelsBelow ? 22 : 4;
    const botBand = labelsOn && labelsBelow ? 22 : 4;
    const tileW = gw / tileCols;
    const tileH = gh / tileRows;
    const availTileW = Math.max(4, tileW * 0.86);
    const availTileH = Math.max(4, tileH - topBand - botBand);
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const fixed = this._fixedRadius(opts);
    let pitch = 0;
    if (fixed) {
      pitch = 2 * fixed * spacing;
      this._lastDotR = fixed;
    } else {
      pitch = Math.min(availTileW / cols, availTileH / rowsPerTile);
      this._lastDotR = Math.max(1, pitch / (2 * spacing));
    }
    const blockW = cols * pitch;
    const blockH = rowsPerTile * pitch;
    const rowY = (topY, rowIdx) => fillFrom === "bottom" ? topY + blockH - pitch / 2 - rowIdx * pitch : topY + pitch / 2 + rowIdx * pitch;
    const track = [];
    const clusters = [];
    visible.forEach((ci, t2) => {
      const tc = t2 % tileCols;
      const tr = Math.floor(t2 / tileCols);
      const tileX = tc * tileW;
      const tileYtop = tr * tileH;
      const originX = tileX + (tileW - blockW) / 2 + pitch / 2;
      const topY = tileYtop + topBand + (availTileH - blockH) / 2;
      const cellXY = (k) => ({
        x: originX + k % cols * pitch,
        y: rowY(topY, Math.floor(k / cols))
      });
      for (let k = 0; k < cellsPerTile; k++) track.push(cellXY(k));
      const filled = Math.max(
        0,
        Math.min(cellsPerTile, Math.round(counts[ci] / denom * cellsPerTile))
      );
      const dots = [];
      for (let k = 0; k < filled; k++) {
        const p2 = cellXY(k);
        dots.push({ x: p2.x, y: p2.y, slot: t2 * cellsPerTile + k });
      }
      clusters.push({
        i: ci,
        cx: tileX + tileW / 2,
        cy: topY + blockH / 2,
        outerR: blockH / 2,
        // Straight per-tile label (never a curved arc), placed by position.
        flat: true,
        split: true,
        dots
      });
    });
    this._gridDenom = denom;
    this._gridTrack = { cells: track };
    return clusters;
  }
  /**
   * Draw the faint "track" backdrop for the small-multiple grid: every cell of
   * every tile's full lattice, so the filled (coloured) cells drawn on top read
   * as a fraction of the whole. Static (redrawn each render, never animated);
   * painted BEHIND the series groups. `grid.trackColor` overrides the default
   * theme-neutral grey.
   * @param {any} ret @param {Graphics} graphics @param {any} opts
   */
  _drawGridTrack(ret, graphics, opts) {
    const track = this._gridTrack;
    if (!track || !track.cells || !track.cells.length) return;
    const r2 = this._lastDotR;
    const gcfg = opts.grid || {};
    const trackColor = gcfg.trackColor || "rgba(128,128,128,0.14)";
    const g2 = graphics.group({ class: "apexcharts-unit-track" });
    track.cells.forEach((c2) => {
      let el;
      if (opts.shape === "square") {
        const side = r2 * 2;
        el = graphics.drawRect(0, 0, side, side, opts.borderRadius || 0, trackColor, 1, 0, "none");
        el.node.setAttribute("fill", trackColor);
        el.node.setAttribute("x", String(c2.x - r2));
        el.node.setAttribute("y", String(c2.y - r2));
      } else {
        el = graphics.drawCircle(r2, { fill: trackColor, "stroke-width": 0, stroke: "none" });
        el.node.setAttribute("fill", trackColor);
        el.node.setAttribute("cx", String(c2.x));
        el.node.setAttribute("cy", String(c2.y));
      }
      el.node.classList.add("apexcharts-unit-track-cell");
      g2.add(el);
    });
    ret.add(g2);
  }
  /**
   * Scatter / beeswarm layout: position every unit on a real numeric X value
   * axis by its own value (`_unitValueOf`), laned by category on Y. Within a
   * lane an anti-overlap "swarm" pack (or a random jitter) spreads the dots off
   * the centre line so equal / close values do not stack on top of each other.
   * This is the unit chart's answer to "put these on axes": one dot per datum,
   * placed by data, with a drawn value axis + category lanes (see
   * _drawScatterAxes). Needs the per-unit object form (each datum a numeric
   * `value`/`y`); flat counts have no per-unit value, so their lanes stay empty.
   * @param {any} opts
   */
  _layoutScatter(opts) {
    const w2 = this.w;
    const scfg = opts.scatter || {};
    if (scfg.y === "value") return this._layoutScatter2D(opts);
    if (scfg.orientation === "vertical") return this._layoutScatterVertical(opts);
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const unitData = w2.seriesData.unitData || [];
    const names = w2.seriesData.seriesNames || [];
    const valueOf = (d) => this._unitValueOf(d);
    const sizeStats = this._scatterSizeStats(scfg, unitData);
    const catVals = unitData.map(
      (cat) => Array.isArray(cat) ? cat.map(valueOf) : []
    );
    const isNum = (v2) => v2 != null && isFinite(v2);
    const visible = catVals.map((_, i2) => i2).filter((i2) => catVals[i2].some(isNum));
    const Kv = Math.max(1, visible.length);
    let vmin = Infinity;
    let vmax = -Infinity;
    catVals.forEach(
      (vs) => vs.forEach((v2) => {
        if (v2 != null && isFinite(v2)) {
          if (v2 < vmin) vmin = v2;
          if (v2 > vmax) vmax = v2;
        }
      })
    );
    if (vmin === Infinity) {
      vmin = 0;
      vmax = 1;
    }
    const tickAmount = Math.max(2, Math.round(scfg.tickAmount > 0 ? scfg.tickAmount : 5));
    const domain = this._scatterValueDomain(scfg, vmin, vmax, tickAmount);
    const xMin = domain.min;
    const xMax = domain.max;
    const xSpan = xMax - xMin || 1;
    const laneW = scfg.laneLabelWidth != null ? Math.max(0, scfg.laneLabelWidth) : Kv > 1 ? 92 : 8;
    const bottomGutter = 30 + (scfg.xTitle ? 20 : 0);
    const plotL = laneW;
    const plotR = gw - 8;
    const plotT = 10;
    const plotB = gh - bottomGutter;
    const plotW = Math.max(4, plotR - plotL);
    const plotH = Math.max(4, plotB - plotT);
    const plotX = (v2) => plotL + (v2 - xMin) / xSpan * plotW;
    const laneH = plotH / Kv;
    const laneCy = (slot) => plotT + laneH * (slot + 0.5);
    let r2 = 0;
    const fixed = this._fixedRadius(opts);
    if (fixed) {
      r2 = fixed;
    } else {
      const maxLane = Math.max(
        1,
        ...visible.map((i2) => catVals[i2].filter(isNum).length)
      );
      r2 = Math.max(
        2,
        Math.min(6, laneH * 0.12, plotW / (2.5 * Math.sqrt(maxLane)))
      );
    }
    this._lastDotR = r2;
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const step = Math.max(0.5, r2 * spacing);
    const jitter = scfg.spread === "jitter";
    const clusters = [];
    const lanes = [];
    const maxR = sizeStats ? sizeStats.rMax : r2;
    visible.forEach((ci, slot) => {
      const cy = laneCy(slot);
      lanes.push({ i: ci, cy, name: names[ci] || `series-${ci + 1}` });
      const cat = unitData[ci] || [];
      const pts = cat.map((d, j) => {
        const v2 = valueOf(d);
        const p2 = { j, px: plotX(isNum(v2) ? v2 : xMin), y: cy };
        if (sizeStats) p2.r = this._scatterRadius(d, sizeStats, r2);
        return p2;
      });
      if (jitter) {
        const halfLane = Math.max(maxR, laneH / 2 - maxR);
        pts.forEach((p2, k) => {
          const t2 = (k * 9301 + 49297) % 233280 / 233280;
          p2.y = cy + (t2 * 2 - 1) * halfLane;
        });
      } else {
        this._beeswarm(pts, cy, r2, step, maxR);
      }
      clusters.push({
        i: ci,
        cx: (plotL + plotR) / 2,
        cy,
        outerR: laneH / 2,
        dots: pts.map((p2) => ({ x: p2.px, y: p2.y, r: p2.r }))
      });
    });
    const ticks = domain.ticks;
    this._scatterAxis = {
      mode: "1d",
      plotL,
      plotR,
      plotT,
      plotB,
      xMin,
      xMax,
      plotX,
      ticks,
      lanes,
      xTitle: scfg.xTitle,
      formatter: typeof scfg.xFormatter === "function" ? scfg.xFormatter : null,
      gridlines: scfg.gridlines !== false
    };
    return clusters;
  }
  /**
   * Vertical beeswarm: the transpose of _layoutScatter. The value runs UP the Y
   * axis and each category is a column (lane) across X; the swarm pack spreads
   * dots horizontally off each column's centre line. The value-axis config keys
   * (`xMin`/`xMax`/`xTitle`/`xFormatter`/`tickAmount`) still describe the value
   * axis (now Y), so flipping `orientation` keeps the same value settings.
   * @param {any} opts
   */
  _layoutScatterVertical(opts) {
    const w2 = this.w;
    const scfg = opts.scatter || {};
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const unitData = w2.seriesData.unitData || [];
    const names = w2.seriesData.seriesNames || [];
    const valueOf = (d) => this._unitValueOf(d);
    const sizeStats = this._scatterSizeStats(scfg, unitData);
    const catVals = unitData.map(
      (cat) => Array.isArray(cat) ? cat.map(valueOf) : []
    );
    const isNum = (v2) => v2 != null && isFinite(v2);
    const visible = catVals.map((_, i2) => i2).filter((i2) => catVals[i2].some(isNum));
    const Kv = Math.max(1, visible.length);
    let vmin = Infinity;
    let vmax = -Infinity;
    catVals.forEach(
      (vs) => vs.forEach((v2) => {
        if (v2 != null && isFinite(v2)) {
          if (v2 < vmin) vmin = v2;
          if (v2 > vmax) vmax = v2;
        }
      })
    );
    if (vmin === Infinity) {
      vmin = 0;
      vmax = 1;
    }
    const tickAmount = Math.max(2, Math.round(scfg.tickAmount > 0 ? scfg.tickAmount : 5));
    const domain = this._scatterValueDomain(scfg, vmin, vmax, tickAmount);
    const vMin = domain.min;
    const vMax = domain.max;
    const vSpan = vMax - vMin || 1;
    const leftGutter = 46 + (scfg.xTitle ? 18 : 0);
    const bottomGutter = Kv > 1 ? 26 : 10;
    const plotL = leftGutter;
    const plotR = gw - 10;
    const plotT = 10;
    const plotB = gh - bottomGutter;
    const plotW = Math.max(4, plotR - plotL);
    const plotH = Math.max(4, plotB - plotT);
    const plotY = (v2) => plotB - (v2 - vMin) / vSpan * plotH;
    const laneW = plotW / Kv;
    const laneCx = (slot) => plotL + laneW * (slot + 0.5);
    let r2 = 0;
    const fixed = this._fixedRadius(opts);
    if (fixed) {
      r2 = fixed;
    } else {
      const maxLane = Math.max(
        1,
        ...visible.map((i2) => catVals[i2].filter(isNum).length)
      );
      r2 = Math.max(
        2,
        Math.min(6, laneW * 0.12, plotH / (2.5 * Math.sqrt(maxLane)))
      );
    }
    this._lastDotR = r2;
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const step = Math.max(0.5, r2 * spacing);
    const jitter = scfg.spread === "jitter";
    const clusters = [];
    const lanes = [];
    const maxR = sizeStats ? sizeStats.rMax : r2;
    visible.forEach((ci, slot) => {
      const cx = laneCx(slot);
      lanes.push({ i: ci, cx, name: names[ci] || `series-${ci + 1}` });
      const cat = unitData[ci] || [];
      const pts = cat.map((d, j) => {
        const v2 = valueOf(d);
        const p2 = { j, py: plotY(isNum(v2) ? v2 : vMin), x: cx };
        if (sizeStats) p2.r = this._scatterRadius(d, sizeStats, r2);
        return p2;
      });
      if (jitter) {
        const halfLane = Math.max(maxR, laneW / 2 - maxR);
        pts.forEach((p2, k) => {
          const t2 = (k * 9301 + 49297) % 233280 / 233280;
          p2.x = cx + (t2 * 2 - 1) * halfLane;
        });
      } else {
        this._beeswarm(pts, cx, r2, step, maxR, true);
      }
      clusters.push({
        i: ci,
        cx,
        cy: (plotT + plotB) / 2,
        outerR: laneW / 2,
        dots: pts.map((p2) => ({ x: p2.x, y: p2.py, r: p2.r }))
      });
    });
    const ticks = domain.ticks;
    this._scatterAxis = {
      mode: "1d",
      orientation: "vertical",
      plotL,
      plotR,
      plotT,
      plotB,
      vMin,
      vMax,
      plotY,
      ticks,
      lanes,
      valueTitle: scfg.xTitle,
      formatter: typeof scfg.xFormatter === "function" ? scfg.xFormatter : null,
      gridlines: scfg.gridlines !== false
    };
    return clusters;
  }
  /**
   * 2D value-value scatter: each datum is a point at (`x`, `y`) on two numeric
   * axes (a scatter / bubble plot in the unit family - premium, keyed
   * transitions, per-unit colour/tooltip). Category = colour (one series group
   * per category). With `scatter.sizeRange` set, each dot is a BUBBLE scaled (by
   * area) from its `sizeField` (default 'z'). Needs the object form with numeric
   * `x` + `y`.
   * @param {any} opts
   */
  _layoutScatter2D(opts) {
    const w2 = this.w;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    const scfg = opts.scatter || {};
    const unitData = w2.seriesData.unitData || [];
    const isNum = (v2) => typeof v2 === "number" && isFinite(v2);
    const xOf = (d) => d && typeof d === "object" ? d.x : null;
    const yOf = (d) => d && typeof d === "object" ? d.y != null ? d.y : d.value : null;
    const visible = unitData.map((_, i2) => i2).filter(
      (i2) => (unitData[i2] || []).some((d) => isNum(xOf(d)) && isNum(yOf(d)))
    );
    let xmn = Infinity;
    let xmx = -Infinity;
    let ymn = Infinity;
    let ymx = -Infinity;
    unitData.forEach(
      (cat) => (cat || []).forEach((d) => {
        const x = xOf(d);
        const y = yOf(d);
        if (isNum(x) && isNum(y)) {
          if (x < xmn) xmn = x;
          if (x > xmx) xmx = x;
          if (y < ymn) ymn = y;
          if (y > ymx) ymx = y;
        }
      })
    );
    if (xmn === Infinity) {
      xmn = 0;
      xmx = 1;
      ymn = 0;
      ymx = 1;
    }
    const xTicksN = Math.max(2, Math.round(scfg.tickAmount > 0 ? scfg.tickAmount : 5));
    const yTicksN = Math.max(2, Math.round(scfg.yTickAmount > 0 ? scfg.yTickAmount : 5));
    const nx = this._niceScale(
      scfg.xMin != null ? scfg.xMin : xmn,
      scfg.xMax != null ? scfg.xMax : xmx,
      xTicksN
    );
    const ny = this._niceScale(
      scfg.yMin != null ? scfg.yMin : ymn,
      scfg.yMax != null ? scfg.yMax : ymx,
      yTicksN
    );
    const xMin = scfg.xMin != null ? scfg.xMin : nx.min;
    const xMax = scfg.xMax != null ? scfg.xMax : nx.max;
    const yMin = scfg.yMin != null ? scfg.yMin : ny.min;
    const yMax = scfg.yMax != null ? scfg.yMax : ny.max;
    const xSpan = xMax - xMin || 1;
    const ySpan = yMax - yMin || 1;
    const leftGutter = 46 + (scfg.yTitle ? 18 : 0);
    const bottomGutter = 30 + (scfg.xTitle ? 20 : 0);
    const plotL = leftGutter;
    const plotR = gw - 12;
    const plotT = 10;
    const plotB = gh - bottomGutter;
    const plotW = Math.max(4, plotR - plotL);
    const plotH = Math.max(4, plotB - plotT);
    const plotX = (v2) => plotL + (v2 - xMin) / xSpan * plotW;
    const plotY = (v2) => plotB - (v2 - yMin) / ySpan * plotH;
    const sizeStats = this._scatterSizeStats(scfg, unitData);
    const baseR = this._fixedRadius(opts) || 5;
    this._lastDotR = baseR;
    const clusters = [];
    visible.forEach((ci) => {
      const cat = unitData[ci] || [];
      const dots = cat.map((d) => {
        const x = xOf(d);
        const y = yOf(d);
        return {
          x: plotX(isNum(x) ? x : xMin),
          y: plotY(isNum(y) ? y : yMin),
          r: sizeStats ? this._scatterRadius(d, sizeStats, baseR) : void 0
        };
      });
      clusters.push({
        i: ci,
        cx: (plotL + plotR) / 2,
        cy: (plotT + plotB) / 2,
        outerR: plotH / 2,
        dots
      });
    });
    const mkTicks = (lo, hi, span, spacing, pinned, n2) => {
      const out = [];
      if (pinned) {
        for (let k = 0; k < n2; k++) out.push(lo + span * k / (n2 - 1));
      } else {
        const sp = spacing || span / Math.max(1, n2 - 1);
        for (let v2 = lo; v2 <= hi + sp * 0.5; v2 += sp) {
          out.push(Math.abs(v2) < sp * 1e-9 ? 0 : v2);
        }
      }
      return out;
    };
    this._scatterAxis = {
      mode: "2d",
      plotL,
      plotR,
      plotT,
      plotB,
      plotX,
      plotY,
      xTicks: mkTicks(
        xMin,
        xMax,
        xSpan,
        nx.spacing,
        scfg.xMin != null || scfg.xMax != null,
        xTicksN
      ),
      yTicks: mkTicks(
        yMin,
        yMax,
        ySpan,
        ny.spacing,
        scfg.yMin != null || scfg.yMax != null,
        yTicksN
      ),
      xTitle: scfg.xTitle,
      yTitle: scfg.yTitle,
      xFormatter: typeof scfg.xFormatter === "function" ? scfg.xFormatter : null,
      yFormatter: typeof scfg.yFormatter === "function" ? scfg.yFormatter : null,
      gridlines: scfg.gridlines !== false
    };
    return clusters;
  }
  /**
   * Bubble size stats for the scatter layout, or null when `scatter.sizeRange`
   * is not a `[minR, maxR]` pair. Reads the global range of each datum's
   * `sizeField` (default 'z') so a value maps to a radius (area scale) in
   * _scatterRadius.
   * @param {any} scfg @param {any[][]} unitData
   * @returns {{zmin:number,zmax:number,rMin:number,rMax:number,field:string}|null}
   */
  _scatterSizeStats(scfg, unitData) {
    const range = scfg && scfg.sizeRange;
    if (!Array.isArray(range) || range.length < 2) return null;
    const rMin = Math.max(0.5, +range[0]);
    const rMax = Math.max(rMin, +range[1]);
    const field = scfg.sizeField || "z";
    let zmin = Infinity;
    let zmax = -Infinity;
    unitData.forEach(
      (cat) => (cat || []).forEach((d) => {
        const z = d && typeof d === "object" ? d[field] : null;
        if (typeof z === "number" && isFinite(z)) {
          if (z < zmin) zmin = z;
          if (z > zmax) zmax = z;
        }
      })
    );
    if (zmin === Infinity) return null;
    return { zmin, zmax, rMin, rMax, field };
  }
  /**
   * Radius for one datum under the bubble size stats: area proportional to the
   * `sizeField` value (so radius grows with sqrt), between rMin and rMax. A
   * missing value collapses to rMin.
   * @param {any} d
   * @param {{zmin:number,zmax:number,rMin:number,rMax:number,field:string}} st
   * @param {number} fallback @returns {number}
   */
  _scatterRadius(d, st, fallback) {
    if (!st) return fallback;
    const z = d && typeof d === "object" ? d[st.field] : null;
    if (typeof z !== "number" || !isFinite(z)) return st.rMin;
    const t2 = st.zmax > st.zmin ? (z - st.zmin) / (st.zmax - st.zmin) : 1;
    const tc = Math.max(0, Math.min(1, t2));
    const aMin = st.rMin * st.rMin;
    const aMax = st.rMax * st.rMax;
    return Math.sqrt(aMin + tc * (aMax - aMin));
  }
  /**
   * One-dimensional anti-overlap "beeswarm" pack: given points with a fixed x
   * (`px`) and a lane centre `cy`, assign each a y so no two dots overlap (centre
   * distance >= r_i + r_j). Greedy in ascending-x order, trying offsets 0, +step,
   * -step, +2step ... and taking the SMALLEST that clears every already-placed
   * neighbour still within reach in x. No-overlap always wins: a very dense lane
   * grows a taller swarm rather than stacking dots (offsets are not hard-clamped
   * to the lane). Each point may carry its own radius `r` (bubble beeswarm),
   * else `rFallback` applies; `maxR` bounds the value-window break. Deterministic
   * (no physics, no randomness).
   *
   * Orientation-agnostic: the "fixed" axis is the value axis and the "spread"
   * axis is the lane thickness. Horizontal (default): fixed = `px`, spread = `y`
   * (mutates `.y`). Vertical: fixed = `py`, spread = `x` (mutates `.x`).
   * @param {any[]} pts @param {number} center lane centre on the spread axis
   * @param {number} rFallback @param {number} step @param {number} [maxR]
   * @param {boolean} [vertical]
   */
  _beeswarm(pts, center, rFallback, step, maxR, vertical = false) {
    const fk = vertical ? "py" : "px";
    const sk = vertical ? "x" : "y";
    const order = pts.slice().sort((a2, b2) => a2[fk] - b2[fk]);
    const placed = [];
    const rCap = maxR != null ? maxR : rFallback;
    order.forEach((p2) => {
      const pr = p2.r != null ? p2.r : rFallback;
      let chosen = 0;
      for (let k = 0; k < 2e3; k++) {
        const off = k === 0 ? 0 : Math.ceil(k / 2) * step * (k % 2 ? 1 : -1);
        const s2 = center + off;
        let ok = true;
        for (let m2 = placed.length - 1; m2 >= 0; m2--) {
          const q = placed[m2];
          const df = p2[fk] - q.f;
          if (df > pr + rCap) break;
          const need = pr + q.r;
          const ds = s2 - q.s;
          if (df * df + ds * ds < need * need) {
            ok = false;
            break;
          }
        }
        if (ok) {
          chosen = off;
          break;
        }
      }
      p2[sk] = center + chosen;
      placed.push({ f: p2[fk], s: p2[sk], r: pr });
    });
  }
  /**
   * Value-axis domain + ticks for a 1D beeswarm. The domain ALWAYS contains
   * every datum: a swarm that clips a dot outside the plot box is a bug, so an
   * explicit `xMin`/`xMax` only FRAMES the axis and is extended by whole
   * tick-steps whenever the data would otherwise overflow. Orientation-agnostic:
   * the same value axis is X for a horizontal swarm and Y for a vertical one.
   * @param {any} scfg scatter config
   * @param {number} vmin data minimum @param {number} vmax data maximum
   * @param {number} tickAmount desired tick count
   * @returns {{ min:number, max:number, ticks:number[] }}
   */
  _scatterValueDomain(scfg, vmin, vmax, tickAmount) {
    const buildTicks = (min, max, spacing2) => {
      const ticks = [];
      for (let v2 = min; v2 <= max + spacing2 * 0.5; v2 += spacing2) {
        ticks.push(Math.abs(v2) < spacing2 * 1e-9 ? 0 : v2);
      }
      return ticks;
    };
    if (scfg.xMin != null || scfg.xMax != null) {
      let min = scfg.xMin != null ? scfg.xMin : vmin;
      let max = scfg.xMax != null ? scfg.xMax : vmax;
      if (!(max > min)) max = min + 1;
      const spacing2 = (max - min) / Math.max(1, tickAmount - 1);
      if (vmin < min) min -= Math.ceil((min - vmin) / spacing2) * spacing2;
      if (vmax > max) max += Math.ceil((vmax - max) / spacing2) * spacing2;
      return { min, max, ticks: buildTicks(min, max, spacing2) };
    }
    const nice = this._niceScale(vmin, vmax, tickAmount);
    const spacing = nice.spacing || (nice.max - nice.min) / Math.max(1, tickAmount - 1);
    return { min: nice.min, max: nice.max, ticks: buildTicks(nice.min, nice.max, spacing) };
  }
  /**
   * A "nice" numeric scale [min, max] + tick spacing covering [dataMin, dataMax]
   * with about `ticks` ticks, using rounded 1/2/5 x 10^n steps. Homegrown (no
   * dependency) - lean-core.
   * @param {number} dataMin @param {number} dataMax @param {number} ticks
   * @returns {{min:number,max:number,spacing:number}}
   */
  _niceScale(dataMin, dataMax, ticks) {
    const lo = dataMin;
    let hi = dataMax;
    if (!(hi > lo)) hi = lo + 1;
    const range = this._niceNum(hi - lo, false);
    const spacing = this._niceNum(range / Math.max(1, ticks - 1), true);
    return {
      min: Math.floor(lo / spacing) * spacing,
      max: Math.ceil(hi / spacing) * spacing,
      spacing
    };
  }
  /**
   * Round a range to a "nice" 1/2/5 x 10^n number (Heckbert's loose/round label
   * algorithm).
   * @param {number} range @param {boolean} round @returns {number}
   */
  _niceNum(range, round) {
    const rng = range > 0 ? range : 1;
    const exp = Math.floor(Math.log(rng) / Math.LN10);
    const frac = rng / Math.pow(10, exp);
    let nf;
    if (round) {
      nf = frac < 1.5 ? 1 : frac < 3 ? 2 : frac < 7 ? 5 : 10;
    } else {
      nf = frac <= 1 ? 1 : frac <= 2 ? 2 : frac <= 5 ? 5 : 10;
    }
    return nf * Math.pow(10, exp);
  }
  /**
   * Draw the scatter chrome behind the dots, from the geometry the layout
   * stashed on `this._scatterAxis`. 1D (beeswarm): vertical X gridlines +
   * baseline + tick labels (+ x title) + a per-lane category label in the
   * category colour. 2D: both X + Y gridlines, both axes' tick labels, and
   * rotated/placed axis titles (no lane labels - category is colour). Browser-
   * only (SSR renders the dots without the chrome, as with cluster labels).
   * @param {any} ret @param {Graphics} graphics
   */
  _drawScatterAxes(ret, graphics) {
    const w2 = this.w;
    if (!Environment.isBrowser()) return;
    const ax = this._scatterAxis;
    if (!ax) return;
    const NS = "http://www.w3.org/2000/svg";
    const g2 = graphics.group({ class: "apexcharts-unit-axis" });
    const gridColor = w2.config.grid && w2.config.grid.borderColor || "rgba(128,128,128,0.18)";
    const axisColor = "rgba(128,128,128,0.5)";
    const cfgColors = w2.config.xaxis && w2.config.xaxis.labels && w2.config.xaxis.labels.style && w2.config.xaxis.labels.style.colors;
    const configuredLabelColor = Array.isArray(cfgColors) ? cfgColors[0] : cfgColors;
    const labelColor = configuredLabelColor || "rgba(120,130,140,0.9)";
    const line = (x1, y1, x2, y2, stroke) => {
      const l2 = BrowserAPIs.createElementNS(NS, "line");
      l2.setAttribute("x1", String(x1));
      l2.setAttribute("y1", String(y1));
      l2.setAttribute("x2", String(x2));
      l2.setAttribute("y2", String(y2));
      l2.setAttribute("stroke", stroke);
      l2.setAttribute("shape-rendering", "crispEdges");
      g2.node.appendChild(l2);
    };
    const text = (str, x, y, anchor, fill, size, weight, cls) => {
      const t2 = BrowserAPIs.createElementNS(NS, "text");
      t2.setAttribute("class", cls);
      t2.setAttribute("x", String(x));
      t2.setAttribute("y", String(y));
      t2.setAttribute("text-anchor", anchor);
      t2.setAttribute("dominant-baseline", "middle");
      t2.setAttribute("font-size", `${size}px`);
      t2.setAttribute("font-family", w2.config.chart.fontFamily || "inherit");
      t2.setAttribute("font-weight", String(weight));
      t2.setAttribute("fill", fill);
      t2.textContent = str;
      g2.node.appendChild(t2);
    };
    if (ax.mode === "2d") {
      ax.yTicks.forEach((v2) => {
        const y = ax.plotY(v2);
        if (ax.gridlines) line(ax.plotL, y, ax.plotR, y, gridColor);
        const label = ax.yFormatter ? String(ax.yFormatter(v2)) : this._formatTick(v2);
        text(label, ax.plotL - 8, y, "end", labelColor, 11, 400, "apexcharts-unit-tick");
      });
      ax.xTicks.forEach((v2) => {
        const x = ax.plotX(v2);
        if (ax.gridlines) line(x, ax.plotT, x, ax.plotB, gridColor);
        const label = ax.xFormatter ? String(ax.xFormatter(v2)) : this._formatTick(v2);
        text(label, x, ax.plotB + 14, "middle", labelColor, 11, 400, "apexcharts-unit-tick");
      });
      line(ax.plotL, ax.plotB, ax.plotR, ax.plotB, axisColor);
      line(ax.plotL, ax.plotT, ax.plotL, ax.plotB, axisColor);
      if (ax.xTitle) {
        text(
          String(ax.xTitle),
          (ax.plotL + ax.plotR) / 2,
          ax.plotB + 32,
          "middle",
          labelColor,
          12,
          600,
          "apexcharts-unit-axis-title"
        );
      }
      if (ax.yTitle) {
        const yt = BrowserAPIs.createElementNS(NS, "text");
        yt.setAttribute("class", "apexcharts-unit-axis-title");
        const tx = 14;
        const ty = (ax.plotT + ax.plotB) / 2;
        yt.setAttribute("x", String(tx));
        yt.setAttribute("y", String(ty));
        yt.setAttribute("text-anchor", "middle");
        yt.setAttribute("font-size", "12px");
        yt.setAttribute("font-family", w2.config.chart.fontFamily || "inherit");
        yt.setAttribute("font-weight", "600");
        yt.setAttribute("fill", labelColor);
        yt.setAttribute("transform", `rotate(-90 ${tx} ${ty})`);
        yt.textContent = String(ax.yTitle);
        g2.node.appendChild(yt);
      }
      ret.add(g2);
      return;
    }
    if (ax.orientation === "vertical") {
      ax.ticks.forEach((v2) => {
        const y = ax.plotY(v2);
        if (ax.gridlines) line(ax.plotL, y, ax.plotR, y, gridColor);
        const label = ax.formatter ? String(ax.formatter(v2)) : this._formatTick(v2);
        text(label, ax.plotL - 8, y, "end", labelColor, 11, 400, "apexcharts-unit-tick");
      });
      line(ax.plotL, ax.plotT, ax.plotL, ax.plotB, axisColor);
      if (ax.valueTitle) {
        const yt = BrowserAPIs.createElementNS(NS, "text");
        yt.setAttribute("class", "apexcharts-unit-axis-title");
        const tx = 14;
        const ty = (ax.plotT + ax.plotB) / 2;
        yt.setAttribute("x", String(tx));
        yt.setAttribute("y", String(ty));
        yt.setAttribute("text-anchor", "middle");
        yt.setAttribute("font-size", "12px");
        yt.setAttribute("font-family", w2.config.chart.fontFamily || "inherit");
        yt.setAttribute("font-weight", "600");
        yt.setAttribute("fill", labelColor);
        yt.setAttribute("transform", `rotate(-90 ${tx} ${ty})`);
        yt.textContent = String(ax.valueTitle);
        g2.node.appendChild(yt);
      }
      ax.lanes.forEach((lane) => {
        const color = configuredLabelColor || w2.globals.colors[lane.i] || w2.globals.colors[0] || "#008FFB";
        text(lane.name, lane.cx, ax.plotB + 16, "middle", color, 12, 600, "apexcharts-unit-lane-label");
      });
      ret.add(g2);
      return;
    }
    ax.ticks.forEach((v2, idx) => {
      const x = ax.plotX(v2);
      if (ax.gridlines) line(x, ax.plotT, x, ax.plotB, gridColor);
      const label = ax.formatter ? String(ax.formatter(v2)) : this._formatTick(v2);
      const anchor = idx === 0 ? "start" : idx === ax.ticks.length - 1 ? "end" : "middle";
      text(label, x, ax.plotB + 14, anchor, labelColor, 11, 400, "apexcharts-unit-tick");
    });
    line(ax.plotL, ax.plotB, ax.plotR, ax.plotB, axisColor);
    if (ax.xTitle) {
      text(
        String(ax.xTitle),
        (ax.plotL + ax.plotR) / 2,
        ax.plotB + 32,
        "middle",
        labelColor,
        12,
        600,
        "apexcharts-unit-axis-title"
      );
    }
    if (ax.plotL > 12) {
      ax.lanes.forEach((lane) => {
        const color = configuredLabelColor || w2.globals.colors[lane.i] || w2.globals.colors[0] || "#008FFB";
        text(lane.name, ax.plotL - 8, lane.cy, "end", color, 12, 600, "apexcharts-unit-lane-label");
      });
    }
    ret.add(g2);
  }
  /**
   * Compact tick-value formatting: integers as-is, otherwise trimmed to a short
   * decimal; large magnitudes get a k/M suffix.
   * @param {number} v @returns {string}
   */
  _formatTick(v2) {
    if (!isFinite(v2)) return "";
    const a2 = Math.abs(v2);
    if (a2 >= 1e6) return `${+(v2 / 1e6).toFixed(1)}M`;
    if (a2 >= 1e4) return `${+(v2 / 1e3).toFixed(1)}k`;
    if (Number.isInteger(v2)) return String(v2);
    return String(+v2.toFixed(2));
  }
  /**
   * Distribute `total` whole cells across `counts` in proportion to each value,
   * using the largest-remainder method so the parts sum to exactly `total`
   * (used by the grid/waffle percentage mode).
   * @param {number[]} counts @param {number} total @returns {number[]}
   */
  _largestRemainder(counts, total) {
    const sum = counts.reduce((a2, b2) => a2 + b2, 0);
    if (sum <= 0 || total <= 0) return counts.map(() => 0);
    const exact = counts.map((c2) => c2 / sum * total);
    const floors = exact.map((v2) => Math.floor(v2));
    const used = floors.reduce((a2, b2) => a2 + b2, 0);
    const remaining = Math.max(0, total - used);
    const byFrac = exact.map((v2, i2) => ({ i: i2, frac: v2 - Math.floor(v2) })).sort((a2, b2) => b2.frac - a2.frac);
    const out = floors.slice();
    for (let n2 = 0; n2 < remaining && n2 < byFrac.length; n2++) {
      out[byFrac[n2].i]++;
    }
    return out;
  }
  /**
   * Phyllotaxis (sunflower) placement for `n` points around (cx, cy).
   * @param {number} cx @param {number} cy @param {number} n
   * @param {number} step @param {number} startIndex
   * @returns {{x:number,y:number}[]}
   */
  _spiral(cx, cy, n2, step, startIndex) {
    const pts = [];
    for (let k = 0; k < n2; k++) {
      const idx = startIndex + k;
      const r2 = step * Math.sqrt(idx + 0.5);
      const theta = idx * GOLDEN_ANGLE;
      pts.push({ x: cx + r2 * Math.cos(theta), y: cy + r2 * Math.sin(theta) });
    }
    return pts;
  }
  /**
   * A fixed dot radius, if the shape/size implies one: an explicit numeric
   * `size`, or an `image` shape (sized by its own width/height). Returns null
   * when dots should auto-size to fit the plot.
   * @param {any} opts @returns {number | null}
   */
  _fixedRadius(opts) {
    if (opts.shape === "image" && opts.image) {
      return Math.max(opts.image.width || 20, opts.image.height || 20) / 2;
    }
    if (this._bubbleActive(opts) && typeof opts.sizeByValue.maxRadius === "number") {
      return opts.sizeByValue.maxRadius > 0 ? opts.sizeByValue.maxRadius : null;
    }
    if (typeof opts.size === "number" && opts.size > 0) return opts.size;
    return null;
  }
  /**
   * Whether opt-in bubble sizing applies: enabled, and the shape is a circle
   * (squares/images keep a uniform size).
   * @param {any} opts @returns {boolean}
   */
  _bubbleActive(opts) {
    const sbv = opts.sizeByValue;
    return !!(sbv && sbv.enabled && opts.shape !== "image" && opts.shape !== "square");
  }
  /**
   * This datum's numeric value for sizing / tooltip: the number itself, or an
   * object's `value` / `y`. Null when there is no usable number.
   * @param {any} d @returns {number | null}
   */
  _unitValueOf(d) {
    if (typeof d === "number") return d;
    if (d && typeof d === "object") {
      const v2 = d.value != null ? d.value : d.y;
      return typeof v2 === "number" ? v2 : null;
    }
    return null;
  }
  /**
   * Radius for one bubble given the value stats. Default 'area' scaling makes
   * a bubble's AREA proportional to its value (radius grows with sqrt); 'linear'
   * scales the radius directly. Missing values collapse to the min radius.
   * @param {number|null} v
   * @param {{min:number,max:number,minR:number,maxR:number,scale:string}} stats
   * @returns {number}
   */
  _radiusForValue(v2, stats) {
    if (v2 == null || !isFinite(v2)) return stats.minR;
    const t2 = stats.max > stats.min ? (v2 - stats.min) / (stats.max - stats.min) : 1;
    const tc = Math.max(0, Math.min(1, t2));
    if (stats.scale === "linear") {
      return stats.minR + tc * (stats.maxR - stats.minR);
    }
    const aMin = stats.minR * stats.minR;
    const aMax = stats.maxR * stats.maxR;
    return Math.sqrt(aMin + tc * (aMax - aMin));
  }
  /**
   * Value stats + radius bounds for bubble sizing, or null when it does not
   * apply (disabled, non-circle shape, or no per-unit values). `maxR` is the
   * reference radius the layout already spaced the lattice for; `minR` defaults
   * to ~35% of it.
   * @param {any[][]} unitData @param {any} opts @param {number} refR
   * @returns {{min:number,max:number,minR:number,maxR:number,scale:string}|null}
   */
  _bubbleStats(unitData, opts, refR) {
    if (!this._bubbleActive(opts)) return null;
    let vmin = Infinity;
    let vmax = -Infinity;
    unitData.forEach((cat) => {
      if (!cat) return;
      cat.forEach((d) => {
        const v2 = this._unitValueOf(d);
        if (v2 != null && isFinite(v2)) {
          if (v2 < vmin) vmin = v2;
          if (v2 > vmax) vmax = v2;
        }
      });
    });
    if (vmin === Infinity || vmax < vmin) return null;
    const sbv = opts.sizeByValue;
    const maxR = refR;
    const minR = Math.max(
      1,
      Math.min(
        maxR,
        typeof sbv.minRadius === "number" ? sbv.minRadius : maxR * 0.35
      )
    );
    return {
      min: vmin,
      max: vmax,
      minR,
      maxR,
      scale: sbv.scale === "linear" ? "linear" : "area"
    };
  }
  /**
   * Radial step between successive spiral shells. A fixed radius derives the
   * step directly; 'auto' derives it so a cluster of `count` dots fits `availR`.
   * @param {any} opts @param {number} availR @param {number} count
   * @returns {number}
   */
  _resolveStep(opts, availR, count) {
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const fixed = this._fixedRadius(opts);
    if (fixed) return 2 * fixed * spacing;
    return availR / (Math.sqrt(Math.max(1, count)) + 0.5);
  }
  /**
   * @param {number} step @param {any} opts
   * @returns {number}
   */
  _dotRadiusFromStep(step, opts) {
    const spacing = opts.spacing > 0 ? opts.spacing : 1;
    const fixed = this._fixedRadius(opts);
    if (fixed) return fixed;
    return Math.max(1, step / (2 * spacing));
  }
  /**
   * Corner-anchored shapes (square, image) position by their top-left x/y;
   * circles position by their centre cx/cy.
   * @param {any} opts @returns {boolean}
   */
  _isCorner(opts) {
    return opts.shape === "square" || opts.shape === "image";
  }
  /**
   * Half-width/height used to convert a centre point to a corner shape's x/y.
   * @param {any} opts @returns {{hx:number, hy:number}}
   */
  _halfExtent(opts) {
    if (opts.shape === "image" && opts.image) {
      return { hx: (opts.image.width || 20) / 2, hy: (opts.image.height || 20) / 2 };
    }
    const r2 = this._lastDotR;
    return { hx: r2, hy: r2 };
  }
  /**
   * Draw one dot (circle, square, or image icon) with the category fill +
   * stroke, tagged so the shared non-axis tooltip and hover reuse work.
   * @param {Graphics} graphics @param {any} opts @param {number} dotR
   * @param {string} color @param {number} i @param {number} j
   * @returns {any}
   */
  _drawDot(graphics, opts, dotR, color, i2, j) {
    const w2 = this.w;
    const strokeW = w2.config.stroke.show ? w2.config.stroke.width : 0;
    const strokeColor = Array.isArray(w2.globals.stroke.colors) ? w2.globals.stroke.colors[i2] || "none" : "none";
    const fillOpacity = typeof w2.config.fill.opacity === "number" ? w2.config.fill.opacity : 1;
    let el;
    if (opts.shape === "image" && opts.image && opts.image.src) {
      const iw = opts.image.width || 20;
      const ih = opts.image.height || 20;
      el = w2.dom.Paper.image(opts.image.src);
      el.node.setAttribute("width", String(iw));
      el.node.setAttribute("height", String(ih));
      el.node.setAttribute("preserveAspectRatio", "xMidYMid meet");
      if (opts.image.tint) {
        el.node.setAttribute("filter", `url(#${this._tintFilter(color)})`);
      }
    } else if (opts.shape === "square") {
      const side = dotR * 2;
      el = graphics.drawRect(0, 0, side, side, opts.borderRadius || 0, color, 1, strokeW, strokeColor);
      el.node.setAttribute("fill", color);
      if (fillOpacity < 1) el.node.setAttribute("fill-opacity", String(fillOpacity));
    } else {
      el = graphics.drawCircle(dotR, {
        fill: color,
        "stroke-width": strokeW,
        stroke: strokeColor
      });
      el.node.setAttribute("fill", color);
      if (fillOpacity < 1) el.node.setAttribute("fill-opacity", String(fillOpacity));
    }
    el.node.classList.add("apexcharts-unit-area");
    el.node.setAttribute("i", String(i2));
    el.node.setAttribute("j", String(j));
    return el;
  }
  /**
   * Ensure (once per colour) an SVG recolour filter exists in the chart's defs
   * and return its id. The filter floods `color` and clips it to the source
   * graphic's alpha (feComposite operator="in"), so an `<image>` referencing a
   * monochrome icon is repainted in `color` while keeping its silhouette. Reused
   * across every dot of the same colour.
   * @param {string} color @returns {string}
   */
  _tintFilter(color) {
    const w2 = this.w;
    const NS = "http://www.w3.org/2000/svg";
    const safe = String(color).replace(/[^a-zA-Z0-9]/g, "");
    const id = `apexcharts-unit-tint-${w2.globals.chartID}-${safe}`;
    const svg = w2.dom.Paper.node;
    if (svg.querySelector(`#${id}`)) return id;
    let defs = svg.querySelector("defs");
    if (!defs) {
      defs = BrowserAPIs.createElementNS(NS, "defs");
      svg.insertBefore(defs, svg.firstChild);
    }
    const filter = BrowserAPIs.createElementNS(NS, "filter");
    filter.setAttribute("id", id);
    filter.setAttribute("x", "0%");
    filter.setAttribute("y", "0%");
    filter.setAttribute("width", "100%");
    filter.setAttribute("height", "100%");
    const flood = BrowserAPIs.createElementNS(NS, "feFlood");
    flood.setAttribute("flood-color", color);
    flood.setAttribute("result", "flood");
    const comp = BrowserAPIs.createElementNS(NS, "feComposite");
    comp.setAttribute("in", "flood");
    comp.setAttribute("in2", "SourceAlpha");
    comp.setAttribute("operator", "in");
    filter.appendChild(flood);
    filter.appendChild(comp);
    defs.appendChild(filter);
    return id;
  }
  /**
   * Position a non-animated dot at (x, y). Circles use cx/cy at the centre;
   * corner shapes (square, image) use x/y at the top-left.
   * @param {SVGElement} node @param {any} opts @param {number} x @param {number} y
   */
  _placeDot(node, opts, x, y) {
    if (this._isCorner(opts)) {
      const { hx, hy } = this._halfExtent(opts);
      node.setAttribute("x", String(x - hx));
      node.setAttribute("y", String(y - hy));
    } else {
      node.setAttribute("cx", String(x));
      node.setAttribute("cy", String(y));
    }
  }
  /**
   * Parse a `#rgb` / `#rrggbb` / `rgb()` / `rgba()` colour to `[r, g, b]`, or
   * null if it cannot be parsed (the colour tween is then skipped).
   * @param {string} str @returns {number[] | null}
   */
  _rgb(str) {
    if (typeof str !== "string") return null;
    let s2 = str.trim();
    if (s2[0] === "#") {
      if (s2.length === 4) s2 = "#" + s2[1] + s2[1] + s2[2] + s2[2] + s2[3] + s2[3];
      const n2 = parseInt(s2.slice(1, 7), 16);
      if (isNaN(n2)) return null;
      return [n2 >> 16 & 255, n2 >> 8 & 255, n2 & 255];
    }
    const m2 = s2.match(/rgba?\(([^)]+)\)/);
    if (m2) {
      const p2 = m2[1].split(",").map((x) => parseFloat(x));
      if (p2.length >= 3 && p2.every((v2) => !isNaN(v2))) return [p2[0], p2[1], p2[2]];
    }
    return null;
  }
  /**
   * Whether to run the gather / transition animation. Runs on the initial mount
   * and on data-driven updates (keyed old->new tween or cross-type burst).
   * Skipped: in SSR, when animations are off, when the caller passed
   * `animate:false` (shouldAnimate === false), on a PURE window resize (resized
   * with no data change - re-gathering on every resize would be jarring), and
   * when the user prefers reduced motion.
   *
   * Note: `w.globals.resized` is set true on every update (not just window
   * resize), so it must be paired with `!dataChanged` to isolate a real resize.
   * @returns {boolean}
   */
  _shouldAnimate() {
    const w2 = this.w;
    const anim = w2.config.chart.animations;
    if (!Environment.isBrowser()) return false;
    if (!anim || anim.enabled === false) return false;
    if (w2.globals.shouldAnimate === false) return false;
    if (w2.globals.resized && !w2.globals.dataChanged) return false;
    if (anim.respectReducedMotion && prefersReducedMotion()) return false;
    return true;
  }
  /**
   * Give every dot an x/y spring, reusing the live springs of a gather this
   * render just cancelled.
   *
   * The reuse is the whole point of the spring path. A carried spring holds a
   * dot's real on-screen position AND its velocity, so an interrupted gather
   * resumes from there. Without it the dot restarts from `cx0`, which on an
   * update is the slot it was still travelling towards - so every interruption
   * teleports it forward and then re-animates from a standstill. A dragged
   * slider or a scrubbed storyboard interrupts on almost every frame, which is
   * where that reads worst.
   *
   * Springs left over from a completed gather are at rest on their targets, so
   * carrying them is identical to making fresh ones. Only an interrupted flight
   * carries anything.
   *
   * @param {UnitAnimDot[]} dots
   * @param {any} gcfg plotOptions.unit.gather
   * @param {number} speed chart.animations.speed, in ms
   * @param {any} opts plotOptions.unit
   */
  _seedSprings(dots, gcfg, speed, opts) {
    const [stiffness, damping] = springParams(gcfg.spring, speed);
    const live = this.ctx ? this.ctx._unitSprings : null;
    const springs = /* @__PURE__ */ new Map();
    for (let k = 0; k < dots.length; k++) {
      const d = dots[k];
      const carried = live && !d.isEnter && d.key != null ? live.get(d.key) : null;
      const sx = carried ? carried.x : v(d.cx0, stiffness, damping);
      const sy = carried ? carried.y : v(d.cy0, stiffness, damping);
      if (carried) {
        sx.stiffness = stiffness;
        sy.stiffness = stiffness;
        sx.damping = damping;
        sy.damping = damping;
        d.cx0 = sx.value;
        d.cy0 = sy.value;
        this._placeDot(d.node, opts, d.cx0, d.cy0);
        if (sx.velocity !== 0 || sy.velocity !== 0) d.delay = 0;
      }
      d.sx = sx;
      d.sy = sy;
      if (d.key != null) springs.set(d.key, { x: sx, y: sy });
    }
    if (this.ctx) this.ctx._unitSprings = springs;
  }
  /**
   * One rAF loop that tweens every dot from its start (cx0/cy0 - either the
   * cluster centre on first mount / for entering dots, or its previous slot on
   * an update) to its target slot, staggered by index. Entering dots fade in;
   * moving dots stay opaque. Dots whose group colour changed (a 'flow' regroup)
   * cross-fade their fill from the old colour to the new one over the same ease;
   * dots whose radius changed (bubble sizing) grow/shrink over it too (circles).
   *
   * Position travels on a spring by default (`gather.motion`), so a gather
   * interrupted by the next render resumes from where the dots actually are,
   * carrying their velocity, rather than restarting from a standstill. Colour,
   * radius and opacity stay on a fixed-duration ease either way: those are 0..1
   * quantities, and the shared solver's rest thresholds are absolute (0.05 in
   * caller units), which is negligible for pixels but 5% of a unit interval.
   * @param {UnitAnimDot[]} dots
   */
  _runGather(dots) {
    const w$1 = this.w;
    const opts = w$1.config.plotOptions.unit;
    const speed = Math.max(1, w$1.config.chart.animations.speed || 800);
    const corner = this._isCorner(opts);
    const { hx, hy } = this._halfExtent(opts);
    const maxDelay = Math.min(speed * 0.6, 450);
    const n2 = dots.length;
    for (let k = 0; k < n2; k++) {
      dots[k].delay = n2 > 1 ? k / (n2 - 1) * maxDelay : 0;
    }
    const cxAttr = corner ? "x" : "cx";
    const cyAttr = corner ? "y" : "cy";
    const offX = corner ? hx : 0;
    const offY = corner ? hy : 0;
    const gcfg = opts.gather || {};
    const motion = gcfg.motion || "auto";
    const useSpring = motion === "spring" || motion === "auto" && (!gcfg.easing || gcfg.easing === "outCubic");
    if (useSpring) this._seedSprings(dots, gcfg, speed, opts);
    else if (this.ctx) this.ctx._unitSprings = null;
    if (corner) {
      for (let k = 0; k < n2; k++) {
        dots[k].node.setAttribute(cxAttr, String(dots[k].cx0 - offX));
        dots[k].node.setAttribute(cyAttr, String(dots[k].cy0 - offY));
      }
    }
    if (!corner) {
      for (let k = 0; k < n2; k++) {
        const d = dots[k];
        if (d.r0 != null && d.r1 != null && d.r0 !== d.r1) {
          d.node.setAttribute("r", String(d.r0));
        }
      }
    }
    for (let k = 0; k < n2; k++) {
      const d = dots[k];
      if (d.fill0 && d.fill1 && d.fill0 !== d.fill1) {
        d._c0 = this._rgb(d.fill0);
        d._c1 = this._rgb(d.fill1);
      }
    }
    const easePos = gcfg.easing === "outBack" ? easeOutBack(typeof gcfg.overshoot === "number" ? gcfg.overshoot : 1.70158) : gcfg.easing === "inOutCubic" ? easeInOutCubic : easeOutCubic;
    if (this.w.globals.unitGatherRAF != null) {
      BrowserAPIs.cancelAnimationFrame(this.w.globals.unitGatherRAF);
      this.w.globals.unitGatherRAF = null;
    }
    const start = performance.now();
    let last = start;
    const stepFn = (now) => {
      if (this.w.globals.isDestroyed) {
        this.w.globals.unitGatherRAF = null;
        return;
      }
      const dt = Math.min(MAX_FRAME_STEP, Math.max(0, (now - last) / 1e3));
      last = now;
      let done = true;
      for (let k = 0; k < n2; k++) {
        const d = dots[k];
        const elapsed = now - start - d.delay;
        const t2 = Math.max(0, Math.min(1, elapsed / speed));
        const ec = easeOutCubic(t2);
        let cx, cy;
        if (d.sx && d.sy) {
          if (elapsed >= 0 && !d.released) {
            w(d.sx, d.x);
            w(d.sy, d.y);
            d.released = true;
          }
          const restX = b(d.sx, dt);
          const restY = b(d.sy, dt);
          if (!d.released || !restX || !restY) done = false;
          cx = d.sx.value;
          cy = d.sy.value;
        } else {
          const e2 = easePos(t2);
          cx = d.cx0 + (d.x - d.cx0) * e2;
          cy = d.cy0 + (d.y - d.cy0) * e2;
        }
        d.node.setAttribute(cxAttr, String(cx - offX));
        d.node.setAttribute(cyAttr, String(cy - offY));
        if (d.isEnter) d.node.style.opacity = String(Math.min(1, t2 * 2.5));
        if (d._c0 && d._c1) {
          const cr = Math.round(d._c0[0] + (d._c1[0] - d._c0[0]) * ec);
          const cg = Math.round(d._c0[1] + (d._c1[1] - d._c0[1]) * ec);
          const cb = Math.round(d._c0[2] + (d._c1[2] - d._c0[2]) * ec);
          d.node.setAttribute("fill", `rgb(${cr},${cg},${cb})`);
        }
        if (!corner && d.r0 != null && d.r1 != null && d.r0 !== d.r1) {
          d.node.setAttribute("r", String(d.r0 + (d.r1 - d.r0) * ec));
        }
        if (t2 < 1) done = false;
      }
      if (done) {
        for (let k = 0; k < n2; k++) {
          const d = dots[k];
          d.node.style.opacity = "";
          if (d._c1 && d.fill1) d.node.setAttribute("fill", d.fill1);
          if (!corner && d.r0 != null && d.r1 != null && d.r0 !== d.r1) {
            d.node.setAttribute("r", String(d.r1));
          }
        }
        this.w.globals.unitGatherRAF = null;
      } else {
        this.w.globals.unitGatherRAF = BrowserAPIs.requestAnimationFrame(stepFn);
      }
    };
    this.w.globals.unitGatherRAF = BrowserAPIs.requestAnimationFrame(stepFn);
  }
  /**
   * Keys present in the previous render but not the current one, resolved back
   * to their old slot {x, y, fill}. These are the dots that must animate out.
   * @param {Map<string, {x:number,y:number,fill:string}>} prev
   * @param {Map<string, {x:number,y:number,fill:string}>} nextPrev
   * @param {any} opts
   * @returns {{x:number,y:number,fill:string}[]}
   */
  _collectExits(prev, nextPrev, opts) {
    const cap = Math.max(0, opts.maxUnits || 5e3);
    const exits = [];
    for (const [key, slot] of prev) {
      if (!nextPrev.has(key)) {
        exits.push(slot);
        if (exits.length >= cap) break;
      }
    }
    return exits;
  }
  /**
   * Animate the exit ghosts out, then remove them. Layouts whose positions carry
   * data (a waffle / grid lattice, or a scatter / beeswarm on real axes) fade
   * their ghosts OUT IN PLACE - drifting them toward the plot centre would drag
   * cells across tiles or bubbles across the plane, which reads as wrong. The
   * blob / bar layouts keep the gentle inward collapse so a removal reads as
   * motion rather than a pop.
   * @param {any} group @param {{x:number,y:number,fill:string}[]} exits @param {any} opts
   */
  _runExits(group, exits, opts) {
    const w2 = this.w;
    const graphics = new Graphics(w2, this.ctx);
    const dotR = this._lastDotR;
    const cx = w2.layout.gridWidth / 2;
    const cy = w2.layout.gridHeight / 2;
    const drift = opts.layout === "grid" || opts.layout === "scatter" || opts.layout === "arc" || opts.layout === "custom" ? 0 : 0.35;
    const ghosts = [];
    exits.forEach((slot) => {
      const el = this._drawDot(graphics, opts, dotR, slot.fill, 0, 0);
      el.node.classList.add("apexcharts-unit-exit");
      this._placeDot(el.node, opts, slot.x, slot.y);
      group.add(el);
      ghosts.push({ node: el.node, x0: slot.x, y0: slot.y });
    });
    if (!this._shouldAnimate()) {
      ghosts.forEach((g2) => g2.node.remove());
      return;
    }
    const speed = Math.max(1, w2.config.chart.animations.speed || 800);
    const corner = this._isCorner(opts);
    const { hx, hy } = this._halfExtent(opts);
    const offX = corner ? hx : 0;
    const offY = corner ? hy : 0;
    const cxAttr = corner ? "x" : "cx";
    const cyAttr = corner ? "y" : "cy";
    if (this.w.globals.unitExitRAF != null) {
      BrowserAPIs.cancelAnimationFrame(this.w.globals.unitExitRAF);
      this.w.globals.unitExitRAF = null;
    }
    const start = performance.now();
    const stepFn = (now) => {
      if (this.w.globals.isDestroyed) {
        this.w.globals.unitExitRAF = null;
        return;
      }
      const t2 = Math.max(0, Math.min(1, (now - start) / speed));
      const e2 = easeOutCubic(t2);
      for (let k = 0; k < ghosts.length; k++) {
        const g2 = ghosts[k];
        if (drift) {
          const x = g2.x0 + (cx - g2.x0) * e2 * drift;
          const y = g2.y0 + (cy - g2.y0) * e2 * drift;
          g2.node.setAttribute(cxAttr, String(x - offX));
          g2.node.setAttribute(cyAttr, String(y - offY));
        }
        g2.node.style.opacity = String(1 - e2);
      }
      if (t2 < 1) {
        this.w.globals.unitExitRAF = BrowserAPIs.requestAnimationFrame(stepFn);
      } else {
        this.w.globals.unitExitRAF = null;
        group.node && group.node.remove();
      }
    };
    this.w.globals.unitExitRAF = BrowserAPIs.requestAnimationFrame(stepFn);
  }
  /**
   * A cluster label placed above (default) or below the cluster/bar. A TOP label
   * over a wide grouped/packed blob rides a curved arc (invisible arc path +
   * <textPath>, centred at 50% offset); a bottom label, a 'columns' bar, or a
   * cluster too small for the arc gets a straight centred label instead.
   * `clusterLabels.position` = 'top' | 'bottom'; `offsetY` pushes it further from
   * the blob in either direction.
   * @param {any} elSeries @param {{ i:number, cx:number, cy:number, outerR:number, flat?:boolean }} cluster
   * @param {number} value @param {number} total @param {any} opts @param {string} color
   */
  _drawClusterLabel(elSeries, cluster, value, total, opts, color) {
    const w2 = this.w;
    if (!Environment.isBrowser()) return;
    const NS = "http://www.w3.org/2000/svg";
    const name2 = w2.seriesData.seriesNames[cluster.i] || `series-${cluster.i + 1}`;
    const percent = total > 0 ? value / total * 100 : 0;
    const cfg = opts.clusterLabels;
    const fontSize = parseFloat(cfg.fontSize) || 13;
    let text;
    if (typeof cfg.formatter === "function") {
      text = cfg.formatter(name2, {
        seriesIndex: cluster.i,
        value,
        percent,
        w: w2
      });
    } else {
      text = `${name2} (${percent.toFixed(1)}%)`;
    }
    const str = typeof text === "string" ? text : String(text);
    const textEl = BrowserAPIs.createElementNS(NS, "text");
    textEl.setAttribute("class", "apexcharts-unit-label");
    textEl.setAttribute("text-anchor", "middle");
    textEl.setAttribute("font-size", `${fontSize}px`);
    textEl.setAttribute("font-family", cfg.fontFamily || w2.config.chart.fontFamily || "inherit");
    textEl.setAttribute("font-weight", String(cfg.fontWeight || 600));
    textEl.setAttribute("fill", cfg.color || color);
    const bottom = cfg.position === "bottom";
    const R = cluster.outerR + fontSize * 0.6 + 3 + (cfg.offsetY || 0);
    const estWidth = str.length * fontSize * 0.55;
    const curved = !bottom && !cluster.flat && cfg.curved !== false && estWidth <= Math.PI * R * 0.95;
    if (curved) {
      const yMid = cluster.cy;
      const x1 = cluster.cx - R;
      const x2 = cluster.cx + R;
      const d = `M ${x1} ${yMid} A ${R} ${R} 0 0 1 ${x2} ${yMid}`;
      const arcId = `apexcharts-unit-label-${w2.globals.chartID}-${cluster.i}`;
      const pathEl = BrowserAPIs.createElementNS(NS, "path");
      pathEl.setAttribute("id", arcId);
      pathEl.setAttribute("d", d);
      pathEl.setAttribute("fill", "none");
      pathEl.setAttribute("stroke", "none");
      const tp = BrowserAPIs.createElementNS(NS, "textPath");
      tp.setAttribute("href", `#${arcId}`);
      tp.setAttributeNS("http://www.w3.org/1999/xlink", "xlink:href", `#${arcId}`);
      tp.setAttribute("startOffset", "50%");
      tp.textContent = str;
      textEl.appendChild(tp);
      elSeries.node.appendChild(pathEl);
    } else {
      textEl.setAttribute("x", String(cluster.cx));
      const y = bottom ? cluster.cy + cluster.outerR + fontSize + 6 + (cfg.offsetY || 0) : cluster.cy - cluster.outerR - 6 - (cfg.offsetY || 0);
      textEl.setAttribute("y", String(y));
      textEl.textContent = str;
    }
    elSeries.node.appendChild(textEl);
  }
}
const D2R = Math.PI / 180;
const R2D = 180 / Math.PI;
const SVGNS = "http://www.w3.org/2000/svg";
const XHTML = "http://www.w3.org/1999/xhtml";
const lerp = (a2, b2, t2) => a2 + (b2 - a2) * t2;
class SunburstChart {
  /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {import('../types/internal').ChartContext} ctx
   */
  constructor(w2, ctx) {
    this.ctx = ctx;
    this.w = w2;
    const cnf = w2.config;
    this.cfg = cnf.plotOptions.sunburst;
    this.strokeWidth = cnf.stroke.show ? cnf.stroke.width : 0;
    this.strokeColor = Array.isArray(cnf.stroke.colors) ? cnf.stroke.colors[0] : cnf.stroke.colors || "#fff";
    this.startAngle = this.cfg.startAngle;
    this.endAngle = this.cfg.endAngle;
    this.maxDepth = 0;
    this.centerX = 0;
    this.centerY = 0;
    this.maxRadius = 0;
    this.total = 1;
    this._focusMaxDepth = 0;
    this._focus = null;
    this._zoomGen = 0;
    this._roots = [];
    this._nodesAll = [];
    this._innerR = () => 0;
    this._outerR = () => 0;
    this._tooltipEl = null;
    this._lblSeq = 0;
    this._morphLeafIndex = 0;
    this._graphics = null;
    this._ringsG = null;
    this._labelsG = null;
  }
  /**
   * @param {any[]} series  flattened top-level values (geometry comes from the
   *   config hierarchy; kept for the standard draw(series) signature + noData)
   * @returns {any} SVG group
   */
  draw(series) {
    const w2 = this.w;
    const graphics = new Graphics(this.w);
    this._graphics = graphics;
    const g2 = graphics.group({ class: "apexcharts-sunburst" });
    if (w2.globals.noData || !series || !series.length) return g2;
    const gw = w2.layout.gridWidth;
    const gh = w2.layout.gridHeight;
    this.centerX = gw / 2 + (this.cfg.offsetX || 0);
    this.centerY = gh / 2 + (this.cfg.offsetY || 0);
    this.maxRadius = Math.min(gw, gh) / 2.05 - this.strokeWidth - (!w2.config.chart.sparkline.enabled ? w2.config.chart.dropShadow.blur : 0);
    if (this.maxRadius < 5) return g2;
    this._roots = this._buildHierarchy();
    if (!this._roots.length) return g2;
    this._roots.forEach((r2) => this._fillValues(r2));
    this._validateStrict();
    this._nodesAll = [];
    const colors = w2.globals.colors || [];
    this._roots.forEach((r2, i2) => {
      this._colorPass(r2, r2.color || colors[i2 % colors.length] || "#008FFB");
    });
    this.total = this._roots.reduce((s2, r2) => s2 + Math.max(0, r2.value), 0) || 1;
    this._ringsG = graphics.group({ class: "apexcharts-sunburst-rings" });
    this._labelsG = graphics.group({ class: "apexcharts-sunburst-labels" });
    g2.add(this._ringsG);
    g2.add(this._labelsG);
    this._focus = null;
    this._relayout(this._focus);
    const anims = w2.config.chart.animations;
    let mode = "none";
    if (anims.enabled) {
      if (w2.globals.dataChanged) {
        if (anims.dynamicAnimation.enabled) mode = "update";
      } else if (!w2.globals.resized) {
        mode = "intro";
      }
    }
    this._applyLayout(mode);
    this._renderBreadcrumb();
    return g2;
  }
  // ------------------------------------------------------------------ data
  /**
   * Resolve the config into root nodes `{ name, value, color?, children? }`.
   * Each datum may carry `children` (native) or `drilldown: '<id>'` (adapter).
   * Shared with the treemap - see charts/common/Hierarchy.
   * @returns {any[]}
   */
  _buildHierarchy() {
    return buildHierarchy(this.w);
  }
  /**
   * Fill a parent's value from its children when missing.
   * @param {any} node
   */
  _fillValues(node) {
    fillValues(node);
  }
  /**
   * With `partition: 'strict'`, warn (once) when a parent's value does not
   * match the sum of its children. The angles are still normalized to fill the
   * wedge (strict rendering is a P3 refinement); this just surfaces the data
   * mismatch.
   */
  _validateStrict() {
    if (this.cfg.partition !== "strict") return;
    let warned = false;
    const walk = (node) => {
      if (node.children && node.children.length) {
        const sum = node.children.reduce(
          (s2, c2) => s2 + Math.max(0, c2.value || 0),
          0
        );
        if (!warned && node.value != null && Math.abs(sum - node.value) > 0.5) {
          console.warn(
            `ApexCharts sunburst: partition 'strict' but "${node.name}" (${node.value}) != sum of its children (${sum}). Angles are normalized to fill the wedge.`
          );
          warned = true;
        }
        node.children.forEach(walk);
      }
    };
    this._roots.forEach(walk);
  }
  /**
   * Assign a colour to every node (explicit `color` wins, else the parent's
   * colour tinted lighter). Done once so zoom preserves colours.
   * @param {any} node
   * @param {string} color
   */
  _colorPass(node, color) {
    node._color = node.color || color;
    this._nodesAll.push(node);
    if (node.children) {
      node.children.forEach(
        (c2) => this._colorPass(c2, c2.color || this._lighten(node._color, this.cfg.tint))
      );
    }
  }
  // ---------------------------------------------------------------- layout
  /**
   * Recompute visibility + angles + radii for a focus node (null = whole tree).
   * @param {any} focus
   */
  _relayout(focus) {
    this._nodesAll.forEach((n2) => {
      n2._show = false;
    });
    this._focusMaxDepth = 0;
    if (!focus) {
      const total = this._roots.reduce((s2, r2) => s2 + Math.max(0, r2.value), 0) || 1;
      let a2 = this.startAngle;
      this._roots.forEach((r2) => {
        const span = (this.endAngle - this.startAngle) * Math.max(0, r2.value) / total;
        this._placeVis(r2, 0, a2, a2 + span, null);
        a2 += span;
      });
    } else {
      this._placeVis(focus, 0, this.startAngle, this.endAngle, focus._parent);
    }
    const hole = this._parseSize(this.cfg.innerSize, this.maxRadius);
    const ringCount = this._focusMaxDepth + 1;
    const band = (this.maxRadius - hole) / ringCount;
    const radialGap = ringCount > 1 ? 1 : 0;
    this._innerR = (vd) => hole + vd * band + (vd > 0 ? radialGap / 2 : 0);
    this._outerR = (vd) => hole + (vd + 1) * band - radialGap / 2;
    this._nodesAll.forEach((n2) => {
      if (!n2._show) return;
      n2._iR = this._innerR(n2._vDepth);
      n2._oR = n2._leaf && this.cfg.leaf === "extend" && n2._vDepth < this._focusMaxDepth ? this.maxRadius : this._outerR(n2._vDepth);
    });
  }
  /**
   * @param {any} node
   * @param {number} vDepth
   * @param {number} a0
   * @param {number} a1
   * @param {any} parent
   */
  _placeVis(node, vDepth, a0, a1, parent) {
    node._show = true;
    node._vDepth = vDepth;
    node._a0 = a0;
    node._a1 = a1;
    node._parent = parent;
    node._leaf = !(node.children && node.children.length);
    if (vDepth > this._focusMaxDepth) this._focusMaxDepth = vDepth;
    if (!node._leaf) {
      const total = node.children.reduce(
        (s2, c2) => s2 + Math.max(0, c2.value),
        0
      ) || 1;
      let a2 = a0;
      node.children.forEach((c2) => {
        const span = (a1 - a0) * Math.max(0, c2.value) / total;
        this._placeVis(c2, vDepth + 1, a2, a2 + span, node);
        a2 += span;
      });
    }
  }
  // --------------------------------------------------------------- render
  /**
   * Create / update / remove arc elements to match the current layout, with an
   * animation appropriate to the transition:
   *   intro  — pie/donut-style angular clock sweep from startAngle to endAngle
   *            (all rings reveal together as the sweep line passes them)
   *   update — morph every arc from its previous on-screen geometry (matched
   *            by node key across the re-render); new arcs unfurl in place
   *   zoom   — tween angles + radii between focus layouts (same instance)
   * @param {'intro'|'zoom'|'update'|'none'} mode
   */
  _applyLayout(mode) {
    var _a;
    const w2 = this.w;
    const anims = w2.config.chart.animations;
    const dur = !anims.enabled ? 0 : mode === "none" ? 0 : mode === "update" ? anims.dynamicAnimation.speed || 350 : anims.speed || 500;
    const gen = ++this._zoomGen;
    const morph = (
      /** @type {any} */
      (_a = this.ctx) == null ? void 0 : _a.morphTypeChange
    );
    const morphActive = !!morph && typeof morph.isActive === "function" && morph.isActive() && typeof morph.getInitialPathAt === "function";
    const morphDur = morphActive ? morph.getSpeed() : 0;
    this._morphLeafIndex = 0;
    const prev = mode === "update" ? (
      /** @type {any} */
      this.ctx._sunburstPrevGeoms
    ) : null;
    this._nodesAll.forEach((node) => {
      if (node._show) {
        const target = {
          a0: node._a0,
          a1: node._a1,
          iR: node._iR,
          oR: node._oR
        };
        if (!node._el) node._el = this._createArcEl(node);
        const morphFrom = morphActive ? this._morphSourceFor(node) : null;
        if (morphFrom && morphDur > 0) {
          this._morphArcFrom(node, morphFrom, target, morphDur, gen);
        } else if ((mode === "intro" || morphActive) && dur > 0) {
          this._sweepArc(node, target, morphActive ? morphDur : dur, gen);
        } else {
          let from;
          let isNew = false;
          if (node._cur) {
            from = node._cur;
          } else if (prev && prev.get(node._key)) {
            from = prev.get(node._key);
          } else {
            const mid = (target.a0 + target.a1) / 2;
            from = { a0: mid, a1: mid, iR: target.iR, oR: target.iR };
            isNew = true;
          }
          this._animateArc(node, from, target, dur, false, isNew, gen);
        }
      } else if (node._el && node._cur) {
        const mid = (node._cur.a0 + node._cur.a1) / 2;
        const target = { a0: mid, a1: mid, iR: node._cur.iR, oR: node._cur.iR };
        this._animateArc(node, node._cur, target, dur, true, false, gen);
      }
    });
    const geoms = /* @__PURE__ */ new Map();
    this._nodesAll.forEach((n2) => {
      if (n2._show) {
        geoms.set(n2._key, { a0: n2._a0, a1: n2._a1, iR: n2._iR, oR: n2._oR });
      }
    });
    this.ctx._sunburstPrevGeoms = geoms;
    this._renderLabels(dur);
  }
  /**
   * The captured mark this leaf should unroll from, or null when the node is
   * the outgoing chart had nothing to give it.
   *
   * When both charts carry branch keys the pairing is by identity, so EVERY
   * ring finds the tile that stood for the same branch and the inner rings
   * unroll instead of appearing from nothing.
   *
   * Without keys (a flat treemap, or an older config) only leaves pair, and
   * they consume the captured paths in draw order - the order the outgoing
   * renderer laid its own marks out in, so tile k pairs with leaf k.
   *
   * @param {any} node
   * @returns {string | null}
   */
  _morphSourceFor(node) {
    const ctx = (
      /** @type {any} */
      this.ctx
    );
    const morph = ctx && ctx.morphTypeChange;
    if (!morph) return null;
    if (typeof morph.hasKeyedMarks === "function" && morph.hasKeyedMarks() && typeof morph.getInitialPathForKey === "function") {
      return morph.getInitialPathForKey(morphKey(node._key));
    }
    if (node.children && node.children.length) return null;
    return morph.getInitialPathAt(this._morphLeafIndex++);
  }
  /**
   * Unroll an arc from an arbitrary captured shape.
   *
   * `_animateArc` interpolates arc PARAMETERS (angles and radii), which cannot
   * express a rectangle, so this one tweens the path data itself through
   * Animations.morphSVG - the same call every other morphing renderer makes,
   * which already selects the polygon-resample algorithm while a cross-type
   * morph is active.
   *
   * @param {any} node
   * @param {string} fromD
   * @param {{a0:number,a1:number,iR:number,oR:number}} target
   * @param {number} dur
   * @param {number} gen
   */
  _morphArcFrom(node, fromD, target, dur, gen) {
    const el = node._el;
    const toD = this._arcPath(
      target.iR,
      target.oR,
      target.a0,
      target.a1,
      this.cfg.borderRadius
    );
    el.node.style.display = "";
    el.attr({ d: fromD, opacity: 1 });
    node._cur = target;
    if (this._zoomGen !== gen) return;
    new Animations(
      this.w,
      /** @type {any} */
      this.ctx
    ).morphSVG(
      el,
      0,
      // Not a (series, point) index: an arc has no j, and passing a number here
      // would make morphSVG treat it as the last point of a series and fire the
      // chart's animation-completed hook.
      /** @type {any} */
      null,
      node._color,
      fromD,
      toD,
      dur,
      0
    );
  }
  /**
   * Pie/donut-style intro: a clock sweep from startAngle to endAngle. Each
   * arc's end angle is clamped to the sweep line, so arcs appear in angular
   * order and grow until complete — the whole hierarchy unwipes together.
   * @param {any} node
   * @param {{a0:number,a1:number,iR:number,oR:number}} target
   * @param {number} dur
   * @param {number} gen  layout generation; frames stop once superseded
   */
  _sweepArc(node, target, dur, gen) {
    const el = node._el;
    const br = this.cfg.borderRadius;
    const s0 = this.startAngle;
    const s1 = this.endAngle;
    el.node.style.display = "";
    el.attr({ d: "", opacity: 1 });
    el.animate(dur).during((pos) => {
      if (this._zoomGen !== gen) return;
      const sweep = s0 + (s1 - s0) * pos;
      if (sweep <= target.a0 + 0.01) {
        el.attr({ d: "" });
        return;
      }
      const a1 = Math.min(target.a1, sweep);
      el.attr({ d: this._arcPath(target.iR, target.oR, target.a0, a1, br) });
      node._cur = { a0: target.a0, a1, iR: target.iR, oR: target.oR };
    }).after(() => {
      if (this._zoomGen !== gen) return;
      node._cur = target;
    });
  }
  /**
   * @param {any} node
   * @returns {any} svg.js path element
   */
  _createArcEl(node) {
    const path = this._graphics.drawPath({
      d: "",
      fill: node._color,
      stroke: this.strokeColor,
      strokeWidth: this.strokeWidth,
      fillOpacity: 1,
      classes: "apexcharts-sunburst-arc"
    });
    const el = path.node;
    el.setAttribute("data:name", node.name);
    el.setAttribute("data:value", String(node.value));
    el.setAttribute("data:key", morphKey(node._key));
    el.setAttribute(
      "data:leaf",
      String(!(node.children && node.children.length))
    );
    this._attachTooltip(el, node);
    if (Environment.isBrowser()) {
      el.addEventListener("click", () => this._zoomTo(node));
      el.style.cursor = "pointer";
    }
    this._ringsG.add(path);
    return path;
  }
  /**
   * @param {any} node
   * @param {{a0:number,a1:number,iR:number,oR:number}} from
   * @param {{a0:number,a1:number,iR:number,oR:number}} to
   * @param {number} dur
   * @param {boolean} hide    shrink + fade out, then hide
   * @param {boolean} fadeIn  fade 0 -> 1 (new arcs only; morphs stay opaque)
   * @param {number} gen  layout generation; frames stop once superseded
   */
  _animateArc(node, from, to, dur, hide, fadeIn2, gen) {
    const el = node._el;
    const br = this.cfg.borderRadius;
    el.attr({ fill: node._color });
    if (dur === 0) {
      el.attr({ d: this._arcPath(to.iR, to.oR, to.a0, to.a1, br), opacity: hide ? 0 : 1 });
      el.node.style.display = hide ? "none" : "";
      node._cur = hide ? null : to;
      return;
    }
    el.node.style.display = "";
    const startOp = hide ? Number(el.attr("opacity")) || 1 : fadeIn2 ? 0 : 1;
    const endOp = hide ? 0 : 1;
    el.attr({ opacity: startOp });
    el.animate(dur).during((pos) => {
      if (this._zoomGen !== gen) return;
      const a0 = lerp(from.a0, to.a0, pos);
      const a1 = lerp(from.a1, to.a1, pos);
      const iR = lerp(from.iR, to.iR, pos);
      const oR = lerp(from.oR, to.oR, pos);
      el.attr({
        d: this._arcPath(iR, oR, a0, a1, br),
        opacity: lerp(startOp, endOp, pos)
      });
      node._cur = { a0, a1, iR, oR };
    }).after(() => {
      if (this._zoomGen !== gen) return;
      if (hide) {
        el.node.style.display = "none";
        node._cur = null;
      } else {
        node._cur = to;
      }
    });
  }
  // ---------------------------------------------------------------- labels
  /**
   * Labels are overlays on animated paths, so they reveal gradually AFTER the
   * arcs settle (repo convention: overlays never pop in over a moving path).
   * @param {number} dur  arc animation duration (0 = instant labels)
   */
  _renderLabels(dur) {
    const labelsG = this._labelsG;
    while (labelsG.node.firstChild) labelsG.node.removeChild(labelsG.node.firstChild);
    if (!this.cfg.dataLabels.show) return;
    this._nodesAll.forEach((node) => {
      if (!node._show) return;
      if (node._a1 - node._a0 < this.cfg.dataLabels.minAngleToShow) return;
      this._renderCurvedLabel(node);
    });
    if (dur > 0) {
      labelsG.attr({ opacity: 0 });
      labelsG.animate(250, dur).attr({ opacity: 1 });
    } else {
      labelsG.attr({ opacity: 1 });
    }
  }
  /**
   * Curved label along the arc's mid-radius (flipped on the bottom half so it
   * stays upright). Raw SVG <textPath> — svg.js has no first-class textPath.
   * @param {any} node
   */
  _renderCurvedLabel(node) {
    if (!Environment.isBrowser()) return;
    const style = this.cfg.dataLabels.style;
    const w2 = this.w;
    const r2 = (node._iR + node._oR) / 2;
    const mid = (node._a0 + node._a1) / 2;
    const flip = mid > 90 && mid < 270;
    const padDeg = Math.min(r2 > 0 ? 4 / r2 * R2D : 0, (node._a1 - node._a0) / 2);
    const from = flip ? node._a1 - padDeg : node._a0 + padDeg;
    const to = flip ? node._a0 + padDeg : node._a1 - padDeg;
    const p1 = this._ptAt(r2, from);
    const p2 = this._ptAt(r2, to);
    const largeArc = Math.abs(to - from) > 180 ? 1 : 0;
    const sweep = flip ? 0 : 1;
    const id = `apx-sb-lbl-${w2.globals.cuid}-${this._lblSeq++}`;
    const guide = BrowserAPIs.createElementNS(SVGNS, "path");
    guide.setAttribute("id", id);
    guide.setAttribute(
      "d",
      `M ${p1.x} ${p1.y} A ${r2} ${r2} 0 ${largeArc} ${sweep} ${p2.x} ${p2.y}`
    );
    guide.setAttribute("fill", "none");
    guide.setAttribute("stroke", "none");
    const colors = style.colors;
    const fill = (Array.isArray(colors) ? colors[0] : colors) || "#fff";
    const text = BrowserAPIs.createElementNS(SVGNS, "text");
    text.setAttribute("font-size", style.fontSize || "12px");
    if (style.fontFamily) text.setAttribute("font-family", style.fontFamily);
    text.setAttribute("font-weight", String(style.fontWeight || 400));
    text.setAttribute("fill", fill);
    text.setAttribute("dominant-baseline", "central");
    text.style.pointerEvents = "none";
    const tp = BrowserAPIs.createElementNS(SVGNS, "textPath");
    tp.setAttribute("href", "#" + id);
    tp.setAttributeNS("http://www.w3.org/1999/xlink", "xlink:href", "#" + id);
    tp.setAttribute("startOffset", "50%");
    tp.setAttribute("text-anchor", "middle");
    tp.textContent = this._truncate(node.name, r2, node._a1 - node._a0, style.fontSize);
    text.appendChild(tp);
    this._labelsG.node.appendChild(guide);
    this._labelsG.node.appendChild(text);
  }
  /**
   * Trim a label to the arc length available at its radius.
   * @param {string} name
   * @param {number} r
   * @param {number} spanDeg
   * @param {string} fontSize
   * @returns {string}
   */
  _truncate(name2, r2, spanDeg, fontSize) {
    const arcLen = r2 * spanDeg * D2R - 8;
    const charW = (parseFloat(fontSize) || 12) * 0.58;
    const maxChars = Math.floor(arcLen / charW);
    if (maxChars >= name2.length) return name2;
    if (maxChars <= 1) return "";
    return name2.slice(0, Math.max(1, maxChars - 1)) + "…";
  }
  // ----------------------------------------------------------------- zoom
  /**
   * Focus a node (zoom in), or zoom out one level when the current focus (the
   * innermost ring) is clicked.
   * @param {any} node
   */
  _zoomTo(node) {
    if (this.cfg.zoomOnClick === false) return;
    const nextFocus = node === this._focus ? node._parent || null : node;
    if (nextFocus === this._focus) return;
    this._focus = nextFocus && !(nextFocus.children && nextFocus.children.length) ? nextFocus._parent || null : nextFocus;
    this._relayout(this._focus);
    this._applyLayout("zoom");
    this._renderBreadcrumb();
  }
  /** Root -> focus chain of nodes. */
  _focusChain() {
    const chain = [];
    let n2 = this._focus;
    while (n2) {
      chain.unshift(n2);
      n2 = n2._parent;
    }
    return chain;
  }
  /**
   * Minimal self-contained breadcrumb (reuses the shared `.apexcharts-breadcrumb`
   * CSS classes, but does NOT depend on the drilldown feature).
   */
  _renderBreadcrumb() {
    if (!Environment.isBrowser()) return;
    const w2 = this.w;
    const elWrap = w2.dom.elWrap;
    if (!elWrap) return;
    const existing = elWrap.querySelector(".apexcharts-breadcrumb");
    if (existing && existing.parentNode) existing.parentNode.removeChild(existing);
    if (!this._focus) return;
    const nav = BrowserAPIs.createElementNS(XHTML, "nav");
    nav.setAttribute("class", "apexcharts-breadcrumb");
    nav.setAttribute("aria-label", "Sunburst breadcrumb");
    nav.style.position = "absolute";
    nav.style.top = "0px";
    nav.style.left = "0px";
    const crumbs = [{ name: "All", node: null }].concat(
      this._focusChain().map((n2) => ({ name: n2.name, node: n2 }))
    );
    crumbs.forEach((crumb, i2) => {
      if (i2 > 0) {
        const sep = BrowserAPIs.createElementNS(XHTML, "span");
        sep.setAttribute("class", "apexcharts-breadcrumb-separator");
        sep.textContent = " / ";
        nav.appendChild(sep);
      }
      const isCurrent = i2 === crumbs.length - 1;
      if (isCurrent) {
        const cur = BrowserAPIs.createElementNS(XHTML, "span");
        cur.setAttribute(
          "class",
          "apexcharts-breadcrumb-item apexcharts-breadcrumb-current"
        );
        cur.textContent = crumb.name;
        nav.appendChild(cur);
      } else {
        const btn = BrowserAPIs.createElementNS(XHTML, "button");
        btn.setAttribute("type", "button");
        btn.setAttribute("class", "apexcharts-breadcrumb-item");
        if (i2 === 0) {
          const arrow = BrowserAPIs.createElementNS(XHTML, "span");
          arrow.setAttribute("class", "apexcharts-breadcrumb-arrow");
          arrow.textContent = "←";
          btn.appendChild(arrow);
        }
        const text = BrowserAPIs.createElementNS(XHTML, "span");
        text.setAttribute("class", "apexcharts-breadcrumb-label");
        text.textContent = crumb.name;
        btn.appendChild(text);
        btn.addEventListener("click", () => {
          this._focus = crumb.node;
          this._relayout(this._focus);
          this._applyLayout("zoom");
          this._renderBreadcrumb();
        });
        nav.appendChild(btn);
      }
    });
    elWrap.appendChild(nav);
    this._avoidChromeOverlap(nav);
  }
  /**
   * The breadcrumb is an absolute overlay at top-left, so it can sit on top of
   * a left-aligned title (or subtitle). After mounting, push it below any chart
   * chrome it intersects. (Duplicated from drilldown's Breadcrumb on purpose —
   * sunburst must not import the drilldown feature.)
   * @param {any} nav
   */
  /**
   * Shared with the treemap - see charts/common/Breadcrumb.
   * @param {any} nav
   */
  _avoidChromeOverlap(nav) {
    avoidChromeOverlap(this.w, nav);
  }
  // ------------------------------------------------------------ geometry
  /**
   * @param {number} r
   * @param {number} deg  0 = top, clockwise
   * @returns {{x: number, y: number}}
   */
  _ptAt(r2, deg) {
    return {
      x: this.centerX + r2 * Math.cos((deg - 90) * D2R),
      y: this.centerY + r2 * Math.sin((deg - 90) * D2R)
    };
  }
  /**
   * Rounded donut-segment path (inner radius always > 0). Applies `spacing`
   * (angular gap) and `borderRadius` (corner rounding), both clamped so a thin
   * arc never inverts.
   * @param {number} iR
   * @param {number} oR
   * @param {number} a0
   * @param {number} a1
   * @param {number} borderRadius
   * @returns {string}
   */
  _arcPath(iR, oR, a0, a1, borderRadius) {
    if (oR <= iR + 0.01) return "";
    let spanDeg = a1 - a0;
    const spacing = this.cfg.spacing;
    if (spacing > 0 && spanDeg > 0 && oR > 0) {
      const gapDeg = spacing / oR * R2D;
      const inset = Math.min(gapDeg / 2, Math.max(0, spanDeg / 2 - 0.25));
      a0 += inset;
      a1 -= inset;
      spanDeg = a1 - a0;
    }
    if (spanDeg <= 0) return "";
    const spanRad = spanDeg * D2R;
    const cx = this.centerX;
    const cy = this.centerY;
    let r2 = borderRadius;
    r2 = Math.min(r2, spanRad * iR / 2, spanRad * oR / 2, (oR - iR) / 2);
    if (!(r2 > 0.5)) {
      return sharpDonutSegmentPath({ cx, cy, rIn: iR, rOut: oR, a0, a1, spanDeg });
    }
    return roundedDonutSegmentPath({ cx, cy, rIn: iR, rOut: oR, a0, a1, r: r2, spanDeg });
  }
  // ------------------------------------------------------------- tooltip
  /** @returns {any} */
  _tip() {
    if (!this._tooltipEl) {
      this._tooltipEl = this.w.dom.baseEl.querySelector(".apexcharts-tooltip");
    }
    return this._tooltipEl;
  }
  /**
   * @param {any} el
   * @param {any} node
   */
  _attachTooltip(el, node) {
    if (!this.w.config.tooltip.enabled || !Environment.isBrowser()) return;
    el.addEventListener(
      "mouseenter",
      (e2) => this._showTooltip(e2, node)
    );
    el.addEventListener(
      "mousemove",
      (e2) => this._positionTooltip(e2)
    );
    el.addEventListener("mouseleave", () => this._hideTooltip());
  }
  /**
   * @param {MouseEvent} e
   * @param {any} node
   */
  _showTooltip(e2, node) {
    const t2 = this._tip();
    if (!t2) return;
    const w2 = this.w;
    const pctTotal = (node.value / this.total * 100).toFixed(1);
    const parentVal = node._parent ? node._parent.value : this.total;
    const pctParent = parentVal > 0 ? (node.value / parentVal * 100).toFixed(1) : pctTotal;
    const groupBg = w2.config.tooltip.fillSeriesColor ? `background-color:${node._color};` : "";
    t2.innerHTML = `<div class="apexcharts-tooltip-series-group apexcharts-active" style="display:flex;${groupBg}"><span class="apexcharts-tooltip-marker" style="background-color:${node._color}"></span><div class="apexcharts-tooltip-text"><div class="apexcharts-tooltip-y-group"><span class="apexcharts-tooltip-text-y-label">${node.name}: </span><span class="apexcharts-tooltip-text-y-value">${node.value} (${pctParent}% of parent, ${pctTotal}% of total)</span></div></div></div>`;
    t2.classList.add("apexcharts-active");
    t2.style.opacity = "1";
    this._positionTooltip(e2);
  }
  /**
   * Position beside the cursor, flipping to the opposite side when the box
   * would overflow the chart wrap, and clamping inside it either way.
   * @param {MouseEvent} e
   */
  _positionTooltip(e2) {
    const t2 = this._tip();
    if (!t2) return;
    const rect = this.w.dom.elWrap.getBoundingClientRect();
    const tw = t2.offsetWidth;
    const th = t2.offsetHeight;
    const pad = 12;
    let x = e2.clientX - rect.left + pad;
    if (x + tw > rect.width) x = e2.clientX - rect.left - tw - pad;
    x = Math.max(0, Math.min(x, rect.width - tw));
    let y = e2.clientY - rect.top + pad;
    if (y + th > rect.height) y = e2.clientY - rect.top - th - pad;
    y = Math.max(0, Math.min(y, rect.height - th));
    t2.style.left = x + "px";
    t2.style.top = y + "px";
  }
  _hideTooltip() {
    const t2 = this._tip();
    if (!t2) return;
    t2.classList.remove("apexcharts-active");
    t2.style.opacity = "0";
  }
  // --------------------------------------------------------------- utils
  /**
   * @param {string|number} size
   * @param {number} max
   * @returns {number}
   */
  _parseSize(size, max) {
    if (typeof size === "number") return size;
    const s2 = String(size).trim();
    if (s2.endsWith("%")) return parseFloat(s2) / 100 * max;
    const n2 = parseFloat(s2);
    return isNaN(n2) ? 0.15 * max : n2;
  }
  /**
   * Blend a hex colour toward white by `amount` (0..1). Non-hex returned as-is.
   * @param {string} color
   * @param {number} amount
   * @returns {string}
   */
  _lighten(color, amount) {
    if (typeof color !== "string" || color[0] !== "#") return color;
    let hex = color.slice(1);
    if (hex.length === 3) {
      hex = hex.split("").map((c2) => c2 + c2).join("");
    }
    if (hex.length !== 6) return color;
    const num = parseInt(hex, 16);
    if (isNaN(num)) return color;
    let rC = num >> 16 & 255;
    let gC = num >> 8 & 255;
    let bC = num & 255;
    rC = Math.round(rC + (255 - rC) * amount);
    gC = Math.round(gC + (255 - gC) * amount);
    bC = Math.round(bC + (255 - bC) * amount);
    return "#" + ((1 << 24) + (rC << 16) + (gC << 8) + bC).toString(16).slice(1);
  }
}
ApexCharts.use({
  line: Line,
  area: Line,
  scatter: Line,
  bubble: Line,
  rangeArea: Line,
  bar: Bar,
  column: Bar,
  barStacked: BarStacked,
  rangeBar: RangeBar,
  candlestick: BoxCandleStick,
  boxPlot: BoxCandleStick,
  violin: Violin,
  pie: Pie,
  donut: Pie,
  polarArea: Pie,
  radialBar: Radial,
  radar: Radar,
  heatmap: HeatMap,
  treemap: TreemapChart,
  unit: Unit,
  sunburst: SunburstChart
});
export {
  ApexCharts as default
};
